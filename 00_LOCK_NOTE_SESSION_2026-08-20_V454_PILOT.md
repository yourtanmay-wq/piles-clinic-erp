# V454 LOCK NOTE — 20.08.2026 (PILOT — শুধু একটা টেস্ট ফোনে বসান)

## ⚠️ এই ZIP-টা বাকি ১২ ফোনে বসাবেন না

এটা একটা **পরীক্ষামূলক (pilot)** বিল্ড — শুধু Doctor Queue (CHECK-UP)
স্ক্রিনে "শুধু বদলানো অংশটুকু নামুক" পদ্ধতি প্রথমবার চালু হয়েছে।

**যা করবেন:**
1. এই APK শুধু **একটা টেস্ট ফোনে** বসান (যেটাতে ভুল হলেও ক্ষতি কম)
2. **Netlify-তে ওয়েব ডিপ্লয় করবেন না** এখন — শুধু APK, যাতে বাকি ফোনে
   ভুল করে "আপডেট আছে" ব্যানার না দেখায়
3. কয়েকদিন CHECK-UP স্ক্রিন স্বাভাবিকভাবে ব্যবহার করুন
4. খেয়াল রাখুন: কোনো রোগী দেরিতে/ভুলভাবে দেখাচ্ছে কিনা, চেকআপ হয়ে যাওয়া
   রোগী তালিকা থেকে ঠিকমতো সরে যাচ্ছে কিনা
5. ঠিক থাকলে TK-কে জানান — তবেই বাকি ফোনে যাওয়ার সিদ্ধান্ত হবে

## এই ডেলিভারিতে কী বদলেছে

- `DoctorQueueRepository.kt` — নতুন `fetchQueueDelta()` (পুরনো
  `fetchQueue()` অক্ষত)
- `DoctorQueueActivity.kt` — শুধু ৩০-সেকেন্ডের auto-refresh পথে নতুন
  ফাংশন ব্যবহার
- Version: 454 / 4.54

## যা কখনো ছোঁয়া হয়নি

Payment, Follow-up, Enquiry, Registration, Chamber Attendance, Money —
কিছুই এই পাইলট ছোঁয়নি। V453-এর সব কাজ (ModuleAuth fix, Password Center,
KISHAN6, Dr. Mandal, JPE-CRP cross-branch) অক্ষত আছে।

## সততার সীমাবদ্ধতা

- লাইভ ডিভাইসে টেস্ট করা হয়নি — শুধু কোড-স্তরের syntax/guard যাচাই
- সত্যিকারের DELETE (আপডেট না হয়ে সরাসরি মোছা) delta-তে তাৎক্ষণিক ধরা
  পড়বে না — ২ ঘণ্টার নিয়মিত পূর্ণ-fetch স্বয়ংক্রিয়ভাবে ঠিক করে দেবে
