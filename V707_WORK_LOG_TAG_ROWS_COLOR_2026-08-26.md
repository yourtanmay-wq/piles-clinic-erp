# V707 — Follow-up কার্ডের ট্যাগ: দুই সারি + দ্বিতীয় সারি বেগুনি
**তারিখ:** ২৬.০৮.২০২৬ · **ভার্সন:** V707 / 7.07

## TK কী বলেছেন
> "BAPPA এনার tag উপর নিচে কেন" (ছবি — চারটে ট্যাগ চারটে আলাদা লাইনে)
> "ব্রাঞ্চ নাম+রোগের নাম পাশাপাশি থাকতে হবে"
> "রোগির ঠিকানা + Unexpected যদি হয় তাহলে পাশাপাশি থাকতে হবে"
> "রোগীর ঠিকানা + Unexpected অন্য কালার হবে"
> "ব্রাঞ্চ+রোগের নাম একই কালার হবে · ঠিকানা+Unexpected+RMP অন্য কালার হবে"
> রং বাছাই: TK জিজ্ঞাসা করলেন "আপনার কোনটা পছন্দ" → **বেগুনি** সুপারিশ করা হলো,
> কারণ কমলা/হলুদ অ্যাপে সতর্কতার মানে বহন করে (Overdue লাল, ব্যাকডেট/ডুপ্লিকেট
> হলুদ) — ঠিকানা/RMP সতর্কতা নয়, শুধু তথ্য। TK: **"হ্যাঁ"**।

ডেমো প্রুফ: `V707_DEMO_TAG_WIDTH.png` · `V707_DEMO_TAG_ROWS_COLOR.png`

## ✅ আসল কারণ (কোড ধরে যাচাই)
ট্যাগের সারিটা বসত `info`-র ভিতরে। `info` হলো **ডান দিকের ব্যাজ/রিং-এর পাশের
সরু কলাম** (`top` → `info`, weight = 1)। ব্যাজের **নিচের** ফাঁকা জায়গাটা তাই
ট্যাগ ব্যবহার করতেই পারত না —

* কার্ড ১: `KISHANGANJ | PILES` ছোট, তাই এক সারিতে ধরে যেত
* BAPPA: `KISHANGANJ | HYDROCELE` **কয়েক পিক্সেলের জন্য** না কুলিয়ে আলাদা
  লাইনে নামত, তারপর একই কারণে বাকিগুলোও

## কী করা হয়েছে
### ফোন (Android)
| ফাইল | কী |
|---|---|
| `drawable/bg_tag_extra_purple.xml` | **নতুন** — বেগুনি (#6B4FBB), কোণের মাপ bg_tag_branch-এর হুবহু এক |
| `native/FollowUpActivity.kt` | `pill()`-এ **ঐচ্ছিক** রং (ডিফল্ট আগের নীল); ঠিকানা ও Unexpected/RMP বেগুনি; ট্যাগ এখন **দুটো দল**; `layoutTagsInRows` দল বুঝে সাজায়; **Enquiry কার্ডে** ট্যাগের সারি `info` থেকে `main`-এ (পুরো চওড়া) |

দল ১ = ব্রাঞ্চ + রোগ (নীল) · দল ২ = ঠিকানা + Unexpected/RMP (বেগুনি) —
প্রতিটা দল নিজের সারিতে শুরু হয়।

### কম্পিউটার (Web)
| ফাইল | কী |
|---|---|
| `app.js` | ঠিকানার চিপের আগে `anFuBreak` (flex-এর সারি-ভাঙা, উচ্চতা ০), আর চিপে `anFuTagX` |
| `styles.css` | নতুন `.anFuBreak` ও `.anFuTag.anFuTagX{background:#6B4FBB}` — ফোনের হুবহু এক রং |

## 🛡️ যা ইচ্ছাকৃতভাবে ছোঁয়া হয়নি — এবং কেন
* **Visit/Patient কার্ডে ট্যাগের জায়গা বদলানো হয়নি।** ওখানে ডান দিকে
  `ADVANCE HERE` · `TEST HERE` · `PRESCRIPTION` · টাকার রিং **লম্বা করে** বসে;
  ট্যাগ পুরো চওড়ায় নামালে ওগুলোর নিচে চলে যেত, কার্ডের চেহারা বদলে যেত —
  TK এর অনুমোদন দেননি। ওখানে শুধু রং ও দল-ভাগ নতুন।
* **ব্রাঞ্চ ও রোগের ট্যাগ** — রং · মাপ · লেখা · ক্লিক সব আগের মতোই
  (`pill()`-এর ডিফল্ট `bg_tag_branch`)।
* **B184-এর প্রতিশ্রুতি অক্ষত:** কোনো ট্যাগ কখনো "…" দিয়ে কাটে না, কার্ডের
  বাইরে বেরোয় না — একই দলের দুটো ট্যাগ সত্যিই না কুলোলে আগের নিয়মেই নিচের
  লাইনে নামে।
* ঠিকানার ট্যাগে **এক-ট্যাপে সম্পাদনা** (B172) ও ব্রাঞ্চ/রোগে TripleTapEdit —
  সব আগের মতোই।

## যাচাই
| যাচাই | ফল |
|---|---|
| নতুন drawable-এর XML গঠন | ✅ valid |
| `verify_kotlin_compile.py` | ✅ **নতুন ভুল ০** |
| `verify_android_resources.py` | ✅ নতুন drawable রেজলভ হয় (একমাত্র ❌ পুরোনো, মন্তব্যের ভিতরের `etHistoryNote` — V707-এর আগেও ছিল) |
| `app.js` JS syntax | ✅ OK |
| `tk_guard.py` (২২টা মেশিন-যাচাই) | ✅ সব পাশ |

⚠️ Android চালানোর উপায় নেই — TK দেখে বলবেন।
