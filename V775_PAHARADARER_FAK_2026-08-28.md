# 🛡️🔴 V775 — বিল্ড ভাঙল, অথচ পাহারাদার "PASS" বলেছিল (২৮.০৮.২০২৬)

**TK:** *"পাহারাদার কি করে আপনাকে আটকালো না — আগেই তো বলা ছিল Android Studio-তে
Build করার সময় যেন Error না আসে।"*

**TK সম্পূর্ণ ঠিক বলেছেন।** দুটো আলাদা ভুল — একটা কোডে, একটা পাহারাদারে।

---

## ১. কোডের ভুল (আমার)

```
e: DashboardActivity.kt:854 — Unresolved reference: user
```

V771-এ আমি `refreshOldAppBanner()`-এর ভিতরে লিখেছিলাম:

```kotlin
val mob = user?.mobile.orEmpty()      // ❌ এখানে `user` বলে কিছু নেই
```

`user` আসলে **`onCreate()`-এর ভিতরের নিজস্ব ভেরিয়েবল** (line 79) — অন্য
ফাংশনের নয়। আমি line 79-এ ওটা আছে দেখে ধরে নিয়েছিলাম সব জায়গায় পাওয়া যাবে।
**এটাই আন্দাজ, আর এটাই ভুল ছিল।**

**ঠিক করা হলো** — এই ফাইলের আর ৪ জায়গায় যেভাবে নেওয়া হয় ঠিক সেভাবেই:

```kotlin
val mob = NativeSession.current(this)?.mobile.orEmpty()      // ✅
```

---

## ২. 🔴 আসল প্রশ্ন — পাহারাদার কেন ধরল না

`verify_kotlin_compile.py`-র ভিতরে "গোলমাল বাদ দেওয়ার" নিয়ম ছিল:

```python
return name in ext or name in members
```

`members` = প্রকল্পের **যেকোনো** ফাইলে `.নাম` হিসেবে লেখা সব নাম।

আর প্রকল্পে একটা জায়গায় ছিল:

```
data/repository/AuthRepository.kt:43 →  response.user?.email
```

⇒ `user` নামটা `members`-এ ঢুকে গেছে
⇒ DashboardActivity-র **আসল ভুলটা** "বাইরের লাইব্রেরির গোলমাল" ধরে **বাদ**
   পড়ে গেছে
⇒ পাহারাদার **"নতুন ভুল ০ — PASS"** বলেছে।

### সমাধান
**ছোট হাতের অক্ষরে শুরু হওয়া নাম = ভেরিয়েবল/ফাংশন, ক্লাস নয়।**
এদের জন্য `members`-এর ঢালাও ছাড় আর নেই — শুধু সত্যিকারের import করা বাইরের
নাম হলে ছাড়। বাকি সব **সন্দেহভাজন**, আর বেসলাইনে না থাকলে **FAIL**।

⛔ বড় হাতের নাম (ক্লাস) আগের নিয়মেই — androidx/material এই পরিবেশে নামানো
   যায় না, ওই ছাড়টা না রাখলে শত শত মিথ্যা ভুল আসত।

**বেসলাইন নতুন করে লেখা হলো** (ভালো, বিল্ড-হওয়া অবস্থায়): ৩২৭ → **৮৬৮ সারি**।
বাড়তি ৫৪১টা সবই উত্তরাধিকারে পাওয়া Android সদস্য (`onCreate` · `findViewById`
· `resources` · `applicationContext` · `itemView` · `window` …) — TK-এর
Android Studio-তে ওগুলো নিয়ে **একটাও অভিযোগ আসেনি**, শুধু `user` নিয়ে
এসেছিল; এটাই প্রমাণ যে বাকিগুলো সত্যিই গোলমাল।

---

## ৩. ✅ ফাঁদ-পরীক্ষা (প্রমাণ, দাবি নয়)

TK-এর ঠিক ওই ভুলটাই ইচ্ছে করে আবার বসিয়ে চালানো হলো:

| অবস্থা | পাহারাদারের ফল |
|---|---|
| ভুলটা বসানো (`val mob = user?.mobile`) | ❌ **FAIL — `DashboardActivity.kt : unresolved reference: user`** |
| ভুলটা সরানো | ✅ PASS — নতুন ভুল ০ |

⇒ এই ধরনের ভুল **আর কখনো** পার হতে পারবে না।

---

## ৪. শিক্ষা (খাতায় স্থায়ীভাবে)

> **"এক ফাংশনে থাকা ভেরিয়েবল অন্য ফাংশনে পাওয়া যায় না — লাইন নম্বর দেখে
> ধরে নেওয়া চলবে না, ঠিক ওই ফাংশনের ভিতরে আছে কি না দেখতে হবে।"**

আর পাহারাদারের জন্য:

> **"গোলমাল বাদ দেওয়ার নিয়ম যত ঢিলে, পাহারাদার তত অন্ধ।"**
> ছাড় দিতে হলে কারণ দেখিয়ে দিতে হবে — "নামটা অন্য কোথাও আছে" যথেষ্ট নয়।

---

## ৫. যাচাই

| পরীক্ষা | ফল |
|---|---|
| `verify_kotlin_compile.py` (কড়া নতুন নিয়মে) | ✅ নতুন ভুল **০** |
| ফাঁদ-পরীক্ষা — TK-এর আসল ভুল | ✅ **ধরা পড়ল** |
| `tk_guard.py` (৩৪টা) | ✅ পাশ |
| `verify_android_resources.py` | ✅ পাশ |
