# V405 — ডেটা খরচ (Egress) কমানোর বড় দফা
**তারিখ:** 16.08.2026 · **ভার্সন:** V405 / 4.05 · **ওয়েব cache:** `?v=v427`

## TK-এর অনুমতি
> মাঝারি ১০টা — **"সবকটাই ধরুন"** · `cloudPush()` — **"হ্যাঁ, এটাও ধরুন"** · Pro Plan — **"না, ফ্রিতেই চালান"**

**যেখান থেকে শুরু:** Egress **২.৩১ / ৫ GB (৪৬%)**, সাইকেল ১৩ সেপ্টেম্বর পর্যন্ত।
টিকতে হলে দিনে **০.১০ GB**-এর নিচে নামতে হবে; তখনকার হার **~০.২৮ GB/দিন**।

---

## 🔴 সবচেয়ে বড় কাজ — `cloudPush()` আর পুরো টেবিল পড়ে না

**সমস্যা:** ক্লাউডে লেখার **আগে পুরো টেবিল পড়া হত** (`select(...).limit(2000)`)।
একজন রোগী সেভ করলেই ২০০০ পর্যন্ত সারি নামত — **প্রতিবার**।

**কেন ওই পড়াটা ছিল (V223 §C1):** পুরনো/ব্যাকআপ ডেটা দিয়ে যেন ক্লাউডের নতুন
তথ্য চাপা না পড়ে। **এই সুরক্ষা এক চুলও কমানো হয়নি** — শুধু বোঝা গেল, *যে
সারিগুলো লিখব সেগুলোর* ক্লাউড-রূপ জানলেই সুরক্ষা পুরোপুরি বজায় থাকে; যেসব সারিতে
হাতই পড়ছে না, তাদের পড়ার কোনো দরকার নেই।

**সমাধান — "কোন সারি বদলেছে" তার খাতা:**
প্রজেক্টের **প্রতিটি সেভ একটাই `save()`-এর ভিতর দিয়ে যায়** — তাই ওখানেই ধরা হয়
কোন `id` নতুন/বদলানো (`rk_dirty_<টেবিল>`)। `cloudPush` তখন শুধু সেগুলোর
ক্লাউড-রূপ পড়ে (`.in('id',…)`, ২০০ করে ভাগে) ও সেগুলোই পাঠায়।

**⛔ ৬টা নিরাপত্তা-জাল (একটাও বাদ যায়নি):**

| অবস্থা | কী হয় |
|---|---|
| খাতা নেই / ফাঁকা / গোলমেলে | **পুরো-টেবিল পথ** |
| ৪০০-র বেশি সারি বদলেছে | **পুরো-টেবিল পথ** (তখন ওটাই সস্তা) |
| কোনো dirty id স্থানীয় তালিকায় নেই | **পুরো-টেবিল পথ** |
| রাত ২টা · "Sync Now" · বুট/লগইন · লগ-আউট | **সবসময় পুরো-টেবিল** |
| ক্লাউড পড়া ব্যর্থ | **push বাতিল** (V223 §C1 অটুট), খাতা রয়ে যায় |
| Patient ID সংঘর্ষের পুনরায়-চেষ্টা (B447) | **ইচ্ছে করে পুরো-টেবিল** |

**🔴 সবচেয়ে বিপজ্জনক ফাঁদটা ধরা পড়েছে ও বন্ধ করা হয়েছে:**
`save(t, …)` **পুরো টেবিলটাই বদলে দেয়**। delta পথে `merged`-এ থাকে শুধু পাঠানো
কয়েকটা সারি — সরাসরি save করলে **বাকি ৩২৭২টা সারি স্থানীয়ভাবে মুছে যেত**।
তাই delta পথে merged আগে গোটা স্থানীয় তালিকার উপরে বসানো হয় (`mergeById`),
তবেই save। পরীক্ষায় প্রমাণিত — **৩২৭৩ → ৩২৭৩, একটাও হারায়নি।**

### যাচাই — Chromium-এ আসল কোড, ৩২৭৩ সারির নকল ক্লাউড

| পরীক্ষা | সারি পড়া | সারি লেখা | ফল |
|---|---|---|---|
| খাতা ফাঁকা → পুরো টেবিল | ২০০০ | ৩২৭৩ | আগের আচরণ ✅ |
| **১ জন এডিট → delta** | **১** | **১** | ✅ |
| ৩ জন এডিট → delta | ৩ | ৩ | ✅ |
| `full:true` ("Sync Now") | ২০০০ | ৩২৭৩ | জাল অটুট ✅ |
| ৪৫০ বদল (>৪০০) | ২০০০ | ৩২৭৩ | নিজেই পুরোয় ফেরে ✅ |
| ক্লাউড পড়া ব্যর্থ | — | — | push বাতিল, খাতা রয়ে গেছে ✅ |

**তথ্য-সুরক্ষা:** স্থানীয় সারি **৩২৭৩ → ৩২৭৩** · এডিট টিকে আছে ·
**ক্লাউডেও পৌঁছেছে** · অন্য সারি অটুট · খাতা সফল হলে তবেই পরিষ্কার ·
পেজ-এরর **কিছু নেই**।

---

## বাকি যেগুলো সারানো হলো

| # | কী ছিল | কী হলো |
|---|---|---|
| ১ | `refreshBriefingsFromCloud` — ২ টেবিল × ১০০০ সারি, **প্রতিবার হোমে ফেরা ও Briefing খোলায়, কোনো বিরতি ছাড়া** | ৫ মিনিটের বিরতি। সফল পড়ার সময়ই কেবল ধরা হয় |
| ২ | `BriefingReminderWorker` — অপঠিত নোটিশ থাকলে **প্রতি ১০ মিনিটে পুরো টেবিল, রাতেও** | রাত ১০টা–সকাল ৬টা ঘুমায় (B662-এর একই নিয়ম), চেইন ভাঙে না |
| ৩ | ঘণ্টার গোনায় নোটিশের **পুরো লেখা ও সব উত্তর** নামত | নতুন `fetchRawForCount()` — গোনায় সত্যিই যে ৯টা ঘর লাগে শুধু সেগুলো। **আলাদা cache-চাবি**, তাই পর্দার লেখা ফাঁকা হয় না |
| ৪ | `SupabaseClient` — সরু পড়া ব্যর্থ হলেই **সব ঘর** (= রোগীর base64 ছবি), ~১৯ জায়গায় | টেবিল একবার প্রমাণিত হলে ব্যর্থতা = নেটের সমস্যা ⇒ **একই সরু অনুরোধ আরেকবার**; তাও ব্যর্থ হলে তবেই পুরো-কলাম |
| ৫ | `notebook.js` — `work_reports.select('*')`, **কোনো limit নেই**, রিপোর্টের পুরো লেখাসহ | পর্দায় ব্যবহৃত ৬টা ঘর + সাম্প্রতিক ১০০টা |
| ৬ | `pullUserPasswordsFromCloud` — **প্রতিবার Login চাপায়** ৫০০ সারি | ১০ মিনিটের বিরতি |
| ৭ | Export Data — একবার চাপলেই **প্রায় পুরো ডেটাবেস** (limit 100000, ছবিসহ), কোনো প্রশ্ন ছাড়া | চাপার আগে পরিষ্কার সতর্কবার্তা ও confirm। **Export-এর কাজ এক অক্ষরও বদলায়নি** |
| ৮ | **Settings → "Backup Now"** — একটা চাপে **১১টা পূর্ণ টেবিল-পড়া** (৭টা JSON + ৪টা CSV), সব `select=*` ছবিসহ, limit 100000 | একই ধরনের সতর্কবার্তা ও confirm। ব্যাকআপের কাজ অপরিবর্তিত |

### 🔴 B446 যেন আবার না ফেরে
৪ নম্বরটা ছুঁতে গিয়ে খাতায় দেখলাম — এই জায়গাতেই আগে একটা **আসল বাগ** ছিল
(TK ছবি দিয়ে ধরিয়েছিলেন: Collection Summary ₹0, দুই ফোনে দুই রকম তালিকা)।
তাই "প্রমাণিত হলে আর চেষ্টা কোরো না" শর্টকাটটা **ফিরিয়ে আনিনি** — পুরো-কলাম
চেষ্টা আগের মতোই শেষ ধাপে আছে, শুধু তার আগে একটা সস্তা সরু চেষ্টা যোগ হলো।

---

## ⛔ যা ইচ্ছে করে **করিনি** (সৎভাবে জানাচ্ছি)

| কী | কেন |
|---|---|
| খাতার তালিকার `GlobalSearchActivity` · `PatientTimelineRepository` (মূল পড়া) · `RefundedRecords` · `ClinicalCloudRepository` | **কোডে মিলিয়ে দেখলাম — এগুলো আগেই সারানো আছে** (বা ফাইলটাই নেই)। খাতার তালিকাটা পুরনো ছিল |
| ওয়েবে "Sync Now"-এ ছবি-সহ পড়া | এটাই ওয়েবে তাজা ছবি পাওয়ার **একমাত্র পথ**। হাতে চাপতে হয়, তাই বিরল — খরচে প্রভাব সামান্য, কিন্তু বন্ধ করলে ছবি আটকে যেত |
| `PatientTimelineRepository`-র `medical` সরু করা | ওই সারির কোন কোন ঘর পড়া হয় তা **নিশ্চিত হতে পারিনি**। ছাঁকনি একজন রোগীতেই সীমিত (হাতে গোনা সারি), তাই লাভ সামান্য — **অনিশ্চিত অবস্থায় চিকিৎসার Timeline ভাঙার ঝুঁকি নিইনি**। TK চাইলে পরে মেপে করা যাবে |

---

---

## 🔴 TK-এর "আগে যাচাই করুন" — যা বেরোল

TK বললেন দাবিগুলো আগে প্রমাণ করতে। **তাঁর পাঠানো আসল V401 জিপ আলাদা করে খুলে**
প্রতিটা দাবি লাইন ধরে মিলিয়ে দেখা হলো — এবং **আমার নিজের একটা ফাঁক ধরা পড়ল**।

| দাবি | সত্যি? | প্রমাণ |
|---|---|---|
| Export একবার চাপলেই ~৮০ MB | ✅ | `native/ExportDataActivity.kt:72` — `fetchList(table, limit = 100000)` |
| 🔴 **Settings → Backup Now** | ✅ **আমি আগে মিস করেছিলাম** | `security/SettingsActivity.kt:187` (৪ টেবিল) + `:210` (৭ টেবিল) — **আমি `native/` ফোল্ডারে খুঁজেছিলাম, ফাইলটা `security/`-তে** |
| নেট দুর্বল হলে ছবি-সহ পুরো সারি | ✅ | `native/SupabaseClient.kt:175,183` — ব্যর্থ হলেই `select=*`। খাতায় "~১৯ জায়গা" লেখা, **আসলে ৬৩টা** ব্যবহার |
| নোটিশের ওয়ার্কার রাতেও ১০ মিনিটে | ✅ | `BriefingReminderScheduler.kt:23` = 10L · Worker-এ রাতের শর্ত **০টি** · `BriefingRepository.kt:45` = `select=*`, 5000 |
| refreshBriefingsFromCloud-এ বিরতি নেই | ✅ | `app.js:2250` — ২ টেবিল × ১০০০, কোনো throttle নেই |
| notebook work_reports-এ limit নেই | ✅ | `notebook.js:510` — `select('*')`, limit অনুপস্থিত |
| প্রতিবার লগইনে পাসওয়ার্ড-তালিকা | ✅ | `app.js:1791` — `login()`-এর প্রথম লাইনেই |
| cloudPush পুরো টেবিল পড়ে | ✅ | `app.js:853` — `.select(__ph||'*').limit(2000)` |
| GlobalSearch ২×২০০০ পুরো | ❌ **আগেই সারানো** | `:122,:126` — `fetchListSlim` সরু কলামে |
| RefundedRecords ২×৫০০০ পুরো | ❌ **আগেই সারানো** | `:72,:102` — `fetchListSlimOrNull` সরু কলামে |
| ClinicalCloudRepository | ❌ **ফাইলটাই নেই** | খাতার এন্ট্রি পুরনো |

**⇒ ৮টা দাবি সত্যি, ৩টা ভুল/পুরনো। আর Backup Now-টা আমার নিজের ফাঁক ছিল —
TK যাচাই করতে না বললে ওটা রয়ে যেত।** এখন সারানো হয়েছে।

**⛔ auto-backup (`CloudBackup.kt:35`, limit 100000) ইচ্ছে করে ছোঁয়া হয়নি** —
যাচাই করে দেখা গেল ওটা আগেই master-only + ৭ দিনে একবার (B624), হাতে চাপার
ব্যাপার নেই।

**⛔ Restore বোতাম ভাঙার একটা ভুল আমি নিজেই করে ফেলেছিলাম** — Backup-এর কোড
আলাদা ফাংশনে সরাতে গিয়ে Restore-এর অংশটা এমন এক ফাংশনে চলে গিয়েছিল যেটা কেউ
ডাকত না। ধরা পড়েছে ও ঠিক করা হয়েছে (`setupRestoreButton(btnRestore)` এখন
ডাকা হচ্ছে — লাইন ১৫৩)।

---

## আনুমানিক সাশ্রয় (হিসাব করে, আন্দাজে নয়)

| কাজ | আগে | পরে |
|---|---|---|
| cloudPush (দিনে ~১২০ সেভ ধরে) | ~২৮১ MB/দিন | **~০.৩ MB/দিন** |
| নোটিশের রিমাইন্ডার (১৩ ফোন) | ~৮৩৪ MB/দিন | ~৬৫ MB/দিন |
| লগইনের পাসওয়ার্ড-তালিকা | ~২.৯ MB/দিন | ~০.৬ MB/দিন |

⚠️ এগুলো **হিসাবের অনুমান**, প্রমাণ নয় — আসল প্রমাণ TK-এর Supabase সংখ্যায়।

---

## যাচাই
- ৮টা `.js`-এ `node --check` পাশ · ওয়েবের সব পরিবর্তন Chromium-এ **আসল কোড চালিয়ে** মাপা
- ১০টা বদলানো `.kt`-এ ব্রেস/বন্ধনী/বর্গবন্ধনী ভারসাম্য **০**
- ডাকা প্রতিটি নাম আছে ও স্বাক্ষর মেলে (`fetchRawForCount` · `PremiumAlert.header` · `CloudReadCache.get` · `BriefingReminderScheduler.scheduleRepeat`)
- ⚠️ **এখানে Kotlin কম্পাইলার নেই** — চূড়ান্ত প্রমাণ TK-এর Android Studio বিল্ড

## ⚠️ TK-এর করণীয় (ক্রম মেনে)
1. **V405 বিল্ড করে APK প্রতিটা স্টাফকে পাঠানো** ← সবচেয়ে বড় লাভ এখানেই
2. Netlify-তে `03_NETLIFY_READY` আপলোড (cache `?v=v427`)
3. ১-২ দিন পরে Egress-এর স্ক্রিনশট
