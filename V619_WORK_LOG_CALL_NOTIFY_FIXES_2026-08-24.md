# V619 — কল-নোটিফিকেশন: ২টা সত্যিকারের ফাঁক ঠিক
**তারিখ:** ২৪.০৮.২০২৬ · **ভার্সন:** V619 / 6.19

## TK-এর রিপোর্ট (দুটোই)
1. ভুল নম্বর হলে নোটিফিকেশনে "Cancel/Dismiss" বোতাম থাকা উচিত ছিল, ছিল না।
2. এটা Master-এর ফোন, TK কখনো "এই ফোনে চেম্বারের নম্বর আছে" বলেননি —
   তাহলে কেন নোটিফিকেশন দেখাচ্ছে?

## ১) Dismiss বোতাম না থাকা — কারণ ও সমাধান
আগে কল চলাকালীন `setOngoing(true)` দিয়ে নোটিফিকেশন সোয়াইপ করেও সরানো
যেত না, আর কোনো Dismiss বোতামও ছিল না। এখন:
- সবসময় স্বাভাবিকভাবে সোয়াইপ করে সরানো যায় (`setAutoCancel(true)`)।
- নতুন **"✕ Dismiss"** বোতাম — নতুন, ছোট্ট `CallDismissReceiver.kt`
  (শুধু ওই একটা নোটিফিকেশন সরায়, আর কিছু ছোঁয় না)।

## ২) Master-এর ফোনে অনুমতি ছাড়াই চলা — সৎভাবে ধরা পড়া নিজের ভুল
গভীরে গিয়ে দেখা গেল — `CallNotifyManager.allowed()` ভুলবশত
`hasGenuinelyChosenSim()` ব্যবহার করেছিল, যেটা **Dialer-এর কল-লগ
দেখানোর জন্য ইচ্ছাকৃতভাবে নরম** — পুরনো এক-সিম ফোনকে **প্রশ্ন ছাড়াই**
grandfather করে নেয় (TK-এর ফোনের মতো ব্যক্তিগত/মিশ্র-ব্যবহারের ফোনেও)।
এটা Dialer-এর জন্য ঠিক ছিল, কিন্তু আপনার স্পষ্ট নির্দেশ ("শুধু যারা
আগেই হ্যাঁ বলেছে") মানতে গেলে অনেক বেশি নরম ছিল।

**সমাধান:** `BranchSimHelper.kt`-এ নতুন, কড়া ফাংশন
`hasExplicitlyConfirmedChamberSim()` — শুধু দুটো ক্ষেত্রেই true:
সত্যিই একাধিক SIM থেকে হাতে-বাছা, অথবা প্রশ্নে সত্যিই "হ্যাঁ" বলা
(raw উত্তর, কোনো grandfather-ছাড় ছাড়া)। `CallNotifyManager`-এ এটাই
এখন ব্যবহার হচ্ছে।

⛔ **Dialer-এর নিজের আচরণ এক অক্ষরও বদলায়নি** — পুরনো
`hasGenuinelyChosenSim()`/`hasChamberNumber()` অক্ষত, শুধু নতুন একটা
আলাদা, কড়া ফাংশন যোগ হয়েছে যেটা শুধু কল-নোটিফিকেশনেই ব্যবহৃত হয়।

## নিরাপত্তা
- নতুন receiver `exported=false` — শুধু অ্যাপের নিজের ভিতর থেকেই ডাকা যায়।
- কোনো নতুন অনুমতি লাগেনি।

## যাচাই
| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ |
| `verify_version_json.py` | ✅ V619/6.19 |
| AndroidManifest XML ভ্যালিড | ✅ |
| Dialer-এর পুরনো ফাংশন-ব্যবহার অক্ষত | ✅ যাচাই করা হয়েছে |
