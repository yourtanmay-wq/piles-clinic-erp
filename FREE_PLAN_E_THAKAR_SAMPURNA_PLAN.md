# ফ্রি প্ল্যানে স্থায়ীভাবে থাকার সম্পূর্ণ প্ল্যান

**PILES CLINIC ERP · ১৯.০৮.২০২৬ · V439 কোড ধরে ধরে যাচাই করা**

> প্রতিটা কথার পাশে **ফাইল ও লাইন নম্বর** দেওয়া আছে। যেগুলো আমি নিজে কোডে খুলে
> মিলিয়ে দেখেছি সেগুলো **✅ যাচাই করা**, আর যেগুলো হিসাব করে বার করা সেগুলো
> **📐 আন্দাজ** বলে আলাদা করে লেখা।

> **V441 আপডেট · ১৯.০৮.২০২৬ ০৯:১৮ IST অনুমোদন:** এই পুরনো V439 audit-plan-এর
> মধ্যে Background branch fix V440-এ আগেই সম্পন্ন ছিল। V441-এ আরও তিনটি অনুমোদিত
> safe fix সম্পন্ন: (১) non-photo update-এ পুরনো photo পুনরায় cloud-write বন্ধ,
> (২) web `deleted_records` 5000 সীমার বদলে fail-safe pagination, (৩) Draft enquiry
> narrow read। **ModuleAuth/login-session ইচ্ছে করে ছোঁয়া হয়নি।** নিচের মূল plan
> historical audit হিসেবে অপরিবর্তিত রাখা হয়েছে।

> **V442 আপডেট · ১৯.০৮.২০২৬ ০৯:৪৪ IST অনুমোদন:** Android `DeletedGuard`-এর
> 3000 tombstone limit সম্পূর্ণ সরানো হয়েছে। Cloud `deleted_records` এখন 1000-row
> page ধরে সম্পূর্ণ পড়ে; multi-page read চলাকালীন cloud বদলালে fail-safe ভাবে
> কোনো পুরনো marker unmark হয় না। ফোনের local/localOnly cache-ও আর 3000 পেরোলে
> পুরনো delete marker ফেলে দেয় না। **Login/session বা অন্য workflow ছোঁয়া হয়নি।**


---

## ০. প্রথমে — লক্ষ্যটা সংখ্যায়

ফ্রি প্ল্যানে মাসে **৫ GB** ডেটা নামানো যায়। ৩১ দিনে ভাগ করলে:

| | |
|---|---|
| রোজ খরচ করা যাবে | **১৬৪ MB** |
| ১৩টা ফোনে ভাগ করলে, প্রতি ফোনে | **~১২.৬ MB/দিন** |
| আপনার আসল ডেটা সব মিলে | **~৩১ MB** |
| **এখনকার খরচ** | **~৫৫০ MB/দিন** (৩.৪ গুণ বেশি) |

**গুরুত্বপূর্ণ কথা:** আপনার ডেটা বড় নয় — মাত্র ৩১ MB। সমস্যা হলো **একই ডেটা
বারবার নামছে**। প্রতিটা ফোন দিনে গড়ে **প্রায় ২ বার পুরো ডেটাবেস** নামাচ্ছে।

লক্ষ্য: প্রতি ফোন যেন **দিনে সর্বোচ্চ একবারের কম** পুরো তালিকা নামায়।

---

## ১. 🔴 সবচেয়ে বড় ফুটো — ব্যাকগ্রাউন্ড ওয়ার্কার সবার হয়ে কাজ করছে

**ফাইল:** `native/BackgroundRefreshWorker.kt:89-104` · **✅ আমি নিজে দেখেছি**

প্রতিটা ফোনে ঘণ্টায় একবার (`MIN_GAP_MS = 60 মিনিট`, লাইন ২৩৩) একটা ওয়ার্কার চলে।
সেটা আগে একটা **সস্তা প্রশ্ন** করে — "গতবারের পর কিছু বদলেছে?" — এটা খুব ভালো
ব্যবস্থা, কোনো সারি নামে না।

**কিন্তু ভুলটা এখানে:** প্রশ্নটায় **ব্রাঞ্চের নাম নেই**।

```kotlin
SupabaseClient.fetchCount("followups", "updatedAt=gt." + since)   // ⛔ branch নেই
SupabaseClient.fetchCount("payments",  "updatedAt=gt." + since)   // ⛔ branch নেই
SupabaseClient.fetchCount("briefings", "updatedAt=gt." + since)   // ⛔ branch নেই
```

**এর মানে:** কিশানগঞ্জের একজন স্টাফ একটা ফলোআপ লিখলেই — **পাঁচ ব্রাঞ্চের ১৩টা
ফোনই** "নতুন কিছু হয়েছে" ধরে নেয়, আর প্রত্যেকে নিজের ব্রাঞ্চের পুরো
`patients` + `payments` + `followups` আবার নামায়।

পাঁচ ব্রাঞ্চে সারাদিন কাজ চলছে — তাই এই দরজা **প্রায় প্রতিবারই খুলে যায়**।

📐 হিসাব: ১৬ ঘণ্টা × ১৩ ফোন × ~২ MB ≈ **~৪১৬ MB/দিন**।
এটা একাই ৫৫০ MB-র সিংহভাগ।

### সমাধান
প্রশ্ন তিনটেতে ব্রাঞ্চ যোগ করা — মাস্টার ছাড়া সবার জন্য:

```kotlin
val bq = if (user.branch == "All") "" else "&branch=eq." + URLEncoder.encode(user.branch,"UTF-8")
SupabaseClient.fetchCount("followups", "updatedAt=gt.$since$bq")
```

⛔ ঝুঁকি নেই — সস্তা প্রশ্নটা আরও নির্দিষ্ট হচ্ছে মাত্র; ভুল হলে (`-1`) আগের মতোই
পুরো তালিকা নামে, তাই কোনো তথ্য আটকে যাওয়ার সম্ভাবনা নেই।

📐 আশা করা লাভ: **~৩০০ MB/দিন কমা**।

---

## ২. 🔴 লগইনের ফুটো — ১৫ জন ব্যবহারকারী, ১,৮২৬টা সেশন

**ফাইল:** `modules/ModuleAuth.kt:36, 66-69, 155` · **✅ আমি নিজে দেখেছি**

আপনার ডেটাবেসে `refresh_tokens` · `sessions` · `mfa_amr_claims` — তিনটে টেবিলেই
**১,৮২৬টা করে সারি**, অথচ ব্যবহারকারী মাত্র **১৫ জন**। কারণ তিনটে:

| কোডে যা আছে | ফল |
|---|---|
| `@Volatile var accessToken` — **শুধু মেমরিতে**, কোথাও সেভ হয় না (লাইন ৩৬) | অ্যাপ ব্যাকগ্রাউন্ডে গিয়ে বন্ধ হলেই টোকেন হারায় → **আবার নতুন লগইন** |
| `reAuth()` — টোকেনের মেয়াদ শেষ হলে **আবার পাসওয়ার্ড দিয়ে পুরো লগইন** (লাইন ৬৬-৬৯) | ঘণ্টায় একবার করে নতুন সেশন |
| `signOut()` — শুধু মেমরি মোছে, **সার্ভারে logout পাঠায় না** (লাইন ১৫৫) | পুরনো সেশন চিরকাল জমে থাকে |

সঠিক নিয়ম হলো `refresh_token` জমিয়ে রেখে সেটা দিয়ে টোকেন নবায়ন করা — তাতে
নতুন সেশন তৈরি হয় না। কোডে `refresh_token` ধরাই হয় না (লাইন ১৪৩)।

### সমাধান (দুই ভাগে)

**ক) এখনই পরিষ্কার — SQL Editor-এ** (নিচে §৬-এ পুরো SQL আছে)
**খ) কোডে** — টোকেন ফোনে সেভ করা + `grant_type=refresh_token` ব্যবহার +
`signOut()`-এ `POST /auth/v1/logout` ডাকা।

---

## ৩. 🟠 ছবি বারবার নতুন করে লেখা হচ্ছে (আজকের ২২ MB ফোলার কারণ)

**ফাইল:** `03_NETLIFY_READY/app.js:572` (`upd`) → `:3544` → `CLOUD_SAFE_COLS.patients` এ `photo` আছে

ডাক্তারের নোট · বিল · ডিসকাউন্ট · অ্যাপ্রুভাল — এইসব সেভ করলে **পুরো রোগীর সারি**
আবার ক্লাউডে পাঠানো হয়, **ছবিসহ** — যদিও ছবিতে হাত পড়েনি।

Postgres তখন পুরনো ছবিটা "মৃত" করে নতুন কপি লেখে। এটাই আজ `patients`-কে
৬.৬ → ২২ MB বানিয়েছিল, আর `backuprecords`-কে ১০ → ৬৯ MB।

📌 **সৎ কথা:** এটা ওয়েব অ্যাপের কোড, আর আপনি ওয়েব ব্যবহারই করেন না
(realtime connection = ১)। তাই এটা **এখনকার মূল কারণ নয়**, কিন্তু ভবিষ্যতের
ফাঁদ — সারানো উচিত। ফোনের কোডে (`PatientPhotoRepository.kt:54`) শুধু ছবি ও সময়
পাঠানো হয়, ওটা ঠিক আছে ✅।

### সমাধান
`upd()`-এ — patch-এ `photo` না থাকলে ছবির ঘরটা বাদ দিয়ে পাঠানো। ছোট বদল।

---

## ৪. 🟠 যেসব টেবিল কেবল বাড়ে, কখনো কমে না

সবগুলো **✅ কোডে যাচাই করা**:

| টেবিল | কে ঢোকায় | কখনো মোছে? | এখন |
|---|---|---|---|
| `deleted_records` | `native/DeletedGuard.kt:265` — প্রতিটা delete-এ | **না** | ৩৭৬ kB |
| `trash.record` | `native/TrashHelper.kt:84` — মোছা সারির **পুরো কপি, ছবিসহ** | শুধু হাতে | ২.১ MB |
| `wn.call_taps` | `ModuleAuth.kt:438` — প্রতিটা কল-বোতামে | **না** | ছোট |
| `wn.work_reports` | `WorkNotebookActivity.kt:1930` — এডিট করলে **নতুন সারি**, পুরনোটা থাকে | **না** | ছোট |
| `backuprecords` | ওয়েবের auto-backup, দিনে ১ | **না** (payload এখন বাদ ✅) | ৭.৮ MB |

🔴 **একটা আলাদা বিপদ:** `app.js:671` — `deleted_records` পড়া হয় `limit(5000)` দিয়ে।
এই টেবিল ৫০০০ সারি ছাড়ালে **পুরনো "মোছা" সংকেত আর পৌঁছবে না** — তখন এক ফোনে
মোছা রেকর্ড অন্য ফোনে ফিরে আসতে পারে। এটা জায়গার সমস্যা নয়, **তথ্যের ভুলের** সমস্যা।
তাই নিয়মিত পরিষ্কার করাটা জরুরি।

---

## ৫. 🟡 আরও কিছু ছোট ফুটো (কোডে যাচাই করা)

| কোথায় | কী | সমাধান |
|---|---|---|
| `native/DraftRepository.kt:364` | Draft পর্দা খুললেই `enquiries` **সব কলামসহ** (`select=*`), ৫০০০ সারি | সরু কলাম-তালিকা |
| `native/DoctorQueueRepository.kt:122` | হাতে 🔄 রিফ্রেশ চাপলে `includePhoto` **ডিফল্ট true** — সব ছবি নামে | ডিফল্ট false |
| `native/GlobalSearchActivity.kt:122` | সার্চে **পাঁচ ব্রাঞ্চের** ২০০০+২০০০ সারি | নিজের ব্রাঞ্চে সীমিত |
| `native/PatientTimelineActivity.kt:2724` | `select=*` (ছবিসহ) — একজন রোগীতে সীমিত | সরু কলাম |
| `SettingsActivity.kt:224,247` · `ExportDataActivity.kt:100` | Backup Now / Export — এক চাপে `limit=100000` সব টেবিল | master-only + মাসে ১ বার |

---

## ৬. ✅ এখনই করুন — SQL Editor-এ (ফোনেই হবে, ১০ মিনিট)

⛔ **আগে Backup Center → Download Full Backup।**

### ধাপ ১ — আগে দেখুন (কিছুই বদলায় না)

```sql
select 'sessions' t, count(*) from auth.sessions
union all select 'refresh_tokens', count(*) from auth.refresh_tokens
union all select 'deleted_records', count(*) from public.deleted_records
union all select 'trash', count(*) from public.trash;
```

### ধাপ ২ — পুরনো লগইন-সেশন মোছা

⚠️ এতে কারো কারো ফোনে **একবার আবার লগইন** করতে হতে পারে — ভয়ের কিছু নেই,
পাসওয়ার্ড আগের মতোই।

```sql
delete from auth.refresh_tokens where created_at < now() - interval '3 days';
delete from auth.sessions       where created_at < now() - interval '3 days';
```

### ধাপ ৩ — পুরনো "মোছা"-সংকেত ও ট্র্যাশ

```sql
delete from public.deleted_records where ("deletedAt")::timestamptz < now() - interval '90 days';
delete from public.trash           where ("deletedAt")::timestamptz < now() - interval '180 days';
```

### ধাপ ৪ — জায়গা ফেরত আনা (একটা করে, আলাদা করে চালান)

```sql
vacuum full auth.sessions;
```
```sql
vacuum full auth.refresh_tokens;
```
```sql
vacuum full auth.mfa_amr_claims;
```
```sql
vacuum full public.trash;
```
```sql
vacuum full public.deleted_records;
```

📐 আশা: আরও **~৩-৪ MB** ফেরত, আর `deleted_records`-এর ৫০০০ সীমার বিপদ দূর।

---

## ৭. কোন কাজ কে করবে — সাজানো তালিকা

| # | কাজ | কে | কত সময় | লাভ |
|---|---|---|---|---|
| **১** | উপরের §৬-এর SQL | **আপনি, ফোনেই** | ১০ মিনিট | DB পরিষ্কার + সিঙ্কের ভুল আটকানো |
| **২** | সব ১৩ ফোনে **ভার্সন ৪.৩৯** মিলিয়ে নেওয়া | **আপনি** | ১ দিন | পুরনো APK-র বড় খরচ বন্ধ |
| **৩** | Export / Backup Now — **শুধু আপনি, মাসে ১ বার** | **নিয়ম** | — | এক চাপে ~৮০ MB বাঁচে |
| **৪** | ওয়েবের `upd()` ছবি-ফিক্স + আজকের backup-ফিক্স Netlify-তে তোলা | **আমি বানিয়ে দিয়েছি / দেব** | — | ভবিষ্যতের ফোলা বন্ধ |
| **৫** | 🔴 **BackgroundRefreshWorker-এ ব্রাঞ্চ যোগ** (§১) | **APK — Android Studio** | ১-২ ঘণ্টা | **~৩০০ MB/দিন** |
| **৬** | 🔴 **ModuleAuth টোকেন সেভ + refresh + logout** (§২) | **APK** | ২-৩ ঘণ্টা | সেশন জমা বন্ধ |
| **৭** | Draft · DoctorQueue রিফ্রেশ · GlobalSearch সরু করা (§৫) | **APK** | ২-৩ ঘণ্টা | আরও ~৫০-১০০ MB/দিন |
| **৮** | ৩ মাসে একবার §৬-এর SQL আবার চালানো | **আপনি** | ১০ মিনিট | স্থায়ী সুস্থতা |

---

## ৮. এতে কি সত্যিই ফ্রি প্ল্যানে চলবে? — সৎ হিসাব

| অবস্থা | রোজ খরচ | ৫ GB-তে চলবে? |
|---|---|---|
| এখন | ~৫৫০ MB | ❌ (৩.৪ গুণ বেশি) |
| শুধু §১ (ব্রাঞ্চ যোগ) হলে | 📐 ~২৫০ MB | ❌ এখনো বেশি |
| §১ + §৫ (সরু করা) হলে | 📐 ~১২০-১৫০ MB | ✅ **হ্যাঁ, চলবে** |
| উপরের সবটা + নিয়ম মানলে | 📐 ~৮০-১২০ MB | ✅ **আরামে চলবে** |

**সৎ শর্ত দুটো:**

১. **৫ ও ৭ নম্বর কাজ APK ছাড়া হয় না।** কেউ Android Studio-তে বিল্ড করে ১৩টা
   ফোনে ইনস্টল না করা পর্যন্ত egress কমবে না। এটাই পুরো প্ল্যানের আসল শর্ত।

২. **রোগী বাড়লে হিসাবও বাড়বে।** এখন ২৪৪ জন। ১০০০ জন হলে প্রতিটা "পুরো তালিকা"
   পড়া ৪ গুণ ভারী হবে। তখন §৫-এর সরু-করা কাজগুলো **বাধ্যতামূলক** হয়ে যাবে।

**ডেটাবেসের ৫০০ MB নিয়ে আর চিন্তা নেই।** আজকের পরিষ্কারের পর ~৬০ MB, আর আসল
তথ্য মাত্র ৩১ MB। §৩ ও §৪ সারালে বছরের পর বছর নিশ্চিন্ত।

---

## ৯. এই মাসটা (১৯ – ১৩ সেপ্টেম্বর) — কী হবে

- Egress **৩.৫ / ৫ GB**। এই হারে **২১-২২ আগস্ট শেষ**।
- তারপর Supabase সীমা বসাতে পারে — অনুরোধে **402** ফেরত আসবে, অ্যাপ কাজ করবে না।
- **১৩ সেপ্টেম্বর নিজে থেকেই রিসেট** হবে।
- এই ৩ দিনে খরচ কমানোর একমাত্র বাস্তব উপায়: **কম ফোনে অ্যাপ খোলা রাখা**,
  আর **Export / Backup Now একদম না চাপা**।

📌 তাই বাস্তব পরিকল্পনা: **সেপ্টেম্বরের ১৩ তারিখের আগেই ৫ ও ৭ নম্বর কাজ শেষ করে
নতুন APK সব ফোনে বসিয়ে ফেলুন।** তাহলে নতুন সাইকেল শুরুই হবে ঠিক হারে, আর
এই ঝামেলা আর ফিরবে না।

---

## ১০. এক নজরে — মনে রাখার ৫টা কথা

১. আপনার ডেটা ছোট (**৩১ MB**) — সমস্যা হলো **বারবার নামা**।
২. সবচেয়ে বড় ফুটো একটাই লাইনের ভুল — **ব্যাকগ্রাউন্ড প্রশ্নে ব্রাঞ্চ নেই**।
৩. ডেটাবেসের জায়গার সমস্যা **মিটে গেছে** ✅
৪. Egress-এর সমাধান **APK ছাড়া হয় না** — এটাই একমাত্র শর্ত।
৫. মাসে একবার **Download Full Backup** — Free plan-এ Supabase কিছুই রাখে না।
