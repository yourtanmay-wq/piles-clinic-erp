# V701 — PDF-এর ডান দিক কেটে যাওয়া (V698-এ আমারই ভুল)

**তারিখ:** ২৬.০৮.২০২৬ (IST)
**TK-এর রিপোর্ট (৪টে ছবি ধাপে ধাপে):** *"ছবিগুলো ধাপে ধাপে দেখুন…
এই সমস্যার কথা আপনাকে আগেও বলা হয়েছিল, তাহলে কেন এরকম ফালতু কাজ করেছেন"*

**TK ঠিক বলেছেন। দোষটা আমার, দু'বার।**

---

## ১. ছবিতে যা দেখা গেল

| ছবি | কী |
|---|---|
| ১ | Check-up পর্দা |
| ২ | Check-up History → **বাংলা** বাছা |
| ৩ | Send on WhatsApp → "Preparing PDF…" |
| ৪ | **PDF এখনো ভাঙা** — লোগো বিশাল, `TK BISW…` · `DOCTOR CHECK-UP RE…` · `SERINA KHATTO…` · `KNE-26082026-00…` সব **ডানদিকে কেটে গেছে** |

---

## ২. আসল কারণ — V698-এ আমি নিজেই ভুল লাইন লিখেছি

V698-এ আমি লিখেছিলাম:

```
wv.settings.useWideViewPort  = true      ← ঠিক
wv.settings.loadWithOverviewMode = false ← ❌ এটাই ভুল
```

`useWideViewPort` চালু করায় পাতা ঠিকই **৭৯৪ CSS-পিক্সেলে** সাজে। কিন্তু
`loadWithOverviewMode` বন্ধ থাকায় WebView সেটাকে পর্দার মাপে **ছোট করে না** —
ফোনের ঘনত্ব ধরে আঁকে। TK-এর ৩x ফোনে ৭৯৪ CSS px = **২৩৮২ ডিভাইস px** চওড়া,
অথচ আমরা ছবি তুলি মাত্র **৭৯৪ px**-এ ⇒ **ডান দিকের দুই-তৃতীয়াংশ কেটে যায়।**

**অর্থাৎ V698-এ আমি এক দোষ সারিয়ে আরেকটা বানিয়েছি** —
আগে সব ছোট ও সরু, এখন সব বড় ও কাটা।

### 🔴 আরও বড় দোষ: আমি "যাচাই করেছি" বলেছিলাম

V698-এ TK বলেছিলেন *"আপনি যাচাই করে দেখুন"*। আমি ব্রাউজারে HTML রেন্ডার
করে দেখিয়েছিলাম ৭৯৪-এ পাতাটা ঠিক আসে — **কিন্তু ওটা Android WebView নয়।**
আসল যন্ত্রে চালিয়ে দেখা হয়নি, তবু "কারণ প্রমাণিত" বলে দিয়েছিলাম।
**ব্রাউজারের পরীক্ষা = ফোনের পরীক্ষা নয়** — এটাই আমার শেখা।

---

## ৩. যা করা হলো

### (ক) ভুল লাইনটা ঠিক
`loadWithOverviewMode = true` — তখন WebView ৭৯৪ CSS px-কে ঠিক ৭৯৪ ডিভাইস
px-এ বসায় (১ CSS px = ১ px), অর্থাৎ আসল A4।

### (খ) আর আন্দাজের উপর নির্ভর নয় — কাগজ **নিজে মেপে** বসে

আগে **শুধু উচ্চতা** মাপা হত, চওড়া ধরে নেওয়া হত ৭৯৪। পাতা তার চেয়ে চওড়া
হলে বাকিটা **চুপচাপ কেটে যেত** — কেউ জানতেও পারত না।

এখন **চওড়া ও উচ্চতা দুটোই মাপা হয়**, যা মাপা যায় সেই চওড়াতেই সাজানো হয়,
আর ছবিতে বসানোর সময় সেই মাপ ধরেই A4-এ ছোট করা হয়।

⇒ **WebView যে ফোনে যেভাবেই সাজাক, ডান দিক আর কাটবে না।**
⛔ মাপা না গেলে বা অস্বাভাবিক মাপ এলে আগের ৭৯৪-ই ধরা হয়।

---

## ৪. ⛔ Prescription / Medicine Slip

আগের মতোই — ওদের টেমপ্লেটে `width=device-width` (কোনো সংখ্যা নেই), তাই
উপরের শর্তটা ওদের ক্ষেত্রে মেলে না। তবে (খ)-এর নিজে-মেপে-নেওয়া অংশটা
সবার জন্যই — ওতে কারো কাটা পড়ার ঝুঁকি কমল, বাড়ল না।

---

## ৫. যাচাই

| যাচাই | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ (V701) |
| `verify_kotlin_compile.py` | ✅ PASS — নতুন ভুল ০ |

### ⚠️ সৎ কথা

**এখানে Android চালিয়ে দেখার উপায় আমার নেই।** তাই এবার আমি
**"ঠিক হয়ে গেছে" বলছি না** — TK একবার চালিয়ে দেখবেন, তারপরই বলা যাবে।
আগের বার এই কথাটা না বলে "প্রমাণিত" বলেছিলাম, সেটাই ভুল ছিল।

---

## 🔴 TK দেখবেন

- [ ] Check-up History → Send on WhatsApp → PDF-এ **ডান দিক কাটা নেই**
- [ ] লোগো ঠিক মাপে, পুরো নাম-ঠিকানা দেখা যাচ্ছে
- [ ] বাংলা ও English দুটোতেই
- [ ] **Prescription ও Medicine Slip আগের মতোই** (সবচেয়ে জরুরি)
