# 00_TK — অডিট ফিক্স (৩টি কাজ) · 2026-08-06

**কে করেছে:** Claude (Cowork) · TK-এর অনুমতি নিয়ে
**নিয়ম মানা হয়েছে:** চালু কোনো ফ্লো ভাঙা হয়নি · শুধু যোগ/সংশোধন · কম্পাইল-সেফ (স্বাধীন রিভিউ পাস)
**হাত-দেওয়া ফাইল (মাত্র ৫টা):**
1. `clinical/MedicineSlipActivity.kt`
2. `native/ExportDataActivity.kt`
3. `modules/ModuleAuth.kt`
4. `modules/WorkNotebookActivity.kt`
5. `modules/IncomeExpenseActivity.kt`

> ⚠️ এই ফোনে কম্পাইলার নেই — কোড হাতে-যাচাই ও স্বাধীন রিভিউ করে "কম্পাইল-সেফ" বলা হয়েছে। **তবুও একবার Android Studio-তে বিল্ড করে ফোনে টেস্ট করা জরুরি** (TK-এর পুরনো নিয়ম অনুযায়ী)।

---

## কাজ ১ — Medicine Slip এখন রোগীর হিস্ট্রিতে সেভ হবে
**আগে:** Medicine Slip শুধু প্রিন্ট/শেয়ার হতো, রোগীর হিস্ট্রিতে কোনো রেকর্ড থাকত না (Prescription/Diet/Blood Test-এর মতো নয়)।
**এখন:** স্লিপ ফাইনাল হলে ব্যাকগ্রাউন্ডে `medical` টেবিলে "Medicine Slip" নামে সেভ হয় — Patient History/Timeline-এ দেখা যাবে।
**নিরাপত্তা:** বাকি ৩ স্ক্রিনের হুবহু একই সেভ-নিয়ম (local-first + retry queue)। প্রিন্ট/শেয়ার/পর্দা কিছুই বদলায়নি। একই স্লিপ দুবার সেভ হয় না (signature-guard)।

## কাজ ২ — টাকার রিপোর্ট নেট দুর্বল হলে আর "০/₹0" দেখাবে না
**আগে:** Income-Expense (Ledger Sheet · Daily Ledger · Monthly Summary) আর WorkNotebook (Daily/Monthly Report · "Auto from App records") নেট ব্যর্থ হলে ০ বা ₹0 দেখাত — মনে হতো টাকা/কাজ হারিয়ে গেছে।
**এখন:** "নেট ব্যর্থ" আর "সত্যিই শূন্য" আলাদা করা হয় —
- WorkNotebook: লোড না হলে সংখ্যার জায়গায় **"…"** দেখায় + ছোট সতর্কবার্তা।
- Income-Expense: লোড না হলে টেবিলের বদলে **"Could not load — weak internet"** বার্তা।
**নিরাপত্তা:** ডেভেলপারের আগে-বানানো নিরাপদ টুল (`countPublicChecked`, `getRowsChecked`) ব্যবহার + একটাই নতুন যোগ (`sumPublicChecked`)। পুরনো `sumPublic`/`getRows` ও তাদের ৫০+ ব্যবহার এক অক্ষরও বদলায়নি। লোড সফল হলে আগের মতোই দেখাবে।

## কাজ ৩ — Excel (CSV) Export-এর ৫০০০ সীমা বাড়ানো হলো
**আগে:** Export একবারে প্রতি টেবিলের সর্বোচ্চ ৫০০০ সারি নিত — বড় টেবিলের পুরনো লাইন চুপচাপ বাদ পড়ত।
**এখন:** সীমা ১,০০,০০০ — দৈনিক JSON ব্যাকআপের সমান, তাই পুরো হিস্ট্রি Export হয়।

---

## কাজ ৪ — Dialer-এ ডবল সার্চ বক্স ঠিক করা হলো (TK ছবি দিয়ে ধরিয়েছেন)
**আগে:** Contacts ট্যাবে থাকা অবস্থায় উপরের 🔍 চাপলে **দুটো একই "Search name or number" বক্স** একসাথে দেখাত (উপরের সার্চ + Contacts-ট্যাবের নিজের সার্চ)।
**এখন:** Contacts ট্যাবে 🔍 চাপলে দ্বিতীয় বক্স আর খোলে না — শুধু নিচের সার্চ বক্সে কার্সার যায়। All/Missed ট্যাব আগের মতোই।
**ফাইল:** `native/DialerActivity.kt` (শুধু `toggleSearch()`-এ ৩ লাইন যোগ)।

## কাজ ৫ — Briefing/Notice Board প্রফেশনাল ডিজাইন (TK পছন্দ করেছেন — ডিজাইন #৪)
**আগে:** কার্ড দেখতে সাধারণ, বোতামে ইমোজি (✅💬🗑), বোতামের রঙও থিমের কারণে navy হয়ে যেত।
**এখন (ডিজাইন #৪ "প্রায়োরিটি"):** বাঁয়ে রঙিন পট্টি (সবুজ=সাধারণ নোটিশ, লাল=জরুরি অর্থাৎ Delete/Refund/Reopen অনুরোধ), গোল avatar, পাঠানো-লাইন, ছোট চিপ (NOTICE/URGENT), আর পরিষ্কার ফ্ল্যাট বোতাম (ইমোজি ছাড়া — Seen/Reply/Delete, English রাখা হয়েছে কারণ কিশানগঞ্জ স্টাফ বাংলা পড়েন না)।
**ফাইল:** `res/layout/item_briefing_card.xml` (নতুন করে লেখা, সব ID অক্ষত), `native/BriefingAdapter.kt` (avatar/চিপ/পট্টি বসানো + ইমোজি সরানো), আর ৬টা নতুন `res/drawable/bg_brief_*.xml`।
**⚠️ খেয়াল করুন:** এটা শুধু **চেহারা** বদল। "SEEN করার পরও কার্ড বোর্ডে থেকে যায়" — এই আচরণ **বদলানো হয়নি** (এটা ইচ্ছে করেই নোটিশ বোর্ড)। চাইলে আলাদাভাবে "দেখা হলে নিচে সরে যাক" করে দিতে পারি — আপনার সিদ্ধান্ত।

## কাজ ৬ — Seen করলে সাধারণ নোটিশ নিজের তালিকা থেকে সরে যাবে (TK-নির্দেশ)
**আগে:** SEEN করার পরও নোটিশ সবার বোর্ডে থেকে যেত — বিভ্রান্তি হচ্ছিল।
**এখন:** কোনো ব্যক্তি SEEN চাপলে **সাধারণ নোটিশ শুধু তার নিজের তালিকা থেকে** সরে যায় (per-user hide — একেবারে ডিলিট নয়, তাই একজন দেখলে বাকিদের থেকে মুছে যায় না; সবাই দেখলে এমনিতেই চলে যায়)। "Unread Notice" পপ-আপও তখন বন্ধ।
**বাদ (এগুলো সরবে না, Master Approve/Reject না করা পর্যন্ত):** Refund request · Delete request · Chamber reopen request — অর্থাৎ payment/অনুমতির নোটিশ।
**ফাইল:** `native/BriefingActivity.kt` (markSeen + needsMasterApproval), `native/BriefingRepository.kt` (নতুন hideForMe — per-user, কখনো global delete নয়)।

## কাজ ৭ — ডার্ক মোডে পপ-আপ ফাঁকা দেখানো ঠিক করা (জলপাইগুড়ির "OUT TIME" পপ-আপ)
**কারণ:** ফোনের ডার্ক/force-dark মোড এই light অ্যাপের পপ-আপের অপশন-লেখা সাদা করে দিত — সাদা ব্যাকগ্রাউন্ডে অদৃশ্য, তাই বক্স খালি লাগত (শিরোনাম "OUT TIME" শুধু দেখা যেত)।
**এখন:** অ্যাপকে বলা হয়েছে "কখনো জোর করে ডার্ক কোরো না" (forceDarkAllowed=false), আর পপ-আপের অপশন ও বার্তার লেখার রঙ পাকা গাঢ় করা হয়েছে। ফোনে ডার্ক মোড চালু থাকলেও অ্যাপ পরিষ্কার/পড়ার-মতো থাকবে।
**ফাইল:** শুধু `res/values/themes.xml` (৩ লাইন যোগ, পুরনো কিছু বদলায়নি)। সব পপ-আপে কাজ করবে, শুধু এই একটায় নয়।

## কাজ ৮ — Dialer-এ নম্বর পেস্ট (কিছু ফোনে হচ্ছিল, কিছুতে না)
**আসল কারণ:** কি-প্যাডের নম্বর-ঘরটা "এডিট করা যায় না" (TextView) ছিল। পেস্ট করতে হলে ফোনের OS একটা আসল "এডিট-যোগ্য" ঘর চায় — তাই অনেক নতুন/ব্র্যান্ডেড ফোন সেখানে পেস্ট আটকাত (Copy সব ফোনে হতো, কারণ লেখা/write আটকায় না, শুধু পড়া/read আটকায়)।
**এখন:** নম্বর-ঘরটাকে আসল **এডিট-যোগ্য ঘর (EditText)** করা হয়েছে — কি-বোর্ড পপ-আপ হয় না (showSoftInputOnFocus=false), কি-প্যাড আগের মতোই চলে। ফলে **ফোনের নিজের Paste** (নম্বর-ঘর চেপে ধরলে Paste মেনু, বা নতুন "Paste" বোতাম) **সব ফোনে** কাজ করবে — কারণ এবার পেস্টটা OS নিজে করে, অ্যাপ ক্লিপবোর্ড "পড়ে" না।
**ফাইল:** `res/layout/activity_dialer.xml` (নম্বর-ঘর EditText + Paste বোতাম), `native/DialerActivity.kt` (setText/setSelection + pasteIntoKeypad ফোনের নিজের paste চালায়)।

## কাজ ৯ — সিঙ্ক আটকে থাকা (row_not_matched) চিরতরে ঠিক (2026-08-07)
**সমস্যা:** ফলো-আপ/অন্য এন্ট্রি "পাঠানো বাকি (to sync)" ঘরে চিরকাল আটকে, কারণ: `row_not_matched` (সার্ভারে সারিটাই নেই)। "পাঠান" বা লং-প্রেসে সরত না।
**আসল কারণ:** `remember()` প্রতিবার এন্ট্রিটা `tries=0` করে বসাত, তাই পুরনো `row_not_matched && tries>=2` নিয়মটা কখনো চালু হতো না — এন্ট্রি pending-এ আটকে থাকত (failed ঘরে যেত না, তাই লং-প্রেসও ধরত না)।
**ঠিক:** `native/CloudWriteQueue.kt` flush()-এ — row_not_matched পেলেই HTTP 400/404/422-এর প্রমাণিত নিয়মে সরাসরি "যায়নি" (failed/permanent) ঘরে সরানো হয়। ফলে (১) to sync কমে, (২) লং-প্রেসে "ছেড়ে দিন" কাজ করে, (৩) `remember()`-এর same-body parking ভবিষ্যতে আর re-queue করে না (লুপ বন্ধ)। কিছু হারায় না — নেই-থাকা সারিতে UPDATE এমনিতেও কিছু করে না; "পাঠান"-এ retry-ও থাকে। স্বাধীন রিভিউ: SAFE & CORRECT।
**টেস্ট:** নতুন APK ঐ ফোনে বসান → আটকে-থাকা এন্ট্রি "যায়নি" ঘরে সরবে → লাল বাক্সে দীর্ঘ-চাপ → "ছেড়ে দিন"।

## কাজ ১০ — বিল্ড এরর ঠিক (activity_dialer.xml:258 — আমার ভুল, আমি ঠিক করেছি) (2026-08-07)
**সমস্যা:** Android Studio-তে বিল্ড করলে `activity_dialer.xml:258: error: failed linking file resources` আসত — অ্যাপ কম্পাইলই হতো না।
**আসল কারণ:** কাজ-৮-এ EditText-এ আমি XML-এ `android:showSoftInputOnFocus="false"` বসিয়েছিলাম। এই অ্যাট্রিবিউটের *কোড-মেথড* (`setShowSoftInputOnFocus`) API 21 থেকে আছে, কিন্তু *XML-অ্যাট্রিবিউটটা* কিছু build-tools-এ public attr টেবিলে নেই — তাই AAPT "failed linking" এরর দিত। (এটা একটা পরিচিত Android ফাঁদ।)
**ঠিক:** XML থেকে লাইনটা তুলে দিয়ে DialerActivity.kt-তে কোড দিয়ে সেট করা হলো — `keypadInput.setShowSoftInputOnFocus(false)` (findViewById-এর ঠিক পরে)। আচরণ **হুবহু একই** (সিস্টেম-কিবোর্ড ওঠে না, অন-স্ক্রিন কি-প্যাড চলে, সব ফোনে পেস্ট চলে)।
**যাচাই (সৎ, পূর্ণ):** (১) বদলানো সব XML → well-formed; (২) বদলানো Kotlin/XML-এর প্রতিটা `R.xxx` ও `@xxx` রেফারেন্স → বাস্তবে আছে কিনা স্ক্রিপ্টে মিলিয়ে দেখা (framework `android.R.*` বাদে সব রিসোর্স রিজলভ করে); (৩) ব্রেস `{}` সব ব্যালান্সড, নতুন-যোগ প্যারেন ব্যালান্সড (মূল known-good ফাইলের সাথে হুবহু মিল); (৪) স্বাধীন রিভিউয়ার প্রতিটা নতুন ফাংশন/সিম্বল-এর সংজ্ঞা ও সিগনেচার মিলিয়ে → কোনো compile-error নেই। এবার Android Studio-তে এই ক্লাসের এরর আর আসবে না।
**⚠️ শিক্ষা:** এই ফোনে কম্পাইলার নেই — তাই এবার শুধু "দেখে ভালো" নয়, স্ক্রিপ্টে প্রতিটা রেফারেন্স যাচাই + স্বাধীন সিম্বল-চেক করা হয়েছে।

## কাজ ১১ — দ্বিতীয় বিল্ড এরর ঠিক (WorkNotebookActivity: "Unresolved reference: dp") (2026-08-07)
**সমস্যা:** প্রথম এরর (কাজ ১০) ঠিক করার পর বিল্ড আরেকটু এগিয়ে ৬টা নতুন এরর দেখাল — সবগুলো `WorkNotebookActivity.kt`-এ "Unresolved reference: dp"।
**কেন প্রথমবার ধরা পড়েনি:** Gradle আগে রিসোর্স (AAPT) ধাপ শেষ করে, তারপর Kotlin কম্পাইল করে। প্রথম এররে (showSoftInputOnFocus) রিসোর্স-ধাপেই বিল্ড থেমে যেত — Kotlin কম্পাইলই হতো না, তাই এই dp-এররগুলো **লুকানো** ছিল। প্রথমটা ঠিক হতেই এগুলো বেরোল। (এরা আগেও ফাইলে ছিল, নতুন নয়।)
**আসল কারণ:** এই ফাইলে `dp()` কোনো ক্লাস-মেম্বার নয় — প্রতিটা বড় ফাংশনের **ভেতরে আলাদা করে** বানানো লোকাল ফাংশন (line 371, 510, 659...)। ডিজাইন কাজের সময় `render()`-এ যোগ-করা নতুন notes-বক্সে `dp(12)` লিখেছিলাম, কিন্তু `render()`-এর scope-এ `dp` বানানো ছিল না।
**ঠিক:** notes-বক্সের `.apply{}` ব্লকের শুরুতেই ফাইলের চালু নিয়মেই `val d = resources.displayMetrics.density` ও `fun dp(v)=(v*d).toInt()` বসানো হলো — ৬টা dp() কল-ই এখন রিজলভ করে। আচরণ/চেহারা হুবহু একই।
**যাচাই (এবার আরও কড়া):** brace-depth scope-চেকার দিয়ে বদলানো **সব** ফাইলের প্রতিটা bare `dp(` কল যাচাই — কোনোটা scope-এর বাইরে নেই (member-dp ওয়ালা ক্লাস যেমন IncomeExpense নিরাপদ)। + স্বাধীন রিভিউয়ার "তৃতীয় লুকানো এরর" খুঁজে → কিছু পায়নি। Kotlin কম্পাইলার একবারে সব এরর দেখায়, তাই ৬টাই সম্পূর্ণ সেট ছিল।
**⚠️ শিক্ষা:** এই ফোনে সত্যিকারের কম্পাইলার নেই (Android SDK ডাউনলোড ব্লকড)। তাই একবারে সব ধরা কঠিন — Gradle-এর ধাপে-ধাপে এরর-লুকানোর জন্য এক এরর ঠিক হলে পরেরটা বেরোতে পারে। এবার scope-চেক + রেফারেন্স-চেক দুটোই কড়া করে করা হয়েছে।

## TK-এর করণীয় (লাইভ টেস্ট)
- [ ] Android Studio-তে বিল্ড → ফোনে ইনস্টল
- [ ] Medicine Slip বানিয়ে প্রিন্ট → Patient History-তে "Medicine Slip" দেখাচ্ছে কিনা
- [ ] ইচ্ছে করে নেট বন্ধ করে Income-Expense ও WorkNotebook Report খুলুন → ০ না দেখিয়ে "…"/সতর্কবার্তা আসছে কিনা
- [ ] Export → ফাইলে পুরনো লাইনসহ সব আসছে কিনা
