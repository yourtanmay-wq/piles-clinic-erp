# V592 — App Calls গোনা কেন 0 দেখাচ্ছিল
**তারিখ:** ২৩.০৮.২০২৬ · **ভার্সন:** V592 / 5.92

TK:
> *"App এর কল গুলি সঠিক ভাবে কাউন্টিং কেন হচ্ছে না · আরে একই সমস্যার কথা এর
> আগে অনেকবার আপনাকে বলা হয়েছিল · তারপরও আপনি সঠিকভাবে এটা কার্যকরী করেন নাই
> কেন? গভীরে গিয়ে সত্যতা আগে যাচাই করুন"*

**TK ঠিক বলেছেন।** V590-এ আমি একটা আসল ফুটো বন্ধ করেছিলাম (`getRows()` ব্যর্থ
হলেও 0 ফেরাত), কিন্তু **মূল কারণটাই খুঁজে বার করিনি**। এবার কোড চালিয়ে
বার করা হলো।

---

## 🔴 আসল কারণ ১ — লেখার নাম আর পড়ার নাম এক ছিল না

| দিক | ফাইল | `staff_code`-এ কী বসত |
|---|---|---|
| **লেখা** (কল-বোতাম চাপলে) | `ModuleAuth.logCallTap()` | `expectedCode()` → **`MASTER-TK`** · **`DR-KH-MANDAL`** … |
| **পড়া** (Today Work) | `WorkNotebookActivity` | `staffCode = user.name` → **`TK BISWAS`** · **`Dr. K.H MANDAL`** … |

দুটো কখনো মেলে না ⇒ **সংখ্যা সবসময় 0**।

`StaffDirectory`-র ২২টা অ্যাকাউন্ট নিয়ে দুটো নিয়ম **চালিয়ে** দেখা হয়েছে:

```
মোবাইল         নাম (পড়ার দিক)        কোড (লেখার দিক)        মেলে?
8001080080     TK BISWAS              MASTER-TK              ❌
9883605917     KNE-LAXMI              KNE-LAXMI              ✅
7980993652     Dr. K.H MANDAL         DR-KH-MANDAL           ❌
8001800148     Dr. JAY BANIK          DR-JAY-BANIK           ❌
9046366596     AMIT GOLDAR            DR-AMIT-GOLDAR         ❌
7479173399     J.H MANDAL             DR-JH-MANDAL           ❌
9002610352     GOKUL                  DR-GOKUL               ❌
7810907954     Dr. SAIKAT ROY         DR-SAIKAT-ROY          ❌
9242009205     Dr. PRANAB BISWAS      DR-PRANAB-BISWAS       ❌
9002003540     Field Officer          FIELD-OFFICER          ❌
…
মোট 22 টার মধ্যে 9 টায় গোনা কখনো মিলত না
```

⇒ **মাস্টার · ৭ জন ডাক্তার · ফিল্ড অফিসার — এই ৯টা অ্যাকাউন্টে App Calls
কোনোদিনই গোনা হয়নি।** ব্রাঞ্চ-স্টাফের ১৩টায় নাম আর কোড একই বলে ওগুলো মিলত।

**আর এই ভুলটা ফোনে-জমা গোনাতেও ছিল** (`localCallTapCount`) — লেখা হত কোড দিয়ে,
পড়া হত নাম দিয়ে। তাই নেট থাকলেও-না-থাকলেও **দুই দিকেই 0**, আর V590-এর
`max(cloud, phone)` জালটাও কাজে লাগত না (দুটোই 0)।

## 🟡 "আসল কারণ ২" — **আমি ভুল বলেছিলাম, TK প্রমাণ করে দিলেন**

আমি বলেছিলাম ফোন থেকে `call_date` পাঠানো হয় না, তাই তারিখ ফাঁকা বা ভুল
দিনে বসতে পারে। TK Supabase-এ চালিয়ে দেখালেন — **সত্যি নয়**:

```
call_date  date  DEFAULT ((now() AT TIME ZONE 'Asia/Kolkata'::text))::date  NOT NULL
```

ঘরটার ডিফল্ট **আগে থেকেই ঠিক ছিল, আর ভারতীয় সময়েই**। ৭২৩টা সারির
**একটারও তারিখ ফাঁকা নয়** (প্রতিটা নামের `tarikh_faka` = 0)।

⇒ আমি যে সংশোধনটা করেছিলাম (ফোন থেকে তারিখ পাঠানো) সেটা **ফিরিয়ে নেওয়া
  হয়েছে**। কারণ ওটা শুধু অপ্রয়োজনীয়ই নয়, **ঝুঁকিরও** — সার্ভারের ঘড়ি
  সবসময় ঠিক, কিন্তু কোনো স্টাফের ফোনের ঘড়ি/টাইমজোন ভুল থাকলে ফোন থেকে
  পাঠানো তারিখ ভুল দিনে বসিয়ে দিত। অর্থাৎ যা ঠিক আছে তাকে খারাপ করা হত।

⛔ ফোনে-জমা গোনার তারিখটা ভারতীয় সময়ে করা হয়েছে — ওটা রাখা হলো, কারণ ওটা
   সত্যিই অমিল ছিল (লেখা হত ফোনের নিজের টাইমজোনে, পড়া হত ভারতীয় সময়ে)।

---

## ✅ আসল তথ্য দিয়ে কারণ ১ প্রমাণিত

TK-এর পাঠানো ডেটা (`V592_CALL_TAPS_ASOL_TOTHYO_2026-08-23.csv`):

| নাম | মোট কল | তারিখ ফাঁকা | শেষ কল |
|---|---|---|---|
| JPE-JALPAI-13 | 413 | 0 | 22.08 |
| COB-UTTAMA | 306 | 0 | 23.08 |
| COB-4 | 277 | 0 | 23.08 |
| KNE-KISHAN5 | 129 | 0 | 15.08 |
| COB-BRANCH | 128 | 0 | 17.08 |
| JPE-CRP | 91 | 0 | 23.08 |
| KNE-LAXMI | 53 | 0 | 22.08 |
| **MASTER-TK** | **27** | **0** | **23.08** |
| KNE-BRANCH | 19 | 0 | 23.08 |
| JPE-BRANCH | 8 | 0 | 11.08 |
| FLK-1 | 2 | 0 | 18.08 |
| JPE-RUPAM | 1 | 0 | 08.08 |

**এটাই প্রমাণ।** `MASTER-TK`-এর নামে **২৭টা কল** জমা আছে, আজকেরটাও আছে —
অথচ পুরোনো কোডে Today Work খুঁজত `TK BISWAS` নামে ⇒ **সবসময় 0**।
ঠিক যেটা TK দেখেছিলেন।

⛔ Staff ও Branch ID-গুলোর নাম আর কোড একই, তাই **ওদের গোনা আগেও ঠিকই হত** —
   ডেটাই বলছে ওদের সারি ঠিক তারিখে জমা আছে।
⛔ ডাক্তার ও ফিল্ড অফিসারের নামে একটাও কল নেই, তাই ওদের ক্ষেত্রে
   সংশোধনটা এখন পর্যন্ত তাত্ত্বিক — ভবিষ্যতে কল করলে ঠিক গোনা হবে।

---

## যা বদলানো হলো

| ফাইল | কী |
|---|---|
| `modules/WorkNotebookActivity.kt` | নতুন `callTapCode()` — লেখার দিকে যে `expectedCode()` বসে, পড়ার দিকেও **ঠিক সেটাই**। ক্লাউড-গোনা (২ জায়গা) ও ফোনে-জমা গোনা (১ জায়গা) — এই তিনটেতেই |
| `modules/ModuleAuth.kt` | ফোনে-জমা গোনার তারিখ ভারতীয় সময়ে (পড়ার দিকের মতোই)। ⛔ `call_date` ফোন থেকে পাঠানো হয় **না** — সার্ভারের ডিফল্টই ঠিক |

⛔ **`staffCode` বাকি ৩৪ জায়গায় এক অক্ষরও বদলানো হয়নি** — `notebook_days` ·
`work_reports` · পর্দার লেখা সব আগের মতোই। শুধু কল-গোনার তিনটে জায়গা।
⛔ ওয়েবে বদলানোর কিছু নেই — যাচাই করে দেখা গেল ওখানে লেখা ও পড়া **আগে থেকেই**
একই `MOD._session.code` ব্যবহার করে, আর `call_date`-ও পাঠায়।

---

## পাহারাদার

নতুন `00_GUARD/verify_v592_app_call_counting.py` — **১৯টা যাচাই, সব পাশ**।
কোড শুধু পড়ে না — `StaffDirectory`-র সব অ্যাকাউন্ট নিয়ে **দুটো নিয়ম চালিয়ে**
মিলিয়ে দেখে, আর "আগে ৯টায় মিলত না · এখন একটাও অমিল নেই" — দুটোই প্রমাণ করে।

**উল্টো-পরীক্ষা** (ইচ্ছে করে ভেঙে দেখা): পুরোনো ভুলটা ফিরিয়ে দিলে ❌ ·
`call_date` সরালে ❌ · তারিখ ভারতীয় সময় থেকে সরালে ❌ — তিনটেই ধরা পড়ে।

🔧 উল্টো-পরীক্ষা করতে গিয়ে **পাহারাদারের নিজেরই একটা ফাঁক** ধরা পড়ে ঠিক করা
হয়েছে: `str.index()` ব্যবহার করায় লেখাটা না থাকলে স্ক্রিপ্ট ক্র্যাশ করত, আর
ক্র্যাশ মানে ❌ ছাপা হত না — ভুলটা চুপচাপ পার হয়ে যেত। এখন `find()`।
আর `verify_v591`-এ ভার্সন "591" হুবহু বাঁধা ছিল, তাই V592-তে মিথ্যে ❌ দিত —
এখন "591 বা তার পরে"।

**মোট ২৪টা পাহারাদার — সব পাশ**, Kotlin কম্পাইলে নতুন ভুল 0।
