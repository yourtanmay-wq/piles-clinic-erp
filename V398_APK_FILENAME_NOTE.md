# V398 — APK ফাইলের নামে ভার্সন (১৬.০৮.২০২৬)

TK-প্রশ্ন: **"APK বিল্ড করেছি, কিন্তু ফাইলের নাম আগেরটাই রয়ে গেছে কেন?"**

## কারণ (খুঁজে বার করা)

`app/build.gradle.kts`-এ **APK-র নাম ঠিক করার কোনো নিয়মই লেখা ছিল না**।
তাই Gradle তার ডিফল্ট নামই দিত — `app-release.apk` / `app-debug.apk`।
ঐ নামে ভার্সন **কখনোই ছিল না**, তাই versionCode বাড়ালেও নাম বদলাত না।

## যা করা হলো

1. ভার্সন এখন **একটাই জায়গায়** (ফাইলের ২৩-২৪ লাইন):
   `val appVersionCode = 397` · `val appVersionName = "3.97"`
   `defaultConfig`-এ ঐ দুটোই ব্যবহার হয় — হাতে দু'জায়গায় বদলানোর ঝুঁকি শেষ।
2. `base { archivesName.set("PilesClinic-V$appVersionCode") }`
   → বিল্ড করলে APK-র নাম হবে **`PilesClinic-V397-release.apk`**
   (ও `PilesClinic-V397-debug.apk`)।

পরের বার ভার্সন বদলাতে **শুধু ঐ দুটো লাইন** বদলালেই তিনটে জিনিস একসাথে বদলাবে —
versionCode · versionName · APK-র নাম।

## ⛔ সৎভাবে জানানো

* `archivesName` হলো Gradle-এর **নিজস্ব স্থায়ী নিয়ম** (কোনো ভিতরের/অনির্ভরযোগ্য
  API নয়), AGP 8.2.2 + Gradle 8.5-এ চলে — এই প্রজেক্টে ঐ দুটোই আছে।
* **আমি এখানে Gradle চালিয়ে বিল্ড করে দেখতে পারিনি** (এই পরিবেশে Android SDK
  নেই)। তাই নামটা আপনার মেশিনে বিল্ড করেই মিলিয়ে নেবেন।
* অ্যাপের কাজ · ডিজাইন · ডেটা · Supabase — **কিছুই ছোঁয়া হয়নি**, শুধু বিল্ডের নাম।

## মনে রাখবেন

ড্যাশবোর্ডের `☁️ Synced · V###` লেখাটা `versionCode` থেকেই আসে
(`DashboardActivity.kt:528`)। তাই নতুন বিল্ডে **V397** দেখাবে —
তবে **এই নতুন জিপ দিয়ে বিল্ড করলে তবেই**।
