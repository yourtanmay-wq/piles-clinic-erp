# 📒 খাতা — সেশন ২১.০৮.২০২৬ (V494)

**ভিত্তি:** V493 · TK-এর ৫টি যাচাই-রিপোর্ট

## TK যা ধরেছেন — পাঁচটাই সঠিক
1. `CloudReadDedupe.inFlight` — চাবি জমত, সরত না (**মেমরি লিক**)
2. Dedupe চাবিতে **ব্যবহারকারীর পরিচয় ছিল না** — লগআউটের পরে ফাঁসের ঝুঁকি
3. SafeWide পড়া ব্যর্থ হলে **সরাসরি null** — B446 ফিরে আসার ঝুঁকি
4. ওয়েবে **হুবহু একই query দুবার** — অর্থহীন
5. বাকি বড় Read এখনো আছে

## সংশোধন
1. Gate refcount + **`finally`** — সফল/ব্যর্থ/exception সব পথে চাবি মুক্ত
2. চাবি = `"<মোবাইল শেষ ১০>|<URL>"` + লগইন·লগআউট·প্রতি-পর্দা·signOut — **চার পথে** `setSession()`
3. চার ধাপ পুরোপুরি: সরু → সরু → ছবি-ছাড়া পূর্ণ → **`select=*`**
4. নতুন `wlv1IsColumnError()` — ঘর-ভুল বনাম নেট আলাদা; ডুপ্লিকেট সরানো
5. `closeSourceEnquiry` সরু (প্রতি রেজিস্ট্রেশনে **ছবি নামা বন্ধ**)

## 🔴 খুঁজতে গিয়ে নিজের দ্বিতীয় বাগ ধরা পড়ল
`SafeWideColumns` চাওয়া ঘর **যাচাই না করেই** ফেরাত — তাই ভুল ঘর-নাম থাকলে
"নিরাপদ" ধাপটাও ব্যর্থ হয়ে `select=*`-এ (ছবিতে) যেত। যে কারণে ধাপটা বানানো,
ঠিক সেই কারণেই কাজ করত না। **Java পরীক্ষা হাতেনাতে ধরেছে** (T3, পরিস্থিতি ক)।
এখন শুধু চেনা ঘর। সঙ্গে `ALL` লাইভ-ধ্রুবক থেকে নতুন করে বানানো — তাতে
**৩টে সত্যিকারের ঘর** বেরোল যা আগের তালিকায় ছিল না
(`payments.cashAmount` · `onlineAmount` · `dailyEvents`)।

## প্রমাণ
- Java পরীক্ষা **৪০/৪০ পাশ** (`00_GUARD/dedupe_test/`)
- ২০,০০০ URL · ৫,০০০ ব্যর্থতা · ২,০০০ exception · ১৬ থ্রেড → `inFlight = 0`
- দুই ব্যবহারকারী: দ্বিতীয়জন **আগের জনের তথ্য পায়নি**
- নেট বন্ধে **₹0 হয় না** — মডেলে ও `PaymentActivity.kt:336-342` আসল কোডে
- পাঁচটা পাহারাদার PASS · `node --check app.js` PASS

বিস্তারিত: **`00_V494_FIX_PROOF_2026-08-21.md`**

## ⚠️ সীমাবদ্ধতা (অপরিবর্তিত)
Android SDK ও Maven Central ব্লকড ⇒ **এখানে Kotlin compile / APK build সম্ভব নয়**।
Android Studio-র বিল্ডই চূড়ান্ত প্রমাণ।
"Free Plan-এ নিশ্চিত চলবে" লেখা হয়নি — ২৪–৪৮ ঘণ্টার Egress chart-ই চূড়ান্ত।

## ছোঁয়া হয়নি
schema · RLS · role · branch permission · ডিজাইন · টাকার হিসাব · payment/refund ·
notification · কোনো তথ্য মোছা হয়নি · **একটাও SQL চালানো হয়নি**।

*লেখা হলো ২১.০৮.২০২৬*
