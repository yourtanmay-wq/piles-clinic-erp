# ✅ যাচাই রিপোর্ট — V139 (2026-07-26/27)

ফাইল পাঠানোর আগে যা যা মিলিয়ে দেখা হয়েছে। **সব পরীক্ষা পাশ।**

## ১) Android — build ভাঙতে পারে এমন প্রতিটি জিনিস

| পরীক্ষা | ফল |
|---|---|
| ১৫০টি Kotlin ফাইলে `{ } ( ) [ ]` মিল (স্ট্রিং ও কমেন্ট বাদ দিয়ে) | ✅ সব সমান |
| ২০৫টি XML well-formed | ✅ |
| XML কমেন্টে `--` (থাকলে build fail) | ✅ কোথাও নেই |
| কোডের প্রতিটি `R.id.x` লেআউটে আছে | ✅ একটাও নিখোঁজ নয় |
| লেআউটের প্রতিটি `@drawable/x` ফাইল আছে | ✅ |
| ম্যানিফেস্টের ৪০টি Activity-র ক্লাস ফাইল আছে | ✅ |
| আজ বদলানো ৮টি ফাইলে `if/else` শৃঙ্খল হাতে যাচাই | ✅ সব বৈধ (`when`/expression-if সহ) |
| versionCode / versionName | ✅ **139 / V139** |

## ২) আজকের নিয়মগুলো সত্যিই বসেছে কিনা

| নিয়ম | ফল |
|---|---|
| Advance — বিল ছাড়াই নেওয়া যাবে (কোনো বাধা নেই) | ✅ |
| 2nd/3rd Payment — বিল না থাকলে আটকাবে ("This patient's Bill has not been created yet — please create the Bill first") | ✅ ঠিক একটি জায়গায়, `showNthPaymentDialog`-এ |
| Chamber — কোনো বাধা নেই | ✅ |
| Visit ট্যাবের self-heal ব্যাকগ্রাউন্ডে | ✅ |
| Delete-guard মেমরিভিত্তিক (ডিস্কে থেমে থাকা নেই) | ✅ |

## ৩) Web

| পরীক্ষা | ফল |
|---|---|
| `node --check app.js` | ✅ পাশ |
| `styles.css` ব্রেস | ✅ 2168 / 2168 |

## ৪) ⚠️ NOT TESTED (সৎ ঘোষণা)

- **Android Studio Build / Gradle / APK — NOT TESTED** (এই পরিবেশে Gradle ও ইন্টারনেট নেই)
- অ্যাপ চালানো, একাধিক ফোনে Live Sync, Live Supabase — **NOT TESTED**
- ZIP-এ কোনো APK/AAB নেই

উপরের সব যাচাই **কোড-স্তরের (Static)**। build-এ কোনো error এলে সঙ্গে সঙ্গে জানাবেন — একই সেশনে ঠিক করা হবে।

---

**🔒 LOCK NOTE:** এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না, অ্যাপ স্লো করা যাবে না।
