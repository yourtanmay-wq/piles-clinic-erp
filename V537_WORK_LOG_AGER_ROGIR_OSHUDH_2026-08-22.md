# V537 — 🔴 আগের রোগীর ওষুধ আর নতুন রোগীর প্রেসক্রিপশনে যাবে না
তারিখ: ২২.০৮.২০২৬ · V536 → **V537 (5.37)** · **রোগী-নিরাপত্তার গুরুতর বাগ**

TK-এর রিপোর্ট (SWAPAN SARKAR-এর প্রেসক্রিপশনের ছবি):
> *"আমি শুধুমাত্র দুটো মেডিসিন দিয়েছি — Abhayaristam syrup, Triphala tablet।
> কিন্তু যখন প্রিন্ট আউট করতে যাচ্ছি, এর আগে যে পেশেন্টকে যে মেডিসিন দিয়েছে
> সেগুলো অটোমেটিক হয়ে গেল কেন?"*

TK-এর ছবিতেই প্রমাণ: পর্দায় **Save (1)** + বাইরের ১টা, অথচ বার্তা
**"Prescription saved (9 item/s)"** ও প্রিন্টে **৯টা** ওষুধ।

---

## ১. আসল কারণ (কোড ধরে প্রমাণিত — আন্দাজ নয়)

`ClinicalRepository.currentPrescription` একটাই **অ্যাপ-জোড়া তালিকা**
(`object` = পুরো অ্যাপে একটাই)। **রোগী বদলালে এটা খালি হত না।**

খালি করার ফাংশন `resetForNewPatient()` **লেখা ছিল**, কিন্তু ডাকা হত
**একমাত্র Dashboard-এর Print টাইল থেকে** (`DashboardActivity.kt:149`) —
CHECK-UP বা Prescription খোলার সময় **নয়**।

⇒ এক রোগীর প্রেসক্রিপশন করার পর, Dashboard→Print দিয়ে না গিয়ে অন্য রোগী
খুললেই **আগের রোগীর ওষুধগুলো তালিকাতেই থেকে যেত** — আর নতুন রোগীর নামে
সেভ ও প্রিন্ট হয়ে যেত। সেভের গোনাটাও (`count`) ওই তালিকা থেকেই আসে, তাই
**৯** দেখিয়েছে।

### ⚠️ এই একই দোষ আগে একবার ধরা পড়েছিল
কোডেই লেখা আছে (খাতার সারি **B437**, ০৫.০৮.২০২৬):
> *"`ClinicalRepository.lastCheckup` … কখনো রিসেট হতো না
> (`resetForNewPatient()` ফাংশন লেখা ছিল কিন্তু **কোথাও ডাকাই হতো না**)"*

তখন **Checkup-এর দিকটা** সারানো হয়েছিল, কিন্তু **ওষুধের তালিকাটা একই দোষ
নিয়ে রয়ে গিয়েছিল।** এবার সেটাই সারানো হলো।

---

## ২. সমাধান — দুটো আলাদা পাহারা

**১) কেন্দ্রীয় — `RoleSession.applyFrom()`**
রোগী খোলার **প্রতিটা পথ** (Doctor Queue · Chamber · Global Search ·
Timeline · Follow-up) এই এক ফাংশনটা দিয়েই যায় — যাচাই করে দেখা হয়েছে।
নতুন রোগীর আইডি এলেই তালিকা খালি।

**২) দ্বিতীয় — `PrescriptionActivity` খোলার সময়**
`ensureListsBelongTo()` আবার দেখে নেয় তালিকাটা সত্যিই **এই** রোগীর কিনা।
একটা পাহারা কোনো কারণে না চললেও অন্যটা ধরবে।

⛔ **একই রোগী হলে কিচ্ছু হয় না** — চলতি প্রেসক্রিপশন কখনো মুছবে না।
⛔ **`patientId` ফাঁকা এলেও কিছু হয় না** — হুবহু আগের আচরণ।
⛔ Prescription-এর সাথে Medicine Slip · Investigation · Diet-ও একসাথে খালি হয়
   (একই ঝুঁকি ওগুলোতেও ছিল)।
⛔ Schema · RLS · data — কিছুই ছোঁয়া হয়নি। **SQL লাগে না।**
⛔ নতুন কোনো ক্লাউড-অনুরোধ নেই।

---

## ৩. যাচাই — **আসল দুটো ফাইল compile করে চালানো হয়েছে**

`RoleSession.kt` ও `ClinicalRepository.kt`-এ Android-এর কিছু নেই বললেই চলে,
তাই **আসল ফাইল দুটোই** stub দিয়ে চালানো গেছে (নকল কপি নয়)।

**১১/১১ PASS:**

| পরীক্ষা | ফল |
|---|---|
| রোগী A-র তালিকায় ২টা ওষুধ | ✅ |
| **একই রোগী আবার খুললে চলতি কাজ অক্ষত** | ✅ |
| **ফাঁকা আইডি এলে কিছু মুছবে না** | ✅ |
| 🔴 **অন্য রোগী খুললে তালিকা খালি** | ✅ |
| তালিকার মালিক ঠিকভাবে বদলায় | ✅ |
| দ্বিতীয় পাহারা — একই রোগীতে কিছু মুছবে না | ✅ |
| দ্বিতীয় পাহারা — অন্য রোগীতে খালি করে | ✅ |
| Medicine Slip-ও একসাথে খালি হয় | ✅ |
| `reset` তালিকা ও মালিক দুটোই খালি করে | ✅ |

**পুরোনো সব suite — একটাও ভাঙেনি:**
PatientIdentity **37·0** · cham **31·0** · rep **25·0** · tt **19·0** ·
rc **19·0** · family **29·0** · rot **21·0** · futab **19·0** · gtest **13·0**

বন্ধনীর হিসাব তিন ফাইলেই `git HEAD`-এর সাথে **হুবহু এক**।

---

## ৪. ⚠️ সৎ কথা

- **আগে ভুলভাবে সেভ হয়ে যাওয়া প্রেসক্রিপশনগুলো নিজে থেকে ঠিক হবে না** —
  সেগুলো ডেটাবেসে ওভাবেই আছে। এই ঠিকটা **আজ থেকে নতুন যা হবে** তার জন্য।
  TK চাইলে পুরোনো ভুল সারিগুলো খুঁজে দেখার আলাদা কাজ করা যাবে।
- SWAPAN SARKAR-এর আজকের যে ৯-ওষুধের সারিটা সেভ হয়ে গেছে, সেটা TK-কে
  **নিজে দেখে ঠিক করে নিতে হবে**।
- Android SDK এখানে নামে না ⇒ `PrescriptionActivity` compile করা যায়নি
  (এক লাইনের ডাক; বাকি পুরো কাজটাই compile করে চালানো হয়েছে)।

---

## ৫. TK-এর করণীয়
1. **V537 বিল্ড** (নতুন SQL নেই)
2. এক রোগীর প্রেসক্রিপশন করুন → **Dashboard-এ না গিয়েই** অন্য রোগী খুলুন →
   তালিকা **ফাঁকা** আসছে কিনা
3. একই রোগীর পর্দায় বারবার ঢুকুন-বেরোন → **চলতি কাজ মুছে যাচ্ছে না** তো
4. SWAPAN SARKAR-এর আজকের ভুল প্রেসক্রিপশনটা দেখে নিন
