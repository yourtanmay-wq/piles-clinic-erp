# V247 — exact change only

- Removed the visible Module Sign-in/password dialog.
- Work Notebook, Staff Profile and Income & Expense now reuse the currently
  logged-in app user's identity and authenticate silently.
- Added one-run V247 SQL to align all configured users with the silent login.
- No Patient, Payment, Refund, Follow-up, design or existing workflow changed.
