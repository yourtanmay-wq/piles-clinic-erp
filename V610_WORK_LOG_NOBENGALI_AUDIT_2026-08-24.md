# V610 — Kishanganj Staff-এর ফোনে বাংলা যাচাই (TK-এর প্রশ্নে সরাসরি উত্তর)
**তারিখ:** ২৪.০৮.২০২৬ · **ভার্সন:** V610 / 6.10

## TK-এর প্রশ্ন
"এবার লাস্ট ফাইলটা যাচাই করুন — Kishanganj Staff-এর ফোনে এখনো কি কোনো
বাংলা আছে?"

## সৎ উত্তর — হ্যাঁ, ছিল। ৭টা জায়গায়, ধরে ঠিক করা হলো।
অটোমেটিক গার্ড ([৯.১৪]) বারবার পাশ করেছিল, কিন্তু হাতে-ধরে পুরো প্রজেক্ট
আবার যাচাই করে দেখা গেল — **গার্ড শুধু "অনুবাদ অভিধানে আছে কিনা" দেখে,
কিন্তু "স্ক্রিনে সেই অনুবাদ আসলে প্রয়োগ হচ্ছে কিনা" (NoBengali.s()/
sweep() আদৌ ডাকা হয়েছে কিনা) সেটা দেখে না।** তাই কিছু জায়গায় অনুবাদ
অভিধানে থাকা সত্ত্বেও, কোড কখনো সেটা ব্যবহারই করত না — ফলে আসল ফোনে
কাঁচা বাংলাই দেখাত।

## যেখানে পাওয়া গেল ও ঠিক করা হলো
| ফাইল | কী ছিল | Kishanganj স্টাফ কতটা প্রায়ই দেখতেন |
|---|---|---|
| **ExpectedTomorrowActivity.kt** | "কাল আসার কথা" (শিরোনাম) + "আসবেন" (প্রতিটা কার্ডে) | প্রতিবার এই স্ক্রিন খুললে |
| **PatientTimelineActivity.kt** | "⏰ আসার কথা" (বোতাম, ২ জায়গায়) | রোগীর টাইমলাইন খুললেই, নিয়মিত |
| **PaymentActivity.kt** | "Reason (কারণ)" | Refund করার সময় |
| **RegistrationActivity.kt** | "এই নম্বরে আরও X জন রোগী আছেন" | ডুপ্লিকেট নম্বর ধরা পড়লে |
| **ReportsActivity.kt** | "আজ কোনো লেনদেন নেই।" | লেনদেন-খালি দিনে রিপোর্ট দেখলে |
| **MoreMenuActivity.kt** | "💵 আয় ও ব্যয়" (মেনু-আইটেম) | More Menu খুললে |

সবগুলোই এখন `NoBengali.s()`-এ মোড়া। প্রতিটার অনুবাদ **আগে থেকেই**
অভিধানে ছিল (substring-মিল দিয়ে) — শুধু ব্যবহারই হচ্ছিল না, তাই নতুন
অনুবাদ লিখতে হয়নি।

## ExpectedTomorrowActivity-এ বাড়তি সুরক্ষা
এই স্ক্রিন RecyclerView-adapter না (সরাসরি addView) — তাই বাকি সব
তালিকার স্ক্রিনে (FollowUp/Chamber/Draft ইত্যাদি) যেমন প্রতি-সারি
`NoBengali.sweep()` ডাকা হয়, এখানে সেটা ছিলই না। এখন তালিকা আঁকা শেষে
`NoBengali.sweep(listHolder)` যোগ হয়েছে — ভবিষ্যতে কেউ ভুলে নতুন বাংলা
লিখলেও (NoBengali.s() ছাড়াই) এই স্ক্রিনে আর কখনো বাংলা দেখা যাবে না।

## ইচ্ছাকৃতভাবে বাদ (ঠিক করিনি)
**PublicSiteActivity.kt** — এটা রোগীর জন্য (patient-facing landing
page), স্টাফের পর্দা না। প্রজেক্টের লক করা নিয়ম (B17): রোগীর কাছে যাওয়া
লেখা ইচ্ছে করেই বাংলা-মুক্তির বাইরে রাখা হয় (তিন-ভাষার নিয়ম আলাদা)।

## সীমাবদ্ধতা (সৎভাবে)
এই যাচাই কোডের সব `.kt` ফাইলে raw-Bengali `text=` প্যাটার্ন খুঁজে
করা হয়েছে — XML লেআউট-ভিত্তিক Bengali (যদি কোথাও থাকে) বা runtime-এ
তৈরি হওয়া জটিল string-বিল্ডিং এই স্ক্যানের বাইরে থাকতে পারে। সন্দেহজনক
কিছু চোখে পড়লে জানাবেন, আবার যাচাই করব।

## যাচাই
| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ |
| `verify_version_json.py` | ✅ V610/6.10 |
| হাতে-ধরে regex স্ক্যান (৬ ফাইল) | ✅ সবগুলো মোড়া |
