# V605 — "পুরনো ডেটা থাকলেও Loading দেখায় কেন" — ২টা সারানো
**তারিখ:** ২৪.০৮.২০২৬ · **ভার্সন:** V605 / 6.05

## TK-এর রিপোর্ট ও নির্দেশ
"মোবাইলে পুরনো ডাটা থাকলেও প্রতিটা সেকশনে Loading দেখায় কেন?"
তারপর: "ট্যাব বদলালে ডাটা সাথে সাথে আসতে হবে, তবে ভুল ডাটা যেন না থাকে,
লোডিং সময় না লাগে — ঝুঁকিহীনভাবে করতে হবে।"

## যা যাচাই করে পাওয়া গেল (২৩টা স্ক্রিনের মধ্যে)
সৎভাবে বলতে হয় — বেশিরভাগ স্ক্রিনই (Payment, Collection, Follow-up ইত্যাদি)
আগে থেকেই ঠিক cache-first প্যাটার্নে চলে। **দুটো জায়গায় সত্যিকারের সমস্যা
পাওয়া গেছে:**

### ১. Chamber Close — নিশ্চিত বাগ, ঠিক করা হলো
`load()` কখনো ২-মিনিটের মেমরি-ক্যাশ চেক না করেই সরাসরি "Loading..." লিখে
দিত। এখন cache তাজা থাকলে সঙ্গে সঙ্গে দেখায় — নতুন `peekCached()` ফাংশন
(`ChamberUnclosedRepository`) দিয়ে, নেট-কল আগের মতোই পিছনে চলে।

### ২. Dialer — ট্যাব বদল, TK-এর আজকের নির্দেশে সংশোধিত
আগে (B490, ০৬.০৮) ইচ্ছে করেই প্রতিবার তালিকা মুছে Loading দেখানো হতো —
তখনকার আসল সমস্যা ছিল ভুল ট্যাবের ডেটা এক মুহূর্ত দেখা যাওয়া। আজ যাচাই
করে ধরা পড়ল: **All ও Missed একই ডেটা (`lastCallRows`) থেকে শুধু আলাদা
ছাঁকনি** — তাই ভুল ট্যাবের ডেটা দেখানোর কোনো পথই নেই যদি সবসময়
`currentTab` অনুযায়ী সঠিক ছাঁকনি প্রয়োগ হয় (যা আগে থেকেই হতো)। Contacts
ট্যাবেরও নিজস্ব তাৎক্ষণিক cache (B502) আগে থেকেই ছিল।

⇒ এখন ট্যাব বদলালে সেই ট্যাবের তথ্য এই সেশনে আগে অন্তত একবার লোড হয়ে
থাকলে **সঙ্গে সঙ্গে, শুধু সেই ট্যাবেরই** সঠিক তথ্য দেখায় — "Loading..."
শুধু কোনো ট্যাব এই সেশনে প্রথমবার খোলা হলেই দেখা যাবে। পিছনে গিয়ে
হালনাগাদ টানাও (`refreshCurrentTab()`) আগের মতোই চলে, ডেটা কখনো বাসি
থেকে যায় না।

## যা এখনো বাকি (TK-এর সিদ্ধান্তের অপেক্ষায়, ছোঁয়া হয়নি)
**Password Center** — এই স্ক্রিনে আসলে কোনো cache-ই নেই (একটা কমেন্টে
ভুল করে "cache-first" লেখা আছে, বাস্তবে নেই)। ছোট, কম-ব্যবহৃত স্ক্রিন —
TK আলাদা করে অনুমতি দিলে ঠিক করা হবে।

## নিরাপত্তা
- দুটো ফিক্সই শুধু **কখন Loading দেখানো হবে** তা বদলেছে — কোন ডেটা কী
  ছাঁকনিতে দেখানো হয় তার যুক্তি এক অক্ষরও বদলায়নি।
- ব্যর্থ/নেট-না-থাকা অবস্থায় আচরণ আগের মতোই (ব্যর্থ পড়া কখনো cache হয় না)।
- B490/B502/B494-এর মূল নিয়ম-কাঠামো অক্ষত, শুধু অপ্রয়োজনীয় Loading-ফ্ল্যাশ সরানো হলো।

## ওয়েব
Chamber Close ও Dialer দুটোই Android-only ফিচার — ওয়েবে সমতুল্য কিছু
নেই, তাই কিছু বদলানো হয়নি।

## যাচাই
| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ |
| `verify_version_json.py` | ✅ V605/6.05 |
| ব্রেস/প্যারেন গণনা (Kotlin-সচেতন) | ✅ মিলেছে |
