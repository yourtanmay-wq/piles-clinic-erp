# V515 — পুরো প্রজেক্টের Egress অডিট ও বাকি বড় পড়াগুলো সারানো
**তারিখ:** 22.08.2026 · **ভার্সন:** V515 / 5.15 · **ওয়েব cache:** `?v=v515`

## TK-এর নির্দেশ
> *"পুরো প্রজেক্টে Egress অডিট করুন, বাকি সব ২০০০–৫০০০ সারির বারবার-পড়া নিরাপদে
> সারান — Briefing · Trash · Reports · Draft · Doctor Visit/RMP · Follow-up ·
> Doctor Queue · Chamber · Payment/Refund ও ওয়েব। ডেটা · schema · UI · workflow ·
> permission · অনুমোদিত ফিচার কিছুই বদলাবে না। Backup/Export ইচ্ছাকৃত, থাকতে পারে।"*
> *"কোনো ভালো কাজ যেন খারাপ করবেন না। সন্দেহ হলে আগে জিজ্ঞাসা করবেন।"*

⇒ **দুটো জায়গায় সন্দেহ ছিল, TK-কে জিজ্ঞাসা করা হয়েছে, TK দুটোরই পথ বেছে
দিয়েছেন** (Trash: `record` ঘর বাদ · Doctor Queue: ছবি আলাদা করে)। বাকি সব
পরিবর্তন এমন যেগুলোয় ফেরত আসা সারি এক অক্ষরও বদলায় না।

---

## 🔎 অডিট — কোথায় কী পাওয়া গেল

প্রথমে ঠিক করা হলো **কোন পড়াগুলো পাহারার বাইরে**। V513/V514-এর পাহারা বসানো
আছে `SupabaseClient.fetchListOrNull`-এ, আর `fetchListSlim`/`fetchListSlimOrNull`
প্রথম লাইনেই ওটাকেই ডাকে। যেগুলো নিজের অনুরোধ নিজে পাঠায় — `fetchList`,
`fetchListOrNullDirect`, `findByMobile*`, আর `ModuleAuth.getRows*` — শুধু
সেগুলোই বাইরে। প্রতিটা ডাক ধরে ধরে দেখা হলো।

### ✅ আগে থেকেই পাহারায় (কিছু করার নেই — কোডে মিলিয়ে দেখা)
| জায়গা | কেন নিরাপদ |
|---|---|
| **Reports** `ReportsRepository:136–142` | `fetchListSlim` → `fetchListOrNull` ✅ (V514-এ মেপে দেখানো) |
| **Draft** `DraftRepository:368–399` | `fetchListSlimOrNull` → `fetchListOrNull` ✅ |
| **Follow-up** `FollowUpRepository:746, 883–884, 991, 1038` | সব `fetchListSlimOrNull`/`fetchListOrNull` ✅ |
| **Chamber** `ChamberAttendanceRepository:171–210, 435–475` | একই ✅ |
| **Doctor Queue (Android)** `DoctorQueueRepository:190, 400` | একই ✅ |
| **Doctor Visit বোর্ড** `DoctorVisitActivity:357, 1173, 2095–2096, 3916–3917` | একই ✅ |
| **Payment/Refund** `PaymentRepository` · `RefundedRecords` | বড়গুলো সব `…SlimOrNull`; বাকিগুলো `id=eq.…`/limit 200 — ছোট ✅ |
| **RMP** `RmpCommissionRepository` | সব `ModuleAuth.getRowsChecked`, প্রতিটাই সরু কলাম + `rmp_id=eq.…`/limit 1–200 — বড় পড়া একটাও নেই ✅ |
| **Staff Profile / Income-Expense / Notebook** | তারিখ বা কোড ধরে ছাঁকা; `staff_profiles`-এর ছবি V404-এ আগেই বাদ ✅ |

### 🔧 যা সারানো হলো (৪টে)
| # | জায়গা | আগে | এখন |
|---|---|---|---|
| ১ | **Briefing পর্দার তালিকা** `BriefingRepository.fetchRaw` | পুরো `briefings`, সব ঘর, limit 5000 — **পাহারার বাইরে**, প্রতিবার পর্দা খুললেই | একই অনুরোধ, শুধু পাহারার ভিতরে |
| ২ | **Doctor Visit / RMP তালিকা** `DoctorVisitRepository.fetchList` + `fetchListRaw` | পুরো `doctor_visits`, সব ঘর (`callHistory`সহ), limit 5000 — **পাহারার বাইরে** | একই অনুরোধ, একই `name.asc` ক্রম, শুধু পাহারার ভিতরে |
| ৩ | **Trash Bin** `TrashRepository` | প্রতিটা সারির `record`-এ মুছে ফেলা **পুরো রেকর্ড, রোগীর ছবিসহ** — অ্যাপের সবচেয়ে ভারী পড়া | তালিকায় শুধু পর্দার **১৯টা ঘর**; পুরো record আসে **শুধু Restore চাপলে**, ওই এক সারির জন্য |
| ৪ | **ওয়েব Doctor Queue** `wlv1QueueCloudPull` | প্রতি ৪৫ সেকেন্ডে ১০০০ পর্যন্ত রোগী **ছবিসহ** | তালিকা ছবি ছাড়া + ছবি **শুধু তাদেরই** যাদের ছবি এই ব্রাউজারে নেই |

### ⛔ যা ইচ্ছে করে ছোঁয়া হয়নি (সৎভাবে)
| কী | কেন |
|---|---|
| **Backup / Export** (`CloudBackup:35`, `ExportDataActivity:100`, limit 100000) | TK বলেছেন ইচ্ছাকৃত — থাকুক। দুটোতেই আগে থেকেই সতর্কবার্তা ও confirm আছে (V405) |
| **ওয়েবের ২৪ ঘণ্টার `WLV1_DELTA_OVERLAP_MS`** | ওটাই ভুল-ঘড়ির লেখা ধরার জাল। ছোট করলে **তথ্য হারানোর আসল ঝুঁকি** |
| **শেষ-ভরসার `select('*')` fallback** (app.js:1208/1386, `fetchListSlim`-এর শেষ ধাপ) | খাতার সারি **B446**-এর জাল — নেট আটকালে "খালি তালিকা / ₹0" ফেরার পথ বন্ধ রাখে |
| **`trash`-কে dedupe/revalidate-এ আনা** | V512-এর কারণ অটুট (ভারী সারি অন্যদের জমানো উত্তর ছিটকে দিত)। তাছাড়া `trash`-এ `updatedAt` ঘরই নেই, তাই সই মেলানো ওখানে চলেই না |

---

## 🔧 ১ ও ২ — কীভাবে (আচরণ শূন্য বদল)

নতুন `SupabaseClient.fetchListGuarded()` — `fetchList()`-এর **হুবহু যমজ**, শুধু
V513/V514-এর পাহারার ভিতর দিয়ে। URL এক অক্ষর আলাদা নয়, ব্যর্থতায় আগের মতোই
**খালি তালিকা** (`null` নয়)। পুরোনো `fetchList()` **এক অক্ষরও বদলায়নি** — তার
৬০+ ডাকের একটাও ছোঁয়া হয়নি, শুধু এই দুটো বড় ও বারবার-চলা জায়গা সরানো হয়েছে।

**সেই সঙ্গে একটা সুরক্ষা যোগ হলো:** `CloudListRevalidate`-এ এখন **একটা তালিকা
একাই জায়গা দখল করতে পারে না** (`MAX_ONE_BYTES` = ২ MB)। V512-এ ঠিক এই ভুলটাই
ধরা পড়েছিল — একটা ভারী তালিকা জমা রাখলে বাকি সব পর্দার জমানো উত্তর ছিটকে যেত
ও Egress **বাড়ত**। ২ MB-র বেশি হলে জমাই হয় না (তখন আচরণ হুবহু পুরনো)।

## 🔧 ৩ — Trash Bin (TK-এর বাছাই)

`trash` সারির ঘর ছয়টা: `id · table · record · deletedAt · deletedBy ·
cascadedFollowups`। ভারী জিনিসটা `record` — মুছে ফেলা **পুরো** রেকর্ড, ছবিসহ।

**পর্দা `record` থেকে সত্যিই কী পড়ে?** চারটে ফাইলের প্রতিটা লাইন মিলিয়ে দেখা
হয়েছে — `TrashCardText` · `TrashAdapter` · `TrashBinActivity` · `TrashRepository`
— মোট **১৯টা ঘর** (`name · mobile · altMobile · branch · patientId · patientCode ·
bill · amount · date · disease · mode · nextFollow · payType · receivedBy ·
refBy · registrationDate · stage · status · id`)। **ছবি ওদের একটাও নয়।**

তাই তালিকা এখন `record->>name` ধরনের jsonb-পথে শুধু ওই ১৯টা ঘর চায়
(`record` ঘরটা `jsonb` — DB_SETUP লাইন ২০৫-এ প্রমাণ)। `cascadedFollowups`
তালিকাতেই থাকে (ওতে শুধু `{id,status,table}` — হালকা)।

**⛔ Restore আগের মতোই পুরো আসল রেকর্ড দিয়েই হয়** — Restore চাপার মুহূর্তে ওই
**একটি** সারির পুরো `record` আনা হয়। আনতে না পারলে **কিছুই লেখা হয় না**,
`false` ফেরে, trash সারি অক্ষত (V223 §C1-এর নিয়ম অটুট)।
**⛔ পুরনো জমানো কপিতে (SharedPreferences) পুরো `record` আছে** ⇒ সেগুলো আগের
মতোই, একটাও বাড়তি অনুরোধ ছাড়াই Restore হয়।
**⛔ সরু পড়া ব্যর্থ হলে হুবহু আগের পুরো পড়াটাই চলে** — পর্দা কখনো ফাঁকা নয়।

## 🔧 ৪ — ওয়েব Doctor Queue (TK-এর বাছাই)

Chamber/Payment-এর মতো এখানে ছবি বাদ দিয়ে দিলেই হয় না — এই পর্দার কার্ড
(`wlv1DqCard`) সত্যিই ছবি দেখায়। তাই **দুই ধাপ:** তালিকা আসে ছবি ছাড়া, আর ছবি
আনা হয় **শুধু তাদেরই** যাদের ছবি এই ব্রাউজারে জমা নেই (২০০ করে ভাগে, ঠিক
V405-এর প্রমাণিত `.in('id',…)` ছাঁচে)।
⇒ প্রথমবার খরচ আগের মতোই; **তারপর থেকে প্রায় শূন্য**।

**⛔ এই যন্ত্রে সদ্য তোলা, এখনো না-পাঠানো ছবি কখনো চাপা পড়ে না** (ছবি থাকলে
সে তালিকাতেই ওঠে না)। **⛔ শুধু `photo` ঘরটাই বসে, শুধু আগে থেকে থাকা সারিতে**
— একটাও সারি যোগ/বাদ/বদল হয় না, আপলোডও হয় না। **⛔ ছবি আনা ব্যর্থ হলে** কার্ড
আগের মতোই 👤 দেখায় ও পরের চক্রে আবার চেষ্টা হয় — কিছুই ভাঙে না।

---

## ✅ যাচাই — ১১১টি পরীক্ষা, ০ ব্যর্থ

kotlinc 2.0.21 + **আসল** okhttp 4.12 ও org.json দিয়ে সত্যিই কম্পাইল ও চালানো।

| পরীক্ষা | ফল |
|---|---|
| কম্পাইল — `SupabaseClient` · `CloudListRevalidate` · `TrashRepository` | **০ error** |
| revalidation-এর আচরণ | **১৭/১৭** |
| Trash Bin (তালিকা + Restore-এর নিরাপত্তা) | **২৩/২৩** |
| এক-টেবিলের integration (বাইট মেপে) | **১৩/১৩** |
| Reports + Draft বোর্ড | **১৮/১৮** |
| Briefing + Doctor Visit/RMP পর্দা | **১৪/১৪** |
| ওয়েব `node --check` | **৮/৮ ফাইল** |
| ওয়েব — সরু সারিতে জমা ছবি টেকে | **১০/১০** |
| ওয়েব — Doctor Queue-র দুই-ধাপ | **১৬/১৬** |

### মেপে দেখা সাশ্রয় (নকল সার্ভার, আসল কোড)
| পর্দা | আগে | পরে (কিছু না বদলালে) |
|---|---|---|
| **Trash Bin** (৪০০ সারি) | **২৩,৮৪০ KB** | **২০৯ KB** — ১১৪× ছোট |
| **ওয়েব Doctor Queue** (৩০০ রোগী, দ্বিতীয়বার) | **১৩,৩৭০ KB** | **১৭৩ KB** |
| **Briefing পর্দা** (৭০০ নোটিশ) | ১,২৬৮ KB | **০ বাইট** |
| **Doctor Visit/RMP** (৭৪৫ ডাক্তার) | ৮৩৬ KB | **০ বাইট** |
| Reports বোর্ড | ৬০১ KB | **০ বাইট** (৪ টেবিলে মোট ১৫২ বাইট সই) |
| Draft বোর্ড | ৮৭৯ KB | **০ বাইট** |

### গুরুত্বপূর্ণ যেগুলো আলাদা করে প্রমাণ করা হয়েছে
- Trash: Restore-এ লেখা সারিতে **ছবি ঠিকই ফিরেছে**, তালিকা যে ঘরগুলো কখনো
  চায়নি সেগুলোও ফিরেছে — **কিছুই হারায়নি**
- Trash: record আনা ব্যর্থ হলে **একটা অক্ষরও লেখা হয়নি**
- Trash: পুরনো জমানো কপি **বাড়তি অনুরোধ ছাড়াই** Restore হয়
- Trash: ১৯টা ঘরের **একটাও বাদ যায়নি**, লেবেল ও ব্রাঞ্চ-ছাঁকনি অটুট
- Doctor Queue: নতুন রোগীর **ছবি ঠিকই আসে**; না-পাঠানো স্থানীয় ছবি **টিকে থাকে**;
  ছবি আনা ব্যর্থ হলেও সারি আসে ও কিছু ভাঙে না; সারি হারায় না, দুবারও হয় না
- Doctor Visit: `name.asc` ক্রম **হুবহু** এক (২০২৬-০৭-২৫-এর "তালিকা লাফায়" বাগ ফেরেনি)
- Briefing: নোটিশের **পুরো লেখা ও উত্তর** আগের মতোই আসে
- Reports/Draft: **শুধু বদলানো টেবিলটাই** নামে, বাকি তিনটে ০ বাইট

---

## ⚠️ যা আগের মতোই সৎভাবে জানানো থাকল
সইয়ের `updatedAt` ফোনের ঘড়ি থেকে আসে। কোনো ফোনের ঘড়ি **পিছিয়ে** থাকলে, আর
ঠিক তখন টেবিলে আর কোনো যোগ/মোছাও না হলে, সই না-ও বদলাতে পারে। তাই জমানো উত্তর
**১০ মিনিট** পরে নিঃশর্তে বাতিল। নিজের সেভের পরে সঙ্গে সঙ্গেই সব ভুলে যাওয়া হয়।

## ⚠️ TK-এর করণীয়
1. **V515 বিল্ড করে APK প্রতিটা স্টাফকে পাঠানো**
2. Netlify-তে `03_NETLIFY_READY` আপলোড (cache `?v=v515`)
3. **Trash Bin খুলে একবার Restore করে দেখা** ← এবার সবচেয়ে বেশি বদলেছে এখানেই
4. ১–২ দিন পরে Egress-এর স্ক্রিনশট

⛔ **কোনো SQL চালাতে হবে না** — ডেটাবেসে একটা অক্ষরও বদলায়নি।
