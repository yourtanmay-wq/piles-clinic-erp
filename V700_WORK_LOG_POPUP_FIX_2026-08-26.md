# V700 — V699-এর তিনটে ভুল সারানো

**তারিখ:** ২৬.০৮.২০২৬ (IST)
**TK-এর রিপোর্ট (ছবিসহ):** *"এটা কি ধরনের ফাজলামো"*

**TK ঠিক বলেছেন। V699-এ আমার তিনটে ভুল ছিল — নিচে প্রতিটার আসল কারণ।**

---

## ১. 🔴 শিরোনাম দুবার দেখাচ্ছিল

সবুজ হেডারে "Doctor Note & Reminder", আর ঠিক তার নিচে কার্ডের নিজের
শিরোনামও। লুকানোর কোড লেখা ছিল, তবু কাজ করেনি।

**আসল কারণ:** কোডের ক্রম ভুল ছিল —

```
originalParent.removeView(card)          ← কার্ডটা পর্দা থেকে খুলে নেওয়া হয়
val headRow1 = findViewById(...)         ← তারপর Activity-তে খোঁজা হয়
```

কার্ডটা খুলে নেওয়ার পরে Activity-র গাছে ওই ঘরগুলো **আর থাকেই না**, তাই
`findViewById` **null** ফেরাত আর `?.` -এর জন্য চুপচাপ কিছুই হত না।

**ঠিক:** কার্ডের নিজের ভিতরে খোঁজা — `card.findViewById(...)`।

---

## ২. 🔴🔴 Save চাপলে অ্যাপ বন্ধ হয়ে যেত (crash)

**এটাই সবচেয়ে বড় ভুল ছিল।** ঠিক একই ফাঁদ `saveDoctorReminderNow()`-এও —
পপ-আপ খোলা মানে কার্ডটা Activity থেকে খোলা, তাই

```
findViewById<EditText>(R.id.etDoctorReminderNote).text   ← null.text ⇒ crash
```

TK Save চাপলে অ্যাপ বন্ধ হয়ে যেত। **ভাগ্য ভালো TK আগে চেহারাটাই ধরেছেন।**

**ঠিক:** কার্ড থেকেই নেওয়া হয়, আর না পেলে crash নয় — ভদ্র বার্তা।

---

## ৩. 🔴 আইকন লেখার উপরে বসে গিয়েছিল

**আসল কারণ:** আইকনটা ঘরের **ব্যাকগ্রাউন্ডে** (layer-list) আঁকা ছিল, আর
লেখাটা ডানে সরানোর জন্য দেওয়া `paddingStart` স্টাইলের `android:padding`-এর
সাথে লড়ে হেরে যাচ্ছিল — ফলে লেখা একদম বাঁ প্রান্ত থেকেই শুরু হত, আইকনের গায়ে।

**ঠিক:** আইকন আর ব্যাকগ্রাউন্ডে নয় — **নিজের `ImageView`**-তে, লেখার পাশে।
দুটো আলাদা ঘর বলে গায়ে লাগার সুযোগই নেই। চাপ ধরার কাজটা এখন **বাইরের
বাক্স** করে, তাই আইকনের উপরে চাপ দিলেও ক্যালেন্ডার/ঘড়ি খোলে।

---

## ৪. আমার শিক্ষা

তিনটের মধ্যে **দুটোই** একই ভুল থেকে — কার্ডটা পর্দা থেকে খুলে নেওয়ার পরে
Activity-তে ঘর খোঁজা। পাহারাদার এটা ধরতে পারে না (কোড ঠিকই কম্পাইল হয়),
আর আমি চোখে দেখে যাচাই করিনি। **ভুলটা আমার।**

---

## ৫. ⛔ যা ছোঁয়া হয়নি

- তিনটে ঘরের id · hint · লেখা — আগের মতোই।
- মূল SAVE-এর কোড · V671-এর সরিয়ে-এনে-ফেরানোর নিয়ম — অক্ষত।
- 💰 টাকা · হিসাব — কিছুই নয়।
- বাড়তি বানানো `bg_input_field_date/time.xml` দুটো **মুছে ফেলা হয়েছে**
  (আর দরকার নেই), আসল `bg_input_field_picker` আগের মতোই।

---

## ৬. যাচাই

| যাচাই | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ (V700) |
| `verify_kotlin_compile.py` | ✅ PASS — নতুন ভুল ০ |

---

## 🔴 TK একবার দেখে নেবেন

- [ ] পপ-আপে শিরোনাম **একবারই** (শুধু সবুজ হেডারে)
- [ ] আইকন ও লেখা **আলাদা**, একটার উপরে আরেকটা নয়
- [ ] তারিখ/সময়ের বাক্সে চাপলে (আইকনের উপরেও) ক্যালেন্ডার/ঘড়ি খোলে
- [ ] লিখে **💾 Save** → অ্যাপ বন্ধ হয় না, "মনে করানোর নোট সেভ হয়েছে"
- [ ] Close চাপলে কার্ডটা নিজের জায়গায় ফিরেছে, শিরোনাম আবার দেখা যাচ্ছে
