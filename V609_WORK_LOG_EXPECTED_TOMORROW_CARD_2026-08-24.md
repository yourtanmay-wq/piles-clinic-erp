# V609 — "কাল আসার কথা" কার্ড: ডিজাইন মেলানো + নাম/নম্বর আলাদা-আলাদা কাজ
**তারিখ:** ২৪.০৮.২০২৬ · **ভার্সন:** V609 / 6.09

## TK-এর নির্দেশ (ধাপে ধাপে অনুমোদন)
1. Follow-up কার্ডের একই বাইরের ডিজাইন (গোল কোণা + নীল ডোরা) — ছবি-প্রুফ পাশ।
2. Remarks/Address/Disease — যাচাই করে দেখানো হলো **আগে থেকেই ছিল**, কাজ লাগেনি।
3. Last Call — শুধু **তারিখ** (কে করেছিলেন তা বাদ, ডেটা বাঁচাতে TK-এর নির্দেশে) — ছবি-প্রুফ পাশ।
4. নামে চাপ → View History, নম্বরে চাপ → সরাসরি কল, লং-প্রেস → কপি (আগের মতোই)।

## যা করা হলো
### কার্ড-শেল
`ExpectedTomorrowActivity.addRow()`-এর `card` background `Color.WHITE` থেকে
`bg_follow_card` (Follow-up কার্ডের হুবহু একই drawable) — শুধু বাইরের রং/
কোণা/ডোরা, ভেতরের কিছুই বদলায়নি।

### Last Call তারিখ (নিরাপদ অংশটুকু, "কে" বাদ)
- `ExpectedItem`-এ নতুন `lastCallDate` ঘর।
- একই bulk `followups` কল-এ (যেটা আগে থেকেই disease/address/lastRemark
  টানত, ব্রাঞ্চ-প্রতি একটাই কল) `lastCallDate` কলাম যোগ — **নতুন fetch
  নেই**, শুধু বিদ্যমান কলের select-এ একটা ছোট text-কলাম বাড়ল।
- **⛔ ইচ্ছে করে বাদ:** "কে করেছিলেন" (যেমন "(JPE-CRP)") দেখাতে হলে
  `history` (jsonb, ভারী) টানতে হতো — TK স্পষ্ট বলেছেন "ডেটা টানলে বাদ
  দিন", তাই শুধু হালকা তারিখ-কলামটুকুই যোগ হয়েছে।
- Cache-এ (SharedPreferences) পড়া/লেখা দুই জায়গাতেই `lastCallDate`
  যোগ, নইলে তাৎক্ষণিক cache-দেখানোর সময় তারিখটা হারিয়ে যেত।

### নাম/নম্বর আলাদা-আলাদা কাজ
আগে পুরো নাম+নম্বর অংশে একটাই ক্লিক ছিল (Follow-up-এ গিয়ে কার্ড হাইলাইট)।
এখন:
- **নামে চাপ** → সরাসরি Patient Timeline (View History), Follow-up-এর
  `openTimelineFor()`-এর হুবহু একই প্যাটার্নে (mobile + preName/preDisease/
  preAddress সহ, তাৎক্ষণিক header আঁকার জন্য)।
- **নম্বরে চাপ** → সরাসরি কল, নিচের "📞 Call" বোতামের **হুবহু একই কোড-পথ**
  (দুই জায়গায় আচরণ কখনো আলাদা হবে না)।
- **লং-প্রেস কপি** (V474) দুটোতেই অক্ষত, একটুও বদলায়নি।

## নিরাপত্তা
- V509-এর egress-সুরক্ষা অক্ষত — কোনো নতুন network round-trip নেই, শুধু
  বিদ্যমান bulk-কলের select-list-এ একটা হালকা কলাম যোগ।
- Follow-up screen-এর `focusCardMobile` হাইলাইট-পথ ছোঁয়া হয়নি (অন্য
  কোথাও থেকে এখনো ব্যবহৃত থাকলে অক্ষত)।

## যাচাই
| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ (দুইবার — আচরণ-বদল ও ডিজাইন-বদল আলাদা ধাপে) |
| `verify_version_json.py` | ✅ V609/6.09 |

## ওয়েব
"কাল আসার কথা"-র মতো আলাদা তালিকা-স্ক্রিন ওয়েবে নেই (এই তথ্য শুধু
রোগীর টাইমলাইনে দেখা যায়) — তুলনীয় কিছু নেই, ছোঁয়া হয়নি।
