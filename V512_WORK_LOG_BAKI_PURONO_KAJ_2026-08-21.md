# V512 — বাকি পুরোনো কাজগুলো (২১.০৮.২০২৬)

TK-এর নির্দেশ: *"বাকি পুরোনো কাজগুলো ধরুন / তবে খুব সাবধান এবং সততার সাথে /
কোন ভালো কাজ যেন খারাপ না / আন্দাজে কিছু করবেন না কোন কাজ"*

**Android Studio Build pending — owner will build.**

---

## ১) 🔴 Trash Bin — নেট খারাপ হলে "Trash empty" দেখাত (সবচেয়ে গুরুতর)

**কারণ (কোড ধরে প্রমাণিত):** `TrashRepository.fetchTrashRaw()` ব্যবহার করত
`SupabaseClient.fetchList()`, আর সেটা **ব্যর্থ হলেও ফাঁকা তালিকা `[]`** ফেরায়
(`if (!response.isSuccessful) return JSONArray()` / `catch { JSONArray() }`)।
ফলে `TrashBinActivity`-র `if (rawRows == null)` পাহারাটা **কখনোই** চালু হতো না —

* ফোনের জমানো কপির উপরে `"[]"` লেখা হয়ে যেত (পরের বারও ফাঁকা), আর
* পর্দায় বসত **"Trash empty"** — অথচ একটাও রেকর্ড মোছা হয়নি।

**সংশোধন**

* `SupabaseClient.fetchListOrNullDirect()` — নতুন, `fetchListOrNull`-এর যমজ
  কিন্তু **`CloudReadDedupe` ছাড়া** (কারণ trash সারিতে রোগীর base64 ছবিসহ
  পুরো রেকর্ড থাকে; ওটা dedupe-এ ৬০ সেকেন্ড রাখলে অন্য পর্দার জমানো উত্তর
  ছিটকে যেত ⇒ **Egress বাড়ত**)।
* `TrashRepository.fetchTrashRawOrNull()` — নতুন, ওটাই ব্যবহার করে।
  পুরোনো `fetchTrashRaw()` **এক অক্ষরও বদলায়নি**।
* ব্যর্থ হলে: জমানো তালিকা পর্দায় থাকে + `Offline - showing this phone's
  saved copy`; জমানো কপিও না থাকলে `Could not load Trash - check internet
  and pull down to retry` (আর কখনো "Trash empty" নয়)।
* **নতুন তালা** — `cloudVerified`: শেষ পড়াটা সফল না হলে **Restore ও Delete
  Forever দুটোই আটকে থাকে** (একটা, এবং একসাথে অনেকগুলো — চারটে জায়গাতেই)।
  কারণ জমানো তালিকায় এমন সারি থাকতে পারে যেটা ইতিমধ্যে **চিরতরে মোছা**;
  ওতে Restore চাপলে `upsertRestoreSafe` রেকর্ডটা **আবার বসিয়ে দিত**।
  ওয়েব অ্যাপে ঠিক এই নিয়মই আগে থেকেই আছে — এখন ফোনও এক।
  ব্যর্থ পড়ায় আগের বাছাগুলোও (`picked`) ছেড়ে দেওয়া হয়।
* 👁 View ও খোঁজা আগের মতোই চলে।

⛔ ওয়েবে এই ভুল ছিল না (`wlv1RefreshTrashFromCloud` আগেই `r.error` দেখে) —
তাই ওয়েবে কিছু বদলানো হয়নি।

---

## ২) Note পপ-আপ থেকে Remark সংশোধন

**কারণ:** সংশোধনের ব্যবস্থা আগে থেকেই ছিল — সারিতে **৩ বার চাপলে**
`editEnquiryHistoryNote()`। কিন্তু ঐ শোনার ব্যবস্থাটা বসানো **পুরো সারিতে**,
আর Note ঘরটা নিজেই `isClickable = true` — তাই Note-এর **লেখার উপরে** চাপ
দিলে ক্লিকটা ওখানেই আটকে যেত, ৩-ট্যাপ পর্যন্ত পৌঁছাত না।

**সংশোধন (Android):** Note পপ-আপে **✏️ Edit** বোতাম, যেটা **ঐ একই পুরোনো
ফাংশনই** ডাকে। শর্তটা ৩-ট্যাপের শর্তের হুবহু নকল
(`e.paymentId == null && (enquiryRowId != null || followUpHistoryId != null …)`)
— তাই কে বদলাতে পারবেন সেই নিয়ম **এক অক্ষরও বদলায়নি**।
Doctor Checkup · Medicine Slip · Prescription আলাদা পর্দায় যায় ⇒ সেখানে
বোতাম নেই (V327-এর "শুধু দেখার" লক অটুট)। ৩-ট্যাপও আগের মতোই আছে।

**সংশোধন (ওয়েব):** Timeline-এর সারিতে ছোট **✏️ Edit**।
সারিতে এখন `_eid` / `_fid` + `_hidx` চিহ্ন থাকে।
সেভ হয় অ্যাপের **নিজস্ব `upd()`** পথে — কারণ হাতে `save()` করলে `updatedAt`
পুরোনোই থেকে যেত, আর `mergeForCloudPush()` (rowStamp তুলনা) ক্লাউডের পুরোনো
লেখাটাকেই জিতিয়ে দিত ⇒ **সংশোধন নিঃশব্দে ফিরে যেত**। (এই ভুলটা প্রথম
লেখাতেই ছিল, নিজের কাজ আবার যাচাই করতে গিয়ে ধরা পড়ে সংশোধন করা হয়েছে।)
`lastRemark` শুধু **শেষ** রিমার্কের বেলাতেই বদলায় — ফোনের নিয়মের হুবহু।

---

## ৩) ব্যানারের সংখ্যা = ভিতরের তালিকার সংখ্যা (ব্রাঞ্চ)

V511-এ **তারিখের** নিয়ম মেলানো হয়েছিল; **ব্রাঞ্চের** নিয়ম তখনো আলাদা ছিল:

* ব্যানার গুনত `session.branch` ধরে — মাস্টারের ওটা আক্ষরিক **"All"**
  (StaffDirectory.kt:26) ⇒ **পাঁচ ব্রাঞ্চ**।
* ব্যানারে চাপ দিলে যে পর্দা খোলে সেটা মাস্টারের **বেছে রাখা** ব্রাঞ্চ ধরে
  (`BranchFilterStore`) ⇒ সংখ্যা কখনো মিলত না।
* JPE-CRP স্টাফের বেলায় উল্টো — ভিতরে Falakata+Birpara-ও দেখাত (V453)।

**সংশোধন:** ব্যানার এখন `FollowUpActivity.effectiveBranch()`-এর হুবহু একই
নিয়ম ব্যবহার করে (`bannerBranch`), আর ফাঁক-হিসাবের চাবিতেও ওটাই থাকে।

**TK-এর সিদ্ধান্ত (২১.০৮.২০২৬, জিজ্ঞাসা করে নেওয়া):** *"শুধু বেছে রাখা
ব্রাঞ্চ"*।
⚠️ **সৎ সীমা:** তাই বেছে রাখা ব্রাঞ্চে আজ ০ হলে ব্যানারটা উঠবে না, যদিও
অন্য ব্রাঞ্চে কল বাকি থাকতে পারে। TK এটা জেনেই বেছেছেন।
⚠️ মাস্টার একবারও ব্রাঞ্চ না বাছলে (`""`) — আগের মতোই পাঁচ ব্রাঞ্চ গোনা হয়
(`branchScopeFilter` ফাঁকা মানে "ছাঁকনি নেই"), কিন্তু চাপ দিলে পর্দা
"Branch বাছুন" বলে। প্রথমবার ব্রাঞ্চ বাছলেই এটা মিটে যায়।

---

## ৪) Doctor Visit-এর তালিকা "লাফ দেয়" (flicker)

**কারণ:** পর্দা একবার খুললে তালিকা **দুবার** আঁকা হয় (জমানো কপি → ক্লাউড)।
জমানো কপিতে `referredCount` **কখনোই থাকে না** — `DoctorVisitModel.parse()`
ওটা ভরতে পারে না (মডেলের ৪০–৪৬ লাইনে নিজেই লেখা)। তাই প্রথম আঁকায়
"👥 N PATIENTS REFERRED" লাইনটা লুকোনো, দ্বিতীয় আঁকায় **হঠাৎ যোগ হয়** —
কার্ড লম্বা হয়ে নিচের সব কার্ড ঠেলে নামায়।

**সংশোধন:** সফল গোনার পরে সংখ্যাগুলোও (`id → সংখ্যা`, ছোট JSON) ফোনে জমা
থাকে; জমানো কপি দেখানোর সময় বসে যায়। **একটাও নতুন ক্লাউড-অনুরোধ নেই।**
দুটো পাহারা: (ক) **সব সংখ্যা ০ হলে জমা হয় না** — কারণ গোনার ভিতরের ধাপটা
নেট খারাপ হলে চুপচাপ ফাঁকা ফেরায়, তখন ভুল ০ জমা হতো; (খ) মাঝপথে ব্রাঞ্চ
বদলালে আগের ব্রাঞ্চের সংখ্যা নতুন ঘরে বসে না (`keyAtStart`)।

⚠️ **সৎ সীমা:** এই আপডেটের পরে **প্রথমবার** পর্দা খুললে জমানো সংখ্যা নেই,
তাই ঐ একবার আগের মতোই লাফ দেবে। দ্বিতীয়বার থেকে নয়।
⚠️ ক্লাউড পড়া ব্যর্থ হলে **শেষবার জানা** সংখ্যাটা পর্দায় থাকবে (আগে ০ থাকত)।
এটা শুধু দেখানোর সংখ্যা — টাকার কোনো হিসাবে ব্যবহার হয় না।

---

## ৫) বাংলা — "বাংলা" বোতামে লেখা উঠত "English"

**কারণ:** `NoBengali.kt`-এ একই চাবি দুই জায়গায় দুই মানে —
WHOLE-এ `"বাংলা" to "English"` (৩৮৪), MAP-এ `"বাংলা" to "Bengali"` (১০১০)।
`res/layout/activity_disease_detail.xml:43`-এর ভাষা-বোতামের লেখা হুবহু
"বাংলা" ⇒ বাংলা-বন্ধ স্টাফের পর্দায় বসত **English · हिन्दी · English** —
একই নামে দুটো বোতাম, আর "English"-এ চাপলে বাংলা লেখা খুলত।
ওয়েবেও একই (`app.js` publicEdu পাতার তিনটে ভাষা-ভাগ)।

**সংশোধন:** দুই জায়গাতেই এখন **"Bengali"**। সঙ্গে —

* `"বাংলা  (Bengali)"` / `"বাংলা (Bengali)"` হুবহু-মিলের সারি (ওয়েবে আগেই ছিল)।
* `clinical/PrescriptionActivity.kt`-এর দুটো পপ-আপ — ওগুলো `native/`+`modules/`-এর
  বাইরে বলে পাহারাদার ৯.১৪ কখনো দেখেনি, আর PremiumAlert দিয়ে আঁকা হয় না বলে
  বাংলা-বন্ধ স্টাফের পর্দাতেও বাংলাতেই থাকত। এখন `NoBengali.installDialog()`
  + তিনটে লেখার ইংরেজি যোগ করা হলো। (বাংলা-বন্ধ না থাকলে `installDialog()`
  প্রথম লাইনেই ফিরে যায় ⇒ বাকি সবার জন্য এক অক্ষরও বদলায়নি।)
* ⛔ **কোনো সারি মোছা হয়নি** (TK-এর নিয়ম)।

⚠️ **সৎ কথা:** পুরো অ্যাপকে ইংরেজি করা হয়নি — সেটা TK চাননি। `NoBengali`
ব্যবস্থাটা শুধু কিশনগঞ্জের একজন স্টাফের (মোবাইল 6207841890 / KNE-KISHAN5)
জন্য চালু হয়; বাকি সবার পর্দা আগের মতোই।

---

## যাচাই (২১.০৮.২০২৬)

| যাচাই | ফল |
|---|---|
| Kotlin compile (kotlinc) | **PASS — নতুন ভুল ০** (বেসলাইন ২৮৯) |
| `node --check` × ৮ ওয়েব ফাইল | OK |
| ১০টি পুরোনো গার্ড | সব PASS |
| নতুন `verify_v512_fixes.py` | PASS |
| গার্ডে ইচ্ছাকৃত ১৯টা ভুল বসিয়ে পরীক্ষা | **১৯/১৯ ধরা পড়েছে** |
| প্রতিকূল (adversarial) পুনঃপর্যালোচনা | ৪টি আসল সমস্যা ধরা পড়ে — সবগুলোই সংশোধন করা হয়েছে (উপরে লেখা) |

**Android Studio Build pending — owner will build.**

---

## ৬) 🔴 ওয়েবে OUT TIME হারিয়ে যাওয়া (ফোনের V511-এর জোড়া)

**কারণ (কোড ধরে প্রমাণিত):** `notebook.js`-এর পুরোনো `loadDay()` ধরে নিত পড়া
ব্যর্থ হলে **throw** হবে, তাই জমানো কপি পড়ার কাজটা `catch`-এ ছিল। কিন্তু
supabase-js ভুলে throw করে না — `{ data: null, error: {...} }` ফেরায়
(অ্যাপের বাকি সব জায়গাতেই `r.error` স্পষ্ট করে দেখা হয়: `MOD.save`,
`MOD.flushQueue`, `wlv1RefreshTrashFromCloud` …)। ফলে `catch` কখনো চলত না,
`renderToday()` একটা **ফাঁকা দিন** বানাত ⇒ `inSet`/`outSet` দুটোই `false` ⇒
**OUT TIME বোতাম ধূসর**। স্টাফের অভিযোগটা সত্যি ছিল।

**সংশোধন**

* `r.error` স্পষ্ট করে দেখা হয়; ক্লাউড থেকে আসা সারি ফোনেও জমা থাকে।
* ব্যর্থ হলে জমানো কপি দেখায় + "📴 Offline - showing this phone's saved copy"।
* ক্লাউডে সারি নেই অথচ ফোনে আজকের IN/OUT আছে — **শুধু তখনই** জমানো কপি
  মানা হয় যখন প্রমাণ আছে যে সেভটা এখনো পাঠানোর অপেক্ষায় (`MOD.queueWrite`)।
  নইলে সারিটা ইচ্ছে করে মোছা হয়েছিল ধরে নেওয়া হয় (মুছে ফেলা সারি যেন
  ফিরে না আসে)।
* **চাবিতে স্টাফের কোড** — `nb_<কোড>_<তারিখ>`, আর ভিতরের `staff_code`/
  `work_date` মিলিয়েও দেখা হয়।

## ৭) ওয়েবে ব্যানারে চাপ দিলে তিন ভাগ এক পর্দায়

আগে ব্যানার তিন ভাগের সংখ্যা যোগ করে দেখাত, কিন্তু চাপ দিলে **একটা ভাগ**
খুলত। এখন ফোনের `loadTodayAllSections()`-এর মতোই তিনটে মিশে এক পর্দায়, আর
ব্যানারের সংখ্যাও **ঠিক সেই একই তালিকা** (`wlv1TodayCallRows`) থেকেই আসে।

এই কাজ করতে গিয়ে **দুটো পুরোনো ভুলও ধরা পড়ল ও ঠিক হলো**:

* `wlv1TodayCallCount()` গুনত `followStats()` ধরে, আর ওটা **আগে বেছে রাখা
  তারিখ-ছাঁকনিটাই** বসায়। কেউ "Overdue" বেছে রেখে ড্যাশবোর্ডে ফিরলে আজকের
  কলগুলো ঐ ছাঁকনিতেই বাদ পড়ত ⇒ **ব্যানার ০ দেখাত, কল বাকি থাকলেও।**
* ব্যানার মাস্টারের বেছে রাখা ব্রাঞ্চ মানত না (`wlv1BranchGate` শুধু তালিকায়
  বসত) ⇒ সংখ্যা ও তালিকা মিলত না।

⛔ ট্যাব বা ছাঁকনিতে চাপলে মিশ্র-মোড শেষ; কল/রিমার্ক সেভ করলে **থেকে যায়**।
⛔ মিশ্র পর্দায় ⬇ Sheet-ও তিন ভাগই নামায়।

---

## প্রতিকূল পুনঃপর্যালোচনায় (২য় দফা) ধরা পড়া ও সংশোধিত ভুল

| # | কী ধরা পড়ল | সংশোধন |
|---|---|---|
| ১ | 🔴 `nb_<তারিখ>` চাবিতে স্টাফের কোড ছিল না — একই কম্পিউটারে একজনের পরে আরেকজন লগইন করলে **আগেরজনের IN/OUT TIME** দেখাত, নিজের হাজিরা দেওয়া যেত না, আর সেভ করলে আগেরজনের সারিতে লেখা যেত | চাবিতে কোড + ভিতরের `staff_code`/`work_date` মিলিয়ে দেখা |
| ২ | 🔴 তথ্য না এলে পর্দা **আটকে** দেওয়া হচ্ছিল — ওয়েবে সেভ queue-এ জমা হয় বলে এতে স্টাফ অফলাইনে **IN TIME দিতেই পারতেন না** (আগে পারতেন) | পর্দা আর আটকায় না; সৎ সতর্কবার্তা + 🔄 Try again |
| ৩ | 🟠 ব্যানারের গোনায় পাহারা তিন ভাগের **বাইরে** ছিল — একটা ভাগে একটা গোলমেলে সারি থাকলেই সব কল অদৃশ্য | প্রতিটা ভাগের ভিতরে আলাদা পাহারা |
| ৪ | 🟠 একটা কল লগ করলেই (`updateFollowAction` → `followup()`) মিশ্র তালিকা ভেঙে এক ভাগে নেমে যেত | ট্যাবে চাপলে তবেই নেভে (`wlv1FuTab`) |
| ৫ | 🟡 Follow-up ছেড়ে ফিরে এসে টাইপ করলে **ছাঁকনি-ছাড়া** তিন ভাগের পুরো তালিকা বসত | `page()`-এ তারিখ-ছাঁকনির সঙ্গে চিহ্নটাও নেভে |
| ৬ | 🟡 গোনা "গেট→dedupe", পর্দা "dedupe→গেট" — তাই সংখ্যা এখনো আলাদা হতে পারত | পর্দাও এখন ঐ একই ফাংশন (`wlv1TodayCallRows`) ব্যবহার করে |
| ৭ | 🟡 ক্লাউডে মুছে ফেলা সারি জমানো কপি থেকে ফিরে আসতে পারত | শুধু "সেভ এখনো অপেক্ষায়" প্রমাণ থাকলেই জমানো কপি |
| ৮ | 🟢 ক্লাউডের পুরোনো কপি দিয়ে ফোনের নতুন কপি মুছে যেতে পারত | `updated_at` মিলিয়ে তবেই লেখা হয় |

⚠️ **সৎ সীমা:** ফোনে তথ্য না এলে পর্দা আটকায়ই (V478-এর নিয়ম — ফোনে সেভ
ক্লাউডে না বসলে কাজ এগোতে দেওয়া হয় না), ওয়েবে আটকায় না (ওয়েবে queue আছে)।
দুই জায়গায় নিয়ম আলাদা, কিন্তু **দুটোই নিজের নিজের ব্যবস্থার সাথে সঙ্গতিপূর্ণ**।
⚠️ মাস্টার একবারও ব্রাঞ্চ না বাছলে ব্যানার সব ব্রাঞ্চ গোনে, আর তালিকা
"Branch বাছুন" বলে — এটা আগের আচরণ, বদলানো হয়নি। একবার ব্রাঞ্চ বাছলেই মেলে।

## যাচাই (২২.০৮.২০২৬ — দ্বিতীয় দফা)

Kotlin compile **নতুন ভুল ০** · ১১টি গার্ডই PASS · `node --check` সব ফাইল OK ·
গার্ডে ইচ্ছাকৃত **৪৬টি** ভুল বসিয়ে পরীক্ষা — সবগুলোই ধরা পড়েছে ·
প্রতিকূল পুনঃপর্যালোচনা দু-দফা।

**Android Studio Build pending — owner will build.**

## এখনো বাকি (সৎভাবে)

* Doctor Visit-এ নোটিফিকেশন থেকে খুললে (deep-link) খোঁজার ঘরে নম্বর বসার
  কারণে এক পলকের জন্য "No doctors found" ঝিলিক দেয় — ছোট, ঠিক করা হয়নি।
* মিশ্র পর্দায় উপরের তিনটে ট্যাবের সংখ্যা ভাগ-ভিত্তিক (dedupe ছাড়া), তাই
  যোগ করলে নিচের মোট সংখ্যার চেয়ে বেশি হতে পারে — শুধু দেখানোর ব্যাপার।
* V512-এর ZIP এখনো বানানো হয়নি, কোনো ফাইল পাঠানো হয়নি।
