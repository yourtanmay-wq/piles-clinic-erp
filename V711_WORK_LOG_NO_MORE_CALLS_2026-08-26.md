# V711 — "আর কল লাগবে না" (চিকিৎসা চলছে এমন রোগীকে কল-তালিকা থেকে সরানো)
**তারিখ:** ২৬.০৮.২০২৬ · **ভার্সন:** V711 / 7.11

## TK কী বলেছেন
> "এত কল পেন্ডিং ওভারডিউ থাকার কথা নয় — তারা নাকি প্রত্যেকটা পেশেন্টকে কল করেছে
> কিন্তু তারপরেও [বাকি] হিসেবে দেখাচ্ছে। **সত্যতা গভীরে গিয়ে আগে যাচাই করে
> তারপরে আমাকে বলুন।**"
> "কোন পেশেন্ট যখন কন্টিনিউ পেশেন্ট অথবা কন্টিনিউ ট্রিটমেন্ট করাচ্ছে, তাদেরকে
> আর ফোন না করলেও চলে — সেটার ব্যবস্থা কীভাবে করা যায় বলুন"
> বাছাই: **উপায় ৩** (রোগী ধরে ধরে) · ডেমো প্রুফে অনুমোদিত

ডেমো প্রুফ: `V711_DEMO_NO_MORE_CALLS.png`

## 🔍 গভীর যাচাইয়ের ফল — তিনটে দাবির মধ্যে **একটাই সত্যি ছিল**
| আমি প্রথমে যা বলেছিলাম | গভীরে গিয়ে যা পেলাম |
|---|---|
| ① কল বোতাম চাপলে রেকর্ডে কিছু বসে না | ✅ **সত্যি।** `actionBtn("📞")` শুধু ডায়াল করে; `callCount`·`lastCallDate`·`nextFollow` বদলায় **কেবল `updateRemark()`** ডাকলে |
| ② Remark না লিখলে সতর্কতা নেই | ❌ **ভুল ছিল — আগে থেকেই আছে।** কল থেকে ফিরলেই `remindPendingRemark()` → "Update remark?" পপ-আপ; সঙ্গে Dashboard পপ-আপ ও ঘণ্টার সংখ্যা (`PendingRemarkStore` · `BellCounter`) |
| ③ ব্যানারের সংখ্যা পুরোনো জমানো তালিকা থেকে | ❌ **ভুল ছিল।** প্রথমে জমানোটা দেখায়, তারপর `bannerWatch` (followups·enquiries·patients·payments) বদল দেখলে বা ৩০ মিনিটে একবার **তিনটে লাইভ `fetchTab()`** চালিয়ে নতুন করে গোনে |

⇒ **TK-কে ② ও ③ সংশোধন করে জানানো হয়েছে।** জোর করে আরও গোনা যোগ করলে V509-এর
লক-করা egress নিয়ম ভাঙত ও Supabase-এর খরচ বাড়ত — তাই ওদুটোয় হাত দেওয়া হয়নি।

⇒ **অর্থাৎ ৪১টা সত্যিই বকেয়া** — TK-এর ছবিতেই প্রমাণ: LAST CALL ০১–০৩.০৮.২০২৬,
আজ ২৬.০৮। মাঝের তিন সপ্তাহে ওই রেকর্ডগুলোয় একটাও Remark লেখা হয়নি।

## কী তৈরি হলো (একটাই নতুন জিনিস)
**"📵 আর কল লাগবে না"** — চাপ দিলে ওই রোগীর **পরের কলের তারিখ ফাঁকা** হয়ে যায়,
তাই ড্যাশবোর্ডের ব্যানার ও Overdue — কোনোটাই তাঁকে আর গোনে না।

| ফাইল | কী |
|---|---|
| `native/ChamberCalendarDialog.kt` | **ঐচ্ছিক** `onNoMoreCalls` (ডিফল্ট null) → ক্যালেন্ডারের নিচে বাড়তি বোতাম |
| `native/FollowUpActivity.kt` | Enquiry-তে বাছাই-পর্দায় তৃতীয় বোতাম; Visit/Patient-এ ক্যালেন্ডারের নিচে; নতুন `saveNoMoreCalls()`; কার্ডে **📵 কল বন্ধ** চিপ |
| `native/NoBengali.kt` | চারটে নতুন ইংরেজি অনুবাদ (গার্ড §৯.১৪ ধরেছিল) |
| `03_NETLIFY_READY/app.js` | হুবহু একই — `wlv1NoMoreCalls()` + Next Follow-up পর্দায় বোতাম |

## 🛡️ কেন এটা নিরাপদ
* **নতুন কোনো কলাম বা SQL লাগেনি** — নিয়মটা অ্যাপে **আগে থেকেই ছিল**:
  `DashboardActivity.isDue` = `nextFollow.isNotBlank() && nextFollow <= today`।
  তাই শুধু তারিখটা ফাঁকা করলেই কাজ হয়ে যায়। **Supabase-এ খরচ বাড়ে না।**
* **রোগীর কোনো তথ্য মোছা হয় না** — নাম · টাকা · Remark · ইতিহাস সব অক্ষত।
* **ফেরানো সহজ** — ➜ চেপে আবার একটা তারিখ দিলেই তিনি আগের মতোই কল-তালিকায়।
* **পুরোনো কিছু বদলায়নি:** ক্যালেন্ডারের নতুন প্যারামিটারের ডিফল্ট `null`,
  বাছাই-পর্দার আগের দুটো বোতাম অপরিবর্তিত, ওয়েবের "Save Date"/"Skip"ও অপরিবর্তিত।
* **চিপের শর্ত সাবধানে:** পরের তারিখ ফাঁকা **এবং** আগে কখনো কল হয়েছিল —
  তাই একদম নতুন, কখনো-কল-না-হওয়া রেকর্ডে "কল বন্ধ" ভুল করে বসে না।

## যাচাই
| যাচাই | ফল |
|---|---|
| `verify_kotlin_compile.py` | ✅ নতুন ভুল ০ |
| `tk_guard.py` (২২টা মেশিন-যাচাই) | ✅ সব পাশ (§৯.১৪ প্রথমে ৪টা বাংলা ধরেছিল — অনুবাদ যোগ করে সারানো) |
| `verify_v509_egress.py` | ✅ PASS — egress-এর নিয়ম অক্ষত |
| `app.js` JS syntax | ✅ OK |

⚠️ Android চালানোর উপায় নেই — TK দেখে বলবেন।
