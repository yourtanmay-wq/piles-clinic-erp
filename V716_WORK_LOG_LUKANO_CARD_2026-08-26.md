# 🛠️ V716 — লুকিয়ে থাকা "Treatment + Cancelled" কার্ড ফেরানোর পথ (২৬.০৮.২০২৬, রাত ~৯.৪৫ IST)

**TK-এর নির্দেশ:** *"হ্যাঁ তিনি ট্রিটমেন্ট করাচ্ছেন"* · *"কোনটা নিরাপদ"* ⇒
নিরাপদ পথ (অ্যাপে যোগ, সরাসরি SQL নয়) বেছে নেওয়া হলো।

---

## ১. সমস্যা (V715-এর তদন্ত থেকে বেরিয়ে আসা দ্বিতীয় দোষ)

`stage = "Treatment"` **+** `status = "cancelled"` — এই অবস্থার কার্ড
**অ্যাপের কোনো তালিকাতেই দেখা যেত না**:

| তালিকা | নিয়ম ছিল | এই কার্ড পড়ে? |
|---|---|---|
| Follow-up | বাতিল সারি বাদ | ❌ |
| Visit Reject | `Patient` + `cancelled` | ❌ (এর stage `Treatment`) |
| Incomplete | `Treatment` + `incomplete` | ❌ (এর status `cancelled`) |
| Running Patient | `Treatment` + বাতিল-নয় | ❌ |

⇒ কার্ডটা **কোথাও নেই**, তাই স্টাফ চাইলেও **ফেরানোর কোনো বোতাম পেত না**।

**লাইভ প্রমাণ:** SADDAM HUSSAIN (কিশনগঞ্জ, …9547006061) — টাকা নেওয়া হয়েছে
ও ক্লাউডে পৌঁছেছে, কিন্তু কার্ড **১৬ জুলাই থেকে** বাতিল অবস্থায় লুকানো।
সেই আটকে থাকা পেমেন্টই দিনে ~৪,০০০ বার অকারণে চেষ্টা করত (V715 দ্রষ্টব্য)।
ডেটাবেসে এমন কার্ড **মোট ২৪টা** (TK-এর সামনে গোনা)।

---

## ২. কী করা হলো

`Treatment` + `cancelled` কার্ড এখন **"Visit Reject"** তালিকায় দেখাবে —
সত্যিকারের বাতিল করা Visit-এর মতোই। স্টাফ চেনা **Restore** বোতামেই ফেরাতে
পারবেন।

| দিক | ফাইল |
|---|---|
| ফোন | `DraftRepository.kt` — `when`-এ একটা নতুন শাখা |
| কম্পিউটার | `app.js` — `wlv1CancelledTreatment` যোগ করে `visitReject`-এ |

### কেন এটাই নিরাপদ পথ (TK-এর প্রশ্নের উত্তর)
- চালু ডেটাবেসে **হাত পড়ে না**
- ফেরানোর সিদ্ধান্ত **মানুষের** — কোড নিজে থেকে কারো বাতিল করা কার্ড খোলে না
- Restore-এর কোড **আগে থেকেই প্রমাণিত** (Android V452 · Web V452) — সে
  `history`-তে `status="Active"` + `"Restored from Visit Reject List"` বসায়,
  আর ডেটাবেসের `tk_terminal_no_return` পাহারাদার **ঠিক এটাই চায়**
  ⇒ **পাহারাদারকে পাশ কাটানো হচ্ছে না**, তার নিজের নিয়মেই ফেরানো
- কম্পাইল ও গার্ড দিয়ে **আগে পরীক্ষা করা গেছে**

### ⛔ যা ছোঁয়া হয়নি
কোনো সারি তৈরি · মোছা · বদল হয় না — এটা শুধু **কোন তালিকায় দেখাবে** তার নিয়ম।
Restore-এ শুধু `status` ও `history` বসে, **`stage` বদলায় না** (দুই দিকেই কোডে
মিলিয়ে দেখা) ⇒ Treatment-এর রোগী Visit-এ নেমে যাওয়ার ঝুঁকি নেই।
`runningTreatment`/`notComplete` থেকে `cancelled` আগে থেকেই বাদ ছিল, তাই
একই সারি দু'জায়গায় যেতে পারে না।

---

## ৩. যাচাই
- ✅ `verify_kotlin_compile.py` — নতুন ভুল **০**
- ✅ `node --check app.js` — পাশ
- ✅ `tk_guard.py` — মেশিনের সব যাচাই পাশ
- ✅ ওয়েব ও ফোন — **দুই দিকেই** একই নিয়ম (TK-এর নিয়ম ৬.৬)

---

## ৪. সাদ্দাম হুসেনের বেলায় এবার কী হবে

১. নতুন বিল্ড ফোনে বসলে **Draft → Visit Reject**-এ তাঁর নাম দেখা যাবে
২. কিশনগঞ্জের স্টাফ **Restore** চাপবেন
৩. কার্ড `Active` হবে, তিনি Treatment তালিকায় ফিরবেন
৪. পরের sync-এ আটকে থাকা পেমেন্টটা **নিজে থেকেই** সফল হবে ও
   Dashboard-এর লাল "pending" লেখা মিলিয়ে যাবে
৫. ⇒ দিনে ~৪,০০০ বারের অকারণ চেষ্টা **বন্ধ**
