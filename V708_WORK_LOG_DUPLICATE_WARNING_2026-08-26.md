# V708 — একই জিনিস দুবার জমা হওয়া আটকানো (Warning + Cancel/OK)
**তারিখ:** ২৬.০৮.২০২৬ · **ভার্সন:** V708 / 7.08

## TK কী বলেছেন
> (Patient Timeline-এর ছবি) "এত ডুপ্লিকেট কেন? আটকায় না কেন? **আগে যাচাই করুন**"
> "Warning দেবে আটকাতে হবে" → "**cancel & Ok ২ টা ই রাখতে হবে**"
> "আপনি বলুন এ স্মার্ট যুগে কি হতে হয়"

TK-এর ছবিতে: ১৫.০৭.২০২৬-এ ৩.১৭–৩.২১-এর মধ্যে **৫টা হুবহু এক Investigation**,
আর ৩.০০/৩.০১-এ **২টা হুবহু এক Prescription**।

ডেমো প্রুফ: `V708_DEMO_DUPLICATE_STOP.png` · `V708_DEMO_DUPLICATE_WARNING.png`

## ✅ আসল কারণ (কোড ধরে যাচাই — আন্দাজ নয়)
`ClinicalCloudRepository.saveMedical()` প্রতিবার একটা **নতুন `med_...` আইডি**
বানিয়ে নতুন সারি লেখে — আগেরটার সঙ্গে হুবহু মিলে গেলেও কোনো যাচাই ছিল না।
কম্পিউটারেও একই (`saveMedicalRecord` → `uid('med')`)।
তার উপর Investigation পর্দায় **`Save` ও `Save & Print` — দুটো বোতামই**
`saveInvestigations()` ডাকে, তাই দুটোতে চাপলেই দুটো সারি।

## সিদ্ধান্ত (আমার সুপারিশ, TK-অনুমোদিত)
> অ্যাপ **ভুল ঠেকাবে, কিন্তু মালিকের হাত বাঁধবে না।**
* **Cancel** → কিছুই সেভ হয় না (ডিফল্ট; অসাবধানে দুবার চাপলে এটাই বাঁচায়)
* **OK** → জেনেশুনে তবুও সেভ হয় (বিরল, কিন্তু পথ খোলা থাকে)

কারণ: ৯৯% ডুপ্লিকেট হয় **ভুল করে**; ওগুলো Warning-ই আটকায়। কিন্তু ডাক্তার
সত্যিই একই দিনে একই টেস্ট আবার দিতে চাইলে অ্যাপের "না" বলার অধিকার নেই —
নইলে স্টাফ ঘুরপথ খুঁজবে, তাতে আরও গোলমাল।

## মিলের নিয়ম — চারটেই মিলতে হবে
রোগী · দিন · ধরন · **লেখা (selected + details)**।
বাড়তি ফাঁকা ও বড়/ছোট হাতের অক্ষর গোনা হয় না; **দুটোই ফাঁকা হলে কখনো মিল ধরা হয় না**।

## ফাইল
| ফাইল | কী |
|---|---|
| `clinical/DuplicateSaveGuard.kt` | **নতুন** — মিল খোঁজা + Warning পপ-আপ (PremiumAlert হলুদ, লেখা ইংরেজি) |
| `clinical/InvestigationAdviceActivity.kt` | সেভ/Toast/প্রিন্ট এখন পাহারার ভিতরে |
| `clinical/DietChartActivity.kt` | একই |
| `clinical/MedicineSlipActivity.kt` | একই (পুরোনো `lastSavedSlipSig` পাহারাও **রেখে দেওয়া হয়েছে**) |
| `03_NETLIFY_READY/app.js` | `wlv1TodaysSameMedical()` + `saveMedicalRecord()`-এ confirm |

## ⛔ যা ইচ্ছাকৃতভাবে ছোঁয়া হয়নি
* **Prescription** — ওখানে আগে থেকেই নিজের, আরও কড়া পাহারা আছে
  (`checkSameDayPrescription`: ফোন **ও** ক্লাউড দুটোই দেখে, একই দিনে যেকোনো
  Prescription থাকলেই সতর্ক করে)। দুটো পপ-আপ একসাথে দেখানো হত, তাই বাদ।
* **Doctor Checkup** — V676-এ আজকের নিজের চেক-আপ **একই id-তে** upsert হয়,
  তাই ওখানে ডুপ্লিকেট হয় না।
* সেভের নিয়ম · অফলাইন-তালিকা · ক্লাউড — কিছুই বদলায়নি; `DuplicateSaveGuard`
  নিজে **একটাও সেভ করে না**, শুধু জিজ্ঞাসা করে।

## ⚠️ যা এটা করবে না (সৎ কথা)
**আগে থেকে জমা থাকা ডুপ্লিকেটগুলো এটা মুছবে না** — শুধু নতুন ডুপ্লিকেট আটকাবে।
পুরোনোগুলো মোছা আলাদা কাজ; TK বললে আলাদা করে করা হবে।

## যাচাই
| যাচাই | ফল |
|---|---|
| মিলের ৬টা নিয়ম চালিয়ে পরীক্ষা (node) | ✅ ৬/৬ পাশ |
| `verify_kotlin_compile.py` | ✅ **নতুন ভুল ০** |
| `app.js` JS syntax | ✅ OK |
| `tk_guard.py` (২২টা মেশিন-যাচাই) | ✅ সব পাশ |

চালিয়ে পরীক্ষার ফল:
```
✅ একই দিন · হুবহু এক → ধরা পড়ে
✅ বাড়তি ফাঁকা/বড়-ছোট হাতের অক্ষর → তবু ধরা পড়ে
✅ টেস্ট বদলালে → ধরা পড়ে না (নতুন সারি হবে)
✅ অন্য রোগী → ধরা পড়ে না
✅ গতকালের সারি → ধরা পড়ে না
✅ দুটোই ফাঁকা → কখনো মিল ধরা হয় না
```

⚠️ Android চালানোর উপায় নেই — TK দেখে বলবেন।
