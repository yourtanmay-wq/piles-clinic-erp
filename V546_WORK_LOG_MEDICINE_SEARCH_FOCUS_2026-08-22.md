# V546 — মেডিসিন খুঁজতে গেলে উপরের বিস্তারিত কার্ডটা সরে যাবে
তারিখ: ২২.০৮.২০২৬ · V545 → **V546 (5.46)** · **SQL লাগবে না**

TK-এর নির্দেশ (ফটো-প্রুফসহ, Prescription পর্দা — BISWAJIT BARMAN):
> *"যখন মেডিসিন সেখানে কিছু লিখব তখন উপরের এত কিছু বিস্তারিত থাকার তো কোনো
> দরকার নেই। যাতে মেডিসিনের নাম ভালোভাবে টাইপ করা যায় তখন শুধুমাত্র সেটুকুই
> থাকবে। সেখান থেকে ব্যাক করলে আবার এই স্ক্রিনে আসবে।"*
> *"আন্দাজে কিছু করবেন না, সঠিকভাবে সততার সাথে কাজ করবেন।"*

---

## ১. কী বদলাল

**খোঁজার ঘরে কিছু লেখা থাকলে** উপরের কার্ডটা সরে যায় —
Sitz Bath — 2 Times · Diet · Disease Name · Symptoms · Since When ·
Chief Complaint · "+ Choose More Information"।

তখন পর্দায় থাকে শুধু —
রোগীর নামের হেডার · **⭐ Common | 🔍 Medicine | ＋ Add** · ওষুধের তালিকা ·
নিচের বার (Cancel · Save)।

**ঘর ফাঁকা হলেই কার্ডটা হুবহু আগের অবস্থায় ফিরে আসে।**

## ২. "ব্যাক করলে আবার এই স্ক্রিনে আসবে"

ফোনের ব্যাক বোতাম **অথবা** উপরের ← তীর —
- খোঁজার ঘরে লেখা থাকলে → **আগে খোঁজাটুকু মুছে গোটা পর্দাটাই ফিরে আসে**,
  পর্দা বন্ধ হয় না, বাছা ওষুধ একটাও হারায় না;
- ঘর ফাঁকা থাকলে → আগের মতোই পর্দা বন্ধ।

(কীবোর্ড খোলা থাকলে অ্যান্ড্রয়েডের নিয়মেই প্রথম ব্যাক কীবোর্ড বন্ধ করে —
এটা ফোনের নিজের আচরণ, অ্যাপের নয়।)

## ৩. কেন "লেখা থাকলে", "ছুঁলে" নয় — কারণটা প্রমাণসহ

পর্দা খোলার সময় `EditText` নিজেই focus নিয়ে নিতে পারে। focus-ভিত্তিক নিয়ম
করলে **পর্দা খোলামাত্র কার্ডটা না-চাইতেই লুকিয়ে যেত**। তাই নিয়মটা করা হয়েছে
লেখা-ভিত্তিক — যা TK-এর কথার সঙ্গেও হুবহু মেলে ("কিছু লিখব তখন")।

## ৪. কোনো তথ্য হারায় না — প্রমাণ

- কার্ডের টিকগুলো **টিক দেওয়ার সঙ্গে সঙ্গেই** `PrescriptionOptionsStore`-এ
  জমা হয় (`setOnCheckedChangeListener`) — লুকিয়ে গেলেও জমা থাকে।
- Diet-এর লেখা `EditText`-এর ভিতরেই থাকে; Save-এর সময় আগের মতোই
  `dietInput?.text` পড়া হয় (একই তিন জায়গা, এক অক্ষরও বদলায়নি)।
- ওয়েবে কার্ডটা DOM-এ থাকেই, শুধু `display:none` — `rxSaveOptions()`
  আগের মতোই `.rxHistoryField:checked` ও `#rxDietOptional` পড়ে।

## ৫. কোথায় কোথায় হাত পড়েনি

- **Medicine Slip** ও **Direct-print** পর্দায় এই কার্ডটাই নেই
  (`topCard = null` / ওয়েবে `.rxCompactOptions` নেই) — সেখানে ব্যাক আগের
  মতোই সরাসরি বন্ধ করে, কিচ্ছু বদলায়নি।
- ওষুধের তালিকা · খোঁজার নিয়ম · Save-এর কোড · ছাপা কাগজ — কিছুই ছোঁয়া হয়নি।

## ৬. ফাইল

| ফাইল | কী হল |
|---|---|
| `clinical/MedicinePickerDialog.kt` | কার্ড লুকানো/ফেরানো + দুই ধাপের ব্যাক |
| `03_NETLIFY_READY/app.js` (`rxSearchFilter`) | ওয়েবে একই নিয়ম (ফোন-ওয়েব একই দিনে) |
| `app/build.gradle.kts` · `index.html` · `version.json` | 545 → 546 |

## ৭. যাচাই (চালিয়ে দেখা হয়েছে)

- **ফোন:** আসল কোডটুকু হুবহু তুলে (re-type নয়) stub দিয়ে কম্পাইল করে চালানো —
  **২২টি পরীক্ষা, ২২টিই পাশ** (কার্ড লুকোয়/ফেরে · ব্যাক দুই ধাপ · ← তীর দুই ধাপ ·
  কীবোর্ড বন্ধ · ACTION_DOWN-এ কিছু হয় না · Medicine Slip-এ পুরোনো আচরণ অটুট)।
- **ওয়েব:** আসল `rxSearchFilter` তুলে চালানো — **৭টি পরীক্ষা, ৭টিই পাশ**।
- `node --check app.js` পাশ; Kotlin ফাইলের `{}` ও `()` HEAD-এর সঙ্গে মিলিয়ে
  দেখা হয়েছে — দুটোই সমান-সমান (+৭ জোড়া `{}`, +৩০ জোড়া `()`, নতুন কোডেরই)।

⚠️ **সৎ কথা:** এখানে Android SDK নেই, তাই আসল অ্যাপ বিল্ড করে স্ক্রিনশট
দেখানো সম্ভব নয় — উপরের প্রমাণ কোড চালিয়ে পাওয়া।
