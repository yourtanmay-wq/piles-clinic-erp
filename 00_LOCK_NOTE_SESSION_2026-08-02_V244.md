# 🔒 LOCK NOTE — সেশন 02.08.2026 (সকাল) — V244

**এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না,
কোনো working flow খারাপ করা যাবে না।**

আগের ভার্সন: V243 (versionCode 243) → এখন: **V244 (versionCode 244, versionName 2.44)**।

---

## এই সেশনে যা হয়েছে (B272, তারিখ-সময় সহ খাতায় লেখা আছে)

1. **সত্যিকারের বিল্ড এরর ঠিক (TK-এর Android Studio ছবি, 02.08.2026 সকাল ~৯.১৩ am)।**
   `PatientMessage.kt`-তে `android.widget.ScrollView.LayoutParams(...)` লেখা ছিল —
   কিন্তু `ScrollView`/`HorizontalScrollView`-এর নিজস্ব কোনো `LayoutParams` ক্লাস
   নেই (এরা `FrameLayout`-এর সাব-ক্লাস)। `FrameLayout.LayoutParams(...)`-এ বদলানো
   হলো। ⛔ আচরণ/চেহারা এক অক্ষরও বদলায়নি (এখনো MATCH_PARENT চওড়া, লেখা কাটে না)।
   পুরো প্রজেক্টে এই একই ভুল আর কোথাও নেই (যাচাই করা)।

2. **পাহারাদারে নতুন স্থায়ী চেক ৯.১৬** — `ScrollView.LayoutParams`/
   `HorizontalScrollView.LayoutParams` কোথাও লেখা হলে ফাইল বানানোই আটকে যাবে।

3. **সব সতর্কবার্তা এক জায়গায় গোছানো** — `00_TK_SOB_NIYOM_EK_JAYGAY_LOCKED.md`-এ
   নতুন "০) এটা ডেমো নয়" (সাবধানতা · সততা · আন্দাজ-নিষেধ · সন্দেহে-আগে-বলা ·
   কথা-কম — পাঁচটা মূল নীতি) ও "৯) বিল্ড-এরর থেকে শেখা" অংশ যোগ হয়েছে।

4. **ভার্সন-বাড়ানোর ভুল সংশোধন (TK নিজে ধরিয়ে দিয়েছেন)।** আগের দুটো ডেলিভারি
   (`_5.zip`, `_6.zip`) ভুল করে V243-নামেই পাঠানো হয়েছিল — "ফাইল পাঠানোর মুহূর্তে
   একবারে একধাপ ভার্সন বাড়বে" এই স্থায়ী নিয়ম মানা হলো না। এখন V244-এ ঠিক করা হলো।

## ➕ এই V244-এর ভিতরেই পরবর্তীতে আরও যা যোগ হয়েছে (B273–B284, বিস্তারিত খাতায়)
- B273 — Enquiry বার্তায় "কিশনগঞ্জ" ভুল বানান ঠিক ("কিষানগঞ্জ")
- B274 — "row_not_matched" সিঙ্ক-আটকানো স্থায়ী সমাধান + Dashboard-এ "যায়নি" এন্ট্রি
  স্থায়ীভাবে ছেড়ে দেওয়ার (long-press) ব্যবস্থা
- B275 — Enquiry বার্তা থেকে তারা-চিহ্ন (*) সরানো (প্রিভিউ+WhatsApp+SMS তিন জায়গাতেই)
- B276 — Blood Test পাতার হেডারে রোগীর নাম/ID লাইন কাটা বন্ধ
- B277 — "Common Blood Test" পপ-আপে বাক্স-সাইজ ঠিক + Cancel/Apply বদলে
  কার্যকরী Share/Cancel/Print
- B278 — Patient Timeline-এর ৪ বোতামের লেখা-কাটা সত্যিকারের সমাধান (আগের B260
  ফিক্স আসলে কাজ করছিল না, MaterialButton-এ autosize অকার্যকর ছিল)
- B279 — Draft Reject List-এ Delete Enquiry বোতাম মাঝে মাঝে না দেখানো ঠিক
- B280 — এই সেশনের সব ফিক্স (B271-B279) আবার হাতে-হাতে যাচাই + ৩টা নতুন স্থায়ী
  পাহারা-লক (পাহারাদারে মোট লক ২০→২৩)
- B281 — "Refund request" নোটিশ থেকেই সরাসরি Approve/Reject (আগে আলাদা লুকানো
  ড্রপডাউন খুঁজতে হতো)
- B282 — নতুন স্টাফ RUPAM (জলপাইগুড়ি, +918167096595) যোগ — ফোন ও ওয়েব দুটোতেই
- B283 — Staff Report-এ নাম-ফাঁকা সারিতে মোবাইল+তারিখ দেখানো
- B284 — Staff Report-এর সারিতে চাপ দিলে রোগীর পাতা খোলা, Back করলে রিপোর্টেই ফেরা

## ⛔ যা এখনো বাকি
- TK-এর লাইভ টেস্ট — V244 বিল্ড করে দেখা (এখন B284 পর্যন্ত সবকিছুসহ)
- item 13, 27/28/31 (বাকি অংশ), 88, 91 — TK-এর ছবি/সিদ্ধান্ত লাগবে
- item 21-24, 60, 73-77, 78-81, 85 — লাইভ ফোন/ডেটাবেস প্রমাণ ছাড়া বোঝা অসম্ভব
- B148 (RLS) — TK-এর অনুমতি ছাড়া ছোঁয়া নিষেধ
- Staff Performance-এ "কত কল করেছে" (call-count) যোগ করা — TK চাইলে আলাদা কাজ

## যাচাই
- ব্র্যাকেট/প্যারেন গোনা হাতে পাশ (প্রতিটা বদলানো ফাইলে, একাধিকবার)
- `00_GUARD/tk_guard.py --release` চালিয়ে **সব ✅ পাশ** (২৩টা লক করা নিয়ম সহ,
  নতুন ৯.১৬-সহ)
- ভার্সন তিন জায়গাতেই (build.gradle.kts · খাতা · এই লক নোট) V244 মিলিয়ে দেখা হয়েছে
- এই সেশনে ১টা SQL চালানো হয়েছে TK নিজে (altMobile কলাম যোগ) — বাকি সব কাজে
  কোনো SQL লাগেনি

TK-এর লাইভ টেস্ট বাকি — B272 থেকে B284 পর্যন্ত সবকিছুই।
