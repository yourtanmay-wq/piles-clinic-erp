# V594 — হেডারের ব্রাঞ্চ-ঘর ছোট
**তারিখ:** ২৩.০৮.২০২৬ · **ভার্সন:** V594 / 5.94

TK (Patient Registration পর্দার ছবি পাঠিয়ে):
> *"উপরে ডান দিকে যে সিলেক্ট ব্রাঞ্চ, এত বড় রাখা যাবে না · শুধুমাত্র মাস্টারের
> ফোনে বলে কথা না, এ টু জেড সবার কাছে যেন ওটা সাইজের ছোট থাকে · মাস্টারের ফোনে
> অন্যান্য জায়গায় যেমন ব্রাঞ্চ সিলেক্ট করার জায়গায় একটু ছোট সাইজের, ঠিক
> সেরকমই রাখবেন · কিন্তু আন্দাজে কাজ করে কোন ভাল কাজ খারাপ করবেন না"*

**কোড লেখার আগে ডেমো-ফটো দেখিয়ে অনুমোদন নেওয়া হয়েছে।**

---

## মাপটা আন্দাজে নেওয়া হয়নি — মেপে নেওয়া হয়েছে

ব্রাউজারে Roboto ফন্টে প্রতিটা লেখার আসল চওড়া মাপা হয়েছে
(`canvas.measureText`):

| লেখা | ১৬sp | **১০sp** |
|---|---|---|
| SELECT BRANCH | 135 dp | **84 dp** |
| COOCH BEHAR (সবচেয়ে বড় নাম) | 120 dp | **75 dp** |
| KISHANGANJ | 103 dp | 71 dp |
| JALPAIGURI | 93 dp | 64 dp |

**আগে কেন কেটে যেত:** ঘর 142dp, তার মধ্যে paddingStart 8 + তীরের জায়গা 34
= 42dp চলে যেত ⇒ লেখার জায়গা **100dp**, কিন্তু "SELECT BRANCH" লাগে **135dp**।
তাই TK-এর ছবিতে "SELECT B.." দেখাচ্ছিল।

**এখন:** ঘর 120dp − 8 − 20 = লেখার জায়গা **92dp**, সবচেয়ে বড় লেখা **84dp**
⇒ **কিছুই কাটে না**।

---

## যা বদলানো হলো

| ফাইল | কী |
|---|---|
| `res/layout/activity_registration.xml` | `spBranch` — 142 × 40 dp → **120 × 28 dp**, paddingEnd 34 → 20 |
| `res/drawable/bg_input_field_picker_small.xml` (**নতুন**) | ছোট ঘরের জন্য — কোণ 14→8dp, তীর 18→12dp, ডান থেকে 14→6dp |
| `native/RegistrationActivity.kt` | `capsAdapter()`-এ ঐচ্ছিক `boxTextSp`, শুধু ব্রাঞ্চের ঘরে **১০sp** |

উচ্চতা 28dp — মাস্টারের অন্য পর্দার ছোট ব্রাঞ্চ-বোতামটার সমান
(`IncomeExpenseActivity`-র chip: ১৩sp + ৬dp padding ≈ ২৮dp)।

---

## ⛔ যা ছোঁয়া হয়নি

- **ব্রাঞ্চের তালিকা · বাধ্যতামূলক নিয়ম · স্টাফের ৩-চাপের লক** — কিচ্ছু নয়।
- **ফর্মের বাকি তিনটে বাছাই-ঘর** (Occupation · Duration Unit · Ref By) —
  আগের মাপেই, আগের লেখাতেই। `boxTextSp` না দিলে কিছুই বদলায় না।
- **পুরোনো `bg_input_field_picker.xml`** — এক অক্ষরও বদলায়নি, নতুন একটা
  আলাদা ফাইল বানানো হয়েছে।
- **খোলা তালিকার লেখা** ছোট করা হয়নি — বেছে নেওয়ার সময় বড় লেখাই থাকবে।

⚠️ এটা **এ-টু-জেড সবার ফোনেই** এক — মাস্টার · ডাক্তার · স্টাফ, রোল দেখে
কিছু আলাদা হয় না (মাপটা লেআউটে, কোডে শর্ত নেই)।

⚠️ **ওয়েবে বদলানো হয়নি** — সেখানে ব্রাঞ্চ-বাক্সটা হেডারে নয়, ফর্মের উপরে
আলাদা সারিতে (`.regTopBranch`)। TK ওটা নিয়ে কিছু বলেননি, তাই আন্দাজে
ছোঁয়া হয়নি।

---

## পাহারাদার

নতুন `00_GUARD/verify_v594_branch_ghor_choto.py` — **২৩টা যাচাই, সব পাশ**।
শুধু মাপ দেখে না — **হিসাব করেও দেখে** যে ভিতরের জায়গা (92dp) সবচেয়ে বড়
লেখার (84dp) চেয়ে বেশি, আর আগের মাপে সত্যিই কম ছিল (100 < 135)।

**উল্টো-পরীক্ষা:** খোলা তালিকাতেও ছোট লেখা বসালে ❌ · মাপ আগের মতো বড়
করলে ❌ — দুটোই ধরা পড়ে।

🔧 উল্টো-পরীক্ষা করতে গিয়ে পাহারাদারের নিজের একটা ফাঁক ধরা পড়ে সারানো
হয়েছে (পুরো ফাইল খুঁজত, তাই নিচের একটা লাইন দেখে মিথ্যে ❌ দিত)।

**মোট ২৬টা পাহারাদার — সব পাশ**, Kotlin কম্পাইলে নতুন ভুল 0,
রিসোর্স-যাচাইও পাশ (drawable ২৫০ → ২৫১)।
