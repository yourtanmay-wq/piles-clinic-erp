# V398 — মাস্টারের ব্রাঞ্চ একবার বাছলেই সব জায়গায় মনে থাকবে
**তারিখ:** 16.08.2026 · **সময়:** 12:10 PM – 01:30 PM IST · **ভার্সন:** V398 / 3.98

TK-এর নির্দেশ (হুবহু):
> "আমি চাইছি মাস্টার প্রতিবার কোন ব্র্যান্ড সিলেক্ট করে নেবে / লাস্ট যে ব্রাঞ্চ সিলেক্ট করা থাকবে / প্রতিবার প্রতিটা সেকশনের সেই ব্রাঞ্চই থেকে যাবে / বারবার যেন সিলেক্ট করার প্রয়োজন না পড়ে / মাস্টার যদি চায় অল ব্রাঞ্চ সিলেক্ট করে রাখতে তবেই অল ব্রাঞ্চ সিলেক্ট থাকবে"
> "ব্রাঞ্চ বাছাই সব সময় হেডারে ডান পাশে থাকবে" · "হেডারে ডান পাশে ব্রাঞ্চ বাছাই রাখুন এবং Calendar"

## TK-এর অনুমোদিত ৪টি সিদ্ধান্ত (16.08.2026)
1. **একটাই মনে-রাখা ব্রাঞ্চ** — সব সেকশনে এক।
2. **প্রথমবারের ডিফল্ট = "Select Branch"** (বাছার আগে খালি)।
3. **RMP পর্দায় "All" যোগ করা হয়নি** — আগের মতোই নির্দিষ্ট ব্রাঞ্চ বাছতে হয়।
4. **টাকার হিসাবও একই ব্রাঞ্চ মানবে।**

---

## ওয়েব (03_NETLIFY_READY)

**নতুন এক জায়গা** — `app.js` (isMaster()-এর ঠিক পরে):
`WLV1_BRANCH_KEY='rk_master_branch'` · `wlv1BranchGet()` · `wlv1BranchSet()` · `wlv1BranchGate()` ·
`wlv1BranchAskCard()` · `wlv1BranchSelectHtml()` · `wlv1RmpBranch()` · `wlv1HeaderPick()`

**যেখানে বসেছে (১৩):** Follow-up · Follow-up Calendar · Check-up Queue · Collection (Today/Cash/UPI/Monthly/History) · Trash Bin · RMP · RMP Performance · Chamber Attendance · Chamber Close · Follow-up Filter · I&E আজকের হিসাব · I&E টাকার খাতা · I&E Monthly Summary।

**হেডারে বসানো:** `wlv1HeaderPick()` — `.wlv1HdrPick` ক্লাসের মোড়কটিকে মোবাইলে (`<900px`) টপবারে, ☰-এর ঠিক আগে সরায়। নতুন এলিমেন্ট বানানো হয়নি, পুরোনোটাই সরে — তাই onchange/onclick অটুট।
CSS: `styles.css` সেকশন **ঢ**, সম্পূর্ণ `@media (max-width:899px)`-এর ভিতরে (Android `bg_branch_pill.xml`-এর হুবহু রং #33FFFFFF / #80FFFFFF / 8dp)।
বার্তার কার্ড: `styles.css` সেকশন **ড** (`.card.wlv1BranchAsk`)।

**ক্যাশ ভার্সন:** `index.html` → `?v=v421` (app.js, styles.css, config.js, finance.js)।

## Android (02_ANDROID_SOURCE_CODE)

**নতুন ফাইল:** `native/BranchFilterStore.kt` — SharedPreferences `piles_branch_filter`, চাবি `branch`।

**যেখানে বসেছে (১২টি ফাইল):**
`DoctorQueueActivity` · `FollowUpActivity` · `FollowCalendarActivity` · `DraftActivity` · `DraftListActivity` ·
`CollectionListActivity` · `ChamberAttendanceActivity` · `ChamberCloseActivity` · `TrashBinActivity` ·
`PaymentActivity` · `DoctorVisitActivity` (RMP + RMP Performance) · `modules/IncomeExpenseActivity` (৩টি পিকার)।

**পুরোনো আলাদা ব্যবস্থা বাতিল:** `DoctorQueueActivity`-র নিজস্ব `"doctor_queue_pick"` (B670) এখন একই কেন্দ্রীয় দোকানে।

**"এখনো বাছা হয়নি" গার্ড (ক্লাউড-অনুরোধ যায় না):**
DoctorQueue · FollowUp (`loadTab`, `refreshTabCounts`, `autoCheckForChanges`) · FollowCalendar · Draft · CollectionList · ChamberAttendance · ChamberClose · TrashBin।
⚠️ FollowUp/FollowCalendar/ChamberClose-এ গার্ডটা **বাধ্যতামূলক** — `FollowUpRepository.branchScopeFilter()` (লাইন ১১৫) ও `ChamberUnclosedRepository` (লাইন ৭৮) ফাঁকা নামকে "ছাঁকনি নেই" ধরে, গার্ড ছাড়া সব ব্রাঞ্চ নেমে যেত।

## যা ইচ্ছে করে ছোঁয়া হয়নি
- `NativeSession` / `config.js`-এর `user.branch` (মাস্টারের মান আক্ষরিক `"All"` — বদলালে permission ভাঙত)।
- প্রিন্টের `BranchSession` (`print/BranchInfo.kt`) — ক্লিনিকের নাম/ঠিকানা/লোগো, ছাঁকনি নয়।
- এন্ট্রি-ফর্মের ব্রাঞ্চ বাক্স (Enquiry, Registration, Add Collection, Add Expense, Walk-in, Medicine Payment, Briefing target, Doctor add/edit) — ওগুলো ছাঁকনি নয়।
- ডেস্কটপ ওয়েব — সব নতুন CSS `@media (max-width:899px)`-এর ভিতরে; `wlv1HeaderPick()` `innerWidth>=900` হলে সঙ্গে সঙ্গে ফেরে।
- স্টাফ/ডাক্তার — `wlv1BranchGet()`/`BranchFilterStore.effective()` মাস্টার ছাড়া সবসময় নিজের ব্রাঞ্চ/ফাঁকা ফেরে।
- `app.js:651` / `app.js:892`-এর মূল টেবিল-পড়া (১০ টেবিল পুরো নামা) — TK-কে জানানো হয়েছে, আলাদা কাজ।

## যাচাই
- `node --check` — `app.js`, `finance.js` পাশ।
- `styles.css` ব্রেস ব্যালান্স 0।
- Chromium (412×915, master সেশন) — ছবি: প্রথমবার "Select Branch", একবার বাছার পরে Follow-up/Collection/Chamber/Trash/Queue সবেতেই সেই ব্রাঞ্চ, পূর্ণ রিফ্রেশের পরেও।
- স্টাফ সেশনে যাচাই: ব্রাঞ্চ বাক্স আসেই না (`#fuBranchWrap` = 0), সারি আগের মতোই।
- Kotlin: ১৩টি সম্পাদিত ফাইলে ব্রেস/বন্ধনী ব্যালান্স 0। ⚠️ **এই পরিবেশে Gradle/kotlinc নেই, তাই Android build চালিয়ে দেখা যায়নি** — TK নিজে বিল্ড করে দেখবেন।

## জানা সীমাবদ্ধতা (সৎভাবে)
1. **I&E (টাকার হিসাব):** কিছু বাছা না-থাকলে আগের মতোই "All Branches" দেখায় (ফাঁকা পর্দা নয়)। একবার যেকোনো জায়গায় বাছলেই সেটাই মানে।
2. **PaymentActivity:** ব্রাঞ্চ না-বাছা থাকলে অনুরোধ যায় কিন্তু ০ সারি ফেরে (ভুল তথ্য নয়, শুধু একটা বাড়তি অনুরোধ)।
3. **ওয়েব RMP Performance:** "All" বাছলে তালিকা ফাঁকা আসে — এটি **আগে থেকেই ছিল** (`app.js:6157`), TK-এর নির্দেশে ছোঁয়া হয়নি।

## ভার্সন
`app/build.gradle.kts` → `appVersionCode = 398`, `appVersionName = "3.98"`, APK নাম `PilesClinic-V398.apk`।

---

# V398 — অতিরিক্ত: Staff/Doctor-এর RMP সরাসরি পেমেন্ট (TK-অনুমোদিত 16.08.2026)

TK লাইভ টেস্টে দুটি সমস্যা জানিয়েছেন। দ্বিতীয়টি ঠিক করা হয়েছে; প্রথমটি এখনো যাচাইয়ের মধ্যে।

## সমস্যা ২ — "Staff/Doctor কেন RMP কে টাকা দিতে পারছে না" ✅ ঠিক করা হয়েছে

**কারণ (দুই স্তরে আটকানো ছিল):**
1. অ্যাপ — `native/DoctorVisitActivity.kt:4021` → `if (!ModuleAuth.isMaster) { Toast("Only Master can record an RMP payment without selecting a patient"); return }`
2. ডেটাবেস — `fin.rmp_record_advance` (V383 ফাইল, লাইন ৩৭) → `if not hr.is_master() then raise exception 'Only Master can record an unallocated RMP payment'`

**যেটা আগে থেকেই কাজ করত:** রোগী বেছে নিয়ে RMP-কে টাকা দেওয়া (`fin.rmp_pay_commission`, V325 লাইন ২৯৭) — ওটা Staff/Doctor-এর জন্য আগেই খোলা ছিল।

**TK-এর সিদ্ধান্ত:** "হ্যাঁ, নিজের ব্রাঞ্চের RMP-কে পারবেন"।

**যা করা হয়েছে:**
- অ্যাপ: master-only বাধা সরিয়ে **নিজের-ব্রাঞ্চ** পরীক্ষা বসানো হয়েছে; Ref. Due-র চেয়ে বেশি হলে Staff/Doctor-কে স্পষ্ট বার্তা ("এই বাড়তি টাকা শুধু Master অনুমোদন করতে পারেন"), Master-এর জন্য আগের "Master Approve & Save" ডায়ালগ অপরিবর্তিত।
- SQL: নতুন ফাইল **`04_SUPABASE_DATABASE_SETUP/V398_STAFF_DOCTOR_RMP_DIRECT_PAYMENT_2026-08-16.sql`** — `fin.rmp_record_advance` এখন `fin.rmp_can_use()` + `fin.rmp_can_write_branch()` মানে, আর অ-Master-এর ক্ষেত্রে Due-র বেশি হলে আটকায়।
- ⛔ `fin.rmp_allocate_advance` (রোগীর সঙ্গে মেলানো) **আগের মতোই Master-only** রাখা হয়েছে — TK আলাদা করে বলেননি।
- ⚠️ **TK-কে এই SQL ফাইলটা Supabase-এ চালাতে হবে**, নইলে অ্যাপ বাধা তুললেও ডেটাবেস আটকাবে।

## সমস্যা ১ — "Payment সাথে সাথে আপডেট হচ্ছে না" ⏳ যাচাই চলছে (TK-নির্দেশ: আগে যাচাই)

**যা নিশ্চিত করা গেছে:**
- টাকা ঠিকমতো জমা হয়েছে — RMP Performance ₹৭,০০০ (৪,৩০০ + ২,৭০০) দেখাচ্ছে।
- **দুই জায়গার হিসাবের সূত্র হুবহু এক**: `legacy_paid + modern_paid + available − legacy_covered`
  - পর্দা: `DoctorVisitActivity.kt:2825`
  - Performance: `fin.rmp_legacy_performance` (V382 লাইন ১৭৬)
  তাই ডেটাবেস দ্বিমত করছে না।
- `fin.rmp_rmp_summary` (V383) `paid_to_this_rmp = v_paid + v_unallocated` — অর্থাৎ unallocated ₹২,৭০০ এতেও ধরা আছে।

**সন্দেহভাজন জায়গা (এখনো প্রমাণিত নয়):** `DoctorVisitActivity.kt:2817-2828` — টাকা সেভ হওয়ার পরে একবারই ক্লাউডে পড়া হয়, আর সেটি ব্যর্থ হলে `?: return@launch` দিয়ে **চুপচাপ কিছুই হয় না, কোনো বার্তাও আসে না**। ফলে পুরোনো সংখ্যাই থেকে যায়।

**পরবর্তী ধাপ (TK-কে অনুরোধ করা হয়েছে):** পর্দাটা বন্ধ করে আবার খুলে দেখা —
- আবার খুললে ₹৭,০০০ দেখালে → সমস্যা শুধু সেভের পরের ওই একবারের পড়ায়।
- আবার খুললেও ₹৪,৩০০ থাকলে → সমস্যা পর্দা লোডের হিসাব/অনুমতিতে (`authReady`, লাইন ১৮৮৯)।
এই একটা পরীক্ষা দুটো কারণকে আলাদা করে দেবে। **TK-এর অনুমতি ছাড়া কোনো কোড বদলানো হয়নি।**

## সমস্যা ১ — কারণ পাওয়া গেছে (TK-এর পরীক্ষার পরে)

**TK-এর পরীক্ষার ফল (16.08.2026, 1:38 PM):** পর্দা বন্ধ করে আবার খোলার পরেও Ref. Paid ₹4,300; RMP Performance ₹7,000।

⇒ সমস্যা সেভের পরের patch-এ নয়, **পর্দা লোডের হিসাবেই**।

**কারণ:** দুই পর্দা দুই আলাদা ডেটাবেস-ফাংশন ব্যবহার করে —

| পর্দা | ফাংশন | unallocated advance ধরে? | ফল |
|---|---|---|---|
| RMP Performance | `fin.rmp_legacy_performance` (V382) | ✅ `amount − allocated_amount` যোগ করে | ₹7,000 |
| ডাক্তারের পর্দা | `fin.rmp_rmp_summary` **V325 রূপ** | ❌ শুধু commission payments | ₹4,300 |
| ডাক্তারের পর্দা | `fin.rmp_rmp_summary` **V383 রূপ** | ✅ `v_paid + v_unallocated` | ₹7,000 |

**সিদ্ধান্ত:** সবচেয়ে সম্ভাব্য কারণ — **V383 ফাইলটি (14.08.2026) Supabase-এ চালানো হয়নি**, তাই ডেটাবেসে এখনো V325-এর পুরোনো `rmp_rmp_summary` রয়ে গেছে। অ্যাপের কোডে কোনো ভুল নেই, তাই **অ্যাপের কোনো কোড বদলানো হয়নি**।

**যা করা হয়েছে:** V383-এর সঠিক `fin.rmp_rmp_summary` সংজ্ঞাটি হুবহু `V398_STAFF_DOCTOR_RMP_DIRECT_PAYMENT_2026-08-16.sql`-এর শুরুতে (ধাপ ০) যোগ করা হয়েছে, যাতে TK একটাই ফাইল একবার চালালেই দুটো সমস্যাই মিটে যায়।

**TK-এর জন্য যাচাই-লাইন (শুধু পড়া, কিছুই বদলায় না):**
```sql
select pg_get_functiondef(oid) like '%v_unallocated%' as v383_installed
  from pg_proc where proname = 'rmp_rmp_summary';
```
`false` = ধারণা সঠিক, ফাইল চালালেই সারবে। `true` = ধারণা ভুল, আরও খুঁজতে হবে।

---

# V398 — অতিরিক্ত: Supabase খরচ কমানো (delta sync) · TK-অনুমোদিত 16.08.2026

TK: *"যেন ফ্রিতে চলে তার ব্যবস্থা করুন"* → *"হ্যাঁ, কাজ শুরু করুন, তবে খুব সাবধানে"*
অনুমোদিত মান: লগইনে পুনঃ-নামানো **২ ঘণ্টা**, রিফ্রেশ **৬০ মিনিট**।

## যা বদলেছে (শুধু `03_NETLIFY_READY/app.js`)
১. **নতুন সহায়ক**: `wlv1CloudRead()` · `wlv1FetchPaged()` · `rk_sync_since` (per-table) — এক জায়গা থেকে পড়া।
২. **পাতা-ধরে-পাতা পূর্ণ পড়া** (`.range()`, ১০০০ করে) — পুরনো `limit(2000)` আর সীমা নয়।
৩. **delta পড়া**: `.gt('updatedAt', শেষ-বার − ১ দিন)` — ঘড়ির ভুল সামলাতে ১ দিনের overlap।
৪. `initCloud()` — শেষ পড়ার **২ ঘণ্টার** মধ্যে হলে কিছুই নামায় না (স্থানীয় তথ্য থাকলে)।
৫. `pullCloudAllForVisibility()` — throttle **১৫ → ৬০ মিনিট**, আর সময়টা এখন `rk_last_pull_at`-এ **জমা থাকে** (আগে শুধু মেমরিতে ছিল, রিফ্রেশে ০ হয়ে যেত)।
৬. `startCloudVisibilityLoop()` — `pullCloudAllForVisibility(reason,true)` → **`false`**। আগে **প্রতিবার অ্যাপ/পাতা খুললেই** জোর করে সব নামত; এটাই সবচেয়ে বড় খরচ ছিল।
৭. **delta-র সময় স্থানীয় সারি কখনো মোছা হয় না** (cloud-authoritative প্রতিস্থাপন শুধু পূর্ণ পড়ায়)। মুছে-ফেলা সারি আগের মতোই tombstone দিয়ে বাদ।
৮. delta-তে `markPendingCloud()` চলে না (কম সারি ফেরাটা স্বাভাবিক, ভুলে সব ক্লাউডে ঠেলে দিত)।

## সুরক্ষা (একটিও বাদ যায়নি)
- এই ব্রাউজারে টেবিল ফাঁকা → পূর্ণ পড়া (ছবি সহ)
- রাত ২টার সিঙ্ক ও "Sync Now" → পূর্ণ পড়া (`force=true`, আগের হুবহু নিয়ম)
- delta ব্যর্থ → পূর্ণ পড়া → দরকারে পুরনো `select('*').limit(2000)` পথ; চুপচাপ পুরনো তথ্য নিয়ে বসে থাকে না
- `trash`-এ `updatedAt` নেই → সবসময় পূর্ণ (১৮১ সারি)

## যাচাই (Chromium, নকল ক্লাউড দিয়ে · ৩২৭৩ সারির followups)
| পরীক্ষা | ফল |
|---|---|
| প্রথমবার পূর্ণ পড়া | **৩২৭৩টি সারিই এসেছে** (আগে ২০০০-এ আটকাত) · ৪ পাতা |
| ২ ঘণ্টার মধ্যে আবার খোলা | **একটিও পাতা নামেনি** ✅ |
| ২ ঘণ্টার পরে | `gt updatedAt > (শেষ − ১ দিন)` — শুধু বদলানো সারি ✅ |
| delta-র পরে স্থানীয় সারি | **৩২৭৪ → ৩২৭৪, কিছুই হারায়নি** ✅ |
| ৮টি স্ক্রিন রেন্ডার | আগের মতোই ✅ |

`index.html` cache ভার্সন → `?v=v422`।

## 🟠 বাকি থাকা একটি খরচ (ছোঁয়া হয়নি, TK-কে জানানো)
`cloudPush()` (app.js) লেখার আগে **পুরো টেবিল** পড়ে (`select(...).limit(2000)`)। স্থানীয় তালিকায় একটিও সারি ক্লাউডে না থাকলে প্রতিবার অ্যাপ খুললেই ওই পড়া ও আপলোড চলে। পরীক্ষায় followups-এ এটি বারবার দেখা গেছে। ⛔ এটি **লেখার পথ** — আলাদা ও বেশি ঝুঁকির, তাই TK-এর আলাদা অনুমতি ছাড়া হাত দেওয়া হয়নি।

## ⛔ Android-এ এই কাজ এখনো হয়নি
এই delta sync শুধু **ওয়েবে**। Android-এর নিজস্ব পড়াগুলো (DoctorQueueRepository ইত্যাদি) আলাদা কাজ — TK-এর অনুমতির অপেক্ষা।

## অতিরিক্ত ধাপ (TK: "বারবার আপলোড ও বিল্ড ভালো নয় · সন্দেহ থাকলে এখনই করুন")

**আরও একটি বড় খরচ বন্ধ করা হলো — `initCloud()`-এর শেষে `flushPendingCloud()` → `flushPendingCloud('pending')`**
আগে **প্রতিবার বুট/লগইনে ১০টা টেবিলেরই** `cloudPush` চলত; আর `cloudPush` লেখার আগে
প্রতিটা টেবিলের **পুরো ২০০০ সারি পড়ে**, তারপর **পুরোটা আবার upsert করে** — অর্থাৎ
নামা ও ওঠা দুই দিকেই খরচ। এখন শুধু যে টেবিলে সত্যিই কিছু আটকে আছে সেটাই যায়।
⛔ নিরাপত্তা-জাল অটুট: **রাত ২টার সিঙ্ক · "Sync Now" · লগ-আউট** — তিন জায়গায় আগের মতোই পুরো ১০ টেবিল।
⛔ সেভ করলেই ঐ টেবিল সঙ্গে সঙ্গে "pending" হয় (`queueCloudTableSave`), তাই কিছু আটকে থাকে না।

## Android নিয়ে যাচাই — এখানে বড় সমস্যা নেই
`native/LiveRefresh.kt` (TICK_MS = ৩০ সেকেন্ড) প্রতি টিকে শুধু **হালকা "বদলেছে কি?" প্রশ্ন**
পাঠায় — বদল না থাকলে **একটাও সারি নামে না**। কাজ শুধু সকাল ৬টা – রাত ১০টা।
সঙ্গে V398-এর ব্রাঞ্চ-মনে-রাখা যোগ হওয়ায় মাস্টারের পর্দা-পড়াও এক ব্রাঞ্চে নেমে এসেছে।
⇒ **Android-এ delta sync-এর মতো গভীর কাজ এখন দরকার নেই** — আর এখানে Gradle/kotlinc না
থাকায় ওরকম কাজ যাচাই করাও যেত না। ইচ্ছে করে হাত দেওয়া হয়নি।

## যা এখনো বাকি (ঝুঁকির কারণে ছোঁয়া হয়নি — TK-কে জানানো)
`cloudPush()` যখন চলে তখন **পুরো টেবিল পড়ে ও পুরোটা upsert করে**। শুধু-বদলানো সারি
পাঠাতে হলে "কোন কোন সারি বদলেছে" আলাদা করে মনে রাখার নতুন ব্যবস্থা লাগবে — এটি
**লেখার পথ**, ভুল হলে তথ্য নষ্ট হতে পারে। এখন এটি শুধু তখনই চলে যখন সত্যিই কিছু
আটকে আছে, তাই খরচ অনেক কম। TK-এর আলাদা অনুমতি ছাড়া হাত দেওয়া হবে না।

## চূড়ান্ত ঝুঁকি-যাচাই (TK: "আর কোথাও কোন ঝুঁকি থাকলে আগে আমাকে বলুন")

**যাচাইয়ে দুটি আসল ঝুঁকি ধরা পড়েছে — দুটোই ঠিক করা হয়েছে:**

**ঝুঁকি ১ (গুরুতর) — নতুন কাজ দেরিতে দেখা যেত।**
`pullCloudAllForVisibility`-এ ৬০ মিনিটের ব্যবধান বসানোয় একজন স্টাফ কিছু সেভ করলে
অন্যজনের পর্দায় **৬০ মিনিট পর্যন্ত** না-ও আসতে পারত। কারণ ওয়েবে (Android-এর
`LiveRefresh`-এর মতো) ৩০-সেকেন্ডের হালকা পাহারাদার নেই।
✅ **সংশোধন:** ভারী ছিল "সব নামানো" — সেটা এখন শুধু প্রথমবার/রাত ২টা/"Sync Now"-তে।
বাকি সময় শুধু নতুন/বদলানো সারি নামে, যা খুবই হালকা — তাই আটকানোর দরকার নেই।
ব্যবধান **৬০ মিনিট → ২ মিনিট** (শুধু একই মুহূর্তে বারবার ডাক পড়া ঠেকাতে)।
⇒ তথ্য আগের মতোই সঙ্গে সঙ্গে আসে, অথচ খরচ কম থাকে।

**ঝুঁকি ২ — কোনো ফোনের ঘড়ি ভুল হলে সারি বাদ পড়া।**
মনে-রাখা বিন্দুটা সারির `updatedAt` থেকে বসে। কোনো ফোনে **ভবিষ্যতের** সময় বসে
গেলে বিন্দুটা সামনে লাফিয়ে যেত, আর পরের সারিগুলো বাদ পড়ত।
✅ **সংশোধন:** বিন্দুটা কখনো "এখন"-এর চেয়ে সামনে যেতে দেওয়া হয় না (clamp)।
পরীক্ষায় প্রমাণিত — নকল ডেটায় ভবিষ্যতের 2026-08-18 ছিল, জমা হয়েছে আজকের সময়।

**যেগুলো যাচাই করে দেখা গেছে ঝুঁকি নেই:**
- `flushPendingCloud('pending')` — সেভ করলেই টেবিল pending হয়; নিরাপত্তা-জাল রাত ২টা · "Sync Now" · **১৫ মিনিট নিষ্ক্রিয়তায় অটো লগ-আউট** (সেখানে পূর্ণ ফ্লাশ চলে, দিনে বহুবার)।
- delta ব্যর্থ হলে → পূর্ণ পড়া → দরকারে পুরনো `limit(2000)` পথ (কোড-পথ যাচাই করা)।
- `address_tags`-এ (০ সারি) `updatedAt` আছে কিনা অজানা — থাকলে delta, না থাকলে উপরের fallback নিজেই সামলাবে।
- Android: `BranchFilterStore`-এর ৮টি সদস্যই ব্যবহৃত ও সংজ্ঞায়িত (pillText·set·get·ALL·notChosen·choices·indexInChoices·ASK_TEXT); `DraftBuckets` ৭টি ঘর = ৭টি আর্গুমেন্ট ✅; ১৩টি ফাইলে ব্রেস/বন্ধনী ব্যালান্স ০।
- আজ মোট **১৮টি ফাইল** বদলেছে — সবকটিই পরিকল্পিত; বাইরের কিছু ছোঁয়া হয়নি।
- ডেস্কটপ ওয়েব মেপে দেখা — অপরিবর্তিত; স্টাফ সেশন — অপরিবর্তিত।

**⚠️ যে ঝুঁকিটা থেকে যাচ্ছে (সৎভাবে):**
Android কোড এখানে **কম্পাইল করে দেখা যায় না** (Gradle/kotlinc নেই)। স্ট্যাটিক যাচাই
সম্পূর্ণ, কিন্তু বিল্ড TK-কেই করতে হবে। বিল্ডে ভুল এলে সেটি হবে সহজ, দ্রুত-সংশোধনযোগ্য
লেখার ভুল — তথ্যের ক্ষতি নয়।
