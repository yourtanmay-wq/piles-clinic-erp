# V530 — ছবি · প্রিন্ট · ডাক্তারের পর্দাতেও "এক নম্বরে একাধিক রোগী"
তারিখ: ২২.০৮.২০২৬ · V529 → **V530 (5.30)**

TK-এর নির্দেশ: *"১-২-৩ একসাথে ধরুন / কিন্তু খুব সাবধানে সততার সাথে করবেন
কোন ভাল কাজ যেন খারাপ না হয়।"*

---

## ১. যে তিনটে ফাঁক বন্ধ হলো

আগে **Registration · Search · Timeline · Follow-up · Payment · Chamber** — এই
ছ'টা পর্দা "এক নম্বরে দু'জন" নিয়ম মানত। বাকি তিন জায়গা **প্রথম সারিটাই** ধরে
নিত (`rows.getJSONObject(0)`) —

| # | জায়গা | আগে যে বিপদ ছিল |
|---|---|---|
| ১ | **রোগীর ছবি** (`PatientPhotoRepository`) | দু'জনের কার্ডে **একই ছবি**; একজনের ছবি বদলালে অন্যজনেরটাও |
| ২ | **Print Centre** (`PrintCenterActivity`, ৬ জায়গা) | **ভুল রোগীর নামে** প্রেসক্রিপশন · Diet Chart · Patient Card · রসিদ · Blood Test ছাপা |
| ৩ | **Doctor Visit** (`DoctorVisitActivity`, ৫ জায়গা) | ভুল রোগীর নামে রেফারেল-আয় ও ভিজিট সেভ |

---

## ২. যেভাবে সারানো হয়েছে — একটাই নিয়ম, এক জায়গায়

নতুন কোনো নিয়ম **বানানো হয়নি**। V520 থেকে `PaymentRepository.identitiesOnMobile()`-এ
যে নিয়মটা চলছে ও পরীক্ষায় পাশ করেছে, হুবহু সেটাই শেয়ার-করা জায়গায়
(`PatientIdentity.kt`) তোলা হলো, যাতে সব পর্দা **একই উত্তর** পায়:

```
১) ভুলে দু'বার রেজিস্ট্রেশন হওয়া সারিগুলো  ⇒ একজন ধরেই গোনা (pickPatientRow, B30)
২) স্টাফ নিজে "Different Patient — Same Mobile" চেপে ঘোষণা করা প্রত্যেকে ⇒ আলাদা
```

নতুন দুটো ফাংশন:
- `PatientIdentity.separateIdentities(rows, mobile)` — এই নম্বরে **ক'জন আলাদা রোগী**
- `PatientIdentity.chooseRow(rows, branch, rowId, code)` — **row id আগে, তারপর কোড**

তারপর তিন জায়গাতেই এক নিয়ম:

> **তালিকার আকার ১ (রোজকার ৯৯%) ⇒ হুবহু আগের লাইনটাই চলে।
> ২ বা বেশি হলে তবেই এক লাইনে জিজ্ঞাসা করা হয় — "কোন রোগী?"**

কার্ড থেকে খুললে জিজ্ঞাসাই করে না — **Timeline ও Follow-up কার্ড এখন "কোন রোগী"
সাথেই পাঠায়** (`patientRowId` / `patientCode` — হুবহু সেই দুটো ঘর যেগুলো
`openPaymentFor` V520 থেকে পাঠাচ্ছে)।

---

## ৩. ⛔ যা এক অক্ষরও বদলায়নি (ভাল কাজ যেন খারাপ না হয়)

- **এক নম্বরে একজন রোগী থাকলে তিনটে পর্দাই আগের মতোই চলে** — কোনো প্রশ্ন নেই,
  কোনো বাড়তি চাপ নেই, ফলও হুবহু আগের সেই প্রথম সারি।
- **ছাপার নকশা · ঘর · ক্রম · লেখা — কিচ্ছু ছোঁয়া হয়নি।**
- `PatientIdentity.pickPatientRow` (নয়টা পর্দার শেয়ার-করা V143 নিয়ম) — **ছোঁয়া হয়নি**,
  শুধু নতুন ফাংশন দুটো ওটাকে ব্যবহার করে।
- `myBranchOnly()` (ব্রাঞ্চের পাহারা) — **অপরিবর্তিত**, আগের মতোই আগে ছাঁকে।
- `savePhoto()` আগে থেকেই **row id ধরে** সেভ করত — তাই ঠিক সারিটা বাছা হলেই
  সেভও ঠিক জায়গায় যায়।
- **Database schema · RLS · production data — কিছুই ছোঁয়া হয়নি।** কোনো SQL চালাতে হবে না।
- **নতুন কোনো ক্লাউড-অনুরোধ নেই।** Print Centre আগে-আনা সারিগুলোর উপরেই কাজ করে।
  ছবির পর্দায় একটাই বাড়তি ডাক, আর সেটাও **শুধু তখনই** যখন স্টাফ নিজে নম্বর
  টাইপ করেছেন (কার্ড থেকে এলে নয়)।

---

## ৪. ওয়েব (Netlify) — যাচাই করে দেখা গেল **কিছু বদলানোর দরকার নেই**

আমি ধরে নিয়েছিলাম ওয়েবেও একই ফাঁক আছে। **কোড ধরে যাচাই করে দেখা গেল তা নয়:**

- ছবি সম্পাদনার আসল পথ `editPatientPhoto('${p.id}')` — **আগে থেকেই row id ধরে** ✅
- মোছার পথ `removePatientPhoto` / `removePatientPhotoById` — **দুটোই row id ধরে** ✅
- `editPatientPhotoByMobile` (মোবাইল ধরে) — খুঁজে দেখা গেল **কোথাও থেকে ডাকা হয় না**
  (`handlePatientPhotoTap`-ও নয়); অর্থাৎ পৌঁছনোই যায় না।

⇒ **ওয়েবের একটা অক্ষরও বদলানো হয়নি।** দরকার নেই বলে বদলাইনি — এটাই সৎ কাজ।

---

## ৫. যাচাই

**নতুন নিয়মটা সত্যিই compile করে চালানো হয়েছে** (নকল কপি নয় — আসল
`PatientIdentity.kt` ফাইলটাই তুলে এনে, `PatientModel.isDeclaredSeparateRowId`-ও
আসল ফাইল থেকে হুবহু কেটে):

**১৫টি পরীক্ষা · ১৫টিই PASS** —
একজন রোগী · ডুপ্লিকেট দুটো সারি তবু একজন · ডুপ্লিকেটে bill-ওয়ালা সারিটাই বাছা ·
ঘোষিত আলাদা রোগী দু'জন · ক্রম ঠিক · **অন্য নম্বরের `pat_` আইডি ভুল করে ঘোষিত ধরা হয় না** ·
খালি তালিকা · rowId দিলে সেই সারি · code দিলে সেই সারি ·
**rowId ও code অমিল হলে rowId জেতে** (V522-এ ধরা পড়া সেই বাগ) ·
দুটোই ফাঁকা হলে আগের নিয়ম · অচেনা rowId হলে আগের নিয়মে ফেরত · branch পছন্দ অটুট।

**পুরোনো সব পরীক্ষা আবার চালানো হয়েছে — একটাও ভাঙেনি:**

| suite | ফল | | suite | ফল |
|---|---|---|---|---|
| cham | **27 PASS · 0 FAIL** | | rot | **21 PASS · 0 FAIL** |
| rep | **25 PASS · 0 FAIL** | | family | **29 PASS · 0 FAIL** |
| tt | **19 PASS · 0 FAIL** | | futab | **19 PASS · 0 FAIL** |
| rc | **19 PASS · 0 FAIL** | | gtest | **13 PASS · 0 FAIL** |

**ওয়েব:** ৮টা `.js` ফাইলেই `node --check` — সব ঠিক।

---

## ৬. ⚠️ সৎ সীমাবদ্ধতা (লুকাচ্ছি না)

1. **Activity ফাইল চারটে compile করে দেখানো যায়নি** — এখানে Android SDK নামে না
   (`dl.google.com` → 403)। তার বদলে হাতে যাচাই: বন্ধনীর হিসাব `git HEAD`-এর
   সাথে মিলিয়ে দেখা (চারটেতেই `+0`), ব্যবহৃত প্রতিটা নাম ও ঘর কোডে খুঁজে
   প্রমাণ করা (`refId`, `preferPatientRowId`, `digits`, `mobileDigits`, `.s()`,
   `AlertDialog`, `kotlin.coroutines.resume`)। **TK-কে বিল্ড করে দেখে নিতে হবে।**
2. **ছবির ঘরে `limit` ১ থেকে ২০ করা হয়েছে** (নইলে দ্বিতীয় রোগীর সারিটা আসতই না)।
   query-র **সংখ্যা বাড়েনি**, শুধু একটাই query একটু বেশি সারি আনতে পারে —
   B30-এর পরে একই নম্বরে সারি সাধারণত ১টাই।
3. **এক নম্বরে একজন থাকলে "প্রথম সারি" কোনটা, সেটা সার্ভারের ক্রমের উপর নির্ভর
   করে** — এটা আগেও তাই ছিল, নতুন কিছু নয়। আর ঠিক যেখানে এটা বিপজ্জনক (দু'জন
   থাকলে), সেখানে এখন আর ক্রমের উপর নির্ভর করা হয় না — জিজ্ঞাসা করা হয়।
4. **স্টাফ *Cancel* করলে** "রোগী মেলেনি"-র পুরোনো পথটাই চলে (কিছু ছাপা/সেভ হয় না)।
   ভুল রোগীর নামে কিছু হয়ে যাওয়ার চেয়ে এটাই নিরাপদ।

---

## ৭. TK-এর করণীয়

1. **V530 বিল্ড করে APK** · Netlify-তে `03_NETLIFY_READY` (cache `?v=v530`)
2. এক নম্বরে দু'জন রোগী রেখে —
   - **Print Centre** থেকে প্রেসক্রিপশন/Patient Card ছাপুন → "কোন রোগী?" জিজ্ঞাসা করছে কিনা
   - **কার্ডের ছবিতে তিন-চাপ** দিন → ঠিক সেই রোগীরই ছবি খুলছে কিনা
   - **Doctor Visit**-এ নম্বর লিখুন → ঠিক নাম বসছে কিনা
3. 🔴 **একজন রোগীর সাধারণ নম্বরে সব আগের মতোই আছে কিনা** — এটাই সবচেয়ে জরুরি।
