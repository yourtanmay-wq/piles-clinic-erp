# V411 — RMP Due List অন্ধ ছিল, চোখ ফেরানো হলো (১৭.০৮.২০২৬)

> **এক লাইনে:** TK ছবি দিয়ে ধরলেন — Due List বলছে "কারও কিছু বাকি নেই ₹0",
> অথচ PK-র কার্ডে "Ref. Due ₹41,750"। যাচাই করে দেখা গেল **পাঁচ ব্রাঞ্চ মিলিয়ে
> সত্যিই ₹২,১০,৮৫০ বাকি**, যার একটাকাও ওই তালিকায় উঠত না। সারানো হলো।

---

## ১. TK যা ধরলেন

| ছবি | কী বলছিল |
|---|---|
| **RMP Due List** (Cooch Behar) | "NOBODY IS PENDING · Every RMP is fully settled" — **₹0** |
| **PK-র কার্ড** (Cooch Behar-এরই RMP) | **Ref. Due ₹41,750** |

দুটো পর্দা একে অন্যকে মিথ্যে বলছিল।

## ২. আসল কারণ (কোড + লাইভ ডেটা ধরে যাচাই)

অ্যাপে **দুটো আলাদা পদ্ধতিতে** RMP-র পাওনা হিসাব হয়:

| পদ্ধতি | কোথায় থাকে | কে ব্যবহার করত |
|---|---|---|
| **পুরনো** | `doctor_visits.referralPayments` (হাতে লেখা এন্ট্রি) | RMP Due List |
| **নতুন** (V325/V380) | `fin.rmp_patient_commissions` (রোগীর বিল × %) | ডাক্তারের নিজের কার্ড |

`showRmpDueList()`-এর ধাপ ২-এ লেখা ছিল:

```kotlin
val check = rows.take(25)
if (check.isEmpty()) return@launch      // 🔴 এখানেই দোষ
```

`rows` বানানো হত **শুধু পুরনো** পদ্ধতি থেকে। তাই যে RMP-র পুরনো পদ্ধতিতে কিছু
বাকি নেই, তিনি `rows`-এ ঢুকতেনই না ⇒ ক্লাউডকে জিজ্ঞেসই করা হত না ⇒ তালিকা
ফাঁকা ⇒ "NOBODY IS PENDING"।

**লাইভ ডেটা তা-ই বলল:** Cooch Behar-এ পুরনো পদ্ধতিতে বাকি আছে এমন RMP **০ জন**,
কিন্তু নতুন পদ্ধতিতে কমিশন আছে এমন **২ জন**।

## ৩. আসল অঙ্ক (TK নিজে SQL চালিয়ে পেয়েছেন)

| ব্রাঞ্চ | RMP | রোগী | মোট কমিশন | দেওয়া | **বাকি** |
|---|---|---|---|---|---|
| Falakata | Tk Bisaws | ১৫ | ₹১,৮২,১০০ | ₹০ | **₹১,৬২,১০০** |
| Cooch Behar | PK | ৫ | ₹৪১,৭৫০ | ₹০ | **₹৪১,৭৫০** |
| Cooch Behar | PKB | ২ | ₹৭,০০০ | ₹০ | **₹৭,০০০** |
| | | | | **মোট** | **₹২,১০,৮৫০** |

(Tk Bisaws-এর ₹১,৮২,১০০-র মধ্যে ₹২০,০০০ অ্যাডভান্স দেওয়া আছে ⇒ বাকি ₹১,৬২,১০০।)

### হিসাবের নিয়ম নিজে মিলিয়ে দেখা
PK-র ৫ জন রোগীর বিল ও জমা ধরে হাতে হিসাব করে **₹৪১,৭৫০** পাওয়া গেল —
অ্যাপের কার্ডে যা লেখা, **হুবহু তাই**। ⇒ নিয়ম সঠিক, তবেই এগোনো হয়েছে।

## ৪. যা করা হলো

### ক) নতুন সার্ভার-ফাংশন — `fin.rmp_branch_due(p_branch)`
**এক ডাকেই** ওই ব্রাঞ্চের সব RMP-র পাওনা। ৪১২ জনের জন্য ৪১২ বার ডাকতে হয় না
⇒ Egress-এ চাপ পড়ে না।
- হিসাবের অঙ্ক `fin.rmp_summary()`-এর **হুবহু নকল** (বিল−ছাড় · চিকিৎসার জমা
  − অনুমোদিত ফেরত · PERCENT/AMOUNT · দেওয়া বাদ · অ্যাডভান্স বাদ)
- পাহারা অন্য RMP-ফাংশনের মতোই — `fin.rmp_can_use()` + ব্রাঞ্চ-পরীক্ষা
- `stable` — **শুধু পড়ে**, কিছু বদলায় না
- ফাইল: `04_SUPABASE_DATABASE_SETUP/V411_RMP_BRANCH_DUE_2026-08-17.sql`
- ✅ **TK নিজে চালিয়েছেন, Success**

> 📌 SQL Editor-এ `select * from fin.rmp_branch_due('Cooch Behar')` চালালে
> `Master, Staff or Doctor identity required` আসে — **এটা ভুল নয়, পাহারা।**
> Editor-এ কেউ অ্যাপে লগইন করা থাকে না। অ্যাপ থেকে ঠিক চলে।

### খ) ফোন — `DoctorVisitActivity.showRmpDueList()`
- ভুল লাইনটা (`if (check.isEmpty()) return@launch`) **বাদ**
- নতুন ফাংশন এক ডাকে ডেকে, পুরনো ও নতুন **দুটো তালিকা মিলিয়ে** একটাই তালিকা
- একই RMP দুই জায়গায় থাকলে **নতুন পদ্ধতির সংখ্যাই চূড়ান্ত** — কারণ ডাক্তারের
  কার্ডেও (Ref. Due) ওটাই দেখানো হয় ⇒ **দুই পর্দা আর কখনো আলাদা বলবে না**
- ⛔ ব্যর্থ/উদ্ভট ফল হলে সব বাতিল ⇒ আগের আচরণ হুবহু বজায়

### গ) কম্পিউটার — `wlv1RmpDueVerify()` (হুবহু একই সংশোধন)
TK-এর স্থায়ী নিয়ম মেনে ফোন ও কম্পিউটার দুটোতেই একই ব্যবস্থা।

## ৫. যাচাই

### কম্পিউটারে — আসল ব্রাউজারে চালিয়ে (Chromium)
নকল সার্ভার বসিয়ে PK-র মতো একজন RMP দিয়ে পরীক্ষা — যাঁর **পুরনো পদ্ধতিতে
কিছুই বাকি নেই**:

| | ফল |
|---|---|
| পুরনো পদ্ধতিতে তালিকায় কতজন | **০** ← আগের বাগ |
| নতুন ফাংশন ডাকা হলো কি | **হ্যাঁ** ✅ |
| পর্দায় শেষে কী উঠল | **"TOTAL STILL TO PAY · 1 RMP has money pending · ₹41,750"** ✅ |
| PK-র কার্ড | দেখাচ্ছে ✅ |

`node --check app.js` — ঠিক আছে।

### ডেটাবেসে
নতুন ফাংশনটা এখানে আসল PostgreSQL 16-এ বানিয়ে নকল তথ্য দিয়ে চালিয়ে দেখা
হয়েছে — অঙ্ক ঠিক আসছে, তবেই TK-কে দেওয়া হয়েছে।

### ফোনে
বন্ধনীর হিসাব (`{}` `()` `[]`) — বদলানো দুটো ফাইলেই **০**।
ব্যবহৃত সব নাম যাচাই: `RepoResult` · `ModuleAuth.rpc` · `BranchDueRow` ·
`activeDoctorBranch()` · `allItems` — সবই আছে।

🔴 **সীমাবদ্ধতা:** এই কম্পিউটারে Kotlin কম্পাইল করা যায় না। বিল্ডে ভুল দেখালে
ছবি পাঠাবেন, সঙ্গে সঙ্গে ঠিক করে দেব।

## ৬. বদলানো ফাইল

| ফাইল | কী |
|---|---|
| 🆕 `04_SUPABASE_DATABASE_SETUP/V411_RMP_BRANCH_DUE_2026-08-17.sql` | নতুন ফাংশন (চালানো হয়ে গেছে) |
| `native/RmpCommissionRepository.kt` | `branchDue()` যোগ |
| `native/DoctorVisitActivity.kt` | Due List-এ নতুন পদ্ধতি জুড়ল |
| `03_NETLIFY_READY/app.js` | কম্পিউটারেও একই |
| `03_NETLIFY_READY/index.html` | cache `?v=v430` → **`?v=v432`** |
| `app/build.gradle.kts` | ভার্সন ৪১০ → **৪১১ / "4.11"** |

⛔ টাকার কোনো অঙ্ক · কমিশনের % · কে কত পাবে — **কিছুই বদলানো হয়নি**।
   শুধু যা **আগে থেকেই পাওনা ছিল অথচ দেখা যাচ্ছিল না**, সেটা দেখানো হচ্ছে।

## ৭. TK-এর যা বাকি

1. **V411 APK** বানিয়ে ফোনে বসানো → RMP Due List খুলে দেখা,
   Cooch Behar-এ **PK ₹৪১,৭৫০ ও PKB ₹৭,০০০** আসছে কিনা,
   Falakata-য় **Tk Bisaws ₹১,৬২,১০০**।
2. Netlify আপলোড (`?v=v432`) — কম্পিউটারেও একই।
3. **RMP Performance-এ PK ৫ নম্বরে আছেন কিনা** একটু স্ক্রল করে দেখা
   (তালিকা শেষ-রেফারের তারিখ ধরে সাজানো; PK-র শেষ রেফার ০৩.০৮, তাই
   ১৪.০৮ ও ০৭.০৮-এর চারজনের নিচে থাকার কথা)। না থাকলে জানাবেন।
