# 📒 খাতা — সেশন ২১.০৮.২০২৬ (V496)

**ভিত্তি:** V495 (checksum যাচাই করে — V494-এর Android কাজ V495-এ **০ পার্থক্যে**
অক্ষত, তার উপরে V495-এর ওয়েব dedupe)। **V494 ও V495 ছোঁয়া হয়নি।**

## যা করা হলো

### ডাক্তার (TK §২ · §৩)
- Dashboard-এ **Work Notebook টাইল লুকানো** — আগে ডাক্তাররাও পেতেন
- **হাজিরা-রিমাইন্ডার আর যায় না** — আগে ডাক্তারের ফোনেও বাজত
- Work Notebook-এ ঢুকলে সঙ্গে সঙ্গে ফিরে যান (নিরাপত্তা-জাল)
- **অ্যাপ খোলার আঙুলের ছাপ** (`DoctorAppLock`) — শুধু অ্যাপ খোলা, **কখনো হাজিরা নয়**

### 🔴 যে ফাঁদটা ধরা পড়েছে
`permissionRole()` ডাক্তারকে **`"staff"`** বানায় (NativeSession.kt:25)। তাই
`user.role` দেখে ডাক্তার চেনা **অসম্ভব** — এই কারণেই V495 পর্যন্ত ডাক্তাররা
IN/OUT TIME ও রিমাইন্ডার পেতেন। এখন `RoleRules` → `displayRole` (প্রজেক্টের
আগে থেকেই প্রমাণিত পথ), আর সার্ভারে `role_kind`।

### Staff-এর IN TIME (TK §৪ · §৬)
পাঁচ ধাপ: Staff কিনা → ছুটি → **ক্লিনিক (GPS)** → **আঙুলের ছাপ** → **সার্ভারে atomic সেভ**
- ফোন **কিছুই পাঠায় না** — সময়, তারিখ, ব্রাঞ্চ, কোড সবই সার্ভার ঠিক করে
- **ফোনের ঘড়ি বদলে ফাঁকি দেওয়া বন্ধ**
- **দিনে একবারই** — প্রথম সময় কখনো বদলায় না

### ৭ দিনের লগআউট (TK §১০)
- "ব্যবহার" = **মানুষ নিজে অ্যাপ সামনে এনেছেন** (`onActivityStarted`)
- Worker · Realtime · নোটিফিকেশন — একটাও হিসাব পিছোয় না
- **ঘড়ি পিছিয়ে দিলেও কাজ হয় না** (`maxSeen` নিয়ম)
- কোনো তথ্য মোছে না

### Suspend/Remove (TK §১১)
- পুরনো `active` ও `suspended_until`-ই সত্যের উৎস — **নতুন কিছু বানানো হয়নি**
- অ্যাপ সামনে এলে ও **গুরুত্বপূর্ণ লেখার আগে** যাচাই, ১৫ মিনিটের সীমাসহ
- ⚠️ **অফলাইনে জানা সম্ভব নয়** — সৎভাবে লেখা আছে

## SQL — ✅ লাইভে চালানো হয়ে গেছে

`04_SUPABASE_DATABASE_SETUP/V496_MARK_CHECK_IN_2026-08-21.sql` — **একটাই ফাংশন**
(`wn.mark_check_in()`)। নতুন টেবিল/ঘর নেই · কিছু মোছে না · RLS বদলায়নি।

| | |
|---|---|
| **চালানো হলো** | **২১.০৮.২০২৬ · ৮:৫২** (TK নিজে, Supabase SQL Editor) |
| প্রকল্প | `biswasayurvedkishanganj` · branch `main` |
| ফল | **Success. No rows returned** ✅ |
| ফাইলের checksum | `a41ed377b8e6bdaf…` |
| অনুমতি | TK নিজে চেয়েছিলেন ("Sql run কপি করে দিন") |

⛔ Claude নিজে লাইভ ডেটাবেসে কিছু চালায়নি — ফাইলটা পাঠানো হয়েছিল, **TK নিজে
Run করেছেন**। আগে একই SQL আলাদা, খালি PostgreSQL 16-এ ১১টা পরীক্ষায় পাশ
করেছিল (`00_GUARD/v496_sql_test/`)।

⚠️ **এখনো বাকি:** ফাংশনটা বসে গেছে, কিন্তু **অ্যাপ এখনো বিল্ড হয়নি**। তাই
IN TIME-এর নতুন পথ ফোনে চালু হয়নি — পুরনো APK আগের মতোই চলছে।
⛔ পুরনো APK এই ফাংশনটা ডাকেই না, তাই কিছু ভাঙার ঝুঁকি নেই।

## পরীক্ষায় ধরা পড়া বাগ (দুটোই ঠিক করা)
1. SQL: `work_date` ambiguous → `#variable_conflict use_column`
2. SQL: একই মিনিটে দ্বিতীয় চাপে ভুল status → `get diagnostics row_count`

## পাহারাদার যা ধরেছে (দুটোই ঠিক করা)
1. `Result` নাম WorkManager-এর `Result`-এর সঙ্গে গুলিয়ে যাচ্ছিল → `Outcome`
2. **৪৬টা নতুন বাংলা লেখার ইংরেজি ছিল না** → `NoBengali`-তে যোগ করা হলো
   (খাতার সারি B158 — বাংলা-বন্ধ স্টাফের পর্দা)

## ⚠️ যা এখনো বাকি
**পাঁচ ব্রাঞ্চের অক্ষাংশ-দ্রাঘিমা।** প্রকল্পের Google Maps লিংক পাঁচটাই খোলা
হয়েছে — নাম ও ঠিকানা মিলেছে, কিন্তু **কোঅর্ডিনেট পাওয়া যায়নি** (Google Maps
JavaScript চায়; সংখ্যা-বহনকারী ঠিকানা `robots.txt`-এ বন্ধ)।
⛔ একটাও সংখ্যা আন্দাজে বসানো হয়নি। খালি থাকলে অ্যাপ IN TIME **নেয় না**।
বসানোর জায়গা: `ClinicLocations.kt` — **একটাই ফাইল**।

## ছোঁয়া হয়নি
OUT TIME · ছুটি · বেতন · রিপোর্ট · WhatsApp · রোগী · টাকা · Refund · RMP ·
ছাপা · নোটিফিকেশন · RLS · ব্রাঞ্চের অনুমতি · Master-এর নিয়ম ·
V493–V495-এর Egress/Dedupe · **ওয়েবের কিছুই নয়** (app.js হুবহু এক)।

*লেখা হলো ২১.০৮.২০২৬ · SQL চালানোর রেকর্ড যোগ ৮:৫২-এ*

---

# 🔁 V496 সংশোধন — ২১.০৮.২০২৬ (TK-এর ৮-দফা নির্দেশ)

TK-এর নতুন নির্দেশ অনুযায়ী V496-এর উপরেই নিচের কাজগুলো করা হলো।
⛔ Live Supabase-এ কিছু চালানো হয়নি · পুরোনো কোনো data মোছা হয়নি ·
V494/V495 ছোঁয়া হয়নি · APK প্রকাশ করা হয়নি।

## ২. `field` বাদ — হাজিরা শুধু আসল `staff`
* `RoleRules.usesAttendance()` — `ROLE_FIELD` সরানো হলো (এখন শুধু `ROLE_STAFF`)
* `wn.mark_check_in()` — `not in ('staff','field')` → `<> 'staff'`
* পরীক্ষায় প্রমাণ: `KNE-FIELD` এখন `not_staff` ফেরায় (আগে `saved` ফেরাত)

## ৩. Doctor ও Field-এর Salary বন্ধ (record অক্ষত)
* `RoleRules.salaryAppliesToRoleKind(role_kind)` — এক জায়গায় নিয়ম
* `StaffProfileActivity.renderSelf()` — ডাক্তার/ফিল্ড-এর পর্দায় "My Salary"
  ভাগটাই বসে না, এবং বেতনের **দুটো cloud-read-ও করা হয় না** (Egress বাঁচে)
* `StaffProfileActivity.loadSalary()` — শেষ পাহারা: `role_kind` staff না হলে
  পর্দা খোলে না (একই পড়াতেই `role_kind` আনা হয় — বাড়তি read নয়)
* Master-এর তালিকায় আগে থেকেই শুধু `role_kind = staff` দেখানো হতো — অপরিবর্তিত
* ⛔ `hr.salary_config` / `hr.salary_payments`-এর একটাও সারি মোছা হয়নি
* ⛔ ডাক্তারের চিকিৎসার কাজ (Briefing · রোগী · ওষুধ) ছোঁয়া হয়নি

## ৪. সেন্সর না থাকলে ডাক্তারের অ্যাপ খুলবে না
* `DoctorAppLock.ALLOW_WHEN_NO_HARDWARE = true` → **`false`**
* সেন্সর নেই / এই Android-এ সম্ভব নয় / নিরাপত্তা আপডেট বাকি — তিন অবস্থাতেই
  পরিষ্কার বাংলা বার্তা + একটাই বোতাম "অ্যাপ বন্ধ করুন" (`allowRetry = false`)
* ⛔ crash নয় · সাদা পর্দা নয় · **কোনো গোপন PIN/পাসকোডের পথ নেই**

## ৫. গুরুত্বপূর্ণ লেখার আগে টাটকা Active/Suspend যাচাই
* নতুন `SessionGuard.WRITE_FRESH_MS = 60_000` (১ মিনিট)
* `ensureFreshForWrite()` এখন রুটিনের ১৫ মিনিট নয়, **১ মিনিট** মানে
* রুটিন (অ্যাপ সামনে আনা) যাচাই আগের মতোই ১৫ মিনিটে একবার
* ⚠️ **সৎ সীমা (লুকানো হয়নি):** অফলাইনে বা সার্ভার উত্তর না দিলে যাচাই হয় না
  এবং লেখা **আটকানো হয় না** (fail-open) — নইলে নেট-সমস্যায় সবার রোগী-এন্ট্রি
  ও টাকার হিসাব থেমে যেত (খাতার সারি B446)। অর্থাৎ **১০০% নিরাপত্তার দাবি
  করা হচ্ছে না**; মাস্টার বন্ধ করার পরেও সর্বোচ্চ ~১ মিনিট একটা লেখা ঢুকে
  যেতে পারে। না-ফসকানো আসল পাহারা সার্ভারের RLS ও `wn.mark_check_in()`-এ।

## ৬. পাহারাদার এখন অসম্পূর্ণ কাজে "PASS" বলে না
`00_GUARD/verify_v496_attendance.py`:
* GPS coordinate খালি থাকলে এটা আর "নোট" নয় — **INCOMPLETE**, exit code 1
* নতুন যাচাই: `usesAttendance`-এ `field` নেই · SQL-এ `<> 'staff'` ·
  `ALLOW_WHEN_NO_HARDWARE = false` · `WRITE_FRESH_MS` · বেতনের ভূমিকা-যাচাই ·
  বেতনের সারি মোছা হচ্ছে না · fail-open সীমা নথিতে লেখা আছে

## ১. ⛔ যা করা যায়নি — GPS coordinate (সৎ রিপোর্ট)
**পাঁচটার একটাও বসানো যায়নি। একটাও আন্দাজে বসানো হয়নি।**

প্রকল্পের পাঁচটা Google Maps লিংক (`PatientMessage.kt:110-134`) খোলা হয়েছে ও
প্রতিটার নাম-ঠিকানা মিলিয়ে দেখা হয়েছে (`ClinicLocations.kt`-এ `verifiedPlace`)।
কিন্তু **সংখ্যা** পাওয়ার প্রতিটা পথ এই পরিবেশে বন্ধ:

| পথ | ফল |
|---|---|
| `maps.app.goo.gl/…` → `/maps/place/…` | "Enable JavaScript to see Google Maps" |
| `maps.google.com/?ftid=…` | ROBOTS_DISALLOWED |
| `nominatim.openstreetmap.org` | ROBOTS_DISALLOWED |
| `photon.komoot.io` | ROBOTS_DISALLOWED |
| `api.openstreetmap.org` | ROBOTS_DISALLOWED |
| `mappls.com` | লগইন পর্দা, কোনো সংখ্যা নেই |

⇒ TK-এর নিয়ম মেনে **থামা হলো**, আন্দাজ করা হয়নি, এবং পাহারাদার
   "PASS" না বলে **INCOMPLETE** বলছে।

### ⚠️ ঠিকানায় অমিল (শুধু জানানো — বদলানো হয়নি)
* **Cooch Behar** — প্রকল্পে "Opp. Mini Bus Stand, Sengupta Complex",
  লিংক দেখায় "Cooch Behar Palace, Rajbari Park Rd"
* **Falakata** — প্রকল্পে "BDO Office Road, near Hotel Nandonik",
  লিংক দেখায় "Kunjanagar Rd, near Railgate"
* **Birpara** — রাস্তা এক (M.G. Road), চিহ্ন আলাদা: প্রকল্পে "near Axis Bank",
  লিংক দেখায় "near Punjab National Bank"

## পরীক্ষা যা সত্যি চালানো হয়েছে (২১.০৮.২০২৬)
* খালি PostgreSQL 16.13-এ V496 SQL নতুন করে চালিয়ে `test_roles.sql` ও
  `test_once_a_day.sql` — সব প্রত্যাশিত ফল
* ৮টা একসঙ্গে সংযোগ → **১টা `saved`, ৭টা `already`, ডেটাবেসে ১টাই সারি**
* সাতটা পাহারাদার স্ক্রিপ্ট — `verify_v496_attendance.py` ইচ্ছাকৃতভাবে
  INCOMPLETE, বাকি সব PASS
* ⚠️ **Kotlin কম্পাইল করা যায়নি** (Android SDK/Maven এই পরিবেশে ৪০৩) —
  **Android Studio Build pending — owner will build**

*লেখা হলো ২১.০৮.২০২৬*

---

# ✅ ১ নম্বর কাজ শেষ — পাঁচ ব্রাঞ্চের GPS বসল (২১.০৮.২০২৬ · ৯:১৪–৯:২০)

Claude নিজে সংখ্যা বের করতে পারেনি (Google Maps JavaScript চায়, বাকি সব
উৎস `robots.txt`-এ বন্ধ)। তাই **TK BISWAS নিজে** ফোনের Google Maps-এ প্রতিটা
চেম্বারে পিন বসিয়ে সংখ্যা পাঠিয়েছেন — প্রতিটার সঙ্গে স্ক্রিনশট-প্রমাণ।

| ব্রাঞ্চ | অক্ষাংশ | দ্রাঘিমা | সময় | প্রমাণ |
|---|---|---|---|---|
| Kishanganj | 26.101650 | 87.957309 | ৯:১৪ | "Biswas Piles Clinic"-এর নিজের পিন |
| Jalpaiguri | 26.536368 | 88.720920 | ৯:১৫ | "Pr. TK BISWAS · মা আয়ুর্বেদ", Raikatpara |
| Cooch Behar | 26.327655 | 89.442545 | ৯:১৬ | "Maa Ayurved Piles Clinic"-এর গায়ে |
| Falakata | 26.522931 | 89.201584 | ৯:১৯ | "Near Hotel Nandanik Palace, Vidya Sagar Pally" |
| Birpara | 26.708834 | 89.140122 | ৯:২০ | "MAA AYURVED PILES CLINIC"-এর গায়ে, M.G. Road |

`radiusMeters = 150` (সবার জন্য এক) — TK না বললে বদলাবে না।

## ঠিকানার আগের তিনটে অমিল — মিটে গেছে
* **Falakata** — Maps লিংক দেখাত "Kunjanagar Rd, near Railgate", কিন্তু
  TK-এর পিন "Hotel Nandanik-এর কাছে" ⇒ **প্রকল্পের নিজের ঠিকানার সঙ্গেই মেলে**।
  ⇒ TK-এর পিনই ধরা হয়েছে, Maps লিংকের ঠিকানা নয়।
* **Cooch Behar** ও **Birpara** — TK-এর পিন সরাসরি ক্লিনিকের গায়ে বসেছে,
  তাই আর কোনো সন্দেহ নেই।

## পাহারাদারের ফল (এই পরিবর্তনের পরে)
`00_GUARD/verify_v496_attendance.py` → **PASS** (আগে INCOMPLETE ছিল)।
বাকি ছয়টা পাহারাদারও PASS।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

## ✅ সংশোধিত SQL লাইভে চালানো হলো — ২১.০৮.২০২৬ · ৯:২৩

TK নিজে Supabase SQL Editor-এ (project `biswasayurvedkis…`, branch `main`)
সংশোধিত `wn.mark_check_in()` চালিয়েছেন → **"Success. No rows returned"**
(ফটো-প্রুফ)। এখন সার্ভারেও হাজিরা **শুধু `role_kind = staff`** — Field
অফিসার আর হাজিরা দিতে পারবেন না।

⛔ Claude নিজে লাইভে কিছু চালায়নি — শুধু SQL কপি করে দিয়েছে।
⛔ কোনো সারি মোছা হয়নি (`create or replace` মাত্র)।

### ⚠️ একই ফটোতে দেখা গেল — Supabase-এর কোটা সতর্কবার্তা
> *"Organization exceeded its quota in the previous billing cycle.
>  Projects will be restricted from 13 Aug, 2026 if your organization
>  remains over quota."*

আজ ২১.০৮.২০২৬ — অর্থাৎ **তারিখটা পেরিয়ে গেছে**, তবু প্রজেক্ট এখনো চলছে
(SQL চলল)। V493–V495-এর Egress কমানোর কাজ **নতুন বিলিং চক্রে** ফল দেবে;
পুরনো চক্রের হিসাব সেটা পিছিয়ে বদলাতে পারে না।
⇒ TK-কে জানানো হলো, কোনো সিদ্ধান্ত নেওয়া হয়নি (Billing TK-এর নিজের বিষয়)।

*লেখা হলো ২১.০৮.২০২৬*

---

# 🔴 V497 — বিল্ড ভেঙেছিল · ভুল সংশোধন · পাহারাদার শক্ত করা (২১.০৮.২০২৬ · ৯:৩৬)

## যা হয়েছিল (আমার দোষ)
TK V496-এর ZIP নিয়ে Android Studio-তে Build দিলে **১৮টা error**-এ বিল্ড ভাঙে:

    Classifier 'Reason' does not have a companion object, and thus must be initialized here
    Comparison of incompatible enums 'BiometricGate.Reason' and …
    Classifier 'Status' does not have a companion object …
    'when' expression must be exhaustive, add necessary 'SAVED' …

কারণ `modules/WorkNotebookActivity.kt`-এ আমি লিখেছিলাম —

    val G = BiometricGate.Reason              // ⛔ Kotlin-এ enum ক্লাস চলকে রাখা যায় না
    val canRetry = r == G.FAILED …
    val S = AttendanceRepository.Status       // ⛔ একই ভুল
    when (res.status) { S.SAVED, … }

TK-এর কথা (হুবহু): *"কেমন পাহারাদার বসিয়েছেন, আমাকে এরকম ফাইল আপনি পাঠালেন
কি করে? পাহারাদার কে আরো শক্ত করবেন… কারণ আমি একজন সাধারন ব্যবহারকারী,
আমি কোন ডেভলপার নই।"* — সম্পূর্ণ ন্যায্য অভিযোগ।

## সংশোধন
`val G = …` / `val S = …` তুলে দিয়ে সরাসরি লেখা হলো, আর দুটো import যোগ:

    import com.tkbiswas.pilesclinic.native.AttendanceRepository
    import com.tkbiswas.pilesclinic.native.BiometricGate
    …
    val canRetry = r == BiometricGate.Reason.FAILED || …
    when (res.status) { AttendanceRepository.Status.SAVED, … }

⛔ হাজিরার নিয়ম · বার্তা · রং · GPS · আঙুলের ছাপ — একটাও বদলায়নি,
   শুধু লেখার ধরন ঠিক করা হলো।

## 🛡️ নতুন পাহারাদার — এবার সত্যিকারের কম্পাইলার
### ১. `00_GUARD/verify_kotlin_compile.py` (প্রধান)
* JetBrains-এর **আসল `kotlinc` ১.৯.২৪** নামিয়ে পুরো Kotlin কোড কম্পাইল করে।
* এই পরিবেশে Android SDK/androidx নামানো যায় না (`dl.google.com`, `maven`
  বন্ধ), তাই হাজার হাজার "unresolved reference" আসে — সেগুলো আসল ভুল নয়।
  ⇒ সমাধান: **বেসলাইন** (`00_GUARD/kotlin_noise_baseline.txt`, ৪১৩১ সারি)।
     ভালো অবস্থার গোলমাল একবার লিখে রাখা; এরপর **নতুন কোনো ভুল = FAIL**।
* কম্পাইলার না পেলে **কখনো PASS বলে না** — "SKIPPED, যাচাই হয়নি", exit 1।

**সত্যিকারের প্রমাণ (আজ চালানো):**
| অবস্থা | ফল |
|---|---|
| ঠিক করা কোড | নতুন ভুল **০** → PASS |
| ইচ্ছে করে বাগটা আবার বসিয়ে | নতুন ভুল **২** → **FAIL**, ঠিক ওই দুটো বার্তা |
⇒ মিথ্যা সতর্কতা শূন্য, আর TK-এর বিল্ড-ভাঙা ভুলটা ধরা পড়ে।

### ২. `00_GUARD/verify_kotlin_patterns.py` (দ্রুত জাল)
কম্পাইলার ছাড়াই ১ সেকেন্ডে চলে। প্রকল্পের ১১টা enum ও ৭০টা ধ্রুবক পড়ে —
* enum **ক্লাসকে** চলকে রাখা (এই বাগটাই) → FAIL
* নেই এমন enum ধ্রুবক (বানান ভুল) → FAIL
পরীক্ষায় প্রমাণিত: বাগ বসালে সঙ্গে সঙ্গে FAIL, ভালো কোডে PASS।

### ⚠️ সৎ সীমাবদ্ধতা
এই দুটো পাহারাদার **Android Studio-র পুরো বিল্ডের বিকল্প নয়** —
resource (R), manifest, dexing, ProGuard এখানে যাচাই হয় না, কারণ
Android SDK এই পরিবেশে নামানোই যায় না। কিন্তু **Kotlin কোডের ভুল আর
TK-এর কাছে পৌঁছাবে না** — এটাই আজ ভেঙেছিল।

## সংস্করণ বদল
V496 → **V497 / 4.97** (`build.gradle.kts` ও `version.json`) — যাতে ভাঙা
V496-এর সঙ্গে গুলিয়ে না যায়, আর ফোনে সংস্করণ দেখলেই বোঝা যায়।
⛔ SQL কিছু বদলায়নি — TK ৯:২৩-এ যেটা চালিয়েছেন সেটাই চলবে, আবার চালানোর দরকার নেই।

## সব পাহারাদারের ফল (V497)
নয়টাই PASS — এর মধ্যে **কম্পাইল-পাহারা নতুন ভুল ০**।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# 🔴 V498 — দ্বিতীয় বিল্ডে আরও ৭টা ভুল · পাহারাদারের ফাঁক বন্ধ (২১.০৮.২০২৬ · ১০:০১)

## যা হয়েছিল
V497 বিল্ড দিলে আবার ভাঙে — **৭টা error**:
* `DashboardActivity.kt:178` — `Unresolved reference: visibility` (১টা)
* `NoBengali.kt` — (৬টা)

### ভুল ১ — `<include>` টাইলে `.root` বাদ পড়া
`activity_dashboard.xml:360`-এ `tileWorkNotebook` একটা **`<include>`**।
ViewBinding তখন ওখানে **আরেকটা binding** দেয়, View দেয় না। তাই —

    binding.tileWorkNotebook.visibility = View.GONE        ⛔
    binding.tileWorkNotebook.root.visibility = View.GONE   ✅

(ঠিক নিচেই `binding.tileBriefing.root.visibility` লেখা ছিল — আমি মেলাইনি।)

### ভুল ২ — লেখার ভিতরে `$distance` ও `$until`
`NoBengali.kt`-এর অনুবাদ-তালিকায় লেখা ছিল —

    " (প্রায় $distance মিটার দূরে)" to " (about $distance metres away)"
    "আপনি $until পর্যন্ত বন্ধ আছেন। …"

Kotlin-এ `"…$নাম…"` মানে **চলকের মান বসানো**, নিছক লেখা নয়। ওই নামে কিছু
নেই ⇒ ৬টা error। তাছাড়া চলতি অ্যাপে ওখানে আসল সংখ্যা/তারিখ বসে, তাই
সারিগুলো **কখনো মিলতই না** — অর্থাৎ ওটা একইসঙ্গে যুক্তির ভুলও ছিল।
এখন স্থির টুকরোগুলো আলাদা করে দেওয়া হলো:

    " (প্রায় " → " (about "        ও      " মিটার দূরে)" → " metres away)"
    " পর্যন্ত বন্ধ আছেন। মাস্টারকে জানান।" → " - you are suspended until this date. …"

⛔ শুধু `"আপনি "` বদলানো হয়নি — ওটা আরও অনেক বার্তার শুরুতে আছে, বদলালে
   সেগুলোর মানে উল্টে যেত।

## 🔴 পাহারাদারের যে ফাঁকটা এই ভুল পার করে দিয়েছিল
V497-এর কম্পাইল-পাহারা **সব ২৮,৬০০ ভুলকেই** বেসলাইনে রেখেছিল। কিন্তু
বেসলাইনটা বানানো হয়েছিল এমন অবস্থা থেকে **যেটা নিজেই বিল্ড হত না** —
তাই `$distance`-এর আসল ভুলটাও "জানা গোলমাল" হয়ে চাপা পড়ে গিয়েছিল।
⇒ এটা আমারই ভুল ধারণা ছিল, এবং TK-এর দ্বিতীয় বিল্ডেই ধরা পড়ল।

### এখন কী বদলাল
**`verify_kotlin_compile.py` — দুই ধাপ হলো:**
১. **বাছাই** — প্রতিটা ভুল "বাইরের লাইব্রেরি নেই বলে" নাকি "আসল"।
   বাইরের ধরা হয় শুধু তখনই যখন না-চেনা নামটা কোনো `import android/androidx/…`-এর,
   বা প্রকল্পে অন্য কোথাও `.নাম` হিসেবে ব্যবহার হয়, বা বার্তাটা স্পষ্ট
   ধাক্কার ফল। ⇒ **২৮,৬২৫ → ২৮৮**।
২. সেই ২৮৮টাই বেসলাইনে, আর **নতুন একটাও এলে FAIL**।

**`verify_kotlin_patterns.py` — নতুন নিয়ম যোগ:**
layout-এর ২১টা `<include>` টাইল পড়ে নেয়; `binding.<টাইল>.<কিছু>` লেখা হলে
সেটা `.root` কিংবা ওই layout-এর ভিতরের id কিনা মিলিয়ে দেখে।

### ✅ সত্যিকারের প্রমাণ (আজ চালানো)
| পরীক্ষা | ফল |
|---|---|
| `.root` তুলে দিয়ে | দ্রুত পাহারা → **FAIL**, ঠিক ওই লাইনটা দেখাল |
| `$distance` আবার বসিয়ে | কম্পাইল পাহারা → **FAIL**, "unresolved reference: distance" |
| ঠিক করা কোড | দুটোই **PASS**, নতুন ভুল ০ |

### ⛔ যা চেষ্টা করেও বাদ দিতে হলো (সৎভাবে)
লেখার ভিতরে `$নাম` ধরার আলাদা দ্রুত-নিয়ম বানিয়েছিলাম, কিন্তু বৈধ
`$packageName` · loop-এর চলক ইত্যাদিতে **১৬টা মিথ্যা সতর্কতা** দিচ্ছিল।
মিথ্যা সতর্কতা দেওয়ার চেয়ে চুপ থাকা ভালো — তাই ওটা তুলে দেওয়া হলো;
কম্পাইল-পাহারাই ওটা নিখুঁতভাবে ধরে।

## সংস্করণ
V497 → **V498 / 4.98**।
⛔ SQL কিছুই বদলায়নি — TK ৯:২৩-এ যেটা চালিয়েছেন সেটাই চলবে।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# 🔓 V499 — অ্যাপের তালা: আঙুল **অথবা** ফোনের পাসওয়ার্ড · সবার জন্য (২১.০৮.২০২৬ · ১০:৪০)

## TK-এর প্রশ্ন ও সিদ্ধান্ত
V498 বিল্ড হয়ে ফোনে চলছে (ড্যাশবোর্ডে "Synced · V498")। কিন্তু TK আঙুলের
ছাপের অপশন খুঁজে পাননি — কারণ তিনি **Master**, আর V496-এ তালাটা শুধু
**Doctor**-এর জন্য বসানো ছিল।

TK: *"মাস্টার কেন ফিঙ্গারপ্রিন্ট দিতে পারবে না? এটা কি কোনো যুক্তির মধ্যে বলল?"*
⇒ **ন্যায্য কথা।** "Master-এর নিয়ম ছোঁয়া হবে না" — এটা হাজিরা ও বেতনের
   কথা ছিল; আমি ভুল করে **নিরাপত্তাতেও** টেনে এনেছিলাম।

TK-এর চূড়ান্ত নিয়ম (হুবহু):
> *"একদম সিম্পল রুল — ফিঙ্গারপ্রিন্ট খুলবে, আবার পাসওয়ার্ড দিলেও খুলবে।
>  যেটা বর্তমান যুগের সব ফোনেই একই রকম ব্যবস্থা আছে। সুতরাং আমার অ্যাপের
>  জন্য আলাদা কোনো বন্দোবস্ত আপনাকে করতে হবে না। ফিঙ্গারপ্রিন্টে কাজ না
>  করলে সে পাসওয়ার্ড দিতে পারবে সেরকম ব্যবস্থা রাখুন।"*
> *"এটা শুধু মাস্টারের ক্ষেত্রে নয় — যারা যারা ব্যবহার করে প্রত্যেকের
>  ক্ষেত্রেই একই নিয়ম।"*

## যা করা হলো
* `native/DoctorAppLock.kt` → **`native/AppLock.kt`** (নাম ও নিয়ম দুটোই বদলাল)
  * ভূমিকা-বাছাই **তুলে দেওয়া হলো** — Master · Doctor · Staff · Field, সবার এক
* `BiometricGate.promptUnlock()` — নতুন। Android-এর **নিজের** পর্দা ডাকে:
  `BIOMETRIC_STRONG or DEVICE_CREDENTIAL` (Android ১১+), আর পুরনো Android-এ
  `setDeviceCredentialAllowed(true)`।
  ⇒ আঙুল না মিললে নিচে "Use PIN / Password" আসে — ঠিক ফোনের লক-স্ক্রিনের মতো।
  ⛔ অ্যাপ কোনো পাসওয়ার্ড **রাখে না, দেখে না, পাঠায় না** — সবটাই Android-এর।
  ⛔ তাই ইন্টারনেট ছাড়াও কাজ করে, আর আলাদা কিছু বানাতেও হয়নি (TK-এর শর্ত)।
* `SessionGuardBridge.onForeground()` — তালা এখন **মাস্টারের `return`-এর আগে**
  ডাকা হয়, নইলে মাস্টারের ফোনে তালাই লাগত না।
  ⛔ ৭ দিনের হিসাব ও suspend-যাচাই আগের মতোই মাস্টারের জন্য নয় — ছোঁয়া হয়নি।

## 🔒 হাজিরা আলাদা রাখা হয়েছে (গুরুত্বপূর্ণ)
**হাজিরায় (IN TIME) ফোনের পাসওয়ার্ড চলবে না — শুধু আঙুল।**
নইলে PIN জানা যে কেউ অন্যের হাজিরা বসিয়ে দিতে পারত। তাই
`BiometricGate.prompt()` (হাজিরার পথ) হুবহু আগের মতোই `BIOMETRIC_STRONG`,
আর পাসওয়ার্ডের পথ কেবল নতুন `promptUnlock()`-এ।
⇒ পাহারাদার এখন **দুটোই আলাদা করে** যাচাই করে।
⚠️ TK চাইলে এটাও বদলানো যাবে — জানালেই হবে।

## যে ফোনে স্ক্রিন-লকই নেই
আঙুলও নেই, PIN/প্যাটার্নও নেই — তখন Android-এর কাছে চাওয়ার মতো কিছু নেই।
সেই অবস্থায় অ্যাপ **খোলে, কিন্তু চুপচাপ নয়**: স্পষ্ট বার্তা + "Settings খুলুন"।
⛔ আটকে দিলে ঢোকার কোনো পথই থাকত না — সেটা তালা নয়, ফাঁদ হত।
(V496-এর `ALLOW_WHEN_NO_HARDWARE = false` এখানেই বদলাল — কারণ তখন
 পাসওয়ার্ডের কোনো বিকল্প পথ ছিল না, এখন আছে।)

## পাহারাদার যা ধরেছে (ঠিক করা হয়েছে)
`[৯.১৪]` — নতুন বার্তাটা দুই টুকরোয় জোড়া ছিল, তাই `NoBengali`-র তালিকার
সঙ্গে মিলত না ⇒ বাংলা-বন্ধ স্টাফের (KNE-KISHAN5) পর্দায় বাংলা থেকে যেত।
এক টুকরো করে দেওয়া হলো। ৫টা নতুন বাংলা লেখার ইংরেজিও যোগ করা হলো।

## সব পাহারাদারের ফল (V499)
নয়টাই **PASS** — কম্পাইল-পাহারায় নতুন ভুল **০**।

## সংস্করণ
V498 → **V499 / 4.99**। ⛔ SQL কিছুই বদলায়নি।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# 🎨 V501 — Check-up পর্দার সাজ · Share বোতাম · নোটিশে View (২১.০৮.২০২৬)

সবগুলোই **ফটো-প্রুফ দেখিয়ে, TK-এর অনুমতির পরে** করা হয়েছে (৪টা প্রুফ পাঠানো
হয়েছিল; TK প্রতিটায় সংশোধন দিয়েছেন, শেষেরটায় "হ্যাঁ" বলেছেন)।

## ১. Occupation বাক্স বাকিদের সমান
TK: *"occupation বক্সটা এত বড় রাখছেন কেন? বললাম তো প্রতিটা বক্স একই সাইজের হবে।"*
* কারণ: `spOccupation` Android-এর `simple_spinner_dropdown_item` ব্যবহার করত,
  যার ভিতরে `dropdownListPreferredItemHeight` (≈৪৮dp) বসানো — তাই প্রায় দ্বিগুণ লম্বা।
* সমাধান: নতুন `layout/item_docnote_spinner.xml` (প্রকল্পের নিজের
  `item_branch_spinner.xml`-এর প্রমাণিত ধাঁচ)।
* ⛔ বাছাইয়ের তালিকা · মান · সেভ — কিছুই বদলায়নি।

## ২. Investigation → Quick Action-এর মধ্যে
TK: *"investigation উপরে থাকবে না, ওটার নিচে কুইক action-এর মধ্যে থাকতে হবে।"*
* `E. Investigations` ধাপ ২ (Clinical) থেকে সরে ধাপ ৫-এ, Quick Actions-এর নিচে।
* `investigationGroup` **id একই** — কোড/সেভ কিছু বদলায়নি, পুরোনো রেকর্ড অক্ষত।
* ⚠️ **যে ফাঁদটা এড়ানো হলো:** শুধু-দেখার পুরোনো চেকআপে Quick Action-এর
  বোতামগুলো লুকিয়ে যায়। Investigation-এর label ও group **আলাদা id**
  (`investigationLabel` / `investigationGroup`) রাখা হয়েছে, তাই সেগুলো
  লুকাবে না — নইলে কোন পরীক্ষা বলা হয়েছিল সেটাই আর দেখা যেত না।

## ৩. Quick Action-এর আটটা বাক্স ছোট
TK: *"কুইক একশানে যতগুলি বক্স আছে সাইজে ছোট করুন।"*
* চারটে Quick Action বোতাম: উচ্চতা ৩২dp (আগে Material-এর নিজের ≈৪৮dp),
  লেখা ১১.৫sp, ফাঁক ৩dp।
* Investigation-এর চারটে চিপও হুবহু এক মাপে — `buildChecks(..., compact = true)`
  (নতুন প্যারামিটার, ডিফল্ট `false`, তাই Visual/DRE আগের মতোই)।
* ⛔ কোনো বোতামের কাজ · রং · লেখা বদলায়নি — শুধু মাপ।

## ৪. Treatment Plan সব এক মাপ
TK: *"ট্রিটমেন্ট প্ল্যান বক্স সেগুলো সব একই সাইজের হবে।"*
* প্রতিটা সারির ভিতরে স্থির উচ্চতা ৪২dp · প্রতিটা লেখা এক লাইনে
  (`singleLine` + `ellipsize`)।
* তিনটে লম্বা নাম ছোট করা হলো (TK-এর অনুমতি নিয়ে):
  "মেশিনের মাধ্যমে চিকিৎসা"→"মেশিনে চিকিৎসা" · "LIS-এর মাধ্যমে চিকিৎসা করা হবে"
  →"LIS/ এলআইএস চিকিৎসা" · "Injection (Vaccination)-এর মাধ্যমে চিকিৎসা"
  →"Injection/ ইনজেকশন চিকিৎসা"।
* 🔒 **সেভ হওয়া মান একটুও বদলায়নি** — প্রতিটা CheckBox-এর `.tag`-এ স্থির
  ইংরেজি মান বসানো (`Per Piles`, `LIS Treatment` …) আর `checkedText()` সেই
  `.tag`-ই পড়ে, পর্দার লেখা নয়। পুরোনো রেকর্ড ও রিপোর্ট হুবহু আগের মতোই।
* ⛔ টাকার ঘর · দাম · হিসাব — ছোঁয়া হয়নি।

## ৫. নিচে Back · **Share** · Save · Next
TK: *"Back Save মাঝখানে Share রাখতে হবে… জোর করে WhatsApp / business WhatsApp
ওপেন হবে… A4 size pdf চাইলে প্রিন্টও করা যাবে।"*

### 🔴 কাজ করতে গিয়ে যা ধরা পড়ল (এবং যেভাবে ফাঁদ এড়ানো হলো)
১. `btnShareCheckup` **আগে থেকেই ছিল** (সেভের পরে A4 পর্দায়
   Edit · Share · Print বারে)। আমি প্রথমে একই id-তে নতুন বোতাম বানিয়ে
   ফেলেছিলাম — **ডুপ্লিকেট id, বিল্ড ভেঙে যেত**। ধরা পড়ায় নতুনটার নাম
   `btnShareNow` করা হলো।
২. পুরনো `shareCheckup()` আসলে **সাদামাটা লেখা** (`text/plain`) সাধারণ
   chooser-এ পাঠাত — কোনো PDF যেত না, WhatsApp-ও জোর করে খুলত না।
   অর্থাৎ TK যা চেয়েছেন সেটা এতদিন হচ্ছিলই না।

### যা করা হলো
* `PrescriptionWhatsAppShare` সাধারণ করা হলো — নতুন `shareHtml(...)`,
  যেটা **যেকোনো** তৈরি HTML A4 PDF বানিয়ে WhatsApp/Business/Print-এ পাঠায়।
  পুরনো `share(activity, model)` এখন শুধু ওটাকেই ডাকে — **Prescription-এর
  আচরণ এক অক্ষরও বদলায়নি**।
* `shareCheckup()` এখন পর্দায় দেখা **হুবহু সেই A4 রিপোর্টই** পাঠায়
  (`buildA4Html`), আর তিনটে পথ দেখায়: WhatsApp · WhatsApp Business · Print/PDF।
* নতুন `btnShareNow` (Back ও Save-এর মাঝে) → `saveThenShare()`:
  **আগে নিজে থেকে সেভ, তারপর শেয়ার** (TK-এর অনুমোদিত সিদ্ধান্ত), যাতে
  কখনো পুরোনো/অসম্পূর্ণ রিপোর্ট রোগীর কাছে না যায়।
* ⛔ Print-এর জন্যও নতুন কিছু বানানো হয়নি — প্রকল্পের নিজের
  `PdfPrintDocumentAdapter` (যেটা `PrintPreviewActivity`-তে চলছে)।

## ৬. নোটিশ কার্ডে "View"
TK: *"এই ধরনের নোটিফিকেশনে ভিউ অপশন থাকতে হবে; ভিউতে ক্লিক করলে অটো
রিডাইরেক্ট হবে।"*
* `item_briefing_card.xml`-এ নতুন `btnViewRecord` (Reply-এর আগে), শুরুতে `gone`।
* নোটিশের লেখায় মোবাইল নম্বর থাকলেই দেখা যায় (New Enquiry · Registration ·
  Visit · Advance)। না থাকলে লুকানো — অকেজো বোতাম দেখানো হয় না।
* চাপলে `openRecordForNumber()` → ওই নম্বরের **Patient Timeline**; নম্বর
  কোনো রেকর্ডে না থাকলে সৎভাবে জানিয়ে নতুন Enquiry ভরার প্রস্তাব।
* ⛔ নতুন পথ বানানো হয়নি — ফোন করার পরে যে যাচাই হয়
  (`checkNumberAfterCall`), হুবহু সেটাই।
* নম্বর খোঁজার নিয়ম এক জায়গায় আনা হলো (`MOBILE_PATTERN`), যাতে
  "ক্লিকযোগ্য নম্বর" আর "View বোতাম" কখনো আলাদা নিয়মে না চলে।

## পাহারাদার
নয়টাই **PASS**। এর মধ্যে —
* `tk_guard [৯.১৪]` ধরেছিল: Treatment Plan-এর তিনটে নতুন বাংলা লেখার ইংরেজি
  ছিল না ⇒ বাংলা-বন্ধ স্টাফের (KNE-KISHAN5) পর্দায় বাংলা থেকে যেত।
  তিনটেই `NoBengali`-তে যোগ করা হলো (পুরনো সারিও রাখা হলো, কারণ পুরোনো
  রেকর্ডে ওই লেখা থাকতে পারে)।
* কম্পাইল-পাহারা: নতুন ভুল **০**।
* সব XML ফাইলের গঠনও যাচাই করা হয়েছে।

## সংস্করণ
V500 → **V501 / 5.01**। ⛔ SQL কিছুই বদলায়নি।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# 🔧 V502 — A4 Registration WhatsApp-এ শেয়ার হচ্ছিল না (২১.০৮.২০২৬)

## TK-এর রিপোর্ট
> *"A4 Registration শুধুমাত্র Print হচ্ছে, কিন্তু WhatsApp / Business WhatsApp-এ
> শেয়ার হচ্ছে না।"*

## আসল কারণ (কোড পড়ে পাওয়া)
`PatientMessage.kt`-এর "📄 A4 Registration — Send / Print" বোতামটা দুটো আলাদা
পথে যেত:
* **টাকা-জমার রসিদ** → `PrintPreviewActivity` — সেখানে Share **আছে** ✅
* **রেজিস্ট্রেশন** → `RegistrationHtmlPrint.print()` — এটা শুধুই Android-এর
  `PrintManager` খোলে, **শেয়ারের কোনো পথই নেই** ❌

অর্থাৎ বোতামে "Send / Print" লেখা থাকলেও রেজিস্ট্রেশনের ক্ষেত্রে "Send"-এর
ব্যবস্থাই ছিল না। ⛔ এটা V501-এ ভাঙেনি — অনেক আগে থেকেই এমন ছিল।

## সমাধান
রেজিস্ট্রেশনের পথটা এখন V501-এ বসানো প্রমাণিত
`PrescriptionWhatsAppShare.shareHtml()` ডাকে —
হুবহু **একই A4 ডিজাইন** (`RegistrationHtml.build`) PDF হয়ে তিনটে পথ দেখায়:
**WhatsApp · WhatsApp Business · Print/PDF**।

* ⛔ ছাপার কাগজের ডিজাইন এক অক্ষরও বদলায়নি (একই HTML)।
* ⛔ টাকা-জমার রসিদের পথ ছোঁয়া হয়নি — সেখানে Share আগে থেকেই কাজ করছিল।
* ⛔ নতুন কোনো শেয়ার-ব্যবস্থা বানানো হয়নি।

## পাহারাদার
নয়টাই PASS · কম্পাইল-পাহারায় নতুন ভুল ০।

## সংস্করণ
V501 → **V502 / 5.02**। ⛔ SQL কিছুই বদলায়নি।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# 🔍 V503 — "বার্তা কমপ্লিট হয়নি, ক্লিনিকের নাম-ঠিকানা নেই" (২১.০৮.২০২৬)

## TK-এর রিপোর্ট
> *"এখানে ম্যাসেজ যেটা আছে আমার মনে হচ্ছে কমপ্লিট হয় নাই — ক্লিনিকের নাম নেই,
> ঠিকানা নেই। ভালো করে যাচাই করুন, কবে আপনি এই কাজটা নষ্ট করেছেন।"*

ফটোতে যা দেখাচ্ছিল:
```
REGISTRATION CONFIRMED / MERAJ, / Registration being. /
Patient ID … / people Patient ID-items do. /
TK BISWAS / Founder & Consultant / ' ' / : 8676002200
```

## আসল কারণ (কোড থেকে প্রমাণিত)
ওই ফোনটা **Kishanganj স্টাফের**, যেখানে "বাংলা একেবারে বন্ধ" নিয়ম চালু
(খাতার সারি B158, KNE-KISHAN5-এর অনুরোধে)। `NoBengali` পর্দার বাংলা লেখা
**শব্দে-শব্দে** ইংরেজি করে, আর যা বাকি থাকে **মুছে** দেয়।

স্টাফ বাংলা ভাষা বেছে নিলে নমুনাটা তাই ভেঙে যেত —
* `"হয়ে"` → `"being"` ⇒ "রেজিস্ট্রেশন হয়ে গেছে।" → **"Registration being."**
* `"জন"` → `"people"`, `"টি"` → `"items"`, `"করুন"` → `"do"` ⇒ **"people Patient ID-items do."**
* ক্লিনিকের **বাংলা নাম ও ঠিকানা** পুরোটাই মুছে যেত ⇒ `' '`
* `"হেল্পলাইন"` মুছে গিয়ে পড়ে থাকত ⇒ `": 8676002200"`

## 📌 কবে নষ্ট হয়েছে — সৎ উত্তর
**এই সেশনে নয়।** প্রমাণ:
* যে ছোট শব্দগুলো লেখা ভাঙছে (`হয়ে`→being · `জন`→people · `টি`→items ·
  `করুন`→do · `এর`→of …) — **সবগুলোই V495-এ আগে থেকেই ছিল**।
  এই সেশনে যোগ করা ৫৪টা সারির **একটাও ৬ অক্ষরের কম নয়** (যান্ত্রিকভাবে মিলিয়ে দেখা)।
* `NoBengali.installDialog(dlg)` — এই পপ-আপে বাংলা মোছার ডাকটা
  **V493 · V494 · V495 — তিনটেতেই হুবহু একই** (ফাইল-হ্যাশ `42a907e23ebf`)।
* এই সেশনে `PatientMessage.kt`-এ একটাই বদল হয়েছিল — V502-এর A4 Share
  (diff করে দেখা হয়েছে)।

⇒ অর্থাৎ দোষটা আজকের কাজের নয়, কিন্তু **ধরা পড়েনি এতদিন** — সেটা ঠিক করা হলো।

## ✅ যেটা কখনো নষ্ট হয়নি (গুরুত্বপূর্ণ)
**রোগী সবসময় সঠিক, সম্পূর্ণ বার্তাই পেয়েছেন।** WhatsApp `waText` ও SMS `text`
সরাসরি তৈরি-করা স্ট্রিং থেকে যায় (`PatientMessage.kt:1066`), পর্দার
TextView থেকে নয়। ভাঙা লেখাটা **শুধু স্টাফের পর্দার নমুনা** ছিল।

## সমাধান (TK-এর বাছাই: "ইংরেজি নমুনা দেখাবে")
বাংলা-বন্ধ ফোনে এখন নমুনায় **সম্পূর্ণ ইংরেজি বার্তা** দেখানো হয় —
ক্লিনিকের নাম · ঠিকানা · হেল্পলাইন সব সহ (`buildSingleLang("en", …)`)।
* ⛔ রোগীর কাছে যাওয়া বার্তা **এক অক্ষরও বদলায়নি** — তিনি বেছে নেওয়া
  ভাষাতেই (বাংলা/হিন্দি/ইংরেজি) পান।
* ⛔ অন্য সব ফোনে নমুনা আগের মতোই।
* নতুন প্যারামিটার `previewEn` — ডিফল্ট `null`, তাই পুরনো কোনো ডাক ভাঙে না।

## পাহারাদার
নয়টাই PASS · কম্পাইল-পাহারায় নতুন ভুল ০।

## সংস্করণ
V502 → **V503 / 5.03**। ⛔ SQL কিছুই বদলায়নি।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# 🧹 V504 — Enquiry ও First Visit-এর নমুনাও ঠিক (২১.০৮.২০২৬)

TK-এর অনুমোদনে V503-এর হুবহু একই সমাধান আরও দুটো বার্তায় বসানো হলো:
* **Enquiry** (`buildEnquiryLockedTemplate("en", …)`)
* **First Visit Appointment** (`buildFirstVisitAppointment("en", …)`)

⛔ রোগীর কাছে যাওয়া বার্তা এক অক্ষরও বদলায়নি — শুধু বাংলা-বন্ধ ফোনের
   **পর্দার নমুনা** এখন ভাঙা বাংলার বদলে সম্পূর্ণ ইংরেজি।
⛔ ENQUIRY_WHATSAPP_MESSAGE_FINAL_LOCK-এর লেখা ছোঁয়া হয়নি — একই লক-করা
   টেমপ্লেটই শুধু `"en"` দিয়ে আরেকবার বানানো হয়েছে।

## 🔴 মূল কারণ — TK-কে তালিকা পাঠানো হলো, সিদ্ধান্তের অপেক্ষা
`NoBengali`-র তালিকায় **৩৯টা ছোট শব্দ** আছে (`না`→No · `করুন`→do ·
`দিন`→day · `হয়ে`→being · `টি`→items · `এর`→of …)। বড় বাক্য তালিকায়
না থাকলে এই ছোট শব্দগুলো বাক্যের **ভিতরে** ঢুকে লেখা ভেঙে দেয় —
এটাই আজকের সমস্যার আসল উৎস।

⚠️ এই তালিকা **V492-এও হুবহু একই ছিল** (৩৯টা)। তার আগের ZIP এই পরিবেশে
   নেই, তাই ঠিক কোন সেশনে ঢুকেছে **নিশ্চিত করে বলা যায় না** — এবং
   আন্দাজ করা হয়নি। TK-এর কাছে V491 বা তার আগের ZIP থাকলে বার করা যাবে।

### 🐞 তালিকায় আরেকটা দোষ ধরা পড়েছে (এখনো ঠিক করা হয়নি — অনুমতির অপেক্ষা)
`"বাংলা"` চাবিটা **দুবার** আছে — একবার `"English"`, আরেকবার `"Bengali"`।
Kotlin-এর `mapOf`-এ শেষেরটাই টেকে, তাই প্রথমটা মৃত কোড।

⛔ TK-এর নির্দেশমতো তালিকায় হাত দেওয়া হয়নি — তিনি দেখে বলবেন কোনগুলো তুলব।

## সংস্করণ
V503 → **V504 / 5.04**। ⛔ SQL কিছুই বদলায়নি।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# 🕐 V505 — প্রতিটা বার্তায় তারিখের পাশে সময় (২১.০৮.২০২৬)

## TK-এর নির্দেশ
> *"পেশেন্ট আইডি তার নিচে রেজিস্ট্রেশনের তারিখ এবং সময় থাকবে। আর শুধু
> রেজিস্ট্রেশন বলে কথা নয় — যে কোন বার্তায় যেখানে তারিখ থাকবে তার পাশে
> সময় থাকবে। উদাহরণ: DATE: 31/12/2026 TIME: 12.34Pm"*

## যা আগে ভুল ছিল (প্রমাণসহ)
* **রেজিস্ট্রেশনের পরে সঙ্গে সঙ্গে** পাঠালে তারিখ+সময় যেত
  (`RegistrationActivity` → `a4DateTime`)।
* **রোগীর পাতা থেকে আবার** পাঠালে `dateText` পাঠানোই হতো না ⇒ **তারিখের
  লাইনটাই বাদ পড়ত**। TK-এর ফটোতে ঠিক সেটাই ধরা পড়েছে।

## দুটো সিদ্ধান্ত — TK-কে ফটো-প্রুফ দেখিয়ে নেওয়া
১. **ধরন:** TK **তাঁর নিজের ধরনটাই** বেছেছেন — `DATE: 31/12/2026 TIME: 12.34Pm`
   (স্ল্যাশ · ফাঁক ছাড়া `Pm`)।
   ⚠️ সৎভাবে জানানো হয়েছিল: প্রকল্পের বাকি সব জায়গায় নিয়ম **বিন্দু-তারিখ**
   (31.12.2026) ও বড় হাতের `PM`। TK জেনেশুনেই নিজেরটা বেছেছেন, তাই এই ধরন
   **শুধু রোগীর বার্তায়** — রসিদ · ছাপা · রিপোর্ট কিছুই বদলানো হয়নি।
২. **কোন সময়:** **রেজিস্ট্রেশনের আসল সময়** (পাঠানোর সময় নয়)।

## যা করা হলো
* `PatientMessage`-এ নতুন `timeText` (ডিফল্ট `""` — পুরনো কোনো ডাক ভাঙে না)
  ও দুটো সাহায্যকারী `tkDate()` · `tkTime()`, আর একটাই জায়গা `dateTimeLine()`।
* **১৮টা** তারিখ-লাইন এখন `DATE: … TIME: …` ধরনে —
  Registration · Payment · Visit · Next Visit, তিন ভাষাতেই (bn/hi/en)।
* ⛔ **সময় জানা না থাকলে শুধু তারিখ** যায় — ভবিষ্যতের ভিজিট-তারিখে কোনো
  সময় নেই, তাই সেখানে মিথ্যা সময় বসানো হয় না।
* `PatientTimelineRepository`-তে নতুন ঘর `registrationCreatedAt`
  (রোগীর সারির `createdAt` — এতে দিনের আসল সময় থাকে)।
  ⛔ বাড়তি কোনো ক্লাউড-কল নেই — ঘরটা আগে থেকেই একই লোডে আসত।
* `PatientTimelineActivity` এখন ওই আসল তারিখ+সময় বার্তায় পাঠায়।
* `RegistrationActivity`-তে তারিখ ও সময় আলাদা করা হলো।
  ⛔ A4 কাগজে আগের জোড়া-লাগানো লেখাটাই যায় — **ছাপা এক অক্ষরও বদলায়নি**।

## পাহারাদার
নয়টাই PASS · কম্পাইল-পাহারায় নতুন ভুল ০।

## সংস্করণ
V504 → **V505 / 5.05**। ⛔ SQL কিছুই বদলায়নি।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# 🖨 V506 — A4 কাগজেও একই তারিখ-সময়ের ধরন (২১.০৮.২০২৬)

TK ফটো-প্রুফে A4 কাগজ দেখে বলেছেন — কাগজেও বার্তার হুবহু একই ধরন হোক।

* `RegistrationHtmlPrint.build()` — হেডারে এখন `DATE: 21/08/2026 TIME: 10.48Am`
  (আগে `Date: 21/08/2026 · 10.48Am`)।
* **দুটো আসল দোষও ধরা পড়ল ও ঠিক হলো:**
  1. `PatientTimelineActivity`-র A4-তে **কাঁচা `2026-08-21`** যেত (স্ল্যাশ-ধরন নয়)
     আর **সময় যেতই না** — `put("time", …)` কোথাও ছিল না।
  2. `RegistrationActivity`-র A4-তেও `put("time", …)` ছিল না।
  ⇒ দুটোতেই এখন আসল তারিখ ও সময় যায়।
* ⛔ কাগজের সাজ · ঘর · লেখা · লোগো — বাকি কিছুই বদলায়নি।

## সংস্করণ
V505 → **V506 / 5.06**। ⛔ SQL কিছুই বদলায়নি।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# ⏰ V507 — দেরিতে এলেও IN TIME, সঙ্গে "Late" চিহ্ন (২১.০৮.২০২৬)

## যা হয়েছিল
TK-এর স্টাফ (JPE-CRP) দুপুর ১২:৪৯-এ Work Notebook খুলে দেখেন IN TIME বোতাম
নেই, শুধু "Mark As Leave"।

**যাচাই করে পাওয়া গেল — এটা ত্রুটি নয়, TK-এরই পুরোনো নিয়ম** (খাতার সারি
B615, ১১.০৮.২০২৬): *"ডিউটি ৯টা–৬টা; দুপুর ১২টা পার হলে IN TIME বোতাম বন্ধ।"*
কোডে ছিল `inTimeWindowOpen() = nowHourIST() < 12`।
⛔ এই সেশনে ওটা ছোঁয়া হয়নি — V496-এ শুধু IN TIME চাপলে **কী হবে** সেটা
   বদলেছিল (আঙুল + GPS + সার্ভারের সময়)।

## TK-এর সিদ্ধান্ত
তিনটে পথ দেখানো হয়েছিল; TK বেছেছেন **"সীমা থাক, দেরিতে এলে Late হিসেবে বসুক"**।
* কতটা পর্যন্ত → **সন্ধ্যা ৬টা**
* দেরি কখন থেকে → **সকাল ৯:৩০-এর পর** (৩০ মিনিট ছাড়)
* নোটিশ → TK নতুন কিছু চাননি, **তাই বানানো হয়নি**

## যা করা হলো (সবচেয়ে কম-যন্ত্রপাতির পথ)
* `inTimeWindowOpen()` : `< 12` → **`< 18`**
* নতুন `isLateTime()` ও `timeWithLate()` — IN TIME ৯:৩০-এর পরে হলে পর্দায়
  সময়ের পাশে **"(Late)"** দেখায়।
* "আজকের IN TIME-এর সময় শেষ" লেখাটা এখন শুধু **৬টার পরে** আসে।

## 🔒 কেন এতে কিছু ভাঙে না
* **নতুন কোনো ডেটাবেস-ঘর বা SQL লাগেনি** — সময়টা `check_in`-এ আগে থেকেই
  সেভ হয়; "Late" সেই সেভ-করা সময় দেখেই বলা হয়।
* **বেতন/হাজিরার হিসাব অক্ষত** — `V420_STAFF_PERFORMANCE_DAILY.sql:157`-এ
  `present_days` শুধু দেখে `check_in` ফাঁকা কিনা, **কখন** বসেছে তা নয়।
* সার্ভারের `wn.mark_check_in()` ছোঁয়া হয়নি — দিনে একবার · সার্ভারের সময় ·
  ছুটি/suspend যাচাই, সবই আগের মতো।
* সময় পড়া না গেলে **দেরি বলা হয় না** — আন্দাজে কিছু বসে না।

## সংস্করণ
V506 → **V507 / 5.07**। ⛔ SQL কিছুই বদলায়নি।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*

---

# ⏰ V508 — TK-এর চূড়ান্ত হাজিরার নিয়ম (২১.০৮.২০২৬)

## TK-এর কথা (হুবহু)
> *"যখন খুশি আসুক। ১১টার পরে আর ৪টার আগে চলে গেলে Late কাউন্ট হবে।"*

## যা হলো
* **আসা-যাওয়ায় কোনো বাধা নেই** — `inTimeWindowOpen()` এখন সবসময় `true`।
  দিনের যেকোনো সময় IN TIME দেওয়া যায় (V507-এর ৬টার সীমাও তুলে দেওয়া হলো)।
* **Late-এর চিহ্ন দুটো শর্তে:**
  * IN TIME **সকাল ১১টার পরে**  → `(Late)`
  * OUT TIME **বিকেল ৪টার আগে** → `(Late)`
* দুটোর জন্য আলাদা `timeWithLate()` ও `outWithLate()`।

## 🔒 কেন কিছু ভাঙে না
* **নতুন কোনো ডেটাবেস-ঘর বা SQL লাগেনি** — সময় দুটো আগে থেকেই
  `check_in` / `check_out`-এ সেভ হয়; Late সেই সেভ-করা সময় দেখেই বলা হয়।
* **বেতন/হাজিরার হিসাব অক্ষত** — `present_days` শুধু দেখে হাজিরা বসেছে
  কিনা, কখন বসেছে তা নয়।
* সার্ভারের `wn.mark_check_in()` ছোঁয়া হয়নি (দিনে একবার · সার্ভারের সময় ·
  ছুটি/suspend যাচাই — সব আগের মতো)।
* GPS ও আঙুল/পাসওয়ার্ডের যাচাই আগের মতোই — শুধু **সময়ের বাধা** উঠল।
* সময় পড়া না গেলে **Late বলা হয় না** — আন্দাজে কিছু বসে না।

## সংস্করণ
V507 → **V508 / 5.08**। ⛔ SQL কিছুই বদলায়নি।

⚠️ **Android Studio Build pending — owner will build.**

*লেখা হলো ২১.০৮.২০২৬*
