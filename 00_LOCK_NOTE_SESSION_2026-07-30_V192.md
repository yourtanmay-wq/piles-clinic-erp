# 🔒 LOCK NOTE — V192 (৩০.০৭.২০২৬ রাত) · খাতার সারি **B177**

> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

**ভার্সন:** `versionCode 192` · `versionName 1.92` · পর্দায় **V192**
⛔ **কোনো SQL লাগবে না।**

---

## TK কী রিপোর্ট করেছেন

মেডিসিন-পিকার (checkbox তালিকা — Common/Search/+Add/Save) থেকে ব্যাক করলে, PrescriptionActivity-র নিজের রিভিউ-স্ক্রিন (আগে-থেকে-যোগ-করা ওষুধের তালিকা, দোজ/ফ্রিকোয়েন্সি/ইনস্ট্রাকশন সহ, SAVE/SAVE & PRINT বোতাম) আবার দেখা যাচ্ছিল। TK-এর কথা:

> *"যেখান থেকে ওপেন করেছিলাম প্রেসক্রিপশন চুস করার জন্য... ব্যাক করলে আবার সেখানেই আসতে হবে তাহলে মাঝপথে এটা কেন আসছে? ... এই একই কথা কি আপনাকে বলা হয় নাই?"*

## ⚠️ নিজের ভুল স্বীকার করা হচ্ছে

প্রথমে TK-কে ভুল করে বলা হয়েছিল — *"এই অভিযোগ আগে খুঁজে পাইনি"*। এটা **ভুল ছিল** — খাতার সারি **B38** (28.07.2026)-এ ঠিক এই একই অভিযোগ TK আগেও করেছিলেন, আর তখন একটা সমাধানও করা হয়েছিল। খোঁজার সময় আমার ভুল হয়েছিল। TK ঠিক বলেছেন যে এটা দ্বিতীয়বার বলতে হয়েছে।

## আসল কারণ (কোড ধরে, আন্দাজ নয়)

খাতার সারি B38-এর সমাধানে শর্ত ছিল:

```
if (openedFromEntry && ClinicalRepository.currentPrescription.isEmpty()) finish()
```

অর্থাৎ — **"তালিকা ফাঁকা থাকলে তবেই পর্দা বন্ধ হবে"**। এই ধারণাটা ছিল: খালি তালিকায় দেখানোর কিছু নেই, তাই বন্ধ করে দাও।

কিন্তু NOOR ALAM-এর মতো যাঁর প্রেসক্রিপশনে **আগে থেকেই ওষুধ আছে** (এই সেশনেই বা আগের কোনো সেভ থেকে), সেই তালিকা **আর কখনো ফাঁকা থাকে না** — তাই `isEmpty()` শর্তটা মেলেই না, `finish()` হয়ই না, আর নিচের (এখন আর-খালি-নয়) রিভিউ পর্দাটা বেরিয়ে পড়ে। এটাই TK-এর দেখা দৃশ্য।

## TK-এর চূড়ান্ত সিদ্ধান্ত (ছবিতে দাগ দিয়ে দেখানো)

> *"শুধুমাত্র এই ফটোটাই (মেডিসিন-পিকার) থাকবে। আর এখানে যা যা কার্যকারী আছে সব কাজ যেন বজায় থাকে।"*

তালিকা খালি থাক বা ভরা — পিকার থেকে কিছু না করে বেরোলে **সবসময়ই** সরাসরি Take Action-এ ফিরতে হবে।

## সমাধান

`isEmpty()` শর্তটা তুলে দেওয়া হলো — এখন শুধু:

```
if (openedFromEntry) finish()
```

⛔ **Save করে বেরোনোর পথ (কমিট + ক্লাউড-সেভ + প্রিন্ট প্রিভিউ + finish) এক অক্ষরও বদলায়নি** — শুধু "কিছু না করে বেরোনো"-র পথটাই ঠিক হলো।

## ⛔ একই ধরনের বাগ প্রজেক্টে আর কোথায় আছে খুঁজে দেখা হয়েছে

ঠিক এই একই প্যাটার্ন (`openedFromEntry` + `isEmpty()`-গেটেড `onCancelled`) **Medicine Slip পর্দাতেও হুবহু একই ভাবে ছিল** — সেটাও একই নিয়মে ঠিক করা হলো (`currentSlip.isEmpty()` শর্ত তুলে দেওয়া)।

Blood Test (`InvestigationAdviceActivity`) ও Diet Chart (`DietChartActivity`) পর্দায় এই `openedFromEntry`-ভিত্তিক অটো-ওপেন প্যাটার্নটাই নেই (আলাদা ডিজাইন) — তাই ওখানে এই বাগ নেই, ছোঁয়া হয়নি।

## ⛔ যা ছোঁয়া হয়নি

- `MedicinePickerDialog.kt`-এর নিজের `onCancelled`/`anythingAdded` যন্ত্রপাতি — যেটা Prescription · Medicine Slip · Print Center Walk-in তিন জায়গাতেই শেয়ার করা — **একটুও ছোঁয়া হয়নি**। ওটা আগে থেকেই সঠিক ছিল (দিয়ে-বসা কাজ ঠিকভাবে `anythingAdded` ট্র্যাক করে)। ভুলটা ছিল শুধু PrescriptionActivity/MedicineSlipActivity-র নিজের নিজের অতিরিক্ত শর্তে।
- তাই এই বদল **সম্পূর্ণ আলাদা** — Print Center-এর Walk-in প্রবাহ একটুও প্রভাবিত হয়নি।
- Common/Search/+Add/checkbox/Type-cycling/Save/Cancel — পিকারের সব কাজ অক্ষত।

## যাচাই (কাজের পরে আবার, TK-এর নিয়ম)

- নিজের হাতে ব্র্যাকেট গোনা — **২টা ফাইলেই পাশ**
- পাহারাদার `tk_guard.py` — **১৭টা যাচাই, সব পাশ**
- আগের অনুমোদিত কাজের যাচাই — **৫৯/৫৯ পাশ**
- ভার্সন চার জায়গায় এক — ZIP · `versionCode 192` · `versionName 1.92` · পর্দায় **V192**

**ফাইল:** `PrescriptionActivity.kt` · `MedicineSlipActivity.kt` · `build.gradle.kts` · `DashboardActivity.kt` · `app.js` (শুধু ভার্সনের লেখা)

---

## 🔴 যা এখনো বাকি

- TK-এর লাইভ টেস্ট — Prescription/Medicine Slip খুলে পিকার থেকে ব্যাক করে দেখা।
- Chamber/Global Search/Doctor Queue-তে address/age/sex ফিক্সের সিদ্ধান্ত (সারি B175-এর বাকি অংশ)।
- **B148 — RLS** (⛔ TK-এর অনুমতি ছাড়া নিষেধ) · `03_NETLIFY_READY` Netlify-তে আপলোড (V184) · সপ্তাহখানেক পরে Supabase-এর খরচ দেখা।
