# 🔎 ডা. কে. এইচ. মণ্ডলের ৬টি বক্তব্য — গভীর যাচাইয়ের ফল

**তারিখ:** ২৭.০৮.২০২৬ · রাত ~১২.১৫ IST · **কোড এখনো বদলানো হয়নি**

---

## ১ + ৬ · প্রেসক্রিপশনে ডাক্তারের লেখা ওঠে না

**যা পাওয়া গেল (কোডে চোখে দেখে):**
- `PrescriptionOptionsStore.printLines()` — **DISEASE** ঘরটা সরাসরি
  `RoleSession.currentPatientDisease` থেকে নেয় (লাইন ১৪৫)। এটা **রোগী খোলার
  সময় মেমরিতে বসানো** রেজিস্ট্রেশনের রোগ — ডাক্তারের "Probable Disease" নয়।
- ডাক্তার রোগ বদলালে `DoctorCheckupActivity` **ডেটাবেসে `patients.disease`
  ঠিকই বদলে দেয়** (লাইন ৪৪৬–৪৪৯)। কিন্তু **মেমরির `RoleSession` বদলায় না**
  ⇒ ওই একই সেশনে প্রিন্ট করলে **পুরনো রোগটাই ছাপে**। ✅ TK-এর ছবির সঙ্গে মেলে।
- SYMPTOMS · DURATION · CHIEF COMPLAINT — **ডাক্তারের চেক-আপই আগে** পড়া হয়
  (`refreshFromCheckupRecord`, `type=Doctor Checkup`), রেজিস্ট্রেশন **শুধু
  ফাঁকা ঘরে** বসে (`fillBlanksFromRegistration`, V548)।
  ⇒ ডাক্তার ঘরটা ফাঁকা রাখলেই রিসেপশনের লেখা উঠে আসে — ছবিতে "Itching,
  Prolapsed Lump" ঠিক সেভাবেই এসেছে।

**⇒ দুটো আলাদা কারণ:** (ক) রোগের নাম মেমরিতে বাসি থেকে যায়;
(খ) ডাক্তার ফাঁকা রাখলে রিসেপশনের লেখা বসে যায় (এটা V548-এ **ইচ্ছে করে**
বসানো হয়েছিল, TK-এর নির্দেশে — "সব তথ্য থাকা সত্ত্বেও ফাঁকা আসছে কেন")।

---

## ২ · Check-up Record প্রিন্টে ভুল ব্রাঞ্চ + রোগীর তথ্য ফাঁকা

**একটাই আসল কারণ:** ওই কাগজ বানানোর সময় `RoleSession` (রোগীর প্রসঙ্গ)
**খালি ছিল** — তাই নাম · ID · বয়স · মোবাইল · ঠিকানা সব `"-"`।
আর `BranchInfo.byName()` ফাঁকা ব্রাঞ্চ পেলে **চুপচাপ Kishanganj ধরে নেয়**
(`?: KISHANGANJ`, BranchInfo.kt:63) ⇒ Cooch Behar-এর রোগীর কাগজে
Kishanganj-এর হেডার।

**⚠️ এটা বড় ঝুঁকি:** ভুল ব্রাঞ্চের নাম-ঠিকানা-ফোন সহ কাগজ রোগীর হাতে যায়।

---

## ৩ · ওষুধের Instruction প্রিন্টে আসে না

`PrintMappers.prescription()` — Instruction শুধু পুরনো `lines` লেখায় যোগ হয়
(`[WITH LWW]`), কিন্তু A4 ছকটা ছাপে `rxNames · rxDosage · rxFrequency ·
rxDuration` — **Instruction-এর কোনো ঘর ছকে নেই**। তাই লেখাটা হারিয়ে যায়।
⇒ ছকে একটা ঘর যোগ করতে হবে (বা নামের নিচে ছোট হরফে)।

---

## ৪ + ৫ · Timeline / Note-এর ভুল লেখা সংশোধন

- Timeline-এর সারিতে **সংশোধনের ব্যবস্থা আছে** — কিন্তু **তিনবার চাপতে
  (3-tap) হয়** (`TripleTapEdit.attach → editEnquiryHistoryNote`), আর সেটা
  শুধু enquiry/follow-up-history সারিতে। **কোথাও লেখা নেই, তাই কেউ জানে না।**
- **Note পপ-আপে (📝 Note — তারিখ) কোনো Edit বোতামই নেই** — শুধু "Close"।

⇒ অধিকার আসলে আছে, **কিন্তু লুকানো**।

---

## 🟢 কোন ভাল কাজ খারাপ হতে পারে (সৎ তালিকা)

| যদি করি | কী নষ্ট হতে পারে |
|---|---|
| প্রেসক্রিপশনে **শুধু** ডাক্তারের লেখা | ডাক্তার ঘর ফাঁকা রাখলে প্রেসক্রিপশন **ফাঁকা** ছাপবে — V548-এ TK নিজে ঠিক এর উল্টোটা চেয়েছিলেন |
| Timeline-এ Edit বোতাম | স্টাফও চাপতে পারবেন ⇒ ইতিহাস বদলে ফেলার ঝুঁকি (কার অধিকার — ঠিক করতে হবে) |
| Instruction ছকে যোগ | ছকের চওড়া বাড়ে — লম্বা লেখায় লাইন ভাঙতে পারে |
| ভুল ব্রাঞ্চ সারানো | **কিছুই নষ্ট হয় না** — এটা নিছক বাগ |
