# 📦 V714 — Supabase Egress: সম্পূর্ণ হ্যান্ডওভার ফাইল

> **এই একটা ফাইল পড়লেই পুরো ব্যাপারটা বোঝা যাবে।** অন্য কোনো ফাইল না খুলেও
> কাজ শুরু করা যাবে। নতুন সেশনে, বা অন্য কোনো ডেভেলপারকে পাঠালে — দুজনেই
> এটা পড়েই এগোতে পারবেন।

| | |
|---|---|
| **তারিখ** | ২৬ অগাস্ট ২০২৬ (26.08.2026) |
| **সময়** | রাত ~৮.০০ IST (তদন্ত শুরু ~৬.৩০ PM, শেষ ৭.৪৮ PM) |
| **কার নির্দেশে** | মালিক **TK Biswas** — *"আপনি ২ নম্বর কাজ এর প্লান করুন। সাবধানে যাচাই করে গভীরে গিয়ে।"* |
| **অবস্থা** | 🟡 **তদন্ত সম্পূর্ণ · কারণ প্রমাণিত · কোড এখনো এক অক্ষরও বদলানো হয়নি — মালিকের অনুমতির অপেক্ষায়** |
| **অ্যাপের ভার্সন** | V712 (`appVersionCode = 712`, `appVersionName = "7.12"`) — অপরিবর্তিত |
| **সম্পর্কিত ফাইল** | `V714_EGRESS_KOMANOR_PLAN_2026-08-26.md` · `00_SQL/V714_EGRESS_MAPAR_QUERY.sql` |

---

## ১. প্রজেক্টটা কী (নতুন পাঠকের জন্য ৫ লাইন)

Piles Clinic ERP — **৫টা ব্রাঞ্চের চালু ক্লিনিক**, আসল রোগী, আসল টাকা।
দুটো সামনের দিক, একটাই পিছনের ডেটাবেস:

- **Android অ্যাপ** (Kotlin) — `02_ANDROID_SOURCE_CODE/PilesClinicApp/`
- **ওয়েব অ্যাপ / PWA** (একটাই বড় `app.js`) — `03_NETLIFY_READY/`
- **ডেটাবেস** — Supabase (PostgREST + Realtime), প্রজেক্ট `bcyeogjqtupbdyciqfmz`

⛔ **এটা চালু ব্যবসা** — কোনো তথ্য হারানো যাবে না, কোনো পর্দা ভাঙা যাবে না।

---

## ২. সমস্যাটা কী

মালিক Supabase-এর **Pro Plan** থেকে **Free Plan**-এ নামতে চান।
Free Plan-এ মাসে **৫ GB egress** (ডেটাবেস থেকে তথ্য নামা) ধরা আছে।

**মাপা অবস্থা (২৬.০৮.২০২৬):**

| | |
|---|---|
| প্ল্যান | Pro · চক্র ২৪ অগাস্ট – ২৪ সেপ্টেম্বর ২০২৬ |
| ধরা আছে | ২৫০ GB |
| এই চক্রে খরচ | ১.২৫ GB · overage **০** |

**দৈনিক খরচ (Usage পাতার চার্টে প্রতিটা বারে চেপে নেওয়া আসল সংখ্যা):**

| দিন | PostgREST | Auth |
|---|---|---|
| ২৪ অগাস্ট | **৩৫৯.৯৮ MB** | ০.৪৪ MB |
| ২৫ অগাস্ট | **৫৫৯.৪৫ MB** | ০.৪৫ MB |
| ২৬ অগাস্ট (সন্ধ্যা ৭.২৪ পর্যন্ত) | **২৭৫.০৩ MB** | ০.২৭ MB |
| **মোট** | **~১,১৯৪ MB** ≈ Usage পাতার "১.২৫ GB" ✅ | |

⇒ গড়ে **~৪০০ MB/দিন** = মাসে **~১২ GB**।
⇒ Free-র সীমা ৫ GB ⇒ **~২.৫ গুণ কমাতে হবে (দিনে ~১৬৫ MB-এ)**।

**খরচের ধরন:** **৯৯.৯% PostgREST** (ডেটাবেস থেকে তালিকা পড়া) · ০.১% Auth ·
Storage / Realtime / Edge Functions **শূন্য**।
⇒ ছবি-ভান্ডার (Storage) বা Realtime সম্প্রচারের কোনো দোষ নেই।
⇒ **V586-এর "ছবি-ছাড়া Realtime" ঠিক আছে, নিরাপদ প্রমাণিত।**

---

## ৩. আগের তিনটে চেষ্টা কেন কাজ করেনি (এটা জানা জরুরি)

খাতায় লেখা আছে — ২১ অগাস্ট ৬৩৩ MB · ২২ অগাস্ট ৫৭১ MB। V579 · V586 · V601
বসানোর পরেও আজ ~৪০০–৫৬০ MB। **সংখ্যাটা নামেনি।**

**কারণ:** ওই তিনবারই **কোড পড়ে আন্দাজ করে** "সবচেয়ে বড় খরচ" ঠিক করা হয়েছিল
(৩০-সেকেন্ডের ঘড়ি, বড় তালিকার সব ঘর, ওয়েবের ৪৫-সেকেন্ডের টাইমার)।
সেগুলো সত্যিকারের খরচই ছিল, কিন্তু **ছোট** — কোড পড়ে পাওয়া সব খরচের যোগফল
দিনে মাত্র **~৪০ MB**। বাকি **~৯০%** কোড পড়ে দেখাই যাচ্ছিল না।

⇒ **শিক্ষা:** এই ধরনের খরচ **মাপতে হয়, আন্দাজ করা যায় না।**

---

## ৪. কীভাবে মাপা হলো (হুবহু আবার করা যাবে)

### ৪.১ · ধরন-ভিত্তিক ভাগ
Supabase → প্রজেক্ট → **Usage** → নিচে "Egress per day" চার্ট → প্রতিটা বারে চাপ।
⇒ ওখান থেকেই ৯৯.৯% PostgREST-এর হিসাব ও উপরের দৈনিক সংখ্যাগুলো এসেছে।

### ৪.২ · কোন ডাক কতবার
Supabase → বাঁ মেনু → **Logs** → **Log Explorer**
(সোজা লিংক: `https://supabase.com/dashboard/project/bcyeogjqtupbdyciqfmz/logs/explorer`)
→ উপরে সময় **"Last 24 hours"** → নিচের লেখা বসিয়ে **Run**:

```sql
SELECT
  concat(log_attributes['request.method'], ' ', log_attributes['request.path']) AS ke,
  count() AS calls
FROM logs
WHERE source = 'edge_logs'
GROUP BY ke
ORDER BY calls DESC
LIMIT 15
```

### ৪.৩ · একটা টেবিলের ভিতরে কোন ছাঁকনি
```sql
SELECT
  right(log_attributes['request.search'], 70) AS q,
  count() AS calls
FROM logs
WHERE source = 'edge_logs'
  AND log_attributes['request.path'] = '/rest/v1/followups'
  AND log_attributes['request.method'] = 'GET'
GROUP BY q
ORDER BY calls DESC
LIMIT 12
```

### ⚠️ যা কাজ করে না — সময় নষ্ট করবেন না
- **পুরনো BigQuery ধরনের query** (`from edge_logs cross join unnest(metadata)`) **আর চলে না**।
  Supabase লগ এখন **ClickHouse**-এ। নেস্টেড ঘর পড়তে হয় `log_attributes['key']` দিয়ে,
  কোনো `unnest` লাগে না। ভুল হলে পাতায় **"Rewrite with Assistant"** বোতাম নিজেই ঠিক করে দেয়।
- **বাইট মাপা যায় না।** `response.headers.content_length` প্রায় সবসময় ফাঁকা,
  কারণ উত্তর gzip/chunked হয়ে যায়। তাই **"কতবার ডাকা হলো"** দিয়েই মাপতে হয়।
- Pro প্ল্যানে লগ **৭ দিন** জমা থাকে। Free-তে নামলে এই সুবিধা কমে —
  **তাই নামার আগেই মেপে নেওয়া দরকার।**

---

## ৫. মাপের ফল (গত ২৪ ঘণ্টা, ২৬.০৮.২০২৬ রাত ৭.৪৫)

### ৫.১ · কোন ডাক কতবার

| কতবার | কী |
|---|---|
| **২০,৩১২** | GET `/rest/v1/followups` |
| ৫,৬৩৪ | GET `/rest/v1/payments` |
| ৩,৯৩৭ | POST `/rest/v1/rpc/tk_record_treatment_payment` |
| ৩,৯১৯ | POST `/rest/v1/followups` |
| ৩,৪০৪ | GET `/rest/v1/patients` |
| ২,০৯৭ | GET `/rest/v1/enquiries` |
| ১,৮৪১ | HEAD `/rest/v1/doctor_visits` |
| ১,০৭৫ | GET `/rest/v1/briefings` |
| ১,০৫৮ | GET `/rest/v1/address_tags` |
| ১,০১৩ | GET `/rest/v1/doctor_visits` |
| ৮১২ | GET `/rest/v1/deleted_records` |
| ৭৯৩ | HEAD `/rest/v1/payments` |
| ৭৬২ | HEAD `/rest/v1/followups` |
| ৬২৫ | POST `/rest/v1/rpc/suspended_until_for` |
| ৫৪২ | HEAD `/rest/v1/briefings` |

> `HEAD` = "কিছু বদলেছে?" ছোট প্রশ্ন (V513-এর পাহারা) — খরচ প্রায় শূন্য, এগুলো ঠিক আছে।

### ৫.২ · followups-এর ভিতরে

| কতবার | ছাঁকনি (URL-এর শেষ ৭০ অক্ষর) |
|---|---|
| ৪,৫৫৩ | `…stage=eq.Treatment&status=not.in.(Cancelled,Incomplete,Rejected,Closed)` |
| **৩,৯০৩** | **`select=*&order=updatedAt.desc.nullslast&limit=100&mobile=like.*9547006061`** |
| **৩,৯০১** | **`…updatedAt.desc.nullslast&limit=5000&mobile=like.*9547006061&stage=eq.Inquiry`** |
| ১,৪৫২ | `…ment)&status=not.in.(Cancelled,Incomplete,Rejected,Closed)` |
| ৭৮৮ | `…=eq.Inquiry&status=in.(Cancelled,Incomplete,Rejected,Closed)` |
| ৬৮১ | `…Inquiry&status=not.in.(…)` |
| ৬২৩ | `…Patient&status=not.in.(…)` |
| ৪৭৫ | `…eq.Treatment&status=in.(…)` |
| ৪৪১ | `…=eq.Patient&status=in.(…)` |
| ৪৩৬ | `…visitDate&order=updatedAt.desc&limit=5000&stage=eq.Treatment` |
| ২৯৬ | `…select=updatedAt&order=updatedAt.desc&limit=1&stage=eq.Treatment` (সই-পরীক্ষা, সস্তা) |
| ১৪৫ | `…&limit=5000&branch=eq.Kishanganj` |

---

## ৬. 🎯 আসল কারণ (কোড ধরে প্রমাণিত, আন্দাজ নয়)

### ৬.১ · প্রমাণের শৃঙ্খল

তিনটে বড় সংখ্যা প্রায় **একই (~৩,৯০০)** — কারণ তিনটেই **একই ফাংশনের ভিতরের
পরপর তিনটে লাইন**:

**ফাইল:** `02_ANDROID_SOURCE_CODE/PilesClinicApp/app/src/main/java/com/tkbiswas/pilesclinic/native/PaymentRepository.kt`
**ফাংশন:** `promoteFollowUpToTreatment(patient, staffMobile)` (শুরু ~লাইন ২২৪২)

| লাইন | কোড | লগে মিল |
|---|---|---|
| **২২৪৫** | `SupabaseClient.fetchList("followups", "mobile=like.*$digits", 100)` — **`select=*`, ১০০ সারি** | ৩,৯০৩ |
| **২৩৩০** | `"followups", "mobile=like.*$digits&stage=eq.Inquiry", 5000` | ৩,৯০১ |
| **২৩৫৩** | `"followups", "mobile=like.*$digits&stage=eq.Treatment&status=not.in.(…)", 10` | ৪,৫৫৩-এর বড় অংশ |

আর `POST /rest/v1/followups` (৩,৯১৯) ও `POST /rpc/tk_record_treatment_payment`
(৩,৯৩৭) — এই একই কাজের লেখার অংশ। **পাঁচটা সংখ্যাই ~৩,৯০০।**

### ৬.২ · কে বারবার ডাকছে

```
BottomNav.wire()            → প্রতিবার যেকোনো পর্দা খোলার সময়
SyncWorker (প্রতি ১৫ মিনিট)  →
        ↓
PaymentRepository.flushPending()      (লাইন ২১৯৫)
        ↓  পেন্ডিং তালিকার প্রতিটা সারির জন্য
pushPaymentToCloud(...)
        ↓
promoteFollowUpToTreatment(...)       ← উপরের তিনটে ভারী পড়া
```

### ৬.৩ · এক বাক্যে

> **একজন রোগীর একটামাত্র পেমেন্ট একটা ফোনের পেন্ডিং-তালিকায় আটকে আছে
> (মোবাইল শেষ `9547006061`)। সেটা সফল হচ্ছে না, তাই তালিকা থেকেও সরছে না।
> ফলে ওই ফোনে যতবার যেকোনো পর্দা খোলা হয়, ততবার পুরো ভারী কাজটা আবার হয় —
> দিনে প্রায় ৪,০০০ বার।**

### ৬.৪ · কেন এটাই ৪০০ MB-র প্রায় পুরোটা

লাইন ২২৪৫-এর `select=*` মানে `followups`-এর **সব ঘর — রোগীর base64 ছবিসহ**
(একটা ছবি ~৫৫–১২০ KB), এক-একবারে **১০০ সারি পর্যন্ত**।
এই একটা লাইনই দিনে ৩,৯০৩ বার চলছে।
⇒ বাকি সব পর্দা · সব স্টাফ · সব ৩০-সেকেন্ডের ঘড়ি মিলিয়েও এর ধারেকাছে নয়।

### ৬.৫ · এটা শুধু খরচ নয় — **একটা আসল বাগ**

একজন রোগীর টাকা **আজও ক্লাউডে পৌঁছয়নি**। Dashboard-এ যে লাল "pending" লেখাটা
দেখায় (V706-এ যেটা তালিকা করে দেখানোর ব্যবস্থা হয়েছে) — সেটা **এটাই**।
⇒ ঠিক করলে **দুটো লাভ একসাথে**: টাকার হিসাব ঠিক হবে, আর egress ~৯০% কমবে।

---

## ৭. ✅ কাজের প্লান (৪ ধাপ — এখনো কিছুই করা হয়নি)

> **নিয়ম:** এক ধাপে একটাই কাজ। প্রতিটার পরে মালিককে জানানো, ২ দিন চার্ট দেখা,
> তবে পরেরটা। কোনো ধাপেই তথ্য মুছবে না, কম দেখাবে না।

### ধাপ ১ — আটকে থাকা পেমেন্টটা ঠিক করা 🔴 সবচেয়ে জরুরি
**কেন:** এটাই আসল বাগ; রোগীর টাকা আটকে আছে।
**কী করতে হবে:**
1. যে ফোনে "pending" দেখাচ্ছে, সেখানে ব্যানারে চাপ দিলে V706-এর তালিকা খোলে —
   কোন রোগী, কত টাকা, **কেন আটকে** (`lastWhy`) সব লেখা থাকে।
2. `pushPaymentToCloud()` তিনটে ধাপে ভাগ — কোনটায় `false` ফিরছে বের করা:
   টাকার সারি · বিলের হালনাগাদ · রোগীর কার্ড।
3. ওই ধাপটার আসল বাধা সারানো (সম্ভাব্য: রোগীর `id` মেলে না · RLS · পুরনো সারি)।
**ঝুঁকি:** কম, তবে **টাকার হিসাব ছোঁয়া হচ্ছে** — তাই মালিককে দেখিয়ে তবেই।
**যাচাই:** পেন্ডিং তালিকা খালি হবে · Dashboard-এর লাল লেখা যাবে ·
পরদিন লগে ওই ~৩,৯০০ সংখ্যাগুলো উধাও হবে।

### ধাপ ২ — `select=*` থেকে ছবি বাদ
**ফাইল/লাইন:** `PaymentRepository.kt:2245`
**কী:** `fetchList("followups", …, 100)` → `fetchListSlim…(…, SupabaseClient.FOLLOWUP_COLS_NO_PHOTO)`
**কেন নিরাপদ:** এই ফাংশন `stage` · `status` · `id` ছাড়া কিছুই পড়ে না — **ছবি ব্যবহারই করে না**
(কোডে মিলিয়ে দেখতে হবে, তারপরই বদলাতে হবে)।
**ঝুঁকি:** কম। **যাচাই:** পেমেন্ট সেভ করে দেখা — Treatment-এ উঠছে কি না।

### ধাপ ৩ — ব্যর্থ পেন্ডিং সারিতে অপেক্ষা (backoff)
**ফাইল:** `PaymentRepository.flushPending()` (~লাইন ২১৯৫) — এবং একই ধরনের
`EnquiryRepository` · `RegistrationRepository` · `FollowUpRepository`-এর `flushPending()`।
**কী:** প্রতিটা পেন্ডিং সারিতে `failCount` + `nextTryAt` রাখা।
ব্যর্থ হলে ফাঁক বাড়বে: ১ মিনিট → ৫ → ১৫ → ১ ঘণ্টা (সর্বোচ্চ)।
**⛔ শর্ত:** সারি **কখনো ফেলে দেওয়া যাবে না** — শুধু চেষ্টার ফাঁক বাড়বে।
মালিক নিজে "Send now" চাপলে সঙ্গে সঙ্গে চেষ্টা হবে (অপেক্ষা মানবে না)।
**কেন:** ভবিষ্যতে আবার কিছু আটকালে যেন দিনে ৪,০০০ বার না চলে।
**ঝুঁকি:** মাঝারি — ভুল করলে সত্যিকারের সারি দেরিতে যাবে। তাই খুব সাবধানে।

### ধাপ ৪ — নতুন পাহারাদার (guard)
**ফাইল:** `00_GUARD/tk_guard.py` (নতুন check, যেমন §৯.২০)
**কী:** `followups` · `patients` · `medical` টেবিলে `select=*` বা `fetchList(...)`
(সরু ঘর ছাড়া) লেখা থাকলে গার্ড **ফেল** করবে।
**কেন:** মালিকের নিয়ম ৬.২ — *"একই ধরনের সমস্যা বারবার কেন বলতে হবে"*।
**যাচাই:** ইচ্ছে করে একটা `select=*` বসিয়ে গার্ড ❌ দেখাচ্ছে কি না, তারপর সরিয়ে ✅।

---

## ৮. যা **করা যাবে না** (লক করা)

- ⛔ ৩০-সেকেন্ডের `LiveRefresh` ঘড়ি বন্ধ/ধীর করা — মাপা খরচ দিনে মাত্র ~১৫ MB,
  অথচ স্টাফ সঙ্গে সঙ্গে তথ্য দেখতে পাওয়াটা মালিকের ৫টা প্রতিশ্রুতির একটা।
- ⛔ `TrashHelper` · `EnquiryRepository` (ব্রাঞ্চ বদল) · `ReportCard` থেকে ছবি বাদ —
  ওখানে **পুরো সারিটা আবার লেখা হয়**, ছবি বাদ দিলে Restore-এ ছবি চিরতরে হারাবে।
- ⛔ `REPLICA IDENTITY FULL` করা — তাহলে প্রতিটা আপডেটে পুরনো গোটা সারি (ছবিসহ) যাবে।
- ⛔ পেন্ডিং সারি ফেলে দেওয়া / চুপচাপ সরিয়ে দেওয়া — **তথ্য হারানো**।
- ⛔ মালিকের অনুমতি ছাড়া কোনো ফাইল/ZIP পাঠানো (স্থায়ী নিয়ম, প্যাকেজিং নোট §০)।

---

## ৯. পরে দেখার জন্য (এখন নয়, অনুমোদিতও নয়)

- বড় তালিকাগুলোয় **তারিখের কোনো সীমা নেই** — ২ বছর আগের সারিও নামে।
  সম্ভাব্য বড় লাভ, কিন্তু **"কত পুরনো পর্যন্ত দেখাতে হবে" — এটা ব্যবসার সিদ্ধান্ত**,
  মালিক ছাড়া কেউ ঠিক করতে পারবে না।
- `backuprecords` প্রতি ব্যাকআপে ~২.৬ MB বাড়ে · `trash` কখনো নিজে পরিষ্কার হয় না।
- ওয়েবে প্রথমবার পাতা খুললে পুরো টেবিল **ছবিসহ** নামে।

---

## ১০. সাফল্য কীসে বুঝব

| মাপকাঠি | লক্ষ্য |
|---|---|
| Dashboard-এর লাল "pending" | উধাও |
| লগে `mobile=like.*9547006061` | ০ |
| GET `/rest/v1/followups` (২৪ ঘণ্টা) | ২০,৩১২ → **২,০০০-এর নিচে** |
| Usage চার্টে দৈনিক egress | ~৪০০ MB → **১৬৫ MB-এর নিচে** |
| টানা কত দিন ১৬৫ MB-র নিচে | **৭ দিন** — তবেই Free-তে নামার কথা ভাবা হবে |

---

## ১১. এই ফাইলটা লেখার সময় অবস্থা

- ✅ তদন্ত সম্পূর্ণ · কারণ কোডে মিলিয়ে প্রমাণিত
- ⛔ **একটাও কোড ফাইল বদলানো হয়নি** · ভার্সন V712 অক্ষত · কোনো ZIP পাঠানো হয়নি
- 🟡 **মালিক TK-এর অনুমতির অপেক্ষা** — ধাপ ১ থেকে শুরু করার জন্য
- 🟢 **এখন কোনো বিপদ নেই** — Pro-তে ২৫০ GB-র মধ্যে ১.২৫ GB, overage ০

**লিখলেন:** Claude (AI সহকারী) · ২৬.০৮.২০২৬ রাত ~৮.০০ IST
