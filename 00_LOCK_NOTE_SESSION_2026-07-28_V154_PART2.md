# 🔒 LOCK NOTE — সেশন ২৮.০৭.২০২৬ (রাত) · ভার্সন V154

> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

**আগের ভার্সন:** V154 · **এই ভার্সন:** V154 (`versionCode 154`, `versionName 1.54`)
**খাতার সারি:** B52 · **সময়:** ২৮.০৭.২০২৬ রাত ১১.৫৮

---

## ১) TK কী বলেছেন

> *"কোন এক স্টাফ কোন এক পেশেন্টের পেমেন্ট নিতে ভুল করে ৩-৪ বার নিয়ে নিয়েছে — অর্থাৎ হয়তো তখন ইন্টারনেট স্লো ছিল, বুঝতে পারেনি যে পেমেন্ট হয়ে গেছে। তৎক্ষণাৎ সেই স্টাফ কেন সেই পেমেন্ট ডিলিট করতে পারবে না?*
>
> *তাছাড়া সব থেকে বড় কথা — একদিনে একটা পেমেন্টই এলাউ হবে... যতবারই ওই একদিনে পেমেন্ট করবে সেটা সেকেন্ড পেমেন্ট হিসাবেই ধরা হবে। প্রয়োজনে স্টাফকে সেইভাবে ওয়ার্নিং দেবে।"*

**TK-এর ঠিক করে দেওয়া নাম:** ১ম টাকা = **Advance**, তারপর **2nd Payment · 3rd Payment · 4th Payment · 5th Payment** — এভাবেই বাকি সব, কোনো সীমা নেই।

**TK-এর দুটো উত্তর (জিজ্ঞাসা করে নেওয়া, আন্দাজ করা হয়নি):**
- একই দিনে দ্বিতীয়বার সেভ করলে **সোজা যোগ হবে না** — আগে স্টাফকে জিজ্ঞাসা করা হবে।
- **Delete:** স্টাফ নিজের ব্রাঞ্চের **আজ ও গতকালের** এন্ট্রি; **মোছার খবর মাস্টারের ঘন্টায় যাবে**।

---

## ২) আসল কারণ (কোড দেখে, আন্দাজ নয়)

| কোথায় | কী ভুল ছিল |
|---|---|
| `PaymentRepository.nextLabelFor()` | নম্বর বসত **পেমেন্টের সারি গুনে** (`treatmentPaymentCounts`) — তাই একদিনে ৪ বার সেভ = `Advance` · `2nd` · `3rd` · `4th` |
| `PatientTimelineRepository` | দেখানোর সময়েও নম্বর বসত **সারি ধরে**, তাই পুরনো ভুলও চোখে পড়ত |
| `PaymentActivity` | স্টাফের জন্য **শুধু Edit ছিল, Delete ছিল না** |
| — | একই দিনে দ্বিতীয়বার টাকা নেওয়ার সময় **কোনো সতর্কবার্তাই ছিল না** |

---

## ৩) যা করা হয়েছে (V154)

**(১) নম্বর এখন দিন ধরে বসে, সেভ ধরে নয়**
`PaymentRepository`-তে রোগীর টাকার **আলাদা দিনগুলোর তালিকা** রাখা হয় (`treatmentPaymentDates`)। আজ যদি 2nd Payment-এর দিন হয়, আজ যতবারই সেভ হোক নম্বর **2nd Payment**-ই থাকে; পরের নম্বর কেবল **নতুন দিনে**। ব্যাকডেট পেমেন্টে **ওই তারিখেরই** নম্বর বসে (`nextLabelFor(patientId, forDate)`), তাই Master-এর অনুমোদনের পথেও (`approveBackdateRequest`) ঠিক থাকে।

**(২) দেখানোর সময়েও একই হিসাব**
`PatientTimelineRepository`-তে নম্বর এখন **দিন ধরে** — তাই **পুরনো রেকর্ডেও** একই দিনের সব সারি একটাই নম্বর দেখায়। কম্পিউটারেও ঠিক একই নিয়ম (`03_NETLIFY_READY/app.js`)।
✅ **Report Card-এ কিছু বদলাতে হয়নি** — ওটা আগে থেকেই দিন ধরে গোছানো (`byDate`) এবং ওই দিনের সব টাকা যোগ করে দেখায়।

**(৩) ওয়ার্নিং — চার জায়গাতেই** (নতুন ফাইল `PaymentDayGuard.kt`)
Payment পর্দা · Follow-up কার্ডের Advance · Follow-up-এর পরের পেমেন্ট · Chamber Attendance।
আজ ইতিমধ্যে টাকা নেওয়া থাকলে সেভের আগে প্রশ্ন:
> **Already paid today** — "₹500 has already been taken from RAMESH TODAY (Advance). If the connection was slow, that earlier payment may have saved without you noticing. Is this genuinely NEW money?" → **Yes, add it** / **No, cancel**

**(৪) Delete** — ৩ বার ট্যাপের Edit বাক্সের নিচে **🗑 DELETE THIS PAYMENT**
- **কে পারবে:** স্টাফ নিজের ব্রাঞ্চের **আজ ও গতকালের**, মাস্টার যে কোনোটা (ওই পর্দার আগের `canEdit`-এর সেই একই নিয়ম, তাই দুটো কখনো আলাদা হবে না)।
- সারিটা **Trash-এ যায়** (`TrashHelper.moveToTrash`) — **ফেরানো যায়, কিছুই হারায় না**। Trash-এ তুলতে না পারলে কিছুই মোছা হয় না।
- ফোনের জমানো কপি ও **অপেক্ষমাণ তালিকা** দুটো থেকেই ওঠে + `DeletedGuard` চিহ্ন — **তাই মুছে ফেলা টাকা আর কখনো ফিরে আসতে পারে না**।
- **মাস্টারের ঘন্টায় খবর** — টাকা · রোগী · Patient ID · ব্রাঞ্চ · তারিখ · কে মুছল। ⛔ এর জন্য **আগে থেকেই থাকা briefings ব্যবস্থা**, তাই **TK-কে কোনো SQL চালাতে হয়নি**।

---

## ৪) ⛔ যা কখনো বদলানো যাবে না

- **নম্বর দিন ধরে গোনা হবে, সারি ধরে নয়** — এটা TK-এর সরাসরি নির্দেশ (২৮.০৭.২০২৬)। পুরনো "সারি গুনে নম্বর" নিয়মে **কখনো ফেরা যাবে না**।
- **একই দিনে দ্বিতীয়বার টাকা সোজা যোগ হবে না** — আগে স্টাফকে জিজ্ঞাসা করতেই হবে।
- **টাকার অঙ্কে কোনো হাত পড়েনি এবং পড়বে না** — এই পুরো কাজে শুধু **নাম/নম্বর** ঠিক হয়েছে।
- **বিল (bill) ছোঁয়া হয় না** — Delete করলে শুধু টাকার সারিটা ওঠে, জমা/বকেয়া নিজে থেকেই ঠিক হয়।
- **প্রথম Advance মুছলেও রোগীকে Visit-এ ফেরানো হবে না** (TK-কে আগে জানিয়ে সম্মতি নেওয়া হয়েছে) — ফেরালে চেম্বার · ফলো-আপ · টাইমলাইন ভেঙে যাবে।
- **মোছা পেমেন্ট Trash-এ যাবেই** — সরাসরি মুছে ফেলার কোনো পথ কখনো বানানো যাবে না।

---

## ৫) যাচাই (ফাইল দেওয়ার আগে, বাধ্যতামূলক তালিকা)

`00_GUARD/tk_guard.py` — **১২টা যাচাইয়ের সবকটা পাশ** (ব্র্যাকেট · কমেন্ট কোড গেলেনি · `if`-এ `else` · `binding.<view>`/drawable · XML ২২৩ ফাইল · Supabase কলাম · ভার্সন এক V154 · কম্পিউটারের অ্যাপ · সম্পূর্ণ প্রজেক্ট .git সহ · সব নোট · রোগীর সময় ১১–৪ · মাইন-পোঁতা জায়গা)।
`node --check 03_NETLIFY_READY/app.js` — **পাশ**।

**হাতে আলাদা করে মিলিয়ে দেখা:** নতুন/বদলানো প্রতিটা লাইনের API · টাইপ · আর্গুমেন্ট-সংখ্যা — `TrashHelper.moveToTrash(String, JSONObject, String)` · `BriefingRepository.post(context, title, message, "role", branch, "master", mobile)` (ঘন্টা নিজে থেকেই গোনে) · `LocalWorkflowStore.removePayment(id)` · `DeletedGuard.markDeleted` · `DateUtil.display` · `JSONObject.s()` — সবই আগে থেকেই আছে। চারটে জায়গার নতুন lambda-ব্লক হাতে খুলে মিলিয়ে দেখা হয়েছে (brace-গণনা ০, কমেন্ট কোড গেলেনি)। ISO তারিখ (`yyyy-MM-dd`) স্ট্রিং হিসেবেই ঠিকভাবে সাজে, তাই দিনের ক্রম নির্ভুল।

---

## ৬) 🔴 এখনো বাকি

- **TK-এর লাইভ টেস্ট** · **স্টাফদের নতুন APK** · **Netlify আপলোড**।
- **⚠️ পুরনো দিনের Chamber-এর ছাপা লাইনে পুরনো নামটাই (যেমন `3rd Payment`) থেকে যাবে।** ওটা ঠিক করতে গেলে প্রতিটা রোগীর পুরো ইতিহাস আবার ক্লাউড থেকে আনতে হত — Supabase-এর খরচ বাড়ত, তাই TK-কে না জিজ্ঞাসা করে করা হয়নি। **Timeline ও Report Card-এ ঠিক দেখাচ্ছে।**
