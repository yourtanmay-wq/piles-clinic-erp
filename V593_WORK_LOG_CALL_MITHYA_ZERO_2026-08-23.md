# V593 — কলের মিথ্যা "0" (V590 কাজটা অর্ধেক করেছিল)
**তারিখ:** ২৩.০৮.২০২৬ · **ভার্সন:** V593 / 5.93

TK:
> *"আরো যাচাই করুন · কারণ এই একই ধরনের সমস্যা বেশ কয়েকবার আপনাকে বলা হয়েছে ·
> প্রতিবার আপনি বলেছেন হ্যাঁ ঠিক হয়ে গেছে · আরে স্টাফদের কাছে আমার কথা শুনতে
> হয় শুধুমাত্র আপনার কারণে"*

**TK ঠিক বলেছেন।** প্রথম কারণে থেমে না গিয়ে এবার পুরো পথটা ধরে দেখা হলো, আর
**V590-এর সংশোধনটা যে অর্ধেক ছিল** সেটা ধরা পড়ল।

---

## 🔴 যা পাওয়া গেল

New Enquiry · Registration · Collection — এই তিনটে **পড়া না গেলে "…"**
দেখায় (B496 থেকেই), আর নিচে সতর্কবার্তা ওঠে *"weak internet"*।

কিন্তু **App Calls · Outside Calls · Total call** পড়া না গেলেও নির্লজ্জভাবে
**`0`** ছাপত। ফলে দুর্বল নেটে পর্দায় উঠত:

```
New Enquiry: …
Registration: …
Collection recorded: …
App Calls: 0 | Outside Calls: 0 | Total Calls: 0     ← মিথ্যা ০
```

**আর সবচেয়ে খারাপটা:** ওই একই মিথ্যা 0 **স্টাফের পাঠানো WhatsApp রিপোর্টেও**
চলে যেত — **৭ জায়গায়** (দৈনিক · মাসিক · নোটিফিকেশন থেকে OUT TIME · Submit ·
Share · দুটো রিপোর্ট-কার্ড)। অর্থাৎ TK-এর কাছে যে রিপোর্ট পৌঁছাত, সেটাই বলত
"App Calls: 0" — যদিও স্টাফ সত্যিই কল করেছেন।

**V590 কেন ধরতে পারেনি:** V590 শুধু গ্রিডের ঘরটা দেখেছিল (`getRows` →
`getRowsChecked`, আর ফোনের গোনা যেন মুছে না যায়)। কিন্তু —
- `appVal.text = …` লাইনটা **শর্তের বাইরেই** ছিল, তাই প্রথমবার খোলায় (ফোনে
  গোনা 0) ব্যর্থ পড়া "…"-এর জায়গায় **0** বসিয়ে দিত;
- সারাংশ ব্লক ও ৭টা রিপোর্ট-লাইন **ছোঁয়াই হয়নি**।

---

## যা বদলানো হলো

কল-সংখ্যা লেখার **একটাই জায়গা** বানানো হলো, তারপর সবখানে সেটাই:

```kotlin
private fun callsOk(s: JSONObject) = s.optBoolean("appOk", true)
private fun callTxt(s: JSONObject, key: String): String =
    if (callsOk(s)) s.optInt(key).toString() else "…"
```

| জায়গা | আগে | এখন |
|---|---|---|
| গ্রিডের App Calls ঘর | ব্যর্থ পড়াতেও `0` | পড়া সফল হলে, বা এই ফোনেই কল চাপা থাকলে তবেই সংখ্যা — নইলে "…" |
| সারাংশ ব্লক (৩টা সংখ্যা) | `0` | "…" · আর "weak internet" সতর্কবার্তাও ওঠে |
| ৭টা রিপোর্ট-লাইন (WhatsApp) | `App Calls: 0` | `App Calls: …` |
| ২টা রিপোর্ট-কার্ড | `0` | "…" |
| সাথে-সাথে দেখানো স্থানীয় গোনা | `staffCode` (নাম) | `callTapCode()` (V592-এর মতোই) |
| ওয়েব `notebook.js` | ব্যর্থ পড়ায় `return 0` | `return null` → সবখানে "…" |

⛔ **সত্যিকারের ০ লুকানো হয়নি** — পড়া সফল হলে সংখ্যা যা, তাই দেখায় (০-ও)।
   কেউ সত্যিই কল না করলে TK আগের মতোই ০-ই দেখবেন। শুধু **"জানি না"**-কে আর
   **"শূন্য"** বলা হয় না।

### ওয়েবে আরেকটা লুকোনো ভুল
`totalCalls: apc + occ` — JavaScript-এ `null + 5` = **5**। অর্থাৎ কল-সংখ্যা
পড়া না গেলেও Total-এ একটা **বানানো সংখ্যা** জমা হয়ে যেত। এখন `callSum()`,
যেটা একটা না-জানা থাকলে পুরোটাই না-জানা রাখে।

---

## চালিয়ে প্রমাণ

**ফোনের নিয়মটা কম্পাইল করে চালিয়ে** (`kotlinc`):

```
পড়া সফল · ৭টা কল          → পর্দায় "7"   রিপোর্টে "7"
পড়া সফল · সত্যিই ০        → পর্দায় "0"   রিপোর্টে "0"      ← সত্যি ০ ঠিকই দেখায়
পড়া ব্যর্থ · ফোনে ০       → পর্দায় "…"   রিপোর্টে "…"      ← আর মিথ্যা ০ নয়
পড়া ব্যর্থ · ফোনে ৩টা     → পর্দায় "3"   রিপোর্টে "…"
```

**ওয়েবের নিয়মটা node দিয়ে চালিয়ে:**

```
App=5    Outside=3  →  App: 5 · Total: 8
App=0    Outside=3  →  App: 0 · Total: 3
App=null Outside=3  →  App: … · Total: …
```

---

## পাহারাদার

নতুন `00_GUARD/verify_v593_call_mithya_zero.py` — **২২টা যাচাই, সব পাশ**।
সবচেয়ে জরুরি যাচাইটা হলো: *"কল-সংখ্যা আর কোথাও কাঁচা ছাপা হয় না"* — পুরো
ফাইল স্ক্যান করে দেখে `optInt("appCalls"/"outsideCalls"/"totalCalls")` কোনো
ছাপার লাইনে আর নেই। অর্থাৎ ভবিষ্যতে নতুন কোনো রিপোর্ট-লাইন যোগ করে ভুলে
কাঁচা 0 বসালে **সঙ্গে সঙ্গে ধরা পড়বে**।

**উল্টো-পরীক্ষা** (ইচ্ছে করে ভেঙে): একটা রিপোর্ট-লাইনে কাঁচা 0 ফেরালে ❌ ·
পর্দার শর্ত তুলে দিলে ❌ · ওয়েবে আবার 0 ফেরালে ❌ — তিনটেই ধরা পড়ে।

🔧 **দুটো পুরোনো পাহারাদারের নিজের ফাঁক** সারানো হলো: `verify_v590` লেখার
ধরন দেখত (`s.optBoolean(...)`), তাই একই মানে অন্য নামে লিখলে মিথ্যে ❌ দিত —
এখন আচরণ দেখে। আর `verify_v592`-এ ভার্সন হুবহু "592" বাঁধা ছিল (V591-এ ধরা
পড়া একই ভুল) — এখন "592 বা তার পরে"।

**মোট ২৫টা পাহারাদার — সব পাশ**, Kotlin কম্পাইলে নতুন ভুল 0।

---

## ⚠️ TK-কে সৎভাবে

এই সবই **কোডে** সারানো। **ফোনে এখনো পুরোনো APK চলছে** — নতুন APK না বানিয়ে
না বসানো পর্যন্ত স্টাফের ফোনে আগের মতোই দেখাবে।
