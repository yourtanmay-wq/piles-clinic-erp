# V531 — বাকি তিনটে ছোট ফাঁক বন্ধ
তারিখ: ২২.০৮.২০২৬ · V530 → **V531 (5.31)**

TK-এর নির্দেশ: *"হ্যাঁ ধরুন, সাবধানে করবেন, কোন ভালো কাজ যেন খারাপ না হয় /
সততার সাথে করবেন নিজে থেকে আন্দাজে কোন কাজ করবেন না।"*

---

## ১. যে তিনটে ফাঁক বন্ধ হলো

| # | জায়গা | আগে যা হত | এখন |
|---|---|---|---|
| ১ | **Doctor Queue** ও **Global Search** → ক্লিনিক্যাল পর্দা | ঠিকানা/বয়স/লিঙ্গ শুধু মোবাইল ধরে, **প্রথম সারি** | রোগীর **নিজের সারির আইডি** ধরে |
| ২ | **Chamber → Clinical মেনু** | একই — অথচ সারিটা V526 থেকেই `patientRowId` জানে | সেই আইডি ধরেই |
| ৩ | **Chamber → Mark Expected** বাক্স | নাম/ব্রাঞ্চ autofill-এ **প্রথম সারি** | একাধিক হলে **"কোন রোগী?"** জিজ্ঞাসা |

---

## ২. ⛔ কেন এতে পুরোনো কিছু ভাঙে না

- **১ ও ২ নম্বরে আইডি ফাঁকা থাকলে বা সার্ভারে না মিললে — হুবহু পুরোনো
  মোবাইল-পথেই ফিরে যায়।** অর্থাৎ ফল **কখনোই আগের চেয়ে খারাপ নয়**।
  (এটা ইচ্ছে করে বসানো — ফোনে-বানানো সারি যেগুলো এখনো ক্লাউডে ওঠেনি,
  সেগুলোর আইডি সার্ভারে থাকে না।)
- **৩ নম্বরে একজন রোগী থাকলে হুবহু আগের সেই লাইনটাই** (`pats.getJSONObject(0)`)।
  স্টাফ *Cancel* করলে **কিছুই বসানো হয় না** — ভুল নাম বসার চেয়ে নিরাপদ।
- **জমানো ফলের (cache) চাবিতে আইডি যোগ হয়েছে** — নইলে এক নম্বরের দু'জনের
  ঠিকানা একে অপরের ঘরে ঢুকে যেত। আইডি ফাঁকা থাকলে চাবি **আগের মতোই শুধু
  মোবাইল**, তাই পুরোনো জমানো ফল নষ্ট হয় না।
- **ক্লাউড-অনুরোধ একটাও বাড়েনি।** ১ ও ২-এ আইডি ধরে খোঁজা মোবাইল ধরে খোঁজার
  চেয়ে **ছোট**। ৩-এ সেই একটাই ডাক, শুধু `id`/`patientId` ঘর দুটোও আসে।
- **Database schema · RLS · production data — কিছুই ছোঁয়া হয়নি।** কোনো SQL নেই।
- **`PatientIdentity.pickPatientRow` (নয় পর্দার শেয়ার-করা V143 নিয়ম) — ছোঁয়া হয়নি।**

---

## ৩. যা যাচাই করে **ইচ্ছে করে ছোঁয়া হয়নি** (সৎভাবে)

| জায়গা | কেন ছোঁয়া হয়নি |
|---|---|
| **Delete / Trash** (Timeline · Draft) | কোড পড়ে দেখা গেল — আগে **রোগীর আইডি** ধরে খোঁজে, মোবাইল শুধু শেষ উপায়। ঠিকই আছে। |
| **Registration-এর autofill** | `enquiries` থেকে আসে; enquiry নম্বরেরই জিনিস (তখনো রোগীর সারি তৈরিই হয়নি)। ঠিকই আছে। |
| **`EnquiryRepository` · `RegistrationRepository`** | ডুপ্লিকেট-চেক ও enquiry — নম্বর ধরাই উদ্দেশ্য। বদলালে B30-এর পাহারা দুর্বল হত। |
| **`CloudPasswordCheck` · `PartnerShares` · `MessageSentLog`** | রোগী নয় — স্টাফের পাসওয়ার্ড, পার্টনার, আর WhatsApp বার্তার লগ (বার্তা নম্বরেই যায়)। |

---

## ৪. যাচাই

**নতুন নিয়মটা আবার compile করে চালানো হয়েছে** (আসল `PatientIdentity.kt` ফাইল) —
**১৫/১৫ PASS**।

**পুরোনো সব পরীক্ষা আবার — একটাও ভাঙেনি:**

| suite | ফল | | suite | ফল |
|---|---|---|---|---|
| cham | **27 · 0** | | rot | **21 · 0** |
| rep | **25 · 0** | | family | **29 · 0** |
| tt | **19 · 0** | | futab | **19 · 0** |
| rc | **19 · 0** | | gtest | **13 · 0** |

**ওয়েব:** ৮/৮ ফাইল `node --check` ঠিক। **ওয়েবে একটাও অক্ষর বদলানো হয়নি।**

**হাতে যাচাই (নাম সত্যিই আছে কিনা):** `hit.rowId` ✅ · `QueuePatient.id` ✅ ·
`ChamberAttendanceRow.patientRowId` ✅ · `askWhichChamberPatient` সংজ্ঞা ১ · ডাক ১ ✅ ·
বন্ধনীর হিসাব চার ফাইলেই `git HEAD`-এর সাথে মিলেছে (`+0`)।

---

## ৫. ⚠️ সৎ সীমাবদ্ধতা

**Activity ফাইলগুলো compile করে দেখানো যায়নি** — এখানে Android SDK নামে না
(`dl.google.com` → 403)। **TK-কে বিল্ড করে দেখে নিতে হবে।**

---

## ৬. TK-এর করণীয়

1. **V531 বিল্ড করে APK** · Netlify-তে `03_NETLIFY_READY` (cache `?v=v531`)
2. এক নম্বরে দু'জন রোগী রেখে — Doctor Queue ও Chamber-এর Clinical মেনু থেকে
   ঢুকে **বয়স/লিঙ্গ ঠিক জনের কিনা** দেখুন
3. 🔴 **সাধারণ (একজন রোগীর) নম্বরে সব আগের মতোই আছে কিনা** — এটাই সবচেয়ে জরুরি
