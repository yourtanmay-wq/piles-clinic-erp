# 🔒 LOCK NOTE — V197 (৩০.০৭.২০২৬ বিকেল ৪.৫০)

**ভার্সন:** `versionCode 197` · `versionName 1.97` · পর্দায় **V197** · খাতার সারি **B184 – B187**
⛔ **কোনো SQL লাগবে না।**

---

## ⛔ সবার আগে — এই ফাইলের স্থায়ী নিয়ম

> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

📌 প্রতিটা ফাইল Android Studio-তে বিল্ড করলে **কোনো error আসবে না** — এটা নিশ্চিত করা সম্পূর্ণ Claude-এর দায়িত্ব (TK-এর স্থায়ী নিয়ম)।

🔒 **TK-এর ৬ দফা স্থায়ী নির্দেশ (৩০.০৭.২০২৬ বিকেল ৪.২১ · খাতার সারি B183)** — পুরো লেখা `00_SOBAR_AGE_PORUN_SOTORKOBARTA.md`-এর একদম উপরে: ১ সাথে সাথে সমাধান · ২ তারিখ-সময় সহ নোট · ৩ সততা ও ঝুঁকিহীনতা · ৪ ঝুঁকি হলে আগে জানানো · ৫ ডিজাইন হলে আগে ফটো-প্রুফ · ৬ এক-দুই লাইনে সরল বাংলা।

---

## ✅ এই ভার্সনে কী কী ফাইনাল হলো

### ১. Follow-up কার্ডের ট্যাগ — আর কখনো কাটবে না (সারি B184)
TK ফটো-প্রুফ দেখে **ফাইনাল** করেছেন, তারপরেই কোড।

- চারটে ট্যাগেরই (Branch · Disease · ঠিকানা · RMP/UNEXPECTED TIME) **এক ফন্ট-মাপ (১০.৫sp)**, **এক রং**, এক প্যাডিং।
- **বাক্সের মাপ ঠিক লেখার মাপে** — বেশি নয়, কম নয়।
- **ব্র্যাঞ্চের পুরো নামই থাকে** (আগে জায়গা না কুলোলে JALPAIGURI → JPE হয়ে যেত)।
- এক লাইনে না কুলোলে ট্যাগটা **পুরোটাই নিচের লাইনে নেমে যায়**।
- ⛔ কাটার ব্যবস্থাটাই (`ellipsize`) **তুলে দেওয়া হয়েছে** — কোনো ট্যাগ কখনো `…` দিয়ে কাটবে না, কার্ডের বাইরে বেরোবে না, আরেকটার গায়ে ঘেঁষবে না।
- ⛔ লেখা সবসময় **CAPITAL** (আগে থেকেই ছিল, অক্ষত)।
- ⛔ ট্রিপল-ট্যাপ এডিট ও ঠিকানা-ট্যাগে এক-ট্যাপ — **অক্ষত** (ভিউগুলোই সরানো হয়, নতুন করে বানানো হয় না)।
- **নতুন ফাংশন:** `FollowUpActivity.layoutTagsInRows()` · **বাতিল:** পুরনো `shrinkTagsToFit()`।

### ২. Remarks এক লাইনে (সারি B184)
স্টাফের লেখা রিমার্কে এন্টার/লাইন-ব্রেক থাকলে সেটাই হুবহু দেখাত। এখন **দেখানোর সময়** লাইন-ব্রেক ও বাড়তি ফাঁকা এক ফাঁকা জায়গায় মিলিয়ে দেওয়া হয় — তাই এক লাইনে বসে, না ধরলে নিজে থেকেই দ্বিতীয় লাইনে যায়।
⛔ **ডেটাবেসে সেভ হওয়া লেখা এক অক্ষরও বদলায়নি** — শুধু কার্ডে দেখানোর চেহারা।

### ৩. ডক্টর ভিজিটে Msg 2 · 3 · 4 (সারি B187) — **তৃতীয়বার বলতে হয়েছে**
**আসল কারণ (কোড ধরে, আন্দাজ নয়):** `pickReferredPatient()`-এর ভিতরে রোগীর নামের সারি বসানোর লুপটা এমন একটা তালিকার নাম (`list`) ধরে ঘুরত **যেটা ওই ফাইলে কোথাও তৈরিই হয়নি**। তাই Select Patient খুলত (উপরের পাহারা ঠিক তালিকাই দেখত), কিন্তু **ভিতরে একটাও ট্যাপযোগ্য সারি বসত না** — নামের উপর চাপ দিলে কিছুই হত না। Msg 1-এ রোগী বাছার দরকার নেই, তাই সেটা ঠিকই চলত।

⛔ **এটা একটা সত্যিকারের বিল্ড-ভাঙা ভুলও ছিল** — Kotlin-এ না-থাকা নাম মানেই কম্পাইল এরর।

**সমাধান:** লুপটাও এখন ঠিক সেই একই তালিকা (`data.referred`) ধরেই ঘোরে — যেটা "Referred Patient"-এ দেখা যায়।
⛔ বার্তার লেখা · ভাষা বাছাই · কোন রোগী দেখানো হবে · পরের ধাপ — **কিছুই বদলানো হয়নি**।

### ৪. একই ধরনের আরেকটা বিল্ড-ভাঙা ভুল খুঁজে বের করে সারানো (সারি B187)
নতুন `layoutTagsInRows()` (ক্লাস-লেভেল ফাংশন) ভিতরে `tv(...)` ডাকছিল, অথচ ওটা `buildFollowCard`-এর **ভিতরের** সহায়ক ফাংশন — বাইরে থেকে দেখাই যায় না। সেপারেটরটা এখন ওখানেই তৈরি হয়, **চেহারা হুবহু আগের মতোই**।

---

## ⛔ যা ছোঁয়া হয়নি
টাকার হিসাব · ব্রাঞ্চের নিয়ম · ডেটাবেস (SQL লাগেনি) · Follow-up-এর অন্য কোনো ডিজাইন · ডাক্তার-বার্তার লেখা ও ভাষা · Action মেনুর বাকি অপশন (Call · WhatsApp · Log Call · Msg 1 · Edit · Delete) · কম্পিউটারের (ওয়েব) অ্যাপ।

## 🔍 যাচাই
- পাহারাদার (`tk_guard.py`) **১৭/১৭ পাশ** (১৮৫টা Kotlin ফাইলে Kotlin-সচেতন ব্র্যাকেট গোনা সহ)
- আগের কাজের যাচাই (`YACHAI_2026-07-30_V173_V174.py`) **৫৯/৫৯ পাশ**
- ভার্সন দুই জায়গায় এক — `build.gradle.kts` (197 / 1.97) ও `DashboardActivity.kt` (V197)
- প্রতিটা নতুন/পরিবর্তিত লাইনের নাম ও স্কোপ হাতে মিলিয়ে দেখা

## 🔴 এখনো বাকি
- **TK-এর লাইভ টেস্ট** — V197 বিল্ড করে (ক) Follow-up কার্ডের ট্যাগ ও Remarks (খ) ডক্টর ভিজিটে Msg 2 · 3 · 4
- **B148 — RLS** ⛔ TK-এর অনুমতি ছাড়া নিষেধ
- `03_NETLIFY_READY` Netlify-তে আপলোড (TK-এর কাজ)
