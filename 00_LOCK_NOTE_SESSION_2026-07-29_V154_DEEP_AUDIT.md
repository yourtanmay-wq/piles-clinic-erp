# 🔒 লক নোট — ২৯.০৭.২০২৬ সকাল ৯.৫৮ · সম্পূর্ণ প্রজেক্টের ডিপ অডিট

**ভার্সন:** V154 (বাড়ানো হয়নি — ফাইল পাঠানো হয়নি)
**খাতার সারি:** B72 · B73 · B74 · B75

> ⛔ **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

---

## ১) TK কী বলেছিলেন

*"সম্পূর্ণ প্রজেক্ট ডিপ অডিট করুন"* — ২৯.০৭.২০২৬ সকাল।

অডিটের ৫টা ফলের উপরে TK-এর নিজের সিদ্ধান্ত (৯.৫০ am):

| # | অডিটে যা পাওয়া গেল | TK-এর সিদ্ধান্ত |
|---|---|---|
| ১ | ভুল ক্লিনিকের নাম রোগীর কাছে যাচ্ছে | **"ঠিক করুন"** |
| ২ | স্টাফের পর্দায় ~২০৪টা বাংলা লেখা | **"পরে ভাববো এখন থাক, তবে প্রিন্ট আউটে যেন বাংলা না যায় — একমাত্র Diet Chart ছাড়া"** |
| ৩ | ছাপার তারিখে Locale-এর ঝুঁকি | **"বুঝলাম না, আগে বুঝিয়ে নেবেন"** |
| ৪ | ড্যাশবোর্ডে `BISWAS PILES CLINIC` | **"নো ইস্যু, যা আছে চলবে"** |
| ৫ | একই টাকা দু'বার দেখানোর ঝুঁকি | **"ঠিক করুন"** |

---

## ২) 🟢 যা করা হলো — ২টা

### (ক) খাতার সারি B72 — ক্লিনিকের নাম এখন ব্রাঞ্চ ধরে

**দোষটা কী ছিল:** Prescription · Medicine Slip · Diet Chart — এই তিনটে পর্দার WhatsApp/শেয়ার লেখায় ক্লিনিকের নাম **হাতে লেখা** ছিল, সব ব্রাঞ্চেই `TK Biswas Piles Clinic`। তাই **জলপাইগুড়ি · কোচবিহার · ফালাকাটা · বিরপাড়ার রোগী ভুল নাম পেতেন।** Medicine Slip পর্দার উপরের হেডারেও নামটা লেআউটে বসানো ছিল।

**যেভাবে ঠিক হলো:** নাম এখন **শুধু একটাই জায়গা `BranchCatalog` থেকে** আসে —
`BranchCatalog.byName(RoleSession.currentPatientBranch).clinicName`

- কিশনগঞ্জ → **TK BISWAS PILES CLINIC**
- জলপাইগুড়ি · কোচবিহার · ফালাকাটা · বিরপাড়া → **MAA AYURVED PILES CLINIC**

**যে ফাইলগুলো বদলেছে:**
- `clinical/PrescriptionActivity.kt`
- `clinical/MedicineSlipActivity.kt`
- `clinical/DietChartActivity.kt`
- `res/layout/activity_medicine_slip.xml` (নতুন id `tvSlipClinicName` — লেখাটা এখন কোড থেকে বসে)

**⚠️ ছোট চেহারার বদল:** Medicine Slip পর্দার নামটা এখন **বড় হাতের অক্ষরে** (`TK BISWAS PILES CLINIC`) দেখায়, আগে ছিল `TK Biswas Piles Clinic` — ছাপা ও অ্যাপের বাকি সব জায়গার সঙ্গে মিলিয়ে।

**নিরাপত্তা:** ব্রাঞ্চ জানা না গেলে `BranchCatalog.byName()` আগের মতোই কিশনগঞ্জ ধরে — অর্থাৎ পুরনো আচরণই থাকে, কিছু ফাঁকা হয় না। ⛔ নতুন কোনো ক্লাউড-কল নেই · ডেটাবেসে কিছু লেখা হয় না।

💻 **কম্পিউটারে এই দোষ ছিল না** — ওয়েবে `clinicPrintTitle()` আগে থেকেই ব্রাঞ্চ ধরে নাম বসায় (মিলিয়ে দেখা হয়েছে, `app.js` ছোঁয়া হয়নি)।

### (খ) খাতার সারি B73 — একই টাকা দু'বার দেখানোর ঝুঁকি বন্ধ

**ঝুঁকিটা কী ছিল:** Today's Collection-এ ফোনে জমা থাকা টাকা তালিকার সঙ্গে মেলানোর সময় নম্বর **পুরো সংখ্যা ধরে** মেলানো হত। এক দিকে `+91XXXXXXXXXX` (১২ সংখ্যা) আর অন্য দিকে `XXXXXXXXXX` (১০ সংখ্যা) থাকলে দুটোকে আলাদা মনে করত — ফলে **একই টাকা দু'বার** দেখানো সম্ভব ছিল।

**এখন:** অ্যাপের বাকি সব জায়গার মতোই **শেষ ১০ সংখ্যা** ধরে মেলে (`takeLast(10)`)।

**ফাইল:** `native/PaymentRepository.kt` — `mergeOwnPhonePayments()`-এর **দুই জায়গাতেই হুবহু একই নিয়ম** (একটা বদলে অন্যটা না বদলালে মেলানো ভেঙে যেত)।

⛔ **টাকার একটিও অঙ্ক বদলায়নি** — শুধু "এই সারিটা কি আগে থেকেই তালিকায় আছে?" মেলানোর নিয়ম। নতুন ক্লাউড-কল নেই · ডেটাবেসে কিছু লেখা হয় না।

---

## ৩) 🟡 যা TK-এর সিদ্ধান্তের অপেক্ষায় — কোডে হাত দেওয়া হয়নি

### খাতার সারি B74 — ছাপায় বাংলা (TK-এর স্থায়ী নির্দেশ)

> 🔒 **স্থায়ী নিয়ম (TK, 29.07.2026):** ছাপা কাগজে কখনো বাংলা যাবে না — **একমাত্র Diet Chart ছাড়া।**

**যা মিলিয়ে দেখা হয়েছে:** ছাপার প্রতিটা ফাইল — `print/` ফোল্ডারের ১০টা ফাইল · `ClinicPdfBuilder` · `ChamberRegisterPdfBuilder` · `ReportCardPrinter` · `PrintMappers` · `PrintCenterActivity`। **অ্যাপের নিজের লেখা কোনো বাংলা ছাপায় যায় না।**

**একটাই আসল উৎস পাওয়া গেছে — Treatment Progress-এর ৯টা "দ্রুত চিপ":**

`CHECK-UP করা হলো` · `KTA করা হল` · `DRESSING করা হল` · `KSHAR SUTRA করা হল` · `KSHAR SUTRA ক্লিয়ার করা হল` · `MEDICINE দেওয়া হল` · `TEST করতে পাঠানো হল` · `MACHINE এর কাজ করা হল` · `LIS করা হল`

চিপে চাপ দিলে ওই **বাংলা লেখাটাই** Treatment Progress ঘরে বসে যায়, আর সেটাই ছাপা হয় —
- **Report Card**-এর `PROGRESS` কলামে
- **Chamber Register**-এর `TREATMENT PROGRESS` কলামে

⚠️ **চিপগুলো দুই জায়গায় আলাদা করে লেখা আছে** — `native/ChamberAttendanceActivity.kt` (১৫৮০–১৫৮২) ও `native/ReportCardActivity.kt` (৪৫৫–৪৫৭)। ঠিক করার সময় **দুটোই একসাথে** করতে হবে, নইলে দুই পর্দায় দুই রকম হয়ে যাবে।

⚠️ **পুরনো রেকর্ডে আগে থেকে বসে থাকা বাংলা লেখা বদলাবে না** — ওগুলো ডেটাবেসে সেভ হয়ে আছে। নতুন এন্ট্রি থেকে ইংরেজি হবে।

🔴 **বাকি:** ইংরেজি লেখা কী হবে **TK ফাইনাল করবেন** — এগুলো ক্লিনিকের নিজের শব্দ (KTA · KSHAR SUTRA · LIS · MACHINE), তাই আন্দাজে অনুবাদ করা হয়নি।

💻 কম্পিউটারে এই চিপগুলো নেই (মিলিয়ে দেখা হয়েছে)।

### খাতার সারি B75 — ছাপার তারিখের Locale (অডিটের ৩ নম্বর)

**সহজ কথায়:** তারিখ বানানোর সময় কোডে বলে দিতে হয় "কোন ভাষার নিয়মে লেখো"। অ্যাপের প্রায় সব জায়গায় লেখা আছে `Locale.US` (ইংরেজি অঙ্ক — `29.07.2026`)। কিন্তু **ছাপার একটা ফাইলে** (`print/PrintMappers.kt`) লেখা আছে `Locale.getDefault()` — মানে **ফোনের ভাষা যা, সেই নিয়মে**। কোনো স্টাফের ফোনের ভাষা বাংলা হলে ওই তারিখ ছাপায় **বাংলা অঙ্কে (`২৯.০৭.২০২৬`)** উঠতে পারে।

**সমাধান এক শব্দের** — `Locale.getDefault()` কেটে `Locale.US`। আর কিছুই বদলায় না। এটা সরাসরি B74-এর নিয়মেরই অংশ।

---

## ৪) ⛔ যা ইচ্ছে করে করা হয়নি (TK-এর সিদ্ধান্ত অনুযায়ী)

- **স্টাফের পর্দার ~২০৪টা বাংলা toast/পপ-আপ** (১৯টা পর্দায় — সবচেয়ে বেশি Chamber ৫৭ · Timeline ৩৮ · RMP ১৯)। TK: *"পরে ভাববো এখন থাক।"* **কেউ যেন নিজে থেকে এটা শুরু না করে।**
- **ড্যাশবোর্ডের `BISWAS PILES CLINIC` নাম** (`DashboardActivity.kt` ৮৩)। TK: *"নো ইস্যু, যা আছে চলবে, পরে লাগলে বলবো।"* ⛔ **TK না বললে এটা বদলানো যাবে না।**
- **রোগীর তিন-ভাষার বার্তা · Diet Chart-এর বাংলা-হিন্দি পরামর্শ · পাবলিক সাইট** — এগুলো ইচ্ছাকৃত, ঠিক আছে, ছোঁয়া হয়নি।

---

## ৫) ✅ অডিটে যা যাচাই হয়েছে ও ঠিক পাওয়া গেছে

- পাহারাদার (`00_GUARD/tk_guard.py`) **১২/১২ পাশ** — কাজের আগেও, পরেও।
- ১৭৬টা Kotlin ফাইলের ব্র্যাকেট নির্ভুল · ২২৮টা XML well-formed · Supabase কলাম মেলে · ভার্সন সব জায়গায় **V154**।
- **`select=*` কোথাও নেই** · সারি-ধরে-ধরে ক্লাউড-কল নেই · বড় তালিকায় ঘর কমানো আছে।
- `runBlocking` যেখানে আছে সব **ব্যাকগ্রাউন্ড থ্রেডে** — পর্দা আটকায় না।
- ৯টা `!!` পরীক্ষা করা হয়েছে — সবগুলোই আগের লাইনে পাহারা দেওয়া, crash-এর ঝুঁকি নেই।
- `resolveFollowUpId` **চারটে পর্দাতেই** ঠিকভাবে ব্যবহার হচ্ছে (Follow-up · Timeline · Calendar)।
- Patient ID তৈরির নিয়ম — ক্লাউড + ফোনের নিজের খাতা, দু'দিকেই পাহারা আছে।
- ব্রাঞ্চের নম্বর/ঠিকানা `BranchCatalog`-এ মিলিয়ে দেখা হয়েছে (কোচবিহারের B33 সংশোধন ঠিক আছে)।

---

## ৬) নতুন লাইনের হাতে-যাচাই (build গ্যারান্টি)

| যা লেখা হয়েছে | যাচাই |
|---|---|
| `BranchCatalog.byName(x).clinicName` | `object BranchCatalog` · `fun byName(branch: String?): BranchInfo` · `val clinicName: String` — ✅ |
| `RoleSession.currentPatientBranch` | `var ... : String` — একই `clinical` প্যাকেজে, ✅ (ছাপার কোডও এটাই ব্যবহার করে, তাই ভরা থাকে) |
| `sb.append(...).append("\n")` | `StringBuilder.append` নিজেই `StringBuilder` ফেরত দেয় — ✅ |
| `findViewById<TextView>(R.id.tvSlipClinicName)` | id লেআউটে আছে · `TextView` আগে থেকেই import করা · `setContentView`-এর পরে বসানো — ✅ |
| `String.filter{}.takeLast(10)` | `String.takeLast(n): String` — ✅ |

XML-এ কোনো `--` নেই · কমেন্ট কোড গিলে ফেলেনি (পাহারাদার যাচাই করেছে) · কোনো নতুন লাইব্রেরি যোগ হয়নি · **Supabase-এ কোনো SQL লাগে না।**

---

# 🔒 দ্বিতীয় অংশ — ২৯.০৭.২০২৬ সকাল ১০.১০ · তারিখ ও সময়ের স্থায়ী নিয়ম

**খাতার সারি:** B75 (শেষ) · B76 (নতুন স্থায়ী নিয়ম)

## TK-এর কথা

> *"হ্যাঁ। Date সব সময় এরকমই হতে হবে — `31/12/2026` অথবা `31.12.2026`। আর যেখানে Time-এর ব্যাপার আছে সেখানে `11.30 AM`।"*

## ⛔ এটা একটা পুরনো নিয়ম বদলে দিয়েছে

24.07.2026-এ লক করা নিয়মে সময় লেখা হত **ছোট হাতের অক্ষরে** (`5.40 pm`)। **TK নিজে সেটা বদলেছেন** — এখন থেকে **বড় হাতের অক্ষরে** (`5.40 PM`)।

> ⛔ **ছোট হাতের `am`/`pm`-এ আর কখনো ফেরানো যাবে না।** কেউ পুরনো নোট দেখে যেন উল্টে না দেয়।

## যা যা বদলাল

| ফাইল | কী বদলাল |
|---|---|
| `native/DateUtil.kt` | `displayWithTime()` → `31.12.2026 5.40 PM`। **নতুন `timeOnly()`** (`11.30 AM`) — সময়ের নিয়ম যেন **এক জায়গা থেকেই** আসে, দুই পর্দায় দুই রকম না হয়। |
| `native/PatientTimelineActivity.kt` | `displayTime()` বড় হাতের অক্ষরে। ⛔ **৯টা–৬টার বাইরের কল লাল দেখানোর হিসাব অক্ষত** — ওটা আসল ISO সময় ধরে চলে, চোখে দেখা লেখা ধরে নয়। |
| `native/FollowUpModel.kt` | `displayShort()` আগে `16 Jul` দেখাত → এখন **`16.07.2026`** (ফলো-আপ ক্যালেন্ডারের দিন-পপ-আপে রিমার্কের তারিখ)। |
| `print/PrintMappers.kt` | **সারি B75** — তারিখ `Locale.getDefault()` → **`Locale.US`**। ফোনের ভাষা বাংলা/হিন্দি হলে ছাপা কাগজে তারিখ **বাংলা অঙ্কে** ওঠার ঝুঁকি ছিল, সেটা বন্ধ। |
| `clinical/MedicineSlipActivity.kt` | কোথাও ব্যবহার না হওয়া একটা **মৃত** তারিখ-ফরম্যাট (`Locale.getDefault()`) ও তার অকেজো import দুটো তুলে দেওয়া হলো — যাতে ভবিষ্যতে কেউ ভুল করে ওটা ব্যবহার না করে। |
| 💻 `03_NETLIFY_READY/app.js` | `wlv1Ampm()` বড় হাতের অক্ষরে — কম্পিউটারেও একই নিয়ম। **`node --check` পাশ।** |

## ✅ যা আগে থেকেই ঠিক ছিল — ছোঁয়া হয়নি

- **সেভ করার তারিখ `yyyy-MM-dd`** (৫৭ জায়গা) — এটা **ডেটাবেসের নিজের ধাঁচ**, চোখে দেখার জিনিস নয়। ⛔ **এটা কখনো বদলানো যাবে না** — বদলালে ছাঁকনি · ক্রম · টাকার হিসাব সব ভেঙে যাবে।
- **দেখানোর `dd.MM.yyyy`** (১১ জায়গা) ও কম্পিউটারের `fmtDate()` — আগে থেকেই নিয়ম মেনে চলত।
- **ক্যালেন্ডারের মাসের নাম** (`July 2026`) ও **সপ্তাহের দিনের নাম** (`Monday`) — এগুলো তারিখ নয়, লেবেল; তাই ছোঁয়া হয়নি।

## যাচাই

- পুরো প্রজেক্ট খুঁজে দেখা হয়েছে — তারিখ/সময় বানানোয় **আর কোথাও `Locale.getDefault()` নেই**।
- **আর কোথাও ছোট হাতের `am`/`pm` নেই** — ফোন ও কম্পিউটার দুটোতেই।
- পাহারাদার **১২/১২ পাশ** · `node --check` পাশ।
- TK-এর আসল ZIP-এর সঙ্গে মিলিয়ে দেখা হয়েছে — **ঠিক যে ফাইলগুলোতে হাত দেওয়ার কথা, শুধু সেগুলোই বদলেছে**, আর কিছু নয়।

**নতুন লাইনের হাতে-যাচাই:** `SimpleDateFormat("h.mm a", Locale.US).format(d)` → `String` ✅ · `DateUtil.timeOnly()` নতুন ফাংশন, কোথাও নাম-সংঘাত নেই ✅ · `buildSlipText(medicines)` — একটাই ডাকার জায়গা, সেটাও বদলানো হয়েছে ✅ · সরানো import দুটো ওই ফাইলে আর ব্যবহারই হয় না ✅।

⛔ **Supabase-এ কোনো SQL লাগে না। ভার্সন এখনো V154 — ফাইল পাঠানো হয়নি।**

---

# 🔍 তৃতীয় অংশ — ২৯.০৭.২০২৬ সকাল ১০.৩০ · দ্বিতীয়বার যাচাই (TK-এর নির্দেশে)

TK-এর কথা: *"যা যা কাজ করেছেন সেটা আরো একবার যাচাই করে দেখুন কোথাও কোন ভুল করেন নাই তো।"*

## ❌ নিজের ২টা ভুল ধরা পড়েছে — দুটোই সংশোধন করা হয়েছে

**১) অডিটের রিপোর্টে ভুল কথা বলেছিলাম।**
বলেছিলাম *"ছাপায় অ্যাপের নিজের কোনো বাংলা যায় না — একটাই উৎস, ৯টা চিপ।"* **এটা ভুল ছিল।** প্রথমবার শুধু `print/` ফোল্ডারের ফাইলগুলো খুঁজেছিলাম, কিন্তু বাংলা লেখা **বাইরের ফাইল থেকে ছাপার ঘরে পাঠানো** হচ্ছিল।

**দ্বিতীয় উৎস:** `native/ChamberAttendanceActivity.kt` (~২৪৮৮ লাইন)
```
treatment = r.remark.ifBlank { "⚠️ Progress লেখা বাকি" }
```
যে রোগীর Treatment Progress ফাঁকা, **Chamber Register-এর ছাপা কাগজে তার ঘরে ওই বাংলা লেখাটাই ছাপা হয়।** খাতার সারি B74-এ সংশোধন লেখা হয়েছে। 🔴 **ইংরেজি লেখা TK ফাইনাল করবেন** (প্রস্তাব: `⚠️ PROGRESS PENDING`)।

**২) TK যা চাননি, তা যোগ করেছিলাম।**
`DateUtil`-এ একটা নতুন `timeOnly()` ফাংশন বসিয়েছিলাম — **TK চাননি, আর কোথাও ব্যবহারও হচ্ছিল না**। তুলে দেওয়া হয়েছে (সঙ্গে `PatientTimelineActivity`-র কমেন্টে থাকা রেফারেন্সও)। TK-এর নিয়ম: *যেটুকু করতে বলা হয়েছে ঠিক শুধু সেটুকুই।*

## ✅ যা যাচাই করে ঠিক পাওয়া গেছে

| যা দেখা হলো | ফল |
|---|---|
| TK-এর আসল ZIP-এর সঙ্গে ফাইল-ধরে-ধরে মেলানো | **ঠিক ১১টা ফাইলেই হাত পড়েছে** (৮ Kotlin · ১ XML · ১ `app.js` · নোট), আর কিছুতে নয় ✅ |
| কেউ কি `am`/`pm` **লেখা ধরে** তুলনা বা খোঁজা করে? | **না** — তাই বড় হাতের অক্ষর করায় কোনো ছাঁকনি/হিসাব ভাঙেনি ✅ |
| Timeline-এর ৯টা–৬টার বাইরের কল **লাল দেখানো** | আসল ISO সময় ধরে চলে, দেখানোর লেখা ধরে নয় — **অক্ষত** ✅ |
| টাকার Audit লাইনে বাংলা আছে — ছাপায় যায়? | **না** — `PaymentModel.typedPartOf()` `Audit:`-এর পর থেকে সব বাদ দেয় ✅ |
| `buildSlipText()`-এর signature বদল | ডাকার জায়গা **একটাই**, সেটাও বদলানো হয়েছে ✅ |
| সরানো import দুটো | ওই ফাইলে আর কোথাও ব্যবহার হয় না ✅ |
| embedded `assets/www/app.js` | ওতে `wlv1Ampm` **নেই** (পুরনো কপি, WebView তুলে দেওয়া) — বদলানোর দরকার নেই ✅ |
| পাহারাদার · `node --check` · ভার্সন | **১২/১২ পাশ** · পাশ · **V154** ✅ |
