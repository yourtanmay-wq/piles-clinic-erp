# 🔒 LOCK NOTE — V209 (৩০.০৭.২০২৬ রাত)

**ভার্সন:** `versionCode 209` · `versionName 2.09` · পর্দায় **V209** · খাতার সারি **B202**
⛔ **কোনো SQL লাগবে না।**

---

## ⛔ সবার আগে — স্থায়ী নিয়ম
> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

---

## TK-এর রিপোর্ট (তীব্র ক্ষুব্ধ)
Follow-up-এর "Enquiry" তালিকায় ৩০.০৭.২০২৬-এ করা এনকোয়ারিতে "1d Due"/"3d Due" ব্যাজের পাশে একটা ছোট ক্যালেন্ডার-চিহ্নে "১৭" দেখা যাচ্ছিল — TK মনে করেছিলেন এটা ভুল তারিখ (১৭ জুলাই) দেখাচ্ছে।

## ✅ আসল কারণ (যাচাই করে ১০০% নিশ্চিত)

**"১৭" সংখ্যাটা অ্যাপের কোনো ডেটা নয়।** এটা 📅 (U+1F4C5, "Tear-Off Calendar") ইমোজি ক্যারেক্টারটার নিজের আঁকা ছবির অংশ — ফোনের ফন্ট (Noto Color Emoji ও প্রায় সব প্রধান emoji font) এই ইমোজিটা **সবসময় "১৭" সংখ্যা এঁকে** দেখায়, WhatsApp/Gmail/SMS-সহ পৃথিবীর প্রতিটা অ্যাপে এই একই ইমোজি এই একই "১৭" দেখাবে। পাশের **"1d Due"/"3d Due"/"Today Due" লেখাটাই আসল, সঠিক হিসাব** — সেটা কখনো ভুল ছিল না।

### ⛔⛔ গুরুত্বপূর্ণ — এই বাগ একবার আগেও ধরা পড়েছিল
`FollowUpActivity.kt`-এর **হেডারের ক্যালেন্ডার বোতামে** ঠিক এই একই সমস্যা **১৬.০৭.২০২৬-এ** TK রিপোর্ট করেছিলেন, ও তখন সেটা ঠিক করা হয়েছিল (দুই-লাইনের আসল Month/Day ব্যাজ বসিয়ে)। কিন্তু **"Due" ব্যাজের ভিতরের 📅 চিহ্নটা তখন বাদ পড়ে গিয়েছিল** — তাই একই বাগ একটা ভিন্ন জায়গায় (Due badge) আবার দেখা দিল। এটা নতুন কোনো ভুল নয়, পুরনো একটা ফিক্স সম্পূর্ণ না হওয়ার ফল।

## ✅ সমাধান
`FollowUpActivity.kt`-এ "Due" ব্যাজের 📅 বদলে **⏰** (একই ফাইলের "Overdue" লেবেলে আগে থেকেই এই আইকন ব্যবহার হয়, কখনো এই সমস্যা হয়নি কারণ ⏰-এর ছবিতে কোনো বাড়তি সংখ্যা আঁকা থাকে না):

```kotlin
days == 0 -> { label = "⏰ Today Due"; ... }
else -> { label = "⏰ ${days}d Due"; ... }
```

⛔ **রং · লজিক · দিন-গণনা · ছাঁকনি-তে চাপ দিলে যা হয় — কিছুই বদলায়নি**, শুধু আইকন।

## 🔍 একই ধরনের বাগ প্রজেক্টের অন্য কোথাও (TK-এর স্থায়ী নিয়ম অনুযায়ী খুঁজে ঠিক করা হয়েছে)

| ফাইল | অবস্থা | কী করা হলো |
|---|---|---|
| `FollowUpAdapter.kt` | বর্তমানে অব্যবহৃত (dead code — এই ক্লাস প্রজেক্টের কোথাও instantiate হয় না) | ভবিষ্যতের নিরাপত্তার জন্য একইভাবে ঠিক করা হলো |
| `03_NETLIFY_READY/app.js` | ওয়েব অ্যাপ, এখনো Netlify-তে আপলোড হয়নি | ঠিক করা হলো |
| `.../assets/www/app.js` | অ্যাপের ভিতরে বান্ডিল করা পুরনো ওয়েব কপি | ঠিক করা হলো |

## 🔍 যাচাই
- পাহারাদার (`tk_guard.py`) **১৭/১৭ পাশ**
- চারটে ফাইলেই এডিটের পরে গ্রেপ করে নিশ্চিত করা হয়েছে যে 📅 আর কোথাও "Due" ব্যাজে নেই
- ব্র্যাকেট-গণনা `FollowUpActivity.kt`/`FollowUpAdapter.kt`-এ আলাদা করে মিলিয়ে দেখা হয়েছে

## 🔴 এখনো বাকি
- **TK-এর লাইভ টেস্ট** — এবার "Due" ব্যাজে শুধু ⏰ দেখাবে, কোনো সংখ্যা-জাতীয় বিভ্রান্তি থাকবে না
- **B148 — RLS** ⛔ TK-এর অনুমতি ছাড়া নিষেধ
- `03_NETLIFY_READY` Netlify-তে আপলোড (TK-এর কাজ)
