# 🔒 LOCK NOTE — V184 (৩০.০৭.২০২৬ দুপুর ২.০০) · খাতার সারি **B168**

> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

**ভার্সন:** `versionCode 184` · `versionName 1.84` · পর্দায় **V184**
**⛔ Supabase-এ কোনো SQL লাগবে না।**
**🔴 TK-কে করতে হবে: `03_NETLIFY_READY` Netlify-তে আপলোড — নইলে এই কাজ কম্পিউটারে আসবে না।**

---

## TK কী বলেছেন (৫ নম্বর সন্দেহ)

> *"Web App-এর Delete ব্যবস্থা Android-এর মতো নিরাপদ নয়। Web App সাধারণ Delete করলে মূল Cloud record সত্যিকারভাবে Delete বা Tombstone হয় না। পরে Cloud merge হলে record আবার ফিরে আসতে পারে। Payment Delete-এর ক্ষেত্রেও Cloud Delete ব্যর্থ হয়েছে কি না না দেখেই স্থানীয়ভাবে Delete সফল দেখানোর পথ আছে।"*

**TK-এর অনুমতি:** "করুন"।
ওয়েবের **প্রতিটা ডিলিটের পথ** খুঁজে দেখা হয়েছে — পাঁচটা জায়গা পাওয়া গেছে।

---

## ১) 💰 Payment Delete — চালু পথ, সবচেয়ে জরুরি

লাইনটা ছিল:
`try{ if(sb) await sb.from('payments').delete().eq('id',row.id) }catch(_e){}`

অর্থাৎ **ফলটা দেখাই হত না**। লাইন কেটে গেলেও পর্দায় *"Payment deleted — Master informed"* দেখাত, অথচ সারিটা ক্লাউডে রয়ে যেত — পরের merge-এ **টাকাটা আবার ফিরে আসত**। ফোনে এক হিসাব, কম্পিউটারে আরেক।

**এখন:** ক্লাউডে **সত্যিই মোছার পরেই** এই কম্পিউটার থেকে মোছা হয়। না পারলে **কিছুই মোছা হয় না**, আর স্পষ্ট বার্তা যায়।
সঙ্গে **ডিলিটের চিহ্ন ক্লাউডে লেখা হয়**, তাই **ফোনগুলোও জেনে যায়** — এতদিন ওয়েবে এই চিহ্নই ছিল না (খাতার সারি B145-এ TK-কে জানানো ফাঁকটা এবার বন্ধ হলো)।

## ২) ♻️ Trash থেকে Restore

পুরনো `restoreTrash()` রেকর্ডটা **শুধু এই কম্পিউটারে** ফেরাত ও Trash সারিটা শুধু এখান থেকেই মুছত — **ক্লাউডে কিছুই হত না**।
ফল: ক্লাউডে Trash সারিটা থেকে যেত, আর ফোনে বসানো "মুছে ফেলা" চিহ্ন থেকে যাওয়ায় **ফেরানো রেকর্ড ফোনে আটকেই থাকত** (ঠিক সারি B167-এর সমস্যাটাই, উল্টো দিক থেকে)।

**এখন তিন ধাপ:** ক্লাউডে রেকর্ড ফেরানো → চিহ্ন তোলা → Trash সারি মোছা।
⛔ **প্রথম ধাপ ব্যর্থ হলে কিছুই এগোয় না।**

## ৩) 🗑️ Delete Forever

ক্লাউডের ফল দেখত না — লাইন কেটে গেলেও "মুছে ফেলা হয়েছে" দেখাত, অথচ সারিটা পরে আবার ফিরে আসত। **এখন ক্লাউডে সত্যিই মোছার পরেই।**

## ৪) 🧹 `delSoft` (সাধারণ Delete)

ক্লাউডে কিছুই মুছত না।
⛔ **যাচাই করে দেখা গেছে এটা এখন কোথাও ব্যবহারই হয় না** (পুরো ফাইলে একটাও কল নেই) — তাই আজ কোনো কাজের পথ বদলায়নি। কিন্তু ভবিষ্যতে কেউ জুড়ে দিলে যাতে বিপদ না হয়, এখনই ফোনের নিয়মে আনা হলো।

## ৫) 🔑 Trash-এর আইডি

ওয়েবেও এখন **ফোনের হুবহু একই নিয়মে** রেকর্ড ধরে বাঁধা (সারি B165-এর `trashIdFor` — জাভা-হ্যাশ পর্যন্ত এক)। তাই বারবার চেষ্টা করলে, এমনকি **ফোন ও কম্পিউটার দুই জায়গা থেকে মুছলেও**, Trash-এ একটাই এন্ট্রি থাকবে।

## ⛔ যা ছোঁয়া হয়নি

- **চালু কোনো কোডে হাত পড়েনি** — নতুন সবকিছু ফাইলের **শেষে যোগ**, পুরনো ফাংশন `window.<নাম>` বদলে ঢাকা হয়েছে (প্রজেক্টের চালু নিয়ম, সারি B160)।
- কোনো **ডিজাইন** · **টাকার হিসাব** · **ব্রাঞ্চের ছাঁকনি** বদলায়নি।
- **SQL লাগবে না** — `deleted_records` টেবিলটা TK নিজেই 29.07.2026-এ বানিয়েছেন (সারি B139)।

## ⚠️ নিজের একটা ভুল ধরা পড়েছে ও সঙ্গে সঙ্গে ফেরানো হয়েছে

কাজ করার সময় ভুল করে APK-র ভিতরের পুরনো `assets/www/app.js` (৪৩৭৫ লাইনের **আলাদা** ফাইল, পুরনো WebView-এর) ওয়েবের ৭৩৫৫ লাইনের ফাইল দিয়ে চাপা দিয়ে ফেলেছিলাম।

**আসল ZIP থেকে হুবহু ফিরিয়ে আনা হয়েছে** ও যন্ত্রে লাইন-ধরে মিলিয়ে দেখা হয়েছে — এখন ওই ফাইলে **শুধু পর্দায় লেখা ভার্সনটুকুই** বদলেছে, আর একটা অক্ষরও নয়।

## যাচাই (কাজের পরে আবার, TK-এর নিয়ম)

- `node --check` — **দুটো `app.js`-এই পাশ**
- পুরনো `assets/www/app.js`-এর সঙ্গে **লাইন-ধরে মিলিয়ে দেখা** — শুধু ভার্সনের লেখা আলাদা
- পাহারাদার `tk_guard.py` — **১৭টা যাচাই, সব পাশ** (কম্পিউটারের অ্যাপের যাচাই ৯.৯ সহ)
- আগের অনুমোদিত কাজের যাচাই — **৫৮/৫৮ পাশ**
- ভার্সন চার জায়গায় এক — ZIP · `versionCode 184` · `versionName 1.84` · পর্দায় **V184**

**ফাইল:** `03_NETLIFY_READY/app.js` · `build.gradle.kts` · `DashboardActivity.kt` · `assets/www/app.js` (শুধু ভার্সনের লেখা)

---

## 🔴 যা এখনো বাকি

TK-এর ৯টা সন্দেহের **বাকি ৪টা** — ⛔ প্রতিটার আগে আলোচনা ও TK-এর স্পষ্ট অনুমতি বাধ্যতামূলক:

৬. একাধিক Retry এক তালার নিচে · ৭. Chamber Close সতর্কবাতিতে গোনা · ৮. Automatic Refresh (খরচের সিদ্ধান্ত TK-এর) · ৯. TK-এর লেখাটা কেটে গেছে — পুরো কথা জানতে হবে।

আগে থেকেই বাকি: **B148 — RLS** (⛔ TK-এর অনুমতি ছাড়া নিষেধ) · TK-এর লাইভ টেস্ট · সপ্তাহখানেক পরে Supabase-এর খরচ দেখা।
