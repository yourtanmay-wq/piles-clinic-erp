# V521 — স্টাফ পর্দার পাঁচটা কাজ
**তারিখ:** ২২.০৮.২০২৬ · **ভিত্তি:** V520 · সব কটাই TK-এর সরাসরি নির্দেশে।

---

## ১. কার্ডে "View" বোতাম নেই — কার্ডে চাপ দিলেই View

TK: *"এই কার্ডের মধ্যে ভিউ থাকবে না, কিন্তু কার্ডে চাপ দিলে ভিউ হবে। তাহলে
Salary · Performance · Fix Attendance — এই তিনটা পাশাপাশি থাকবে।"*

- **Android** `StaffProfileActivity.staffCard()` — "View" বোতাম সরানো, পুরো কার্ড clickable।
- **Web** `profile.js` — একই বদল, নতুন `pfCardTap()` হেল্পার।
- ⛔ ভিতরের বোতামে চাপ দিলে কার্ডের চাপ **চলে না** (Android নিজেই আটকায়;
  ওয়েবে `event.target.closest('button')` দেখে নেওয়া হয়)। তাই Salary চাপলে
  ভুল করে View খুলবে না।
- ⛔ `onView`/`profEdit` ফাংশন এক অক্ষরও বদলায়নি।

---

## ২. Extra Income — **কেন** টাকা পাওনা, সেটা এখন দেখা যায়

TK: *"কি কারণে দিচ্ছি সেটা তো বোঝা যাচ্ছে না… আমি বুঝবো কী করে যে স্টাফটা কী
কারণে টাকা নিচ্ছে?"*

**আসল নিয়মটা কোডে যাচাই করা** (`V418_INCENTIVE_AUTO_2026-08-17.sql`):
রোগীর সারিতে `timeType = 'Unexpected Time'` হলে তবেই Extra।
Registration Fee জমা → **₹১০০**; প্রথম Advance/Treatment → আরও **₹৪০০**;
Enquiry-ভরা ও Registration-করা স্টাফের মধ্যে **৫০-৫০**।

**যা ফাঁক ছিল:** SQL শুধু `"Registration · COB-…"` লিখত — *Unexpected Time*
কথাটা **কোথাও লেখাই হত না**। তাই পপ-আপ খুলেও বোঝার উপায় ছিল না।

**এখন দেখায়** (পপ-আপ ও তালিকার লাইন, দুই জায়গাতেই):
```
Timing:  ⏰ UNEXPECTED TIME
For:     Registration · COB-21082026-004
Step:    Registration Fee received  →  ₹100
────────────
Rule: only an UNEXPECTED TIME enquiry earns extra.
₹100 when that number registers and pays the fee,
₹400 more when the same patient pays an advance.
Shared 50-50 with the staff who took the enquiry.
```
রোগীর সারিতে `Unexpected Time` **না** থাকলে লাল সতর্কবার্তা — কারণ তখন
টাকাটা পাওনাই হওয়ার কথা নয়, চুপ করে থাকা ঠিক নয়।

- ⛔ **কোনো SQL চালাতে হবে না** · database-এ কিছু বদলায় না · টাকার অঙ্ক
  এক পয়সাও বদলায় না — এটা পুরোপুরি **দেখানোর** কাজ।
- ⛔ **Android-এ একটাও বাড়তি cloud-read নেই** — যে ব্যাচ-পড়াটা আগে থেকেই
  রোগীর নাম আনত, তাতে শুধু `timeType` কলামটা যোগ হয়েছে।
- **Web-এ একটা ছোট খরচ হয়েছে, লুকাচ্ছি না:** `patients`-এর কলাম-তালিকায়
  `timeType` যোগ করতে হয়েছে (ওয়েব এটা আগে আনতই না)। লেখাটা সর্বোচ্চ ১৫
  অক্ষরের; ওই তালিকায় `photo`/`doctorFullNote`/`medicalHistory` থাকায়
  আনুপাতিক বৃদ্ধি নগণ্য।

## ৩. রোগীর History পর্দাতেও Timing চিপ

ID-র পাশে `⏰ UNEXPECTED TIME` / `🕐 OFFICIAL TIME` — Android ও Web দুটোতেই।
Enquiry ধাপে ID থাকে না, তবু Timing দেখায় (এখান থেকেই তো সব শুরু)।

- ⛔ **বাড়তি cloud-read নেই** — এই পর্দা `patients`/`enquiries` আনে
  `select=*` দিয়ে (`PatientTimelineRepository.byMobile`, লাইন ২৪৯–২৫৫),
  তাই `timeType` আগে থেকেই আসত, কেউ পড়ত না।
- `TimelineCache`-এও ঘরটা যোগ — cache থেকে আঁকার সময়েও চিপ দেখা যায়।

## ৪. Add Salary — দুটো বোতাম পাশাপাশি, নতুন লেখা

TK: *"Add Payment · Cancel এই দুইটা পাশাপাশি থাকবে, একটা যেন আরেকটার গায়ে
ঘেঁষে না যায়। 'Add Payment' লেখা থাকবে না — 'Salary Payment' হবে।"*

দুটো ফর্মেই (`payForMonth`, `addSalaryAnyMonth`) এক সারিতে সমান দুই ভাগ,
মাঝে ফাঁক — ঠিক যে সাজ `editSalaryConfig`-এ TK আগেই পাশ করেছেন।
লেখা: **Cancel** · **Salary Payment**।
⛔ সেভের নিয়ম · টাকার হিসাব · database — এক অক্ষরও বদলায়নি।

## ৫. একই স্টাফ খুললে বারবার "Loading..." — বন্ধ

TK: *"একই স্টাফের বারবার কেন লোডিং দেখাবে? যতবারই ওপেন করি ততবারই লোডিং কেন?"*

**আসল কারণ (কোডে যাচাই করা):** `loadSalary()` প্রতিবার **চারটে** নেট-কাজ
করত (`incentive_sync` RPC + তিনটে পড়া), আর **কিছুই জমা রাখত না**।

**এখন cache-first** — ঠিক যে পদ্ধতি `TimelineCache`-এ (V216 §10) আগেই পাশ:
- জমানো তথ্য থাকলে **সঙ্গে সঙ্গে** পর্দা আঁকে, "Loading..." নয়।
- পিছনে আসল পড়া চলে; নতুন তথ্য এলে নিজে থেকেই হালনাগাদ।
- জমানো তথ্য সর্বোচ্চ **১০ মিনিট** পুরনো হতে পারে।
- **বেতন যোগ / সেটিং বদল / Extra যোগ — প্রতিটা লেখার পরে জমানো তথ্য
  সঙ্গে সঙ্গে মুছে যায়** (`salaryCacheClear`), তাই কখনো পুরনো হিসাব দেখাবে না।
- পড়া **ব্যর্থ** হলে জমানো তথ্যই থাকে (আগে পুরো পর্দা ফাঁকা হয়ে যেত)।
- `incentive_sync` (একটা **লেখার** RPC) এখন **৫ মিনিটে একবার** — আগে প্রতিবার।

⇒ Supabase-এ **query কমেছে, বাড়েনি**।

## ৬. অ্যাপ লক — ১৫ মিনিটের ছাড়

TK: *"যতবারই বের হই যতবারই ঢুকি তখন এরকম দেখায়… এত হাই সিকিউরিটি প্রয়োজনে
আমার দরকার নেই। একটা নির্দিষ্ট টাইম পিরিয়ডের পরে প্রয়োজন এরকম দেখাবে।"*

**আসল কারণ (কোডে যাচাই করা):** `PilesClinicApplication.onActivityStopped`-এ
শেষ পর্দা বন্ধ হওয়া মাত্রই `AppLock.onAppBackgrounded()` → **সঙ্গে সঙ্গে**
`locked = true`। কোনো সময়সীমা ছিল না। তাই ছবি তুলতে ক্যামেরায় গেলে বা
স্ক্রিন এক মুহূর্ত নিভলেও ফিরে এসে আবার আঙুল চাইত।

**এখন (TK-এর নিজের বাছা):**
- পিছনে যাওয়ার সময় মনে রাখা হয়; **১৫ মিনিটের কম** হলে তালা লাগে না।
- **অ্যাপ পুরোপুরি বন্ধ হলে বা ফোন রিস্টার্ট হলে সবসময় চাইবে** — কারণ
  সময়টা শুধু চলতি process-এ থাকে, ফোনে জমা হয় না।
- লগআউট করলেও (`reset()`) ছাড় নেই।
- `SystemClock.elapsedRealtime()` ব্যবহার — ফোনের ঘড়ি বদলে ছাড় বাড়ানো যায় না।
- ⛔ **হাজিরার নিয়ম এক অক্ষরও বদলায়নি** (আলাদা গেট + GPS)।

🔓 **সৎ কথা:** এটা তালাটাকে হালকা করে। ১৫ মিনিটের মধ্যে ফোন অন্য কারো হাতে
পড়লে অ্যাপ খোলা পাওয়া যাবে। TK এটা জেনেই বেছেছেন।

---

## যাচাই

| যাচাই | ফল |
|---|---|
| V521 Extra-income কারণ — Android (আসল কোড তুলে) | ১৭/১৭ PASS |
| V521 Extra-income কারণ — Web (আসল কোড তুলে) | ১৫/১৫ PASS |
| V520 walk-in + offline | ৩৪/৩৪ PASS |
| V520 ওয়েব টাকা-আলাদা | ১৪/১৪ PASS |
| V516 Registration (+offline) | ALL PASSED |
| ওয়েব: webfu · webfam · websearch · dq | ALL PASSED |
| ৮টি ওয়েব ফাইল `node --check` | ৮/৮ OK |

## সৎ সীমাবদ্ধতা

**Activity ফাইল কম্পাইল করা যায়নি** — এই মেশিন থেকে Android SDK
(`dl.google.com`) পৌঁছয় না (403)। `StaffProfileActivity` ·
`PatientTimelineActivity` · `AppLock` — হাতে যাচাই করা হয়েছে:
ব্যবহৃত প্রতিটা নাম কোডে খুঁজে দেখা হয়েছে (`tvChips` layout-এ আছে,
`MODE_PRIVATE` একই ফাইলে আগেই ব্যবহৃত, `renderSalary` শুরুতেই
`box.removeAllViews()` করে, দুটো `TimelineData(...)` ডাকেই named-arg),
আর ব্রেস-ভারসাম্য `git HEAD`-এর সঙ্গে মিলিয়ে দেখা হয়েছে (সব `+0`)।
`StaffProfileActivity`-র পপ-আপের অংশটা **হুবহু তুলে এনে কম্পাইল ও চালানো
হয়েছে**, তাই ওই অংশটা প্রমাণিত। **TK-কে বিল্ড করে একবার দেখে নিতে হবে।**

---

## ৭. নম্বর আর লুকানো নয় · সারিতে চাপ দিলে রেকর্ডে যাওয়া

TK: *"নাম্বার এত হাই সিকিউরিটি ভাবে দেখানোর কিছু নেই… নাম্বারটা সম্পূর্ণভাবে
দেখাবে। আর এই নাম্বারের উপর চাপ দিলে যেন রিডাইরেক্ট হয়।"* এবং
*"সেই ব্যক্তি রেজিস্ট্রেশন যেখানে হয়েছে সেখানে যেন রিডাইরেক্ট হয়ে যায়।"*

**(ক) পুরো নম্বর** — নতুন `ModuleUi.fullMobile()` / `MOD.fullMobile()`।
বসেছে: স্টাফ কার্ড · My Profile · Alternate Mobile · Work Notebook-এর কল-লগ
(Android ও Web দুটোতেই)।
⛔ পুরোনো `maskMobile()` **মোছা হয়নি** — সরকারি আইডি ও কল-লগে সংরক্ষিত মুখোশ
(`target_mobile_mask`) ওটাই ব্যবহার করে; ভাঙা চলবে না।

**(খ) সারিতে চাপ = ঐ নম্বরের রেকর্ড** — নতুন `perfOpenNumber()`।
বসেছে: **Registrations** · **Started Treatment** · **RMP Added** · **Calls**।
আগে Registrations-এ চাপ দিলে কিছুই হত না। এখন সোজা ঐ রোগীর History খোলে।
⛔ বাড়তি cloud-read নেই — নম্বরটা তালিকার সারিতেই আছে।

**⚠️ একটা সত্যি কথা, যেটা লুকাচ্ছি না —**
*"Calls From App"*-এ যে সারিগুলোয় `••••••8496` দেখাচ্ছে, সেগুলোয় অ্যাপ
নম্বরটা **লুকাচ্ছে না** — **পুরো নম্বরটা কখনো সেভই হয়নি**।
সার্ভার লেখে `coalesce(nullif(c.target_mobile,''), c.target_mobile_mask)`
(`V452_STAFF_PERFORMANCE_EXACT_DETAIL_2026-08-19.sql`, লাইন ১০২) — অর্থাৎ
পুরো নম্বর থাকলে সেটাই দেয়, না থাকলে তখন সংরক্ষিত মুখোশটা।
আপনার ছবিতেই তার প্রমাণ: **২২/০৮/২০২৬-এর সারিটা পুরো নম্বর দেখাচ্ছে**
(`919733411825`) — নতুন কলে পুরো নম্বর সেভ হচ্ছে; পুরোনোগুলোয় হয়নি।
⇒ **নতুন কলগুলোয় চাপ দিলে এখন সোজা ঐ নম্বরের রেকর্ডে চলে যাবে।**
   পুরোনো (মুখোশ) সারিতে চাপ দিলে আগের মতোই ডিটেল পর্দা খোলে, যেখানে
   কারণটা স্পষ্ট লেখা আছে। **পুরোনো নম্বর ফিরিয়ে আনা সম্ভব নয় — নেই।**

| যাচাই | ফল |
|---|---|
| V521 পুরো-নম্বর ফাংশন (আসল কোড তুলে) | ৭/৭ PASS |
