# V591 — ভাঙা বাংলা সারানো · সরানো ছবি ফেরানো
**তারিখ:** ২৩.০৮.২০২৬ · **ভার্সন:** V591 / 5.91

TK দুটো কাজের **সম্পূর্ণ অনুমতি** দিয়েছেন:
> *"হ্যাঁ সাবধানে করুন · দুইটা কাজেরই অনুমতি সম্পন্ন দেয়া হলো · সততার সাথে
> সঠিকভাবে কাজ করবেন কোন ভাল কাজ যেন খারাপ না হয় · কোন প্রকার ঝুঁকি নিয়ে
> চলবে না সাবধানে নিরিবিলি করবেন"*

---

## কাজ ১ · কিশানগঞ্জের স্টাফের পর্দায় ভাঙা বাংলা

### TK কী ধরেছিলেন
Enquiry Reject List-এ *"মুছে ফেলা হচ্ছে"* — বাংলা রয়ে গেছে।
> *"কিশান্গঞ্জ এর যে কোন স্টাফ এর কাছে যেন বাংলা পর্দা কোথাও না থাকে ·
> সত্যতা যাচাই করুন আগে খুঁটিয়ে খুঁটিয়ে সম্পূর্ণ প্রজেক্টে · বাংলার পরিবর্তে
> হয় হিন্দি অথবা ইংলিশ"*

### যাচাই করে পাওয়া আসল কারণ
`NoBengali` ইঞ্জিনের **শেষ জাল** — অনুবাদের তালিকায় যে বাংলা লেখাটা নেই,
সেটার **বাংলা অক্ষরগুলো মুছে** দেওয়া হয়। তাই লেখাটা উধাও হত না, **ভেঙে**
যেত (কখনো ফাঁকা, কখনো অর্ধেক)। অর্থাৎ দোষটা জালের নয় — তালিকায় লেখাগুলো
**ছিলই না**।

শিপ-করা `fix()`-এর নিয়মটা হুবহু চালিয়ে গোনা হয়েছে (আন্দাজে নয়):
**পর্দায় সত্যিই ভাঙা লেখা ছিল 206টা** (370 নয় — বাকিগুলো ছাপা/WhatsApp-এর
লেখা, ওখানে এই জাল পৌঁছায়ই না)।

### যা করা হলো
| | আগে | এখন |
|---|---|---|
| পর্দায় ভাঙা লেখা (ফোন) | 206 | **0** |
| `WHOLE` তালিকা | 101 | **117** |
| `HINDI` তালিকা | 147 | **347** |
| ওয়েবের তালিকা | 40 · 238 · 136 | **ফোনের হুবহু এক** |

**দুটো আসল ফাঁদ চলতে গিয়ে ধরা পড়েছিল** (দু'বার কাজ ফিরিয়ে নিয়ে ঠিক করা হয়েছে):

1. `fix()` লেখাটাকে **আগে `strip()`** করে, তারপর মেলায়। তাই শুরুতে/শেষে
   নতুন-লাইন রাখা চাবি কোনোদিনই মিলত না।
2. **হিন্দির তালিকা আগে চলে**, ইংরেজিরটা পরে। তাই ছোট হিন্দি শব্দ আগে বসে
   গিয়ে লম্বা ইংরেজি বাক্য ভেঙে দিচ্ছিল।
   ⇒ নতুন সব সারি **হিন্দির তালিকায়**, চাবিগুলো `strip()` করা।

### সংখ্যা বসা ঘর — তালিকায় রাখা যায় না
`"ফোলা $piles টা"` · `"${h}টা"` ধাঁচের লেখায় সংখ্যাটা **চলার সময়ে** বসে,
তাই তালিকায় চাবি বানানো অসম্ভব — জাল ওগুলোর "টা" মুছে দিত।

দুটো ছোট, **শক্ত করে বাঁধা** নিয়ম বসানো হলো (ফোন `NoBengali.kt` · ওয়েব `app.js`):

```kotlin
private val NUM_COUNT  = Regex("([0-9]+) টা(?![ঀ-৿])")   // "3 টা" → "3 नग"
private val NUM_OCLOCK = Regex("([0-9]+)টা(?![ঀ-৿])")    // "3টা"  → "3 बजे"
```

⛔ **নিরাপত্তা** — শুধু **সংখ্যার পরেই** চলে, আর "টা"-র পরে আর কোনো বাংলা
অক্ষর থাকলে চলেই না। কম্পাইল করে চালিয়ে দেখা হয়েছে:

| লেখা | ফল |
|---|---|
| `ফোলা 3 টা` | `सूजन 3 नग` ✅ |
| `মাংস ফোলানো 12 টা` | `माँस की सूजन 12 नग` ✅ |
| `7টা` · `12টা` | `7 बजे` · `12 बजे` ✅ |
| `ফোলা: 3টা, 7টা` | `सूजन: 3 बजे, 7 बजे` ✅ |
| `₹6000 / ক্ষার সূত্র` | `₹6000 / क्षार सूत्र` ✅ |
| `অবশিষ্ট টাকা` · `💵 টাকার হিসাব` | **অবিকল অপরিবর্তিত** ⛔ |
| `অফিস টাইম শেষ` · `টাটকা ফল ও সবজি খান` | **অবিকল অপরিবর্তিত** ⛔ |
| `আঙুল টানুন` · `মোট 4টাকা দিন` · `5 টাকা` | **অবিকল অপরিবর্তিত** ⛔ |

### ওয়েব ও ফোন এক করা
ওয়েবের তিনটে তালিকা এখন **ফোনের `NoBengali.kt` থেকেই তৈরি** — একটাই উৎস।
মিলিয়ে দেখা হয়েছে: ওয়েবের পুরোনো ৪১৪টা সারির **একটাও বাদ যায়নি**, সবগুলোই
ফোনের তালিকার ভিতরেই ছিল।

---

## কাজ ২ · সরানো রোগের ছবি ফেরানো

### TK-এর প্রশ্ন
> *"এখান থেকে যে ফটো গুলি বাদ দেওয়া হয়েছে — সে গুলি পরে আবার কিভাবে এবং
> কোথায় পাবো"*

### যাচাই করে পাওয়া সত্যি
ছবিগুলো **কখনোই মোছা হয়নি** — ক্লাউডের `anatomy_pictures` সারিতে
`hidden = "1"` বসে শুধু **দেখানো বন্ধ** হত। কিন্তু অ্যাপে ফেরানোর **কোনো পথই
ছিল না**, তাই ব্যবহারিকভাবে হারিয়েই যেত।

### যা করা হলো
ছবির সারিতে **♻ ফেরান** ঘর — সরানো ছবি থাকলে **তবেই** দেখায় (না থাকলে
সারিতে বাড়তি কিছুই বসে না)। চাপলে সরানো ছবির তালিকা, একটা বাছলেই সেটা
**আগের ক্রমে, আগের নামে** ফিরে আসে।

| ফাইল | কী যোগ হলো |
|---|---|
| `clinical/AnatomyPictureRepository.kt` | `hiddenRows()` · `restoreRow()` · `fetchPhoto()` |
| `clinical/DoctorCheckupActivity.kt` | ♻ ঘর · `askRestorePicture()` · `doRestorePicture()` |
| `03_NETLIFY_READY/app.js` | `wlv1AnatHiddenRows()` · `wlv1AnatRestoreOpen()` · `wlv1AnatRestorePic()` |
| `03_NETLIFY_READY/styles.css` | `.wlv1AnatAdd.wlv1AnatBack` (শুধু রঙ, মাপ হুবহু আগেরটাই) |

**চালিয়ে প্রমাণ** (`node` দিয়ে সত্যিকারের ফাংশনগুলো চালিয়ে):

```
সরানোর পর পর্দায়      : ফোলা · কাছ থেকে · ছোট ফোলা
সরানো আছে             : একটা ফোলা · চেম্বারের ছবি
অ্যাপের ছবি ফেরানোর পর : ফোলা · কাছ থেকে · একটা ফোলা · ছোট ফোলা   ← আগের জায়গাতেই
যোগ-করা ছবি ফেরানোর পর : চেম্বারের ছবি · ফোলা · কাছ থেকে · একটা ফোলা · ছোট ফোলা
এখন সরানো বাকি        : 0
```

### ⛔ যে ঝুঁকিগুলো আটকানো হয়েছে
- ডাক্তারের **যোগ-করা** ছবির ছবিটুকু এই ফোনে না-ও থাকতে পারে (V576-এ egress
  বাঁচাতে লুকানো সারির ছবি নামানো বন্ধ)। তখন ক্লাউড থেকে **শুধু ওই একটাই**
  নামানো হয়। **না পারলে কিচ্ছু জমা হয় না** — ফাঁকা ছবির সারি বসে ছবিটা
  চিরতরে ফাঁকা হয়ে যাওয়ার পথ বন্ধ।
- নেটওয়ার্কের কাজটা আলাদা থ্রেডে, তাই পর্দা কখনো আটকায় না।
- ফেরানোর সারিতে `id · picKey · label · sortOrder · createdAt` **হুবহু আগেরটাই**
  থাকে, শুধু `hidden` ফাঁকা হয় — তাই ক্রমও আগের জায়গাতেই ফেরে।

### ⚠️ TK-কে সৎভাবে জানানো
ছবি **সরানো ও ফেরানো — দুটোই সব ব্রাঞ্চে একসাথে** কাজ করে, কারণ তালিকাটা
একটাই ক্লাউড-তালিকা (V573-এর নকশা, TK-অনুমোদিত)। এটা V591-এ বদলানো হয়নি।

---

## পাহারাদার

নতুন: `00_GUARD/verify_v591_bangla_o_chhobi_ferano.py` — **52টা যাচাই, সব পাশ**।
এটা কোড শুধু *পড়ে* না — কাজ ১-এর জন্য ফোনের নিজের অনুবাদ-নিয়মটা **এখানে
হুবহু চালিয়ে** দেখে (verification by execution)।

**উল্টো-পরীক্ষাও করা হয়েছে** (ইচ্ছে করে ভেঙে দেখা হয়েছে পাহারাদার ধরে কিনা):
নিয়মের নাম বদলালে ❌ · নিয়মের ভিতরের সুরক্ষা তুলে দিলে ❌ · `hiddenRows()`
সরালে ❌ · ওয়েবের একটা অনুবাদ সরালে ❌ — চারটেই ধরা পড়ে।

দুটো **পুরোনো** পাহারাদারের নিজেরই মাপার ভুল ধরা পড়ে ঠিক করা হয়েছে
(কোডের ভুল নয়): `verify_session_parity_2026-08-23.py` দু-লাইনে লেখা সারি
গুনতই না।

**মোট ২২টা পাহারাদার — সব পাশ**, `verify_kotlin_compile.py`-তে নতুন ভুল 0।
