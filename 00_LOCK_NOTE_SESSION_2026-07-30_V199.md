# 🔒 LOCK NOTE — V199 (৩০.০৭.২০২৬ সন্ধ্যা)

**ভার্সন:** `versionCode 199` · `versionName 1.99` · পর্দায় **V199** · খাতার সারি **B191**
⛔ **কোনো SQL লাগবে না।**

---

## ⛔ সবার আগে — স্থায়ী নিয়ম
> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

---

## TK-এর অভিযোগ (হুবহু)
*"এগুলো ডিলিট কেন করতে পারছিনা, আমি তো মাস্টার এডমিন।"* — Patient Timeline-এ একটা টেস্ট রোগীর (DEMO TEST, +917777777777, KNE-26072026-001) "Delete Patient?" পপ-আপে Delete চাপলে "Record not found — it may already be deleted" আসছিল, অথচ এই একই রোগী Draft-এর "Incomplete Patient" তালিকায় স্পষ্ট দেখা যাচ্ছিল।

## ✅ আসল কারণ (কোড ধরে)
এই একই ফাইলে (`PatientTimelineActivity.kt`) ঠিক উপরে থাকা `confirmDeleteEnquiry()` ফাংশনে আগে থেকেই একটা সঠিক নিয়ম আছে:
> আইডি দিয়ে খুঁজে না পেলে → **মোবাইল দিয়ে আরেকবার খোঁজা** → তবেই "Record not found"।

কিন্তু Patient Timeline-এর আসল Delete বোতাম চালায় `confirmDeletePatient()` — এখানে এই মোবাইল-ফলব্যাক **শুধু তখনই চলত যখন আইডি একেবারে ফাঁকা থাকত**। আইডি একটা থাকলে (even সেটা পুরনো বা আর না-মেলা হলেও — যেমন অফলাইনে সেভ হওয়া রেকর্ড পরে ক্লাউডে ভিন্ন আইডি নিয়ে যাওয়া), কোড **আর মোবাইল দিয়ে খোঁজার চেষ্টাই করত না** — সঙ্গে সঙ্গে "NOT_FOUND" বলে দিত।

## ✅ সমাধান
`confirmDeletePatient()`-এ এখন `confirmDeleteEnquiry()`-এর হুবহু একই প্যাটার্ন:
```
আইডি দিয়ে খোঁজা → না পেলে → মোবাইল দিয়ে খোঁজা → তাও না পেলে → "Record not found"
```

⛔ **যা অপরিবর্তিত:**
- অনুমতির যাচাই (শুধু Master Admin ডিলিট করতে পারবেন)
- Trash Bin-এ সরানো, Restore-এর সুবিধা
- Follow-up/Payment history অক্ষত থাকা
- Registration Cancel-এর আলাদা টাইটেল/লেখা

## 🔍 একই ধরনের বাগ প্রজেক্টের অন্য কোথাও আছে কিনা (TK-এর স্থায়ী নিয়ম)
- **DoctorVisitActivity** (ডাক্তার ডিলিট) — আইডি সবসময় তাজা তালিকা থেকে আসে (cache করা পুরনো আইডি নয়), তাই এই ঝুঁকি নেই। ছোঁয়া হয়নি।
- **DraftListActivity → DraftRepository.deleteEnquiry()** — আইডি একইভাবে তাজা তালিকা থেকে আসে। ছোঁয়া হয়নি।
- **PatientTimelineActivity.resolveFollowUpIdHere()** (রিমার্ক/স্ট্যাটাস বদল) — এখানে ইতিমধ্যেই সঠিক ফলব্যাক আছে (খাতার সারি B96)। ছোঁয়া হয়নি।

শুধু `confirmDeletePatient()` — যেটা `currentPatientRowId` নামের একটা **আগে-থেকে-লোড-করা (সম্ভবত পুরনো হয়ে যেতে পারে এমন)** ভ্যারিয়েবলের উপর নির্ভর করত — তাতেই বাগটা ছিল।

## 🔍 যাচাই
- পাহারাদার (`tk_guard.py`) **১৭/১৭ পাশ**
- ভার্সন দুই জায়গায় এক — `build.gradle.kts` (199/1.99) ও `DashboardActivity.kt` (V199)
- নতুন কোড হাতে মিলিয়ে `confirmDeleteEnquiry()`-এর সঙ্গে লাইন-বাই-লাইন তুলনা করা হয়েছে

## 🔴 এখনো বাকি
- **TK-এর লাইভ টেস্ট** — এই একই DEMO TEST রোগী দিয়ে আবার Delete চেপে দেখা
- **B190 — ঝুঁকিসহ লোডিং-ফিক্স** — পরের সেশনে আলোচনা করে ফাইনাল হবে
- **B148 — RLS** ⛔ TK-এর অনুমতি ছাড়া নিষেধ
- `03_NETLIFY_READY` Netlify-তে আপলোড (TK-এর কাজ)
