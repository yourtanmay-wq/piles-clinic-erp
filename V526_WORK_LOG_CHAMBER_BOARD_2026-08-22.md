# V526 — Chamber বোর্ডে এক নম্বরে দুজন হলে দুটো সারি
**তারিখ:** ২২.০৮.২০২৬ · **ভিত্তি:** V525 · **TK-এর নির্দেশ:** *"হ্যাঁ Chamber ধরুন"*

এই সিরিজের **শেষ বাকি কাজ** — "এক মোবাইলে একাধিক রোগী" প্রোগ্রামটা এখন সম্পূর্ণ।

---

## সমস্যা যেটা ছিল (কোডে প্রমাণিত)

`ChamberAttendanceRepository.loadBoard()` সারি বানাত **শুধু মোবাইল নম্বর ধরে**
(`byMobile`, ১৯ জায়গায়)। V516-এর পরে এক নম্বরে স্বামী ও স্ত্রী **দুজন আলাদা
রোগী** থাকতে পারেন — তখন দুজনের **নাম · Patient ID · টাকা · রিমার্ক ·
"আসার কথা" চিহ্ন** সব **একটাই সারিতে মিশে** যেত।

---

## সমাধান — "পরিচয়ের চাবি" (V518/V520-এর হুবহু একই প্রমাণিত নিয়ম)

স্টাফ নিজে *"Different Patient — Same Mobile"* চাপলে তবেই রোগীর সারির আইডি হয়
`pat_<১০ সংখ্যা>_<লেজ>` ধাঁচের — **অন্য কোনো পথে এই ধাঁচ তৈরি হয় না**।

- এই ধাঁচের রোগী ⇒ চাবি হবে **তাঁর নিজের আইডি** (আলাদা সারি)
- বাকি সবাই ⇒ চাবি **আগের মতোই মোবাইল** (এক অক্ষরও বদলায়নি)

### প্রতিটা উৎস কীভাবে নিজের রোগী চেনে (সবই কোডে প্রমাণিত)

| উৎস | পরিচয় আসে কোথা থেকে |
|---|---|
| `patients` | সারির নিজের `id` |
| `payments` | `payments.patientId` = রোগীর সারির আইডি (V520-এ প্রমাণিত) |
| `followups` (রিমার্ক · followUpId) | `followups.refId` = রোগীর সারির আইডি (V518-এ প্রমাণিত) |
| `enquiries` | রোগীর আইডিই নেই ⇒ **আগের মতোই মোবাইল** (এনকোয়ারি তো রোগী নন) |

### সঙ্গে যা যা ঠিক হলো
- **`chamberChosenByMobile`** — "ভুলে দুবার রেজিস্ট্রেশন হলে কোনটা আসল"
  (V143-এর নিয়ম) — সেই দলে ঘোষিত আলাদা রোগীকে আর ঢোকানো হয় না, নইলে
  তাঁর কার্ডে **অন্যজনের Patient ID** বসে যেতে পারত।
- **আসল মোবাইলটা আলাদা ঘরে** (`mobile10`) — আগে **চাবিটাই** মোবাইল হিসেবে
  বসত; চাবি বদলানোয় সেটা আর চলত না।
- **`ChamberAttendanceRow.patientRowId`** (নতুন, ডিফল্ট ফাঁকা) — বোর্ড থেকে
  **Payment · History · Report Card** এখন ঠিক এই রোগীতেই যায়, আন্দাজে নয়।
  জমানো কপিতেও (cache) রাখা হয়; পুরোনো cache-এ না থাকলে ফাঁকা ⇒ আগের আচরণ।

---

## ⛔ যেখানে কিচ্ছু বদলায়নি (পরীক্ষায় প্রমাণিত)

- **যে নম্বরে ঘোষিত আলাদা রোগী নেই, সেখানে প্রতিটা চাবি হুবহু মোবাইলই** —
  অর্থাৎ রোজকার ৯৯% ক্ষেত্রে বোর্ড **অবিকল আগের মতোই**।
- খাতার সারি **B30** (ভুলে দুবার রেজিস্ট্রেশন) — আগের মতোই **এক সারি**, টাকাও
  আগের মতোই একসাথে।
- এনকোয়ারি + রোগী একই নম্বরে — আগের মতোই এক সারি।
- টাকার হিসাব · Refund-এর নিয়ম · ঘোস্ট-সারি বাদ দেওয়া · সাজানোর ক্রম — অক্ষত।
- **Supabase-এ একটাও বাড়তি query নেই** — চিহ্নটা `patients` তালিকা থেকেই
  বের করা হয়, যেটা বোর্ড এমনিতেই আনে।
- Database schema · RLS · production data — **ছোঁয়া হয়নি**।

---

## যাচাই — এবার সবচেয়ে শক্ত প্রমাণ

**আসল `ChamberAttendanceRepository.kt` কম্পাইল করা হয়েছে (শূন্য error) এবং
আসল `loadBoard()` চালিয়ে বোর্ড বানানো হয়েছে** — নকল কপির উপরে নয়।
(এই ফাইলে Android-এর ভারী কিছু নেই, তাই চারপাশটা নকল করে আসল কোডটাই চালানো গেছে।)

| যাচাই | ফল |
|---|---|
| **V526 Chamber বোর্ড (আসল `loadBoard()` চালিয়ে)** | **২৭/২৭ PASS** |
| V525 ট্যাগ · V524 ঘোরানো · V523 Reports · V522 Report Card | ALL PASSED |
| V521 · V520 · V518 Follow-up · V516 · V517 | ALL PASSED |
| ওয়েব: webfu · webfam · websearch · dq · pay · why · mask · rc | ALL PASSED |
| ৮টি ওয়েব ফাইল `node --check` | ৮/৮ OK |

**পরীক্ষায় যা যা মিলিয়ে দেখা হয়েছে:** দুই সারি · দুজনের আলাদা Patient ID ·
টাকা কখনো না মেশা (দুই দিক থেকেই) · মোবাইল ঠিক থাকা · বোর্ডের মোট যোগফল ·
একজন রোগীর ক্ষেত্রে হুবহু আগের ফল · B30-এর মিশে যাওয়া অক্ষত · এনকোয়ারি ·
"আসার কথা" চিহ্ন ঠিক রোগীর সারিতে · রিমার্ক ঠিক রোগীর সারিতে · অন্য নম্বর আলাদা।

## সৎ সীমাবদ্ধতা
- `ChamberAttendanceActivity` (পর্দা) কম্পাইল করা যায়নি — Android SDK এই
  মেশিন থেকে পৌঁছয় না। তবে এবারের **আসল কাজটা Repository-তে, আর সেটা
  কম্পাইল করে চালিয়ে দেখা হয়েছে**। পর্দায় শুধু আইডি পাঠানোর লাইন যোগ
  হয়েছে (হাতে যাচাই, brace `+0`)।
- **TK-কে বিল্ড করে একবার দেখে নিতে হবে।**
