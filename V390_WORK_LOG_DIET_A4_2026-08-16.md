# V390 — DIET & LIFESTYLE CHART (A4), অ্যান্ড্রয়েড ও ওয়েব দুই জায়গাতেই

তারিখ: ১৬.০৮.২০২৬ · আদেশ: **"অ্যান্ড্রয়েড ও ওয়েব দুই জায়গাতেই বসান"**

## ১. বদলানো ফাইল — মোট ৪টি

| ফাইল | কী হলো |
|---|---|
| `03_NETLIFY_READY/app.js` | নতুন `wlv1DietA4Css()` · `wlv1DietA4Row()` · `wlv1DietA4Html()`; `printDiet()` এখন এগুলোই ডাকে |
| `03_NETLIFY_READY/index.html` | ক্যাশ-নম্বর `?v=v417` |
| `.../print/DietChartHtmlPrint.kt` | **নতুন ফাইল** — ওয়েবের হুবহু একই HTML+CSS, WebView + PrintManager (A4) |
| `.../clinical/DietChartActivity.kt` | **এক লাইন** — Save & Print এখন উপরের ফাইলটা ডাকে |

⛔ **`ClinicPdfBuilder.kt` (OWNER LOCKED) ছোঁয়া হয়নি** — যন্ত্রে গোনা, ০ বদল।
তাই Prescription · Medicine Slip · Registration · Blood Test — সব আগের পথেই।
⛔ **SQL-এ ০ ফাইল বদল।** নতুন SQL লাগবে না।
⛔ কোন আইটেম বাছা হয়, কী সেভ হয়, ক্লাউডে কী যায় — **এক অক্ষরও বদলায়নি**।

## ২. আগের ছাপায় যে তিনটে আসল ভুল ছিল — তিনটেই সারানো

1. **"Avoid" লেখাতেও ☑ টিক বসত** → রোগী উল্টো বুঝতে পারতেন।
   এখন **✓ EAT (সবুজ)** ও **✗ AVOID (লাল)** সম্পূর্ণ আলাদা দুই কলামে —
   ঠিক ফোনের `PrintMappers.dietChart()`-এর নিয়মেই (kt:168-172)।
2. **তিনটে ভাষা এক লাইনে জোড়া লেগে যেত** (`\n` HTML-এ ফাঁকা হয়ে যায়) → এখন
   প্রতিটা ভাষা নিজের লাইনে।
3. **ট্যাগলাইন বাক্স থাকত না** → এখন আছে (ফোনে সব প্রিন্টেই থাকে)।

## ৩. ডিজাইন বানাতে গিয়ে যে তিনটে বাগ নিজে ধরেছি ও সারিয়েছি

1. `.sheet>*{position:relative}` জলছাপকেও ধরছিল → জলছাপ লেখার জায়গা দখল করে
   নিচের কলাম শূন্য করে দিচ্ছিল। → `:not(.wm)`
2. `.en/.bn/.hi` inline ছিল → লাইনের উচ্চতা ঠিক হচ্ছিল body-র ১৬px স্ট্রাট দিয়ে,
   এক-একটা সারি **১৬ মিমি** খাচ্ছিল। → `display:block`
3. `.tg div` ভিতরের সব div ধরছিল → প্রতিটা লক্ষ্য-ঘরে তিনটে বাক্স আঁকা হচ্ছিল। → `.tg>div`

## ৪. দৈনিক লক্ষ্যের সংখ্যা — আন্দাজে নয়, প্রকাশিত নির্দেশিকা থেকে

* আঁশ **২৫–৩০ গ্রাম/দিন**, গরম সিটজ বাথ **~১০ মিনিট দিনে কয়েকবার** — UCSF Health
* জল **৬–৮ গ্লাস/দিন**, পায়ের নিচে টুল (হাঁটু কোমরের উপরে) — Cambridge University Hospitals NHS
* আঁশ **ধীরে ধীরে** বাড়ানো, সঙ্গে বেশি জল — NIDDK, NIH

কাগজেই সূত্র ছাপা আছে, সঙ্গে **"ডাক্তারের নির্দেশের বিকল্প নয়"** কথাটাও।

## ৫. যন্ত্রে যাচাই

* কাগজ **210 × 297 মিমি = A4** — দুই ব্রাঞ্চেই মেপে দেখা
* **EAT ৮টা · AVOID ৬টা — একটাও কাটা যায়নি**
* সবচেয়ে নিচের লেখা শেষ: জলপাইগুড়ি **292.5mm**, কিশানগঞ্জ **288.5mm** — দুটোই ২৯৭-এর মধ্যে ✅
* **ব্রাঞ্চ-নিয়ম ঠিক আছে:** কিশানগঞ্জ → `TK BISWAS PILES CLINIC`, **"BRANCH" লাইন বসে না**;
  জলপাইগুড়ি → `MAA AYURVED PILES CLINIC` + `JALPAIGURI BRANCH`
* **দুই দিকের CSS মিলিয়ে দেখা:** ওয়েবে ৫২টি নিয়ম, অ্যান্ড্রয়েডে ৫২টি —
  **কোনো পার্থক্য নেই** (স্ক্রিপ্টে তুলনা করা)
* বাকি ৪৩টি পর্দার পিক্সেল-মিলানে **নতুন কোনো বদল আসেনি**
* `node --check app.js` পাশ

## ৬. আপনার কাজ

1. Netlify-তে ফাইল তুলুন (`?v=v417`)।
2. **অ্যান্ড্রয়েডে নতুন APK বিল্ড করতে হবে** — দুটো Kotlin ফাইল বদলেছে।
3. দুই জায়গা থেকেই একবার Diet Chart ছেপে মিলিয়ে দেখুন।

---

# V391 (১৬.০৮.২০২৬) — নিচের সই-সারি, TK-র নিয়ম অনুযায়ী

TK-নির্দেশ: **বাঁয়ে `TK BISWAS / Founder & Consultant`, মাঝে বারকোড,
ডানে `Dr. K.H. MANDAL / (B.A.M.S) Regd 12386`।**

আগে Diet Chart-এ সাধারণ "Patient / Guardian Signature" ও "Doctor's Signature"
লেখা ছিল। কারণ অ্যাপের নিজের নিয়মে (`printDoctorsHtml`) ঐ দুজনের নাম **শুধু
PRESCRIPTION-এ** বসত, Diet Chart-এ নয়। TK-র নির্দেশে এখন Diet Chart-এও ঐ দুজনই।

* বদলানো ফাইল: `app.js` · `print/DietChartHtmlPrint.kt` · `index.html` (`?v=v418`)
* দুই দিকের সই-সারি ও CSS **স্ক্রিপ্টে মিলিয়ে দেখা — হুবহু এক**
* এক পাতায় ধরছে: জলপাইগুড়ি **292.5mm**, কিশানগঞ্জ **288.5mm** (A4 = 297)

⚠️ **জানিয়ে রাখছি (নিজে থেকে বদলাইনি):** Prescription প্রিন্টে এখন লেখা আছে
`Founder / Consultant` এবং `B.A.M.S.` + `Regd. No. 12386` (দু'লাইনে) —
আপনার দেওয়া লেখার সাথে সামান্য অমিল। **বললে Prescription-টাও একই করে দেব।**
