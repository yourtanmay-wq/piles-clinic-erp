# 📒 খাতা — সেশন ২০.০৮.২০২৬ (V486 → V492)

**তারিখ:** ২০ অগাস্ট ২০২৬
**শুরু:** সন্ধ্যা ~৮:০০ · **শেষ:** রাত ১১:০০ (ভারতীয় সময়)
**নির্দেশ:** TK BISWAS
**অবস্থা:** সব কাজ শেষ ও যাচাই করা। SQL তিনটে Supabase-এ চালানো হয়ে গেছে।
APK এখনো বিল্ড হয়নি — TK নিজে Android Studio-তে করবেন।

---

## 🕐 সময় ধরে ধরে কী কী হলো

| সময় | কাজ | ফল |
|---|---|---|
| ~৮:০৫ PM | APK ইনস্টল হচ্ছিল না — "package conflicts" | কারণ ধরা, V486-এ ঠিক |
| ~৮:৩৮ PM | Staff তালিকায় Back করলে উপরে চলে যাচ্ছিল | V486-এ ঠিক |
| ~৮:৪৫ PM | টাকা ফেরতের রঙ লাল হচ্ছিল না | V487-এ ঠিক + **Cash-এর ঘরে ভুল টাকা ধরা পড়ল** |
| ~৮:৪৭ PM | প্রেসক্রিপশন পর্দার ৬টা বদল | V488-এ শেষ |
| ~৯:২৪ PM | RMP কমিশন সব ₹0 দেখাচ্ছিল | V488 SQL (১০% ডিফল্ট) |
| ~৯:৪৪ PM | V488 SQL Supabase-এ **Run হয়েছে** | ✅ Success |
| ~৯:৫৫ PM | V489 SQL Supabase-এ **Run হয়েছে** | ✅ Success |
| ~১০:১৮ PM | V490 SQL Supabase-এ **Run হয়েছে** | ✅ Success |
| ~১০:২১ PM | সব কাজের চূড়ান্ত যাচাই | ✅ কোনো গোলমাল নেই |

---

# 🔴 V486 — চাবির সমস্যা (APK ইনস্টল হচ্ছিল না)

**TK-এর রিপোর্ট (~৮:০২ PM, ছবিসহ):**
> *"App not installed as package conflicts with an existing package. Clean করে আবার Build করে APK বানানো হলো — তবুও কাজ হচ্ছে না।"*

### আসল কারণ (যাচাই করা, আন্দাজ নয়)

`app/permanent-debug-key/piles_clinic_permanent_debug.jks` ফাইলটার তৈরির তারিখ পরীক্ষা করে দেখা গেল:

```
Creation date: Aug 20, 2026 12:54:47 UTC
SHA1: 72:C5:47:F5:EE:AA:A2:95:22:7C:6E:FE:2E:59:E3:3F:F9:F0:39:D7
```

অর্থাৎ V477-এ যে "স্থায়ী চাবি" বানানো হয়েছিল, সেটা **সেদিনই প্রথম তৈরি**।
ফোনে বসে থাকা অ্যাপ পুরনো `~/.android/debug.keystore` দিয়ে সই করা — দুটো সই আলাদা।
Android কখনোই আলাদা সইয়ের অ্যাপ উপরে বসাতে দেয় না।

⇒ **Clean/Rebuild-এ কখনোই কাজ হত না** — সমস্যা কোডে নয়, ফোনে বসে থাকা পুরনো অ্যাপে।

### সমাধান

`app/build.gradle.kts` — debug signing এখন তিন ধাপে চাবি খোঁজে:

1. `app/permanent-debug-key/debug.keystore` (প্রজেক্টে কপি করা পুরনো চাবি)
2. `~/.android/debug.keystore` (কম্পিউটারের নিজের পুরনো চাবি) ← **সাধারণত এটাই**
3. V477-এর নতুন `.jks` (শেষ উপায়)

বিল্ডের সময় Gradle ছাপে: `🔑 Debug signing key in use: ...`

⛔ Release বিল্ড এক অক্ষরও বদলায়নি।

---

# 🔴 V486 — Staff তালিকায় Back করলে উপরে চলে যাওয়া

**TK-এর রিপোর্ট (~৮:৩৮ PM, ছবিসহ):**
> *"যে স্টাফের পারফরমেন্স ওপেন করলাম, ব্যাক করলে আবার সেখানেই আসার কথা — কিন্তু পুরো উপরে চলে যাচ্ছে।"*

### আসল কারণ

`StaffProfileActivity` আলাদা আলাদা Activity নয় — একই Activity বারবার নতুন করে আঁকা হয়
(`renderList()` / `performanceOne()` …)। Back মানে `renderList()` আবার ডাকা, অর্থাৎ
`ModuleUi.screen()` একটা **সম্পূর্ণ নতুন ScrollView** বানায় — নতুন ScrollView সবসময় ০ থেকে শুরু।

### সমাধান — `StaffProfileActivity.kt`

- নতুন: `listScrollY`, `listScroll`, `trackListScroll`, `restoreListScroll()`
- `renderList()`-এ ScrollView-তে scroll-listener বসিয়ে অবস্থান মনে রাখা
- আঁকা শেষে ও ক্লাউড-রিফ্রেশের পরে সেই অবস্থানে ফেরানো
- আঁকার সময় উচ্চতা ক্ষণিকের জন্য ০ হয় বলে তখন মাপা বন্ধ (`trackListScroll=false`) — নইলে ভুল ০ মনে থেকে যেত

⛔ কোনো হিসাব · ডেটা · ডিজাইন · বোতাম বদলায়নি।

---

# 🔴 V487 — টাকা ফেরতের রঙ ও **Cash-এর ঘরের ভুল হিসাব**

**TK-এর রিপোর্ট (~৮:৪৫ PM, ছবিসহ):**
> *"আজকে যখন টাকা ফেরত দেয়া হয়েছে এবং মাইনাস, তাহলে কালারটা লাল কেন হল না?"*

### ক) রঙের সমস্যা

`CollectionAdapter.kt` — রঙ ঠিক হত **শুধু মোড দেখে** (ক্যাশ ⇒ সবুজ, অনলাইন ⇒ নীল)।
টাকা জমা না ফেরত সেটা দেখাই হত না, তাই ₹-14,000 আর ₹14,000 একই সবুজ।

**এখন:** `item.amount < 0` হলে পুরো বাক্স লাল (`#FDECEC / #F3C2C2 / #B3261E`)।

### খ) 🚨 আরও বড় ভুল — Cash-এর ঘরে ভুল টাকা

TK-এর ছবি থেকেই ধরা পড়ল:

| ঘর | দেখাচ্ছিল | হওয়ার কথা |
|---|---|---|
| মোট | ₹-23,600 | ✅ ঠিক |
| **Cash** | **₹24,400** | **₹-23,600** ❌ |

ওই দিনের সারি: −14,000 · −10,000 · +400 → ক্যাশও −23,600 হওয়ার কথা।

**আসল কারণ:** `PaymentRepository`-তে refund সারিতে শুধু
`copy(amount = -abs(amount))` করা হত — `cashAmount` / `onlineAmount`
**যোগ (+) থেকেই যেত**। মোট হিসাব `amount` ধরে, Cash-এর ঘর `cashAmount` ধরে —
তাই ফেরত-দেওয়া টাকা Cash-এর ঘরে উল্টে **যোগ** হয়ে যাচ্ছিল।

**সমাধান:** `PaymentModel.parseApprovedRefundRow()` — তিনটে ঘরই একসাথে বিয়োগ।
`PaymentRepository`-র ৩টে জায়গা (দৈনিক · ফোনে-জমা · মাসিক) এখন এটাই ডাকে।

**সাথে:** যোগফল মাইনাস হলে Total/Cash/Online সংখ্যাও লাল — Today's Collection ও Monthly দুটোতেই।

⛔ ক্লাউডের কোনো সারি বদলানো হয়নি — শুধু ভুল যোগফল ঠিক করে দেখানো।

---

# 🔵 V488 — প্রেসক্রিপশন পর্দার ৬টা বদল

**TK-এর নির্দেশ (~৮:৪৭ PM, ৩টে ছবিসহ)** — এবং TK-এর ৩টে প্রশ্নের উত্তর:
১) Sitz Bath তুললে কাগজ থেকেও উঠবে — **হ্যাঁ**
২) "0 selected" — **খ** (তুলে দিয়ে Save-এর ভিতরে সংখ্যা)
৩) রোগের নাম — **খ** (উপরে "Piles" একটু বোল্ড)

| # | কাজ | ফাইল |
|---|---|---|
| ১ | Sitz Bath টিক দেওয়া-তোলা যায়; তুললে ছাপাতেও থাকে না | `MedicinePickerDialog` · `PrescriptionOptionsStore` · `PrintDocumentModel` · `PrintMappers` · `PrescriptionHtmlPrint` · `rx_print.html` · `ClinicPdfBuilder` |
| ২ | Common · Search · Add — উচ্চতা **52 → 36dp**, লেখা এক লাইনে | `MedicinePickerDialog` |
| ৩ | "🔍 Search" → **"🔍 মেডিসিন"** | `MedicinePickerDialog` · `NoBengali` |
| ৪ | নিচে **WhatsApp · Cancel · Save** পাশাপাশি, উচ্চতা কম | `MedicinePickerDialog` |
| ৫ | WhatsApp-এ **PDF শেয়ার** | `PrescriptionWhatsAppShare.kt` (নতুন) · `PrescriptionActivity` |
| ৬ | উপরে রোগের নাম বোল্ড + সাদা | `MedicinePickerDialog` |

### ⚠️ ৫ নম্বরে যে আসল বাধা ছিল (গুরুত্বপূর্ণ)

`PrintPreviewActivity.kt` লাইন ৮৬–৯০ — Prescription ও Medicine Slip-এ
**Share PDF বোতাম ইচ্ছে করেই লুকানো**। কারণ V409 থেকে এই দুটো কাগজ
WebView → PrintManager পথে ছাপে (যাতে ফোন-কম্পিউটারের কাগজ এক থাকে) —
ওই পথে **কোনো PDF ফাইল তৈরিই হয় না**।

**সমাধান:** নতুন `PrescriptionWhatsAppShare` — **একই HTML** (`PrescriptionHtml.build`)
একটা WebView-তে বসিয়ে তার `PrintDocumentAdapter`-কে ছাপার বদলে
সরাসরি **ফাইলে লিখতে** বলা (`onLayout` → `onWrite`)। উৎস এক, তাই নকশাও হুবহু এক।
তারপর FileProvider দিয়ে WhatsApp / WhatsApp Business-এ (V449-এর একই বাছাই-নিয়ম)।

⛔ ছাপার পুরনো পথ এক অক্ষরও বদলায়নি।

### 🔴 এখনো পরীক্ষা বাকি
প্রথমবার ব্যবহারের সময় **TK মিলিয়ে দেখবেন** — WhatsApp-এ যাওয়া PDF আর
ছাপা কাগজ হুবহু এক কি না। (এটা নতুন কোড, এখানে বিল্ড করে দেখা যায়নি।)

---

# 🟢 V488 SQL — সব RMP-র জন্য স্বয়ংক্রিয় ১০% ডিফল্ট কমিশন

**ফাইল:** `V488_RMP_AUTO_10_PERCENT_DEFAULT_2026-08-20.sql`
**Supabase-এ Run:** ✅ ~৯:৪৪ PM — Success

**TK-এর নির্দেশ (~৯:৩৫ PM):**
> *"সমস্ত ব্রাঞ্চের সমস্ত RMP-দের automatic 10% Default কমিশন করে রাখুন। কিন্তু আগে যাদের সেট করে রেখেছিলাম সেগুলোই থাকবে।"*

### যেভাবে করা হয়নি
৯১৪ জন RMP-র প্রত্যেকের জন্য সারি লেখা — হাজার হাজার নতুন সারি, Free-plan-এ চাপ,
আর নতুন RMP এলে আবার একই কাজ।

### যেভাবে করা হলো
**নিয়ম বদলানো হলো, তথ্য নয়।** একটাও নতুন সারি লেখা হয়নি।

অগ্রাধিকারের ক্রম:
1. ব্রাঞ্চ-নির্দিষ্ট Default (V470) → থাকলে সেটাই
2. বৈশ্বিক Default (V325) → থাকলে সেটাই
3. **কোনোটাই নেই → ১০%** ← নতুন
   (মোড/মান হাতে দিলে — যেমন সরাসরি ₹৫০০ — উপরের কোনোটাই ছোঁয়া হয় না)

বদলানো: `fin.rmp_get_branch_default` (দেখানোর সময়) ও
`fin.rmp_set_patient_commission`-এ শুধু `raise exception 'RMP default is not set'`-এর
জায়গায় ১০%। হারটা একটাই জায়গায়: `fin.rmp_auto_default_percent()` — পরে বদলাতে হলে শুধু ওই সংখ্যাটা।

---

# 🟢 V489 SQL — পুরনো রোগীদের কমিশন নিজে থেকেই বসা

**ফাইল:** `V489_RMP_AUTO_BACKFILL_OLD_PATIENTS_2026-08-20.sql`
**Supabase-এ Run:** ✅ ~৯:৫৫ PM — Success

**TK-এর রিপোর্ট (~৯:২৪ PM, ছবিসহ):** Dr. JAFAR-এর রোগী ANIKUL — Bill ₹৩০,০০০,
Paid ₹৩০,০০০ — তবু সব ₹0।

### আসল কারণ
কমিশনের হিসাব `fin.rmp_patient_commissions` সারি ধরে হয়। ওই সারি তৈরি হয়
**পেমেন্ট সেভ হওয়ার পরে** (V325, ১২.০৮.২০২৬ থেকে)। ANIKUL-এর টাকা জমা
**১২.০৭.২০২৬** — ব্যবস্থাটা চালু হওয়ার এক মাস আগে। তাই সারিই নেই,
আর `fin.rmp_summary()` সারি না পেয়ে সব ০ ফেরত দেয় (V325 লাইন ২৫৭)।

⇒ হিসাবে ভুল ছিল না — শুধু পুরনো রোগীদের সারিটাই ছিল না।

### সমাধান
নতুন `fin.rmp_backfill_commissions(rmp_id)` — RMP-র পর্দা খুললেই
(`fin.rmp_rmp_summary`-র শুরুতে) অনুপস্থিত সারিগুলো তৈরি করে দেয়।

- **সার্ভারে** বসানো ⇒ ফোন ও ওয়েব দুটোতেই চলে, অ্যাপ আপডেট ছাড়াই
- **প্রতি RMP-র জন্য জীবনে একবারই** (`fin.rmp_commission_backfill_done` টেবিলে দাগ)
- **শুধু যাঁরা টাকা দিয়েছেন** তাঁদের সারি হয় — অকারণে টেবিল ভরে না
- রোগী-RMP মেলানোর নিয়ম **V328-এর প্রমাণিত নিয়মই** (নাম বা মোবাইল)
- একজনে সমস্যা হলে বাকিরা আটকায় না; পর্দাও ভাঙে না

---

# 🔔 V490 — নতুন Enquiry · Registration · Advance-এ নোটিফিকেশন

**ফাইল:** `V490_AUTO_NOTICE_ENQUIRY_REGISTRATION_ADVANCE_2026-08-20.sql`
**Supabase-এ Run:** ✅ ~১০:১৮ PM — Success

**TK-এর রিপোর্ট (~৯:৪০ PM, ছবিসহ):**
> *"ফালাকাটা ব্রাঞ্চ থেকে একটা এনকোয়ারি কিশনগঞ্জের জন্য করেছে। ব্রাঞ্চ থেকে ফোন করলো তাই দেখতে পেলাম — না করলে হারিয়ে যেত।"*

**TK-এর উত্তর:** যিনি লিখলেন তাঁর কাছে নোটিফিকেশন **যাবে না** ·
নোটিশ থাকবে **যতক্ষণ সিন না করবে, সর্বোচ্চ ১ সপ্তাহ**

### ঝুঁকিগুলো TK-কে আগে জানানো হয়েছিল
১. Supabase কোটা (প্রজেক্ট এখনই কোটা-ছাড়া) · ২. বারবার অ্যালার্ম ·
৩. ওয়েবের কাজ বাদ পড়া · ৪. ডবল নোটিফিকেশন · ৫. ঘন্টার সংখ্যা বিশাল হওয়া
— পাঁচটারই ব্যবস্থা নিচে।

### ক) ডেটাবেসে (SQL) — নোটিশ তৈরি
৩টে trigger: `public.enquiries` · `public.patients` · `public.payments` (INSERT-এ)
→ `public.auto_notice_post()` → `public.briefings`-এ সারি।

- targets = `{"branches":["<ওই ব্রাঞ্চ>"], "roles":["master"]}`
- `hiddenFor` = যিনি লিখলেন তাঁর মোবাইল ⇒ তাঁর কাছে যায় না
- id স্থির (`brief_auto_enq_<id>`) + `ON CONFLICT DO NOTHING` ⇒ **ডবল নয়**
- শুধু **আজকের তারিখের** সারিতে ⇒ পুরনো ডেটা import-এ বন্যা নয়
- **Advance** = রোগীর **১ম** ট্রিটমেন্ট পেমেন্ট (TK-এর নিয়ম)। Registration Fee ·
  Visit Fee · Medicine · ২য়/৩য় পেমেন্ট — কোনোটাই নয়
- ৭ দিনের পুরনো **এই স্বয়ংক্রিয়** নোটিশ নিজে থেকেই মুছে যায় (কোটা বাঁচাতে)
- নোটিশে ভুল হলেও **মূল কাজ (এনকোয়ারি/রেজিস্ট্রেশন/পেমেন্ট সেভ) কখনো আটকায় না**

### খ) অ্যাপে (Kotlin)
- `BriefingModel` — নতুন `isAutoNotice()` · `withinDays()` · `AUTO_NOTICE_DAYS = 7`
- `BriefingRepository.visibleForUser()` — এই তিনটে নোটিশ **৭ দিন** থাকে
  (সাধারণ নোটিশ আগের মতোই শুধু আজকেরটা)। রাত ১১টার এনকোয়ারি পরদিন সকালেও দেখা যাবে।
- `BriefingRepository.unseenCountForReminder()` — এই নোটিশগুলো **বাদ**, নইলে
  ১০ মিনিট পর পর অ্যালার্ম বাজত (V433-এর Staff IN/OUT TIME-এর হুবহু একই নিয়ম)।
  ⇒ **শব্দ একবারই** (BellNotifier, সংখ্যা বাড়লে)

### গ) ওয়েবে (`03_NETLIFY_READY/app.js`)
নতুন `isAutoNotice()` · `autoNoticeFresh()` · `briefingDateOk()` —
`activeBriefings()` ও `briefingVisibleForMaster()` এখন এই একই নিয়ম মানে।
⇒ ফোন ও কম্পিউটারে নোটিশ হুবহু একই আচরণ করে।

---

# ✅ যাচাই — কী কী পরীক্ষা করা হয়েছে

## ১) SQL — **আসল PostgreSQL 16.13** বসিয়ে চালিয়ে

তিনটে ফাইল একদম নতুন ডেটাবেসে পরপর চালানো হয়েছে — তিনটেই Success।

**কমিশনের হার (V488):**

| RMP-র সেটিং | ফল | |
|---|---|---|
| বৈশ্বিক ২০% | ২০% | ✅ |
| ব্রাঞ্চ ১৫% (বৈশ্বিক ২০%) | ১৫% | ✅ |
| কিছুই নেই | **১০%** | ✅ |
| সরাসরি ₹৫০০ | ₹৫০০ | ✅ |
| PERCENT ১৫০ (ভুল) | আটকে দিয়েছে | ✅ |

**পুরনো রোগী নিজে বসা (V489):**

| পরীক্ষা | ফল | |
|---|---|---|
| Dr. JAFAR-এর পর্দা খোলা → ANIKUL | ১০% → earned ₹৩,০০০ | ✅ |
| ২০% সেট করা RMP | ₹৬,০০০ (১০% হয়নি) | ✅ |
| আগে ৫% বসানো রোগী | ৫%-ই আছে | ✅ |
| টাকা দেননি এমন রোগী | সারি হয়নি | ✅ |
| দ্বিতীয়বার পর্দা খোলা | ডবল হয়নি | ✅ |

**নোটিফিকেশন (V490):**

| পরীক্ষা | ফল | |
|---|---|---|
| কিশনগঞ্জের এনকোয়ারি | কিশনগঞ্জ + মাস্টার | ✅ |
| যিনি লিখলেন | `hiddenFor`-এ, তাঁর কাছে যায়নি | ✅ |
| একই সারি দুবার | নোটিশ একটাই | ✅ |
| পুরনো তারিখের সারি (import) | নোটিশ হয়নি | ✅ |
| ২য় পেমেন্ট / Visit Fee | Advance ধরেনি | ✅ |

## ২) অ্যাপের কোড
১৬টা বদলানো ফাইলের গঠন (bracket balance) মূল ফাইলের সাথে মিলিয়ে দেখা — **কোনো গোলমাল নেই**।
নতুন ফাইল `PrescriptionWhatsAppShare.kt` আছে।
⚠️ Gradle বিল্ড এখানে করা যায়নি — TK Android Studio-তে করবেন।

## ৩) ওয়েবের কোড
`node --check app.js` ✅ · নতুন `briefingDateOk()` ৯টা অবস্থায় পরীক্ষা করা — সব ঠিক।

---

# 📌 TK-এর জন্য বাকি কাজ

1. **অ্যাপ বিল্ড** — `02_ANDROID_SOURCE_CODE/PilesClinicApp` খুলে Build।
   এবার অ্যাপ **মুছতে হবে না** (V486-এর চাবি ফিক্স)।
2. **ওয়েব ডিপ্লয়** — `03_NETLIFY_READY` Netlify-তে।
3. **পরীক্ষা:** WhatsApp-এ যাওয়া PDF ↔ ছাপা কাগজ মেলে কি না।
4. **🔐 চাবির ব্যাকআপ** — `piles_clinic_permanent_debug.jks` ও কম্পিউটারের
   `~/.android/debug.keystore` — দুটোরই ব্যাকআপ রাখতে হবে।

---

# ⚠️ আলাদা করে মনে রাখার সতর্কতা

**Supabase কোটা ছাড়িয়ে গেছে** — Supabase-এর লাল বার্তা:
> *"Organization exceeded its quota in the previous billing cycle.
> Projects will be restricted from 13 Aug, 2026 if your organization remains over quota."*

এই সেশনের কাজগুলো কোটার কথা মাথায় রেখেই করা হয়েছে
(back-fill প্রতি RMP-তে একবার · নোটিশ ৭ দিনে মুছে যায় · কোনো mass-write নেই)।
তবু **কোটার সমস্যাটা আলাদাভাবে দেখতে হবে** — নইলে প্রজেক্ট বন্ধ হয়ে যেতে পারে।

---


---

# 🔴🔴 V491 — বিল্ড ভেঙে যাওয়া ও তার সংশোধন

**TK-এর রিপোর্ট (২০.০৮.২০২৬ রাত ১০:৩১, ছবিসহ):** V490 বিল্ড করতে গিয়ে ব্যর্থ —

```
app:compileDebugKotlin  2 errors
PrescriptionWhatsAppShare.kt
  Cannot access '<init>': it is package-private in 'LayoutResultCallback'
  Cannot access '<init>': it is package-private in 'WriteResultCallback'
```

## আসল কারণ

V490-এ WhatsApp-এর PDF বানাতে WebView-এর নিজের `PrintDocumentAdapter`-কে
সরাসরি ফাইলে লিখতে বলা হয়েছিল (`adapter.onLayout` → `adapter.onWrite`)।
ওই দুটো ডাকের জন্য `PrintDocumentAdapter.LayoutResultCallback` ও
`WriteResultCallback`-এর object বানাতে হয় — কিন্তু ওদের constructor
**package-private**, অর্থাৎ `android.print` প্যাকেজের বাইরে থেকে বানানোই
যায় না। ⇒ ওই পথ Android-এ সোজাসুজি **সম্ভবই নয়**।

## 🔴 কেন পাহারাদার আটকাল না — সৎ স্বীকারোক্তি

**পাহারাদার Kotlin কম্পাইল করে না।** সে নিয়ম · নাম · ভার্সন · ডিজাইন-লক ·
বাংলা-বন্ধ — এসব মেলায়, কিন্তু কোড আসলে কম্পাইল হবে কিনা তা দেখে না।
তাই এই ভুলটা TK-এর কম্পিউটার পর্যন্ত পৌঁছে গিয়েছিল। এটা আগেই স্পষ্ট করে
বলা উচিত ছিল — বলা হয়নি, এটা ভুল হয়েছে।

## সংশোধন (V491)

`PrescriptionWhatsAppShare.kt` নতুন করে লেখা হলো — **শুধু স্বীকৃত, নিরাপদ API**:

একই HTML (`PrescriptionHtml.build`) একটা WebView-তে **A4-এর মাপে**
(৭৯৪ × ১১২৩ px — ব্রাউজার A4 ছাপার সময় ঠিক এই মাপই ধরে) বসিয়ে,
Android-এর নিজের `PdfDocument`-এর পাতায় (৫৯৫ × ৮৪২ পয়েন্ট) এঁকে দেওয়া হয়,
ঠিক সেই অনুপাতে ছোট করে। লেখা ভেক্টর হিসেবেই বসে, ঝাপসা হয় না।
বড় প্রেসক্রিপশনে একাধিক পাতা নিজে থেকেই হয়।

ব্যবহৃত সব API সর্বজনীন: `PdfDocument` · `PageInfo.Builder` · `View.draw(Canvas)` ·
`WebView.evaluateJavascript` · `FileProvider.getUriForFile` — কোথাও
package-private constructor নেই।

⛔ ঘুরপথে নিজের ক্লাস `android.print` প্যাকেজে রাখা **হয়নি** — ওটা কিছু
   ফোনে চলার সময় ভেঙে যেতে পারত; ক্লিনিকের অ্যাপে সেই ঝুঁকি নেওয়া হয়নি।

## 🆕 নতুন পাহারাদার — `00_GUARD/verify_forbidden_android_api.py`

যাতে একই ভুল আর কখনো TK-এর কম্পিউটার পর্যন্ত না পৌঁছায়:

- `PrintDocumentAdapter.LayoutResultCallback` / `WriteResultCallback`-এর
  object বানানো হলে **আটকে দেয়**
- নিজের কোনো ক্লাস `android.*` প্যাকেজে রাখা হলে **আটকে দেয়**

**প্রমাণ:** পুরনো (ভাঙা) কোডে চালিয়ে দেখা হয়েছে — সে ঠিক ওই লাইনটাই ধরেছে।
নতুন কোডে PASS (২৪১টা ফাইল যাচাই)।

⚠️ তবু মনে রাখতে হবে — **পাহারাদার আসল কম্পাইলারের বিকল্প নয়।**
   Android Studio-তে বিল্ড করাই একমাত্র চূড়ান্ত প্রমাণ।

*লেখা হলো ২০.০৮.২০২৬ · রাত ১১:০০ (ভারতীয় সময়)*


---

# 🌐💰 V492 — Supabase Egress কমানো (৯২% থেকে নামানো)

**TK-এর প্রমাণ (Supabase-এর ছবি, ২০.০৮.২০২৬ রাত ১০:৫১):**

| জিনিস | অবস্থা |
|---|---|
| Egress | **৪.৫৮৫ / ৫ GB — ৯২%** 🔴 |
| Database Size | ০.০৬৬ / ০.৫ GB — ১৩% ✅ |
| Monthly Active Users | ১৬ / ৫০,০০০ ✅ |
| রোজকার ডাউনলোড | ৩৮০ – ৭০০ MB |

**TK-এর কথা:** *"প্রতিবার নতুন করে সমস্ত তথ্য না নামিয়ে শুধু যেটুকু আপডেট বা
নতুন বা বদল হয়েছে সেটুকুই যদি হত, তাহলে ফ্রি প্ল্যানে কাজ হবে।"*

## আসল কারণ (কোড ধরে খুঁজে বের করা, আন্দাজ নয়)

পুরো ডেটাবেস মাত্র **৬৬ MB**, অথচ রোজ নামছে **৩৮০–৭০০ MB** — অর্থাৎ একই তথ্য
রোজ **৯–১০ বার**। তথ্য বেশি নয়, **বারবার নামাটাই** সমস্যা।

সবচেয়ে বড় অপরাধী দুটো, দুটোই `briefings` (ঘন্টার নোটিশ) টেবিল:

| কে | কত ঘন ঘন | দিনে কতবার (১৬ ফোন) |
|---|---|---|
| `BackgroundRefreshWorker` → `BellCounter` | প্রতি **১৫ মিনিটে** | ~১,৫০০ |
| `BriefingReminderWorker` | প্রতি **১০ মিনিটে** (অপঠিত থাকলে) | ~২,৩০০ |
| ওয়েব `refreshBriefingsFromCloud` | প্রতি **৫ মিনিটে** | প্রতি কম্পিউটারে ~২৯০ |

প্রতিবারই **পুরো টেবিল** নামত। ⇒ এটাই রোজকার ৩৮০–৭০০ MB-র প্রধান উৎস।

⚠️ **সৎ স্বীকারোক্তি:** V490-এর নতুন স্বয়ংক্রিয় নোটিশ এই টেবিলটাকে রোজ বাড়ায়,
তাই আমার কালকের কাজ সমস্যাটা **আরও বাড়িয়েছিল**। এই ফাইলে সেটাও শোধরানো হলো।

## যা করা হলো

**ফোনে — নতুন `BriefingCloudCache.kt`**
প্রথমবার একবার পুরোটা, তারপর থেকে শুধু `updatedAt` বদলেছে এমন সারি।

**ওয়েবে — `app.js` · `refreshBriefingsFromCloud()`**
হুবহু একই নিয়ম (`.gt('updatedAt', since)`)। merge-এর কোড আগে থেকেই ছিল, ছোঁয়া হয়নি।

## 🔒 নিরাপত্তা (প্রতিটা যাচাই করে দেখা)

১. **গোনার নিয়ম এক অক্ষরও বদলায়নি** — `visibleForUser` · `unseenCount` অটুট (খাতার সারি B60)।
২. **ঘরগুলো হুবহু একই** — যা আগে নামত, তাই নামে।
৩. **হার্ড-ডিলিট নেই** — কোড ধরে মিলিয়ে দেখা: ফোন ও ওয়েব কোথাও `briefings`
   হার্ড-ডিলিট করে না। Master মুছলে `deletedAt`, স্টাফ মুছলে `hiddenFor` —
   দুটোই *আপডেট*, তাই delta-তে ধরা পড়ে। একমাত্র হার্ড-ডিলিট V490-এর ৭ দিনের
   পরিষ্কার, আর ফোনের `prune()` ঠিক সেই একই ৭ দিনের নিয়মেই চলে।
৪. **প্রতি ৬ ঘণ্টায় পুরো মিলান** — বেমিল হলে নিজে থেকেই সেরে যায়।
৫. **Master-এর অনুমোদন কখনো হারায় না** — `needsMasterApproval` নোটিশ ছাঁটা হয় না।
৬. **নেট খারাপ হলে** — delta ব্যর্থ হলে জমানো তালিকাই ফেরে; কিছুই না থাকলে
   আগের পুরনো পথই চলে। ঘন্টা কখনো ভুল করে ০ দেখাবে না।
৭. **`ctx` না দিলে আচরণ হুবহু আগের মতোই** — পুরনো কোনো ডাক ভাঙার সুযোগ নেই।

## ধরা পড়া দুটো ফাঁদ (লেখার সময়ই শুধরানো)

- **খালি তালিকা ≠ জমানো নেই।** শান্ত সপ্তাহে ছাঁটার পর তালিকা খালি হতে পারে।
  `length() > 0` দেখলে ঠিক শান্ত দিনগুলোতেই পুরো পড়া ফিরে আসত — সাশ্রয় উবে যেত।
- **ভবিষ্যতের তারিখ।** কোনো ফোনের ঘড়ি এগিয়ে থাকলে তার নোটিশের তারিখ কালকের
  হতে পারে; সেটা যেন বাদ না পড়ে তাই আজ-বা-পরের তারিখও রাখা হয়।

## ⛔ ইচ্ছে করে যা ছোঁয়া হয়নি

- **নোটিশ পর্দার তালিকা** (`fetchRaw`) — মানুষ দিনে দু-একবার খোলে, Egress-এর
  সমস্যা নয়; হাত দিলে নোটিশের লেখা/উত্তর ফাঁকা দেখানোর ঝুঁকি ছিল।
- **`CloudReadCache`-এর ২০ সেকেন্ড** — TK-এর লক করা নিয়ম: এক পর্দায় লেখা টাকা
  অন্য পর্দায় সঙ্গে সঙ্গে দেখাতে হবে। সময় বাড়ালে সেটা ভাঙত। delta-তে
  তাজা-ভাব একটুও কমে না, শুধু বাইট কমে — তাই এটাই বেছে নেওয়া হলো।

## যা পরে করা যেতে পারে (এখন করা হয়নি — ঝুঁকি বনাম লাভ)

- Dr. Visit / RMP পর্দার ৪টে পুরো-টেবিল পড়া (মানুষ খুললে তবেই চলে, তাই অনেক কম)
- Follow-up পর্দার `enquiries` সব-ঘর পড়া

*লেখা হলো ২০.০৮.২০২৬ · রাত ১১:৪৫ (ভারতীয় সময়)*
