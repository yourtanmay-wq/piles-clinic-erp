# 🔒 LOCK NOTE — V188 (৩০.০৭.২০২৬ সন্ধ্যা) · খাতার সারি **B172**

> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

**ভার্সন:** `versionCode 188` · `versionName 1.88` · পর্দায় **V188**
🔴 **একটা ছোট SQL লাগবে — নিচে দেখুন।**

---

## TK কী বলেছেন

TK চেয়েছিলেন Enquiry/Visit/Patient — যেখান থেকেই স্টাফ কল করুক না কেন, কার্ডের ট্যাগ-সারিতে (Branch|Disease, আর প্রয়োজনে Unexpected Time/RMP) রোগীর ঠিকানার **একটা ছোট ট্যাগ** যোগ হবে — গ্রাম/পোস্ট/থানা/জেলা থেকে বেছে নেওয়া, স্টাফ যেকোনো সময় বদলাতে পারবে, বদলটা তিন কার্ডেই এক থাকবে।

তারপর ধাপে ধাপে ছবি-প্রুফ দেখানো হয়েছে (কার্ডের আসল ছবির উপর বসিয়ে, ৪ ট্যাগ একসাথে থাকলে কেমন দেখাবে) — TK শেষে **"ফাইনাল করুন"** বলেন। তারপর ব্র্যাঞ্চের শর্ট-কোড ব্যবহারের নির্দেশ ও নতুন SQL টেবিলে স্পষ্ট সম্মতি দেন — **"হ্যাঁ, তৈরি করে SQL দিন।"**

---

## যা তৈরি হলো

### ১) নতুন ছোট টেবিল — `address_tags`

মোবাইল ধরে একটাই সারি (id = মোবাইলের শেষ ১০ সংখ্যা, value = ট্যাগের লেখা)। ⛔ **patients · enquiries · followups — পুরনো কোনো টেবিলে হাত পড়েনি।** এই একটাই কারণে Enquiry/Visit/Patient — তিন কার্ডেই বদল এক থাকে (এক জায়গায় বদলালে সব জায়গায়)।

### ২) নতুন `AddressTagRepository.kt`

- রেজিস্ট্রেশনের combined address ("Vill: Chada, PO: Panchita, PS: Bongaon, Dist: North 24 Parganas") থেকে গ্রাম/পোস্ট/থানা/জেলা আলাদা করা।
- ডিফল্ট **থানা** (TK-এর সিদ্ধান্ত) auto বেছে নেওয়া — ⛔ **কোনো বাড়তি ক্লাউড-কল ছাড়াই** (এমনিতেই তালিকার সঙ্গে নামা address-এর একটা টুকরো পড়া মাত্র)।
- ⛔ স্টাফ কিছু না বসানো পর্যন্ত `address_tags`-এ **কিছুই লেখা হয় না** — তাই ফিচার চালু হওয়ার সঙ্গেই হাজার হাজার সারির জন্য হুট করে লেখার হুড়োহুড়ি (bulk-write) হয়নি, Supabase-এর কোটার উপর কোনো চাপ নেই।
- একাধিক মোবাইলের সেভ-করা ট্যাগ একসাথে আনা (একটাই ছোট ব্যাচ-অনুরোধ)।
- সেভ করলে `SupabaseClient.upsert()`-এর নিজের নিয়মেই ব্যর্থ হলে `CloudWriteQueue`-তে জমা থাকে ও পরে আবার চেষ্টা হয় — আলাদা কিছু বানাতে হয়নি।

### ৩) কার্ডের ট্যাগ-সারি নতুন করে লেখা (`FollowUpActivity.kt`)

Branch · Disease · ঠিকানা-ট্যাগ · RMP/Unexpected Time — **চারটেই এখন একই রং (Branch/Disease-এর নীল), একই ফন্ট মাপ।**

লেআউট হয়ে যাওয়ার পরে (তখনই আসল প্রস্থ জানা যায়) দরকার হলে:
1. **সবগুলো ট্যাগ একসাথে ছোট** হয় (আগে Branch/Disease ছিল fixed, এখন তারাও শেয়ার করে)।
2. তাও না কুলোলে ব্র্যাঞ্চের পুরো নামের বদলে **সংক্ষিপ্ত কোড** — KNE/JPE/COB/FLK/BIR (প্রজেক্টে **আগে থেকেই থাকা** `PatientIdGenerator.branchCode()`, নতুন কিছু বানানো হয়নি)।
3. অত্যন্ত বিরল ক্ষেত্রে (৪টে ট্যাগ, প্রতিটা লম্বা) তাও না কুলোলে শেষ পাহারা — প্রতিটার একটা সমান সর্বোচ্চ প্রস্থ, "…" দিয়ে কাটতে পারে কিন্তু কার্ডের বাইরে কখনো বেরোবে না বা একে অন্যের গায়ে ঘেঁষবে না।

⛔ Branch/Disease-এর **ট্রিপল-ট্যাপ এডিট আগের মতোই** পুরো ফর্ম খোলে — এতে হাত পড়েনি।

### ৪) ঠিকানা-ট্যাগের এডিট পপ-আপ

ঠিকানা-ট্যাগে **এক-ট্যাপ করলে** ছোট পপ-আপ খোলে (TK-এর নির্দেশ: *"শুধুমাত্র ওই ট্যাগ টুকু যেন আলাদাভাবে পরিবর্তন করা যায়"* — তাই পুরো Registration/Visit ফর্ম খোলে না)।

- গ্রাম/পোস্ট/থানা/জেলা — যেগুলো রেজিস্ট্রেশনের ঠিকানায় সত্যিই আছে, শুধু সেগুলোই বোতাম হিসেবে দেখা যায় (আন্দাজে বসানো হয় না)।
- নিজে লেখার ঘরও আছে (Enquiry-স্তরে, যেখানে এখনো গ্রাম/পোস্ট/থানা/জেলা আলাদা করে নেই, শুধু এই ঘরটাই দেখা যায়)।
- Save করলে `address_tags`-এ আপডেট হয়, স্ক্রিন সঙ্গে সঙ্গে রিফ্রেশ হয়।
- ⛔ নতুন পপ-আপে **বাধ্যতামূলক `PremiumAlert.paint()`** ব্যবহার হয়েছে (B158/B177-এর লক করা নিয়ম) — বাংলা-বন্ধ স্টাফের পর্দাতেও এটা নিরাপদ, যদিও এই পপ-আপের লেখা এমনিতেই সব ইংরেজি।

---

## ছবি-প্রুফের সময় নিজের দুটো ভুল ধরা পড়েছে ও তখনই সারানো হয়েছে

**(১)** প্রথম রাউন্ডে নতুন ছোট ট্যাগ বসানোর সময় আসল ছবির পুরনো পিক্সেল পুরোপুরি না মুছেই উপরে বসানো হয়েছিল, তাই পুরনো লেখা উঁকি দিচ্ছিল। **TK নিজে ধরিয়ে দেন** ("একটা একটার গায়ে ঘেঁষে গেছে") — সঙ্গে সঙ্গে সারানো হয়েছে (মুছে-তারপর-বসানো, পিক্সেল মিলিয়ে যাচাই করা হয়েছে)।

**(২)** কোড লেখার পরে পাহারাদার (`tk_guard.py`) একটা মৃত/অপ্রয়োজনীয় লাইন ধরে — edit পপ-আপে একটা ব্যাকগ্রাউন্ড-রিসোর্স বসিয়ে সঙ্গে সঙ্গেই আরেকটা দিয়ে চাপা দেওয়া হচ্ছিল (অকেজো লাইন)। ফাইল বানানোর আগেই সারানো হয়েছে।

---

## ⛔ যা ছোঁয়া হয়নি

- পুরনো **কোনো ডিজাইন বা কাজের নিয়ম** বদলায়নি।
- **টাকার হিসাব · ব্রাঞ্চের ছাঁকনি** কিছুই ছোঁয়া হয়নি।
- 📌 **ওয়েব অ্যাপে এই ফিচার এখনো নেই** — TK শুধু ফোনের কথা বলেছিলেন।

## যাচাই (কাজের পরে আবার, TK-এর নিয়ম)

- নিজের হাতে ব্র্যাকেট গোনা — **৪টা ফাইলেই পাশ**
- প্রতিটা নতুন লাইনের API/টাইপ হাতে মিলিয়ে দেখা (`Paint.measureText` · `TextView.maxWidth` · `View.post` · `data class .copy()` · `SupabaseClient.upsert/fetchListSlimOrNull`-এর প্যারামিটার ক্রম) — **পাশ**
- পাহারাদার `tk_guard.py` — **১৭টা যাচাই, সব পাশ**
- আগের অনুমোদিত কাজের যাচাই — **৫৯/৫৯ পাশ**
- ভার্সন চার জায়গায় এক — ZIP · `versionCode 188` · `versionName 1.88` · পর্দায় **V188**

**ফাইল:** নতুন `AddressTagRepository.kt` · নতুন `04_SUPABASE_DATABASE_SETUP/PATCH_2026-07-30_address_tags.sql` · `FollowUpModel.kt` · `FollowUpRepository.kt` · `FollowUpActivity.kt` · `build.gradle.kts` · `DashboardActivity.kt` · `app.js` (শুধু ভার্সনের লেখা)

---

## 🔴 TK-কে যা করতে হবে

১. **Supabase → SQL Editor → `PATCH_2026-07-30_address_tags.sql`-এর লেখাটা পেস্ট করে RUN।** "Success" দেখালেই শেষ। এটা না চালালে ঠিকানা-ট্যাগ ফিচারটা কাজ করবে না, কিন্তু বাকি সব আগের মতোই চলবে।
২. V188 বিল্ড করে লাইভ টেস্ট।

## 🔴 যা এখনো বাকি

📌 TK-এর ৯টা সন্দেহের **৯ নম্বর বাদ** — TK নিজে বলেছেন মনে নেই। **এই ৯টা সন্দেহের সবকটাই এখন শেষ।**

আগে থেকেই বাকি: **B148 — RLS** (⛔ TK-এর অনুমতি ছাড়া নিষেধ) · TK-এর লাইভ টেস্ট · `03_NETLIFY_READY` Netlify-তে আপলোড (V184-এর কাজ) · সপ্তাহখানেক পরে Supabase-এর খরচ দেখা।
