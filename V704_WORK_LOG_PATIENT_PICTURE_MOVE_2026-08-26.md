# V704 — "Patient Picture" ধাপ ৪ থেকে সরে ধাপ ৫-এ
**তারিখ:** ২৬.০৮.২০২৬ · **ভার্সন:** V704 / 7.04

## TK কী বলেছেন
> "এই ফটোটা অন্য একটা যায়গায় থাকবে — সম্ভাব্য রোগ এবং সময়ের মধ্যে থাকবে না"
> বাছাই: **ধাপ ৫ (Photo & Video)**
> "হ্যাঁ কাজ শুরু করুন, তবে খুব নিরাপদে এবং সাবধানে করবেন"

ডেমো প্রুফ `V704_DEMO_PATIENT_PICTURE_MOVE.png` — অনুমোদনের পরে কাজ শুরু।

## কী করা হয়েছে
| ফাইল | কী বদলাল |
|---|---|
| `res/layout/activity_doctor_checkup.xml` | Patient Picture ব্লক (৬১ লাইন) `secEstimate` থেকে কেটে `photoFoldBody`-র ভিতরে, তিনটে ছবির ঠিক নিচে |
| `clinical/DoctorCheckupActivity.kt` | `fitAnatomyHolderToImage()`-এর অপেক্ষা-শোনাটা এখন চওড়া সত্যিই মাপা না হওয়া পর্যন্ত চালু থাকে |
| `03_NETLIFY_READY/app.js` | `wlv1AnatBoxHtml(note,id)` ধাপ ৪ থেকে ধাপ ৫-এ |

## 🛡️ সাবধানতা — একটা ফাঁদ ধরা পড়েছে ও বন্ধ করা হয়েছে
ধাপ ৫-এর ঘরটা **শুরুতে বন্ধ ভাঁজের ভিতরে** (`photoFoldBody` = `gone`)।
`fitAnatomyHolderToImage()` ছবির উচ্চতা বসায় বাক্সের চওড়া মেপে; চওড়া তখনো
মাপা না থাকলে সে `OnGlobalLayoutListener` দিয়ে **একবার** অপেক্ষা করত এবং
প্রথম layout-এই শোনাটা খুলে নিত। বন্ধ ভাঁজে চওড়া বরাবরই ০ — তাই ওই প্রথম
ডাকেই শোনা খুলে যেত, আর পরে ভাঁজ খুললেও উচ্চতা কখনো বসত না ⇒ **ছবি বিকৃত
দেখাত**। ⇒ এখন চওড়া > ০ না হওয়া পর্যন্ত শোনা চালু থাকে।
⛔ মাপার হিসাব (`apply`) এক অক্ষরও বদলায়নি।

## যা বদলায়নি
`anatomyStrip` · `anatomyHolder` · `anatomyTools` — তিনটে id একই; ব্লকের
একটাও লাইন লেখা হয়নি, হুবহু কেটে বসানো। আঁকার হাতিয়ার · Restore · কেন্দ্র ·
সেভ-লজিক · A4-রিপোর্ট — সব অক্ষত। ধাপ ১·২·৩ ছোঁয়া হয়নি।

## যাচাই
| যাচাই | ফল |
|---|---|
| XML গঠন (`xml.dom.minidom`) | ✅ valid |
| ব্লকের ৩টে id সরানোর আগে-পরে আছে | ✅ assert-এ যাচাই |
| `verify_kotlin_compile.py` | ✅ **নতুন ভুল ০** |
| `app.js` JS syntax | ✅ OK |
| `tk_guard.py` (২২টা মেশিন-যাচাই) | ✅ সব পাশ |

⚠️ Android চালানোর উপায় নেই — TK দেখে বলবেন।
