# V535 — পুরোনো দিনের Chamber বোর্ডে সেদিনের লেখা ফিরে এল
তারিখ: ২২.০৮.২০২৬ · V534 → **V535 (5.35)**

TK-এর রিপোর্ট (২১ অগাস্টের বোর্ডের ছবি):
> *"গতকাল সব কিছু ফিলাপ করেছিল কিন্তু এখন অনেকগুলি ফাঁকা কেন?"*

---

## ১. ⛔ প্রথমেই — এটা V533/V534-এর জন্য হয়নি (প্রমাণসহ)

| প্রশ্ন | প্রমাণ |
|---|---|
| বোর্ডের **TREATMENT PROGRESS** কোথা থেকে পড়ে? | **`followups.lastRemark`** (`ChamberAttendanceRepository.kt:1081`) |
| V533-এ আমি কী বদলেছি? | **`payments.remarks` → `payments.progress`** — **অন্য টেবিল, অন্য ঘর** |
| TK কি V533/V534 বিল্ড করেছেন? | **না** — শেষ বিল্ড V526 |
| SQL কি কিছু মুছতে পারে? | **না** — শুধু একটা **ফাঁকা ঘর যোগ** করেছে |

⇒ কারণটা **পুরোনো**, এতদিন চোখে পড়েনি কারণ পুরোনো দিন খুলে দেখা হয়নি।

---

## ২. আসল কারণ

বোর্ড followups আনে **তারিখের কোনো ছাঁকনি ছাড়াই** (শুধু ব্রাঞ্চ ধরে) —
`ChamberAttendanceRepository.kt:485`. তাই ২১ তারিখের বোর্ড খুললেও সে দেখায়
প্রতিটা রোগীর সারিতে **এই মুহূর্তে** যে লেখাটা আছে, সেদিন কী লেখা ছিল তা নয়।

রোগী আবার এলে বা টাকা দিলে `lastRemark` **নতুন লেখায় বদলে যায়**। নতুন লেখাটা
অ্যাপের নিজের অটো-লেখা হলে (`"Advance Payment received"`,
`"Converted to Patient / Treatment"` …) বোর্ড সেটা **ইচ্ছে করে লুকায়**
(`isAppAutoRemark`) ⇒ পর্দায় **"—"**।

TK-এর ছবিতেও তাই: **AJIJAR RAHAMA ₹1,000** ও **BISHNUROBI DAS ₹1,000** —
দু'জনেই টাকা দিয়েছেন, দু'জনেরই ঘর ফাঁকা।

---

## ৩. ✅ তথ্য হারায়নি — যাচাই করে দেখা হয়েছে

`FollowUpRepository.kt:2610` — নোট লেখার সময় **প্রতিবার** `history`-তে
**তারিখ · সময় · লেখা · কে লিখেছেন** জমা হয়:

```kotlin
history.put(JSONObject().put("date", FollowUpModel.today()).put("time", isoNow())
    .put("remark", remark).put("staff", staffName))
```

⇒ ২১ তারিখের লেখাগুলো **ওখানেই আছে**, শুধু বোর্ড সেখান থেকে পড়ত না।

---

## ৪. সমাধান — এবং কেন খরচ বাড়েনি

বোর্ড `history` ঘরটা **আনেই না** (`FOLLOWUP_COLS_CHAMBER_BOARD`)। সবসময় আনলে
৫০০০ সারির `history` নামত — **Supabase-এর খরচ অনেক বেড়ে যেত**, আর TK-এর
পর্দায় ইতিমধ্যেই কোটার সতর্কবার্তা এসেছে। তাই **শুধু দরকারে**:

```
পুরোনো দিনের বোর্ড?  → না হলে কিচ্ছু হয় না
   ↓ হ্যাঁ
কোনো সারি ফাঁকা?     → না হলে কিচ্ছু হয় না
   ↓ হ্যাঁ
শুধু ওই কয়েকটা সারির `id,history` — একটাই ছোট অনুরোধ
   ↓
ওই তারিখের নিজের লেখাটা বসে
```

- ⛔ **আজকের বোর্ডে একটাও বাড়তি অনুরোধ নয়**
- ⛔ ভরা সারিতে হাত পড়ে না
- ⛔ অন্য দিনের লেখা ভুল করে বসে না
- ⛔ ব্যর্থ হলে বোর্ড **আগের মতোই** চলে (`try/catch`)

---

## ৫. যাচাই — **আসল `loadBoard()` compile করে চালানো হয়েছে**

`ChamberAttendanceRepository.kt`-এ Android-এর কিছু নেই, তাই আসল ফাইলটাই
compile করে stub দিয়ে চালানো গেছে (নকল কপি নয়)।

**cham suite: ৩১/৩১ PASS** — তার মধ্যে V535-এর ৪টে নতুন:

| পরীক্ষা | ফল |
|---|---|
| পুরোনো দিনে সেদিনের লেখাটাই দেখায় | ✅ `DRESSING করা হল` |
| **আজকের বোর্ড হুবহু আগের মতোই** | ✅ |
| আগে থেকে লেখা থাকলে বদলায় না | ✅ `CHECK-UP করা হলো` |
| **অন্য দিনের লেখা ভুল করে বসে না** | ✅ |

**বাকি সব suite — একটাও ভাঙেনি:**
PatientIdentity **27·0** · rep **25·0** · tt **19·0** · rc **19·0** ·
family **29·0** · rot **21·0** · futab **19·0** · gtest **13·0**

---

## ৬. ⚠️ সৎ কথা — কোনটা ফিরবে, কোনটা ফিরবে না

- যে লেখাগুলো **Chamber-এর Treatment Progress বাক্স** দিয়ে লেখা হয়েছিল —
  সেগুলো `history`-তে আছে ⇒ **ফিরে আসবে** ✅
- যদি কোনো লেখা **কখনো `history`-তে যায়নি** (যেমন Report Card-এর নিজের
  ৩-ট্যাপ এডিট, যেটা শুধু payment সারিতে লেখে) — **সেটা এভাবে ফিরবে না**।
  V533-এর পর ওগুলো `payments.progress`-এ তারিখ সমেত জমছে, তাই **আগামী
  দিনগুলোর জন্য** সমস্যাটা এমনিতেই আর হবে না।

⚠️ Android SDK এখানে নামে না ⇒ Activity ফাইল compile করা যায় না; তবে এই
কাজটা পুরোটাই Repository-তে, আর সেটা **সত্যিই চালিয়ে দেখা হয়েছে**।

---

## ৭. TK-এর করণীয়
1. **V535 বিল্ড** (V533-এর SQL আগেই চালানো হয়ে গেছে ✅)
2. **২১ অগাস্টের বোর্ড খুলুন** → ফাঁকা ঘরগুলোয় সেদিনের লেখা ফিরেছে কিনা
3. 🔴 **আজকের বোর্ড আগের মতোই আছে কিনা** — এটাই সবচেয়ে জরুরি
