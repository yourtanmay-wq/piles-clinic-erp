# V534 — অন্য রোগীর সারিতে হাত পড়া বন্ধ
তারিখ: ২২.০৮.২০২৬ · V533 → **V534 (5.34)**

TK-এর নির্দেশ: *"সম্পূর্ণ প্রজেক্ট খুলে দেখুন এরকম ধরনের আর সমস্যা কোথাও নেই তো"*
→ *"হ্যাঁ দুটোই ঠিক করুন, সাবধানে করবেন।"*

---

## ১. কীভাবে খোঁজা হয়েছে (আন্দাজে নয়)

পুরো কোডে যত জায়গায় **একসাথে একাধিক সারিতে লেখা হয়** — **৬৩টা**।
তার মধ্যে যেগুলো **মানুষের লেখা তথ্য** (নাম · Remark · রোগ · ইতিহাস) একাধিক
সারিতে বসায় — **১৫টা**। প্রত্যেকটা হাতে পড়ে দেখা হয়েছে।

- **২টা**-তে DRESSING-এর মতোই ফাঁক পাওয়া গেছে ⇒ **এই ভার্সনে ঠিক করা হলো**
- **২টা**-তে পাহারা **আগে থেকেই ছিল** (`MobileChangeSync` · `FollowUpActivity`)
- বাকি **১১টা** যাচাই করে দেখা গেল ঠিকই আছে (রোগীর নিজের সারিতেই লেখে)

---

## ২. ফাঁক ১ — রোগীর মোবাইল বদলালে **অন্য রোগীর নামও বদলে যেত**

`PatientTimelineActivity.kt` (নম্বর বদলের cascade)

**আগে:** পুরোনো নম্বরের **সব** followups ও enquiries সারিতে নতুন নাম + নতুন
নম্বর বসত — কে কার সারি তা না দেখেই। এক নম্বরে দু'জন থাকলে একজনের নম্বর
বদলালে **অন্যজনের রেকর্ডও তুলে নিয়ে যেত ও তাঁর নাম মুছে দিত**।

**এখন:**
- নম্বর **ভাগ করা না হলে** (রোজকার ৯৯%) ⇒ **হুবহু আগের আচরণ**
- ভাগ করা হলে ⇒ followups-এ **শুধু প্রমাণসহ এই রোগীর সারি** (`refId`/`patientId`)
- enquiries-এ **রোগী চেনার ঘরই নেই** ⇒ ভাগ করা থাকলে **একটাও ছোঁয়া হয় না**

## ৩. ফাঁক ২ — Restore & Move Branch-এ **অন্য রোগীর রোগও বদলে যেত**

`EnquiryRepository.restoreAndMove()` + `EnquiryActivity`

**আগে:** ওই নম্বরের **সব** followups · enquiries · patients সারিতে নতুন
**ব্রাঞ্চ · status · রোগ · lastRemark** বসত। অন্যজনের **রোগের নাম মুছে গিয়ে**
এঁর রোগ বসে যেত, ব্রাঞ্চও বদলে যেত।

**এখন:** নম্বরে একাধিক আলাদা রোগী থাকলে **"কোন রোগী?" জিজ্ঞাসা** করা হয়
(প্রজেক্টের সেই একই পর্দা — Payment · Print · Doctor Visit · Chamber-এ যেটা চলছে),
তারপর **শুধু তাঁরই সারিগুলো** বদলায়।
⛔ একজন থাকলে কিছুই জিজ্ঞাসা করা হয় না, ডাকটা **হুবহু আগের মতোই**।
⛔ স্টাফ *Cancel* করলে **কিচ্ছু বদলায় না**।

---

## ৪. নিয়মটা নতুন নয় — প্রমাণিত নিয়মই এক জায়গায় আনা হলো

`PatientIdentity.isSharedNumber()` ও `PatientIdentity.rowBelongsTo()` —
হুবহু সেই পাহারা যেটা `MobileChangeSync.sync()` (V134) আর
`FollowUpActivity`-র নম্বর-বদল **আগে থেকেই** করে আসছে:

```
১) নম্বরটা সত্যিই ভাগ করা কিনা দেখা হয়
২) ভাগ করা না হলে ⇒ আগের মতোই সব সারি (কিছুই বদলায় না)
৩) ভাগ করা হলে ⇒ শুধু প্রমাণসহ এই রোগীর সারি
⛔ প্রমাণ না থাকলে সারিটা ছোঁয়াই হয় না — ভুল করে নষ্ট করার চেয়ে নিরাপদ
```

⛔ **নতুন কোনো ক্লাউড-অনুরোধ নেই** — আগে-আনা সারিগুলোর উপরেই কাজ।
⛔ **Database schema · RLS · production data — কিছুই ছোঁয়া হয়নি।** SQL লাগে না।
⛔ `pickPatientRow` (নয় পর্দার শেয়ার-করা V143 নিয়ম) — **ছোঁয়া হয়নি**।

---

## ৫. যাচাই

**নতুন পাহারাটা compile করে চালানো হয়েছে** (আসল `PatientIdentity.kt` ফাইল,
`PatientModel.isDeclaredSeparateRowId`-ও আসল ফাইল থেকে হুবহু কেটে) —
**২৭/২৭ PASS**, তার মধ্যে V534-এর ১২টা নতুন পরীক্ষা:
একজন → ভাগ করা নয় · দুটো ভিন্ন কোড → ভাগ করা · **একই কোড দু'বার → ভাগ করা নয়**
(ডুপ্লিকেট রেজিস্ট্রেশনে যেন ভুল না হয়) · ঘোষিত আলাদা আইডি → ভাগ করা ·
id/কোড/refId মিললে আমার · অন্যের সারি আমার নয় · **দুটো চাবিই ফাঁকা → ছোঁব না** ·
null সারি → ছোঁব না।

**পুরোনো সব পরীক্ষা — একটাও ভাঙেনি:**
cham **27·0** · rep **25·0** · tt **19·0** · rc **19·0** · family **29·0** ·
rot **21·0** · futab **19·0** · gtest **13·0** · ওয়েব ৮/৮ ✅
বন্ধনীর হিসাব চার ফাইলেই `git HEAD`-এর সাথে **হুবহু এক**।

⚠️ Android SDK এখানে নামে না ⇒ Activity ফাইল compile করে দেখানো যায়নি।

---

## ৬. TK-এর করণীয়
1. **V534 বিল্ড** (⚠️ V533-এর SQL আগে চালানো থাকতে হবে)
2. এক নম্বরে দু'জন রেখে একজনের **নম্বর বদলান** → অন্যজন অক্ষত আছেন কিনা
3. একই নম্বরে **Restore** চাপুন → "কোন রোগী?" জিজ্ঞাসা করছে কিনা
4. 🔴 **সাধারণ (একজন রোগীর) নম্বরে সব আগের মতোই আছে কিনা** — সবচেয়ে জরুরি
