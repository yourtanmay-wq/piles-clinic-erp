# V601 — Egress অডিট #২: fetchList() ২১ ফাইলে cache-এ আনা
**তারিখ:** ২৪.০৮.২০২৬ (রাত ১২:৪০) · **ভার্সন:** V601 / 6.01

## প্রেক্ষাপট
আজ Supabase-এর ফ্রি প্ল্যান egress শেষ (6.264/5 GB, 125%), TK-এর Supabase
ড্যাশবোর্ড-স্ক্রিনশটে নিশ্চিত। TK নির্দেশ দেন: "egress আরও কমানোর কাজ করতে
হবে, কিন্তু আগে পুরো প্রজেক্ট খুঁটিয়ে যাচাই করে লিস্ট করুন।"

## যা যাচাই করা হলো (তালিকা)
1. **Trash Bin — প্রথমে সন্দেহ করেছিলাম, যাচাই করে ভুল প্রমাণিত।**
   `TrashRepository.fetchTrashRaw()` (raw, পুরো record+ছবিসহ, ৫০০০ সীমা)
   দেখে মনে হয়েছিল এটাই সবচেয়ে বড় ঝুঁকি। কিন্তু কোড খুঁটিয়ে দেখে
   পাওয়া গেছে — **এই ফাংশনটা এখন কোথাও ডাকাই হয় না** (`grep` দিয়ে
   নিশ্চিত)। আসল Trash Bin স্ক্রিন (`TrashBinActivity.kt:215`)
   `fetchTrashRawOrNull()` ডাকে — যেটা আগের সেশনেই (V509/V512/V515)
   narrow-column, ছবি-ছাড়া নিরাপদ পথে বসানো হয়ে গেছে। **এই অংশে নতুন
   কোনো কাজ লাগেনি** — আমার প্রথম অনুমান ভুল ছিল, সংশোধন করে TK-কে
   জানানো হয়েছে।

2. **আসল ফাঁক — `SupabaseClient.fetchList()`।**
   ২১টা ফাইলে ~৫৫ জায়গায় ব্যবহৃত (Payment, Doctor Visit, Chamber
   Attendance, Print Center, Follow-up, Draft, Timeline, Registration...)।
   `fetchListOrNull()`-এর মতো কখনোই `CloudReadDedupe` (৬০সে TTL) বা
   `CloudListRevalidate` (সই-মিলিয়ে দেখা) দিয়ে যেত না — V515-এর নিজের
   কমেন্টেই স্বীকার করা ছিল, কিন্তু তখন মাত্র ২টা ফাইল (`fetchListGuarded`)
   সরানো হয়েছিল, বাকি ১৯টা আগের মতোই ছিল।

## সমাধান
`fetchList()`-এর ভিতরের কোড বদলে `fetchListOrNull() ?: JSONArray()` করা
হলো — অর্থাৎ এখন এটাও সেই একই প্রমাণিত cache-এর ভিতর দিয়েই যায়।
⇒ **২১টা ফাইলের একটা লাইনও ছোঁয়া হয়নি** — সবাই একসাথে সুরক্ষিত।

## নিরাপত্তা (কেন এটা ঝুঁকিহীন)
- **আচরণ (contract) অভিন্ন:** ব্যর্থ হলে আগের মতোই খালি `[]` (এখনো `null`
  না) — কোনো ডাকার জায়গার `.length()`/`if(rows==null)` চেক ভাঙে না।
- **URL/filter/limit/order/select** — এক অক্ষরও বদলায়নি।
- **বড়-রেকর্ড টেবিলে (trash-এর মতো) ঝুঁকি নেই:** `CloudListRevalidate`-এর
  নিজস্ব সীমা (MAX_ONE_BYTES 2MB, মোট 8MB/12MB) বড় উত্তর কখনো জমা রাখে
  না — জমা না হলে ক্ষতি নেই, শুধু আগের মতোই সরাসরি নেটে যায়। (আর যেহেতু
  সত্যিকারের Trash Bin স্ক্রিন এই ফাংশনই ব্যবহার করে না, এই প্রশ্নটা
  বাস্তবে ওঠেই না।)
- **নিজে সেভ করলে সঙ্গে সঙ্গে ভুলে যায়** (৭ জায়গায় `CloudReadDedupe.clear()`
  আগে থেকেই আছে, আজ যাচাই করা)।

## ওয়েব
ওয়েব সম্পূর্ণ ভিন্ন গঠনে চলে — সব ডেটা `localStorage`-এ local mirror হিসেবে
রাখা থাকে (`load(table)`), প্রতি স্ক্রিনে সরাসরি Supabase কল হয় না। তাই এই
নির্দিষ্ট গর্তটা ওয়েবে নেই, কিছু বদলাতে হয়নি।

## যাচাই
| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py --release` | ✅ সম্পূর্ণ পাশ |
| `verify_version_json.py` | ✅ V601/6.01 |
| SupabaseClient.kt ব্রেস গণনা | ✅ ২০৯/২০৯ মিলেছে |
| Trash-এর আসল কলার | ✅ যাচাই — `fetchTrashRawOrNull()`-ই ব্যবহৃত |
| `fetchList()`-এর ৫৫টা কলসাইট | ✅ কোনোটাই ছোঁয়া হয়নি (শুধু একটা কেন্দ্রীয় ফাংশন বদলেছে) |

## পরের ধাপ (এখনো করা হয়নি, TK-এর সিদ্ধান্তের অপেক্ষায়)
- অবশিষ্ট `id=eq.X, limit=1` একক-সারি কলগুলো (৩০+) — এখন এই ফিক্সের ফলে
  এগুলোও cache-সুরক্ষিত হয়ে গেছে, তাই আলাদা কাজ লাগছে না।
- Egress আরও কমানোর সুযোগ থাকলে (যেমন LiveRefresh-এর বাইরে অন্য কোনো
  পুনরাবৃত্ত পোলিং) পরের সেশনে TK চাইলে আরও গভীরে দেখা যাবে।
