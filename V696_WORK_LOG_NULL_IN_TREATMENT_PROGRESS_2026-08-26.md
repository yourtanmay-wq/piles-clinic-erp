# V696 — TREATMENT PROGRESS-এ "null" লেখা

**তারিখ:** ২৬.০৮.২০২৬ (IST)
**ধরা পড়েছে:** TK-এর পাঠানো Chamber Date-এর ছবিতে — Kishanganj, ২৬.০৮.২০২৬।
**SERINA KHATTON**-এর সারিতে TREATMENT PROGRESS ঘরে লেখা ছিল **`null`**।

---

## ১. আসল কারণ (আন্দাজ নয়)

`payments` টেবিলের **`progress`** ঘরটা ফাঁকা থাকলে ডেটাবেস পাঠায় SQL NULL।
Android-এর `org.json`-এ —

```
row.optString("progress", "")   →  ফেরত দেয়  "null"   ← ফাঁকা নয়, চার অক্ষরের একটা লেখা!
```

কোডে পরের লাইনেই ছিল `if (progressToday.isNotBlank())` — "null" লেখাটা
**ফাঁকা নয়** বলে পরীক্ষাটা পাশ করে যেত, আর ওটাই আসল Treatment Progress
ভেবে সারিতে বসে যেত।

⛔ **এই ফাঁদটার জন্যই প্রজেক্টে আগে থেকে `JsonExt.s()` বানানো ছিল** (ওর
নিজের নোটেই লেখা: *"caused 'null' to show on screen (e.g. Last Remark:
null)"*)। ঠিক উপরের লাইনেই `row.s("mobile")` ব্যবহার হয়েছে — শুধু এই
একটা লাইনে বাদ পড়ে গিয়েছিল।

---

## ২. যা যা ঠিক হলো (নিয়ম ৬.২ — একই ধরনের সব জায়গা)

শুধু ধরা-পড়া লাইনটা নয়, **প্রজেক্টে একই ফাঁদ যেখানে যেখানে ছিল** সবকটা:

| ফাইল | ঘর |
|---|---|
| `ChamberAttendanceRepository.kt` | `progress` (TK-এর ধরা দোষ) · `remark` (জমানো তালিকা) |
| `DialerRepository.kt` | `remark` (২ জায়গা) |
| `DraftRepository.kt` | `lastRemark` · `nextFollow` |
| `FollowUpRepository.kt` | `remark` · `lastRemark` · `nextFollow` · `staffName` (৪ জায়গা) |
| `ExpectedTomorrowActivity.kt` | `lastRemark` · `disease` · `address` · `remark` |

সবগুলোতেই `optString(...)` → **`s(...)`**।
⚠️ এটা **কখনো নতুন ঝুঁকি নয়**: ঘরে লেখা থাকলে দুটোই একই ফল দেয়, শুধু
ঘরটা ফাঁকা হলে আগে "null" আসত, এখন সত্যিই ফাঁকা আসে।

---

## ৩. শেষ পাহারা — পুরনো ফোনের জমানো তালিকা

যাঁদের ফোনে **আগের বিল্ডের জমানো তালিকায় ইতিমধ্যেই "null" ঢুকে গেছে**,
তাঁদের পর্দাতেও যেন আর না দেখায় — তাই Chamber-এর পর্দাতেও "null" লেখাটাকে
ফাঁকা ধরা হয়। উৎস সারানোর পরেও এই দ্বিতীয় পাহারাটা রাখা হলো।

⇒ এখন ওই ঘর ফাঁকা হলে আগের নিয়মেই **"—"** দেখাবে।

---

## ৪. 🟢 কম্পিউটারে এই দোষ নেই (নিয়ম ২০ — যাচাই করে বলা)

ওয়েবে `esc()` ফাংশনটা `String(s ?? '')` করে — অর্থাৎ null নিজে থেকেই
ফাঁকা হয়ে যায়, আর ডাকার জায়গাতেও `if(x.lastRemark)` পাহারা আছে।
তাই ওয়েবে কোনো বদল লাগেনি।

---

## ৫. ⛔ যা ছোঁয়া হয়নি

- 💰 টাকা · বিল · ফি · হিসাব — **কিছুই নয়**।
- Treatment Progress লেখা/সেভ/এডিটের নিয়ম — অপরিবর্তিত।
- V654-এর রঙের নিয়ম (আজকের লেখা গাঢ়, আগের দিনের হালকা) — অপরিবর্তিত,
  শুধু "null"-কে এখন ফাঁকা ধরে বলে রংটাও ঠিক হয় (হলুদ "কিছু লেখা হয়নি")।

---

## ৬. যাচাই

| যাচাই | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ (V696) |
| `verify_kotlin_compile.py` | ✅ PASS — নতুন ভুল ০ |

---

## 🔴 এখনো TK-এর লাইভ টেস্ট বাকি

- [ ] SERINA KHATTON-এর সারিতে আর **"null" নেই** — "—" বা হলুদ "কিছু লেখা হয়নি"
- [ ] যাঁদের সত্যিই Treatment Progress লেখা আছে, তাঁদেরটা **আগের মতোই** দেখাচ্ছে
- [ ] আজকের লেখা গাঢ় সবুজ, আগের দিনের হালকা ধূসর — আগের মতোই
- [ ] Follow-up ও Draft কার্ডে Last Remark-এও কোথাও "null" নেই
