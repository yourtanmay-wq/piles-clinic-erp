# V608 — Master-এর জন্য ব্রাঞ্চ-ভিত্তিক ভাঙা + ৩+ দিন ওভারডিউ সতর্কতা
**তারিখ:** ২৪.০৮.২০২৬ · **ভার্সন:** V608 / 6.08

## TK-এর অনুমোদিত প্রুফ অনুযায়ী (ছবি-প্রুফ পাশ)
1. Master-এর ড্যাশবোর্ডে ব্যানারের নিচে "👁 Breakdown by branch" — চাপলে
   কোন ব্রাঞ্চে কত পেন্ডিং/ওভারডিউ, প্রতিটাতে চাপলে সেই ব্রাঞ্চেই সরাসরি চলে যায়।
2. কোনো কল **৩ দিন বা তার বেশি** ওভারডিউ হলে Master-এর নোটিস বোর্ডে
   "⚠️ Overdue Follow-up Alert" — ব্রাঞ্চ-ভিত্তিক সংখ্যাসহ, দিনে একবারই।

## সৎ সীমা (TK-কে আগেই জানানো হয়েছিল, স্বীকৃত)
আগের প্রুফে স্টাফ-নাম দেখানো হয়েছিল নমুনা হিসেবে — আসল ডেটায় কোন
ফলো-আপ কার দায়িত্বে তা সংরক্ষিত নেই, তাই **শুধু ব্রাঞ্চ-ভিত্তিক** ভাঙা
বাস্তবায়িত হয়েছে, স্টাফ-ভিত্তিক নয়।

## নিরাপত্তা (V509-এর egress-ফিক্স অক্ষত)
⛔ **একটাও নতুন নেটওয়ার্ক-কল নেই।** ব্যানারের সংখ্যা গণনা করতে যে তিনটে
`repo.loadCachedTab()` (স্থানীয় cache-পড়া) আগে থেকেই হতো, এই ফিচার দুটো
সেই **একই** পড়া থেকে পাওয়া আইটেমগুলো (`allDueItems`) পুনর্ব্যবহার করে —
ব্রাঞ্চ ধরে ভাগ করা ও ৩+ দিনের ফিল্টার দুটোই মেমরিতে, কোনো বাড়তি
cloud-read ছাড়া।

- ব্রাঞ্চে চাপলে `BranchFilterStore.set()` (Follow-up-এর নিজস্ব
  branch-picker যা করে, ঠিক তাই) — একটা ভুল intent-extra প্রথমে লিখে
  ফেলেছিলাম যেটা আসলে কোথাও পড়াই হতো না, নিজে ধরে ঠিক করা হয়েছে।
- সতর্কতা-নোটিশ দিনে **একবারই** (SharedPreferences-এ আজকের তারিখ জমা)
  — বারবার নোটিস-বোর্ড ভরে যাওয়া আটকানো হয়েছে।
- `BriefingRepository().post()` — প্রমাণিত, আগে থেকেই ব্যবহৃত পথ
  (queue+retry-সহ), নতুন কোনো লেখার প্রক্রিয়া বানানো হয়নি।

## নতুন XML (additive)
`activity_dashboard.xml`-এ `tvCallBreakdownLink` ও `callBreakdownRows` —
দুটোই ডিফল্ট `gone`, staff-এর পর্দায় কখনো দেখা যাবে না (role-চেক Kotlin-এ)।

## যাচাই
| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ |
| `verify_version_json.py` | ✅ V608/6.08 |
| XML ভ্যালিড | ✅ |

## ওয়েব
Master ওয়েবেও ব্যবহার করেন, কিন্তু এই সেশনে সময়ের সীমার কারণে **শুধু
Android-এ করা হলো** — ওয়েবে একই ফিচার করতে হলে TK জানালে পরের ধাপে করা
হবে।
