# 💊 V723 — ওষুধের Instruction এখন প্রেসক্রিপশনে ছাপে

**তারিখ:** ২৭.০৮.২০২৬ · রাত ~১.৩০ IST
**রিপোর্ট:** ডা. কে. এইচ. মণ্ডল · **অনুমোদন:** TK — ডেমো `V723_DEMO_MEDICINE_INSTRUCTION.png`-এ **প্রস্তাব ১**

---

## ১. সমস্যা
"Add Medicine (Outside List)"-এ Powder **LAXALL**-এর Instruction ঘরে
**"WITH LWW"** লেখা হয়েছিল, কিন্তু **final print-এ আসেনি**। ফলে কোন ওষুধ
গরম জলের সঙ্গে, কোনটা মধু দিয়ে খেতে হবে — রোগী জানতে পারতেন না।

## ২. আসল কারণ (কোড ধরে)
`PrintMappers.prescription()` লেখাটা শুধু পুরোনো `lines`-এ জুড়ত
(`"  [WITH LWW]"`), অথচ A4 ছকটা ছাপে `rxNames · rxDosage · rxFrequency ·
rxDuration` — **Instruction-এর কোনো ঘর ছকে ছিলই না**, তাই হারিয়ে যেত।

## ৩. যা করা হলো (TK-অনুমোদিত প্রস্তাব ১)
Instruction এখন **ওষুধের নামের ঠিক নিচে ছোট হরফে** (বাদামি, ৮pt) ছাপে।

| ফাইল | কী |
|---|---|
| `PrintDocumentModel.kt` | নতুন ঐচ্ছিক ঘর `rxInstructions` (ডিফল্ট `null`) |
| `PrintMappers.kt` | Prescription ও Medicine Slip — দুটোতেই তালিকাটা পাঠানো |
| `PrintCenterActivity.kt` | Walk-in কাগজেও একই |
| `ClinicPdfBuilder.kt` | ছকে নামের নিচে লেখাটা আঁকা |

### 🔒 নিরাপত্তা
- **একটাও কলামের চওড়া বদলায়নি** ⇒ লম্বা ওষুধের নাম ভেঙে যাওয়ার ঝুঁকি তৈরি হয়নি
- **Instruction ফাঁকা হলে সারিটা হুবহু আগের মতোই** ছাপে
  (`nameBlockH == nameLayout.height` ⇒ পুরোনো হিসাবই)
- নতুন ঘরটা **ঐচ্ছিক** ⇒ যেসব জায়গা এটা পাঠায় না, তাদের কোড এক অক্ষরও বদলায়নি
- Type ব্যাজটা এখন নামের লাইনের সঙ্গে মেলে — এক-লাইনের সারিতে আগের জায়গাতেই পড়ে
- লেখা লম্বা হলে **নিজে থেকেই নিচের লাইনে** যায়, সারির উচ্চতাও সেভাবে বাড়ে
- কোনো তথ্য · হিসাব · ডেটাবেস · ক্লাউড-কল ছোঁয়া হয়নি

## ৪. ওয়েব
ওয়েবে **প্রতি-ওষুধের আলাদা Instruction ঘরই নেই** (V430-এ TK-এর সিদ্ধান্তে
"Dose/Instruction" ঘরটা শুধু Custom ওষুধের বাক্সে রাখা হয়েছিল) — তাই ছাপার
মতো কিছু নেই, পার্থক্যও তৈরি হয়নি। যাচাই করা।
👉 TK চাইলে পরে ওয়েবেও ঘরটা যোগ করা যাবে — আলাদা কাজ, অনুমতি নিয়ে।

## ৫. যাচাই
- ✅ Kotlin কম্পাইল — নতুন ভুল **০**
- ✅ গার্ডের সব যাচাই পাশ
**ভার্সন:** V722 → **V723**
