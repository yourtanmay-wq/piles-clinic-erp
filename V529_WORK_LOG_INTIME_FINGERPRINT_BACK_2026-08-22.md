# V529 — "STAFF এর intime দেওয়ার সময় থাকুক"
তারিখ: ২২.০৮.২০২৬ · আগের ভার্সন V528 → এখন **V529 (5.29)**

---

## ১. TK কী বলেছিলেন

> **"STAFF এর intime দেওয়ার সময় থাকুক"**

V528-এ আমি হাজিরার (IN TIME) আঙুলের ছাপটা **তুলে দিয়েছিলাম**।
TK সেটা ফেরত চেয়েছেন। **ফেরত দেওয়া হয়েছে।**

---

## ২. কী করা হয়েছে — এক লাইনে

হাজিরার আঙুলের ছাপের ধাপটা **git থেকে অক্ষরে অক্ষরে (byte-for-byte) ফিরিয়ে আনা
হয়েছে** — নতুন করে হাতে লিখিনি, তাই ভুল ঢোকার কোনো সুযোগই নেই।

ফাইল:
`02_ANDROID_SOURCE_CODE/PilesClinicApp/app/src/main/java/com/tkbiswas/pilesclinic/modules/WorkNotebookActivity.kt`

কমান্ড:
```
git checkout HEAD~1 -- .../modules/WorkNotebookActivity.kt
```

**প্রমাণ (নিজে চালিয়ে দেখা):**

| যাচাই | ফল |
|---|---|
| `git show HEAD~1:<ফাইল> \| diff -q - <ফাইল>` | ✅ **অক্ষরে অক্ষরে এক** — কোনো পার্থক্য নেই |
| `grep -c "V528" <ফাইল>` | **0** — V528-এর কোনো টুকরো পড়ে নেই |
| `{ } ( )` গোনা, HEAD~1-এর সাথে | **582 / 582 / 2612 / 2631** — বন্ধনী মেলে (নতুন কমেন্টের একজোড়া বাদে হুবহু এক) |

তারপর শুধু **একটা কমেন্ট** যোগ করা হয়েছে (কোড নয়), যাতে ভবিষ্যতে আর কেউ —
আমি সমেত — ভুল করে এই ধাপটা তুলে না দেয়:

```
/* 🟢🔒 V529 — TK-নির্দেশ: "STAFF এর intime দেওয়ার সময় থাকুক"
   ⛔ ভবিষ্যতে এই ব্লক সরানো যাবে না, TK নিজে না বললে। */
```

`diff` চালিয়ে দেখানো হয়েছে — **ওই ৫ লাইন কমেন্ট ছাড়া আর একটা অক্ষরও বদলায়নি।**

---

## ৩. এখন আঙুলের ছাপ ঠিক কোথায় কোথায় চাইবে

| জায়গা | চাইবে? | কোন ফাইলে |
|---|---|---|
| **Login** (এই ফোনে আগে লগইন করা থাকলে) | ✅ হ্যাঁ | `native/LoginActivity.kt` |
| **STAFF IN TIME** (হাজিরা) | ✅ **হ্যাঁ — V529-এ ফেরত এল** | `modules/WorkNotebookActivity.kt:1626` |
| **Refund** (রোগীকে টাকা ফেরত) | ✅ হ্যাঁ, **এক টাকা হলেও** | `native/PaymentActivity.kt` |
| অ্যাপ খুললেই বারবার | ❌ **না** (V527-এ বন্ধ, বন্ধই আছে) | `SessionGuardBridge.kt` |
| সাধারণ পেমেন্ট নেওয়া | ❌ না | — |
| বাকি সব পর্দা | ❌ না | — |

**প্রমাণ:** `grep -rn "AppLock.guard"` — কোথাও **ডাকা হয় না**, শুধু
`SessionGuardBridge.kt`-এর ব্যাখ্যার কমেন্টে নামটা লেখা আছে।

---

## ৪. যা ছোঁয়াই হয়নি (ভাল কাজ যেন খারাপ না হয়)

- **GPS পাহারা** (`ClinicPresence`) — হাজিরার ধাপ ৩, **অপরিবর্তিত**। ক্লিনিকে না
  থাকলে হাজিরা আগের মতোই আটকাবে।
- **`AttendanceRepository.markCheckIn`** — সার্ভারে হাজিরা লেখার কাজ, **ছোঁয়া হয়নি**।
- Supabase-এর কোনো টেবিল/RLS/query — **কিছুই বদলায়নি**, একটাও নতুন query নেই।
- পুরনো কোনো হাজিরার তথ্য — **অক্ষত**।

---

## ৫. সৎ কথা (আগেও বলেছি, আবার বলছি)

TK নিজেই বলেছিলেন —
> *"ফিঙ্গারপ্রিন্ট এর কাজ প্রত্যেকটা ব্যক্তির ক্ষেত্রে সমান।"*

কথাটা ঠিক। আঙুলের ছাপ **ফোনটা** প্রমাণ করে, **মানুষটাকে নয়**। কেউ যদি
সহকর্মীকে নিজের ফোন ধরিয়ে দেন, ছাপ দিয়ে হাজিরা বসে যাবে।
তাই **হাজিরার আসল পাহারা GPS** — সেটা আগের মতোই শক্ত আছে। আঙুলের ছাপটা
বাড়তি একটা ধাপ, একমাত্র পাহারা নয়। এটা লুকিয়ে না রেখে বলে রাখলাম।

---

## ৬. যাচাই যা চালানো হয়েছে

**Kotlin regression (আসল কোড তুলে এনে চালানো, নকল কপি নয়):**

| suite | ফল |
|---|---|
| `cham` (Chamber board) | **27 PASS · 0 FAIL** |
| `rep` (Reports) | **25 PASS · 0 FAIL** |
| `tt` (Timeline/timeType) | **19 PASS · 0 FAIL** |
| `rc` (Report Card) | **19 PASS · 0 FAIL** |
| `rot` (ছবি ঘোরানো) | **ALL PASSED** |
| `family` (এক নম্বরে একাধিক রোগী) | **ALL PASSED** |
| `futab` (Follow-up tab) | **ALL PASSED** |
| `gtest` (হাজিরার বার্তা) | **ALL PASSED** |

**ওয়েব:** ৮টা `.js` ফাইলেই `node --check` — **সবগুলো ঠিক আছে**।

⚠️ **সৎ সীমাবদ্ধতা:** এখানে Android SDK নামানো যায় না (`dl.google.com` → 403),
তাই `WorkNotebookActivity.kt` **compile করে দেখানো যায়নি**। তার বদলে —
git-এর সাথে **অক্ষরে অক্ষরে মিল** (`diff -q`) দিয়ে প্রমাণ করা হয়েছে, যেটা
এক্ষেত্রে compile-এর চেয়েও জোরালো: ফাইলটা **হুবহু সেই ফাইল যা V527-এ চলছিল**।

---

## ৭. ভার্সন

| ফাইল | আগে | এখন |
|---|---|---|
| `app/build.gradle.kts` | 528 / "5.28" | **529 / "5.29"** |
| `03_NETLIFY_READY/version.json` | 528 / "5.28" | **529 / "5.29"** |
| `03_NETLIFY_READY/index.html` | `app.js?v=v528` | **`app.js?v=v529`** |
