# 🛠️ V715 — Egress-এর আসল কারণ সারানো (২৬.০৮.২০২৬, রাত ~৯.১৫ IST)

**TK-এর নির্দেশ:** *"এবার সঠিক এবং সততার সাথে যাচাই করে সঠিকভাবে কাজটা কমপ্লিট
করবেন, কিন্তু কোন ভাল কাজ যেন খারাপ না হয়। কোথাও কোন সিদ্ধান্ত নিতে গেলে
আমাকে জিজ্ঞাসা করে নেবেন, নিজে থেকে আন্দাজে কিছু করবেন না।"*

**আগের ফাইল:** `V714_EGRESS_HANDOVER_SOMPURNO_PLAN_2026-08-26.md` (তদন্ত)

---

## ১. আসল কারণ — এখন **সম্পূর্ণ প্রমাণিত** (প্রতিটা সংখ্যা মিলেছে)

TK ডেটাবেসে চালানো query থেকে যা বেরোল:

| যাচাই | ফল |
|---|---|
| ওই রোগীর (মোবাইল …9547006061) followups সারি | **৩টে, তিনটেই `Treatment / Cancelled`** |
| গোটা followups টেবিল | **৮৪৫ সারি**, আজ নতুন **১০** |
| লগে `POST /rest/v1/followups → 201` | **৩,৯৩০ বার/দিন** |
| লগে `POST /rpc/tk_record_treatment_payment → 409` | ৩,২৮৪ বার/দিন |
| followups টেবিলের ট্রিগার | **`tk_terminal_no_return`** (BEFORE INSERT OR UPDATE) |

### ঘটনার শৃঙ্খল (একটাও ধাপ আন্দাজ নয়)

1. একজন রোগীর পেমেন্ট একটা ফোনের `piles_clinic_payment_pending` তালিকায় আছে।
2. `flushPending()` চলে **প্রতিবার যেকোনো পর্দা খোলার সময়** (`BottomNav.wire()`)
   ও **প্রতি ১৫ মিনিটে** (`SyncWorker`)।
3. টাকার RPC **৪০৯ (duplicate)** ফেরায় ⇒ V509-এর নিয়মে "টাকা আগেই পৌঁছে গেছে"
   ধরে নিয়ে এগোয় ✅ (**রোগীর টাকা ক্লাউডে পৌঁছেই গেছে** — এটা স্বস্তির খবর)।
4. `promoteFollowUpToTreatment()` রোগীর followups সারি টানে — তিনটেই
   `Cancelled`, তাই কোডের নিয়মে ছোঁয়া হয় না (বন্ধ সারি বন্ধই থাকবে)।
5. `moved == 0` ⇒ একটা **নতুন `Treatment / Active` সারি** বানানোর চেষ্টা।
6. 🔴 **ডেটাবেসের `tk_terminal_no_return` ট্রিগার সেই নতুন সারিটা চুপচাপ ফেলে
   দেয়** — কারণ ওই মোবাইল+stage-এ আগে থেকেই terminal (Cancelled) সারি আছে।
   PostgREST তবু **201** ফেরায়, তাই অ্যাপ ভাবে লেখা হয়ে গেছে।
7. শেষে `verify` দেখে — কোনো Active Treatment সারি নেই ⇒ **`false`** ⇒
   পেমেন্টটা তালিকাতেই থেকে যায় ⇒ ২ নম্বর ধাপে ফিরে যায়। **দিনে ~৪,০০০ বার।**
8. প্রতিবার তিনটে ভারী পড়া, তার একটা **`select=*` = রোগীর ছবিসহ ১০০ সারি**
   ⇒ দিনের ~৪০০ MB egress-এর সিংহভাগ।

---

## ২. এই ভার্সনে কী করা হলো (V712 → **V715**)

### ✅ কাজ ২ — `select=*` থেকে ছবি বাদ (`PaymentRepository.kt`)
তিনটে পড়াই এখন **সরু ঘর** (`fetchListSlim`) —

| কোথায় | আগে | এখন |
|---|---|---|
| scan (১০০ সারি) | `select=*` (ছবিসহ) | `id,stage,status,lastRemark,mobile,updatedAt` |
| Inquiry বন্ধ (৫০০০) | `select=*` | `id,name,patientId,refId,mobile,stage,status,updatedAt` |
| verify (১০) | `select=*` | `id` |

কোডে মিলিয়ে দেখা হয়েছে — এই লুপগুলো ঠিক এই ঘরগুলোই পড়ে, বেশি কিছু নয়
(verify তো শুধু গোনে)। `fetchListSlim` সরু পড়া ব্যর্থ হলে **আগের মতোই পুরো
সারি** নামায় (V405-এর প্রমাণিত fallback) — তাই কম তথ্য নিয়ে ভুল সিদ্ধান্তের
পথ নেই।

### ✅ কাজ ৩ — বারবার ব্যর্থ সারিতে অপেক্ষা (নতুন `PendingRetryBackoff.kt`)
ব্যর্থ ১–২ বার → সঙ্গে সঙ্গে (আগের আচরণ হুবহু) · ৩ বার → ১ মিনিট ·
৪ বার → ৫ মিনিট · ৫ বার → ১৫ মিনিট · ৬+ → ১ ঘণ্টা (সর্বোচ্চ)।

- ⛔ **কোনো সারি কখনো ফেলা হয় না** — শুধু "এখন চেষ্টা করব কি না"।
- ⛔ **"Send" চাপলে অপেক্ষা মানা হয় না** (`flushPending(force = true)` —
  `PendingSyncStatus.retryAllNow`)। হাতে চাপলে সঙ্গে সঙ্গেই যায়।
- ⛔ প্রথম দু'বার দেরি নেই ⇒ দুর্বল নেটে আচরণ এক অক্ষরও বদলায়নি।
- ⛔ `force` ঘরটা ঐচ্ছিক (ডিফল্ট `false`) ⇒ আগের সব ডাক অপরিবর্তিত।
- ⇒ এই একটাই বদলে ওই আটকে থাকা সারির খরচ **দিনে ~৪,০০০ → ~২৪ বার**।

### ✅ কাজ ৪ — নতুন পাহারাদার (গার্ড **§৯.২০**)
- `promoteFollowUpToTreatment()`-এ `fetchList(` (= `select=*`) ফিরলেই ❌
- `followups/patients/medical`-এ মোবাইল ধরে (`like.`) `select=*` তালিকা ❌
- `findByMobile*`-এ `"*"` + limit ≥ ৫০ ❌
- **নেগেটিভ টেস্ট করা হয়েছে:** ইচ্ছে করে পুরোনো লাইনটা ফিরিয়ে গার্ড ❌ দিয়েছে,
  ঠিক করার পরে ✅।

### ⏳ কাজ ১ — আটকে থাকা পেমেন্টটা — **TK-এর সিদ্ধান্তের অপেক্ষায়** (নিচে §৪)

---

## ৩. যাচাই

- ✅ `verify_kotlin_compile.py` — **নতুন ভুল ০**
- ✅ `tk_guard.py` — মেশিনের সব যাচাই পাশ (২৪টা), নতুন §৯.২০ সহ
- ✅ গার্ডের নেগেটিভ টেস্ট (আগে ❌, পরে ✅)
- **ওয়েব:** এই দোষটা ওয়েবে **নেই** — লগে সব `mobile=like.*` ডাক Android-এর
  ধরনের, আর `app.js`-এ মোবাইল ধরে ক্লাউড-পড়া কোথাও নেই (যাচাই করা)। তাই
  ওয়েবে কোনো বদল দরকার হয়নি; শুধু `version.json` ৭.১৫ করা হয়েছে।

---

## ৪. 🔴 TK-এর সিদ্ধান্ত দরকার (দুটো বিষয়)

### ক) ওই রোগীকে নিয়ে কী করব?
ওই রোগীর তিনটে ফলো-আপ সারিই **Cancelled** — অর্থাৎ কেউ ইচ্ছে করে বন্ধ করেছিল
(১৬ জুলাই ও ২০ অগাস্ট)। কিন্তু তাঁর কাছ থেকে **টাকা নেওয়া হয়েছে** এবং সেই
টাকা ক্লাউডে পৌঁছেও গেছে। দুটোর একটা সত্যি:
1. রোগী সত্যিই চিকিৎসায় আছেন ⇒ কার্ডটা **আবার খুলতে হবে**
2. রোগী সত্যিই বন্ধ (রিফান্ড/বাতিল) ⇒ কার্ড খোলার দরকার নেই, শুধু পেন্ডিং
   তালিকা থেকে সরাতে হবে

### খ) গার্ড §৯.২০ আরও ৮ জায়গা পেয়েছে
একই ধরনের চওড়া পড়া আরও ৮টা ফাইলে আছে (ReportCard · EnquiryRepository ·
PatientTimeline · ChamberAttendance · ReturnVisit · FollowUpActivity)।
**এগুলো এখন ছোঁয়া হয়নি** — কারণ ২৩.০৮.২০২৬-এর অডিটে কয়েকটার বেলায় লেখা আছে
"পুরো সারি সত্যিই দরকার, ছবি বাদ দিলে Restore/ব্রাঞ্চ-বদলে ছবি হারাবে"।
এগুলো এখন গার্ডের **ভিত্তি-তালিকায়** রাখা আছে (নতুন কোনো জায়গা যোগ হলে
গার্ড আটকাবে)। অনুমতি পেলে একটা একটা করে **মেপে** ঠিক করা হবে।

---

## ৫. এই ভার্সনে যা ছোঁয়া হয়নি

টাকার অঙ্ক · হিসাব · ডিজাইন · কোন সারি তালিকায় আসে · ছাঁকনি · limit ·
সাজানো · ব্রাঞ্চ-নিয়ম · অনুমতি · ডেটাবেসের ট্রিগার — **কিছুই নয়**।
কোনো ZIP পাঠানো হয়নি।
