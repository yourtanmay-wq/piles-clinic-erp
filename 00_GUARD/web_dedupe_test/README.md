# V495 — ওয়েবের dedupe পরীক্ষা

`extract.js` **সরাসরি `03_NETLIFY_READY/app.js` থেকেই** কোডটা বের করে, তাই
পরীক্ষা যেটা চালায় আর ওয়েবে যেটা চলে — দুটো কখনো আলাদা হতে পারে না।

    node extract.js && node wt.js

ফল: **১৫টি পরীক্ষার সবগুলো পাশ।**
