# 🔒 LOCK NOTE — V200 (৩০.০৭.২০২৬ সন্ধ্যা)

**ভার্সন:** `versionCode 200` · `versionName 2.00` · পর্দায় **V200** · খাতার সারি **B192**
⛔ **কোনো SQL লাগবে না।**

---

## ⛔ সবার আগে — স্থায়ী নিয়ম
> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

---

## TK-এর নির্দেশ (হুবহু)
প্রথমে Incomplete Patient স্ক্রিন দেখিয়ে বলেছিলেন Google Sheets-এ ডাউনলোড চাই। পরে স্পষ্ট করেছেন: *"শুধুমাত্র ইনকমপ্লিট পেশেন্ট এর ক্ষেত্রেই নয়, সমস্ত সেকশনের ক্ষেত্রেই Google Sheets এ ডাউনলোড করার ব্যবস্থা রাখবেন।"*

## ✅ ফটো-প্রুফ ও অনুমোদন
Draft List স্ক্রিনের হেডারে সবুজ "⬇ Sheet" বোতামের মকআপ দেখানো হয়েছিল (Follow-up-এর অনুমোদিত বোতামের অনুরূপ স্টাইলে)। TK "ঠিক আছে" বলার পরেই কোড লেখা হয়েছে।

## ✅ কী করা হলো

### স্থাপত্য (কেন সহজ হলো)
Draft-এর ছয়টা সেকশনই (My Enquiry (All Branch) · Enquiry Reject List · Visit Reject List · Incomplete Patient · Complete Patient · Unexpected Time Calls) **একটাই পর্দা** — `DraftListActivity` — দিয়ে খোলে (`DraftActivity.showList()` প্রতিটা বোতামের জন্য একই Activity-তে আলাদা তালিকা পাঠায়)। তাই **একটাই বোতাম** যোগ করলে ছয়টা সেকশনেই কাজ করে।

### বোতামের ডিজাইন
Follow-up পর্দার আগে-থেকে-অনুমোদিত "⬇ Sheet" বোতামের (খাতার সারি B69) **হুবহু একই**:
- ব্যাকগ্রাউন্ড: `#E2F6E8` (হালকা সবুজ)
- লেখার রং: `#0F7A3C` (গাঢ় সবুজ)
- মাপ: 32dp উচ্চতা, 11.5sp লেখা

নতুন কোনো ডিজাইন তৈরি হয়নি — প্রজেক্টে যা আগে থেকেই TK-approved আছে তারই পুনর্ব্যবহার।

### CSV-তে কী যায়
পর্দায় তখন যা দেখা যাচ্ছে (Restore/Delete হয়ে যাওয়া সারি বাদ দিয়ে, একই ক্রম) ঠিক তাই। কলাম: **SL · DATE · NAME · MOBILE · BRANCH · DISEASE · PATIENT ID · STATUS · LAST REMARK**।

### ফাইল
- **নতুন:** `DraftSheetExporter.kt` — `FollowUpSheetExporter.kt`-এর (খাতার সারি B69) হুবহু একই প্যাটার্নে লেখা।
- **পরিবর্তিত:** `DraftListActivity.kt` (বোতাম ওয়্যার + `downloadSheet()` ফাংশন, Follow-up-এর `downloadSheet()`-এর একই কাঠামো — `sheetBusy` গার্ড, ফাঁকা-তালিকা চেক, Toast, FileProvider দিয়ে Share)।
- **পরিবর্তিত:** `activity_draft_list.xml` (হেডারে নতুন বোতাম)।

⛔ **FileProvider-এর জন্য নতুন কোনো সেটআপ লাগেনি** — আগে থেকেই থাকা `cacheDir/exports` পথ (`file_paths.xml`-এর `data_exports` এন্ট্রি) ব্যবহার হয়, যা CsvExportHelper/FollowUpSheetExporter আগে থেকেই ব্যবহার করে।

⛔ **ডেটাবেসে কিছু লেখা হয় না, নতুন কোনো টেবিল/কলাম/SQL লাগে না, ক্লাউডে একটাও বাড়তি অনুরোধ নেই** — আগে থেকেই স্ক্রিনে-লোড-করা তালিকা থেকেই ফাইল বানানো হয়।

⛔ **যা ছোঁয়া হয়নি:** Restore/Delete/Call/WhatsApp/View বোতাম, তালিকার লজিক, ক্যাশ, Pull-to-refresh — সবকিছু আগের মতোই।

## ⚠️ ভার্সন নাম্বারিং — গুরুত্বপূর্ণ নোট
`versionCode` ১৯৯ থেকে ২০০-তে (প্রথমবার ৩-অঙ্কে) যাওয়ায় `versionName` প্যাটার্ন বদলেছে:
- আগে: versionCode-এর শেষ ২ অঙ্ক = versionName-এর "1.XX" অংশ (195→1.95, 199→1.99)
- এখন (সাধারণীকৃত একই সূত্র): versionName = `(versionCode÷100).(versionCode%100 দুই অঙ্কে)` → 200 → **"2.00"**

এটা **downgrade নয়** — শুধু সংখ্যার ধরন বদলেছে। পাহারাদারের এক-বারের যাচাই স্ক্রিপ্ট (`YACHAI_2026-07-30_V173_V174.py`)-ও এই নতুন সাধারণ সূত্র মেলাতে আপডেট করা হয়েছে (`00_GUARD/`)। মূল পাহারাদার (`tk_guard.py`) versionName-এর ফরম্যাট নিয়ে কোনো ধরে নেওয়া চেক করে না, তাই সেটা অক্ষত ও অপ্রভাবিত ছিল।

## 🔍 যাচাই
- পাহারাদার (`tk_guard.py`) **১৭/১৭ পাশ**
- YACHAI স্ক্রিপ্ট (সংশোধনের পরে) **৫৯/৫৯ পাশ**
- ভার্সন দুই জায়গায় এক — `build.gradle.kts` (200/2.00) ও `DashboardActivity.kt` (V200)
- ব্র্যাকেট/স্কোপ হাতে ও স্ক্রিপ্টে গুনে মেলানো (186টা Kotlin ফাইল)

## 🔴 এখনো বাকি
- **TK-এর লাইভ টেস্ট** — প্রতিটা Draft সেকশন থেকে "⬇ Sheet" চেপে দেখা
- **B190 — ঝুঁকিসহ লোডিং-ফিক্স** — পরের সেশনে আলোচনা করে ফাইনাল হবে
- **B148 — RLS** ⛔ TK-এর অনুমতি ছাড়া নিষেধ
- `03_NETLIFY_READY` Netlify-তে আপলোড (TK-এর কাজ)
