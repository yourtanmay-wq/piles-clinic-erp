# V522 — Report Card: এক নম্বরে দুজন রোগী থাকলেও ঠিক জনেরটাই
**তারিখ:** ২২.০৮.২০২৬ · **ভিত্তি:** V521 · **TK-এর নির্দেশ:** *"২ ধরুন — Report Card, নিরাপদে করবেন"*

---

## সমস্যা যেটা ছিল (কোডে প্রমাণিত, আন্দাজ নয়)

Report Card পর্দা খুলত **শুধু মোবাইল নম্বর** নিয়ে (`ReportCardActivity.kt:72`)।
V516-এর পরে এক নম্বরে স্বামী ও স্ত্রী **দুজন আলাদা রোগী** থাকতে পারেন — তখন
কার রিপোর্ট দেখাবে, অ্যাপ নিজেই বেছে নিত। ফলে Patient Timeline-এ স্ত্রীকে
দেখে "Report Card" চাপলে **স্বামীর রিপোর্ট** খুলে যেতে পারত, আর **ছাপাও হত
তাঁরই নামে**।

**আরও একটা লুকানো সমস্যা নিজে খুঁজে পেয়েছি** — `TimelineCache`-এর চাবি ছিল
**শুধু মোবাইল নম্বর** (`TimelineCache.kt:40`)। তাই স্বামীর Timeline দেখার
পরে স্ত্রীরটা খুললে **প্রথম মুহূর্তে স্বামীর জমানো তথ্যই আঁকা হত**
(cache-first, V216 §10)। আসল পড়া শেষ হলে ঠিক হয়ে যেত, কিন্তু ওই কয়েক
সেকেন্ড ভুল রিপোর্ট চোখের সামনে থাকত — **Patient Timeline পর্দাতেও একই ভুল**।

---

## যা করা হলো

### ১. `TimelineCache` — জমানো কপি এখন রোগী-প্রতি আলাদা
চাবিতে রোগীর আইডিও যোগ হলো।
⛔ আইডি ফাঁকা রাখলে চাবিটা **অক্ষরে অক্ষরে আগের মতোই** (`tl_<১০ সংখ্যা>`) —
তাই ফোনে **আগে থেকে জমা থাকা তথ্য নষ্ট হয় না**, পুরোনো সব ডাকও অবিকল চলে।
⛔ চাবি হয় **চাওয়া**-আইডি দিয়ে (পাওয়া-আইডি দিয়ে নয়), তাই জমানো ও খোঁজা
সবসময় একই চাবিতে মেলে — কোনো সারি "হারায়" না।

### ২. `ReportCardActivity` — কোন রোগী, সেটা এখন জানে
নতুন দুটো Intent-ঘর: `patientRowId` ও `patientCode`।
এই একটাই পরিচয় ব্যবহার হয় **চার জায়গায়** —
পর্দা আঁকা · জমানো কপি · **ছাপা** · টাকার সংশোধনের অনুরোধ।
⛔ দুটোই ফাঁকা রাখলে **হুবহু আগের আচরণ**।

### ৩. `PatientTimelineRepository.build()` — Official Patient ID দিয়েও চলবে
Chamber Attendance বোর্ড রোগীর **সারির আইডি** জানে না, কিন্তু **Official
Patient ID** জানে — সেটাও রোগী-প্রতি অনন্য। তাই `preferPatientCode` যোগ।
⛔ একই লুপ, **বাড়তি একটাও cloud-read নেই**।

**🔴 নিজের যাচাইয়ে একটা ভুল ধরা পড়েছে ও ঠিক করা হয়েছে:** প্রথমে দুটো শর্ত
একই লুপে রেখেছিলাম। তাতে দুটো আইডি একসাথে এলে ও তারা আলাদা রোগীকে দেখালে,
তালিকায় যে সারি **আগে** আছে সেটাই জিতে যেত — অর্থাৎ কোড দিয়ে সারির আইডি
হেরে যেত। **পরীক্ষাটাই এটা ধরেছে।** এখন দুই ধাপে: আগে সারির আইডি (সবচেয়ে
নির্দিষ্ট), না পেলে তবেই কোড।

### ৪. `ReportCardPrinter` — ছাপাও ঠিক রোগীরই
### ৫. `PatientTimelineActivity` — নিজের জমানো কপিও রোগী-প্রতি আলাদা, আর Report Card-কে পরিচয় পাঠায়
### ৬. ডাকার সব জায়গা এখন পরিচয় পাঠায়
| পর্দা | কী পাঠায় |
|---|---|
| Patient Timeline | সারির আইডি |
| Doctor Queue | সারির আইডি + Official ID |
| Chamber Attendance (২ জায়গা) | Official Patient ID |

### ৭. ওয়েব (কম্পিউটার)
ওয়েবের Report Card **আগে থেকেই আইডি-ভিত্তিক** (`wlv1ReportCard(p.id)`) —
তাই মূল সমস্যাটা সেখানে ছিল না। কিন্তু তার **পুরোনো মোবাইল-fallback**
তালিকার **প্রথম** সারিটা তুলে নিত, যেখানে ফোনে নয়টা পর্দা `pickPatientRow`
(V143) মানে। ফলে এক নম্বরে দুজন থাকলে **ফোন ও কম্পিউটার আলাদা রোগী**
দেখাতে পারত। তিন জায়গায় (Report Card · তার text সংস্করণ · Clinical History)
এখন সেই একই শেয়ার-করা নিয়ম।

---

## যাচাই

| যাচাই | ফল |
|---|---|
| V522 চাবি + রোগী-বাছাই — Android (আসল কোড হুবহু তুলে) | ২০/২০ PASS |
| V522 Report Card বাছাই — Web (আসল কোড তুলে) | ৮/৮ PASS |
| V521 Extra-income কারণ (Android ১৭ · Web ১৫) | ALL PASSED |
| V520 walk-in + offline (৩৪) · ওয়েব টাকা-আলাদা (১৪) | ALL PASSED |
| V516 Registration · V517 Search · V517 Timeline | ALL PASSED |
| ওয়েব: webfu · webfam · websearch · dq · mask | ALL PASSED |
| ৮টি ওয়েব ফাইল `node --check` | ৮/৮ OK |

পরীক্ষায় ব্যবহৃত কোড **আসল ফাইল থেকে হুবহু তুলে আনা** — নকল করে লেখা নয়।

## সৎ সীমাবদ্ধতা

**Activity ফাইল কম্পাইল করা যায়নি** — এই মেশিন থেকে Android SDK
(`dl.google.com`) পৌঁছয় না (403)। যে অংশগুলো compile করে চালানো গেছে:
`TimelineCache.key()` ও `build()`-এর রোগী-বাছাই — **দুটোই হুবহু তোলা ও
প্রমাণিত**। বাকিটা হাতে যাচাই: ব্যবহৃত প্রতিটা নাম কোডে খুঁজে দেখা
(`QueuePatient.id` = patients সারির আইডি, `ChamberAttendanceRow.patientId`
= Official ID, `preferPatientRowId`/`currentPatientCode` আগে থেকেই আছে),
আর ব্রেস-ভারসাম্য `git HEAD`-এর সঙ্গে মিলিয়ে দেখা (সব `+0`)।
**TK-কে বিল্ড করে একবার দেখে নিতে হবে।**

## ⛔ যেখানে কিছুই বদলায়নি
- Database schema · RLS · production data — **ছোঁয়া হয়নি**
- Supabase-এ **একটাও বাড়তি query নেই**
- ফোনে আগে জমা থাকা Timeline cache নষ্ট হয় না
- `PatientIdentity.pickPatientRow` (৯টা পর্দার শেয়ার করা নিয়ম, V143) অটুট
- খাতার সারি **B30** (ভুলে দুবার রেজিস্ট্রেশন) — বাছাই আগের নিয়মেই, পরীক্ষায় প্রমাণিত
