# 🔒 LOCK NOTE — V189 (৩০.০৭.২০২৬ রাত) · খাতার সারি **B173**

> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

**ভার্সন:** `versionCode 189` · `versionName 1.89` · পর্দায় **V189**
✅ B172-এর SQL TK ইতিমধ্যে Supabase-এ RUN করেছেন। এই ভার্সনে আর নতুন SQL লাগবে না।

---

## TK কী বলেছেন

> *"থাক বাদ দিন, পেসেন্ট কার্ডে লাগবে না ওগুলি, যেহেতু View All-এ চাপলে সব দেখা যাবে।"*

## প্রেক্ষাপট

Visit কার্ডের প্রুফে TK একটা সম্ভাব্য ঝুঁকি ধরিয়ে দিয়েছিলেন — নতুন ঠিকানা-ট্যাগ ADVANCE/TEST HERE বক্সের গায়ে ঘেঁষে যাচ্ছে বলে মনে হচ্ছিল।

**যাচাই করে দেখা গেল:** এটা আসল অ্যাপের বাগ নয়। কোডে `info` কলামের প্রস্থ Android-এর নিজের weight-নিয়মে (weight=1f, width=0) আগে থেকেই বাঁধা — সেটা `right` কলাম (ADVANCE/TEST HERE) যতটুকু জায়গা নেয় তার বাইরে কখনো যেতেই পারে না, এটা Android নিজেই নিশ্চিত করে। **আমার পাঠানো প্রুফ-ছবিটাই ভুল (বড়) সীমানা ধরে বানানো হয়েছিল** — কার্ডের একদম শেষ কিনারা পর্যন্ত জায়গা আছে ধরে নিয়েছিলাম, অথচ আসল সীমা তার আগেই।

সংশোধিত প্রুফে (আসল সীমা লাল দাগ দিয়ে দেখিয়ে) TK নিশ্চিত হন যে ওভারল্যাপ নেই। তারপর Patient কার্ডের প্রুফ দেখার পরে TK এই সিদ্ধান্ত নেন — Patient কার্ডে এই ফিচারটাই দরকার নেই, যেহেতু "View All" চাপলে সব বিস্তারিত এমনিতেই দেখা যায়।

## যা করা হলো

- **Patient (Treatment) কার্ডে ঠিকানা-ট্যাগের পিল আর দেখানো হয় না** — `isTreatment` সত্যি হলে `addressLabel` ফাঁকা রাখা হয়, তাই ট্যাগ-সারিতে ওটা যোগই হয় না।
- **Enquiry ও Visit কার্ডে আগের মতোই আছে** — কিছু বদলায়নি।
- ⚡ **Patient ট্যাব লোড হওয়ার সময় `address_tags` টেবিলের ব্যাচ-অনুরোধটাও আর করা হয় না** — `FollowUpRepository.fetchTab()`-এ `stage == "Treatment"` হলে ঠিকানা-ট্যাগ বসানোর ধাপে যাওয়ার আগেই ফেরত যায়। তাই বাড়তি একটা ক্লাউড-কলও বাঁচল।

## ⛔ যা ছোঁয়া হয়নি

- `address_tags` টেবিলের **ডেটা মোছা হয়নি** — শুধু Patient কার্ডে দেখানো বন্ধ। পরে আবার দেখাতে চাইলে এক লাইনের পরিবর্তন (`if (isTreatment) ""` অংশটা সরালেই)।
- Enquiry/Visit কার্ডের ঠিকানা-ট্যাগ, এডিট পপ-আপ, auto-ডিফল্ট — সবই অপরিবর্তিত।

## যাচাই (কাজের পরে আবার, TK-এর নিয়ম)

- নিজের হাতে ব্র্যাকেট গোনা — **২টা ফাইলেই পাশ**
- পাহারাদার `tk_guard.py` — **১৭টা যাচাই, সব পাশ**
- আগের অনুমোদিত কাজের যাচাই — **৫৯/৫৯ পাশ**
- ভার্সন চার জায়গায় এক — ZIP · `versionCode 189` · `versionName 1.89` · পর্দায় **V189**

**ফাইল:** `FollowUpActivity.kt` · `FollowUpRepository.kt` · `build.gradle.kts` · `DashboardActivity.kt` · `app.js` (শুধু ভার্সনের লেখা)

---

## 🔴 যা এখনো বাকি

**B148 — RLS** (⛔ TK-এর অনুমতি ছাড়া নিষেধ) · TK-এর লাইভ টেস্ট (Enquiry/Visit কার্ডে ঠিকানা-ট্যাগ, Patient কার্ডে যেন না দেখায়) · `03_NETLIFY_READY` Netlify-তে আপলোড (V184-এর কাজ) · সপ্তাহখানেক পরে Supabase-এর খরচ দেখা।
