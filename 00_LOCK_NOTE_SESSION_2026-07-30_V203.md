# 🔒 LOCK NOTE — V203 (৩০.০৭.২০২৬ রাত)

**ভার্সন:** `versionCode 203` · `versionName 2.03` · পর্দায় **V203** · খাতার সারি **B195**
⛔ **কোনো SQL লাগবে না।**

---

## ⛔ সবার আগে — স্থায়ী নিয়ম
> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

---

## পটভূমি
V202-এর কাজের সময় (সিঙ্ক-ব্যর্থতার আসল কারণ দেখানো) একটা পুরনো ঝুঁকি ধরা পড়েছিল ও TK-কে জানানো হয়েছিল: `deleteById()`-এর `catch (Exception)` ব্লকে কোনো `remember()` কল নেই — তাই সত্যিকারের নেটওয়ার্ক-এক্সেপশন হলে (শুধু HTTP ব্যর্থতা নয়) Delete-এর retry হারিয়ে যেতে পারে।

TK-এর নির্দেশ: **"সাবধানে কাজ করুন, সঠিক হয় যেন, কোনো ক্ষতি যেন না হয়।"**

## ✅ আসল কারণ ও প্রেক্ষাপট
খাতার সারি **B166**-এ (২৯.০৭.২০২৬) TK একবার এই একই সমস্যা রিপোর্ট করেছিলেন — *"ব্যর্থ Delete-এর স্থায়ী Retry নেই।"* তখন `deleteById()`-এর **`else` ব্লক** (HTTP উত্তর এসেছে কিন্তু সফল হয়নি) ঠিক করা হয়েছিল। কিন্তু **`catch (e: Exception)` ব্লক** (প্রকৃত ব্যতিক্রম — টাইমআউট, DNS ব্যর্থতা, সংযোগ হঠাৎ কেটে যাওয়া) তখন বাদ পড়ে গিয়েছিল — ঠিক দুর্বল নেটে Delete চাপলে যে পরিস্থিতির জন্য B166 বসানো হয়েছিল, সেটাই এই একটা পথে এখনও ঘটতে পারত।

## ✅ সমাধান (upsert()/updateById()-এর হুবহু একই প্যাটার্ন)

```kotlin
} catch (e: Exception) {
    val reason = (e.javaClass.simpleName + ": " + (e.message ?: "")).trim()
    try { CloudWriteQueue.remember("DELETE", table, id, null, reason) } catch (_: Throwable) { }
    false
}
```

## ✅ নিরাপত্তা যাচাই (TK-এর নির্দেশ অনুযায়ী সাবধানে)

| যাচাই | ফলাফল |
|---|---|
| আইডি ফাঁকা হলে ক্ষতি হতে পারে? | না — `remember()`-এর ভিতরেই `if ((kind == "UPDATE" \|\| kind == "DELETE") && id.isBlank()) return` আগে থেকেই আছে |
| দুবার retry হলে ভুল সারি মুছে যেতে পারে? | না — আইডি ধরে মোছা হয় (deterministic), একই আইডি দুবার পাঠালেও একই সারিই মোছে |
| Restore করা রেকর্ড আবার মুছে যেতে পারে? | না — Restore-এর সময় `DeletedGuard.unmark()` → `CloudWriteQueue.forget()` ডাকা হয়, তাই অপেক্ষমাণ এই Delete-কাজটাও মুছে যায় |
| return type/retry-ক্রম বদলেছে? | না — `Boolean` return type অক্ষত, `flush()`-এর retry loop-এ কোনো পরিবর্তন নেই |
| dedup/tries-গোনার জটিল লজিক (B164/B166) ছোঁয়া হয়েছে? | না — `remember()`-এর ভিতরের কোনো লজিক বদলানো হয়নি, শুধু একটা নতুন কল-সাইট যোগ হয়েছে |

## ⛔ যা ছোঁয়া হয়নি
`upsert()` · `updateById()` · `remember()`-এর ভিতরের লজিক · `flush()`-এর retry-ক্রম · dedup/tries-গোনা · Restore-এর নিয়ম — সবকিছু আগের মতোই।

## 🔍 যাচাই
- পাহারাদার (`tk_guard.py`) **১৭/১৭ পাশ**
- ব্র্যাকেট-গণনা `SupabaseClient.kt`-এ আলাদা করে মিলিয়ে দেখা হয়েছে
- এডিটের পরে `deleteById()` ফাংশনের পুরোটা আবার পড়ে নিশ্চিত হওয়া হয়েছে যে ঠিক জায়গাতেই বসেছে

## 🔴 এখনো বাকি
- **TK-এর লাইভ টেস্ট**
- **B190 — ঝুঁকিসহ লোডিং-ফিক্স** — পরের সেশনে আলোচনা করে ফাইনাল হবে
- **B148 — RLS** ⛔ TK-এর অনুমতি ছাড়া নিষেধ
- `03_NETLIFY_READY` Netlify-তে আপলোড (TK-এর কাজ)
