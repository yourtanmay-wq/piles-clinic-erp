# V698 — Check-up History-র WhatsApp PDF ভাঙা

**তারিখ:** ২৬.০৮.২০২৬ (IST)
**TK-এর রিপোর্ট (৩টে ছবিসহ):** *"কাজ হচ্ছে না"*
Check-up History → **Send on WhatsApp** → "Preparing PDF…" → যে PDF তৈরি হলো
তাতে লেখা **সরু কলামে**, **লোগো বিশাল হয়ে কাটা**, পাতাগুলো **প্রায় ফাঁকা**।

---

## ১. আসল কারণ (কোড ধরে, আন্দাজ নয়)

PDF বানানোর সময় একটা অদৃশ্য WebView-কে হাতে করে **৭৯৪ ডিভাইস-পিক্সেল**
চওড়ায় বসানো হয় (`PrescriptionWhatsAppShare.layoutAt()`), কারণ A4 = ৭৯৪ px
(৯৬ dpi)।

কিন্তু ওই WebView-এ **`useWideViewPort` চালু ছিল না**। তখন WebView পাতার
মাথার এই লাইনটা **পড়েই না** —

```
<meta name="viewport" content="width=794">     ← CheckupA4Report.kt
```

— বরং CSS-এর চওড়া ধরে নেয় **৭৯৪ ÷ ফোনের ঘনত্ব**। TK-এর ৩x ফোনে সেটা মাত্র
**~২৬৫ CSS px**।

ফল:

| | হওয়ার কথা | যা হচ্ছিল |
|---|---|---|
| পাতার চওড়া | ৭৯৪ px (A4) | ~২৬৫ px |
| ৭৮px-এর লোগো | পাতার ~১০% | পাতার **~৩০%** → বিশাল, কাটা |
| লেখা | পুরো চওড়ায় | সরু কলামে |
| উচ্চতার মাপ | ঠিক | ভুল → **বাড়তি ফাঁকা পাতা** |

তিনটে ছবির প্রতিটা লক্ষণ এই একটাই কারণে মেলে।

---

## ২. সমাধান

পাতা নিজে যদি একটা **নির্দিষ্ট চওড়া** চায় (viewport-এ `width=<সংখ্যা>`),
তবেই wide-viewport চালু করা হয়। তখন CSS চওড়া হয় ঠিক **৭৯৪**, আর
১ CSS px = ১ ডিভাইস px — অর্থাৎ হুবহু A4।

---

## ৩. ⛔ Prescription ও Medicine Slip ছোঁয়াই হয়নি

এই একই শেয়ার-পথ Prescription/Medicine Slip-ও ব্যবহার করে, তাই **খুব সাবধানে**
শর্ত বসানো হয়েছে। ওদের টেমপ্লেটে (`assets/www/rx_print.html`) লেখা আছে —

```
<meta name="viewport" content="width=device-width, initial-scale=1">
```

— কোনো **নির্দিষ্ট সংখ্যা নেই**, তাই শর্তটা ওদের ক্ষেত্রে মেলেই না, আর
আগের আচরণ **এক অক্ষরও বদলায় না**।

✅ পরীক্ষা করে দেখা হয়েছে (তিনটে ধরন):

| পাতা | ফল |
|---|---|
| Check-up (`width=794`) | wide-viewport **চালু** → ঠিক |
| Prescription (`width=device-width`) | **আগের আচরণ**, কিছু বদলায় না |
| viewport-ই নেই | **আগের আচরণ**, কিছু বদলায় না |

---

## ৪. ✅ যাচাই করে দেখা হলো (TK-এর নির্দেশে — *"আপনি যাচাই করে দেখুন"*)

### ক) কারণটা সত্যিই এটাই — মেপে দেখা

`CheckupA4Report.kt`-এর **আসল CSS হুবহু** নিয়ে (হাতে লেখা নয়) পাতাটা
দুই চওড়ায় রেন্ডার করে মাপা হলো —

| চওড়া | পাতার উচ্চতা | A4-এ কত পাতা লাগে |
|---|---|---|
| **২৬৫px** (আগে যা হত) | ৮৮৭px | **৩ পাতা** |
| **৭৯৪px** (এখন) | ৫৭৫px | **১ পাতা** |

🎯 **TK-এর পাঠানো ছবিতে PDF-এ ঠিক ৩টে পাতাই ছিল** — হিসাবের সাথে হুবহু
মিলে গেল। অর্থাৎ কারণটা এটাই, আন্দাজ নয়।
ছবির প্রমাণ: `V698_PROOF_PDF_FIX.png` (বাঁয়ে আগে, ডানে এখন)।
২৬৫px-এ লোগো বিশাল, "Bleeding per rectum" লেখাটা "Duration"-এর উপরে
উঠে গেছে — TK-এর ছবির লক্ষণগুলো হুবহু।

### খ) **A4 Print** — যাচাই করে দেখা গেল ওটা ভাঙা নয়

A4 Print আলাদা পথে যায় — Android-এর নিজের প্রিন্ট-ব্যবস্থা
(`createPrintDocumentAdapter`), যেটা নিজেই কাগজের মাপে পাতাটা আবার সাজায়।
প্রজেক্টে এই ব্যবস্থা **১১টা জায়গায়** ব্যবহার হয় (Prescription · Diet
Chart · Investigation · Registration · Statement · Report Card ইত্যাদি),
আর **কোনোটাতেই `useWideViewPort` বসানো নেই** — তবু সেগুলো বহু ভার্সন ধরে
ঠিকঠাক ছাপছে, TK কখনো অভিযোগ করেননি।

⇒ অর্থাৎ দোষটা প্রিন্ট-ব্যবস্থার নয়, দোষটা ছিল **হাতে-আঁকা**
(`view.draw(canvas)`) পথটায় — যেটা শুধু WhatsApp-এর PDF বানাতে ব্যবহার হয়।
**তাই A4 Print-এ হাত দেওয়ার দরকার নেই, দিলে বরং চালু জিনিস ভাঙার ঝুঁকি।**

---

## ৫. ⛔ যা ছোঁয়া হয়নি

- রিপোর্টের লেখা · সাজ · ভাষা-টগল (বাংলা/English) — কিছুই নয়।
- 💰 টাকা · হিসাব · ডেটা — কিছুই নয়।
- V639-এর সমাধান (WebView পর্দার সাথে যুক্ত রাখা) অক্ষত।

---

## ৬. যাচাই

| যাচাই | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ (V698) |
| `verify_kotlin_compile.py` | ✅ PASS — নতুন ভুল ০ |

---

## 🔴 এখনো TK-এর লাইভ টেস্ট বাকি

- [ ] Check-up History → **Send on WhatsApp** → PDF **পুরো A4 জুড়ে**, লোগো ঠিক মাপে
- [ ] বাড়তি ফাঁকা পাতা নেই
- [ ] **বাংলা ও English** — দুই ভাষাতেই ঠিক
- [ ] **Prescription** ও **Medicine Slip**-এর WhatsApp PDF **আগের মতোই** (এটা সবচেয়ে জরুরি)
- [ ] A4 Print কেমন বেরোচ্ছে — ভাঙা হলে জানাবেন
