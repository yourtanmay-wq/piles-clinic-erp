# ⌨️🔴 V774 — নম্বরের সাজেশন: **এতদিনের আসল ফাঁকটা পাওয়া গেছে** (২৮.০৮.২০২৬)

**TK:** *"যেকোনো পর্দাতে মোবাইল নাম্বার সাজেস্ট — এই জিনিসটা আর একটু ভালো করে
গভীরে যাচাই করুন, কারণ এই সমস্যা নিয়ে আমি বেশ কয়েকবার আপনাকে কমপ্লেন করেছি।"*

---

## ১. প্রথমে প্রমাণ — এটা Autofill **নয়**

`res/layout/activity_registration.xml`-এর মোবাইলের ঘরে **XML-এই** লেখা আছে:

```xml
<EditText android:id="@+id/etMobile" android:inputType="phone"
          android:importantForAutofill="no" android:autofillHints="@null" />
```

তার উপর পর্দার `decorView`-তেও `IMPORTANT_FOR_AUTOFILL_NO_EXCLUDE_DESCENDANTS`
বসে। **তবু TK-এর ফোনে ওই ঘরেও সাজেশন আসে।**

⇒ Autofill-এর সব দরজা বন্ধ থাকা সত্ত্বেও সাজেশন আসছে
⇒ **সাজেশনটা কীবোর্ডের নিজের পট্টি**, Android-এর Autofill নয়।

আগের চারবার (V418 · V752 · V758 · V761) আমি Autofill-এর দিকেই খুঁজেছি — **তাই
সারেনি।**

---

## ২. 🔴 আসল ফাঁক — কোড ধরে ধরা

কীবোর্ডকে থামানোর একমাত্র পতাকা **`IME_FLAG_NO_PERSONALIZED_LEARNING`**।
এর দুটো বৈশিষ্ট্য আগে খেয়াল করিনি:

1. এটা **প্রতিটা ঘরে আলাদা করে** বসাতে হয়। উপরের বাক্সে বসালে ভিতরের ঘরে
   **নামে না** (`importantForAutofill`-এর মতো উত্তরাধিকার নেই)।
2. পপ-আপের **নিজের আলাদা উইন্ডো** — পর্দার পাহারা ওখানে পৌঁছায় না।

আর কোডে যা ছিল:

```kotlin
override fun onActivityStarted(activity: Activity) { apply(activity) }   // scrub() এখানে
override fun onActivityResumed(activity: Activity) { apply(activity) }
```

🔴 **`scrub()` চলত শুধু পর্দা খোলার মুহূর্তে** — অর্থাৎ **তখন যে ঘরগুলো ছিল**
কেবল সেগুলোতেই পতাকা বসত।

**কিন্তু এই অ্যাপের প্রায় সব ঘরই পরে তৈরি হয়** — তথ্য ক্লাউড থেকে আসার পরে,
বোতাম চাপার পরে, তালিকা আঁকার পরে, পপ-আপ খোলার পরে।
⇒ **সেই ঘরগুলোতে পতাকা কখনোই বসত না।**

### এই এক কারণেই "যেকোনো পর্দাতে, যেকোনো ঘরে"।

---

## ৩. কী করা হলো

### ক) 📱 ফোনে — প্রতিটা layout-এর পরে আবার মিলিয়ে দেখা
`NoBengali`-র বহুদিনের প্রমাণিত পথ (`OnGlobalLayoutListener`) এখন
`NoAutofill`-এও। পরে বসানো ঘরও, **চোখে পড়ার আগেই**, পতাকা পেয়ে যায়।

- ⛔ যে ঘরে পতাকা আগেই বসেছে সেটা আর ছোঁয়া হয় না ⇒ খরচ নগণ্য।
- ⛔ ঘরটা যদি ঠিক তখন **খোলা** থাকে, একবার `restartInput()` — নইলে কীবোর্ড
  পুরনো নিয়ম ধরে বসে থাকত। ঘরপ্রতি একবারই, তাই বাংলা/হিন্দি টাইপ করায় ব্যাঘাত নেই।
- ⛔ `inputType`-এ **হাত দেওয়া হয়নি** — নইলে বাংলা লেখার সুবিধা নষ্ট হত।

### খ) 🪟 প্রতিটা পপ-আপেও
গুনে দেখা গেল **১২৫টা পপ-আপ** `PremiumAlert` দিয়ে যায় না — অর্থাৎ ওগুলোর
ঘরে পতাকা কখনো বসত না। সবক'টায় এখন `NoAutofill.scrubAnyDialog(...)` বসল
(৩০টা ফাইলে), আর পপ-আপের ভিতরেও প্রতিটা layout-এর পরে মেলানো হয়।

### গ) 💻 কম্পিউটারেও **হুবহু একই ফাঁক ছিল**
`wlv1NoSuggest()` চলত শুধু পর্দা/পপ-আপ **আঁকার সময়**। পরে যোগ হওয়া ঘর
(➕ Add Medicine · নতুন সারি) কখনো ঢাকা পড়ত না।
এখন একটাই **`MutationObserver`** — নতুন ঘর বসলেই সঙ্গে সঙ্গে বন্ধ।

**ব্রাউজারে চালিয়ে প্রমাণ:**
| যা যোগ করা হলো | ফল |
|---|---|
| পরে বসানো `<input>` | `autocomplete="off"` ✅ |
| নতুন বাক্সের ভিতরের `<input>` | `autocomplete="off"` ✅ |
| নতুন বাক্সের ভিতরের `<textarea>` | `autocomplete="off"` ✅ |
| **পাসওয়ার্ডের ঘর** | **ছোঁয়াই হয়নি** ✅ (ব্রাউজারে সেভ করা পাসওয়ার্ড অক্ষত) |

### ঘ) 🛡️ পাহারা ৯.৩০ — যেন আর কখনো ফিরে না আসে
- `NoAutofill`-এর তিনটে খুঁটি (`scrubAnyDialog` · `keepScrubbing` +
  layout-listener · `restartInput`) — একটাও সরলে guard **FAIL**।
- কোনো পপ-আপ `PremiumAlert.paint` বা `scrubAnyDialog` — দুটোর একটাও না ডাকলে
  guard **FAIL**।
- ✅ **ফাঁদ-পরীক্ষা করা হয়েছে:** একটা জায়গা থেকে ইচ্ছে করে সরিয়ে দিলে guard
  ফাইলের নাম ও লাইন নম্বরসহ ধরে ফেলে।

---

## ৪. এখন মোট কয়টা স্তর

| স্তর | কী থামায় | কবে |
|---|---|---|
| ১ | Autofill — পর্দার পুরো বাক্সে | V418/V758 |
| ২ | কীবোর্ডের personalized learning — ঘরে ঘরে | V752 |
| ৩ | ফোকাস পড়লেই `cancel()` | V761 |
| ৪ | যেকোনো উইন্ডোর জাল (`AutofillCallback`) | V772 |
| ৫ | **কপি করা নম্বর গোপন** (ক্লিপবোর্ড-চিপ) | V772 |
| ৬ | **পরে তৈরি হওয়া প্রতিটা ঘর + প্রতিটা পপ-আপ** ⬅ আসল ফাঁক | **V774** |

---

## ৫. ⚠️ একটা জিনিস কোনো অ্যাপই পারে না — সৎভাবে বলছি

কীবোর্ড **আগে যা শিখে ফেলেছে** (গত কয়েক মাসে টাইপ করা নম্বর), সেটা কোনো
অ্যাপ মুছতে পারে না। নতুন করে আর শিখবে না (V752+V774), কিন্তু পুরনো শেখা
মোছাতে ওই ফোনে **একবার** এটা করতে হবে:

> **Settings → Gboard (বা যে কীবোর্ড) → Dictionary → Delete learned words**
> এবং **Gboard → Clipboard → "Show recently copied text…" বন্ধ**

এটা ফোনপ্রতি একবারের কাজ। এরপর অ্যাপ নিজে থেকেই আর কিছু শিখতে দেবে না।

---

## ৬. যাচাই

| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py` (৩৪টা, নতুন ৯.৩০ সহ) | ✅ পাশ |
| ৯.৩০-এর ফাঁদ-পরীক্ষা | ✅ ধরা পড়ল |
| `verify_android_resources.py` | ✅ পাশ |
| `verify_kotlin_compile.py` | ✅ নতুন ভুল **০** (১২৫টা বদলের পরেও) |
| `node --check app.js` | ✅ পাশ |
| ব্রাউজারে MutationObserver — ৪টে কেস | ✅ সব পাশ |
