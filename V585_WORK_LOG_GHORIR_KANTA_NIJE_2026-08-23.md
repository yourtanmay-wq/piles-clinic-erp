# V585 — ঘড়ির কাঁটা নিজে থেকে হিসাব
**তারিখ:** ২৩.০৮.২০২৬ · **ভার্সন:** V585 · **অবস্থা:** কোড শেষ, লাইভ টেস্ট বাকি

---

## TK যা ধরেছেন

স্ক্রিনশটে দশটা চিহ্নেই লেখা ছিল **"4টা"**। TK: *"যেখানেই থাকবে চারটা কেন বাঁচবে — এটাতো অটোমেটিক্যালি হওয়ার কথা"*।

**আসল কারণ (কোডে পাওয়া):** `askPileLabel()` ডাকা হত **"চিহ্ন" হাতিয়ার বাছার সময়, একবার**। ডাক্তার যেটা বাছতেন সেটা `view.pileLabel`-এ জমা হত, আর এরপর **প্রতিটা** চিহ্নে সেই একই লেখাই বসত (`AnatomyView.kt`, পুরনো লাইন ৩১২)।

---

## এখন কী হয়

| | ফোন | ওয়েব |
|---|---|---|
| চিহ্ন যেখানে বসল, সেখান থেকেই o'clock হিসাব | ✅ | ✅ |
| 12 = ছবির একদম উপর, ঘড়ির কাঁটার দিকে (TK-এর বাছাই) | ✅ | ✅ |
| নতুন **"কেন্দ্র"** হাতিয়ার — একবার ছুঁয়ে দিলে ওই ছবির জন্য মনে থাকে | ✅ | ✅ |
| হাতে আঁকা ছক (anat26) ও 3D মডেল (anat27) — কেন্দ্র আগে থেকেই বসানো | ✅ | ✅ |
| পর্দায় হালকা সবুজ ঘড়ি — কোন দিক কত o'clock চোখে দেখা যায় | ✅ | ✅ |
| পুরনো তালিকা/prompt আর খোলে না — এক ছোঁয়াতেই চিহ্ন | ✅ | ✅ |

**কেন কাজটা ছোট হলো:** o'clock-এর লেখাটা ঠিক **সেই ঘরেই** (`Mark.label`) বসে যেখানে আগে পপ-আপের বাছাই বসত। তাই A4 রিপোর্ট · 📜 History · প্রিন্ট · সেভ — নিচের কিছুই ছুঁতে হয়নি।

---

## কেন্দ্র কোথা থেকে আসে

- **anat26 · anat27** — ছবির ভিতরের বৃত্ত/পুচ্ছ **পিক্সেল মেপে** বার করা (চোখে আন্দাজ নয়): anat26 = (49.8, 28.7), anat27 = (50.0, 52.0)।
- **বাকি সব ছবি** — ডাক্তার প্রথমবার **একবার ছুঁয়ে** দেবেন। ওই ছবির জন্য জমা থাকে, আর কখনো জিজ্ঞাসা করে না।
  > ⚠️ **সৎ কারণ:** ৩১টা ছবি খুঁটিয়ে দেখা হয়েছে। আসল রোগীর ফটোয় মাংস বেরিয়ে থাকায় পায়ুপথের মুখ প্রায়ই ঢাকা, ক্যামেরার কোণও একেক রকম। চোখে আন্দাজ করে কেন্দ্র বসালে সেটা একটু সরে গেলেই **প্রতিটা o'clock ভুল হয়ে রেকর্ডে বসে যেত** — এখনকার দোষের চেয়েও খারাপ। TK-কে ডেমোতে জানিয়ে অনুমতি নেওয়া হয়েছে।

**কোথায় জমা:** ফোনে `SharedPreferences`, ব্রাউজারে `localStorage`। **একটাও ক্লাউড-কল নেই** — free-plan-এ বাড়তি egress শূন্য।

---

## নতুন ও বদলানো ফাইল

**নতুন:** `clinical/AnatomyClock.kt` · `00_GUARD/verify_v585_clock.py`
**বদলানো:** `clinical/AnatomyView.kt` · `clinical/AnatToolIcon.kt` · `clinical/DoctorCheckupActivity.kt` · `03_NETLIFY_READY/app.js` · `version.json` · `build.gradle.kts`

⛔ `askPileLabel()` ফাংশনটা **মোছা হয়নি** (TK-এর নিয়ম: নিজে থেকে কিছু সরাই না) — শুধু আর ডাকা হয় না। `pileLabel` ঘরটাও রয়ে গেছে।

---

## যাচাই (মেশিনে চালিয়ে, অনুমানে নয়)

| পাহারা | ফল |
|--------|-----|
| **আসল Kotlin চালিয়ে বনাম আসল ওয়েব চালিয়ে** | ১৬টা বিন্দুর হিসাব **হুবহু এক** ✅ |
| `verify_v585_clock.py` (নতুন) | ২৫/২৫ ✅ |
| `verify_kotlin_compile.py` | নতুন ভুল **০** ✅ |
| `verify_android_resources.py` | সব রিসোর্স আছে ✅ |
| `tk_guard.py` + বাকি ১২টা পাহারা | সবগুলো সবুজ ✅ |
| `node --check app.js` | ঠিক ✅ |
| ভার্সন | version.json ও build.gradle.kts — দুটোই **V585 / 5.85** ✅ |

**ডেমোতে প্রমাণ:** 3D মডেলের ছবিতে (anat27) আগে থেকেই ১–১২ নম্বর ছাপা আছে। চিহ্ন বসিয়ে দেখা গেছে অ্যাপের হিসাব করা লেখা (12টা · 2টা · 3টা · 6টা · 9টা · 10টা) **ছাপা নম্বরের গায়েই** পড়ে।

---

## লাইভ টেস্টে যা দেখতে হবে

1. হাতে আঁকা ছকে (TK যেটা ব্যবহার করেন) সরাসরি চিহ্ন দিন — আলাদা আলাদা o'clock বসবে, সব "4টা" নয়।
2. আসল ফটোতে প্রথমে **⊕ কেন্দ্র** দিয়ে পায়ুপথের মাঝখানে ছুঁয়ে দিন, তারপর চিহ্ন।
3. একই ছবিতে আবার এলে কেন্দ্র আর চাইবে না।
4. A4 রিপোর্টে ভাগ ৬-এ লেখাগুলো ঠিক আসছে কিনা।
