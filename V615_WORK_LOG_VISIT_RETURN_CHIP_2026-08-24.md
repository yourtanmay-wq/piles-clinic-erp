# V615 — "Visit Return করা হল" — নতুন কুইক-চিপ
**তারিখ:** ২৪.০৮.২০২৬ · **ভার্সন:** V615 / 6.15

## TK-এর নির্দেশ
Treatment Progress-এর কুইক-চিপ তালিকায় ("CHECK-UP করা হলো", "KTA করা হল"
ইত্যাদির পাশে) নতুন একটা: **"Visit Return করা হল"**।

## সমাধান — একটাই জায়গায় যোগ, চারটে স্ক্রিনেই আপনা-আপনি
Android-এ `TreatmentQuickNotes.kt` এই তালিকার **একমাত্র উৎস** — চারটে
আলাদা বাক্সই (Chamber বোর্ড, Close Chamber-এর ফাঁকা-প্রগ্রেস পপ-আপ,
Review পর্দার তিন-চাপ, Report Card) এখান থেকেই তালিকা নেয়। তাই একবার
যোগ করাতেই সবগুলোতে দেখা যাবে।

## যা যোগ হলো (৩ জায়গায়, সব মিলিয়ে সামঞ্জস্যপূর্ণ)
1. **`TreatmentQuickNotes.kt`** — বাংলা তালিকায় "Visit Return করা হল",
   ইংরেজি/হিন্দি তালিকায় (KNE-KISHAN5-এর জন্য) "Visit Return done /
   विज़िट रिटर्न हो गया"।
2. **`PrintTextEnglish.kt`** (Android ছাপা) — "Visit Return করা হল" →
   "VISIT RETURN DONE" (B74-এর নিয়ম, কাগজে বাংলা যাবে না)।
3. **`app.js`** (ওয়েব) — `WLV1_TREAT_QUICK_BN`/`WLV1_TREAT_QUICK_ENHI`
   (চিপ তালিকা) ও `WLV1_PRINT_EN` (ওয়েবের ছাপা-অনুবাদ) — তিন জায়গাতেই,
   Android-এর সাথে হুবহু মিলিয়ে (ফাইলের নিজের কমেন্ট-নিয়ম অনুযায়ী,
   নইলে ফোন আর কম্পিউটারের কাগজে দুই রকম ছাপা হতো)।

## নিরাপত্তা
⛔ বাকি ৯টা লেখা এক অক্ষরও বদলায়নি — শুধু ১০ নম্বর যোগ হলো।
⛔ হাতে লেখার ঘরের আচরণ অপরিবর্তিত — চিপ শুধু টেক্সটে জোড়ে।

## যাচাই
| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ |
| `node -c app.js` | ✅ JS সিনট্যাক্স ঠিক |
| `verify_version_json.py` | ✅ V615/6.15 |
