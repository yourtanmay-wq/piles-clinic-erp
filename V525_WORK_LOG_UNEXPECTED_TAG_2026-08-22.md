# V525 — "Unexpected Time" ট্যাগ ফিরে এলো
**তারিখ:** ২২.০৮.২০২৬ · **ভিত্তি:** V524
**TK-এর প্রশ্ন:** *"Unexpected time ট্যাগটা কিভাবে সরে গেল?"*

---

## প্রথমে সত্যতা — এটা এই session-এর কোনো কাজের ফল নয়

| যাচাই | ফল |
|---|---|
| ট্যাগ আঁকার ফাইল `FollowUpAdapter.kt`-এর কমিট | **একটাই — প্রথম import** (`3bfb9a9`) |
| কার্ডের layout (`tvTimeType`) ছোঁয়া হয়েছে? | না |
| `FollowUpModel.kt`-এ `timeType`-এ পরিবর্তন | ০ |
| `FollowUpRepository.kt`-এ `timeType`-এ পরিবর্তন | ০ |
| `SupabaseClient.kt`-এ `timeType`-এ পরিবর্তন | ০ |
| `EnquiryModel.kt` · `EnquiryActivity.kt`-এ পরিবর্তন | ০ |

আরও একটা প্রমাণ: TK-এর আজকের ছবিতে Staff কার্ডে **View বোতামটা এখনো আছে** —
সেটা V521-এ সরানো হয়েছে। অর্থাৎ **ফোনে এই session-এর কোড এখনো যায়নি**।

⇒ ট্যাগটা "সরে যায়নি" — **কম্পিউটার থেকে করা এনকোয়ারিতে ওটা কোনোদিনই আসেনি।**

---

## আসল কারণ (কোডে প্রমাণিত)

ফোনের Enquiry কার্ডের 🌙 Unexpected / 🕘 Official ট্যাগ আসে **`followups`
সারির `timeType`** ঘর থেকে (`FollowUpAdapter.kt`, লাইন ১১৬–১২৯)।

| সারিটা কে বানায় | `timeType` লেখে? |
|---|---|
| **ফোনের অ্যাপ** — `EnquiryModel.buildFollowUpRow()` লাইন ৯২ | ✅ হ্যাঁ |
| **কম্পিউটার (ওয়েব)** — `ensureFollow()` app.js লাইন ৪৩৮৮ | ❌ **না** |

ওয়েবের ওই সারিতে ঘরটা **কখনোই ছিল না** — V512 থেকেই। তাই কম্পিউটার থেকে
করা এনকোয়ারির কার্ডে ট্যাগ আসত না। UTPAL DAS-এর কার্ডটা সেরকমই।

---

## দুটো সমাধান

### ১. ওয়েব এখন ঘরটা লেখে
`ensureFollow()` এখন উৎস-সারি থেকে `timeType` কপি করে — ঠিক যেভাবে ফোন করে।
⛔ উৎসে না থাকলে ফাঁকা যায় ⇒ আচরণ হুবহু আগের, কিছুই ভাঙে না।

### ২. **আগে জমা হয়ে যাওয়া** কার্ডেও ট্যাগ ফিরে আসে
ঘরটা ফাঁকা হলে ফোনের অ্যাপ এখন **এনকোয়ারির নিজের সারি** থেকে পড়ে নেয়।
⛔ **বাড়তি একটাও cloud-read নেই** — Enquiry ট্যাব ওই তালিকাটা
   (`preEnquiries`) **আগে থেকেই** এনে রাখে। ঠিক যেভাবে পাশের
   `branchFromEnquiries()` ফাঁকা branch ভরে — একই তালিকা, একই প্রমাণিত নিয়ম।
⛔ ঘর ভরা থাকলে **কিছুই ছোঁয়া হয় না** (পরীক্ষায় প্রমাণিত: একই বস্তুই ফেরে)।
⛔ Enquiry ছাড়া অন্য ট্যাবে **এক অক্ষরও বদলায় না**।
⛔ **কোনো সারি লেখা/বদলানো হয় না** — শুধু দেখানোর সময় ভরা হয়।
⛔ **টাকার হিসাব সম্পূর্ণ অস্পৃশ্য** — Extra Income `patients.timeType`
   দেখে (V418-এর SQL), `followups`-এরটা নয়।

---

## যাচাই

| যাচাই | ফল |
|---|---|
| V525 ট্যাগ (আসল কোড ও ট্যাগ-নিয়ম হুবহু তুলে) | ২১/২১ PASS |
| **আসল `FollowUpRepository.kt` কম্পাইল করে V518 suite** | **ALL PASSED** |
| V524 ঘোরানো · V523 Reports · V522 Report Card · V521 · V520 | ALL PASSED |
| V516 Registration · V517 Search · Timeline | ALL PASSED |
| ওয়েব: webfu · webfam · websearch · dq · pay · why · mask · rc | ALL PASSED |
| ৮টি ওয়েব ফাইল `node --check` | ৮/৮ OK |

এবার **আসল ফাইলটাই কম্পাইল হয়েছে** (শুধু brace-গোনা নয়) — তাই এই
পরিবর্তনটা বিল্ডে আটকাবে না, সেটা প্রমাণিত।

## সৎ সীমাবদ্ধতা
- **ওয়েবের পুরোনো সারিগুলো** ক্লাউডে এখনো ঘর-ফাঁকা অবস্থায়ই আছে। ফোনে
  ট্যাগ দেখা যাবে (উপরের ২ নম্বর), আর কম্পিউটারে ওই কার্ড পরের বার ছোঁয়া
  হলে ঘরটা নিজে থেকেই ভরে যাবে। **কোনো SQL চালাতে হবে না।**
- Activity ফাইল কম্পাইল করা যায় না (Android SDK পৌঁছয় না) — তবে এবারের
  পরিবর্তন Repository-তে, আর **সেটা কম্পাইল করে দেখা হয়েছে**।
