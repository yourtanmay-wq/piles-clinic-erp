# V318 — সম্পূর্ণ প্রজেক্ট রিপোর্ট (সহজ বাংলা)

তারিখ: ১১.০৮.২০২৬ · ভার্সন: **V318** (versionCode 318 / versionName 3.18)
ভিত্তি: V317-এর উপর। আগের নিরাপদ ভার্সন = V317/V316।

## ⭐⭐ V318-এর নতুন কাজ (B630) — Doctor/RMP: instant চেক + একাধিক নম্বর

Dr. Visit → Add/Edit Doctor ফর্মে:
- মোবাইলে **১০ ডিজিট বসানোর সাথে সাথেই** নিচে দেখাবে নম্বরটা আগে সেভ আছে কিনা (থাকলে কোন ডাক্তারের)। আগে থাকলে **Save আটকাবে** (ডুপ্লিকেট ডাক্তার এড়াতে)।
- **"＋ Add another number"** দিয়ে একই ডাক্তারের **একাধিক নম্বর** সেভ; প্রতিটাও সাথে সাথে চেক হয়। Edit ফর্মে "Other numbers (comma separated)" দিয়ে বিদ্যমান ডাক্তারেও নম্বর যোগ।
- ⛔ মূল নম্বর (কল/খোঁজা/referral) অটুট; বাড়তি নম্বর নতুন `altMobiles` কলামে।

🔴🔴 **আগে ছোট এক SQL চালাতে হবে** (নতুন কলাম): `04_SUPABASE_DATABASE_SETUP/V318_doctor_altMobiles_2026-08-11.sql` — তারপর V318 ইনস্টল। (নিরাপত্তা: বাড়তি নম্বর না দিলে অ্যাপ altMobiles লেখেই না, তাই SQL ছাড়াও সাধারণ ডাক্তার-সেভ চলবে।)

---

## ⭐⭐ V317-এর নতুন কাজ (B629) — Staff Salary: মাস বেছে যোগ + স্যালারি-তারিখে reminder

সত্যি কথা: **স্যালারি দেওয়া ও History আগে থেকেই অ্যাপে ছিল** (Staff Profiles → Salary)।
"not set/disabled" দেখাচ্ছিল কারণ কারো স্যালারি সেট করা হয়নি। **স্যালারি সেট করার নিয়ম:**
Salary স্ক্রিনে উপরের বক্সে **তিনবার চাপ** → Amount + Salary Date + enabled → Save।

এই ভার্সনে নতুন **২টো** জিনিস যোগ হলো:
1. **"Add Salary — choose month"** বোতাম — মাস বেছে (জয়েনিং ডেট থেকে সব মাস) + Amount + Mode
   দিয়ে যেকোনো (পুরনো) মাসের স্যালারি রেকর্ড করা যায়। মানে **আপনি নিজেই জয়েনিং থেকে
   আজ পর্যন্ত History ভরতে পারবেন** — SQL লাগবে না। (জয়েনিং ডেট `staff_profiles`-এ আগেই আছে।)
2. **স্যালারি-তারিখে reminder** — যার স্যালারির তারিখ পেরিয়েছে অথচ এ মাসে দেওয়া হয়নি,
   তা **Master ও Doctor দুজনের** ঘণ্টায় (bell) + **Briefing-এ "Salary Due" তালিকা**-তে দেখাবে।
   Master "➕ Pay Salary" চাপলে সরাসরি ওই স্টাফের Salary পর্দা খোলে। Doctor শুধু দেখেন।

⛔ **নতুন কোনো টেবিল/SQL লাগবে না** — বিদ্যমান `salary_config`/`salary_payments` পড়েই।
⛔ পুরনো Salary পে/History/সেট — সব আগের মতোই, কিছু ভাঙেনি। calendar-emoji বসানো হয়নি।

শুধু **V317 বিল্ড করে ফোনে ইনস্টল** করলেই চলবে।

---

## (আগের) V316 কাজ (B628) — Referral Income এডিট/ডিলিট (তিনবার-চাপ)

DoctorVisit → View All → **Referral Income এন্ট্রিতে তিনবার চাপ দিলে এডিট-বক্স** খোলে
(Add-এর হুবহু ইংরেজি ডিজাইন — টাকা + Paid/Unpaid + **Delete**)। সেভ করলে
Ref. Paid/Due নিজে থেকে ঠিক হয়।
- **মাস্টার:** যেকোনো সময়, সরাসরি।
- **স্টাফ/ডাক্তার:** একই দিনে সরাসরি; **দিন পেরিয়ে গেলে মাস্টারের Approve লাগবে**
  (বেল/Briefing-এ "Pending Referral Edit Requests"-এ মাস্টার Approve/Reject করবে)।
- referral সারিতে **একবার চাপ = আগের মতোই Report Card** খোলে (কিছু হারায়নি)।

🔴🔴 **এটা কাজ করতে আগে একটা ছোট SQL চালাতে হবে** (নতুন টেবিল তৈরি):
`04_SUPABASE_DATABASE_SETUP/V316_referral_edit_requests_2026-08-11.sql` — একবার
Supabase-এ চালান, তারপর V316 বিল্ড ইনস্টল করুন। (SQL পুরনো কোনো ডেটা/লগইন/রোল
বদলায় না; RLS চালু করে না।)

---

## ১) V315/V316-এ কী কী আছে

**Android (আগের V314-এর মতোই, বদলায়নি):**
- **B625 (egress):** ব্যাকগ্রাউন্ড pre-warm এখন Doctor Queue **ছবি ছাড়া** টানে; ছবি শুধু কেউ স্ক্রিন খুললে নামে; ব্যবধান ১২→৩০ মিনিট; জমানো ছবি cache-এ অটুট।
- **B624 (ব্যাকআপ):** অটো-ব্যাকআপ শুধু মাস্টার-ফোনে, সপ্তাহে একবার; স্টাফ-ফোনে পুরো-DB ব্যাকআপ নেই; Manual Backup আগের মতো।

**ওয়েব (এই ভার্সনে যুক্ত — Netlify redeploy লাগবে):**
- **B626:** Reject করা এনকোয়ারি আর ফিরে আসবে না; একই এনকোয়ারিতে ডুপ্লিকেট followup আর জমবে না।
- **B627:** ওয়েবে **অটো লগ-আউট** — ১৫ মিনিট নিষ্ক্রিয় থাকলে (ক্লিক/টাইপ/স্ক্রল না হলে) নিজে থেকে লগ-আউট; লগ-আউটের আগে বাকি কাজ সেভ করে; লগ-আউটে **লাইভ সংযোগ (realtime) বন্ধ** হয় (egress বাঁচে); **আবার লগইন করলে লাইভ সংযোগ নিজে থেকে ফিরে আসে।**

**ফিরিয়ে আনা (V314-এ ভুলে বাদ পড়েছিল):**
- `MAA_PILES_ANDROID_SOURCE_v296.zip` ও `MAA_PILES_WEB_NETLIFY_v299.zip` — এই দুটো ফাইল আবার প্রজেক্টে আছে। (V314 প্যাকেজে `.zip` বাদ দেওয়ার কারণে পড়ে গিয়েছিল; এবার ঠিক।)

---

## ২) কী কী **বদলানো/মোছা হয়নি** (নিশ্চিতকরণ)

কোনো ফিচার, স্ক্রিন, বোতাম, workflow, হিসাব, রিপোর্ট, প্রিন্ট, রোল, ব্রাঞ্চ-নিয়ম,
রোগীর রেকর্ড, পেমেন্ট, **রোগীর ছবি**, মেডিকেল রেকর্ড, অফলাইন সিস্টেম, pending-sync,
ব্যাকআপ সিস্টেম, বা সিকিউরিটি/লগইন-নিয়ম — **কিছুই সরানো/বন্ধ/বদলানো হয়নি।**
ডেটাবেসের গঠন/RLS/পাসওয়ার্ড/রোল-ব্রাঞ্চ অনুমতি — কিছুই ছোঁয়া হয়নি।
রোগীর ছবি Registration, Patient Details, History, প্রিন্ট — সব জায়গায় আগের মতোই।

---

## ৩) বদলানো ফাইলের তালিকা (V313 → V315)

Android (B625 · versionCode 315):
1. `.../native/BackgroundRefreshWorker.kt`
2. `.../native/DoctorQueueRepository.kt`
3. `.../app/build.gradle.kts` (versionCode 314→315, versionName 3.15)

ওয়েব (B626 + B627):
4. `03_NETLIFY_READY/app.js`
5. `03_NETLIFY_READY/index.html` (app.js cache v346→v348)

নথি/রেফারেন্স:
6. `00_TK_KAJER_KHATA_SOBAR_AGE_PORUN.md` (সেশন-আলোচনা তারিখ-সময় সহ + B625/B626/B627)
7. `04_SUPABASE_DATABASE_SETUP/CLEANUP_2026-08-11_reject_enquiries_resurrect.sql` (রেফারেন্স; আগেই চালানো, নতুন করে দরকার নেই)

---

## ৪) যাচাই-রিপোর্ট (কোড-স্তরে)

- ✅ Kotlin brace/paren ব্যালান্স: BackgroundRefreshWorker.kt (0,0), DoctorQueueRepository.kt (0,0)।
- ✅ Doctor Queue **স্ক্রিন-পথের কলাম-তালিকা V313-এর সাথে হুবহু এক** (ছবি সহ) — স্ক্রিনে কিছু বাদ পড়ে না; শুধু background ছবি ছাড়া টানে। সব caller (screen/background) মেলে।
- ✅ `node --check app.js` — পাশ (B626 + B627 সহ)।
- ✅ প্রজেক্ট-গার্ড (`tk_guard.py`): ব্যর্থতা-তালিকা V313-এর সাথে **হুবহু এক (২১টি পুরনো লক করা baseline)** — আমার বদলে **একটাও নতুন সমস্যা নেই**।
- ✅ versionCode 315 / versionName 3.15 নিশ্চিত; দুটো MAA zip প্রজেক্টে আছে।

**সৎ সীমা (Android Studio বিল্ড):** এই পরিবেশে **Android SDK নেই**, তাই এখানে সত্যিকারের Gradle/APK বিল্ড চালানো যায়নি — সেটা আপনার Android Studio-তেই হবে। তবে বিল্ড-এররের ঝুঁকি কমাতে যা করা সম্ভব সব করা হয়েছে: Android-এ V314-এর পর **নতুন কোনো কোড বদল নেই** (শুধু versionCode বাড়ানো), আর B625-এর বদল brace/caller/কলাম মিলিয়ে যাচাই করা। মিথ্যা "ফোনে/বিল্ডে টেস্ট করেছি" দাবি করা হয়নি।

---

## ৫) আপনার ফোনে লাইভ-টেস্ট চেকলিস্ট (সংক্ষিপ্ত)

- [ ] Android Studio-তে বিল্ড হয় (এরর ছাড়া) ও APK ইনস্টল হয়।
- [ ] Master / Staff / Doctor লগইন; ব্রাঞ্চ-নিয়ম আগের মতো।
- [ ] **Doctor Queue খুললে রোগীর ছবি দেখায়** (স্ক্রিন খুললে ছবি আসে)।
- [ ] Enquiry/রেজিস্ট্রেশন/ছবি/Check-up/স্লিপ/প্রেসক্রিপশন/পেমেন্ট/রিফান্ড/Follow-up/রিপোর্ট/প্রিন্ট/Trash — সব ঠিক।
- [ ] অফলাইন সেভ → নেট এলে নিজে থেকে সিঙ্ক; কিছু হারায় না।
- [ ] (ওয়েব redeploy করলে) ১৫ মিনিট নিষ্ক্রিয় থাকলে অটো লগ-আউট; আবার লগইনে লাইভ আপডেট ফেরে।
- [ ] ২–৩ দিন Supabase → Usage → Egress কমছে কিনা।

---

## ৬) Rollback (নিরাপত্তা)

সমস্যা হলে **V314/V313**-ই আগের নিরাপদ ভার্সন — ফিরে গেলে সব আগের মতো চলবে।
V315 কেবল ওয়েবে ২টো ফিক্স + version যোগ করেছে; কোনো ডেটা/গঠন বদলায়নি, তাই ফিরে যাওয়া সম্পূর্ণ নিরাপদ।

---

*তৈরি: Claude (claude-opus-4-8), Anthropic — TK BISWAS-এর নির্দেশে, ১১.০৮.২০২৬ ~৩:২৫ PM IST।*
