# V258 LOCK NOTE (B379–B380)

Base: V257 (uploaded PILES_CLINIC_APP_V257_FINAL.zip).

এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না,
কোনো working flow খারাপ করা যাবে না।

## এই ডেলিভারিতে যা হয়েছে (B379 → B380)

TK-এর নির্দেশে **সম্পূর্ণ প্রজেক্ট সেকশন-ধরে-ধরে যাচাই করা হয়েছে** — কোনো নতুন
ফিচার/ডিজাইন/লজিক বদলানো হয়নি। শুধু দুটো জিনিস ঠিক হয়েছে:

1. **রুট ফোল্ডারের নাম** — ভুল করে "V256_FINAL" থেকে যাচ্ছিল, এখন সঠিক
   "V258_FINAL" (TK-এর D নিয়ম)।
2. **ভার্সন** — 257 → **258** (নিয়ম: একই ZIP-নাম/ভার্সন দুইবার পাঠানো যাবে না)।

## যাচাই করা হয়েছে (আন্দাজ না, টুল চালিয়ে)

- `00_GUARD/tk_guard.py --release` — **সব ✅ পাশ** (ব্র্যাকেট ১৯৭ ফাইল, XML ২৩২
  ফাইল, Supabase কলাম, লক করা নিয়ম ২৪টা, বাংলা-বন্ধ নিয়ম, ভার্সন এক জায়গায়)।
- `node --check` — `app.js` · `notebook.js` · `profile.js` তিনটেতেই ✅।
- খাতা (`00_TK_KAJER_KHATA_SOBAR_AGE_PORUN.md`) ও তারিখ-সময়ের লগ — আপডেট করা।

## অপরিবর্তিত (নিশ্চিত)

- কোনো Activity/ফাইলের ভিতরের কোড, ডিজাইন, বা টাকার হিসাব — এক অক্ষরও ছোঁয়া হয়নি।
- আগে-লক-করা সব ডিজাইন যেমন ছিল তেমনই আছে।

## Android Studio বিল্ড

Bracket/import/API — সব যাচাই-পাশ, তাই বিল্ড এররের কথা না। তবু TK নিজে বিল্ড
করে নিশ্চিত হবেন (নিয়ম অনুযায়ী)।

## SQL

এই ডেলিভারিতে নতুন কোনো SQL লাগেনি।

## TK-এর করণীয়

- Android Studio-তে বিল্ড করে দেখা।
- আগের সেশনের লাইভ-টেস্ট-বাকি ফিচারগুলো (More মেনু, Public Appointment
  সিগন্যাল, RMP ডুপ্লিকেট-ফিক্স, Staff Profile/Work Notebook কম্পিউটারে
  ইত্যাদি) ফোনে/কম্পিউটারে চালিয়ে দেখা।
- নতুন সেশনে লাইভ-টেস্টের সমস্যাগুলো জানালে সেগুলো ঠিক করা হবে।

**🔒 LOCK NOTE:** এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে
না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না, অ্যাপ স্লো
করা যাবে না।
