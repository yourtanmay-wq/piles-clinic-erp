# 🔧 V494 — TK-এর ৫টি যাচাই: সংশোধন ও প্রমাণ

**তারিখ:** ২১.০৮.২০২৬ · **ভিত্তি:** V493 · **নতুন সংস্করণ:** V494 / 4.94

TK-এর পাঁচটা রিপোর্টই **সঠিক**। প্রতিটা যাচাই করে ঠিক করা হয়েছে।
তার উপর ওই খুঁজতে গিয়ে **আরও একটা নিজের বাগ** ধরা পড়েছে (নিচে §৩ক)।

---

## ১. CloudReadDedupe-এর Memory Leak

### সমস্যা (নিশ্চিত)
```kotlin
private val inFlight = HashMap<String, Any>()
private fun gate(key: String): Any = synchronized(lock) { inFlight.getOrPut(key) { Any() } }
```
চাবি **যোগ হত, কখনো সরত না**। প্রতিটা নতুন URL (রোগী-ধরে, তারিখ-ধরে…)
চিরকাল জমে থাকত।

### সমাধান
প্রতিটা চাবির সঙ্গে **কতজন এখন ব্যবহার করছে** তা গোনা হয়; শেষজন সরলেই চাবি
মুছে যায় — এবং সেটা **`finally`**-তে, তাই সফল · ব্যর্থ · exception সব
অবস্থাতেই চলে।

```kotlin
val g = acquireGate(key)          // users++  (synchronized(g)-এ ঢোকার আগে)
try { synchronized(g) { … } }
finally { releaseGate(key, g) }   // users--, শূন্য হলে inFlight.remove(key)
```
গোনা বাড়ে **তালা ধরার আগেই**, তাই দ্বিতীয়জন এসে ঠিক সেই একই তালাই পায় —
**ডুপ্লিকেট আটকানোর ক্ষমতা এক চুলও কমেনি**।

### প্রমাণ (`debugInFlightSize()`)
```
── ১. Memory Leak (TK-যাচাই ১) ──
  ✅ ২০,০০০ আলাদা URL-এর পরে inFlight খালি   →  inFlight = 0
  ✅ ৫,০০০ ব্যর্থ পড়ার পরেও খালি              →  inFlight = 0
  ✅ ২,০০০ exception-এর পরেও খালি             →  inFlight = 0
  ✅ ১৬ থ্রেডে ৪,০০০ কাজের পরেও খালি          →  inFlight = 0

── ২. একসঙ্গে ৮টা একই অনুরোধ (ক্ষমতা অটুট) ──
  ✅ নেটে গেল ১ বার                           →  network hit = 1 (আগে ৮)
  ✅ কাজ শেষে inFlight খালি                   →  inFlight = 0
```

---

## ২. Login / Logout নিরাপত্তা

### সমস্যা (নিশ্চিত)
চাবি ছিল শুধু URL। এক ফোনে একজন লগআউট করে অন্যজন লগইন করলে ৬০ সেকেন্ডের
ভিতরে **আগের জনের তালিকা** নতুন জনের কাছে যেতে পারত।

### সমাধান — দুই স্তর
১. চাবির সামনে পরিচয়: `"<মোবাইলের শেষ ১০ অঙ্ক>|<URL>"` — দুজনের চাবি কখনো
   এক হতে পারে না, ভুল করে `clear()` বাদ পড়লেও নয়।
২. পরিচয় বদলালেই `setSession()` নিজে সব জমানো মুছে দেয়।

**বসানো হয়েছে চার পথে:**

| পথ | ফাইল |
|---|---|
| লগইন / ব্যবহারকারী বদল | `NativeSession.save()` |
| লগআউট | `NativeSession.clear()` |
| প্রতি পর্দায় যাচাই (জাল) | `NativeSession.current()` |
| জোর করে সাইন-আউট | `ModuleAuth.signOut()` |

### প্রমাণ (দুই ব্যবহারকারী)
```
── ৩. Login / Logout / User switch (TK-যাচাই ২) ──
  ✅ লগআউটে সব জমানো মুছে যায়              →  জমা = 0
  ✅ দ্বিতীয় জন নতুন করে নেট থেকে পায়      →  network hit = 2
  ✅ আগের জনের তথ্য পায়নি                   →  [{"branch":"Falakata"}]
  ✅ পরিচয় বদলালে চাবিও আলাদা              →  network hit = 2, পেল [খ]
  ✅ একই পরিচয় আবার দিলে অকারণে মুছে না     →  জমা = 1
```
(স্টাফ ক Kishanganj দেখেছিল; স্টাফ খ পেল **শুধু Falakata** — ফাঁস হয়নি।)

---

## ৩. SafeWideColumns-এর failure path

### সমস্যা ক — TK যেটা ধরেছেন (নিশ্চিত)
```kotlin
val safe = SafeWideColumns.forTable(table, cols)
if (safe != null) return fetchListOrNull(… select = safe …)   // ব্যর্থ হলে null!
return fetchListOrNull(…)                                      // এখানে পৌঁছাতই না
```
ছবি-ছাড়া পড়াটাও ব্যর্থ হলে **সরাসরি `null`** ফিরত, পুরনো `select=*` চেষ্টা
হতই না ⇒ দুর্বল নেটে আবার "খালি তালিকা / ₹0"-এর ঝুঁকি (খাতার সারি B446)।

**সমাধান:** এখন চার ধাপই পুরোপুরি চলে —
সরু → সরু আবার → **ছবি-ছাড়া পূর্ণ ঘর** → **`select=*`**।

### 🔴 সমস্যা খ — খুঁজতে গিয়ে পাওয়া আমার নিজের দ্বিতীয় বাগ

V493-এ `SafeWideColumns` ডাকার জায়গার চাওয়া ঘর **যাচাই না করেই** ফিরিয়ে আনত:
```kotlin
for (w in wanted) if (w != "*") out.add(w)
```
ফল: সরু পড়া যদি **ঘরের নাম ভুল** থাকার কারণেই ব্যর্থ হয়, সেই ভুল নামটা
"নিরাপদ" তালিকাতেও ঢুকে যেত ⇒ এই ধাপও ব্যর্থ ⇒ `select=*` ⇒ **ছবি নেমে যেত**।
অর্থাৎ যে কারণে ধাপটা বানানো, ঠিক সেই কারণেই সেটা কাজ করত না।

**Java পরীক্ষা এটা হাতেনাতে ধরেছে** (T3, পরিস্থিতি "ক" — প্রথমে ❌ ছিল)।

**সমাধান:** শুধু **চেনা ঘরই** ফেরানো হয় (`w in knownSet`)।

**সঙ্গে:** `ALL` তালিকাগুলো নতুন করে বানানো হয়েছে —
`PATIENT_COLS_NO_PHOTO` · `PATIENT_COLS` · `FOLLOWUP_COLS_NO_PHOTO` ·
`FOLLOWUP_COLS_CHAMBER_BOARD` · `PAYMENT_COLS_LIST` (সবই **লাইভ-ডেটাবেসে
যাচাই করা**, খাতার সারি B105) + schema + ভারী ঘর।

⚠️ এতে **তিনটে সত্যিকারের ঘর** ধরা পড়ল যা V493-এর তালিকায় ছিল না —
`payments.cashAmount` · `payments.onlineAmount` · `payments.dailyEvents`
(V452-এর Cash/Online ভাগের ঘর)। পাহারাদার এখন এই মিল প্রতিবার যাচাই করে।

### প্রমাণ — দুটো পরিস্থিতি আলাদা করে
```
── পরিস্থিতি ক: ঘরের নাম ভুল (নেট ঠিক আছে) ──
  ✅ তবু আসল তথ্য ফেরে (₹24,400)
  ✅ select=* পর্যন্ত যেতেই হয়নি        →  চাওয়া হয়েছে: 2 বার
  ✅ রোগীর ছবি চাওয়া হয়নি              →  photo ছাড়া ৪০ ঘর

── পরিস্থিতি খ: নেট বন্ধ (খাতার সারি B446) ──
  ✅ চারটে ধাপই চেষ্টা করা হয়েছে        →  চেষ্টা = 3 ধাপ
  ✅ শেষে select=* পর্যন্ত গেছে
  ✅ সৎ null ফেরে (খালি তালিকা নয়)
  ✅ Collection ₹0 হয় না                →  পর্দায় দেখাবে ₹24,400

── পরিস্থিতি গ: নেট ফিরে এল ──
  ✅ পরের চেষ্টায় টাটকা তথ্য · একবারেই
```

### 🔒 আসল কোডেও যাচাই (মডেল নয়)
`PaymentActivity.kt:336-342` —
```kotlin
if (rows == null) {
    if (!hadCache) { …"No collection"… }
    return@launch                     // ← জমানো তালিকা পর্দায় থেকেই যায়
}
```
অর্থাৎ পড়া ব্যর্থ হলে **আগের সত্যিকারের Collection পর্দায় থাকে**, ₹0 হয় না।

---

## ৪. ওয়েবের অর্থহীন Duplicate Retry

### সমস্যা (নিশ্চিত — V493-এ আমারই ভুল)
```js
let r=await sb.from(ct).select(__ph||'*').in('id',chunk);
if(__ph&&r.error){r=await sb.from(ct).select(__ph).in('id',chunk);}   // ← হুবহু একই
if(__ph&&r.error){r=await sb.from(ct).select('*').in('id',chunk);}
```
একই query দ্বিতীয়বার — নেট খারাপ থাকলে সেটাও ব্যর্থ, শুধু একটা বাড়তি
অনুরোধ খরচ। `wlv1CloudRead`-এও একই ভুল ছিল।

### সমাধান
নতুন `wlv1IsColumnError(err)` — **ব্যর্থতার আসল কারণ** চেনে
(PostgREST `42703` / বার্তায় "column" / "does not exist")।

| অবস্থা | আগে | পরে |
|---|---|---|
| ঘরের নাম ভুল | একই query আবার → `'*'` | সরাসরি `'*'` (কারণ ওটাই কাজে দেয়) |
| নেট সমস্যা | একই query আবার → `'*'` (ছবি!) | **আর চেষ্টা নয়** — পুরনো local তথ্য অটুট, পরে আবার |

শেষ-ভরসা `select('*').limit(2000)`-ও এখন শুধু **ছবি সত্যিই দরকার** বা
**ঘর-ভুল** হলে চলে। নেট-সমস্যায় `remoteKnown=false` ⇒ push বাতিল ⇒
পুরনো local তথ্য অক্ষত (V223 §C1 অটুট)।

**৪ জায়গায় প্রয়োগ:** `wlv1CloudRead` ×২ · `cloudPush` chunk ×১ · limit ×১।

---

## ৫. বাকি বড় Read — যা করা হলো ও যা হয়নি (সৎ হিসাব)

### ✅ করা হলো
`RegistrationRepository.closeSourceEnquiry()` — **প্রতিটা রেজিস্ট্রেশনে চলে**।

| পড়া | আগে | পরে | যাচাই |
|---|---|---|---|
| `enquiries` (mobile ধরে) | `select=*` | `select=id` | নিচে শুধু `id` পড়া হয় (:260) |
| `followups` (mobile ধরে) | `select=*` → **ছবি সহ** | `select=id,history` | শুধু `id` ও `history` পড়া হয় (:282, :284) |

### ❌ ইচ্ছে করে করা হয়নি — কারণসহ

| জায়গা | কেন হাত দেওয়া হয়নি |
|---|---|
| `fetchTab`-এর `enquiries select=*` | ঘর গুনে দেখা গেছে **২০-এর মধ্যে ১৬টাই পড়া হয়** (`address`, `remarks` সহ)। সরু করলে লাভ ~৫%, অথচ ভুলের ঝুঁকি বড়। **কোড-প্রমাণ:** `row.s(...)` তালিকা §৫ক |
| `DoctorVisitActivity`-র ৪টে পড়া | সবগুলোই **ইতিমধ্যেই সরু** (৫–৮ ঘর)। সারি কমাতে branch/date ছাঁকনি লাগত, যা মাস্টারের মোট হিসাব বদলে দিতে পারত |
| `Reports` · `Draft` | দুটোই **ইতিমধ্যেই সরু**। তারিখের ছাঁকনি বসালে "সব সময়ের" রিপোর্ট ভেঙে যেত |
| মাস্টারের Visit-Fee হিসাব | **ইতিমধ্যেই সরু**; ছাঁকনি বসালে বাকি Visit Fee লুকিয়ে যেতে পারত |
| `showCallSummary`-র `callHistory` | ভারী, কিন্তু **শুধু বোতাম চাপলে** চলে — রোজকার খরচে নয় |

**সৎ কথা:** বাকিগুলো সবই আগে থেকেই column-সরু। বাকি আছে শুধু **সারির সংখ্যা**,
আর সেটা কমাতে গেলে ছাঁকনি বসাতে হয় — যা টাকার মোট বা রোগীর তালিকা বদলে
দিতে পারে। TK-এর নির্দেশ ("কোনো ভালো কাজ যেন খারাপ না হয়") মেনে সেগুলো
**আন্দাজে ছোঁয়া হয়নি**। Egress চার্ট দেখে দরকার হলে একটা একটা করে,
প্রতিটার আগে প্রমাণ দেখিয়ে করা হবে।

---

## পরিবর্তিত ফাইলের পূর্ণ তালিকা

### বদলানো (৮টি)
| ফাইল | কী |
|---|---|
| `native/CloudReadDedupe.kt` | Gate refcount + `finally` + `sessionTag` + `debugInFlightSize()` |
| `native/SafeWideColumns.kt` | শুধু চেনা ঘর; `ALL` লাইভ-ধ্রুবক থেকে নতুন করে |
| `native/SupabaseClient.kt` | SafeWide ব্যর্থ হলেও `select=*` পথ |
| `native/NativeSession.kt` | তিন পথে `setSession()` |
| `modules/ModuleAuth.kt` | `signOut()`-এ `clear()` |
| `native/RegistrationRepository.kt` | দুটো পড়া সরু (ছবি বাদ) |
| `03_NETLIFY_READY/app.js` | `wlv1IsColumnError` + ৪ জায়গায় ডুপ্লিকেট সরানো |
| `00_GUARD/verify_no_photo_and_dedupe.py` | ৭টা নতুন যাচাই |

### নতুন (৩টি)
`00_GUARD/dedupe_test/T2.java` · `T3.java` · এই নথি
(`build.gradle.kts` ও `version.json` → V494 / 4.94)

---

## Before / After — Cloud Read

| ঘটনা | V492 | V493 | V494 |
|---|---|---|---|
| Follow-up পর্দা: `enquiries` (5000) | ৬ | ১ | ১ |
| Follow-up পর্দা: `payments` (5000) | ৬ | ১ | ১ |
| একসঙ্গে ৮টা একই অনুরোধ | ৮ | ১ | ১ |
| প্রতি রেজিস্ট্রেশনে `followups` **ছবিসহ** | ১ | ১ | **০** |
| ওয়েবে সরু পড়া ব্যর্থে অনুরোধ | ২ | **৩** | ১–২ (কারণ অনুযায়ী) |
| নেট বন্ধে ছবিসহ পুরো টেবিল (ওয়েব) | হ্যাঁ | হ্যাঁ | **না** |
| `inFlight` চাবি (২০,০০০ URL পরে) | — | **২০,০০০** | **০** |
| লগআউটের পরে আগের জনের তথ্য | — | **সম্ভব** | **অসম্ভব** |

---

## বাধ্যতামূলক যাচাইয়ের ফল

| যাচাই | ফল |
|---|---|
| Web `node --check app.js` | **PASS ✅** |
| Java যুক্তি-পরীক্ষা T (V493) | **১৬/১৬ ✅** |
| Java যুক্তি-পরীক্ষা T2 (লিক · সেশন · expiry) | **১৫/১৫ ✅** |
| Java যুক্তি-পরীক্ষা T3 (B446 চেইন) | **৯/৯ ✅** |
| একসঙ্গে ৮ অনুরোধ → network hit ১ | **প্রমাণিত ✅** |
| Cache expiry-র পরে নতুন তথ্য | **প্রমাণিত ✅** |
| অন্য স্টাফের বদল ≤ ৬০ সেকেন্ডে | **প্রমাণিত ✅** |
| Login/Logout-এর পরে আগের Cache নেই | **প্রমাণিত ✅** |
| Network failure-এ ₹0 হয় না | **প্রমাণিত ✅** (মডেল + আসল কোড) |
| ছবি শুধু প্রয়োজনীয় পর্দায় | **প্রমাণিত ✅** |
| পাঁচটা পাহারাদার | **সব PASS ✅** |
| **Android Kotlin compile / APK Build** | ⚠️ **এই পরিবেশে সম্ভব নয়** |

### ⚠️ Android Build সম্পর্কে সৎ কথা
TK-এর বাধ্যতামূলক তালিকায় "Kotlin compile ও APK Build সফল হতে হবে" আছে।
**এই পরিবেশে সেটা করা যায় না** — Android SDK (`dl.google.com`) ও Maven
Central দুটোই নেটওয়ার্কে ব্লকড, তাই Gradle বিল্ড বা `kotlinc` চালানোর
কোনো উপায় নেই। এটা আগেও (V490-এর বিল্ড ভাঙার সময়) স্পষ্ট করে বলা হয়েছে।

যা করা গেছে: ব্র্যাকেট-মিল · নিষিদ্ধ-API পাহারা · যুক্তি Java-তে সত্যিকারের
কম্পাইল ও চালানো · পাঁচটা পাহারাদার। **তবু Android Studio-র বিল্ডই চূড়ান্ত প্রমাণ।**

---

## ছোঁয়া হয়নি
DB schema · RLS · role · branch permission · অনুমোদিত ডিজাইন · টাকার হিসাব ·
payment/refund · notification · কোনো তথ্য মোছা হয়নি · **একটাও SQL চালানো হয়নি**।

## চূড়ান্ত সিদ্ধান্ত
**"Free Plan-এ নিশ্চিত চলবে" লেখা হয়নি।** Deployment-এর পরে
**২৪–৪৮ ঘণ্টার Supabase Egress chart-ই চূড়ান্ত প্রমাণ।**
লক্ষ্য দৈনিক ০.১০–০.১২ GB।

*লেখা হলো ২১.০৮.২০২৬*
