# V417 — বাড়তি টাকা (Extra Income) এখন "বাকি" হিসেবেও রাখা যায় · ১৭.০৮.২০২৬

## TK কী বলেছিলেন

> *"extra Income কি ভাবে অটোমেটিক হবে / Due হিসাবে থাকবে সেটা বলুন /
> আন্দাজে কিছু করবেন প্রয়োজনে আমাকে জিজ্ঞাসা করতে হবে"*

জিজ্ঞাসা করার পর TK যেটা বেছেছেন — **আগে অঙ্কটা ঠিক করে রাখা হবে, সেটা "বাকি"
হিসেবে দেখাবে, আর টাকাটা দেওয়া হলে "বাকি" মুছে গিয়ে "দেওয়া হয়েছে" হবে।**

আর একই সঙ্গে:

> *"Payment History open কেন থাকবে"* ⇒ Payment History আর নিজে থেকে খোলা থাকে না।

---

## ২. কী কী বদলেছে

### ক) ডেটাবেস (TK নিজে চালিয়েছেন · Success)

`hr.salary_payments`-এ একটাই নতুন ঘর — `status` (`PAID` / `DUE`), ডিফল্ট `PAID`।
⛔ পুরনো সব সারি নিজে থেকেই `PAID` হয়ে যায় ⇒ **পুরনো কোনো হিসাব এক পয়সাও বদলায়নি।**
ফাইল: `04_SUPABASE_DATABASE_SETUP/V417_SALARY_EXTRA_DUE_STATUS_2026-08-17.sql`

### খ) বাড়তি টাকা দেওয়ার সময় দুটো পথ

| বাছাই | কী হয় |
|---|---|
| **এখনই দিচ্ছি** | সারি জমা হয় `status='PAID'` · Mode (Cash/Online) নেওয়া হয় |
| **পরে দেব (বাকি)** | সারি জমা হয় `status='DUE'` · Mode ফাঁকা থাকে |

### গ) স্যালারি পর্দায় "বাকি" দেখা যায়

- উপরের তিনটে বাক্সে **"Extra"** = শুধু **দেওয়া হয়ে গেছে** এমন বাড়তি টাকা।
  ⛔ "বাকি" টাকা এখানে ঢোকে না — নইলে না-দেওয়া টাকা দেওয়া দেখাত।
- বাকি থাকলে নিচে **লাল করে** "Extra income due ₹…" আর একটা বোতাম —
  **✅ Pay Extra Income (₹…)**।

### ঘ) "দিয়ে দিলাম" চাপলে

**নতুন কোনো সারি বানানো হয় না** — যে সারিটা "বাকি" ছিল সেটাই "দেওয়া হয়েছে" হয়
(শুধু `status` · `mode` · `paid_on` বদলায়)। ⇒ **একই টাকা দুবার গোনার সুযোগ নেই।**

### ঙ) Statement-এ আলাদা করে দেখা যায়

`Extra · DUE` লাল করে লেখা থাকে, আর নিচে চারটে মোট —
Salary paid · Extra income paid · **Extra income due (লাল)** · Grand total paid।

---

## ৩. যাচাই — কী কী করে দেখা হয়েছে

ওয়েবের আসল পর্দা Chromium-এ চালিয়ে (নকল ডেটাবেস, আসল কোড):

| যা দেখা হলো | ফল |
|---|---|
| ₹9,000+₹9,000 বেতন · ₹2,000 Extra দেওয়া · ₹2,000 Extra বাকি | Salary paid ₹18,000 · Extra **₹2,000** · Total ₹20,000 ✅ |
| "বাকি" টাকা কি "দেওয়া হয়েছে"-তে ঢুকল? | **না** ✅ |
| লাল লাইন ও বোতাম | "Extra income due ₹2,000" + "✅ Pay Extra Income (₹2,000)" ✅ |
| Statement-এ DUE লেখা | ২টো সারিতেই `Extra · DUE` ✅ |
| "দিয়ে দিলাম" চাপার পর নতুন সারি বানাল? | **না** — সারি ছিল ৫টা, রইল ৫টা ✅ |
| ডেটাবেসে কী গেল? | ঠিক ২টো সারি, `status=PAID` · `mode=Online` ✅ |
| তারপর পর্দা | Extra ₹4,000 · Total ₹22,000 · লাল লাইন উধাও ✅ |
| "পরে দেব" বেছে নতুন Extra | জমা হলো `kind=EXTRA · status=DUE · mode=""` ✅ |
| "এখনই দিচ্ছি" বেছে নতুন Extra | জমা হলো `kind=EXTRA · status=PAID · mode=Cash` ✅ |
| বেতনের "এই মাসে বাকি কত" | সব ক্ষেত্রেই অপরিবর্তিত (Paid ✅) ✅ |
| পর্দার ভুল (page error) | **০টা** ✅ |

ফোনের কোড (`StaffProfileActivity.kt`) — ওয়েবের হুবহু একই নিয়মে লেখা,
বন্ধনী-মিল `{} () []` তিনটেই **০** (ভাঙা নেই)।

---

## ৪. সততার কথা — এখানে যা প্রমাণ করা যায়নি

- এই পরিবেশে **Kotlin কম্পাইল করা যায় না** (Gradle/kotlinc নেই)। তাই ফোনের অংশটা
  চিহ্ন-মিল · নাম-মিল · যুক্তি-মিল দিয়ে যাচাই হয়েছে, **কম্পাইল করে নয়**।
  বিল্ডে কোনো লাল লেখা এলে ছবি তুলে পাঠাবেন।
- আসল Supabase-এ লিখে দেখা হয়নি (TK-এর আসল ডেটা ছোঁয়া হয়নি) — নকল ডেটাবেসে
  আসল কোড চালিয়ে দেখা হয়েছে।

---

## ৫. যে ফাইলগুলো বদলেছে

- `03_NETLIFY_READY/profile.js`
- `03_NETLIFY_READY/index.html` (cache `?v=v441`)
- `02_ANDROID_SOURCE_CODE/.../modules/StaffProfileActivity.kt`
- `02_ANDROID_SOURCE_CODE/.../app/build.gradle.kts` (**V417 / 4.17**)
- `04_SUPABASE_DATABASE_SETUP/V417_SALARY_EXTRA_DUE_STATUS_2026-08-17.sql` (নতুন)
