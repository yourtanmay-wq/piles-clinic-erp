# V712 — কার্ডে ঠিকানা · UNEXPECTED · RMP ট্যাগ উধাও হওয়া (গোড়া থেকে সারানো)
**তারিখ:** ২৬.০৮.২০২৬ · **ভার্সন:** V712 / 7.12

## TK কী বলেছেন
> "Tag এ Unexpected লেখা নেই, কিন্তু View All এ ক্লিক করলে আছে।"
> "সততার সাথে সঠিকভাবে কাজটা ঠিক করবেন, কোন ভাল কাজ যেন খারাপ না হয়।
> গভীরে গিয়ে যাচাই করে কাজটা সম্পন্ন করুন।"
> **"একই ধরনের সমস্যা আপনাকে প্রত্যেকবার কেন বলতে হয়, একবারে কেন ঠিক করতে পারেন না?"**

TK-এর ছবিতে: কার্ডে শুধু `KISHANGANJ | PILES`, কিন্তু View All-এ স্পষ্ট
`MALDA` (ঠিকানা) ও `⏰ UNEXPECTED TIME`।

## 🔴 আমার নিজের দোষ — আগে ধরেও সারাইনি
এই সমস্যাটা আমি **আগেই ধরেছিলাম** এবং TK-কে বলেছিলাম *"করব?"* — কিন্তু
উত্তরের অপেক্ষায় বসে থেকে **সারাইনি**। TK-কে দুবার একই কথা বলতে হয়েছে।
এবার গোড়া থেকে সারানো হলো, **আর একটা পাহারাদারও বসানো হলো** যাতে এই ভুল
আর কখনো ফিরতে না পারে।

## ✅ আসল কারণ (কোড ধরে যাচাই)
`FollowUpRepository.saveCachedTab()` — ফোনে জমানো তালিকায় **তিনটে ঘর লেখাই হত না**:
`timeType` (UNEXPECTED) · `refDoctor` (RMP) · `addressTag` (ঠিকানা)।
তার উপরে ঠিকানার ট্যাগ বসানো হত **`saveCachedTab()`-এর পরে** — তাই ওটা কখনোই
জমা হতে পারত না (কোডের মন্তব্যেই লেখা ছিল, "ছোট আপস")।

⇒ ফল: লাইভ তালিকা আসার আগে — বা লাইন খারাপ থাকলে চিরকাল — কার্ডে এই তিনটে
ট্যাগ **উধাও**, অথচ View All (আলাদা করে পড়ে) ঠিকই দেখায়। **ঠিক TK-এর ছবির মতো।**

## কী করা হয়েছে
| ফাইল | কী |
|---|---|
| `native/FollowUpRepository.kt` | `saveCachedTab()`-এ তিনটে ঘর যোগ; `loadCachedTab()`-এ ফেরত পড়া; **জমানো তালিকা এখন ঠিকানার ট্যাগ বসানোর পরে লেখা হয়**; ফোনের নিজের (PENDING) সারিতেও `timeType`·`refDoctor` |
| `00_GUARD/tk_guard.py` | **নতুন যাচাই ৯.১৯** — নিচে দেখুন |

## 🛡️ নতুন পাহারাদার [৯.১৯] — যাতে আর কখনো ফিরতে না পারে
দুটো জিনিস দেখে:
1. কার্ডে দেখানো **১১টা দরকারি ঘর** (`timeType` · `refDoctor` · `addressTag` ·
   `lastCallDate` · `lastCallBy` · `nextFollow` · `callCount` · `patientId` ·
   `bill` · `paid` · `address`) জমানো তালিকায় **লেখা হয়** কি না।
2. জমানো তালিকা **পড়ার** সময় যে ঘর চাওয়া হয়, **লেখার** সময়ও বসে কি না —
   অর্থাৎ লেখা-পড়া কখনো আলাদা হয়ে যেতে পারবে না।

**নেগেটিভ টেস্ট (চালিয়ে প্রমাণ):**
```
লাইনটা মুছে দিলে   → ❌ [৯.১৯] `timeType` লেখা হচ্ছে না ... (ধরা পড়ল)
ফেরত আনলে         → ✅ [৯.১৯] পাশ
```
🔴 **টেস্ট করতে গিয়ে নিজের পাহারাদারেরই একটা ফাঁক ধরা পড়ল:** লাইনটা `//` দিয়ে
মন্তব্য করে দিলে পাহারাদার ঠকে যাচ্ছিল। ⇒ এখন মন্তব্য বাদ দিয়ে তবেই দেখা হয়।

## ⛔ কী বদলায়নি
* ক্লাউডে **একটাও বাড়তি অনুরোধ যায় না** — Supabase-এর খরচ বাড়েনি।
* জমানো ফাইল বড় হয় না (তিনটেই ছোট লেখা, ছবি নয়)।
* পুরোনো জমানো তালিকায় ঘরগুলো না থাকলে **ফাঁকা** ধরা হয় — অর্থাৎ ঠিক আগের আচরণ।
* Treatment ট্যাবে ঠিকানার ট্যাগ আগের মতোই দেখানো হয় না (খাতার সারি B173),
  বাড়তি অনুরোধও যায় না।

## ⚠️ সৎ কথা — যা এখনো আলাদা
কম্পিউটারের অ্যাপের Follow-up কার্ডে **UNEXPECTED / RMP ট্যাগ কখনোই ছিল না**
(ওখানে ব্রাঞ্চ · রোগ · ঠিকানা — এই তিনটেই)। এটা এই দোষের অংশ নয়, আগে থেকেই এমন।
TK চাইলে আলাদা কাজ হিসেবে যোগ করা যাবে।

## যাচাই
| যাচাই | ফল |
|---|---|
| `verify_kotlin_compile.py` | ✅ নতুন ভুল ০ |
| `tk_guard.py` (এখন ২৩টা মেশিন-যাচাই) | ✅ সব পাশ |
| নতুন [৯.১৯]-এর নেগেটিভ টেস্ট | ✅ দোষ ফেরালে ধরে, সারালে ছাড়ে |

⚠️ Android চালানোর উপায় নেই — TK দেখে বলবেন।
