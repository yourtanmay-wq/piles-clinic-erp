# V533 — চিকিৎসার নোট আলাদা ঘরে · পেমেন্টে ₹0 সারি বাদ
তারিখ: ২২.০৮.২০২৬ · V532 → **V533 (5.33)** · **TK-এর সিদ্ধান্তে**

---

## 🔴 প্রথমে: TK-কে একবার SQL চালাতে হবে

**`04_SUPABASE_DATABASE_SETUP/V533_PAYMENTS_PROGRESS_COLUMN_2026-08-22.sql`**
— **V533 বিল্ড করার আগে** Supabase-এ একবার চালান।

ওটা শুধু **একটা নতুন ফাঁকা ঘর** যোগ করে (`payments.progress`)। আর কিচ্ছু না।
⛔ পুরোনো কোনো তথ্য মোছে না · RLS ছোঁয় না · দু'বার চালালেও কিছু হয় না।

না চালালে নতুন নোট সার্ভারে বসবে না (অ্যাপ নিজের কিউ-তে জমিয়ে রাখবে, **হারাবে
না**) — কিন্তু Report Card-এ দেখা যাবে না।

---

## ১. 🔴 আসল সমস্যা — স্টাফের লেখা Remark **মুছে যাচ্ছিল**

**TK-এর প্রশ্ন:** *"পেমেন্টের এখানে ড্রেসিং-এর কথা উল্লেখ কেন হবে?"*

**কোড ধরে পাওয়া আসল কারণ:** Chamber-এ চিকিৎসার নোট লিখলে অ্যাপ সেই লেখাটা
**ওইদিনের প্রতিটা payment সারির `remarks` ঘরে** বসিয়ে দিত
(`ChamberAttendanceActivity.kt:2298` · `PatientTimelineActivity.kt:2889`)।

**যা মুছে যেত (শুধু দেখার সমস্যা নয় — সত্যিই হারিয়ে যেত):**

| কী | কোথায় লেখা হয় |
|---|---|
| **Refund-এর কারণ** | `PaymentModel.kt:798` |
| **Medicine পেমেন্টের Remark** | `MedicinePaymentActivity.kt:320` |
| **টাকার অঙ্ক/Mode বদলের অডিট-লাইন** (`Amount ₹500 → ₹700 by …`) | `PaymentActivity.kt:1073` |

**সমাধান (TK-এর বেছে নেওয়া পথ):** চিকিৎসার নোটের **নিজের আলাদা ঘর** — `progress`।

- **লেখা:** তিনটে জায়গাই এখন `progress`-এ লেখে; `remarks` **আর ছোঁয়াই হয় না**
  (Chamber · Timeline · Report Card-এর নিজের ৩-ট্যাপ এডিট)
- **পড়া:** `progress` ফাঁকা হলে **আগের মতোই `remarks`** পড়া হয়
  (`PatientTimelineRepository.kt:864`)

⛔ **তাই পুরোনো একটা নোটও হারায় না।** পুরোনো প্রতিটা সারিতে `progress` ফাঁকা,
তাই Report Card · Timeline · Chamber — সব পর্দা **অবিকল আগের মতোই** দেখাবে।
⛔ Report Card-এর Progress কলাম (TK-এর ২৪.০৭.২০২৬-এর কাজ) **অক্ষত** — শুধু
নতুন নোট নতুন ঘরে যায়, আর পড়ার সময় দুটোই দেখা হয়।
⛔ **নতুন কোনো ক্লাউড-অনুরোধ নেই** — Timeline/Report Card এমনিতেই `select=*`
দিয়ে আনে, তাই নতুন ঘরটা আপনাআপনি আসে। কোনো কলাম-তালিকা বাড়াতে হয়নি।

---

## ২. পেমেন্টের তালিকায় ₹0 সারি বাদ

**TK-এর প্রশ্ন:** *"জিরো পেমেন্ট আবার কেন দেখাবে এখানে?"*

**সত্যি কথা:** এটা কোডের ভুল ছিল না — **TK নিজেই আগে চেয়েছিলেন**। কোডে মন্তব্য
লেখা আছে (১৬.০৭.২০২৬): *"Marked Arrived (₹0) entries still show in the list
below (**TK wanted that**)"*। এখন চান না ⇒ সরানো হলো।

`attendance_mark` · `chamber_expected` · `bill_edit` — এই তিনটে আর দেখা যায় না।

⛔ **টাকার একটাও হিসাব বদলায়নি** — এগুলো এমনিতেই "X টি পেমেন্ট" গোনায় ধরা হত
না (`realPaymentCount`), আর প্রজেক্টের সেই একই শেয়ার-করা নিয়মেই
(`PaymentModel.isMarkerOnlyRow`, V523) বাদ দেওয়া হয়েছে — নতুন নিয়ম বানানো হয়নি।
⛔ ক্লাউডের সারি **মোছা হয়নি** — শুধু এই পর্দায় দেখানো হয় না। ঘটনাগুলো
Timeline ও Report Card-এ আগের মতোই আছে।

---

## ৩. ওয়েব — যাচাই করে দেখা গেল **বদলানোর দরকার নেই**

খুঁজে দেখলাম: ওয়েব **কখনোই** চিকিৎসার নোট `payments.remarks`-এ লেখে না
(`upd('payments', …)` শুধু অঙ্ক/Mode ও Refund-অনুমোদনে)। ⇒ **ওয়েবের একটাও
অক্ষর বদলানো হয়নি।**

---

## ৪. যাচাই

cham **27·0** · rep **25·0** · tt **19·0** · rc **19·0** · family **29·0** ·
rot **21·0** · futab **19·0** · gtest **13·0** — **সব পাস** ·
ওয়েব ৮/৮ `node --check` ✅ · বন্ধনীর হিসাব পাঁচ ফাইলেই `git HEAD`-এর সাথে **হুবহু এক**।

⚠️ **সৎ সীমাবদ্ধতা:** Android SDK এখানে নামে না, তাই Activity ফাইল compile করে
দেখানো যায়নি। **TK-কে বিল্ড করে দেখে নিতে হবে।**

---

## ৫. TK-এর করণীয় (ক্রম মেনে)

1. 🔴 **আগে SQL চালান** — `V533_PAYMENTS_PROGRESS_COLUMN_2026-08-22.sql`
2. **তারপর V533 বিল্ড** · Netlify-তে `03_NETLIFY_READY` (cache `?v=v533`)
3. Chamber-এ একটা নোট লিখুন → **Payment-এর পপ-আপে সেটা আর দেখা যাবে না**,
   কিন্তু **Report Card-এর Progress-এ দেখা যাবে**
4. 🔴 **পুরোনো রোগীর Report Card খুলে দেখুন পুরোনো নোটগুলো ঠিক আছে কিনা** —
   এটাই সবচেয়ে জরুরি পরীক্ষা
