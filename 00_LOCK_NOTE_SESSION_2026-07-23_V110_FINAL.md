# Session Lock Note — V109 → V110 (2026-07-23)

এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।

## এই সেশনে যা ফিক্স হলো (TK-অনুমোদিত, ধাপে ধাপে)

### ১. Follow-up স্ক্রিন — ব্রাঞ্চ পিকার + সংখ্যা মিসম্যাচ
- ব্রাঞ্চ পিকার (Master-only) আগে শুধু উপরের সংখ্যা ফিল্টার করত, লিস্ট না — এখন লিস্টও ফিল্টার করে (নতুন `effectiveBranch()`)।
- Visit/Enquiry/Patient ট্যাবের সংখ্যা আগে "safety-net" ব্যাকআপ কার্ড বাদ দিয়ে গুনত (তাই "0" দেখাত যদিও কার্ড ছিল) — এখন লিস্টের সাইজ থেকেই গোনে, সবসময় মিলবে।
- বদলানো ফাইল: `FollowUpActivity.kt` (শুধু)
- ঝুঁকি (TK-কে জানানো, অনুমোদিত): সংখ্যা আনতে এখন কিছুটা বেশি Supabase রিড লাগে (আগে count-only ছিল, এখন fetchTab পুনর্ব্যবহার করে)।

### ২. Dr. Visit/RMP — "Already exists" কিন্তু সার্চে পাওয়া যায় না
- কারণ: `checkDuplicate` মোবাইল দিয়ে খোঁজে (status দেখে না), কিন্তু `fetchList`/`fetchListRaw` শুধু status="Active" দেখাত — পুরনো/মাইগ্রেট করা ডেটায় status ফাঁকা থাকা ডাক্তার তালিকা/সার্চ থেকে বাদ পড়ত।
- ফিক্স: এখন status="Active" **অথবা** status ফাঁকা — দুটোই দেখাবে।
- বদলানো ফাইল: `DoctorVisitRepository.kt` (শুধু)

## যাচাই
- দুটো ফাইলেই স্ট্রিং → ব্লক-কমেন্ট → লাইন-কমেন্ট সরিয়ে brace/paren balance যাচাই করা হয়েছে — ঠিক আছে।
- এই দুটো ফাইল ছাড়া প্রজেক্টের আর কিছু ছোঁয়া হয়নি।
- বিস্তারিত: `00_PROJECT_STATE_MASTER_NOTE.md`-এর সেকশন ৭৬ ও ৭৭।

**সর্বশেষ ফাইল:** `PILES_CLINIC_APP_V110_FINAL.zip`
