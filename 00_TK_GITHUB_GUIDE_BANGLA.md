# 🗂️ GitHub গাইড (বাংলা) — প্রজেক্ট নিরাপদে জমা রাখার ধাপে ধাপে নিয়ম

> **কেন দরকার:** কম্পিউটার নষ্ট হলে, ভুলে ফোল্ডার মুছে গেলে বা কোনো ফাইল হারালে —
> GitHub-এ তোলা থাকলে পুরো প্রজেক্ট এক ক্লিকে ফেরত পাওয়া যায়। এটা সম্পূর্ণ ফ্রি।
> **সতর্কতা:** এই কাজে অ্যাপের কোনো কোড বদলায় না, কোনো ঝুঁকি নেই।

---

## ধাপ ১ — অ্যাকাউন্ট খোলা (একবারই)

1. ব্রাউজারে যান → **github.com**
2. ডানদিকে উপরে সবুজ **Sign up** বোতামে চাপুন।
3. পরপর চাইবে:
   - **Email** — আপনার চালু ইমেইল দিন
   - **Password** — কমপক্ষে ৮ অক্ষর (একটা ডায়েরিতে লিখে রাখুন)
   - **Username** — যেমন `tkbiswas-clinic` (ছোট হাতের অক্ষর, ফাঁকা জায়গা চলবে না)
   - **Public email?** — `n` লিখে দিন
4. একটা ছোট ধাঁধা (puzzle) মেলাতে বলবে → মেলান।
5. ইমেইলে আসা **৮ সংখ্যার কোড** বসান → অ্যাকাউন্ট তৈরি।

**পর্দায় যা দেখবেন:** সাদা পাতায় মাঝখানে ইমেইল/পাসওয়ার্ড/ইউজারনেম-এর ঘর, নিচে সবুজ **Continue** বোতাম।

---

## ধাপ ২ — Repository (প্রজেক্টের ঘর) বানানো

1. লগইন করার পর ডানদিকে উপরে **+** চিহ্ন → **New repository**
2. ঘরগুলো এভাবে ভরুন:
   - **Repository name:** `piles-clinic-app`
   - **Description:** `Maa Ayurved Piles Clinic ERP`
   - 🔒 **Private** বেছে নিন (এটা খুব জরুরি — রোগীর তথ্য আছে, Public করা যাবে না)
   - বাকি ঘর (README/gitignore/license) — কিছু টিক দেবেন না
3. নিচে সবুজ **Create repository** চাপুন।
4. পরের পাতায় একটা ঠিকানা দেখাবে, যেমন:
   `https://github.com/tkbiswas-clinic/piles-clinic-app.git`
   👉 **এই ঠিকানাটা কপি করে রাখুন।**

**পর্দায় যা দেখবেন:** "Quick setup" লেখা পাতা, উপরে ঠিকানা আর পাশে কপি করার ছোট আইকন।

---

## ধাপ ৩ — Android Studio থেকে প্রজেক্ট তোলা

প্রজেক্টে Git আগেই চালু করা আছে (V30-এ করা হয়েছে), তাই শুধু তুলে দিলেই হবে।

1. Android Studio-তে প্রজেক্ট খুলুন।
2. উপরের মেনু → **Git** → **Manage Remotes** → **+**
   - **Name:** `origin`
   - **URL:** ধাপ ২-এ কপি করা ঠিকানা
   - **OK**
3. উপরের মেনু → **Git** → **Commit**
   - বাঁদিকে সব ফাইলে টিক দিন
   - নিচের ঘরে লিখুন: `V133 backup`
   - **Commit and Push** চাপুন
4. প্রথমবার GitHub-এর **Username** আর **Token** চাইবে (নিচে ধাপ ৪ দেখুন)।

---

## ধাপ ৪ — Token (পাসওয়ার্ডের বদলে যা লাগে)

GitHub এখন সাধারণ পাসওয়ার্ড নেয় না, একটা লম্বা "Token" নেয়।

1. github.com-এ ডানদিকে উপরে নিজের ছবি → **Settings**
2. একদম নিচে বাঁদিকে → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)** → **Generate new token (classic)**
4. **Note:** `android studio`, **Expiration:** `No expiration`
5. টিক দিন শুধু **repo** ঘরে।
6. নিচে **Generate token** → একটা লম্বা লেখা আসবে (যেমন `ghp_xxxxxxxx...`)
   👉 **এটা একবারই দেখা যায় — সঙ্গে সঙ্গে ডায়েরিতে লিখে রাখুন।**
7. Android Studio যখন পাসওয়ার্ড চাইবে, পাসওয়ার্ডের জায়গায় এই Token বসাবেন।

---

## ধাপ ৫ — পরে যতবার কাজ শেষ হবে

শুধু তিনটে চাপ:
**Git → Commit → (লিখুন কী বদলালো) → Commit and Push**

---

## ⚠️ কখনো করবেন না

| ❌ যা করবেন না | কেন |
|---|---|
| Repository **Public** করা | রোগীর তথ্য পৃথিবীর সবাই দেখতে পাবে |
| Token কাউকে পাঠানো | ওটা পাসওয়ার্ডের সমান |
| GitHub-এ ফাইল **Delete** করা | কম্পিউটারের কপিও গোলমাল হতে পারে |

---

## 🆘 আটকে গেলে

- **"Authentication failed"** → Token ভুল বা মেয়াদ শেষ → ধাপ ৪ আবার করুন।
- **"Push rejected"** → আগে **Git → Pull**, তারপর আবার Push।
- **কিছুই বুঝতে না পারলে** → স্ক্রিনশট তুলে রাখুন, পরে দেখিয়ে দেবেন।

---

**🔒 LOCK NOTE:** এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না, অ্যাপ স্লো করা যাবে না।
