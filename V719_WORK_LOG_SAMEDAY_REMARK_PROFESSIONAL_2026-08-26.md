# 🎨 V719 — "Same-day Remark" পপ-আপ প্রফেশনাল করা

**তারিখ:** ২৬.০৮.২০২৬ · রাত ~১১.১৫ IST
**TK-এর নির্দেশ (ছবিসহ):** *"এবার এটাকে প্রফেশনাল বানান"*
**অনুমোদন:** ডেমো `V719_DEMO_SAMEDAY_REMARK.png` দেখিয়ে TK **"১"** (প্রস্তাব ১) বেছেছেন।

---

## ১. আগে কী সমস্যা ছিল
Android নিজের তিনটে বোতাম (Positive · Negative · Neutral) **ডান দিকে, তিন
মাপে** সাজাত, আর **"Cancel" মাঝখানে** পড়ে যেত। দেখতে অগোছালো।

## ২. এখন কী হলো (প্রস্তাব ১)
- আজকের লেখাটা আলাদা **হালকা-হলুদ বাক্সে** (`TODAY'S REMARK` ক্যাপশনসহ) —
  এক নজরেই চোখে পড়ে
- তিনটে বোতামই **সমান চওড়া, উপর-নিচ সাজানো**
- ক্রম: **Save New Remark** (মূল, হলুদ) → **Update Previous** (সাদা, বর্ডার)
  → **Cancel** (সাদামাটা)

| দিক | ফাইল |
|---|---|
| ফোন | `PatientTimelineActivity.kt` — `showSameDayRemarkWarning()` |
| কম্পিউটার | `app.js` — একই পপ-আপের হুবহু যমজ |

## ৩. ⛔ যা ছোঁয়া হয়নি
- **তিনটে বোতামের কাজ এক অক্ষরও বদলায়নি** — `saveQuickRemark` ·
  `replaceRemarkText` · কিছু না (Cancel)
- হলুদ হেডার · শিরোনাম · `PremiumAlert.header()` / `paint()` — অপরিবর্তিত
  (`paint()` বোতাম না পেলে চুপচাপ ছেড়ে দেয় — `?.let`, তাই নিরাপদ)
- বাংলা-বন্ধ স্টাফের ঢাকনা (`NoBengali.installDialog`) পুরো পপ-আপেই বসে,
  তাই নতুন বোতামগুলোও ঢাকা পড়ে
- রং নিজে বানানো হয়নি — `PremiumAlert`-এর লক করা হলুদ (#E8A100 · লেখা
  #3A2600) ও নিরপেক্ষ (#D4DAE4 · #22304A) মানগুলোই ব্যবহার
- সব লেখা **ইংরেজি** (TK-এর নিয়ম)
- কোনো তথ্য · ইতিহাস · টাকার হিসাব · ডেটাবেস — কিছুই নয়

## ৪. যাচাই
- ✅ `verify_kotlin_compile.py` — নতুন ভুল **০**
- ✅ `node --check app.js` — পাশ
- ✅ `tk_guard.py` — সব যাচাই পাশ
- ✅ ফোন ও কম্পিউটার — **দুই দিকেই**

**ভার্সন:** V718 → **V719**
