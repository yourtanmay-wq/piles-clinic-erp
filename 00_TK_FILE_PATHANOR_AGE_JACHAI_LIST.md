# ✅ ফাইল পাঠানোর আগে যা যা যাচাই করা হয়েছে — V145 (27.07.2026)

**TK-এর নির্দেশ:** *"সমস্ত কিছু যাচাই করে পাঠান, যাচাই লিস্টটা যেন ফাইলের নোটে লেখা থাকে। Android Studio-তে Build করার সময় যেন কোনো error না আসে।"*

নিচের প্রতিটা যাচাই **এই ZIP পাঠানোর আগে** চালানো হয়েছে এবং সবগুলো পাশ করেছে।

---

## ক) পুরো প্রজেক্টের যাচাই (সংখ্যা সহ)

| # | কী দেখা হলো | কীভাবে | ফল |
|---|---|---|---|
| ১ | **সব XML ফাইল ভালো আছে কিনা** (২১০টি) | প্রতিটা ফাইল পার্স করা হয়েছে | ✅ একটাও ভাঙা নেই |
| ২ | **একই ঘরে একই অ্যাট্রিবিউট দুবার আছে কিনা** (এটা হলে Build error হয়) | পার্সার নিজেই ধরে | ✅ কোথাও নেই |
| ৩ | **XML কমেন্টের ভিতরে `--` আছে কিনা** (থাকলে Build error) | সব কমেন্ট মিলিয়ে দেখা | ✅ নেই |
| ৪ | **সব Kotlin ফাইলের ব্র্যাকেট মিল** (১৬০টি ফাইল) | স্ট্রিং ও কমেন্ট বাদ দিয়ে `{ } ( )` গোনা | ✅ সব মিলেছে |
| ৫ | **এক ক্লাসে একটার বেশি `companion object`** (V137-এ এই ভুলেই Build ভেঙেছিল) | ১৬০টি ফাইলে খোঁজা | ✅ কোথাও নেই |
| ৬ | **কোডে যত `binding.` ব্যবহার হয়েছে, সেই id আসলেই লেআউটে আছে কিনা** | Dashboard · Registration · Follow-up · Chamber · Menu — পাঁচটা পর্দার ১৩১টি রেফারেন্স মিলিয়ে দেখা | ✅ একটাও অনুপস্থিত নয় |
| ৭ | **এবার যেসব লেখা XML থেকে মোছা হয়েছে, সেগুলোর কোনো id কোডে ব্যবহার হত কিনা** | মোছা প্রতিটা লাইন দেখা | ✅ কোনোটারই id ছিল না, তাই কোড ভাঙেনি |
| ৮ | **নতুন কোডে ডাকা প্রতিটা ফাংশন সত্যিই আছে কিনা** | `upsert` · `fetchListOrNull` · `loadBoard(date, branch, context)` · `NativeSession.current` · `targetsHit` · `fetchPendingBackdateCount` · `fetchPendingEditCount` · `fetchMissingVisitFeePatients` — সব উৎস ফাইলে খুঁজে দেখা | ✅ সব আছে, প্যারামিটারও মিলেছে |
| ৯ | **নতুন ড্রয়েবল ফাইল দুটো আছে ও ঠিক আছে** (`bg_btn_save_green.xml`, `bg_btn_photo_pill.xml`) | পার্স করা | ✅ |
| ১০ | **পুরনো শেয়ার করা ফাইলে হাত পড়েছে কিনা** (`bg_btn_green` ১০টা পর্দা ব্যবহার করে, `styles_form.xml` সব ফরম ব্যবহার করে) | খোঁজা | ✅ একটাও ছোঁয়া হয়নি |
| ১১ | **কম্পিউটারের অ্যাপ** | `node --check app.js` | ✅ পাশ |
| ১২ | **CSS-এর ব্র্যাকেট মিল** | গোনা | ✅ মিলেছে |
| ১৩ | **ব্রাউজার নতুন ফাইল নেবে কিনা** | `index.html`-এ `?v=v145` | ✅ |
| ১৪ | **Menu-তে কোনো এন্ট্রি দুবার আছে কিনা** | Reports · Backup · Password · Trash · Search গোনা | ✅ প্রতিটা একবার |
| ১৫ | **ভার্সন** | `versionCode 145` · `versionName V145` · Dashboard-এ "V145" | ✅ তিন জায়গাতেই |
| ১৬ | **পুরো প্রজেক্ট ফেরত যাচ্ছে কিনা** | ZIP-এ ১,২৭৫টি ফাইল, `.git` ফোল্ডার সহ (৬৬২টি) | ✅ কিছু বাদ যায়নি |

---

## খ) হাতে করা যাচাই (যেটা যন্ত্র ধরতে পারে না)

| # | কী | ফল |
|---|---|---|
| ১ | ক্যাপিটাল দেখানোর জন্য `setAllCaps(true)` ব্যবহার করা হয়েছে (Android-এর সব ভার্সনে চলে); **ভিতরের মান বদলায় না** | ✅ |
| ২ | ড্রপডাউনের ক্যাপিটাল শুধু দেখার জন্য — `selectedItem` আগের মতোই আসল লেখা ফেরত দেয়, তাই "Dr. Visit" মেলানো ভাঙেনি | ✅ |
| ৩ | Branch তালিকায় "Select Branch" যোগ হওয়ায় ক্রম বদলেছে — এনকোয়ারি থেকে অটো-বসানো ও সেভের যাচাই দুটোই নতুন তালিকা ধরে করা হয়েছে | ✅ |
| ৪ | "Payment Mode"/"Sex"/"Registration Timing" লেখা বাদ দেওয়ার পরে বোতামগুলো যেন নিজের জায়গাতেই থাকে — মার্জিন দিয়ে মেলানো | ✅ |
| ৫ | চেম্বার-রিমাইন্ডারের ২টো আসল ভুল ধরা ও ঠিক করা | ✅ `00_TK_JACHAI_REPORT_V145.md` |
| ৬ | Follow-up-এর ধাপে ধাপে আঁকা — নতুন তালিকা এলে আগেরটা বাতিল হয়, দুটো তালিকা মিশবে না | ✅ |

---

## গ) ⚠️ যেটা এই ফাইলে যাচাই করা সম্ভব নয় (সৎ কথা)

এখানে Android Studio বা Gradle নেই, তাই **সত্যিকারের কম্পাইল** চালানো যায় না। উপরের যাচাইগুলো Build-ভাঙা ভুলের সবচেয়ে চেনা কারণগুলো ধরে — ব্র্যাকেট, দুবার লেখা অ্যাট্রিবিউট, না-থাকা id, না-থাকা ফাংশন, দুটো companion object, XML কমেন্টের `--`।

**যদি তবুও কোনো error আসে:** শুধু লাল লেখাটার ফটো পাঠাবেন — কারণটা ধরে সঙ্গে সঙ্গে ঠিক করে দেব।

---

**🔒 এই তালিকা প্রতি সেশনে ফাইল পাঠানোর আগে চালাতে হবে। TK-কে আর কখনো এটা মনে করিয়ে দিতে হবে না।**


---

## ঘ) 🔁 শেষ যাচাই — 28.07.2026 সকাল ১১.০০ (ফাইল পাঠানোর ঠিক আগে)

| # | কী দেখা হলো | ফল |
|---|---|---|
| ১ | ২১৪টি XML ফাইল ভাঙা কিনা · একই ঘরে একই অ্যাট্রিবিউট দুবার · কমেন্টে `--` | ✅ |
| ২ | একই ফাইলে একই id দুবার | ✅ নেই |
| ৩ | ১৬২টি Kotlin ফাইলের ব্র্যাকেট · এক ক্লাসে একটাই companion object | ✅ |
| ৪ | ১৩টি পর্দার প্রতিটা `binding.<id>` লেআউটে আছে কিনা + Queue কার্ডের অ্যাডাপ্টার | ✅ |
| ৫ | এই সেশনে বানানো ৭টি নতুন ড্রয়েবল ফাইল আছে কিনা | ✅ |
| ৬ | **এই সেশনের ৩৪টি কাজ সত্যিই কোডে আছে কিনা** — এক এক করে খুঁজে দেখা | ✅ একটাও বাদ নেই |
| ৭ | ৮টি পর্দায় টেনে-নামিয়ে রিফ্রেশ | ✅ |
| ৮ | ভার্সন তিন জায়গায় V145 | ✅ |
| ৯ | `node --check app.js` · CSS ব্র্যাকেট · `index.html`-এ `?v=v145` | ✅ |
| ১০ | নতুন যোগ হওয়া বোতামের কাজ সত্যিই যুক্ত (Report Card → ReportCardActivity) | ✅ |
| ১১ | নতুন অ্যাডাপ্টার-প্যারামিটার **ডিফল্ট সহ** যোগ, তাই পুরনো কোনো কল ভাঙেনি | ✅ |
| ১২ | সেফটি-নেট চালু আছে ও তিন জায়গায় ফ্লাশ হয় (অ্যাপ চালু · অ্যাপ ব্যবহার · ব্যাকগ্রাউন্ড) | ✅ |
