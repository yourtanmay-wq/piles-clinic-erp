# 🧾 (১) যাচাই Query  +  (২) পরবর্তী Fix-এর Safe Plan
### তারিখ: ২৬.০৭.২০২৬ · Base: V131 · Version: V132
### ⚠️ এই ধাপে কোনো Code পরিবর্তন করা হয়নি (শুধু Version ছাড়া)

---

# ভাগ ১ — Supabase-এ চালানোর Query (শুধু দেখার, কিছু বদলায় না)

## কোথায় চালাবেন
Supabase খুলুন → বাঁ দিকের মেনু থেকে **SQL Editor** → **New query** → নিচের লেখাটা হুবহু বসান → **Run**।

## Query

```sql
select table_name, column_name
from information_schema.columns
where table_schema = 'public'
  and column_name = 'timeType'
order by table_name;
```

## এটা নিরাপদ কেন
- এখানে `select` ছাড়া আর কিছুই নেই — **কোনো create / alter / insert / update / delete নেই**।
- `information_schema` হলো Supabase-এর নিজের **তথ্য-তালিকা**, আপনার রোগীর ডেটা নয়। এটা শুধু "কোন টেবিলে কোন কলাম আছে" তার নামের তালিকা দেখায়।
- **একটা রোগী, একটা পেমেন্ট, একটা কলাম — কিছুই তৈরি, বদল বা মুছবে না।** বারবার চালালেও কিছু হবে না।

## ফলাফল কীভাবে পড়বেন

ফল আসবে এরকম একটা ছোট তালিকা:

| table_name | column_name |
|---|---|
| enquiries | timeType |
| followups | timeType |

👉 **`patients` লেখা সারিটা তালিকায় আছে কিনা — শুধু সেটাই দেখার।**

- তালিকায় **`patients` আছে** → ✅ সব ঠিক, এই দিকে কোনো সমস্যা নেই।
- তালিকায় **`patients` নেই** (উপরের মতো শুধু enquiries আর followups) → ❌ কলামটা সত্যিই নেই। তাহলে এটাই "Registration অর্ধেক Save" হওয়ার আসল কারণ।

> `enquiries` আর `followups` তালিকায় আসাটাই প্রমাণ করে Query ঠিকঠাক কাজ করেছে।

## আপনি যা করবেন
**Run** চেপে যে তালিকাটা আসবে, তার একটা ছবি বা লেখা আমাকে পাঠান।
**ফল না দেখা পর্যন্ত Registration বা Database-এর কোনো কাজ শুরু করব না।**

---

# ভাগ ২ — পরবর্তী ৫টা Fix-এর Safe Plan

> সব কটাতেই সাধারণ নিয়ম: **কোনো Approved Design, রং, সাজানো, বোতাম, Navigation বা Permission বদলাবে না।**
> **Rollback copy:** `PILES_CLINIC_APP_V131_FINAL.zip` (আপলোড করা মূল ফাইল, অক্ষত রাখা আছে)।

---

## দফা ১ — Sync-এর দ্বন্দ্ব বন্ধ

**কী করব:** `SyncWorker`-এর ভিতর থেকে শুধু পুরনো `SyncManager.syncAll()`-এর ডাকটা সরাব। এর বদলে Worker শেষ করার আগে দেখবে — **কোনো pending queue-তে কিছু বাকি আছে কিনা**। বাকি থাকলে `Result.retry()`, খালি থাকলে `Result.success()`।

| | |
|---|---|
| **কোন File** | **মাত্র ১টা:** `data/sync/SyncWorker.kt` (লাইন ৩৩–৪১) |
| **নতুন কিছু?** | না। ৮টা রিপোজিটরিতে হাত না দিয়ে, Worker নিজেই queue-গুলো **শুধু পড়ে** দেখবে — যাতে ঝুঁকি এক ফাইলেই আটকে থাকে |
| **কী মুছব** | **কিছুই না।** `SyncManager.kt`, `AppDatabase.kt`, DAO, `SyncState.kt` — সব জায়গামতো থাকবে, শুধু আর ডাকা হবে না |
| **Design/Workflow** | কিছুই বদলাবে না। `SyncStatusHolder` কোনো স্ক্রিনে দেখানো হয় না (যাচাই করা), তাই পর্দায় কোনো পরিবর্তন নেই |
| **Rollback** | ওই ১টা ফাইল V131 থেকে ফেরত কপি — ব্যস |
| **Test** | ১. Flight mode-এ Enquiry সেভ → pending থাকে<br>২. নেট চালু → Supabase-এ সারি পৌঁছায়, **একবারই**<br>৩. Payment, Follow-up, Registration একইভাবে<br>৪. অ্যাপ বন্ধ রেখে ১৫ মিনিট পর pending নিজে থেকে যায় কিনা |
| **লাভ** | প্রতি Save-এ ও প্রতি ১৫ মিনিটে ৪টা আস্ত টেবিল নামানো বন্ধ → কোটা, ডেটা ও ব্যাটারি বাঁচবে, অ্যাপ হালকা হবে |

---

## দফা ২ — Patient Edit-এ Payment / Medical History নিরাপদ রাখা

**কী করব:** রোগীর মোবাইল বা নাম বদলালে এখন শুধু `patients` / `enquiries` / `followups` আপডেট হয়। এর সঙ্গে **`payments`** আর **`medical`** (Prescription, Medicine Slip, Blood Test, Diet Chart) সারিগুলোও পুরনো নম্বর ধরে খুঁজে নতুন নম্বর/নামে আপডেট হবে। **কোনো একটা ধাপ ব্যর্থ হলে "Save হয়েছে" দেখাবে না** — স্পষ্ট বার্তা দেবে।

> ⚠️ **`doctor_visits` টেবিলে হাত দেব না** — ওখানকার `mobile` রেফার করা **ডাক্তারের** নম্বর, রোগীর নয়। ছুঁলে RMP তালিকা নষ্ট হবে।

| | |
|---|---|
| **কোন File** | **২টা:** `native/PatientTimelineActivity.kt` · `native/FollowUpActivity.kt` |
| **Design/Workflow** | Edit পপ-আপের চেহারা, ঘর, বোতাম — কিছুই বদলাবে না। শুধু Save চাপার পর ভিতরে বাড়তি দুটো টেবিল আপডেট হবে |
| **Rollback** | ওই ২টা ফাইল V131 থেকে ফেরত |
| **Test** | ১. পুরনো পেমেন্ট আছে এমন রোগীর মোবাইল বদলান<br>২. Payment History, Total Paid, Due — সব আগের মতো দেখাচ্ছে কিনা<br>৩. Prescription / Blood Test / Diet খুলে দেখুন হারায়নি<br>৪. Report Card ও Full Journey মিলিয়ে দেখুন<br>৫. নেট বন্ধ করে Edit — মিথ্যা "Save হয়েছে" দেখায় কিনা |

---

## দফা ৩ — Delete করা Data ফিরে আসা বন্ধ

**কী করব:** এখন Delete শুধু ক্লাউড থেকে মোছে; ফোনের ক্যাশ আর pending queue-তে সারিটা থেকে যায়, তাই পরের Sync-এ ফিরে আসতে পারে। Delete-এর সময় **ওই id-টা ৯টা pending queue ও লোকাল ক্যাশ থেকেও মুছে দেব**।

| | |
|---|---|
| **কোন File** | **১টা বদল:** `native/TrashHelper.kt`<br>**১টা নতুন:** `native/PendingQueuePurge.kt` (শুধু "এই id-টা queue থেকে সরাও" — আর কিছু করে না) |
| **কেন নতুন ফাইল** | ৯টা রিপোজিটরিতে আলাদা আলাদা হাত না দিয়ে পুরো কাজটা **এক জায়গায়** রাখলে ঝুঁকি অনেক কম, আর ফেরত নেওয়াও সহজ |
| **Design/Workflow** | **Trash Bin ও Restore-এর নিয়ম একেবারেই বদলাবে না।** Delete-এর অনুমতি (একই দিন / Master) আগের মতোই |
| **Rollback** | নতুন ফাইলটা মুছে দিন + `TrashHelper.kt` ফেরত — সব আগের মতো |
| **Test** | ১. Flight mode-এ Enquiry তৈরি → সঙ্গে সঙ্গে Delete → নেট চালু → **ফিরে আসে কিনা** (আসা চলবে না)<br>২. Trash Bin-এ গিয়ে Restore — আগের মতো ফিরে আসে কিনা<br>৩. অন্য ব্রাঞ্চের ফোনেও মোছা রেকর্ড দেখায় না তো<br>৪. Delete করা রোগীর Follow-up কার্ড লুকিয়েছে কিনা |

---

## দফা ৪ — একই Patient ID দুবার হওয়া আটকানো

**দুই স্তরে — দুটোই দরকার।**

**স্তর ১ (আসল তালা, Supabase-এ):** `patientId`-এর উপর একটা unique index। দুই ফোন যত দ্রুতই চেষ্টা করুক, ডেটাবেস নিজেই দ্বিতীয়টা আটকে দেবে।
**স্তর ২ (অ্যাপে):** ডেটাবেস আটকালে অ্যাপ **নিজে থেকে পরের সিরিয়াল নিয়ে আবার চেষ্টা করবে** (সর্বোচ্চ ৩ বার)। স্টাফ কিছু টেরও পাবেন না।

| | |
|---|---|
| **কোন File** | **অ্যাপে ২টা:** `native/PatientIdGenerator.kt` · `native/RegistrationRepository.kt`<br>**SQL:** ১টা index তৈরি (কিছু মোছে না) |
| **আগে যা করতে হবে** | index বসানোর আগে দেখতে হবে **এখনই কোনো ডুপ্লিকেট আছে কিনা** — সেটার জন্যও শুধু-দেখার Query দেব। ডুপ্লিকেট থাকলে আগে সেগুলো আপনি ঠিক করবেন |
| **Design/Workflow** | কিছুই বদলাবে না। **পুরনো কোনো Patient ID বদলাবে না।** ID-র চেহারা (`FLK-26072026-001`) একই |
| **Rollback** | index মুছে ফেলা নিরাপদ (`drop index`) — ডেটার কিছু হয় না। অ্যাপের ২টা ফাইল ফেরত |
| **Test** | ১. দুই ফোন থেকে একই ব্রাঞ্চে একই সময়ে Registration → দুটো আলাদা ID হয় কিনা<br>২. একটা সাধারণ Registration — ID ঠিক ক্রমে বাড়ছে কিনা<br>৩. নেট দুর্বল অবস্থায় Registration |

---

## দফা ৫ — ফটো 600px, তবু ১০০–১৫০ KB-এর মধ্যে

**কী করব:** ছবির মাপ 400px → **600px**, আর গুণমান 70 → **85**। মেপে দেখা গেছে এতে ফাইল হয় প্রায় **৫৪ KB** (ডেটাবেসে প্রায় **৭২ KB**) — আপনার ১৫০ KB সীমার অনেক নিচে। **৩–৪ গুণ বড় হওয়ার দরকারই নেই।**

| | |
|---|---|
| **কোন File** | **২টা:** `native/PhotoUtils.kt`<br>`native/PatientPhotoActivity.kt` — ⚠️ এখানে একই কাজের **দ্বিতীয় একটা কপি** আছে; একটা বদলে অন্যটা না বদলালে দুই স্ক্রিনে দুই মাপের ছবি হবে |
| **Design/Workflow** | ফটোর ডিজাইন, গোল/চৌকো আকার, বসানোর জায়গা — কিছুই বদলাবে না। শুধু **নতুন তোলা ছবি** পরিষ্কার হবে |
| **পুরনো ছবি** | **একটাও ছোঁয়া হবে না, মোছা হবে না।** পুরনো ছবি আগের মতোই দেখাবে |
| **Rollback** | ওই ২টা ফাইলে সংখ্যা দুটো (600 → 400, 85 → 70) ফিরিয়ে দিলেই হলো |
| **Test** | ১. নতুন রোগীর ছবি তুলে দেখুন মুখ পরিষ্কার কিনা<br>২. Patient Card, Report Card, Print-এ ছবি ঠিক দেখাচ্ছে কিনা<br>৩. পুরনো রোগীর পুরনো ছবি আগের মতোই আছে কিনা<br>৪. ছবি সেভ হতে বেশি সময় লাগছে কিনা |

---

# ভাগ ৩ — কাজের ক্রম (আমার পরামর্শ)

সবচেয়ে নিরাপদ থেকে শুরু করে ঝুঁকিপূর্ণর দিকে:

| ক্রম | দফা | কেন এই ক্রমে |
|---|---|---|
| ১ | **ফটো (দফা ৫)** | সবচেয়ে সহজ — মাত্র দুটো সংখ্যা, ডেটার কোনো ঝুঁকি নেই |
| ২ | **Sync (দফা ১)** | ১টা ফাইল, দৃশ্যমান কিছু বদলায় না, লাভ সবচেয়ে বেশি |
| ৩ | **Patient ID (দফা ৪)** | আগে দেখার Query, তারপর index, তারপর অ্যাপ |
| ৪ | **Patient Edit (দফা ২)** | ২টা ফাইল, ভালো করে পরীক্ষা দরকার |
| ৫ | **Delete (দফা ৩)** | সবচেয়ে স্পর্শকাতর — সবার শেষে, ধীরে |

**একবারে একটা।** প্রতিটার পর আপনি লাইভ টেস্ট করে "ঠিক আছে" বললে তবেই পরেরটা।

---

## এখন পর্যন্ত অবস্থা

- V131 থেকে আলাদা **মাত্র ১টা ফাইল**: `app/build.gradle.kts` (versionCode 132, versionName "V132")
- আগের সেশনের অনুমোদিত ফিক্সগুলো (তারিখ ডট, সময় am/pm, RMP সিরিয়াল, স্টেজ) আগের মতোই আছে
- **আপনার অনুমতি ছাড়া নতুন কোনো Fix শুরু করা হয়নি**

---

**🔒 LOCK NOTE:** এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন মালিককে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না, অ্যাপ স্লো করা যাবে না।
