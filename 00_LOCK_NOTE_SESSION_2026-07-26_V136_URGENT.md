# 🚨 LOCK NOTE — V136 (2026-07-26, জরুরি গতি-সমস্যার ফিক্স)

**Base:** V135 · **নতুন:** V136 · **ZIP:** `PILES_CLINIC_APP_V136_FINAL.zip`
**versionCode / versionName:** 135 → **136 / V136**

## TK যা রিপোর্ট করেছেন
Follow-up পর্দায় **Visit ও Patient ট্যাব ঘণ্টার পর ঘণ্টা "Loading..."**, সংখ্যা দেখায় **0 Visit / 0 Patient**। Enquiry ট্যাব ঠিক চলছে (46)।

## যা ঠিক করা হয়েছে (একটাই ফাইল)
**`native/DeletedGuard.kt`** — V134-এ যোগ করা Delete-guard প্রতিটি সফল cloud-save-এর সময়:
- SharedPreferences খুলত,
- পুরো JSON তালিকা পার্স করত,
- এবং **synchronous `commit()`** দিয়ে ডিস্কে লিখত।

Visit ও Patient ট্যাব লোড হওয়ার সময় প্রতিটি সারির জন্য **self-heal upsert** চলে (কয়েক ডজন)। ফলে লোডিং-এর মধ্যেই কয়েক ডজন ব্লকিং ডিস্ক-রাইট হচ্ছিল — পর্দা আটকে থাকার মতো দেখাত।

**এখন:** তালিকাটা মেমরিতে রাখা হয়, ডিস্ক পড়া হয় **একবারই**, স্বাভাবিক ক্ষেত্রে (মোছার তালিকা ফাঁকা বা id নেই) **কোনো ডিস্ক-কাজই হয় না**, আর লেখা হয় `apply()` (ব্যাকগ্রাউন্ড) দিয়ে — `commit()` নয়।

Delete-guard-এর কাজ (মোছা রেকর্ড ফিরে না আসা) হুবহু আগের মতোই আছে।

## অন্য কিছু বদলানো হয়নি
V135-এর চারটি Audit-fix, V134-এর সব লক করা ডিজাইন (Remark বক্স, চকলেট বোতাম, ব্রাঞ্চ পপ-আপ, সতর্কবাতি), ওয়েব অ্যাপ, ডেটাবেস, SQL, RLS, ছবি, Patient ID — **কিছুই ছোঁয়া হয়নি**।

## ⚠️ সৎ কথা — এটাই একমাত্র কারণ কিনা তা এখানে প্রমাণ করা যায়নি
- **Android Studio Build / লাইভ টেস্ট — NOT TESTED** (এই পরিবেশে সম্ভব নয়)।
- উপরের কারণটি কোড দেখে নিশ্চিতভাবে একটি আসল ত্রুটি, এবং লোডিং পথেই পড়ে।
- **কিন্তু আরেকটি সম্ভাব্য কারণ আছে:** Supabase ফ্রি-প্ল্যানের কোটা/ব্যান্ডউইথ শেষ হয়ে গেলে ভারী কোয়েরিগুলো (Visit/Patient) ব্যর্থ হয়, হালকাটা (Enquiry) চলে।
- **দ্রুত বোঝার উপায় (TK করতে পারেন):** পুরনো APK (V133 বা তার আগের) ফোনে বসিয়ে Visit/Patient ট্যাব খুলুন।
  - পুরনো APK-তেও "Loading..." → কারণ ক্লাউড/কোটা, অ্যাপের কোড নয়।
  - পুরনো APK-তে ঠিক চলে → কারণ V134-এর কোড, আর এই V136 ফিক্সেই সেটা সারবে।

---

**🔒 LOCK NOTE:** এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না, অ্যাপ স্লো করা যাবে না।
