# 📵 V718 — "আর কল লাগবে না" শুধু **Patient** কার্ডে · "কল বন্ধ" লেখা তুলে দেওয়া

**তারিখ:** ২৬.০৮.২০২৬ · রাত ~১০.৪৫ IST
**TK-এর লাইভ রিপোর্ট (ছবিসহ):** *"কল বন্ধ, এখানে কেন লেখা আছে"*
**TK-এর নির্দেশ:** *"কল লাগবে না এটা শুধু পেশেন্ট এর ক্ষেত্রেই হবে। enquiry
visit এই সমস্ত ক্ষেত্রে হবে না — কারণ সেই ক্ষেত্রে একটা নিয়ম অলরেডি করাই
আছে, অর্থাৎ পাঁচ বার ফোন কল করার পরে অটোমেটিক রিজেক্ট হয়ে যায়। … আর তাছাড়া
সিস্টেমে ওটা দেখানোরও দরকার নেই, লুকিয়ে থাকলেও চলবে।"*

---

## ১. আমার ভুল (V711) — সৎভাবে

V711-এ কার্ডে **"📵 কল বন্ধ"** লেখাটা বসিয়েছিলাম, শর্ত ছিল —
`nextFollow` ফাঁকা **এবং** `lastCallDate` আছে।

**সেটা ভুল ছিল।** যে রোগীর পরের কলের তারিখ **এখনো বসানোই হয়নি**, তাঁর কার্ডেও
লেখাটা উঠত। TK-এর ছবিতে **MOKIM** (শেষ কল ১৯.০৮) ও **MUZAFFAR HUSSAIN**
(শেষ কল ২৬.০৮) — দুজনেই ভুল করে "কল বন্ধ" দেখাচ্ছিলেন।

**আসল কারণ:** বোতাম চাপলে শুধু `nextFollow` ফাঁকা করা হয় — আলাদা কোনো চিহ্ন
রাখা হয় না। তাই "কে সত্যিই বন্ধ করেছেন" আর "কার তারিখ বসেনি" — দুটো আলাদা
করার উপায় ছিল না।

---

## ২. TK-এর সিদ্ধান্ত অনুযায়ী যা করা হলো

| # | কাজ | ফোন | ওয়েব |
|---|---|---|---|
| ১ | **"📵 কল বন্ধ" লেখাটা পুরোপুরি তুলে দেওয়া** | ✅ | — (ওয়েবে কখনো ছিলই না) |
| ২ | **বোতামটা শুধু "Patient" ট্যাবের কার্ডে** (stage = `Treatment`) | ✅ | ✅ |
| ৩ | **Enquiry-তে বোতাম বাদ** (৫ কলের পরে অটো-রিজেক্ট নিয়ম আগে থেকেই আছে) | ✅ | ✅ |
| ৪ | **Visit-এ বোতাম বাদ** | ✅ | ✅ |

### 🔍 নাম নিয়ে সাবধানতা (আন্দাজে নয়, কোডে মিলিয়ে দেখা)
এই অ্যাপে ট্যাবের নাম আর ভিতরের `stage`-এর নাম **উল্টো**:

| পর্দায় ট্যাব | ভিতরে `stage` | প্রমাণ |
|---|---|---|
| **Patient** | `"Treatment"` | `binding.tabPatient.setOnClickListener { switchTab("Treatment") }` |
| **Visit** | `"Patient"` | `DraftRepository` · V681-এর নোট |
| Enquiry | `"Inquiry"` | — |

⇒ TK যেটাকে "পেশেন্ট ট্যাগ" বলছেন, কোডে সেটা `stage == "Treatment"`।
**এই মিলটা কোডে চোখে দেখে নেওয়া হয়েছে** — ভুল ধাপে বসালে উল্টো ফল হত।

---

## ৩. যা ছোঁয়া হয়নি
- বোতামটার **কাজ** (`saveNoMoreCalls` → `updateNextFollow(id, "")`) এক অক্ষরও বদলায়নি
- Enquiry-র **"আসবে" / "শুধু ফোন করব"** — দুটো বোতামই অক্ষত
- ক্যালেন্ডার · তারিখ বাছা · **Save Date / Skip** · Overdue/Today Due/xd Due
  ব্যাজ — কিছুই বদলায়নি
- ৫-কলের অটো-রিজেক্ট নিয়ম ছোঁয়া হয়নি
- কোনো তথ্য · টাকার হিসাব · ডেটাবেস — কিছুই নয়

## ৪. যাচাই
- ✅ `verify_kotlin_compile.py` — নতুন ভুল ০
- ✅ `node --check app.js` — পাশ (মাঝে টেমপ্লেটের ভিতরে ব্যাকটিক বসিয়ে একবার
  ভেঙেছিল — নিজে ধরে সঙ্গে সঙ্গে ঠিক করা হয়েছে)
- ✅ `tk_guard.py` — সব যাচাই পাশ
- ✅ ফোন ও কম্পিউটার — দুই দিকেই একই নিয়ম

**ভার্সন:** V717 → **V718**
