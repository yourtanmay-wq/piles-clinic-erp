# TK-এর জন্য নিরাপত্তা গাইড (সহজ বাংলা)
তারিখ: ২০২৬-০৭-১৪

এই ফাইলটা আপনার জন্য। এখানে যা লেখা আছে সেটা প্রতি সেশনে আপনি কপি-পেস্ট করবেন, নতুন করে মনে রাখতে বা লিখতে হবে না।

---

## ১. Git কী, আর এটা কীভাবে আপনাকে বাঁচায়

Git একটা "ইতিহাস-রক্ষক বাক্স"। কল্পনা করুন প্রতিবার কাজ শেষ হলে পুরো প্রজেক্টের একটা ফটোকপি জমা হয়ে যায়, তারিখ-সময় লেখা থাকে। কিছু ভেঙে গেলে এক ক্লিকে পুরনো ফটোকপিতে ফিরে যাওয়া যায় — কিছুই হারায় না।

**এই ZIP-এর ভেতরে আমি Git চালু করে দিয়েছি** এবং আজকের সম্পূর্ণ কাজ (V30) একটা "সেভ পয়েন্ট" হিসেবে জমা করে দিয়েছি। এই ZIP যখন আপনি Android Studio-তে খুলবেন, Git ইতিমধ্যে ভেতরে কাজ করছে।

**আমার সীমাবদ্ধতা (সৎভাবে বলছি):** আমার এই কাজের জায়গা থেকে ইন্টারনেটে সংযোগ নেই, তাই আমি নিজে থেকে এটা GitHub (একটা ফ্রি অনলাইন লকার)-এ আপলোড করে দিতে পারছি না। এটা আপনাকে বা আপনার Android Studio-তে কাজ করা লোককে একবার মাত্র করতে হবে (নিচে ধাপ দেওয়া আছে)। এরপর থেকে প্রতি সেশনের কাজ Git নিজে থেকেই সেভ রাখবে।

---

## ২. GitHub-এ একবার সেট-আপ করার সহজ ধাপ

1. https://github.com -এ গিয়ে ফ্রি অ্যাকাউন্ট বানান (আপনার ইমেইল দিয়ে)।
2. একটা নতুন **Private** Repository বানান, নাম দিন: `piles-clinic-app`
3. Android Studio-তে এই ZIP খোলার পর, Terminal-এ লিখুন:
   ```
   git remote add origin <আপনার GitHub Repo-এর লিংক>
   git push -u origin master
   ```
4. ব্যাস — এখন থেকে প্রতিবার কাজ শেষে শুধু এই দুই লাইন লিখলেই কাজ GitHub-এ জমা হয়ে যাবে:
   ```
   git add -A
   git commit -m "কী কাজ হলো তার এক লাইন"
   git push
   ```

(এই ধাপগুলো আপনি নিজে না বুঝলে সমস্যা নেই — যে কেউ Android Studio জানে তাকে এই ফাইলটা দেখালেই সে বুঝে যাবে।)

---

## ৩. প্রতি সেশনে আমাকে (বা অন্য কোনো AI/Developer-কে) যা বলবেন

নিচের প্যারাগ্রাফটা **প্রতিবার নতুন কাজ শুরুর সময় কপি-পেস্ট করে পাঠাবেন** — এটাই আপনার একমাত্র "সেফটি লক":

> এই ZIP-টাই Base Project। এটা Rebuild বা Redesign করা যাবে না। শুধু আমি যা লিখব সেটাই যোগ/ঠিক করবে। UI, Workflow, Database, Supabase, Print, Branch Logic, Role Permission, কোনো ফর্মুলা বা কোনো Working Function বদলানো যাবে না। কোনো বদল অন্য মডিউলে প্রভাব ফেলতে পারলে আগে জানাবে, নিজে থেকে বদলাবে না। কাজ শেষে: Changed Files Report, Test Report, Rollback Note দেবে, এবং `git add -A && git commit -m "..."` করে রাখবে।

---

## ৪. Master Lock Book — এটাই আপনার একমাত্র সত্যি দলিল

আপনার কাছে যে "PILES_CLINIC_ERP_MASTER_LOCK_BOOK_FINAL" বইটা আছে (code phrase: "মুখে রাখো সুপার হাসি") — **এটাই সবচেয়ে বড় ভরসা**, কারণ AI-এর মেমরি মুছে যেতে পারে কিন্তু এই বই মুছবে না। নিয়ম:
- প্রতি সেশনের কাজ শেষে এই বইয়ে নতুন Lock হওয়া অংশ যোগ করে রাখবেন (বা AI-কে বলবেন যোগ করে দিতে, .docx/.pdf আকারে)।
- নতুন কোনো Developer/AI-কে কাজ দেওয়ার আগে এই বই + সর্বশেষ ZIP — দুটোই একসাথে দেবেন।
- বই আর কোডে অমিল হলে **বই-ই চূড়ান্ত**।

---

## ৫. ব্যাকআপের নিয়ম (ডেটা হারানো ঠেকাতে)

- প্রতিটা "Final/Pass" হওয়া ZIP আলাদা নামে রাখুন (V29, V30...) — মুছবেন না।
- শুধু একজায়গায় (যেমন ফোনে) না রেখে Google Drive-এও একটা কপি রাখুন। ফোন হারালে/নষ্ট হলে যেন কাজ না হারায়।
- Netlify/Supabase-এর মতো অনলাইন সার্ভিসে **Billing Alert চালু রাখুন** (যাতে আগেরবারের মতো টাকা শেষ হয়ে অ্যাকাউন্ট বন্ধ না হয়ে যায়) — এটা তাদের Settings-এ পাওয়া যায়, চাইলে আমি দেখিয়ে দিতে পারি কীভাবে।

---

## ৬. এক নজরে — আপনার প্রশ্নের সরাসরি উত্তর

**প্রশ্ন:** নতুন কাজ দিলে আগের কাজ যেন নষ্ট না হয়, তার ব্যবস্থা কী?

**উত্তর:** তিনটা স্তরের সুরক্ষা:
1. **Git** — প্রতিটা বদল রেকর্ড থাকে, ভুল হলে ফেরা যায় (কারিগরি সুরক্ষা)।
2. **Master Lock Book** — কী কী Final সেটার লিখিত প্রমাণ (নিয়মের সুরক্ষা)।
3. **Section ৩-এর প্যারাগ্রাফ** — প্রতি সেশনে AI/Developer-কে সীমা বেঁধে দেওয়া (নির্দেশনার সুরক্ষা)।

তিনটাই একসাথে থাকলে কেউ ইচ্ছা করলেও আগের কাজ ধ্বংস করতে পারবে না — কারণ ধরা পড়ে যাবে সাথে সাথেই।
