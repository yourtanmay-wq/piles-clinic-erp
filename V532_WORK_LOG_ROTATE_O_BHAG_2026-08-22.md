# V532 — বাকি তিনটে কাজ (Rotate ×২ + Extra Income-এর ভাগ)
তারিখ: ২২.০৮.২০২৬ · V531 → **V532 (5.32)**

## ১. Registration পর্দায় 🔄 Rotate বোতাম
Patient Photo পর্দার **হুবহু একই নিয়ম** (V524): মূল ছবিটা ধরে রেখে প্রতিবার
**মূল থেকেই একবার** ঘোরানো হয় — দশবার ঘোরালেও ছবির মান নষ্ট হয় না।
⛔ ছবি না তুললে বোতামটাই দেখা যায় না ⇒ পর্দা **অবিকল আগের মতোই**।
⛔ সেভের পথ এক অক্ষরও বদলায়নি (`patientPhotoData` সেই একই data-URL)।

## ২. ওয়েবেও (Netlify) 🔄 Rotate
Patient Photo পাতায় বোতাম। ছবিটা canvas দিয়ে ৯০° ঘুরিয়ে **fileData()-র সেই
একই মাপ/মানের নিয়মে** (১৬০ KB সীমা) আবার বানানো হয়।
⛔ সেভের পথ **byte-for-byte পুরোনোটাই** — `savePatientPhotoInput`-এর শরীরটা
`wlv1SavePatientPhotoData()` নামে সরানো হয়েছে, একটাও অক্ষর বদলায়নি
(git-এর সঙ্গে md5 মিলিয়ে প্রমাণ করা: `a2e897df…` দু'দিকেই এক)।

## ৩. Extra Income পপ-আপ — ভাগের হিসাব এখন **সত্যি**
এতদিন সবসময় লেখা থাকত *"Shared 50-50 with the staff who took the enquiry."*
**ডেটাবেসের আসল নিয়ম তা নয়** (`hr.incentive_wanted()`, V418 SQL —
`round(st.amt / s.n, 2)`, যেখানে `n` = আলাদা স্টাফ-কোডের সংখ্যা):
- এনকোয়ারি ও রেজিস্ট্রেশন **দুজন আলাদা** ⇒ টাকা **দু'ভাগ**
- **একই লোক**, বা দু'জনের একজনকেই চেনা গেল ⇒ **পুরো টাকাটাই একজনের**

⇒ n=1 হলে "50-50" লেখাটা **ভুল তথ্য** দিত। এখন এই সারিতে আসলে কত পাওয়া
গেছে সেটাই দেখায় (₹১০০-র পুরো, না ₹৫০ — বাকিটা অন্যজনের)।
⛔ নতুন কোনো cloud-read নেই — অঙ্কটা আগে থেকেই হাতে। ফোন ও ওয়েব **একই লেখা**।

---
## যাচাই
`PatientIdentity` **15/15** · cham 27 · rep 25 · tt 19 · rc 19 · family 29 ·
rot 21 · futab 19 · gtest 13 — **সব ০ FAIL** · ওয়েব ৮/৮ `node --check` ✅ ·
সব XML ঠিক ✅ · বন্ধনীর হিসাব `git HEAD`-এর সাথে মিলেছে।

⚠️ Android SDK এখানে নামে না, তাই Activity ফাইল compile করে দেখানো যায়নি।

---
## ⚠️ TK-এর ছবিতে ধরা পড়া দুটো আলাদা সমস্যা — নিচে আলাদা করে লেখা আছে
এগুলো V532-এ **ঠিক করা হয়নি**, কারণ একটার সমাধানে TK-এর সিদ্ধান্ত দরকার।
`V532_TK_SCREENSHOT_FINDING.md` দেখুন।
