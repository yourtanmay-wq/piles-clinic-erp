# V553 — CHECK-UP হেডারে "Ref By"
তারিখ: ২২.০৮.২০২৬ · V552 → **V553 (5.53)** · **SQL লাগবে না** · ফোন + ওয়েব

TK-এর কাগজ (Patient History Sheet)-এর **ভাগ ১** মিলিয়ে দেখার পর TK-এর নির্দেশ:
> *"app এ যা আছে থাকবে, স্থান পরিবর্তন করবেন না।"* → *"Ref By যোগ করুন"*

---

## ১. মিলিয়ে দেখা (কোড ধরে) — ভাগ ১

কাগজের উপরের নীল ঘর = অ্যাপের CHECK-UP হেডার কার্ড।
**দুটোতেই আছে:** Name · Age · Sex · Ph No · Vill · P.S. · Dist
**অ্যাপে আছেই** (শুধু ওই রোগীর ভরা ছিল না): Occupation · P.O.
**কাগজে আছে, অ্যাপে ছিল না — ৪টে:** Date · Serial No · **Refd. by** · State

TK **শুধু "Ref By"** যোগ করতে বলেছেন। বাকি তিনটে (Date · Serial No · State)
**যোগ করা হয়নি** — TK বলেননি।

## ২. কী হলো

CHECK-UP হেডারে ঠিকানার **নিচে নতুন একটা লাইন**: `Ref By: …`

- তথ্যটা **রেজিস্ট্রেশনে আগে থেকেই আছে** — `refBy`
  (Self · Online · Offline · Dr. Visit · Old Patient · Others) ও `refDoctor`
  (RegistrationActivity.kt:1014,1050)।
- "Dr. Visit" হলে সঙ্গে **ডাক্তারের নামটাও** দেখায় — যেমন `Ref By: Dr. Visit · DR. PRANAB`।
  দুটোই সেভ করা তথ্য, **কিছুই বানানো হয়নি**।
- **কিছু না থাকলে লাইনটাই দেখায় না** — তাই পুরোনো রোগীর কার্ড আগের মতোই।

## ৩. ⛔ যা বদলায়নি

- **উপরের কোনো লাইন সরানো/বদলানো হয়নি** (TK-এর নির্দেশ) — এটা ঠিকানার নিচে
  **নতুন** একটা লাইন মাত্র।
- **Supabase-এ একটাও বাড়তি query নেই** — হেডার এমনিতেই রোগীর যে সারিটা আনে,
  `refBy`/`refDoctor` সেই একই সারিতেই আছে।
- **নতুন কলাম/টেবিল লাগেনি · SQL লাগেনি**।
- ছোট (mini) হেডার বারটা ছোঁয়া হয়নি।

## ৪. যাচাই

- **ওয়েব:** আসল `wlv1RefByLine` হুবহু তুলে চালানো — **৭টি পরীক্ষা, ৭টিই পাশ**
  (সাধারণ · Dr. Visit + নাম · শুধু নাম · কিছু না থাকলে লাইন নেই · ফাঁকা/স্পেস ·
  রোগী না থাকলেও ক্র্যাশ নয় · লেখা escape হয়)।
- `node --check` পাশ · XML বৈধ · Kotlin ফাইলের `{}`/`()` HEAD-এর সঙ্গে সমান।
- ⚠️ **সৎ কথা:** এখানে Android SDK নেই, তাই ফোনের অংশটা কম্পাইল করে দেখানো
  সম্ভব নয় — ওয়েবের যমজ কোডটাই চালিয়ে দেখানো হলো।

## ৫. ফাইল

| ফাইল | কী হল |
|---|---|
| `res/layout/activity_doctor_checkup.xml` | নতুন `tvPatientRefBy` লাইন |
| `clinical/DoctorCheckupActivity.kt` | একই সারি থেকে `refBy`/`refDoctor` পড়া |
| `03_NETLIFY_READY/app.js` | ওয়েবে একই (`wlv1RefByLine`) |
| `build.gradle.kts` · `index.html` · `version.json` | 552 → 553 |
