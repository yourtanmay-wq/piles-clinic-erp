# V416 — Salary পর্দা: Statement · Extra Income · নতুন সাজ (১৭.০৮.২০২৬)

## TK-এর চারটে নির্দেশ
1. Back বোতাম একদম নিচে ✅
2. তারিখ `31/12/2026` ধাঁচে ✅
3. বেতনের Statement (কার কবে কত) ✅
4. বেতন ছাড়াও **Extra Income** দেওয়া ও দেখা ✅

## SQL (TK নিজে চালিয়েছেন ✅)
`04_SUPABASE_DATABASE_SETUP/V416_SALARY_KIND_EXTRA_2026-08-17.sql`
`hr.salary_payments`-এ `kind` ('SALARY'/'EXTRA') ও `extra_reason` ঘর যোগ।
পুরনো সব সারি ডিফল্ট `SALARY` ⇒ **পুরনো হিসাব এক পয়সাও বদলায়নি।**

## 🔴 সবচেয়ে জরুরি নিয়ম (ভবিষ্যতের জন্য)
`kind='EXTRA'` সারি **বেতনের হিসাবে ধরা যাবে না** — নইলে "এই মাসে বাকি কত" ভুল হবে।
- ফোনে: `payKind()` · `renderSalary()`
- ওয়েবে: `salIsExtra()` · `salaryDueThisMonth()` · `profSalary()`
তিন জায়গাতেই বাদ দেওয়া আছে। নতুন কোনো বেতন-হিসাব লিখলে এটা মনে রাখবেন।

## সাজ (TK-অনুমোদিত)
- বোতাম **মডেল ৩** — সাদা ঘর, সবুজ কিনারা (বেতন) / সোনালি কিনারা (বাড়তি)
- পর্দা **মডেল ২** — উপরে তিনটে ছোট বাক্স: Salary paid · Extra · Total
- ⛔ **নেভি ব্লু কোথাও নেই** (TK-নির্দেশ)

## যাচাই
- বন্ধনীর হিসাব — **০** · `node --check` চারটে ওয়েব ফাইলেই ঠিক
- আসল ব্রাউজারে আসল কোড চালিয়ে ছবি তোলা — ₹2,000 বাড়তি টাকা বেতনের
  হিসাবে ঢোকেনি ("This month · Paid ✅" ঠিক আছে) ✅
- পুরনো সারিতে `kind` না থাকলে `SALARY` ধরা হয় — পরীক্ষা করা ✅

## ⚠️ TK-কে জানানো
`hr.salary_payments` টেবিল এখনো **সম্পূর্ণ ফাঁকা**। বেতন বাস্তবে দেওয়া হলেও
অ্যাপে তোলা হয়নি, তাই Statement আপাতত খালি দেখাবে। "Add Salary — choose month"
দিয়ে পুরনো মাসগুলো ভরলে তবেই ইতিহাস দেখা যাবে।

## বদলানো ফাইল
`modules/StaffProfileActivity.kt` · `03_NETLIFY_READY/profile.js` ·
`index.html` (cache `?v=v440`) · `build.gradle.kts` (**V416 / 4.16**) ·
🆕 `V416_SALARY_KIND_EXTRA_2026-08-17.sql`
