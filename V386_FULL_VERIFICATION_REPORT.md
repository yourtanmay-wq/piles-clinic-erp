# V386 — পূর্ণ যাচাই রিপোর্ট (সব পাতা)

**তারিখ:** 16.08.2026 · **পদ্ধতি:** শুধু আবার দেখা নয় — ভুল **খুঁজে বের করার** চেষ্টা।
৬টি আলাদা পরীক্ষা চালানো হয়েছে; **৪টি আসল সমস্যা ধরা পড়েছে ও সারানো হয়েছে**।

---

## 🔴 যে ৪টি সমস্যা ধরা পড়ল ও সারানো হলো

### ১. `index.html`-এর cache লিংক পুরনো ছিল — সবচেয়ে গুরুতর
৫টি module ফাইল (`notebook` · `profile` · `finance` · `partners` · `rmp_commission`)
বদলেছিলাম, কিন্তু `index.html`-এ ওদের লিংক পুরনো ভার্সনেই ছিল
(`v384` · `v340` · `v338`)। **ফলে ব্রাউজার পুরনো কপিই দেখাত — module-এর কোনো
কাজই চোখে পড়ত না।** সব সাতটি বদলানো ফাইল এখন `v413`।

### ২. `textarea`-র উচ্চতা জোর করে ৫০px হয়ে যাচ্ছিল
আমার নিয়ম `.input{height:50px!important}` `class="input"`-ওয়ালা **textarea**-কেও
ধরছিল, তাই `rows="2"` / `rows="3"` মানত না — Enquiry-র ঠিকানা, Registration-এর
অভিযোগ, সব এক লাইনে চেপে যাচ্ছিল (মেপে প্রমাণ: ৫০px)।
`height:auto` বসানোর পরে — ৫৮px (rows=2) ও ৭৬px (rows=3)। ✅
অ্যান্ড্রয়েডেও `FieldInputMultiline` = wrap_content (`styles_form.xml:26-35`)।

### ৩. টাইমলাইনের হেডার ৩৬০px পর্দায় ৪px বাইরে বেরোত
প্রান্ত-থেকে-প্রান্ত হেডারে স্থির `-16px` মার্জিন দিয়েছিলাম, কিন্তু `.page`-এর
পাশের ফাঁক **৩৮০px-এর নিচে 12px, উপরে 16px** (মেপে দেখা)। এখন
`100vw` + `calc(50% - 50vw)` — ফাঁক যত-ই হোক ঠিক মেলে। ✅

### ৪. ৪টি মরা CSS নিয়ম
`.anList` · `.anPrintList` · `.fieldErr` · `.err` — এই ক্লাসগুলো ওয়েবের কোডে
**কোথাও নেই**, তাই নিয়মগুলো কখনো কাজ করত না (দেখতে মনে হত কাজ হয়েছে)।
সরিয়ে দিয়ে **কেন সরানো হলো** তা কমেন্টে লিখে রাখা হয়েছে।

---

## ✅ পরীক্ষা ১ — অখণ্ডতা

| বিষয় | ফল |
|---|---|
| Android সোর্সে বদল | **০ ফাইল** (৫৭২টির একটিও নয়) |
| SQL ফোল্ডারে বদল | **০ ফাইল** — নতুন কোনো SQL লাগবে না |
| অন্য সব ফোল্ডারে বদল | **০ ফাইল** |
| ওয়েবে বদলানো ফাইল | **৮টি** (৭টি কোড + `index.html`) |
| সব JS syntax | ✅ PASS (৮টি ফাইল) |
| CSS ব্রেস ব্যালান্স | ✅ ঠিক |
| পুরনো CSS | ✅ **একটি অক্ষরও মোছা/বদলানো হয়নি** — শুধু নিচে ১০৪৭ লাইন যোগ |
| যোগ হওয়া media query | ✅ মাত্র দুটি: `max-width:899px` ও `min-width:900px` |
| মুছে যাওয়া JS ফাংশন | ✅ **একটিও নয়** |
| cache লিংক | ✅ বদলানো ৭টি ফাইলই `v413` |

## ✅ পরীক্ষা ২ — মরা CSS

যোগ করা **৩৮১টি ক্লাসের** প্রতিটি ধরে ধরে JS-এ খোঁজা হয়েছে।
৪টি মরা পাওয়া গিয়েছিল (উপরে) — সরানো হয়েছে। **বাকি সব ব্যবহৃত।**

## ✅ পরীক্ষা ৩ — ঢালাও নিয়মের ক্ষতি

`!important`-ওয়ালা **২০০টি** ঢালাও সিলেক্টর ধরে ধরে দেখা হয়েছে।
এর আগেই ধরা পড়েছিল যে ঢালাও `button{...!important}` ড্যাশবোর্ডের টাইল,
RMP পিকার, আয়/ব্যয় টাইল — সব সবুজ করে দিচ্ছিল; সেটা মূল থেকে সারানো হয়েছে
(`!important` তুলে দিয়ে)। এই দফায় নতুন করে `textarea`-র সমস্যা ধরা পড়ল।

## ✅ পরীক্ষা ৪ — ডেস্কটপ সত্যিই অক্ষত কি না

**পুরনো CSS বনাম নতুন CSS — ১৪৪০px-এ ২৯টি পর্দার ছবি পিক্সেল-মিলিয়ে দেখা হয়েছে:**

- **২৬টি পর্দা — হুবহু এক পিক্সেল, কোনো বদল নেই** ✅
- **৩টি বদলেছে** — Follow-up · Draft · Timeline। এগুলোই সেই তিনটি যাদের **HTML গঠন
  ভাঙা ছিল** (কলামের চওড়া ০px, বোতাম কার্ডের বাইরে)। আগে-পরে ছবি মিলিয়ে দেখা —
  আগেরগুলো ভাঙা, নতুনগুলো ঠিক। এটা **উন্নতি**, ক্ষতি নয়।

## ✅ পরীক্ষা ৫ — ২৯টি পর্দা, দুই মাপে যান্ত্রিক পরীক্ষা

৩৬০px ও ৩৯০px — দুটোতেই প্রতিটি পর্দার **প্রতিটি এলিমেন্ট** মেপে দেখা:
পাশে গড়ানো · পর্দার বাইরে বেরোনো · ৮px-এর ছোট লেখা · ২০px-এর ছোট বোতাম।

**সব পরিষ্কার**, শুধু দুটি **ইচ্ছাকৃত** জিনিস বাদে:

| চিহ্নিত | কেন এটা সমস্যা নয় |
|---|---|
| `.wlv1FuFilter` পর্দার বাইরে | তারিখ-ছাঁকনির সারি **আড়াআড়ি স্ক্রল** করে — অ্যান্ড্রয়েডেও `HorizontalScrollView` (`activity_followup.xml:156-187`)। পাতা নিজে পাশে গড়ায় না (মেপে দেখা)। |
| `anFuPill` ১৯px · `anFuId` ১১px | অ্যান্ড্রয়েডের **হুবহু মাপ** — পিল 10sp + 3dp padding, ID 10sp টেক্সট (`FollowUpActivity.kt:1648-1663`)। এগুলো বোতাম নয়, তিন-ট্যাপে সম্পাদনার লেখা। |

## ✅ পরীক্ষা ৬ — চোখে দেখা

২৯টি পর্দার পূর্ণ ছবি ৩৯০px-এ: `V386_FINAL_ALL_SCREENS.png`

---

## 📌 এখনো যা বাকি (সততার সঙ্গে)

1. **"🩸 TEST HERE" বোতাম** — অ্যান্ড্রয়েডের Visit কার্ডে আছে (`kt:1699`), ওয়েবে নেই।
   ওয়েবের `blood(id)` **patient id** চায় (`app.js:6869`), কিন্তু Visit ট্যাবে রোগী
   তখনো নিবন্ধিত না-ও থাকতে পারে — আন্দাজে বসালে বোতামটা কাজ করত না।
2. **Dialer-এর All / Missed ট্যাব** — ফোনের নিজস্ব কল-লগ পড়ে
   (`BranchSimHelper.kt:188-200`, `READ_CALL_LOG`); সার্ভারে জমা থাকে না।
   **ব্রাউজারে অসম্ভব** — কারণ পর্দাতেই লিখে দেওয়া আছে।
3. **ট্যাগ নিচের লাইনে নামলে আগের `|` দাগ লাইনের শেষে ঝুলে থাকে** — অ্যান্ড্রয়েডের
   `layoutTagsInRows()` রান-টাইমে চওড়া মেপে এটা এড়ায়; CSS-এ সম্ভব নয়। ছোট প্রসাধনী।
4. **Print Center-এর দ্বিতীয় দল** (Diet/Registration/Receipt/Checkup) অ্যান্ড্রয়েডে
   তালিকা-সারি, ওয়েবে টাইল — দুটো দল আলাদা করার মতো ক্লাস ওয়েবে নেই।
5. **লাইভ টেস্ট** — Supabase-এ আসল লগইন করে যাচাই TK-এর নিজের পরিবেশে বাকি।
   (এই যাচাই ব্রাউজারে আসল CSS + আসল markup দিয়ে করা, কিন্তু আসল তথ্য দিয়ে নয়।)
