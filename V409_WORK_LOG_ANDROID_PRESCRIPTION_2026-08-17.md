# V409 — ফোনেও অনুমোদিত Prescription ও Medicine Slip (১৭.০৮.২০২৬)

> **এক লাইনে:** কম্পিউটারে যে ডিজাইন অনুমোদিত (V397/V406), ফোনের Prescription ও
> Medicine Slip এখন **হুবহু সেটাই** ছাপে। TK ছবি দেখে অনুমোদন দিয়েছেন।
>
> ⛔ ছোঁয়া হয়েছে মাত্র **৩টে ফাইল**। অন্য কোনো কাগজ · কোনো হিসাব · কোনো
> workflow · ওয়েবের কিছুই বদলায়নি।

---

## ১. 🔴 আগে এই সীমাবদ্ধতাটা জেনে রাখুন

**এই কম্পিউটারে ফোনের কোড (Kotlin) কম্পাইল করে দেখা যায় না।** কম্পাইলার
নামানোর তিনটে পথ (Maven · Ubuntu apt · npm) চেষ্টা করা হয়েছে — নেটওয়ার্ক
তিনটেই আটকায় (403)। TK-কে কাজ শুরুর আগেই এটা পরিষ্কার করে বলা হয়েছে।

⇒ তাই পুরো নকশাটা **Kotlin-এর বাইরে** রাখা হয়েছে, যাতে ভুল হওয়ার জায়গা
  যতটা সম্ভব কম থাকে। কীভাবে — নিচে §২।

---

## ২. কীভাবে করা হলো (এবং কেন এভাবে)

### ক) নকশা হাতে লেখা হয়নি — যন্ত্রে বের করা হয়েছে
আগের পরিকল্পনা ছিল ~৪০০ লাইন Kotlin-এ HTML+CSS হাতে লেখা। সেটা **বাদ**।
বদলে:

1. আসল ওয়েব অ্যাপটা Chromium-এ চালিয়ে একজন নমুনা রোগীর **PRESCRIPTION** ও
   **MEDICINE SLIP** ছাপার পাতা তৈরি করা হলো।
2. ওই পাতার HTML আর তার উপর **সত্যিই যে CSS নিয়মগুলো খাটছে** — ৩,৫৬৯টা
   নিয়ম যাচাই করে তার মধ্যে **২১৭টা** — যন্ত্রেই বেছে বের করা হলো।
3. সেটাই একটা স্বয়ংসম্পূর্ণ ফাইল:
   `app/src/main/assets/www/rx_print.html` (৩২ KB)।
   ভিতরে রোগীর তথ্যের জায়গায় `{{NAME}}` ধাঁচের ১৯টা ঘর।

⇒ **নকশা নিয়ে Kotlin-এ কোনো হিসাব নেই**, তাই নকশা এদিক-ওদিক হওয়ার
  সুযোগও নেই।

### খ) Kotlin-এ শুধু ঘর ভরা
`print/PrescriptionHtmlPrint.kt` — ওই ফাইলটা পড়ে `{{...}}` ঘরে রোগীর নাম ·
বয়স · Patient ID · মোবাইল · তারিখ · লিঙ্গ · রোগ · ঠিকানা · রোগীর ইতিহাস ·
Diet · পরের তারিখ · ওষুধের সারি বসায়, তারপর ফোনের নিজের PrintManager দিয়ে
A4-এ ছাপে।

এটা প্রজেক্টের **নতুন কিছু নয়** — `DietChartHtmlPrint.kt` (V390) ও
`RegistrationHtmlPrint.kt` (B601) ঠিক এই পথেই ছাপে, দুটোই TK-এর ফোনে চলছে।

### গ) সুইচ এক জায়গায়
`print/PrintPreviewActivity.kt` — কাগজের নাম **Prescription** বা
**Medicine Slip** হলে নতুন পথে যায়, নইলে **আগের ঠিক আগের কোড** চলে।
এক জায়গায় সুইচ বলে প্রতিটা প্রবেশপথ (Doctor Queue · Print Center ·
Medicine Slip-এর তিনটে পথ) একসাথে ঢাকা পড়ে — ৫ জায়গায় হাত দিতে হয়নি।

---

## ৩. যাচাই — ছবি মিলিয়ে দেখা

কম্পিউটারের অনুমোদিত ছাপা আর ফোনের নতুন ফাইল — দুটোই ছবি করে **পিক্সেল ধরে
ধরে** মেলানো হয়েছে (দুটোই A4, print মিডিয়া, ১৫৮৮×২২৪৬):

| কাগজ | মোট পিক্সেল | আলাদা | শতাংশ | কোথায় |
|---|---|---|---|---|
| PRESCRIPTION | ৩৫,৬৬,৬৪৮ | **৫৩২** | ০.০১৪৯% | শুধু `℞`-এর কিনারা |
| MEDICINE SLIP | ৩৫,৬৬,৬৪৮ | **৫৩২** | ০.০১৪৯% | শুধু `℞`-এর কিনারা |

পার্থক্যের জায়গাটা একটাই ৩৪×৩৩ পিক্সেলের চৌকো — অক্ষরের ধার মসৃণ করার
পার্থক্য, চোখে ধরা পড়ে না।

**আরও জরুরি যাচাই:** উপরের মিলটা যদি শুধু "একই HTML দু'বার আঁকা" হতো তবে
তার মানে থাকত না। তাই দ্বিতীয় দফায় **Kotlin-এর ধাপগুলো হুবহু নকল করে**
(ফোনের আসল তথ্যের চেহারা দিয়ে — বড় হাতের ইতিহাস-শিরোনাম · `42 / Female` ·
`2026-09-02` ধাঁচের তারিখ · ফাঁকা ঘরে `-`) আবার ছবি বানিয়ে মেলানো হয়েছে —
**ফল একই: ৫৩২ পিক্সেল।**

⇒ অর্থাৎ ঘরের নাম · বড়-হাতের নিয়ম · তারিখ বদল (2026-09-02 → 02.09.2026) ·
  `-` কে ফাঁকা করা · ইতিহাসের শিরোনাম-মান আলাদা করা · Prescription আর Slip-এর
  পার্থক্য — সবগুলো ধাপ ঠিক কাজ করছে।

### আর যা যা মিলিয়ে দেখা হয়েছে
- বন্ধনীর হিসাব (`{}` `()` `[]`) — নতুন ও বদলানো দুটো Kotlin ফাইলেই **০**
- ব্যবহৃত প্রতিটা বাইরের নাম (`PrintDocumentModel` · `PrintSection` ·
  `BranchCatalog` · `BranchInfo`) সত্যিই প্রজেক্টে আছে
- ব্যবহৃত প্রতিটা `R.id.` (`pageNavRow` · `ivPreview` · `btnSavePdf` ·
  `btnSharePdf` · `btnPrint` · `btnClosePreview` · `tvError`) layout ফাইলে আছে
- টেমপ্লেটে কোনো **বাইরের ইন্টারনেট লিংক নেই** (০টা) — ফোন অফলাইনেও ছাপবে
- টেমপ্লেটের ১৯টা ঘরের একটাও ফাঁকা থেকে যায় না
- ওয়েবের চারটে ফাইল (`app.js` · `finance.js` · `profile.js` · `notebook.js`)
  এই কাজে **ছোঁয়া হয়নি** — আবার যাচাই করা, চারটেই ঠিক

---

## ৪. যা বদলাল — মাত্র ৩টে ফাইল

| ফাইল | কী |
|---|---|
| 🆕 `app/src/main/assets/www/rx_print.html` | অনুমোদিত নকশা (৩২ KB) |
| 🆕 `print/PrescriptionHtmlPrint.kt` | ঘর ভরে ছাপে |
| `print/PrintPreviewActivity.kt` | ৩০ লাইনের একটা সুইচ যোগ |
| `app/build.gradle.kts` | ভার্সন ৪০৮ → **৪০৯ / "4.09"** |

⛔ **`ClinicPdfBuilder.kt` (OWNER LOCKED) একটুও ছোঁয়া হয়নি।**
   Blood Test · Registration · Payment Receipt · Doctor Visit · Check-up ·
   Chamber Register — সব আগের পথেই, হুবহু আগের চেহারায়।

---

## ৫. 🟡 একটা জিনিস বদলে গেছে — TK-কে জানানো হয়েছে

এই দুটো কাগজে **Save PDF ও Share PDF বোতাম দুটো লুকানো** হয়েছে।

কারণ ওই বোতাম দুটো পুরনো নকশার PDF বানাত। রাখলে **ছাপা কাগজ এক নকশায়, আর
শেয়ার করা PDF আরেক নকশায়** যেত — সেটা আরও খারাপ হতো।

⇒ ফোনের নিজের প্রিন্ট-শিটেই **"Save as PDF"** আছে — সেখান থেকে সেভ করে
  শেয়ার করা যায়। তাই কাজ হারায়নি, শুধু বোতামের জায়গা বদলেছে।
  Print ও Close বোতাম আগের মতোই।

Diet Chart (V390)-এও ঠিক এই একই ব্যবস্থা, TK আগে সেটা মেনে নিয়েছেন।

---

## ৬. TK-এর যা বাকি

1. **V409 APK** বানিয়ে সব স্টাফের ফোনে বসানো
   (⚠️ ডেমো করা ফোনে আগে আনইনস্টল — V408-এর কারণে)।
2. বসানোর পর একজন রোগীর **Prescription** ও **Medicine Slip** ছেপে দেখা —
   কাগজটা `V409_PHONE_PROOF_*.png`-এর মতো এসেছে কিনা।
3. Netlify আপলোড (`?v=v428`) ও Egress-এর স্ক্রিনশট — আগের মতোই বাকি।

🔴 **বিল্ডে যদি কোনো ভুল ধরা পড়ে** — এই কম্পিউটারে Kotlin কম্পাইল করা যায়নি
   (§১), তাই ভুলের লেখাটা ছবি তুলে পাঠাবেন, সঙ্গে সঙ্গে ঠিক করে দেওয়া হবে।
   ফেরত যাওয়ার পথও সহজ: উপরের ৩টে ফাইল বাদ দিলেই আগের অবস্থা।
