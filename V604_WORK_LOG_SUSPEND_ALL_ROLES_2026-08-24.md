# V604 — Staff Profile: সবাইকে (Doctor/Field-সহ) Suspend করার সুযোগ
**তারিখ:** ২৪.০৮.২০২৬ · **ভার্সন:** V604 / 6.04

## TK-এর অনুরোধ
"যে কোনো ব্যক্তির ফোনে অ্যাপ চলবে না — সেরকম ব্যবস্থা নিরাপদে করা যায় কি?"
যাচাই করে দেখা গেল **ব্যবস্থা আগে থেকেই আছে** (B618, Suspend বোতাম) —
কিন্তু Staff Profile তালিকায় শুধু `role_kind = staff` দেখানো হতো, তাই
Doctor/Field-দের Suspend বোতামই চোখে পড়ত না।

## যা পাওয়া গেল (সৎভাবে লেখা)
এটা বাগ না — B306 (03.08.2026)-এ **ইচ্ছে করেই** এই সীমা বসানো হয়েছিল।
TK আজ ছবি-প্রুফ দেখে এই সিদ্ধান্ত **আংশিক উল্টে** দিতে বলেছেন।

## সমাধান
`StaffProfileActivity.kt`-এ তালিকার ফিল্টার `staff`-এর সাথে `doctor` ও
`field`-ও ধরবে (`master` বাদ, কখনোই না)। ⛔ ডেটা-টানা এক অক্ষরও বদলায়নি —
আগে থেকেই সব role-এর সারি টানা হতো (`select=...&order=person_code`, কোনো
role-ফিল্টার সার্ভারে ছিল না), শুধু রেন্ডার-লেভেলে বাদ পড়ত। তাই এই ফিক্সে
**কোনো বাড়তি egress-খরচ নেই**।

## নিরাপত্তা
- Backend suspend-যাচাই (SessionGuard.kt) আগে থেকেই Master ছাড়া সবার
  জন্য কাজ করে — তাই এটা নতুন কোনো ঝুঁকিপূর্ণ ব্যবস্থা না, আগে-থেকে-থাকা
  সুরক্ষা এখন UI-তে দৃশ্যমান হলো মাত্র।
- Master কখনো এই তালিকায় আসবে না/suspend হবে না।
- Doctor/Field-এর "Removed/Restore" ভাগ (V404) ছোঁয়া হয়নি — শুধু staff-এর
  জন্যই প্রযোজ্য থাকল, যেমন আগে ছিল।

## ওয়েব
ওয়েবে Suspend করার কোনো Admin UI-ই নেই (শুধু login-এর সময় suspended_until
যাচাই হয়) — তাই তুলনা করার কিছু নেই, ছোঁয়া হয়নি।

## যাচাই
| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ |
| `verify_version_json.py` | ✅ V604/6.04 |
| ব্রেস/প্যারেন গণনা | ✅ মিলেছে (Kotlin-সচেতন যাচাই) |
