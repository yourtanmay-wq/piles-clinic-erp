# 🔒 LOCK NOTE — V509

**DATE: 21/08/2026  TIME: 3.27Pm (IST)**
আগের ভিত্তি: `PILES_CLINIC_APP_V508_FINAL.zip`
**Android Studio Build pending — owner will build**

---

## ১ · TK এই সেশনে যা যা বলেছেন (হুবহু) এবং কী হলো

| # | TK-এর কথা | অবস্থা |
|---|---|---|
| ১ | *"এই ধরনের বাংলা থাকবে না"* (তালার পপ-আপের ছবি সহ) | ✅ হয়েছে |
| ২ | *"হ্যাঁ, সবাই পারবে — Visit Fee-ও ফেরতের সীমায় ধরা হবে"* | ✅ হয়েছে |
| ৩ | OUT TIME — *"৩. থাক"* (বাড়ি থেকেও পারবে, কিন্তু রিপোর্টে লেখা থাকবে) | ✅ হয়েছে |
| ৪ | *"এই সেশনে যে সমস্ত কাজ… সেগুলি ভালো ভাবে আগে যাচাই করুন"* | ✅ হয়েছে — **৪টে আসল বাগ ধরা পড়ে ঠিক করা হয়েছে** |

---

## ২ · 🔴 যাচাই করতে গিয়ে নিজের যে ভুলগুলো ধরা পড়ল (পাঠানোর আগেই)

> এগুলো TK-র কাছে যাওয়ার **আগেই** ধরা পড়েছে। সৎভাবে লিখে রাখা হলো, যাতে
> পরের সেশনে কেউ আবার একই ভুল না করে।

### বাগ ১ — 🔴🔴🔴 **টাকা-নষ্ট করা বাগ** (সবচেয়ে গুরুতর)
`PaymentModel.isVisitFeeRow()` লেখার ঘর (`remarks`) দেখেও "Visit Fee" চিনত।
কিন্তু **Refund সারির `remarks` হলো স্টাফের নিজের হাতে লেখা কারণ**।
তাই কেউ কারণ হিসেবে সবচেয়ে স্বাভাবিক কথাটাই — **"VISIT FEE REFUND"** — লিখলে
সেই ফেরতের সারিটাই "Visit Fee জমা" বলে গোনা হত।

**ফল হত:** ৫০০ ফেরত দিলে **১০০০ টাকার ভুল** — যোগ হত (+৫০০), আর বিয়োগটাও
বাদ পড়ত (−৫০০)। রোগীর ফেরতযোগ্য টাকা প্রতিবার **বেড়ে** যেত, Due কমে গিয়ে
**আসল বাকি টাকা লুকিয়ে** যেত, আর Reports-এ অন্য সংখ্যা দেখাত।

**ঠিক করা হলো:** `isVisitFeeRow()`-এর একেবারে শুরুতে `if (t == "refund") return false`
— এক জায়গায়, যাতে ভবিষ্যতে নতুন কোথাও থেকে ডাকলেও একই ভুল আর না হয়।
ডাকার দুই জায়গাতেও বাড়তি পাহারা (`!isRefundRow(row)`)।

### বাগ ২ — 🔴🔴🔴 **একই টাকা দুবার ফেরত**
Refund সফল হওয়ার পরেও পিছনের রোগী-কার্ডটা **পুরোনো সংখ্যা নিয়ে খোলা থাকে**।
DUE-তে আবার চাপ দিলে সেই পুরোনো "ফেরতযোগ্য ৫০০" নিয়েই ফর্ম খুলত, আর
**মাস্টারের ক্ষেত্রে সঙ্গে সঙ্গে অনুমোদিত** হয়ে যেত — ৫০০-র জমায় ১০০০ বেরিয়ে যেত।

⚠️ ফাঁকটা আগে থেকেই ছিল, কিন্তু **V509-এই সেটা শুধু-Visit-Fee-দেওয়া রোগীদের
নাগালে চলে আসত** (আগে তাঁদের ফর্মটাই খুলত না)।

**ঠিক করা হলো:** `saveRefund()` এখন সেভের ঠিক আগে **ক্লাউড থেকে তাজা হিসাব**
এনে দুটোর মধ্যে **ছোটটাই** নেয়। নেট খারাপ হলে আগের মতোই (সৎ রিফান্ড আটকায় না)।

### বাগ ৩ — 🔴🔴 **OUT TIME-এ ১২ সেকেন্ডের ঝুলে থাকা**
প্রথম চেষ্টায় অবস্থান দেখা শুরু হত **সেভ চাপার পরে**। তিনটে বিপদ ছিল —
১) পর্দায় ১২ সেকেন্ড কিছু হত না → স্টাফ আবার চাপতেন → **দুবার রিপোর্ট, দুবার WhatsApp**;
২) ঐ সময়ে ফোন ঘোরালে **বসানো OUT TIME মুছে যেতে পারত**;
৩) খাতায় বসত চাপার সময় নয়, **১২ সেকেন্ড পরের সময়**।

**ঠিক করা হলো:** এখন OUT TIME-এর পপ-আপ **খোলার সঙ্গে সঙ্গেই** অবস্থান দেখা শুরু
(`startPlaceProbe`)। স্টাফ যতক্ষণে কারণ বাছেন ততক্ষণে উত্তর হাতে — **বাড়তি অপেক্ষা
প্রায় শূন্য**। না এলে সর্বোচ্চ ৬ সেকেন্ড, তাও **আটকানো পপ-আপসহ** (দুবার চাপা অসম্ভব)।
সময়টা **চাপার মুহূর্তেই** ধরা হয় (`outAt`)।

### বাগ ৪ — 🔴🔴 **রোগীর পুরোনো তথ্য দেখানো** (ঝিলিক-পাহারায়)
`ExpectedTomorrowActivity`-তে চিহ্ন বানানো হত শুধু **নাম ও মোবাইল** দিয়ে।
কিন্তু সারিতে **রোগ · ঠিকানা · সর্বশেষ রিমার্ক**-ও আঁকা হয়। কেউ Follow-Up-এ
নতুন রিমার্ক লিখলে নাম-মোবাইল একই থাকায় **নতুন রিমার্কটা পর্দায় বসতই না** —
স্টাফ পুরোনো কথা পড়ে ফোন করতেন। **ঠিক করা হলো:** সারির সব ঘর চিহ্নে ধরা হয়।

### ছোট সংশোধন
- `awayText()`-এ `Locale.US` — নইলে বাংলা-ভাষার ফোনে **"৪.২ km"** (বাংলা অঙ্ক)
  লেখা হয়ে মাস্টারের রিপোর্টে চলে যেত (TK-এর ১ নম্বর নির্দেশের ঠিক উল্টো)।
- `PaymentModel.sourceLabel()`-এ "Refund" যাচাইটা উপরে তোলা হলো (বাগ ১-এর
  ভাইপো — টাকার হিসাব ঠিক ছিল, শুধু তালিকায় ভুল নাম দেখাত)।
- **`DoctorVisitAdapter` ও `TrashAdapter`-এর ঝিলিক-পাহারা তুলে নেওয়া হলো** —
  ঐ সারিগুলো ভিতরে কাঁচা JSON রাখে, যা তুলনা করা যায় না, তাই পাহারাটা
  **কোনোদিনই কাজ করত না**। কাজ না করা কোড রেখে "ঠিক হয়েছে" বলা যায় না।
  ⛔ সৎ কথা: **ঐ দুই পর্দায় ঝিলিক এখনো আছে।**
- `StaffProfileActivity` — ক্যাশের প্রথম আঁকাটা পাহারার বাইরে ছিল, তাই পাহারাটা
  কখনো কাজ করত না। এখন ঠিক।

---

## ২খ · 🔴 দ্বিতীয় দফা যাচাইয়ে আরও যে ভুলগুলো ধরা পড়ল

### বাগ ৫ — 🔴🔴🔴 **উপরে ৫৬, নিচে ৪৭** (TK-এর জলপাইগুড়ি স্টাফের রিপোর্ট)
উপরের **সংখ্যা** আর নিচের **তালিকা** দুই আলাদা নিয়মে ছাঁকা হত —
সংখ্যা repository-র `branchAllows()` মানত (যেটা V453-এর অনুমোদিত অতিরিক্ত
ব্রাঞ্চ Falakata + Birpara ধরে), কিন্তু তালিকা শুধু `user.branch`-এর সঙ্গে
**হুবহু** মিলিয়ে ঐ কার্ডগুলো **ছেঁটে ফেলত**। তাই একই ফোনে দুই সংখ্যা।
**ঠিক করা হলো:** তালিকাও এখন `CrossBranchStaffAccess`-এর অনুমোদিত তালিকা মানে।
⛔ পাহারা আলগা হয়নি — অনুমোদন ছাড়া কেউ কিছুই বেশি দেখে না; টাকার নিয়ম অক্ষত।

### বাগ ৬ — 🔴🔴🔴 **মাসের ৩২ তারিখ** (Monthly রিপোর্টে কল ০)
মাসের শেষ সীমা লেখা হত `"2026-08-32"` — যে তারিখ পৃথিবীতে নেই। `date` ঘরে
ডেটাবেস অনুরোধটাই বাতিল করে, তাই **App Calls · Outside Calls · Leave Days**
তিনটেই সবসময় ০। (Enquiry/Registration `text` ঘরে দেখে বলে ঠিকই আসত — TK-এর
ছবিতে হুবহু তাই।) **ঠিক করা হলো:** পরের মাসের ১ তারিখ।
⚠️ এটা **দ্বিতীয়বার** ঘটেছিল — ১১.০৮.২০২৬-এ ছুটির গোনায় (B618) একই ভুল ধরা
পড়ে ঠিক হয়েছিল, কিন্তু এখানে রয়ে গিয়েছিল।

### বাগ ৭ — 🔴🔴 **ওয়েবেও একই ভুল, আরও লুকিয়ে**
`03_NETLIFY_READY/notebook.js`-এ `-32` ছাড়াও `-31` ছিল — যেটা আরও চালাক ভুল:
৩১ দিনের মাসে কাজ করে, কিন্তু **ফেব্রুয়ারি · এপ্রিল · জুন · সেপ্টেম্বর ·
নভেম্বরে** অসম্ভব তারিখ হয়ে ০ দেখাত। তাই মাসে-মাসে লুকিয়ে থাকত। ঠিক করা হলো।


### বাগ ৮ — 🔴🔴🔴 **Trash Bin কোনোদিনই কাজ করেনি**
TK-এর রিপোর্ট: *"trash bin তো সারাজীবন ফাঁকাই দেখলাম, মাস্টার হিসাবে।"*
**কারণ প্রমাণ করে দেখা হয়েছে:** `fetchList()` ডিফল্টে `order=updatedAt.desc`
দেয়, কিন্তু **`trash` টেবিলে `updatedAt` ঘরটাই নেই** (ঘর মাত্র ছয়টা)। নেই-ঘর
ধরে সাজাতে বললে ডেটাবেস অনুরোধটাই বাতিল করে —
`ERROR: column "updatedAt" does not exist` — ফাঁকা তালিকা ফেরে, পর্দায় বসে
**"Trash empty"**। অর্থাৎ **মুছে যাওয়া কোনো রেকর্ড কোনোদিন ফেরানোই যেত না।**
**ঠিক করা হলো:** `deletedAt` ধরে সাজানো (এই টেবিলে ঘরটা সত্যিই আছে, আর
সবচেয়ে সাম্প্রতিক ডিলিট উপরে থাকে)।
⛔ কোনো তথ্য হারায়নি — `trash` টেবিলে সব এতদিন ঠিকই জমা ছিল।
✅ প্রকল্পের **সব টেবিল মিলিয়ে দেখা হয়েছে** — `updatedAt`-হীন টেবিল একমাত্র
   `trash`, তাই এই ভুল আর কোথাও নেই।


### বাগ ৯ — 🔴🔴🔴 **payments_pkey duplicate — চিরকাল আটকে থাকা পেমেন্ট**
TK-এর দুই ফোনের ছবি: KNE-LAXMI *"24 to sync (11 other work, 13 Payment)"* ও
COB-4 *"২টি তথ্য এখনো ক্লাউডে যায়নি (১ অন্য কাজ, ১ পেমেন্ট)"* — দুটোই
HTTP 409 · 23505 · `payments_pkey` duplicate।

**আসল কারণ (কোড ধরে প্রমাণিত):** পেমেন্টের জন্য **দুটো আলাদা অপেক্ষমাণ তালিকা**
আছে। `CloudWriteQueue`-তে V479-এর নিয়ম আছে ("নিজের id-তে pkey duplicate মানে
সারিটা ইতিমধ্যেই ক্লাউডে বসে আছে → সফল ধরে বাদ দাও") — **সেটা কাজ করে**।
কিন্তু `PaymentRepository`-র নিজের তালিকায় ওরকম কোনো নিয়ম **নেই**।
তাই সে প্রতিবার আবার পাঠায় → ৪০৯ আসে → পেমেন্ট তার তালিকাতেই থেকে যায়,
**আর সেই ব্যর্থতা `CloudWriteQueue`-তে নতুন একটা ভূতুড়ে এন্ট্রি বানিয়ে দেয়**।
(ক) সেটা বাদ দেয়, (খ) আবার বানায় — **"other work + Payment" চিরকাল ঘোরে**,
"send" চাপলেও শূন্য হয় না। ঠিক এই কারণেই সংখ্যা দুই ভাগে দেখাত।

**ঠিক করা হলো:** `recordTreatmentPayment()`-এ V479-এর ঐ প্রমাণিত নিয়মটাই বসানো
হলো — `_pkey` duplicate হলে ক্লাউড থেকে **আসল সারিটা পড়ে** সেটাই ফেরত দেওয়া হয়,
অর্থাৎ সফল। তখন পেমেন্ট (খ) থেকেও ওঠে, ভূতুড়ে এন্ট্রিও তৈরি হয় না।
⛔ টাকা দুবার বসতে পারে না — এখানে **কিছুই লেখা হয় না, শুধু পড়া হয়**।
⛔ পড়া ব্যর্থ হলে আগের মতোই retry — কিছুই হারায় না।

### ✅ নতুন সুবিধা — **স্টাফের নিজের হাজিরা-খাতা**
TK-এর নির্দেশ (কাগজের খাতার ছবিসহ): স্টাফ যেন সারা মাসের IN TIME · OUT TIME ·
ছুটির দিন নিজে দেখতে পারেন।
**নতুন কিছু বানানো হয়নি** — হাজিরা-খাতার পর্দাটা (DATE · IN · OUT · LEAVE)
আগে থেকেই তৈরি ও পরীক্ষিত ছিল, শুধু **মাস্টার** সেটা খুলতে পারতেন। এখন স্টাফের
নিজের পর্দায় **"🗓️ My Attendance Sheet"** বোতাম বসানো হলো।
সঙ্গে ছোট SQL প্যাচ (`V509_MY_ATTENDANCE_SHEET_2026-08-21.sql`) — একটিমাত্র
লাইন: `hr.is_master()` → `hr.is_master() or p_code = hr.my_code()`।
✅ লোকাল PostgreSQL-এ তিনটে পরীক্ষা পাশ: নিজের খাতা দেখা যায় · **অন্যের খাতা
   ০ সারি** · মাস্টার আগের মতোই সবার খাতা দেখেন।

### 🔻 যা বানিয়ে **তুলে নেওয়া হলো** (সৎভাবে)
সংখ্যা বাসি হলে ⚠️ দেখানোর ব্যবস্থাটা বানিয়েছিলাম, TK অনুমোদনও দিয়েছিলেন।
কিন্তু যাচাইয়ে দুটো জিনিস প্রমাণ হলো —
 (১) আসল কারণ ছিল উপরের **বাগ ৫**, বাসি তথ্য নয়; আর
 (২) ঐ ⚠️ **ভুল সময়েও জ্বলত** — অ্যাপের অন্য যে কোনো পর্দার একটা ব্যর্থ
     পড়াও তিনটে ট্যাবে চিহ্ন বসিয়ে দিত।
⇒ মিথ্যে সতর্কবাতি না-থাকার চেয়েও খারাপ। তাই পুরো ব্যবস্থাটা **তুলে নেওয়া
  হলো**, আর আসল রোগটাই সারানো হলো। `FollowUpActivity.kt` ও
  `SupabaseClient.kt` হুবহু V508-এ ফেরত, শুধু বাগ ৫-এর সংশোধনটুকু বসানো।

### 🔻 আরও একটা ঝিলিক-পাহারা তুলে নেওয়া হলো
`FollowUpActivity`-র সারি-তুলনার পাহারাটা **সিরিয়াল নম্বর পুরোনো দেখিয়ে দিতে
পারত** (সিরিয়াল তালিকার বাইরের হিসাব থেকে আসে)। তাই সেটাও বাদ।

---

## ২গ · পাহারাদার আরও শক্ত করা হলো

দুটো নতুন পাহারা বসানো হয়েছে, আর **ইচ্ছে করে কোড ভেঙে প্রমাণ করা হয়েছে যে
সে সত্যিই ধরে** (প্রতিবারই FAIL দিয়েছে, তারপর ঠিক করা হয়েছে):

1. **OUT TIME** — সময় চাপার মুহূর্তে ধরা হচ্ছে কিনা, আর অবস্থান না পেলেও
   OUT TIME বসে যাচ্ছে কিনা (সময়সীমাসহ)।
2. **অসম্ভব তারিখ (৩২/৩১)** — `call_date`/`work_date`/`leave_date`-এর পাশে
   কোথাও যেন আর কখনো না আসে। **ফোন ও ওয়েব — দুই দিকেই** দেখে, কারণ এবার
   ওয়েবেই ভুলটা টিকে ছিল।
3. **নেই-ঘর ধরে সাজানো** — SQL-এ যে টেবিলে `updatedAt` নেই, তাকে নিজের
   `order=` ছাড়া পড়া যাবে না। (Trash Bin-এর বাগটা ঠিক এভাবেই লুকিয়ে ছিল।)

---

## ২ঘ · 🌐 ফোন ↔ ওয়েব — দুই জায়গা মিলিয়ে দেখা হয়েছে

TK-এর প্রশ্ন: *"অ্যান্ড্রয়েড এবং ওয়েব দু জায়গাতেই সঠিকভাবে কাজ করেছেন তো?"*
প্রতিটা সংশোধন ওয়েবের কোডেও খুঁজে মিলিয়ে দেখা হয়েছে — **অনুমান নয়, লাইন ধরে**।

| সংশোধন | ওয়েবে ছিল? | ওয়েবেও একই দোষ? | কী করা হলো |
|---|---|---|---|
| Visit Fee ফেরতের সীমায় | হ্যাঁ | **হ্যাঁ** (`app.js:14122` visit_fee বাদ) | ✅ ওয়েবেও ঠিক করা হলো |
| Refund-কে "Visit Fee" ভাবা | হ্যাঁ | **না** — ওয়েবে refund আগেই যাচাই হয় (`app.js:2548`) | কিছু করার নেই |
| Trash Bin ফাঁকা | হ্যাঁ | **না** — ওয়েব `id` ধরে সাজায় (`app.js:1107,1144`) | কিছু করার নেই |
| payments 23505 চিরকাল আটকা | হ্যাঁ | **না** — ওয়েবের সব লেখা `upsert(onConflict:'id')` (`app.js:1325`), তাই ৪০৯ হয়ই না | কিছু করার নেই |
| সংখ্যা ≠ তালিকা (৫৬ বনাম ৪৭) | হ্যাঁ | **না** — ওয়েবে দুটোই এক পাইপে (`app.js:5183` ও `5189`) | কিছু করার নেই |
| স্টাফের নিজের হাজিরা-খাতা | RPC ছিল, দরজা ছিল না | **হ্যাঁ, মাস্টার-only** | ✅ ওয়েবেও বোতাম বসানো হলো |
| মাসের ৩২ তারিখ | হ্যাঁ | **হ্যাঁ**, উপরন্তু `-31`ও | ✅ আগেই ঠিক করা হয়েছে |

### 🔶 মিলিয়ে দেখতে গিয়ে আরও একটা ধরা পড়ল — **V453 ওয়েবে মানা হচ্ছিল না**
`canSeeFinal()` (`app.js:10765`) হলো `inScope()`-এর হুবহু যমজ, কিন্তু তাতে
**`wlv1CrossBranchAllows()` শর্তটা বাদ ছিল** — অথচ `scoped()` তালিকা ছাঁকে
ঐ ফাংশন দিয়েই। ফল: TK-এর V453 নির্দেশ (*"JPE-CRP যেন FALAKATA & BIRPARA-র
সমস্ত ডিটেইলস দেখতে পারে"*) **ফোনে মানা হত, কম্পিউটারে হত না**।
✅ শর্তটা বসানো হলো — এখন দুই জায়গায় এক নিয়ম।
⛔ পাহারা আলগা হয়নি: ঐ ফাংশন শুধু TK-এর নিজের অনুমোদিত মোবাইলটাই চেনে।

**সব ওয়েব-ফাইলের syntax যাচাই করা হয়েছে** (৮টা .js — সবগুলো OK)।

---

## ৩ · কী কী বদলাল (ফাইল ধরে)

| ফাইল | কী |
|---|---|
| `native/BiometricGate.kt` | তালার সব বার্তা ইংরেজি |
| `native/AppLock.kt` | পপ-আপের সব লেখা ইংরেজি |
| `modules/WorkNotebookActivity.kt` | তালার লেখা ইংরেজি · OUT TIME-এ অবস্থানের নোট (probe + ৬ সেকেন্ড + `outAt`) |
| `native/PaymentModel.kt` | `visitFeePaid` · `refundableTotal` · `isVisitFeeRow` (refund-পাহারাসহ) · `sourceLabel` ক্রম |
| `native/PaymentRepository.kt` | Visit Fee আলাদা গোনা · ফেরতের পকেট-ভাগ · `saveRefund`-এ তাজা হিসাব |
| `native/PaymentActivity.kt` | "Refundable now" লাইন · DUE-র দরজা · সীমার বার্তা |
| `native/ExpectedTomorrowActivity.kt` | চিহ্নে সব ঘর (বাগ ৪) |
| `modules/StaffProfileActivity.kt` | ক্যাশের আঁকাও পাহারায় |
| `native/RedrawGuard.kt` | **নতুন ফাইল** — ঝিলিক-পাহারা |
| `native/MedicinePaymentActivity.kt`, `native/AppointmentActivity.kt`, `native/FollowUpActivity.kt`, `native/TimelineAdapter.kt`, `native/ChamberAttendanceAdapter.kt`, `native/DoctorQueueAdapter.kt`, `native/CollectionAdapter.kt`, `native/PasswordCenterAdapter.kt` | ঝিলিক-পাহারা |
| `03_NETLIFY_READY/app.js` | Visit Fee ফেরত · DUE-র দরজা · canSeeFinal (V453) |
| `03_NETLIFY_READY/profile.js` | স্টাফের নিজের হাজিরা-খাতা |
| `03_NETLIFY_READY/notebook.js` | মাসের ৩২/৩১ তারিখ |
| `native/SupabaseClient.kt` | payments duplicate = সফল (বাগ ৯) |
| `native/TrashRepository.kt` | Trash Bin (বাগ ৮) |
| `modules/StaffProfileActivity.kt` | 🗓️ My Attendance Sheet |
| `04_SUPABASE_DATABASE_SETUP/V509_MY_ATTENDANCE_SHEET_2026-08-21.sql` | **নতুন SQL** |
| `app/build.gradle.kts`, `03_NETLIFY_READY/version.json` | V509 / 5.09 |

**⚠️ একটা ছোট SQL চালাতে হবে** — শুধু স্টাফের হাজিরা-খাতার জন্য:
`04_SUPABASE_DATABASE_SETUP/V509_MY_ATTENDANCE_SHEET_2026-08-21.sql`
(একটিমাত্র লাইন বদলায়; কোনো টেবিল/সারি তৈরি বা মোছা হয় না।)
বাকি সব সংশোধনের জন্য SQL লাগবে না। OUT TIME-এর নোট আগে থেকেই থাকা
`wn.notebook_days.check_out_reason` ঘরেই লেখা হয়। Live Supabase-এ কিছু চালানো হয়নি।
পুরোনো কোনো তথ্য মোছা হয়নি। V494/V495/V508 overwrite করা হয়নি।

---

## ৪ · ⚠️ যা এখনো বাকি (সৎভাবে)

1. **২৪টা আটকে থাকা কাজ (১৩টা পেমেন্ট)** — কারণ প্রমাণ হয়েছে (পেমেন্টের
   নিজস্ব আলাদা তালিকায়, যেখানে ছেড়ে দেওয়ার নিয়ম নেই), কিন্তু **TK-এর অনুমতি
   না পাওয়ায় ঠিক করা হয়নি**।
2. **রিমার্ক বদলানো যাচ্ছে না** — এখনো দেখা হয়নি।
3. **"3 Calls Pending Today"** চাপলে যা খোলে — এখনো ব্যাখ্যা করা হয়নি।
4. **DoctorVisit ও Trash পর্দায় ঝিলিক** — এখনো আছে (উপরে লেখা কারণে)।
5. **NoBengali-র ৩৯টা ছোট শব্দ** ও ডুপ্লিকেট `"বাংলা"` — TK-র সিদ্ধান্তের অপেক্ষায়।

---

## ৫ · পাহারার ফল

সব পাহারা চালানো হয়েছে — নিচের "যাচাই" অংশ দেখুন।
**Android Studio Build pending — owner will build**
