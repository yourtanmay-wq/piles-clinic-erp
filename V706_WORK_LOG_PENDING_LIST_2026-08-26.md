# V706 — "কোন রোগীর টাকা আটকে আছে" — এখন দেখা যাবে
**তারিখ:** ২৬.০৮.২০২৬ · **ভার্সন:** V706 / 7.06

## TK কী বলেছেন
> "12 item(s) not yet sent to the cloud (12 Payment)" — ছবি
> "কি ভাবে দেখে আপনাকে বলব, কোন পেশেন্ট এর পেমেন্ট আটকে রয়েছে সেটাই বা আমি জানবো কি করে"
> "হ্যাঁ বানান, তবে খুব সাবধানে করবেন। **বাংলা হবে না, শুধুমাত্র ইংরেজিতে করুন**"

ডেমো প্রুফ `V706_DEMO_PENDING_LIST.png` — অনুমোদনের পরে কাজ।

## যা তৈরি হলো
Dashboard-এর লাল বাক্সে **চাপ দিলে তালিকা** খোলে:
`রোগীর নাম` · `ধরন · রোগী নম্বর · তারিখ` · `Rs টাকা` · আর **কেন আটকে** —

| ব্যাজ | মানে |
|---|---|
| `Money row not sent yet` (লাল) | টাকার সারিটাই এখনো ক্লাউডে যায়নি |
| `Money sent - bill update pending` (হলুদ) | **টাকা ক্লাউডে পৌঁছে গেছে**, শুধু বিল আপডেট বাকি |
| `Money sent - patient card pending` (হলুদ) | **টাকা ক্লাউডে পৌঁছে গেছে**, শুধু রোগীর Follow-up কার্ড বাকি |

নিচে **Send All** — এটা হুবহু **আগের সেই `retry`**-ই চালায়, নতুন কোনো পাঠানোর পথ বানানো হয়নি।

## ফাইল
| ফাইল | কী |
|---|---|
| `native/PendingSyncStatus.kt` | নতুন `Item` + `details()` — **শুধু পড়ে**, ঠিক সেই একই SharedPreferences তালিকা থেকে যা `summary()` আগে থেকেই গোনে |
| `native/DashboardActivity.kt` | লাল বাক্সে চাপ → `showPendingListDialog()` (PremiumAlert হেডার, সব লেখা ইংরেজি) |
| `native/PaymentRepository.kt` | `pushPaymentToCloud()`-এ **ঐচ্ছিক** `whyOut: StringBuilder? = null`; `flushPending()` ব্যর্থ হলে সারিতে `lastWhy` লেখে |

## 🛡️ সাবধানতা — টাকার কোডে কী করা হয়েছে ও কী হয়নি
* `pushPaymentToCloud()`-এর **return মান · টাকার হিসাব · ক্লাউড-কল — একটাও বদলায়নি**।
  নতুন প্যারামিটারের ডিফল্ট `null`, তাই আগের দুটো ডাক অপরিবর্তিত।
* `lastWhy` **শুধু এই ফোনের ভিতরের তালিকায়** বসে — ক্লাউডে যায় না, আর
  `pushPaymentToCloud` ওই ঘরটা **পড়েও না**, তাই পাঠানোর সিদ্ধান্তে প্রভাব নেই।
* পাশের ছোট **"send" বোতাম আগের মতোই** সরাসরি পাঠায় (এক অক্ষরও বদলায়নি),
  **দীর্ঘ-চাপের** কাজটাও (B274) অপরিবর্তিত।
* তালিকা পড়া না গেলে বোতামটা **আগের আচরণেই** ফেরত যায় (সরাসরি retry) —
  কোনো অবস্থাতেই বোতাম "মরে" যাবে না।

## ⚠️ যা এখনো বাকি (সৎ কথা)
`lastWhy` বসে **পরের চেষ্টার সময়** — অর্থাৎ V706 বসানোর পরে একবার "Send All"
চাপলে (বা অ্যাপ নিজে চেষ্টা করলে) তবেই কারণটা দেখা যাবে। তার আগে ব্যাজটা
ফাঁকা থাকবে, বাকি সব (নাম · নম্বর · টাকা · তারিখ) সঙ্গে সঙ্গেই দেখা যাবে।

## যাচাই
| যাচাই | ফল |
|---|---|
| `verify_kotlin_compile.py` | ✅ নতুন ভুল ০ |
| `tk_guard.py` (২২টা মেশিন-যাচাই, §৯.১৪ বাংলা-বন্ধ সহ) | ✅ সব পাশ |
| নতুন সব লেখা ইংরেজি? | ✅ (TK-নির্দেশ) |

⛔ কম্পিউটারের অ্যাপে অফলাইন-তালিকা নেই (ব্রাউজার সরাসরি ক্লাউডে লেখে), তাই
app.js-এ কিছু বদলানোর দরকার হয়নি।

⚠️ Android চালানোর উপায় নেই — TK দেখে বলবেন।
