# V550 — ওয়েবের Chamber বোর্ডে এক নম্বরে দুজন হলে দুটো সারি
তারিখ: ২২.০৮.২০২৬ · V549 → **V550 (5.50)** · **SQL লাগবে না** · **ওয়েব-only**

TK: *"হ্যাঁ ধরুন, তবে খুব সাবধানে সততার সাথে কাজ করবেন, কোন কাজ আন্দাজে করবেন না,
যাচাই করে কাজ করবেন।"*

ফোনে এটা **V526**-এ হয়েছিল; ওয়েবে বাকি ছিল — এখন হলো।

---

## ১. সমস্যা (কোডে প্রমাণিত)

ওয়েবের `wlv1ChamberRows()` সারি বানাত **শুধু মোবাইল নম্বর ধরে** (`rows[m]`)।
V516-এর পরে এক নম্বরে স্বামী-স্ত্রী **দুজন আলাদা রোগী** থাকতে পারেন — তখন
দুজনের **নাম · Patient ID · টাকা · রিমার্ক** সব **একটাই সারিতে মিশে** যেত।

## ২. সমাধান — চাবির নিয়ম (V518/V520-এর সেই প্রমাণিত নিয়মই)

স্টাফ নিজে *"Different Patient — Same Mobile"* চাপলে তবেই রোগীর সারির আইডি হয়
`pat_<১০ সংখ্যা>_<লেজ>` ধাঁচের — **অন্য কোনো পথে এই ধাঁচ তৈরি হয় না**।

- এই ধাঁচের রোগী ⇒ চাবি **তাঁর নিজের আইডি** ⇒ **আলাদা সারি**
- বাকি সবাই ⇒ চাবি **আগের মতোই মোবাইল** ⇒ **এক অক্ষরও বদলায়নি**

### কে কোথা থেকে নিজের রোগী চেনে (সবই কোডে প্রমাণিত, ফোনের V526-এর হুবহু)

| উৎস | পরিচয় |
|---|---|
| `payments` | `payments.patientId` = রোগীর সারির আইডি |
| `patients` (আজকের রেজিস্ট্রেশন) | সারির নিজের `id` |
| `followups` (রিমার্ক) | `followups.refId` = রোগীর সারির আইডি |
| `enquiries` | রোগীর আইডিই নেই ⇒ **আগের মতোই মোবাইল** |

### সঙ্গে যা যা ঠিক হলো
- **Patient ID** — ঘোষিত আলাদা রোগীর কার্ডে এখন **তাঁরই** কোড বসে
  (আগে মোবাইল ধরে খোঁজায় অন্যজনেরটা বসতে পারত)।
- **"কোনটা আসল সারি"** (ভুলে দুবার রেজিস্ট্রেশনের নিয়ম) — ঘোষিত আলাদা রোগীকে
  আর ওই দলে ঢোকানো হয় না।
- **রিমার্ক** — নিজের Follow-up খাতা থেকেই; না থাকলে **ঘর ফাঁকা**,
  **অন্যজনের লেখা কখনো দেখানো হয় না**।
- **কার্ডে/Review-তে চাপ দিলে** — রোগীর বিবরণ · চিকিৎসার নোট · টাকা সংশোধন,
  আটটা জায়গাতেই এখন **সঠিক রোগীতেই** যায় (সারিটা নিজের আইডি সঙ্গে নিয়ে যায়)।
  ⛔ আইডি ফাঁকা (রোজকার ৯৯% ক্ষেত্রে) ⇒ প্রতিটা ফাংশন **হুবহু আগের মতোই** চলে।

## ৩. ⛔ যা বদলায়নি

- যে নম্বরে ঘোষিত আলাদা রোগী নেই — বোর্ড **অবিকল আগের মতোই** (পরীক্ষায় প্রমাণিত)।
- ভুলে দুবার রেজিস্ট্রেশন (খাতার সারি B30) — আগের মতোই **এক সারি**।
- এনকোয়ারি + রোগী একই নম্বরে — আগের মতোই এক সারি।
- টাকার হিসাব · Refund-এর নিয়ম · সাজানোর ক্রম · ছাপা কাগজ — কিছুই ছোঁয়া হয়নি।
- Supabase-এ **একটাও বাড়তি query নেই**; schema/RLS/production data ছোঁয়া হয়নি।
- অ্যান্ড্রয়েডের কোডে হাত পড়েনি (শুধু ভার্সন নম্বর)।

## ৪. সৎ কথা — একটা সীমা এখনো আছে (ফোনেও একই)

**"আসার কথা" (expected) ও "এসেছেন" (arrived) চিহ্নের সারিটা এখনো নম্বর ধরেই**
জমা হয় — আইডি `exp_<১০ সংখ্যা>` (ফোনেও হুবহু একই:
`ChamberAttendanceRepository.kt:1402,1435`)। তাই এক নম্বরে দুজন থাকলে **ওই দুটো
চিহ্ন দুজনের মধ্যে ভাগ হয়ে যায়**।
এটা বদলাতে হলে **জমা রাখার আইডির ধরনই** বদলাতে হবে, আর সেটা ফোন-ওয়েব দুই
জায়গায় একসাথে করতে হবে — **পুরোনো সারি যেন হারিয়ে না যায়**। তাই আন্দাজে
করিনি; আপনি বললে আলাদা ভাবে, সময় নিয়ে করব।

## ৫. যাচাই (চালিয়ে দেখা)

- আসল `wlv1ChamberRows` হুবহু তুলে (re-type নয়) চালানো — **১৬টি পরীক্ষা, ১৬টিই পাশ**:
  সাধারণ নম্বরে এক সারি ও সব আগের মতো · দুজন হলে দুটো সারি · টাকা কখনো মেশে না ·
  নাম/ID/রিমার্ক নিজের নিজের · অন্যজনের লেখা কখনো ধার করা হয় না · নোট নিজের খাতাতেই বসে।
- পুরোনো সব ওয়েব-পরীক্ষা আবার চালানো — **সবই পাশ**; `node --check` পাশ।
