-- ============================================================
-- প্যাচ — এই SQL-টা Supabase-এর SQL Editor-এ গিয়ে একবার Run করুন
-- ============================================================
-- কেন দরকার (V1193, ০৭.০৯.২০২৬, TK-নির্দেশ):
--   • "আমি তো পাঠালাম, তাহলে অল টাইম আমার হোম স্ক্রিনে কেন দেখাবে"
--   • "এটা হাইড রাখার ব্যবস্থা তো রাখতে হবে"
--   • "উপরের ঘন্টাতে নোটিফিকেশন আসুক"  ·  Accept হলে পাঠানো ব্যক্তির ঘন্টায় খবর
--
-- দুটো নতুন ঘর লাগে —
--   hiddenBy : যাঁরা নিজের হোম/ঘন্টা থেকে সরিয়েছেন তাঁদের মোবাইল (কমা দিয়ে)
--   ackBy    : "Accepted" খবরটা যাঁরা দেখে নিয়েছেন তাঁদের মোবাইল (কমা দিয়ে)
--
-- ⚠️ শুধু দুটো নতুন কলাম যোগ হয় — পুরনো কোনো ঘর/সারি/ডেটা ছোঁয়া হয় না।
-- একাধিকবার চালালেও কোনো সমস্যা নেই।
--
-- কীভাবে Run করবেন:
-- ১) Supabase Dashboard → SQL Editor → New query
-- ২) নিচের পুরো লেখাটা কপি-পেস্ট করে Run (সবুজ "Success" দেখা উচিত)
-- ============================================================

alter table public.doctor_reminders
    add column if not exists "hiddenBy" text default '';

alter table public.doctor_reminders
    add column if not exists "ackBy" text default '';

-- Run করা শেষে "Success" বার্তা দেখলে বুঝবেন ঠিকভাবে হয়েছে।
