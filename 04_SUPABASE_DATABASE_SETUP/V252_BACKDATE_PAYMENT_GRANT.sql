-- ============================================================
-- প্যাচ — এই SQL-টা Supabase-এর SQL Editor-এ গিয়ে একবার Run করুন
-- ============================================================
-- কেন দরকার (B337, 03.08.2026, TK-নির্দেশ):
-- এখন প্রচুর পুরনো পেশেন্টের (কয়েক বছর আগের) ডিটেইলস অ্যাপে তোলা হচ্ছে —
-- এই বাল্ক-আপলোডের সুবিধার জন্য Master নির্দিষ্ট স্টাফকে নির্দিষ্ট
-- দিন/সময়ের জন্য ব্যাকডেট-পেমেন্ট (তোলা/Edit/ডিলিট) করার সাময়িক অনুমতি
-- দিতে পারবেন — সেই সময়ের মধ্যে প্রতিটা আলাদা পেমেন্টে Master-এর
-- আলাদা অনুমোদন লাগবে না।
--
-- ⚠️ এই প্যাচ শুধু ১টা নতুন টেবিল বানায় — কোনো পুরনো টেবিল/কলাম/
-- ডেটা কোনোভাবেই ছোঁয়া হয় না। একাধিকবার চালালেও সমস্যা নেই
-- (IF NOT EXISTS)। `payment_backdate_requests` টেবিলের হুবহু একই
-- কনভেনশনে বানানো (public schema, text id, RLS নেই — বাকি সব টেবিলের
-- মতো app-এর anon key দিয়েই অ্যাক্সেস হয়)।
--
-- ⚠️ TK-এর জন্য মনে রাখার নোট: সমস্ত পুরনো পেশেন্টের ডিটেইলস অ্যাপে
-- উঠে গেলে, এই সাময়িক-অনুমতি ব্যবস্থা বন্ধ/অকেজো করে আবার Master-এর
-- অনুমোদন সবসময় বাধ্যতামূলক করে দেওয়া উচিত (কোড বদলাতে হবে না —
-- শুধু Briefing/ঘন্টা স্ক্রিন থেকে সক্রিয় সব অনুমতি Revoke করে দিলেই
-- এই টেবিল খালি থাকবে, ফলে সবার জন্য আবার সবসময় অনুমোদন লাগবে)।
--
-- কীভাবে Run করবেন:
-- ১) Supabase Dashboard-এ লগইন করুন
-- ২) বাঁ পাশের মেনু থেকে "SQL Editor" এ ক্লিক করুন
-- ৩) "New query" চাপুন
-- ৪) নিচের পুরো লেখাটা কপি করে পেস্ট করুন
-- ৫) "Run" চাপুন (সবুজ "Success" বার্তা দেখা উচিত)
-- ============================================================

create table if not exists public.backdate_payment_grants (
    "id" text primary key,
    "staffMobile" text,
    "staffName" text,
    "branch" text,
    "startDate" text,
    "endDate" text,
    "note" text,
    "active" boolean default true,
    "grantedBy" text,
    "grantedByName" text,
    "grantedAt" text,
    "revokedAt" text,
    "revokedBy" text
);

-- দ্রুত খোঁজার জন্য (কোনো নির্দিষ্ট স্টাফের এই মুহূর্তে সক্রিয় অনুমতি আছে
-- কিনা যাচাই করার সময় প্রতিটা পেমেন্ট-সেভে এই টেবিলে খোঁজা হবে বলে সূচক
-- জরুরি — নইলে টেবিল বড় হলে পেমেন্ট নেওয়া ধীর হয়ে যেতে পারে)।
create index if not exists idx_backdate_payment_grants_staff
    on public.backdate_payment_grants ("staffMobile", "active");

-- Run করা শেষে "Success" বার্তা দেখলে বুঝবেন ঠিকভাবে হয়েছে।
