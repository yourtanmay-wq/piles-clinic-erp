-- =====================================================================
-- V671 — Doctor Note & Reminder: সময়ও যোগ (২৫.০৮.২০২৬, TK-নির্দেশ)
-- =====================================================================
-- TK: "শুধুমাত্র তারিখ বাঁচলে হবে না, সময়টাও তো বাঁচতে হবে তবেই তো
-- নোটিফিকেশন সাউন্ড হবে।"
--
-- V656-এ শুধু doctorReminderNote/doctorReminderDate ঘর দুটো ছিল।
-- এখন একটা নতুন ঘর doctorReminderTime (HH:mm, ২৪-ঘণ্টা ফরম্যাটে
-- টেক্সট, যেমন "09:30") — কোন সময়ে মনে করাতে হবে সেটা রাখে।
--
-- ⛔ শুধু একটা নতুন ঘর যোগ হয় — কোনো ডেটা/টেবিল মোছা হয় না।
-- চালানোর নিয়ম: Supabase → SQL Editor → New query → পুরো ফাইল পেস্ট → Run।
-- =====================================================================

alter table if exists public.patients
  add column if not exists "doctorReminderTime" text;

-- "Success. No rows returned" দেখলেই হয়ে গেছে।
