-- ============================================================
-- প্যাচ — এই SQL-টা Supabase-এর SQL Editor-এ গিয়ে একবার Run করুন
-- ============================================================
-- কেন দরকার (V1196, ০৭.০৯.২০২৬, TK-নির্দেশ ও ফটো-প্রুফ পাশ):
--   • "যে স্টাফ হ্যান্ডওভার করে দেবে, এখানে J.H MANDAL-এর পাসওয়ার্ড
--      সেই staff-এর ফোনে কেন দেখাবে? তার জন্য তো একটা নোটিফিকেশন যাওয়া উচিত —
--      শুধুমাত্র যাকে টাকাটা বুঝে দেবে তার কাছে আর মাস্টারের কাছে, অন্য কারো কাছে নয়।"
--
-- এখন স্টাফের ফোনে আর কারো পাসওয়ার্ড লাগে না। টাকা কাকে দেওয়া হচ্ছে সেটা
-- বেছে "Send" করলেই নোটিফিকেশন যায় **শুধু তাঁর ও মাস্টারের** কাছে; তিনি নিজের
-- ফোনে স্বীকার করলে তবেই "received" হয়। তখন **যে স্টাফ দিয়েছিল তাঁকেই**
-- ফেরত-নোটিফিকেশন যাবে — তার জন্য তাঁর মোবাইলটা জমা রাখা দরকার।
--
-- ⚠️ শুধু একটা নতুন কলাম যোগ হয় — পুরনো কোনো ঘর/সারি/ডেটা ছোঁয়া হয় না।
-- একাধিকবার চালালেও কোনো সমস্যা নেই।
--
-- কীভাবে Run করবেন:
-- ১) Supabase Dashboard → SQL Editor → New query
-- ২) নিচের পুরো লেখাটা কপি-পেস্ট করে Run (সবুজ "Success" দেখা উচিত)
-- ============================================================

alter table public.chamber_close
    add column if not exists "handoverBy" text default '';

-- Run করা শেষে "Success" বার্তা দেখলে বুঝবেন ঠিকভাবে হয়েছে।
