-- ============================================================
-- প্যাচ — 27.07.2026 · চেম্বার বন্ধ হয়েছে কিনা মনে রাখার টেবিল
-- ============================================================
-- কেন দরকার:
-- রাত ৭টার পরে "চেম্বার এখনো বন্ধ হয়নি" মনে করানোর ব্যবস্থা বসছে।
-- কিন্তু এতদিন "চেম্বার বন্ধ হয়েছে" কথাটা কোথাও জমা থাকত না —
-- Close Chamber শুধু রেজিস্টার প্রিন্ট করত। ফলে এক স্টাফ বন্ধ করলেও
-- ওই ব্রাঞ্চের অন্য ফোনে মনে করানো বাজতেই থাকত।
--
-- এই টেবিলে প্রতিটি ব্রাঞ্চের প্রতিদিনের জন্য মাত্র একটি সারি জমা হবে
-- (কে বন্ধ করলেন, কখন)। পুরনো কোনো টেবিল, কোনো ডেটা ছোঁয়া হয়নি।
-- একাধিকবার চালালেও কোনো ক্ষতি নেই (IF NOT EXISTS)।
--
-- কীভাবে চালাবেন:
-- ১) Supabase Dashboard-এ লগইন করুন
-- ২) বাঁ পাশে "SQL Editor" → "New query"
-- ৩) নিচের পুরো লেখাটা কপি করে পেস্ট করুন
-- ৪) "Run" চাপুন — সবুজ "Success" দেখলে হয়ে গেছে
-- ============================================================

create table if not exists public.chamber_close (
  "id" text primary key,
  "branch" text,
  "date" text,
  "closedBy" text,
  "closedByName" text,
  "closedAt" text,
  "arrivedCount" text,
  "createdAt" text,
  "updatedAt" text
);

alter table public.chamber_close disable row level security;

-- "Success. No rows returned" দেখলে বুঝবেন ঠিকভাবে হয়েছে।
