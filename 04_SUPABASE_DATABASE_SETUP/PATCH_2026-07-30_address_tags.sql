-- ============================================================
--  PATCH 2026-07-30 : address_tags
--  TK-এর নির্দেশ (30.07.2026) — Enquiry/Visit/Patient কার্ডে ব্র্যাঞ্চ ও
--  রোগের নামের পাশে রোগীর ঠিকানার একটা ছোট ট্যাগ (গ্রাম/পোস্ট/থানা/জেলা
--  থেকে বেছে নেওয়া, বা স্টাফ নিজে লেখা) — সব কার্ডে এক থাকবে, স্টাফ
--  যেকোনো সময় বদলাতে পারবে।
--
--  এটা কী করে (সহজ কথায়):
--  এই ছোট টেবিলটা শুধু "কোন মোবাইলের জন্য কোন ট্যাগ" রাখে। patients ·
--  enquiries · followups — পুরনো কোনো টেবিলে হাত পড়েনি, নতুন কোনো ঘরও
--  ওখানে যোগ হয়নি।
--
--  ⛔ এখানে রোগীর অন্য কোনো তথ্য বা টাকার হিসাব থাকে না — শুধু মোবাইল ও
--     ট্যাগের লেখা।
--  ⛔ এই SQL একাধিকবার চালালেও কোনো ক্ষতি নেই (if not exists)।
--
--  TK-কে যা করতে হবে: Supabase → SQL Editor → এই ফাইলের লেখাটা পেস্ট
--  করে RUN → "Success" দেখালেই কাজ শেষ।
-- ============================================================

create table if not exists public.address_tags (
  "id"        text primary key,   -- মোবাইলের শেষ ১০ সংখ্যা
  "mobile"    text,
  "value"     text,
  "updatedBy" text,
  "updatedAt" text
);

alter table public.address_tags disable row level security;
