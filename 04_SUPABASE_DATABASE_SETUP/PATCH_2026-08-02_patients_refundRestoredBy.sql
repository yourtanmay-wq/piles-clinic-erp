-- ============================================================
-- প্যাচ — এই SQL-টা Supabase-এর SQL Editor-এ গিয়ে একবার Run করুন
-- ============================================================
-- কেন দরকার:
-- Draft-এর নতুন "Refunded" ঘর (B302) থেকে TK চাইলে হাতে করে একজন রোগীকে
-- আবার Patient কার্ডে ফিরিয়ে আনতে পারবেন (নতুন টাকা জমা না দিয়েও) —
-- ঠিক আগে থেকেই থাকা "Complete despite Due" (completeApprovedBy) নিয়মের
-- হুবহু একই প্যাটার্নে। এই নতুন কলামে কে (মোবাইল) হাতে ফিরিয়েছেন সেটা
-- লেখা থাকবে — সত্যি রিফান্ড/পেমেন্ট রেকর্ড এতে একটুও বদলায় না, শুধু
-- এই একটা ফ্ল্যাগ Patient ট্যাবে দেখানো/না-দেখানোর সিদ্ধান্ত নেয়।
--
-- ⚠️ এই প্যাচ কোনো টেবিল drop করে না, কোনো কলাম rename করে না,
-- কোনো রেকর্ড delete করে না। শুধু একটা নতুন nullable কলাম যোগ করে।
-- একাধিকবার চালালেও কোনো সমস্যা নেই (IF NOT EXISTS)।
--
-- কীভাবে Run করবেন:
-- ১) Supabase Dashboard-এ লগইন করুন
-- ২) বাঁ পাশের মেনু থেকে "SQL Editor" এ ক্লিক করুন
-- ৩) "New query" চাপুন
-- ৪) নিচের পুরো লেখাটা কপি করে পেস্ট করুন
-- ৫) "Run" চাপুন (সবুজ "Success" বার্তা দেখা উচিত)
-- ============================================================

alter table public.patients add column if not exists "refundRestoredBy" text;

-- Run করা শেষে "Success" বার্তা দেখলে বুঝবেন ঠিকভাবে হয়েছে।
