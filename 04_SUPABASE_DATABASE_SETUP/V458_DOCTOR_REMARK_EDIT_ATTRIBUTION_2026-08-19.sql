-- =====================================================================
-- V458 — Doctor/RMP "Edit Doctor" পপ-আপের Remarks ঘরে কে/কবে বদলেছেন
--        সেটা নীরবে জমা রাখা (১৯.০৮.২০২৬, TK-নির্দেশ)
-- =====================================================================
-- TK: "কোন স্টাফ কল করেছিল কত তারিখে সেটা কিভাবে বুঝব?" — যাচাই করে দেখা
-- গেল "Edit Doctor / RMP" পপ-আপের Remarks ঘরটা সরাসরি লেখে, কোনো
-- স্টাফ/তারিখের হদিস রাখে না (Log Call/Edit Remark-এর মতো callHistory-তে
-- যায় না)। এই দুটো নতুন ঘর শুধু সেই একটা জায়গার জন্যই — কল-কাউন্ট/
-- callHistory কিছুই ছোঁয়া হয় না।
--
-- ⛔ শুধু দুটো নতুন ঘর যোগ হয় — কোনো ডেটা/টেবিল মোছা হয় না।
-- চালানোর নিয়ম: Supabase → SQL Editor → New query → পুরো ফাইল পেস্ট → Run।
-- =====================================================================

alter table if exists public.doctor_visits
  add column if not exists "remarksEditedBy" text,
  add column if not exists "remarksEditedAt" text;

-- "Success. No rows returned" দেখলেই হয়ে গেছে।
