# SESSION NOTE — 2026-07-19 — V90 → V91 (সম্পূর্ণ, বিস্তারিত)

**এই ফাইলটার উদ্দেশ্য:** এই একদিনের (অনেক লম্বা) সেশনে যা যা আলোচনা ও কাজ হয়েছে, তার সবকিছু এখানে বিষয়ভিত্তিক করে লেখা আছে — যাতে পরের সেশনে TK যেকোনো প্রশ্ন করলে, এই ফাইল পড়েই নির্ভুলভাবে উত্তর দেওয়া যায়। `00_PROJECT_STATE_MASTER_NOTE.md`-তেও এই সেশনের অংশ (সেকশন ৬৭-৭৪) লেখা আছে, কিন্তু সেটা অনেক পুরনো সেশনের সাথে মিশে আছে — এই ফাইলটা শুধু **আজকের সেশনের জন্যই**, খুঁজে বের করা সহজ করতে।

**চূড়ান্ত ফাইল:** `TK_PILES_V91.zip` (একাধিকবার একই নামে পাঠানো হয়েছে এই সেশনে — TK-কে জানানো হয়েছে এটা বিভ্রান্তিকর ছিল; **সর্বশেষ পাঠানো কপিটাই আসল**, timestamps মিলিয়ে TK নিজে নিশ্চিত করে নিয়েছেন)।

---

## ১. বিল্ড-ব্রেকিং bug ফিক্স (এই সেশনের শুরুতে)

- **PatientTimelineActivity.kt** — দুটো জায়গায় KDoc কমেন্টের শুরুর `/**` লাইন হারিয়ে গিয়েছিল (আগের এক এডিটে), Kotlin সেটাকে ভুল syntax ধরছিল ("Expecting member declaration")। TK ছবি পাঠিয়ে ধরিয়েছিলেন। **ফিক্স করা হয়েছে।**
- **DoctorVisitActivity.kt** — `showAddReferralIncome`-এর `nameField` (EditText) টাইপে ৪ জায়গায় `.text = "..."` (property-assignment) ব্যবহার হচ্ছিল, যেটা EditText-এ Kotlin-এ কাজ করে না (type mismatch)। **`.setText(...)` দিয়ে ঠিক করা হয়েছে।**
- এই দুটোই TK নিজে Android Studio-তে বিল্ড করে ছবি পাঠিয়ে ধরিয়েছিলেন — আমার bracket-balance checker এই ধরনের ভুল ধরতে পারে না বলে আগে থেকে ধরা পড়েনি। **এখন থেকে "এতিম কমেন্ট" স্ক্যানও প্রতি এডিটের পর চালানো হয় (স্থায়ী নিয়ম)।**

## ২. Follow-up স্ক্রিন — স্পিনার/রিফ্রেশ ফিক্স

- **সমস্যা:** Enquiry ↔ Visit ↔ Patient ট্যাব বদলালে পুরো স্ক্রিনে লোডিং স্পিনার দেখাত।
- **কারণ:** `switchTab()` ফাংশন `loadTab(stage)` কল করছিল silent=true ছাড়া (আগে ২৫-সেকেন্ড ব্যাকগ্রাউন্ড রিফ্রেশ আর onResume silent করা হয়েছিল, কিন্তু ট্যাব-বদলানোর এই পথটা তখন বাদ পড়ে গিয়েছিল)।
- **ফিক্স:** `switchTab()`-এ এখন `loadTab(stage, silent = true)`।

## ৩. Chamber Attendance / Chamber Date — একাধিক ধাপে বড় পরিবর্তন

### ৩.১ স্ক্রল-পজিশন + সাইলেন্ট রিফ্রেশ ফিক্স
- আগে প্রতিবার স্ক্রিনে ফিরে এলে (Payment/Registration থেকে) পুরো লিস্ট (adapter) নতুন করে তৈরি হতো — Android-এ এতে স্ক্রল পজিশন জোর করে উপরে চলে যায়।
- **ফিক্স:** `ChamberAttendanceAdapter.update()` দিয়ে শুধু ডাটা বদলানো হয় এখন, adapter object same থাকে। onResume-এর রিফ্রেশও silent করা হয়েছে।

### ৩.২ Enquiry তৈরি হওয়া আর "Arrived" না হওয়া
- আগে নতুন Enquiry তৈরি হলেই (আজকের তারিখ হলে) স্বয়ংক্রিয়ভাবে "Arrived" ধরে নিত — কিন্তু Enquiry ফোনেও হতে পারে (উদাহরণ: Laxmi-র কেস, রিমার্কে লেখা ছিল "10 Days baad")।
- **ফিক্স:** Enquiry তৈরি হওয়া আর Arrived-এর মধ্যে সম্পর্ক সম্পূর্ণ বিচ্ছিন্ন করা হয়েছে। Arrived এখন শুধু: Patient বাটন দিয়ে সরাসরি মার্ক, Mark Expected (ভবিষ্যতের তারিখ দিয়ে), বা সত্যিকারের Payment/Registration হলে।
- সংশ্লিষ্ট: এই পরিবর্তনের ফলে "Walk-in" লেবেল ভুল হয়ে যাচ্ছিল (ফোন-এনকোয়ারিকেও Walk-in বলছিল) — সেটা **"📞 Enquiry"**-তে ঠিক করা হয়েছে।

### ৩.৩ সম্পূর্ণ পেপার-রেজিস্টার-স্টাইল রিডিজাইন (TK-এর পুরনো কাগজের ছবি দেখে)
- আগে ছিল: বড় কার্ড, প্রতি কার্ডে Payment/Treatment/Action তিনটা বাটন।
- **এখন:** সহজ টেবিল-স্টাইল সারি — **SL | নাম+মোবাইল | Status | Fees | Cash | Online**।
- **নাম/মোবাইলে ১-ট্যাপ করলে সরাসরি সেই পেশেন্টের Patient Timeline ("Report Card") খোলে** (আগে ৩-ট্যাপ ছিল, এখন সহজ ১-ট্যাপ)।
- **শুধু মোবাইল নম্বরে আলাদাভাবে ট্যাপ করলে সরাসরি কল হয়** (TK confirmed, শেষে যোগ করা হয়েছে)।
- Payment/Treatment/Action বাটন এই লিস্ট থেকে সরিয়ে **Report Card স্ক্রিনেই** রাখা হয়েছে (Call/WhatsApp/Payment/Action বাটন সেখানে আগে থেকেই ছিল)।
- ঠিকানা/রোগের নাম এই লিস্টে আর দেখানো হয় না (TK-এর অনুরোধে, সরলতার জন্য)।

## ৪. Patient Timeline ("Report Card") — Progress/Remark যুক্তি বদল

- **Progress কলাম এখন Remark দেখায়** — বিশেষ করে Payment নেওয়ার সময় স্টাফ যে Treatment রিমার্ক লেখে সেটাই (আগে শুধু "₹৫০০ · CASH" auto-text দেখাত)।
- **একই দিনে একাধিক ঘটনা (একাধিক Payment ইত্যাদি) হলে একটাই সারি দেখায়** — সেদিনের সব Payment-এর টাকা যোগ হয়ে একসাথে, সবচেয়ে সাম্প্রতিক Treatment রিমার্কই Progress হিসেবে ওঠে। (ছোট ঝুঁকি নোট: একই দিনে একাধিক Payment থাকলে ৩-ট্যাপ এডিট সেদিনের **সবচেয়ে সাম্প্রতিক** Payment-টা খোলে, প্রথমটা না — এটা যুক্তিসঙ্গত ডিফল্ট হিসেবে TK-কে জানানো হয়েছিল, আপত্তি আসেনি)।
- **Due কলাম বাদ দেওয়া হয়েছে** টেবিল থেকে (উপরে Estimated/Paid/Due চিপেই আছে, পুনরাবৃত্তি দরকার নেই)।
- **Report Card-এ Progress এডিট করতে এখনো ৩-ট্যাপই লাগে** — TK-কে ১-ট্যাপ vs ৩-ট্যাপ নিয়ে অনেকবার জিজ্ঞেস করা হয়েছিল, স্পষ্ট প্রুফ দিয়েও বোঝানো যায়নি; শেষে TK নিজেই বলেছেন "তুমি ৬০ দিনের প্রজেক্ট জানো, তোমার মতো করো" — তাই **আগের প্যাটার্ন (টাকার ব্যাপারে সবসময় ৩-ট্যাপ সাবধানতা) বজায় রাখা হয়েছে, কিছু বদলানো হয়নি।**

## ৫. Close Chamber-এর "Print" — সম্পূর্ণ নতুন A4 PDF ফিচার (বড় কাজ)

**আগে যা ছিল:** "Close Chamber (Save & Print Arrived)" আসলে PDF প্রিন্ট করত না — টেক্সট বার্তা হিসেবে WhatsApp/Notepad-এ শেয়ার হতো। TK এটা দেখে "এটা তো চলবে না" বলেন এবং আসল A4 PDF চান, TK-এর কাগজের রেজিস্টারের মতো দেখতে।

**নতুন ফাইল তৈরি হয়েছে:** `ChamberRegisterPdfBuilder.kt` (সম্পূর্ণ আলাদা, `ClinicPdfBuilder.kt`-এর OWNER LOCKED প্রেসক্রিপশন-কোড ছোঁয়া হয়নি)। `ChamberAttendanceActivity.kt`-এর `finalizeAndShare()` এখন এই নতুন বিল্ডার কল করে, Android-এর native Print ডায়ালগ খোলে (Save as PDF বা প্রিন্টার — দুটোই করা যায়)।

**হেডার (কয়েকবার ঠিক করা হয়েছে, চূড়ান্ত অবস্থা):**
- বাঁয়ে লোগো, তার পাশে ব্রাঞ্চ-ভিত্তিক ক্লিনিকের নাম (Kishanganj → **"TK BISWAS PILES CLINIC"**, বাকি সব ব্রাঞ্চ → **"MAA AYURVED PILES CLINIC"** — এটা আগে থেকেই `BranchInfo.kt`-তে ছিল, শুধু নতুন প্রিন্টে reuse করা হয়েছে)।
- নিচে ঠিকানা+মোবাইল, **কেন্দ্রে** (ক্লিনিকের নামের সাথে সারিবদ্ধ — একবার ভুলবশত বাঁ দিকে সরে গিয়েছিল, ঠিক করা হয়েছে)।
- **Date+Day** ছোট করে পাতার উপরে-ডান কোণে (আলাদা লাইনে "Branch: X" ছিল, সেটা বাদ — ক্লিনিকের নামেই ব্রাঞ্চ বোঝা যায় বলে পুনরাবৃত্তি সরানো হয়েছে)।
- প্রেসক্রিপশনের "Kshar Sutra" ট্যাগলাইন ব্যাজ **এই রেজিস্টারে নেই** (TK স্পষ্ট করে বলেছেন এটা লাগবে না, সরল রাখতে)।
- উপরের সবুজ দাগ আর টেবিল-হেডারের মধ্যে ফাঁকা জায়গা কমানো হয়েছে।

**টেবিল কলাম (চূড়ান্ত):** SL | NAME | MOBILE | PATIENT ID | NEW/OLD | FEES | CASH | ONLINE — সব একই লাইনে (compact, single-line row, স্ট্যাক করা হয়নি)।
- **STATUS ("Arrived") কলাম বাদ** — যেহেতু এই প্রিন্টে শুধু Arrived-রাই থাকে, তাই "Arrived" বারবার লেখা অর্থহীন (TK ধরিয়েছিলেন)।
- **NEW/OLD** — TK-এর কাগজের কলাম হুবহু পুনরুৎপাদন। NEW = আজই Registration হয়েছে এমন পেশেন্ট, OLD = ফিরে আসা পেশেন্ট। রঙ: **NEW = হালকা সবুজ ব্যাকগ্রাউন্ড, OLD = ধূসর ব্যাকগ্রাউন্ড** (সারি জুড়ে)।
- **Patient ID** নিজের আলাদা কলামে (নাম/মোবাইলের নিচে স্ট্যাক করা ছিল, ৩০-সারি-এক-পাতায় ফিট করতে গিয়ে আলাদা কলামে সরানো হয়েছে)।
- সব নাম **CAPITAL letters**-এ।
- টাকা **₹ চিহ্ন** দিয়ে (Rs লেখা হয় না)।
- **সারি সাজানো এখন প্রকৃত সময়ানুক্রমিক (chronological)** — নাম অনুযায়ী বর্ণানুক্রমিক না। এর জন্য `ChamberAttendanceRow`-এ নতুন `arrivedAt` টাইমস্ট্যাম্প ফিল্ড যোগ করা হয়েছে (Registration বা প্রথম Payment/Mark-Arrived-এর `createdAt`, first-writer-wins)।

**পাতার আকার — TK-এর নির্দিষ্ট মাপ অনুযায়ী (হিসাব করে বসানো):**
- **ঠিক ৩০ জন এক A4 পাতায় ধরে।**
- **নিচে সবসময় কমপক্ষে ৩ ইঞ্চি (২১৬পয়েন্ট) ফাঁকা থাকে** — বাস্তবে প্রায় ৩.০৭–৩.২৬ ইঞ্চি (সেটিংস অনুযায়ী)।
- **⚠️ গুরুত্বপূর্ণ, TK-এর নিজের সংশোধনী:** প্রথমে আমি ভুল করে ভেবেছিলাম কম পেশেন্ট (যেমন ১৫ জন) হলে টেবিল ছোট হয়ে নিচে বেশি ফাঁকা জায়গা থাকবে — TK বলেছেন এটা "প্রফেশনাল না", প্রতিদিন একই রকম দেখানো উচিত। **তাই এখন টেবিলে সবসময় পুরো ৩০টা সারির জায়গাই থাকে (ROWS_PER_PAGE=30 ধ্রুবক) — কম পেশেন্ট হলে বাকি সারিগুলো খালি রুলড-লাইন হিসেবে দেখায় (কাগজের অব্যবহৃত লাইনের মতো), TOTAL-এর অবস্থানও সবসময় ৩০-সারির পরেই স্থির থাকে।** ৩০ জনের বেশি হলে (বিরল) দ্বিতীয় A4 পাতায় বাকিরা যায়, একই হেডারসহ।
- **TOTAL সারি** — Fees/Cash/Online তিনটাই আলাদা যোগফল, তাদের নিজ নিজ কলামের নিচে।

## ৬. Dashboard — টাইল নাম ও গ্রিড

- Chamber Date স্ক্রিনের ভেতরের হেডার আগেই "Chamber Date"-এ বদলানো হয়েছিল (আগের সেশনে), কিন্তু **Dashboard হোমপেজের টাইলে এখনো পুরনো "Chamber Attendance" লেখা ছিল** — এটা আলাদা জায়গায় সেভ করা ছিল বলে বাদ পড়ে গিয়েছিল। এখন `DashboardActivity.kt`-তে ঠিক করা হয়েছে।
- Dashboard-এর গ্রিডে ১০টা দৃশ্যমান মডিউল লুকানো মডিউলগুলোর (Reports/Backup/Password/Trash/ইত্যাদি) সাথে ছড়িয়ে ছিল বলে শেষ দুই সারিতে ফাঁকা জায়গা তৈরি হচ্ছিল (৩+৩+২+২ প্যাটার্ন)। **XML-এ ক্রম সাজিয়ে ঠিক করা হয়েছে** — এখন দৃশ্যমান ১০টা প্রথমে, লুকানোগুলো পরে, ফলে ৩+৩+৩+১ প্যাটার্নে সুন্দর দেখায়।

## ৭. স্থায়ী নিয়ম (এই সেশনে যোগ/পুনর্নিশ্চিত হয়েছে)

- **ফাইল পাঠানো শুধু TK স্পষ্ট করে "ফাইল পাঠান" বললেই** — একবারের অনুমতি বারবার প্রযোজ্য না, প্রতিবার নতুন করে জিজ্ঞেস করা লাগবে। (এই সেশনে একবার এই নিয়ম ভাঙা হয়েছিল ভুলবশত, TK ধরিয়ে দেওয়ার পর সংশোধন করা হয়)।
- **প্রতিটা এডিটের পর তিনটা চেক:** bracket-balance, এতিম-কমেন্ট স্ক্যান, XML validity।
- **ডিজাইন/লজিক পরিবর্তনের আগে ঝুঁকি জানিয়ে অনুমতি নেওয়া**, তারপর কাজ করা।
- **যা প্রুফ চাওয়া হয়, কোড লেখার আগে প্রুফ (মকআপ/ছবি) পাঠানো**, তারপর অনুমতি পেলে আসল কোডে বসানো।
- আন্দাজে কিছু না বলা/না বানানো — আসল কোড/ডিজাইন দেখে যাচাই করে তারপর বলা (এই সেশনে দুইবার এই নিয়ম ভাঙা হয়েছিল — একবার প্রেসক্রিপশন হেডার না দেখেই প্রুফ বানানো, একবার Chamber Attendance-এর "Status" মানে ভুল বোঝা (NEW/OLD-কে Arrived ভাবা) — দুটোই TK ধরিয়ে দেওয়ার পর সংশোধন করা হয়)।
- **⚠️ প্রতিটা ডেলিভারিতে `build.gradle.kts`-এর versionCode/versionName ZIP-এর নামের সাথে মিলিয়ে বাম্প করা বাধ্যতামূলক** — এই সেশনে এটা মিস হয়ে গিয়েছিল (কোড V91 হলেও versionName থেকে গিয়েছিল "V90", TK বিল্ড করে ধরেছিলেন), সেশনের শেষে ঠিক করা হয়েছে (`versionCode=91, versionName="V91"`)।

## ৮. এই সেশনে টাচ হওয়া ফাইলের সম্পূর্ণ তালিকা

- `FollowUpActivity.kt` — tab-switch silent fix, wifi triple-tap (আগের সেশনের বাকি অংশ)
- `ChamberAttendanceActivity.kt` — scroll fix, silent refresh, print-wiring, sort-by-time
- `ChamberAttendanceAdapter.kt` — সম্পূর্ণ রিরাইট (টেবিল-রো ডিজাইন, tap-to-open, tap-to-call)
- `ChamberAttendanceRepository.kt` — Enquiry-Arrived বিচ্ছিন্ন, patientId, arrivedAt ফিল্ড যোগ
- `PatientTimelineActivity.kt` — build-error ফিক্স (দুইবার orphaned comment)
- `PatientTimelineRepository.kt` — Progress=Remark, দিন-ভিত্তিক একত্রীকরণ, sortKey
- `DoctorVisitActivity.kt` — build-error ফিক্স (EditText.setText)
- `DashboardActivity.kt` — টাইল নাম ঠিক করা
- **`ChamberRegisterPdfBuilder.kt`** — সম্পূর্ণ নতুন ফাইল (A4 প্রিন্ট)
- `item_chamber_attendance.xml`, `activity_chamber_attendance.xml`, `activity_dashboard.xml` — লেআউট পরিবর্তন
- `bg_chamber_table_row.xml`, `bg_chamber_table_header.xml` — নতুন drawable

---
**এই ফাইলটা `00_PROJECT_STATE_MASTER_NOTE.md`-এর পরিপূরক, প্রতিস্থাপক না। পরের সেশনে দুটোই পড়া উচিত — এটা দ্রুত রেফারেন্সের জন্য, মাস্টার নোট সম্পূর্ণ ইতিহাসের জন্য।**

## ৯. সর্বশেষ সংযোজন — "(syncing…)" আটকে থাকা bug ফিক্স

TK-এর স্ক্রিনশট থেকে ধরা পড়েছিল Follow-up-এ একাধিক পেশেন্টের "Last Remark: Registered (syncing…)" স্থায়ীভাবে দেখাচ্ছিল। কারণ: এই fallback শুধু cloud চেক করত, ফোনের local cache চেক করত না। **ফিক্স:** এখন প্রথমে local cache চেক করে, আসল রিমার্ক পেলে সেটাই দেখায়। টাচ হওয়া ফাইল: `FollowUpRepository.kt`।
