# V455 LOCK NOTE — 20.08.2026

## এই ডেলিভারিতে যা যোগ হয়েছে

**Payment স্ক্রিনে auto-refresh** — TK-এর প্রশ্নে ধরা পড়েছিল যে Payment
স্ক্রিনে কোনো live-refresh ছিল না (স্ক্রিন খোলার সময় একবারই লোড হতো)।
এখন Follow-up/Chamber/Doctor Queue-র প্রমাণিত একই পদ্ধতিতে ৩০ সেকেন্ডে
চেক করে, বদল থাকলে আজকের Collection তালিকা নিজে থেকেই নতুন হয়।

⛔ টাকার হিসাব, Save, `renderCollectionSummary()` — কিছুই বদলায়নি, শুধু
"কখন আবার লোড হবে" তার নিয়ম যোগ হয়েছে।

## এখনো যা বাকি (TK-কে জানানো)

- `CollectionListActivity.kt` (Monthly/History) — এই দফায় ছোঁয়া হয়নি
- Web (`app.js`) — কোনো স্ক্রিনেই live-polling নেই (পুরো Web-এর জন্যই সত্য,
  শুধু Payment-এর জন্য না) — এটা আলাদা, বড় কাজ, আজ করা হয়নি

## সততার সীমাবদ্ধতা

লাইভ ডিভাইসে টেস্ট করা হয়নি — শুধু কোড-স্তরের syntax/guard যাচাই
(`tk_guard.py` PASS, brace/paren balance মিলেছে)। TK-কে একটা ফোনে
টেস্ট করে ফলাফল জানাতে অনুরোধ করা হচ্ছে — ঠিক আগের Doctor Queue
পাইলটের মতোই (নতুন payment ঢুকিয়ে ৩০-৬০ সেকেন্ড অপেক্ষা করে দেখা)।
