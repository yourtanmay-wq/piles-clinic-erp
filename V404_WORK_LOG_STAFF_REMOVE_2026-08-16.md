# V404 — SWAPNA ADHIKARI সম্পূর্ণ বাদ + "কর্মী বাদ দিন" বোতাম
**তারিখ:** 16.08.2026 · **ভার্সন:** V404 / 4.04 · **ওয়েব cache:** `?v=v426`

## TK-এর নির্দেশ (হুবহু)
> "SWAPNA ADHIKARI / কাজ ছেড়ে দিয়েছে"
> "ওই নাম্বারের কোনো অংশ যেন না থাকে"

**TK-এর ৪টি সিদ্ধান্ত (জিজ্ঞেস করে নেওয়া):**

| প্রশ্ন | TK-এর উত্তর |
|---|---|
| কীভাবে বন্ধ করব | ওই নম্বরের কোনো অংশ যেন না থাকে |
| মাইনে বাকি আছে? | সব ক্লিয়ার |
| পুরনো কাজের রেকর্ড | **রেকর্ড অটুট থাক** |
| ভবিষ্যতের বোতাম | হ্যাঁ, বসান |

---

## 🔴 যাচাই করে যে দুটো আসল ফাঁক ধরা পড়েছিল

**১. অ্যাপে "কর্মী বাদ দিন" বোতামই ছিল না।** ওয়েবে বা ফোনে, কোথাও নেই — শুধু
Suspend ছিল। ডেটাবেসে `hr.staff_profiles.active` ঘরটা V246 থেকেই আছে, কিন্তু
সেটাকে `false` করার কোনো পথ অ্যাপে বসানো হয়নি।
⇒ TK "Swapna-কে বাদ দিয়েছি" বললেও ডেটাবেসে তিনি **সচলই** ছিলেন
⇒ **V403-এর সুরক্ষা তাঁর উপরে খাটছিল না।**

**২. `active = false` করলেও লগইন বন্ধ হত না** — লগইনের গেট শুধু
`suspended_until` দেখত (`app.js` ও `CloudPasswordCheck.kt`, দুটোই
`suspended_until_for` ডাকে)।

**৩. তাঁর ব্যক্তিগত নম্বর ওয়েবসাইটে Birpara-র প্রকাশ্য ফোন হিসেবে দেখানো হত**
(`8538002200 / ...`)। ফোনের অ্যাপে আগেই একটাই নম্বর ছিল — শুধু ওয়েবটাই বাকি ছিল।

---

## যা যা বদলানো হলো (৬টি ফাইল + ১টি নতুন SQL)

| ফাইল | কী |
|---|---|
| `03_NETLIFY_READY/config.js` | লগইন তালিকা থেকে FALA-15 বাদ (স্টাফ ১৪ → ১৩) |
| `02_.../native/StaffDirectory.kt` | একই — ফোনের লগইন তালিকা |
| `03_NETLIFY_READY/app.js` | `publicBranchPhones` — Birpara-য় শুধু ব্রাঞ্চের নম্বর · লগইন-গেটে "removed" বার্তা |
| `02_.../native/LoginActivity.kt` | লগইন-গেটে "This account has been removed." বার্তা |
| `03_NETLIFY_READY/profile.js` | বাদ-দেওয়া কর্মী তালিকা ও Salary Due থেকে বাদ · **Remove Staff** ও **Restore** বোতাম |
| `02_.../modules/StaffProfileActivity.kt` | একই — **Remove** ও **Restore** বোতাম + "Removed Staff" ভাগ |
| `02_.../modules/SalaryReminder.kt` | বাদ-দেওয়া কর্মীর নাম আর "বেতন বাকি"-তে ওঠে না |
| 🆕 `04_.../V404_STAFF_REMOVE_FALA15_2026-08-16.sql` | নিচে দেখুন |

### V404 SQL কী করে
১) `hr.app_identity` থেকে তাঁর পরিচয় মোছে · ২) Supabase লগইন অ্যাকাউন্ট
(`auth.users` + `auth.identities`) মোছে · ৩) আয়-খরচের চাবি মোছে ·
৪) মাইনে বন্ধ · ৫) প্রোফাইল `active=false` + ব্রাঞ্চ Birpara → **Falakata** ঠিক ·
৬) 🔑 `suspended_until_for()` নতুন করে লেখা — **বাদ-দেওয়া হলে `2999-12-31`
ফেরায় ⇒ লগইন বন্ধ**। ওয়েব ও ফোন দুটোর গেটই এই একটাই ফাংশন ডাকে, তাই
**লগইনের কোডে হাত দিতে হয়নি — লগইন ভাঙার ঝুঁকি শূন্য।**

⛔ **যা ছোঁয়া হয়নি:** রোগী · ফলোআপ · এনকোয়ারি · চেম্বার · কল-হিস্ট্রি ·
`hr.salary_payments` · `wn.leave_requests` — একটাও সারি নয়। অন্য কোনো
কর্মী/ডাক্তার/ব্রাঞ্চ — একজনও নয়।

---

## যাচাই

### আসল PostgreSQL 16-এ (নকল স্কিমা বানিয়ে, দুবার চালিয়ে)
| পরীক্ষা | ফল |
|---|---|
| পরিচয় · লগইন অ্যাকাউন্ট · identity · চাবি — সব ০ | ✅ |
| প্রোফাইল `active=false`, ব্রাঞ্চ `Falakata` | ✅ |
| মাইনে `salary_enabled=false` | ✅ |
| **তাঁর ২টি মাইনের রসিদ + ১টি ছুটি অটুট** | ✅ |
| **অন্য কর্মী · অন্য লগইন অ্যাকাউন্ট — কিছুই বদলায়নি** | ✅ |
| দ্বিতীয়বার চালালেও ঠিক (idempotent) | ✅ |

### লগইন-গেট (`suspended_until_for`)
| কে | ফেরত | ফল |
|---|---|---|
| FALA-15 (বাদ) | `2999-12-31` | 🔒 আটকাবে |
| FLK-1 (সচল) | `null` | ঢুকবে |
| COB-4 (সাসপেন্ডেড) | `2026-08-20` | আগের মতোই |
| BIR-BRANCH (প্রোফাইল নেই) | `null` | ঢুকবে — অটুট |
| `+91` সহ নম্বর | `2999-12-31` | 🔒 আটকাবে |

### ওয়েব (Chromium-এ আসল কোড চালিয়ে)
| পরীক্ষা | ফল |
|---|---|
| Birpara-র প্রকাশ্য ফোন = `8538002200` (একটাই) | ✅ |
| বাকি ৪ ব্রাঞ্চের ফোন অপরিবর্তিত | ✅ |
| লগইন তালিকায় FALA-15 / নম্বরটা নেই · স্টাফ ১৩ | ✅ |
| Staff Profiles-এ SWAPNA নেই | ✅ |
| **Salary Due-তে তাঁর নাম নেই** (ইচ্ছে করে `salary_enabled=true` রেখেও) | ✅ |
| "Removed Staff (1)" বাক্স + Restore বোতাম | ✅ |
| Remove চাপলে ঠিক ৩টি জায়গায় ঠিক তথ্য যায় | ✅ |
| পেজ-এরর | কিছু নেই ✅ |

### কোড
- ৮টি `.js` ফাইলেই `node --check` পাশ
- ৪টি বদলানো `.kt`-এ ব্রেস/বন্ধনী/বর্গবন্ধনী ভারসাম্য **০**
- ডাকা প্রতিটি ফাংশন সত্যিই আছে ও স্বাক্ষর মেলে
  (`ModuleAuth.update` · `ModuleUi.toast` · `PremiumAlert.header` · `renderList` · `editProfile` · `salary`)
- RLS মিলিয়ে দেখা: `staff_profiles` · `salary_config` · `entry_permits` — তিনটেতেই master-এর `for all` অধিকার আছে
- 🔴 **একটা বাগ ধরা পড়ে ঠিক করা হয়েছে:** প্রথমে `MOD.toast(...)` লিখেছিলাম,
  কিন্তু `module_core.js`-এ **`MOD.toast` বলে কিছু নেই** — ওটা থাকলে Remove
  বোতাম চাপলেই পর্দা ভেঙে যেত। এখন `pfToast()` মোড়ক, ভিতরে try/catch।

---

## ⚠️ TK-কে যা করতে হবে (ক্রম মেনে)
1. Supabase → SQL Editor → `V404_STAFF_REMOVE_FALA15_2026-08-16.sql` চালান
2. Netlify-তে `03_NETLIFY_READY` আপলোড (cache `?v=v426`)
3. নতুন APK বিল্ড (V404 / 4.04)

⛔ শুধু SQL চালালে হবে না — নম্বরটা অ্যাপের **ভিতরের তালিকাতেও** ছিল,
তাই নতুন ওয়েব ফাইল ও নতুন APK দুটোই লাগবে।

## ⛔ জেনে রাখার কথা
পুরনো রেকর্ডে (কোন রোগী তিনি তুলেছিলেন) এখন `FALA-15`-এর বদলে কাঁচা নম্বরটা
দেখাবে — TK জেনেশুনে **"রেকর্ড অটুট থাক"** বেছেছেন। BIR-BRANCH-এর পুরনো
নম্বরের বেলাতেও (29.07.2026) হুবহু এই একই নিয়মই মানা হয়েছিল।
