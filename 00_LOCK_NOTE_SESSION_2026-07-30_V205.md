# 🔒 LOCK NOTE — V205 (৩০.০৭.২০২৬ রাত)

**ভার্সন:** `versionCode 205` · `versionName 2.05` · পর্দায় **V205** · খাতার সারি **B197**
⛔ **কোনো SQL লাগবে না।**

---

## ⛔ সবার আগে — স্থায়ী নিয়ম
> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

---

## TK-এর নির্দেশ
*"তাড়াতাড়ি ও হবে এবং সঠিক কাজ ও হবে, তার ব্যবস্থা করুন।"*

## ✅ সমস্যা ও কেন সহজ ছিল না
Remark সেভ করতে `FollowUpRepository.updateRemark()`-এর ভিতরে **তিনটে ধারাবাহিক ক্লাউড-কল**:
1. আগের ইতিহাস (history) পড়া
2. নতুন লেখা বসানো (PATCH)
3. **আবার পড়ে যাচাই করা যে সত্যিই বসেছে**

ধাপ ৩ — এই "আবার পড়ে যাচাই" — **২৮.০৭.২০২৬-এ TK-এর রিপোর্ট করা একটা আসল বাগ ঠিক করতে বসানো হয়েছিল**: *"রিমার্ক লিখলাম, সেভ হয়েছে দেখাল, কিন্তু পুরনো লেখাটাই থেকে গেল।"* তাই এই ধাপ সরানো বা দ্রুত করা মানে ঠিক সেই পুরনো বাগটাই ফিরিয়ে আনার ঝুঁকি — TK নিজেই বলেছিলেন এমন ঝুঁকি নেওয়া যাবে না।

## ✅ সমাধান — দুটো লক্ষ্যই মিলল
`FollowUpRepository.updateRemark()`-এর **ভিতরের একটা অক্ষরও ছোঁয়া হয়নি** — ইতিহাস-মেলানো, যাচাই, retry-queue সব অক্ষত। বদলটা শুধু **কলার-পর্যায়ে (`ChamberAttendanceActivity.showRemarkDialog()`)**:

1. স্টাফ "Save" চাপার সঙ্গে সঙ্গেই — স্ক্রিনে থাকা বোর্ডের (`lastBoard`, মেমোরিতে) সেই রোগীর সারির `remark`-এ স্টাফের টাইপ করা লেখাটা **সরাসরি বসিয়ে** পর্দা রিফ্রেশ করা হয় — **কোনো নেটওয়ার্ক-কল ছাড়াই**।
   - ⛔ এটা আন্দাজ নয় — স্টাফ নিজে যা টাইপ করেছেন **ঠিক সেই লেখাটাই** দেখানো হয়, তাই ভুল দেখানোর কোনো সুযোগ নেই।
2. Toast দেখানো হয় সঙ্গে সঙ্গেই।
3. আসল সেভ (`FollowUpRepository(appCtx).updateRemark(...)` — পুরো ইতিহাস-মেলানো ও যাচাই সহ, অপরিবর্তিত) এখন `BackgroundWork.run{}`-এ **পিছনে** চলে।
4. ব্যর্থ হলে — আগের মতোই `updateRemark()`-এর নিজের `queueFieldUpdate()` মেকানিজম রেকর্ডটা retry-তালিকায় তোলে, পরের বার নিজে থেকেই ঠিক হয়ে যায়। **কোনো correctness guarantee কমেনি।**

## ✅ নিরাপত্তা যাচাই

| প্রশ্ন | উত্তর |
|---|---|
| `updateRemark()`-এর ভিতরের যাচাই-লজিক বদলেছে? | না — এক অক্ষরও না |
| এই ফাংশনের অন্য ১১টা কল-সাইট প্রভাবিত? | না — শুধু এই একটা কল-সাইট (`showRemarkDialog`) বদলেছে |
| স্ক্রিনে ভুল রিমার্ক দেখানোর ঝুঁকি? | না — যা টাইপ হয়েছে ঠিক তাই দেখানো হয় |
| ব্যর্থ হলে ডেটা হারানোর ঝুঁকি? | না — background কলে সেই একই retry-queue ব্যবহার হয় |
| context-leak ঝুঁকি? | না — `applicationContext` ব্যবহার হয়েছে |

## ⛔ যা ছোঁয়া হয়নি
- `FollowUpRepository.updateRemark()`-এর ভিতরের কোনো লজিক
- এই ফাংশনের অন্য ১১টা কল-সাইট
- `editRemarkInReview()` (Chamber Attendance-এর Review-পপ-আপের ভিতরের Remark এডিট) — **এখনো বাকি**, একই প্যাটার্নে পরে করা যাবে

## 🔍 যাচাই
- পাহারাদার (`tk_guard.py`) **১৭/১৭ পাশ**
- `ChamberAttendanceActivity.kt`-এ ব্র্যাকেট-গণনা আলাদা করে মিলিয়ে দেখা হয়েছে
- `lastBoard`/`closeTapCount` mutable ভ্যারিয়েবল ও স্কোপ নিশ্চিত করা হয়েছে

## 🔴 এখনো বাকি
- **TK-এর লাইভ টেস্ট**
- `editRemarkInReview()` — একই প্যাটার্নে (TK চাইলে)
- **ধাপ ৩: Payment** — টাকা জড়িত বলে সবচেয়ে সাবধানে, এখনো শুরু হয়নি
- `markExpected()` — একই পুরনো markArrived-জাতীয় সমস্যা, ছোঁয়া হয়নি
- **B148 — RLS** ⛔ TK-এর অনুমতি ছাড়া নিষেধ
- `03_NETLIFY_READY` Netlify-তে আপলোড (TK-এর কাজ)
