# V440 — ফ্রি প্ল্যানে থাকার কাজ (Egress + Database)

**তারিখ:** ১৯.০৮.২০২৬ · **ভার্সন:** V440 / 4.40 · **ওয়েব cache:** `app.js?v=v472`

## TK-এর নির্দেশ
> *"সম্পূর্ণ প্রজেক্ট আরো ভালো করে যাচাই করে দেখুন আরো কোথায় কোথায় আছে এমন।
> আমি ফ্রি প্লানে চালাতে চাই তার সম্পূর্ণ ব্যবস্থা প্ল্যান করে বলতে হবে।"*
> এবং — *"সততার সাথে সঠিকভাবে... আন্দাজে কিছু বলবেন না সবকিছু যাচাই করে বলবেন।"*

---

## ০. এই সেশনে যা সংখ্যায় প্রমাণ হয়েছে (আন্দাজ নয়)

TK-এর Supabase ড্যাশবোর্ড ও SQL Editor-এর ফল থেকে:

| মাপ | সকালে | কাজের পরে |
|---|---|---|
| Database Size | **১৩৬.৮৮ MB** | **~৬০ MB** |
| `backuprecords` | ৬৯ MB (আসল ছিল ১০ MB) | **৭.৮ MB** |
| `patients` | ২২ MB (আসল ৬.৬ MB) | **৬.৬ MB** |
| Egress | ৩.৪৮ / ৫ GB (৭০%), ৬ দিনে | — (অপরিবর্তিত) |
| Realtime messages · peak conn. | **০** · **১** | — |
| MAU | ১৫ | — |

**দুটো বড় শিক্ষা:**
1. **ওয়েব অ্যাপ প্রায় ব্যবহারই হয় না** (realtime peak connection = ১, message = ০)।
   ⇒ egress-এর প্রায় পুরোটাই **ফোনের অ্যাপ** থেকে আসছে।
2. আসল ডেটা সব মিলে **~৩১ MB**, অথচ ৬ দিনে **৩,৪৮৮ MB** নেমেছে।
   ⇒ একই ডেটা **~৭০ বার** নেমেছে। সমস্যা ডেটার আকার নয়, **বারবার নামা**।

---

## ১. 🔴 এই বিল্ডে সারানো হলো — সবচেয়ে বড় ফুটো

**ফাইল:** `native/BackgroundRefreshWorker.kt` (~লাইন ১০৬-১১৬)

**যা ছিল:** প্রতিটা ফোনে ঘণ্টায় একবার (`MIN_GAP_MS = 60 মিনিট`) "নতুন কিছু হয়েছে?"
— এই সস্তা HEAD-প্রশ্নটা করা হত। প্রশ্নে **ব্রাঞ্চের নাম ছিল না**:

```kotlin
fetchCount("followups", "updatedAt=gt.$since")   // ⛔ সব ব্রাঞ্চ
fetchCount("payments",  "updatedAt=gt.$since")   // ⛔ সব ব্রাঞ্চ
```

**ফল:** যেকোনো এক ব্রাঞ্চে একটা এন্ট্রি হলেই **পাঁচ ব্রাঞ্চের ১৩টা ফোনেই** দরজা
খুলে যেত, আর প্রত্যেকে নিজের ব্রাঞ্চের পুরো `patients` + `payments` + `followups`
আবার নামাত। পাঁচ ব্রাঞ্চে সারাদিন কাজ চলে বলে দরজাটা প্রায় প্রতিবারই খুলত।

📐 হিসাব: ১৬ ঘণ্টা × ১৩ ফোন × ~২ MB ≈ **~৪১৬ MB/দিন** — ৫৫০ MB-র সিংহভাগ।

**যা করা হলো:** স্টাফ/ডাক্তারের প্রশ্নে শুধু **নিজের ব্রাঞ্চ**:

```kotlin
val bScope = if (branch=="All"||branch.isEmpty()) ""
             else "&or=(branch.eq.$enc,branch.is.null)"
```

**⛔ ছয়টা সুরক্ষা অটুট:**

| অবস্থা | কী হয় |
|---|---|
| master / "All" | আগের মতোই **সব ব্রাঞ্চ**, কোনো ছাঁকনি নেই |
| সদ্য তৈরি সারিতে branch এখনো বসেনি | `branch.is.null` ধরা আছে ⇒ গোনায় আসে |
| প্রশ্ন ব্যর্থ হলে | আগের মতোই `-1` ⇒ সন্দেহ হলে **পুরো তালিকা** নামে (B145 অটুট) |
| `briefings` | **ইচ্ছে করে ছোঁয়া হয়নি** — নোটিশ/Approval যেন কোনো অবস্থাতেই মিস না হয় |
| প্যাটার্ন | প্রজেক্টের **প্রমাণিত** `branchScopeFilter()`-এর হুবহু একই (`FollowUpRepository.kt:115-120`, B531-এ পরীক্ষিত) |
| নামানোর কোড | এক অক্ষরও বদলায়নি — শুধু **কখন** নামবে সেটা বদলেছে |

**যাচাই:** `tk_guard.py` — **সব ২১টা পরীক্ষা পাশ** ✅ (ব্র্যাকেট ২৩৫ ফাইল · Supabase
কলাম মেলে · লক করা ২৪টি নিয়ম অক্ষত · ভার্সন এক V440)। ব্রেস ৬০/৬০ · বন্ধনী ১৫০/১৫০।

📐 আশা করা লাভ: **~৩০০ MB/দিন**।

---

## ২. 🔵 ওয়েবে সারানো হলো — ব্যাকআপ আর ডেটাবেসে জমবে না

**ফাইল:** `03_NETLIFY_READY/app.js` — `CLOUD_SAFE_COLS.backuprecords` থেকে
`payload` বাদ (~লাইন ৬১৩), ও Backup Center-এর লেখায় "Download Full Backup
চেপে ফাইলটা নিজের কাছে রাখুন" যোগ (~লাইন ৭৪২৪)।

**কারণ (SQL-এ প্রমাণিত):** ক্লাউডে মাত্র **৩টে** ব্যাকআপ-কপি (মোট ১০ MB) থাকা
সত্ত্বেও টেবিলটা **৬৯ MB** দখল করে ছিল — বাকিটা পুরনো সংস্করণের জঞ্জাল, কারণ
~৫ MB-র `payload` বারবার নতুন করে লেখা হত।

**⛔ যা বদলায়নি:** ব্যাকআপ নেওয়া · তালিকা দেখা · Download · Restore — সব আগের মতো
(payload এই যন্ত্রেই localStorage-এ ৩০টা থাকে, `wlv1BackupPayload()` আগে সেখানেই খোঁজে)।
**একমাত্র পার্থক্য:** অন্য যন্ত্র থেকে পুরনো ব্যাকআপের ভিতরের তথ্য আর টানা যাবে না।

**যাচাই:** `node --check app.js` পাশ ✅ · আসল কোড আলাদা করে চালিয়ে পরীক্ষা —
ক্লাউডে যাওয়া ঘর: `id,date,reason,status,size,by,createdAt,updatedAt` ·
payload নেই ✅ · size/status/date অটুট ✅ · অন্য টেবিল অক্ষত ✅ · পাঠানো ৫২০৩ → ১৭৫ bytes (**৯৭% কম**)।

---

## ৩. ⛔ যা ইচ্ছে করে **করিনি** (সৎভাবে জানাচ্ছি)

| কী | কেন করিনি |
|---|---|
| **`ModuleAuth` টোকেন সেভ + `refresh_token` + সার্ভারে logout** (`modules/ModuleAuth.kt:36,66,155`) — ১৫ জনের জন্য ১,৮২৬টা সেশনের কারণ | লগইনের মূল পথ। ভুল হলে **সব স্টাফ মডিউলে ঢুকতে পারবে না**। বিল্ড করে আসল ফোনে পরীক্ষা না করে এটা ছোঁয়া বিপজ্জনক |
| **ওয়েবের `upd()`-এ ছবি বাদ** (`app.js:572`) | ভবিষ্যতের ফাঁদ, কিন্তু ওয়েব ব্যবহারই হয় না — এখন তাড়া নেই, আর এটা মূল সেভ-পথ ছোঁয় |
| **`DraftRepository` `enquiries` সরু করা** (`native/DraftRepository.kt:364`) | কোন কোন ঘর পর্দায় লাগে তা নিশ্চিত হতে পারিনি — অনিশ্চিত অবস্থায় Draft তালিকা ভাঙার ঝুঁকি নিইনি |
| **DoctorQueue হাতে-রিফ্রেশে `includePhoto=false`** (`DoctorQueueRepository.kt:122`) | ওটাই ওই পর্দায় তাজা ছবি পাওয়ার পথ। বন্ধ করলে ছবি আটকে যেতে পারে |
| **GlobalSearch ব্রাঞ্চে সীমিত করা** (`GlobalSearchActivity.kt:122`) | master-কে সব ব্রাঞ্চে খুঁজতে হয়; ভুল করলে রোগী "খুঁজে পাওয়া যাচ্ছে না" হবে |
| **রোগীর ছবি ছোট করা** | TK-এর ফোনে আসল যন্ত্র চালিয়ে **মেপে দেখা গেছে** — ৯৪টা ছবি মোট ৫.৯ MB, ছোট করলে বাঁচত মাত্র **১.৩৬ MB (২৩%)**। ছবির মান নষ্ট করার মতো লাভ নয় ⇒ **বাতিল** |

---

## ৪. 🔴 এখনো বাকি — TK-এর সিদ্ধান্ত লাগবে

1. **`ModuleAuth`-এর সেশন-ফুটো** (উপরে §৩)। বিল্ড ও লাইভ টেস্টের সুযোগ পেলে করা যাবে।
2. **SQL পরিষ্কার** — পুরনো সেশন · `deleted_records` · `trash`।
   পুরো SQL আছে: `FREE_PLAN_E_THAKAR_SAMPURNA_PLAN.md` §৬।
   🔴 **আলাদা করে জরুরি:** `app.js:671` — `deleted_records` পড়া হয় `limit(5000)` দিয়ে।
   টেবিল ৫০০০ ছাড়ালে **এক ফোনে মোছা রেকর্ড অন্য ফোনে ফিরে আসতে পারে**।
   এটা জায়গার নয়, **তথ্যের ভুলের** সমস্যা।
3. **সব ১৩ ফোনে V440 ইনস্টল** — এটা না হলে §১-এর সাশ্রয় শূন্য।

---

## ৫. 📐 হিসাব — ফ্রি প্ল্যানে চলবে কি না

সীমা: ৫ GB ÷ ৩১ দিন = **১৬৪ MB/দিন** (১৩ ফোনে ~১২.৬ MB/ফোন)।

| অবস্থা | রোজ | চলবে? |
|---|---|---|
| এখন (V439) | ~৫৫০ MB | ❌ |
| **V440 (এই বিল্ড)** | 📐 ~২৫০ MB | ❌ কাছাকাছি |
| V440 + §৩-এর বাকি সরু-করা | 📐 ~১২০-১৫০ MB | ✅ |

📌 **এই মাস (১৩ সেপ্টেম্বর পর্যন্ত) বাঁচানো যাবে না** — ৩.৪৮/৫ GB, ২১-২২ আগস্টেই শেষ।
লক্ষ্য: **১৩ সেপ্টেম্বরের নতুন সাইকেল যেন ঠিক হারে শুরু হয়।**

---

## ৬. ফাইল-তালিকা (এই বিল্ডে যা বদলেছে)

| ফাইল | কী |
|---|---|
| `native/BackgroundRefreshWorker.kt` | সস্তা প্রশ্নে ব্রাঞ্চ যোগ (§১) |
| `app/build.gradle.kts` | versionCode 439→**440**, versionName 4.39→**4.40** |
| `03_NETLIFY_READY/app.js` | backuprecords payload বাদ + Backup Center-এর লেখা (§২) |
| `03_NETLIFY_READY/index.html` | cache `app.js?v=v471` → **v472** |
| `03_NETLIFY_READY/version.json` | 439/4.39 → **440/4.40** |
| `V440_WORK_LOG_FREE_PLAN_EGRESS_2026-08-19.md` | এই নোট |
| `FREE_PLAN_E_THAKAR_SAMPURNA_PLAN.md` | সম্পূর্ণ প্ল্যান |
| `CHOBI_CHOTO_KORUN.html` | ছবি মাপার যন্ত্র (মেপে দেখা হয়েছে — ব্যবহারের দরকার নেই) |

⛔ **ডিজাইন · টাকার হিসাব · রোগীর তথ্য · SQL টেবিল — কিচ্ছু ছোঁয়া হয়নি।**
