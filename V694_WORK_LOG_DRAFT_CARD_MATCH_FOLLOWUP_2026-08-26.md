# V694 — Draft-এর কার্ড হুবহু Follow-up কার্ডের মতো

**তারিখ:** ২৬.০৮.২০২৬ (IST)
**নির্দেশ:** TK (দুটো ছবিসহ) — *"এখানের চেহারা এরকম কেন?"* →
*"followup Card এ যেমন চেহারা ঠিক তেমন করে দিন, নিরাপদে"*

---

## ১. আসল কারণ (আন্দাজ নয় — কোড ধরে বার করা)

অ্যাপে **দুটো আলাদা কার্ড-টেমপ্লেট** ছিল:

| পর্দা | কার্ড আসে কোথা থেকে |
|---|---|
| Follow-up (আসল) | `FollowUpActivity.buildFollowCard()` — কোড দিয়ে আঁকা |
| Draft → Complete Patient ইত্যাদি | `item_followup_card.xml` + `FollowUpAdapter` |

দুটো আলাদা বলেই চেহারা মিলত না। V648/V649/V683-এ কিছুটা মেলানো হয়েছিল,
কিন্তু **সবচেয়ে চোখে-পড়া দুটো জিনিস বাকি ছিল**।

---

## ২. যা যা ঠিক হলো

### 🔴 (ক) লাল সিরিয়াল ব্যাজটা কোনোদিন দেখাই যায়নি

V648-এ ব্যাজটা যোগ করা হয়েছিল, কিন্তু বসানো হয়েছিল `FrameLayout`-এর
**প্রথম** সন্তান হিসেবে — আর তার পরেই আসত কার্ডের **নিরেট সাদা
ব্যাকগ্রাউন্ড**। ফলে ব্যাজটা প্রতিবার **কার্ডের নিচে চাপা পড়ে যেত**।
কোডে ছিল, পর্দায় ছিল না।

⇒ এখন ব্যাজটা নামের ঠিক বাঁ পাশে, নিজের সারিতে — হুবহু আসল কার্ডের মতো
(লাল বর্গ · ৯dp ফাঁক · নামের কলাম আলাদা)।

### 🔴 (খ) বাঁদিকের ৬২dp কলামটা রয়ে গিয়েছিল

আসল কার্ডে **২৭.০৭.২০২৬-এ TK-এর অনুমোদনে** Visit ও Patient কার্ড থেকে
বাঁদিকের কলামটা (ছবি + VISITED/PATIENT পিল + আইডি) **পুরোপুরি তুলে দেওয়া
হয়েছিল**, যাতে নাম ও মোবাইল একদম বাঁ প্রান্ত থেকে শুরু হয়। পিল ও আইডি
হারায়নি — ট্যাগের নিচে নিজের সারিতে চলে গিয়েছিল।

XML টেমপ্লেটটায় ওই বদলটা কখনো করা হয়নি। তাই Draft-এর কার্ডে নাম মাঝখানে
সরে যেত আর পিল/আইডি বাঁদিকে বসত — TK-এর ছবিতে ঠিক এটাই ধরা পড়েছে।

⇒ এখন Visit/Patient কার্ডে বাঁদিকের কলাম বাদ, আর **PATIENT/VISITED পিল ও
রোগীর আইডি ট্যাগের নিচে নিজের সারিতে** — হুবহু আসল কার্ডের `idRow`।

### (গ) ছোট মিল

- নামের আগে আর `👤` নেই (ব্যাজ আছে বলে — আসল কার্ডের নিয়ম)
- নাম **16sp**, মোবাইল **12.5sp** (আগে 15sp / 12sp)

---

## ৩. Enquiry কার্ড অক্ষত

Enquiry-তে বাঁদিকের কলাম **থাকে** (কল-সিগন্যালের দাগ ওখানেই), আর পিল/আইডির
সারি নেই — আসল কার্ডেও ঠিক তাই। এক অক্ষরও বদলায়নি।

⚠️ **নিজে ধরা একটা ফাঁদ:** তালিকার সারি পুরনো সারি আবার ব্যবহার করে
(RecyclerView)। Enquiry-র শাখায় স্পষ্ট করে না লিখলে আগের Patient কার্ডের
লুকানো/দেখানো অবস্থাটা Enquiry কার্ডে থেকে যেত — স্ক্রল করলে কার্ড
এলোমেলো দেখাত। তাই দুটোই স্পষ্ট করে বসানো হলো।

---

## ৪. কম্পিউটারে কিছু বদলায়নি — কারণ ওখানে দোষটা নেই

যাচাই করে দেখা গেছে ওয়েবে Draft-এর সব তালিকা (Complete Patient সহ)
**একই `fuCard()`** দিয়েই আঁকা হয় — অর্থাৎ Follow-up আর Draft-এ ওখানে
চিরকালই এক চেহারা। দুটো আলাদা টেমপ্লেটের সমস্যাটা **শুধু ফোনেই** ছিল।
🟢 তাই নিয়ম ২০ মানা হয়েছে — দুই জায়গা মিলিয়ে দেখেই বলা হচ্ছে।

---

## ৫. ⛔ যা ছোঁয়া হয়নি

- `buildFollowCard()` (আসল Follow-up কার্ড) — **এক অক্ষরও নয়**। এটা লক করা
  ডিজাইন; XML-টাকে ওর মতো করা হয়েছে, উল্টোটা নয়।
- টাকা/বিল/হিসাব/ডেটা — কিছুই নয়। শুধু দেখার সাজ।
- `llPhoto`-র ভিতরের পুরনো ঘরগুলো (`tvAvatar`/`tvVisitedPill`/`tvRegDate`)
  **মোছা হয়নি**, শুধু লুকানো — TK চাইলে এক মিনিটে ফেরানো যাবে।

---

## ৬. যাচাই

| যাচাই | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ (V694) — §৯.৫ binding ও drawable সব আছে · §৯.৬ XML ঠিক |
| `verify_kotlin_compile.py` | ✅ PASS — নতুন ভুল ০ |

---

## 🔴 এখনো TK-এর লাইভ টেস্ট বাকি

- [ ] Draft → **Complete Patient** — কার্ড এখন Follow-up-এর মতো
- [ ] বাঁ পাশে **লাল সিরিয়াল নম্বর** দেখা যাচ্ছে
- [ ] নাম ও মোবাইল একদম **বাঁ প্রান্ত** থেকে
- [ ] **PATIENT + আইডি** ট্যাগের নিচে এক সারিতে
- [ ] Incomplete Patient · Return Visit · Trash — সবেতেই একই
- [ ] **Enquiry-র কার্ড আগের মতোই** (কল-সিগন্যাল বাঁদিকে)
- [ ] অনেকদূর স্ক্রল করে ফিরে এলেও কার্ড এলোমেলো হয় না
