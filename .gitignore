# ---- Android / Gradle ----
build/
.gradle/
local.properties
*.apk
*.aab
captures/
.cxx/

# ---- IDE ----
.idea/
*.iml
.vscode/

# ---- Node / web tooling ----
node_modules/

# ---- Signing secrets (NEVER commit) ----
release-keystore.jks
keystore.properties
*.keystore

# ---- OS junk ----
.DS_Store
Thumbs.db

# ---- পাঠানোর জন্য বানানো জিপ ----
# এগুলো সোর্স থেকে যখন খুশি আবার বানানো যায়, আর একেকটা ২৩-৫২ MB।
# রেপোতে রাখলে চিরকাল ভারী হয়ে থাকত, তাই রাখা হয় না।
PILES_CLINIC_APP_V*.zip
