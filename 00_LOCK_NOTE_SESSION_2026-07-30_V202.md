# 🔒 LOCK NOTE — V202 (৩০.০৭.২০২৬ রাত)

**ভার্সন:** `versionCode 202` · `versionName 2.02` · পর্দায় **V202** · খাতার সারি **B194**
⛔ **কোনো SQL লাগবে না।**

---

## ⛔ সবার আগে — স্থায়ী নিয়ম
> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

---

## TK-এর প্রশ্ন
Dashboard-এ "1টি তথ্য এখনো ক্লাউডে যায়নি" — "পাঠান" চাপার পরেও "1 still waiting — try again when the network is back"। TK ফাস্ট WiFi-তে থাকা সত্ত্বেও এই বার্তা কেন, জানতে চান। নির্দেশ: **"ঝুঁকিহীনভাবে কাজটা করতে হবে।"**

## ✅ আসল কারণ (কোড ধরে)
`SupabaseClient.upsert()` / `updateById()` / `deleteById()` — এই তিনটে ফাংশনই অ্যাপের **সব** ক্লাউড-লেখার একমাত্র পথ। ব্যর্থ হলে তারা শুধু `CloudWriteQueue.remember(...)` ডেকে "এই কাজটা ব্যর্থ হয়েছে" মনে রাখত — **কেন ব্যর্থ হয়েছে তা কোথাও লেখা থাকত না** (HTTP স্ট্যাটাস কোড ফেলে দেওয়া হত, এক্সেপশনের বার্তাও ফেলে দেওয়া হত)। তাই Dashboard-এর বার্তা সবসময় ধরে নিত "নেটওয়ার্ক খারাপ" — প্রকৃত কারণ (যেমন সার্ভারের একটা নির্দিষ্ট প্রত্যাখ্যান) সম্পূর্ণ ভিন্ন হলেও।

## ✅ সমাধান — সম্পূর্ণ ঝুঁকিহীন

**⛔ যা এক অক্ষরও বদলায়নি:** কোনো কিছু পাঠানোর নিয়ম, retry-এর ক্রম, তিনটে ফাংশনের return type (এখনও শুধু `Boolean`), dedup/tries-গোনার জটিল পুরনো লজিক (খাতার সারি B164/B166) — সবকিছু অক্ষত।

**⛔ যা যোগ হলো (শুধু তথ্য টুকে রাখা, নতুন কোনো সিদ্ধান্ত নেয় না):**

1. **`SupabaseClient.kt`** — তিনটে ফাংশনেই ব্যর্থতার আসল কারণ ধরে রাখা হয়:
   - HTTP উত্তর এলেও সফল না হলে → `"HTTP <code>"` (যেমন "HTTP 401")
   - প্রকৃত এক্সেপশন হলে (টাইমআউট, DNS ব্যর্থতা ইত্যাদি) → `"<Exception নাম>: <বার্তা>"`
2. **`CloudWriteQueue.remember()`** — নতুন **ঐচ্ছিক** `reason: String = ""` প্যারামিটার (৫টা কল-সাইটই একই ফাইলের ভিতরে, সবগুলো হাতে মিলিয়ে দেখা হয়েছে)। এন্ট্রির নিজের JSON-এ `lastError` হিসেবে বসে — ফাঁকা হলে ঘরটাই বসে না, পুরনো এন্ট্রির আকার অক্ষত।
3. **`CloudWriteQueue.peekLastError()`** — নতুন **শুধু-পড়ার** ফাংশন, অপেক্ষমাণ ও "যায়নি" দুই তালিকা মিলিয়ে সবচেয়ে সাম্প্রতিক কারণ খুঁজে বার করে। কিছুই লেখে না বা বদলায় না।
4. **`PendingSyncStatus.summary()`** — এখন `detail` স্ট্রিং-এ কারণটা যোগ করে (থাকলে): `"1 অন্য কাজ — কারণ: HTTP 401"`। কারণ না পাওয়া গেলে আগের মতোই শুধু সংখ্যা।

## 🔍 একই ধরনের সমস্যা প্রজেক্টে আর কোথায় (পাওয়া গেছে, ইচ্ছাকৃতভাবে ছোঁয়া হয়নি)
`deleteById()`-এর `catch (e: Exception)` ব্লকে **কোনো `remember()` কলই নেই** — একটা সত্যিকারের নেটওয়ার্ক-এক্সেপশনে (শুধু HTTP ব্যর্থতা নয়) Delete-এর রিট্রাই সম্পূর্ণ হারিয়ে যেতে পারে। এটা এই সেশনের কাজের সুযোগের বাইরে (TK শুধু "কারণ দেখানো" চেয়েছেন, retry-লজিক বদলাতে বলেননি) — তাই **ছোঁয়া হয়নি**, শুধু TK-কে জানিয়ে রাখা হলো ভবিষ্যতের জন্য।

## 🔍 যাচাই
- পাহারাদার (`tk_guard.py`) **১৭/১৭ পাশ** (186 Kotlin ফাইল)
- ব্র্যাকেট-গণনা তিনটে বদলানো ফাইলেই আলাদা করে মিলিয়ে দেখা হয়েছে
- `remember()`-এর ৫টা কল-সাইটই (সব `SupabaseClient.kt`-এর ভিতরে) হাতে মিলিয়ে দেখা হয়েছে — প্রতিটাতে সঠিক reason পাঠানো হচ্ছে

## 🔴 এখনো বাকি
- **TK-এর লাইভ টেস্ট** — পরের বার কোনো সেভ ব্যর্থ হলে Dashboard-এর বার্তায় আসল কারণ দেখা যাবে কিনা
- **B190 — ঝুঁকিসহ লোডিং-ফিক্স** — পরের সেশনে আলোচনা করে ফাইনাল হবে
- **B148 — RLS** ⛔ TK-এর অনুমতি ছাড়া নিষেধ
- `03_NETLIFY_READY` Netlify-তে আপলোড (TK-এর কাজ)
