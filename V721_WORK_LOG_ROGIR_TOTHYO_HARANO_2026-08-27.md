# 🛠️ V721 — কল এলে/অ্যাপ বন্ধ হলে রোগীর তথ্য হারিয়ে যাওয়া বন্ধ

**তারিখ:** ২৭.০৮.২০২৬ · রাত ~১২.৪৫ IST
**রিপোর্ট:** ডা. কে. এইচ. মণ্ডল (Cooch Behar) · **অনুমোদন:** TK — *"হ্যাঁ নিরাপদে কাজটা করুন"*

---

## ১. সমস্যা (ডাক্তারের ছবি)
Doctor Check-up Record কাগজে —
- নাম · Patient ID · বয়স/লিঙ্গ · মোবাইল · ঠিকানা — **সব `"-"`**
- রোগ = "Piles" (কোডে বসানো fallback)
- হেডার = **Kishanganj**, অথচ রোগী **Cooch Behar**-এর

## ২. আসল কারণ (কোড ধরে প্রমাণিত)
রোগীর প্রসঙ্গ (`RoleSession`) **শুধু মেমরিতে** থাকত — ওই ফাইলে
SharedPreferences-এর ব্যবহার ছিল **০**। ফোনে কল এলে / মেমরি কম পড়লে Android
অ্যাপের প্রসেস বন্ধ করে দেয়; পরে পর্দা আবার খোলে, **কিন্তু ঘরগুলো ফাঁকা**।
তখন —
- `pid` ফাঁকা ⇒ `bindPatientHeader()`-এর `if (pid.isBlank()) return`
  ⇒ ডেটাবেস থেকে রোগীর তথ্য **আনার চেষ্টাই হয় না**
- ব্রাঞ্চ ফাঁকা ⇒ BranchInfo-র byName ফাংশন চুপচাপ **Kishanganj** ধরে

⛔ **রোগীর ব্রাঞ্চ ডেটাবেসে ঠিকই আছে** (রেজিস্ট্রেশনে বাধ্যতামূলক) — TK এই
ভুল ধারণাটা ধরিয়ে দিয়েছিলেন; হারাত শুধু **মেমরির কপিটা**।
**প্রমাণ:** ডাক্তারের পাঠানো ছবিতেই ফোনের উপরে **"On call"** লেখা ছিল।

## ৩. যা করা হলো
| ফাইল | কী |
|---|---|
| `RoleSession.kt` | রোগীর ৯টি ঘর ফোনে **জমা** (`persist()`) ও **ফেরত** (`restoreIfEmpty()`) |
| `PilesClinicApplication.kt` | প্রসেস চালু হলে **একবার** ফেরানো |
| `NativeSession.kt` | **লগ-আউটে জমা তথ্য মুছে যায়** |

### 🔒 নিরাপত্তা
- **রোগী বদলালে মুছে যাওয়ার নিয়ম (V537/V538) এক অক্ষরও বদলায়নি**
- **৩০ মিনিটের সীমা** — এর চেয়ে পুরোনো জমা তথ্য কখনো ফেরানো হয় না
  (কল কয়েক মিনিটের; পরের দিন পুরোনো রোগী কিছুতেই ফিরবে না)
- **মেমরিতে রোগী থাকলে ফেরানো হয় না** — চালু কাজের উপর কখনো লেখে না
- যেকোনো গোলমালে চুপচাপ সরে দাঁড়ায় (try/catch)
- কোনো ক্লাউড-কল নয় · নতুন কলাম নয় · **Egress-এ প্রভাব শূন্য**

### ⛔ যা ইচ্ছে করে ছোঁয়া হয়নি
ব্রাঞ্চ না জানলে Kishanganj ধরার পুরোনো নিয়মটা (`?: KISHANGANJ`) **রাখা
হয়েছে** — ওটা অন্য অনেক ছাপার কাগজেও ব্যবহার হয়, TK-এর অনুমতি ছাড়া বদলানো
ঠিক হত না। এখন রোগীর তথ্য আর হারায় না, তাই ওই fallback-এ পৌঁছানোরই কথা নয়।

## ৪. ওয়েব
ওয়েবে এই দোষ **নেই** — সেখানে কাগজ রোগীর সারি (`p`) থেকে বানানো হয়,
মেমরির আলাদা সেশন নেই (কোডে মিলিয়ে দেখা)। তাই ওয়েবে বদল দরকার হয়নি।

## ৫. যাচাই
- ✅ Kotlin কম্পাইল — নতুন ভুল **০**
- ✅ গার্ডের সব যাচাই পাশ (§৯.১০ একবার ধরেছিল — মন্তব্যের লেখা বদলে ঠিক)
**ভার্সন:** V719 → **V721**
