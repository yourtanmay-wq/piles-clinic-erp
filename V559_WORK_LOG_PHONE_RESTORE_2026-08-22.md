# V559 — ফোনেও সেভ করা চেকআপ ফিরিয়ে আনা · ২২.০৮.২০২৬

## কেন
V558-এর কাজ যাচাই করতে গিয়ে ধরা পড়েছিল: **ফোনের CHECK-UP পর্দা আগে সেভ করা
চেকআপ কখনো ফর্মে ফেরাত না।** `populate()` ফাংশনটা ছিল, কিন্তু B437 (এক রোগীর
তথ্য অন্য রোগীর ঘরে বসে যাওয়া) সারানোর সময় ওটার ডাক তুলে দেওয়া হয়েছিল। ফলে
V554–V558-এর কোনো ঘরই ফোনে ফিরত না, অথচ ওয়েবে ফিরত। TK-কে জানানোর পর তিনি
বললেন **"হ্যাঁ"** — তাই এই কাজ।

## কীভাবে করা হল
ওয়েব অনেক দিন ধরেই পুরো চেকআপটা `patients.doctorFullNote`-এ একটা JSON হিসেবে
রাখে। ফোনও এখন **ঠিক সেই জায়গায়, সেই একই নামে** রাখে ও পড়ে।
- **খোলার সময়:** রোগীর সারিটা পর্দা খোলার সময় এমনিতেই একবার আনা হয় — তার
  ভিতরেই ঘরটা থাকে। ⛔ **বাড়তি একটাও query লাগেনি।**
- **সেভের সময়:** `doctorComplete=true` যে কলটা এমনিতেই যেত, তার সাথেই লেখাটা
  বসে যায়। ⛔ **বাড়তি একটাও নেটওয়ার্ক কল নয়।**
- ⛔ নতুন কলাম বা SQL লাগেনি — `doctorFullNote` কলামটা আগে থেকেই আছে।

## B437 যেন আর না ফেরে — তিনটে বেড়া
1. সারিটা **এই রোগীরই** id দিয়ে আনা হয়েছে, তবু populate করার আগে **আরেকবার
   id মিলিয়ে** দেখা হয়।
2. ডাক্তার কিছু টাইপ করে ফেলে থাকলে ফর্মে **হাত দেওয়া হয় না**; একবারের বেশি
   ভরানোও হয় না (`restoredOnce`)।
3. সেভের জন্য ধরে রাখা রেকর্ডটা (`pendingNote`) কাজ শেষ হওয়ামাত্র **মুছে ফেলা
   হয়**, যাতে পরের রোগীর ঘরে আগেরজনের লেখা বসার পথ না থাকে।

## সবচেয়ে জরুরি বেড়া: ফোনে যে ঘর নেই, সেটা যেন ফোনের সেভে মুছে না যায়
ফোনের পর্দা থেকে আগে কিছু ঘর তুলে দেওয়া হয়েছিল (Advance Payment · Patient
Decision · Previous Result/Cost · Treatment Duration · Other Findings ·
Decision Remark · Documents · Acute/Chronic)। ফোন সেগুলো দেখায়ও না, পড়েও না।
সরল "সব লিখে দাও" করলে **ডাক্তার ওয়েবে যা লিখেছিলেন, ফোন থেকে একবার সেভ
করলেই ফাঁকা হয়ে যেত।** তাই `PHONE_KEYS` — ফোনের সেভে **শুধু ফোনের নিজের
ঘরগুলোই** বদলায়, বাকিগুলো ওয়েবের লেখা হুবহু থেকে যায়।

## নতুন ও বদলানো ফাইল
| ফাইল | কাজ |
|---|---|
| `clinical/CheckupNoteJson.kt` | **নতুন** — ফোনের রেকর্ড ↔ ওয়েবের নামের মেলবন্ধন, merge ও `PHONE_KEYS` |
| `clinical/DoctorCheckupActivity.kt` | খোলার সময় ভরানো · সেভের সময় merge করে লেখা · JSON ↔ ম্যাপ |
| `app/build.gradle.kts` | ভার্সন ৫৫৯ |

## যাচাই
- **Kotlin ৩১/৩১** — রেকর্ড ↔ ওয়েবের নাম, ফাঁকা/অচেনা ঘর, merge, `PHONE_KEYS`
- **Kotlin ১৮/১৮** — JSON-এর সেতু (Activity থেকে হুবহু কেটে এনে, আসল `org.json`
  দিয়ে): ওয়েবের তালিকা-ঘরগুলো তালিকা হয়েই ফেরে, null/ভিতরের বাক্সে ভাঙে না,
  ঘুরে এসেও এক
- ওয়েবের কোড এই কাজে **ছোঁয়া হয়নি** — ওয়েব আগে থেকেই ঠিক ছিল, তাই ওয়েবের
  ভার্সন-ট্যাগও বদলানো হয়নি (অকারণে সবার ক্যাশ ভাঙার দরকার নেই)।

## জানিয়ে রাখা
- ফোন `medical` সারিতে এখনো শুধু পড়ার-মত লেখাটাই (`details`) রাখে; পুরো JSON
  রাখে রোগীর সারিতে। ওয়েবও রোগীর সারিটাই আগে পড়ে, তাই দুই দিকেই মিলে যায়।
- ⛔ পুরোনো রোগীর সারিতে `doctorFullNote` না থাকলে কিছুই বদলায় না — ফর্ম আগের
  মতোই ফাঁকা খোলে।
