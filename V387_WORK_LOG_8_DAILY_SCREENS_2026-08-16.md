# V387 — রোজকার কাজের ৮টি পাতা, অ্যান্ড্রয়েডের হুবহু নকল (web-এর মোবাইল ভিউ)

তারিখ: ১৬.০৮.২০২৬ · আদেশ: **"রোজকার কাজের ৮টা পাতা দিয়ে শুরু করুন, তবে খুব
সাবধানে — অ্যান্ড্রয়েড এর কোন কাজে যেন, সম্পূর্ণ প্রজেক্ট এর অন্য কোথাও যেন
কোন ক্ষতি না হয়।"**

---

## ১. এক নজরে

| | |
|---|---|
| যে ৮টি পাতা করা হলো | Notifications · Appointment · Briefing · Collection List · Chamber Close · Expected Tomorrow · Follow-up Calendar · Draft List |
| বদলানো ফাইল | `styles.css`, `app.js`, `index.html` — **মোট ৩টি** |
| অ্যান্ড্রয়েড সোর্সে বদল | **০টি ফাইল** (যন্ত্রে গোনা) |
| SQL-এ বদল | **০টি ফাইল** · নতুন কোনো SQL লাগবে **না** |
| ডেস্কটপ ভিউতে বদল | **একটিও নিয়ম পৌঁছায়নি** (গঠনগত প্রমাণ, নিচে §৫) |
| বাকি ২৯টি পুরনো পর্দায় বদল | **০** (৩৯০px-এ পিক্সেল-মিলান, নিচে §৫) |

---

## ২. কাজের নিয়ম (আগের মতোই)

1. **আগে অ্যান্ড্রয়েডের যে কোডটা সত্যিই চলে সেটা পড়া** — মরা layout/adapter
   বাদ দিয়ে, আর Kotlin যেখানে XML-এর মান রানটাইমে বদলে দেয় সেখানে **Kotlin-কেই
   আসল ধরে**।
2. তারপর ওয়েব যে HTML সত্যিই আঁকে, সেটা পড়া।
3. তারপর **শুধু মোবাইলের CSS** লেখা। JS-এ হাত পড়েছে কেবল সেখানেই যেখানে
   CSS দিয়ে পৌঁছানো যায় না — এবং তখনও **শুধু class যোগ**, একটাও নিয়ম/হিসাব নয়।
4. শেষে **Chromium-এ এঁকে মেপে** দেখা, আর মাপ যা ধরিয়ে দেয় তাই ঠিক করা।

---

## ৩. পাতা ধরে ধরে কী মেলানো হলো

### ১) 🔔 Notifications — `NotificationsActivity.kt` (XML নেই, পুরোটা কোডে)
* সেকশন-শিরোনাম: ১২sp **bold**, প্যাডিং ১৬/১৪/১৬/৪ — **বাঁ-পাশের রঙিন দাগটা
  ফোনে নেই**, তাই লুকানো হলো (kt:224-232)।
* সারি: সাদা, **কোনা গোল নয়**, ছায়া নেই, বর্ডার নেই; margin ১২/৪/১২/০,
  padding ১৪/১২/১৪/১২ (kt:238-247)।
* আইকন-ঘর: ঠিক **৩৬×৩৬dp বর্গাকার** (গোল নয়), সেকশনের গাঢ় রং, ইমোজি ১৬sp
  (kt:251-258)। ⚠️ ওয়েবে রংটা ইনলাইন লেখা ছিল — **JS ছোঁয়া হয়নি**, CSS-এর
  attribute-সিলেক্টর দিয়ে ঠিক ঐ চারটে ফ্যাকাশে মানকেই গাঢ় রঙে বদলানো হলো।
* লেখা: শিরোনাম ১৩.৫sp bold `#10223A` · নিচের লাইন ১১.৫sp `#5b6b81`, উপরে ২dp।
* ডানের **`›`** চিহ্ন (kt:278-282) ওয়েবে ছিল না — CSS-এর `::after` দিয়ে বসানো হলো।

### ২) ⏰ Appointment — `AppointmentActivity.kt:293-307`
* ফোনে কার্ডটা আসলে **একটাই সাদামাটা TextView**: bg `#F4F8FC`, **কোনা গোল নয়**,
  ছায়া/বর্ডার নেই, আর **তিনটে লাইনই এক মাপ-এক রং** (১৩sp, normal, `#10223A`)।
  ওয়েবে চার রকম মাপ-রং ছিল — সব এক করা হলো।
* ফর্মটা (Name/Mobile/Branch/Date/Remarks/Save) আগেই সাধারণ ভিতে মিলে আছে
  (FieldLabel ১৩sp bold `#6B7280` · FieldInput ৫০dp r১৪ `#F6F9FC`)।

### ৩) 💬 Briefing — `item_briefing_card.xml` + `BriefingAdapter.kt`
* কার্ড: radius ১৩dp, elevation ১.৫dp, সাদা, margin ১০/৪, padding ১২/১০,
  বাঁ-পাশে **৪dp দাগ**।
* **দুটো অবস্থা** (kt:110-111 হুবহু): শিরোনামে `request/refund/reopen/urgent`
  থাকলে **জরুরি** — দাগ `#C0392B`, শিরোনাম `#8E2A20`, চিপ `URGENT`; নইলে
  দাগ `#0F7A43`, শিরোনাম `#14361F`, চিপ `NOTICE`।
* `NOTICE`/`URGENT` চিপ (৯sp bold, r২০dp) ওয়েবে ছিল না — CSS `::before` দিয়ে।
* বোতাম: **Reply সাদা-ghost** (১.৫dp `#D7DDE6`, লেখা `#0F172A`),
  **Delete সাদা-লাল** (১.৫dp `#F3D1D8`, লেখা `#E11D48`),
  **✅ Approve গাঢ়** (সবুজ `#0F7A43` / জরুরিতে লাল `#C0392B`) — সব ১১.৫sp bold r১০dp।

### ৪) 💰 Collection List — `item_collection_row.xml` + `CollectionAdapter.kt`
* উপরের পটি: ফোনের হিরো-গ্রেডিয়েন্ট (`#0B2B59→#1167D8→#16A36D`), padding ১২dp,
  **কোনা গোল নয়**, লেখা সাদা / `#E8F2FF`।
* সারি: radius ১২dp, elevation ৩dp, সাদা, margin ৬dp, বাঁ-পাশে **৫dp গ্রেডিয়েন্ট
  দাগ** (`#0A5428→#0EA25F`), padding ১২dp।
* নাম ১৪sp bold `#1A1A1A` · মোবাইল ১২.৫sp **bold** `#2F7D4E` (kt:50-51 স্প্যান
  দিয়ে রং ও bold দুটোই বদলায়) · বাকি লেখা ১২sp `#6B7280`।
* টাকার বাক্স: r১২dp, ১dp স্ট্রোক, minWidth ৭৮dp —
  CASH `#E8F7EE`/`#BFE6CD`/`#0B7A34`, ONLINE `#E7F0FD`/`#C3DCFA`/`#1457B8`।
  মোডের লেখাটা এখন **টাকার সাথে একই রঙে** (আগে চ্যাপ্টা রঙিন লেবেল ছিল)।

### ৫) 🔒 Chamber Close — `ChamberCloseActivity.kt:131-188`
* সতর্ক-পটি: `#FDECEA` + ১dp `#F0B7B0`, r১১dp, padding ১১dp, মাঝ-বরাবর,
  ১২.৫sp bold `#8E2A1E`।
* সারি: সাদা + ১dp `#F0C9C5`, r১২dp, padding ১২/১০, margin ১০/০/১০/৮;
  তারিখ ১৩sp bold `#10223A`, ব্রাঞ্চ ১১sp `#5B6B81`;
  মাঝের ঘর দু'লাইনে ডানে-চেপা ১১.৫sp bold `#0F7A3D`।
* "Close it" বোতাম: **চকলেট গ্রেডিয়েন্ট** `#8A4B12→#C07A28`, r১৬dp, ১১.৫sp bold।
* 🔧 মাপতে গিয়ে ধরা পড়ল: পুরনো একটা ৪৩০px নিয়ম মাঝের ঘরটাকে ১০০% চওড়া করে
  দিচ্ছিল, তাই বোতামটা সারির বাইরে কেটে যাচ্ছিল — সেটা ঠিক করা হলো।

### ৬) 🧑‍⚕️ Expected Tomorrow — `ExpectedTomorrowActivity.kt` (XML নেই)
* উপরের লাইনটা ফোনে **কার্ড নয়**, সাদামাটা লেখা ১২.৫sp `#475467` — তাই কার্ডের
  খোলটা সরানো হলো।
* সারি: সাদা, **কোনা গোল নয়**, বর্ডার/ছায়া নেই, padding ১৪/১২;
  নাম ১৫sp bold `#101828`, নিচের লাইন ১২.৫sp `#475467`।
* 📞 বোতাম: `#12805C`, r১০dp, padding ১৬/১০, ১৩sp bold সাদা — হুবহু।

### ৭) 📅 Follow-up Calendar — `FollowCalendarActivity.kt:201-255`
* ঘর: উচ্চতা **ঠিক ৫২dp**, radius ৮dp, **বর্ডার নেই**; খালি দিন স্বচ্ছ,
  কাজ-থাকা দিন **`#FDE1E1`** (আগে ওয়েবে সাদা-ঘর + সবুজ বর্ডার ছিল)।
* সংখ্যার ব্যাজ: দিন-সংখ্যার **ঠিক নিচে**, ১০.৫sp `#E5484D`, **কোনো গোল/পটভূমি
  নেই** (আগে ডানে-উপরে সবুজ গোল পিল ছিল)।
* সপ্তাহ-শিরোনাম ১২sp **normal** `#6B7280` · মাসের নাম ১৬sp bold `#1A1A1A` ·
  ◀ ▶ ২০sp `#1A1A1A`, **কোনো ঘর/বর্ডার নেই**।
* "Today Pending Call" পটি: গ্রেডিয়েন্ট ০° `#1167D8→#16A36D`, r১৪dp, padding ১৪dp।

### ৮) 📂 Draft List — `item_draft_card.xml` + `DraftCardAdapter.kt`
* যাচাই করে দেখা গেল এটা **V386-এই হুবহু মেলানো হয়ে গেছে** (r১৮dp · ১dp `#E4EDEC`
  · বাঁয়ে ৪dp `#0B66D8` · ১২.৫sp/৯.৫sp/১০.৫sp · বোতাম ২২dp r৯dp · Restore
  `#EAFBF0` · Delete `#D32F2F`)। **তাই এখানে নতুন করে কিছু বদলানো হয়নি** —
  পিক্সেল-মিলানেও এই পর্দা অপরিবর্তিত এসেছে। সৎভাবে জানাচ্ছি: এই একটার জন্য
  আজ নতুন কাজ লাগেনি।

---

## ৪. যেখানে ওয়েব আর ফোন **হুবহু হতে পারেনি** (লুকোচ্ছি না)

1. **Expected Tomorrow — বোতামের সংখ্যা।** ফোনে বোতাম **একটাই** (`📞 ফোন`)।
   ওয়েবে **তিনটে** (Call · WhatsApp · History)। বোতাম মুছে ফেলা মানে কাজ কমে
   যাওয়া — তাই তিনটেই রাখা হলো, ফোনের পিলের মাপেই। ৩৯০px-এ তিনটে এক লাইনে
   বসে; **৩৬০px-এ শেষটা নিচের লাইনে নামে** (কোথাও পর্দা ছাড়ায় না)।
2. **Appointment — সম্পাদনার বোতাম।** ফোনে **তিনবার ট্যাপ** করে সম্পাদনা হয়
   (`TripleTapEdit`), আলাদা বোতাম নেই। ওয়েবে বোতামটা না রাখলে সম্পাদনাই করা
   যেত না — তাই ছোট করে রাখা হলো।
3. **Appointment — লেখার পার্থক্য।** ফোনে `⏰`, ওয়েবে `📅`; ফোনে নাম যেমন
   লেখা তেমনই, ওয়েবে **বড় হাতের অক্ষরে**। এগুলো *লেখা*-র বিষয়, *ডিজাইনের* নয়
   — তাই নিজে থেকে বদলাইনি। **আপনি বললে বদলে দেব।**
4. **Collection List — উপরের তিনটে ছোট বাক্স।** ফোনে হিরো-পটির নিচে
   💵 Cash · 📱 Online · 👥 Patients — তিনটে আলাদা বাক্স আছে। ওয়েবে ঐ তিনটে
   বাক্স **নেই** (শুধু "Total: … · N Transactions" এক লাইনে)। এটা নতুন *জিনিস
   বানানো* হত, শুধু সাজানো নয় — তাই ছুঁইনি। **আপনি বললে যোগ করে দেব।**
5. **Collection List — মোডের নাম।** ফোনে `CASH` / `ONLINE`, ওয়েবে `CASH` / `UPI`।
   এটা হিসাবের নাম, তাই হাত দিইনি।
6. **Briefing — কার্ডে যা ওয়েবে নেই।** ফোনের কার্ডে আছে পাঠকের **আদ্যক্ষরের
   গোল ছবি**, **কে-কখন** লাইন, আর **"Seen by n"** লাইন। ওয়েবের স্টাফ-কার্ডে
   এই তথ্যগুলোই আঁকা হয় না। এগুলো বসানো মানে **নতুন তথ্য টেনে আনা** — সেটা
   ডিজাইনের কাজ নয়, তাই আন্দাজে করিনি। **আপনি বললে করে দেব।**
7. **Expected Tomorrow — পাতার পটভূমি।** ফোনে এই একটা পর্দায় পটভূমি সমান
   `#F5F7FA`, অ্যাপের গ্রেডিয়েন্ট নয়। ওয়েবে পাতা ধরে পটভূমি বদলানোর কোনো
   হাতল (class) নেই, আর জোর করে বসাতে গেলে পুরনো ব্রাউজারে ভাঙার ঝুঁকি ছিল।
   রংদুটো প্রায় একরকম বলে **ঝুঁকি না নিয়ে গ্রেডিয়েন্টই রাখা হলো**।
8. **কাঁচা-পিক্সেল প্যাডিং।** অ্যান্ড্রয়েডে দু'জায়গায় প্যাডিং/মার্জিন **dp-তে
   নয়, কাঁচা পিক্সেলে** লেখা (`AppointmentActivity.kt:298,304` ও
   `FollowCalendarActivity.kt:210,252`) — ওগুলো ফোনভেদে বদলায়। এখানে
   **density = ৩.০** ধরে হিসাব করা হয়েছে এবং CSS-এ প্রতিটা হিসাব লিখে রাখা আছে।

---

## ৫. ক্ষতি হয়নি — যন্ত্রে করা প্রমাণ

**ক) ডেস্কটপ (গঠনগত প্রমাণ, সবচেয়ে শক্ত)**
যোগ হওয়া ৩২৮ লাইন CSS যন্ত্র দিয়ে পার্স করা হয়েছে:
* মিডিয়া-ব্লক সংখ্যা: **১**, শর্ত: **`(max-width:899px)`**
* মিডিয়া-ব্লকের বাইরে থাকা নিয়ম: **একটিও নেই**
* ব্রেস-ভারসাম্য: **০** (কোনো ব্লক খোলা থাকেনি)
→ অর্থাৎ ৯০০px বা তার বেশি চওড়া পর্দায় **এই নিয়মগুলোর একটাও পৌঁছায় না**।

**খ) মোবাইল (পিক্সেল-মিলান)**
পুরনো (V386) CSS বনাম নতুন (V387) CSS দিয়ে **৩৭টি পর্দা** ৩৯০px-এ এঁকে
বাইট-মিলান:
* হুবহু এক: **৩০টি**
* বদলেছে: **৭টি — ঠিক যে ৭টিতে আজ কাজ হয়েছে** (৮ম, Draft List, আগেই মেলানো
  ছিল বলে অপরিবর্তিত)
* **ঐ ৭টি ছাড়া অন্য কোথাও একটি পিক্সেলও বদলায়নি ✅**

**গ) অ্যান্ড্রয়েড ও SQL**
`find -newermt` দিয়ে গোনা — `02_ANDROID_SOURCE_CODE`-এ **০টি** ফাইল বদলেছে,
`03_SUPABASE_SQL` ও `04_SUPABASE_DATABASE_SETUP`-এ **০টি**।
**নতুন কোনো SQL চালাতে হবে না।**

**ঘ) `app.js`-এ ঠিক কী বদলাল**
পুরো ফাইলে বদল **১৯ লাইন**, তার মধ্যে ১২ লাইন নতুন মন্তব্য+ফাংশন। আসল বদল:
1. নতুন ফাংশন `anBrUrgentCls(title)` — `BriefingAdapter.kt:110-111`-এর হুবহু
   নকল, শুধু একটা **class-নাম ফেরত দেয়**।
2. ব্রিফিং-কার্ডের তিনটে জায়গায় ঐ class বসানো।
3. `wlv1ApprovalButtons`-এ চারটে ✅ Approve বোতামে `class="anBrApprove"` বসানো।
**কোনো ফিল্টার, হিসাব, অনুমতি, সেভ, ক্লাউড-কল — কিছুই ছোঁয়া হয়নি।**

**ঙ) যান্ত্রিক অডিট (৩৬০px ও ৩৯০px)**
পর্দা ছাড়িয়ে যাওয়া · ৮px-এর ছোট লেখা · ২০px-এর ছোট বোতাম —
**নতুন ৮টি পাতায় একটিও সমস্যা নেই।** পুরনো দুটি চিহ্ন আগের মতোই আছে এবং
দুটোই যাচাই করা মিথ্যা-সংকেত (ছাঁকনির সারিটা ফোনেও পাশে সরে, আর
`anFuPill`/`anFuId` ফোনের নিজেরই মাপ)।

---

## ৬. এখন আপনার কাজ

1. Netlify-তে `03_NETLIFY_READY`-এর ভেতরের সব ফাইল তুলে দিন
   (`index.html`-এ `?v=v414` বসানো আছে, তাই পুরনো ক্যাশে আটকাবে না)।
2. ফোনের ব্রাউজারে ৮টা পাতা একবার খুলে দেখুন।
3. কিছু বেখাপ্পা লাগলে বলুন — ঠিক করে দেব।
4. তারপর পরের দলটা ধরব: **চিকিৎসা-সংক্রান্ত ৮টি পাতা** (ক্লিনিক্যাল মডিউল,
   ডক্টর চেকআপ, প্রেসক্রিপশন, ডায়েট চার্ট, ডিজিজ ডিটেইল, ইনভেস্টিগেশন
   অ্যাডভাইস, ইনভেস্টিগেশন ক্যাটাগরি, মেডিসিন স্লিপ)।

⛔ মনে রাখবেন: এই পুরো কাজটা **আসল CSS + আসল HTML** দিয়ে Chromium-এ এঁকে
যাচাই করা — কিন্তু **আসল Supabase ডেটা দিয়ে নয়**। তাই আপনার লাইভ টেস্টটাই
শেষ কথা।
