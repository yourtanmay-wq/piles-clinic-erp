-- ============================================================================
-- V1446 (১৪.০৯.২০২৬, TK-রিপোর্ট ছবিসহ) — V1445-এ `reports` স্কিমা API-তে
-- expose হয়েছে (TK নিজে চালিয়ে যাচাই-SELECT-এ "reports" শব্দ দেখেছেন), কিন্তু
-- পরের প্রশ্নে নতুন ভুল: "Could not find the function reports.expected_count
-- (...) in the schema cache"।
--
-- আসল কারণ (মেপে ধরা, আন্দাজ নয়): ফাংশনটা সঠিক নামে-প্যারামিটারেই আগে থেকে
-- বসানো আছে (VOICE_ALL SQL লাইন ৬৪৬-৬৫৭ যাচাই করা হয়েছে)। V1445 শুধু
-- `notify pgrst, 'reload config'` পাঠিয়েছিল — এটা শুধু কোন স্কিমা প্রকাশ
-- হবে সেই তালিকা রিফ্রেশ করে। কিন্তু PostgREST-এর ফাংশন/টেবিলের নিজস্ব
-- "schema cache" (কোন স্কিমায় কোন ফাংশন আছে, তার তালিকা) আলাদা — সেটা
-- রিফ্রেশ হয় `reload schema` দিয়ে। যেহেতু `reports` স্কিমা এই প্রথমবার
-- প্রকাশিত হলো, PostgREST কখনো এর ভিতরের ফাংশনগুলো ক্যাশে তোলেইনি।
--
-- ⛔ এখানে কোনো টেবিল/ফাংশনের সংজ্ঞা ছোঁয়া হয়নি — শুধু PostgREST-কে
-- একবার নতুন করে সব স্কিমা পড়তে বলা হচ্ছে।
-- ============================================================================

notify pgrst, 'reload schema';
