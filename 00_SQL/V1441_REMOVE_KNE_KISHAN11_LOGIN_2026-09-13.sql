-- ============================================================================
-- V1441 (১৩.০৯.২০২৬) — KNE-KISHAN11 (ARCHANA MANDAL BISWAS) কাজ ছেড়ে দিয়েছেন,
-- TK-নির্দেশ: লগইনের দরজা বন্ধ করা হোক।
--
-- ফোনের StaffDirectory.kt ও ওয়েবের config.js থেকে এই এন্ট্রি ইতিমধ্যে
-- তোলা হয়েছে — তাতে দুই অ্যাপের নিজস্ব বাঁধা তালিকা দিয়ে আর লগইন হবে না।
-- কিন্তু দুটোই ব্যর্থ হলে ফোন ও ওয়েব **মেঘের `staff_login_list()`
-- ফাংশনে** ফিরে যায় (V834-এ কোডে ধরা এই একই ফাঁক), আর ওই ফাংশন
-- `hr.staff_profiles`-এর `active` ঘর দেখে — তাই শুধু এইটুকুই দরকার।
--
-- 🔒 নিরাপদ:
--   · কিছুই মোছে না — শুধু `active = false` (হাজিরা/বেতন/কাজের পুরনো
--     রেকর্ড অক্ষত)
--   · `usercredentials`-এর পাসওয়ার্ড-সারি ছোঁয়া হয়নি — মোবাইলটা আর
--     "চেনা" থাকবে না বলে ওই সারি এমনিতেই আর কাজে লাগবে না
--   · দুবার চালালেও ক্ষতি নেই · আর কারও একটাও সারি ছোঁয় না
--
-- চালানোর জায়গা: Supabase → SQL Editor → পুরোটা পেস্ট করে Run
-- ============================================================================

update hr.staff_profiles
   set active = false, updated_at = now()
 where person_code = 'KNE-KISHAN11';

notify pgrst, 'reload schema';

-- ── যাচাই ────────────────────────────────────────────────────────────────
select person_code, full_name, link_mobile, branch, role_kind, active
  from hr.staff_profiles
 where person_code = 'KNE-KISHAN11';
