-- ============================================================================
-- V1447 (১৪.০৯.২০২৬, TK-রিপোর্ট ছবিসহ) — schema cache রিফ্রেশের (V1446) পরেও
-- নতুন ভুল: "permission denied for schema reports"।
--
-- আসল কারণ (VOICE_ALL SQL মিলিয়ে দেখা, আন্দাজ নয়): প্রতিটা ফাংশনে আলাদা
-- করে `grant execute ... to authenticated` করা হয়েছিল (লাইন ৩৭,৫০,৯৩...),
-- কিন্তু স্কিমার উপর নিজের `grant usage on schema reports to authenticated`
-- বাদ পড়ে গিয়েছিল — Postgres-এ ফাংশনে EXECUTE থাকলেও স্কিমায় USAGE না
-- থাকলে "ঢোকার" অনুমতিই থাকে না। hr/wn/fin স্কিমার জন্য এই একই লাইন
-- আগেই ছিল (V246, লাইন ২৯০: "grant usage on schema hr, wn, fin to
-- authenticated") — `reports`-এর বেলায় শুধু এটাই বাদ পড়েছিল।
--
-- ⛔ কোনো টেবিল/ফাংশন/ডেটা ছোঁয়া হয়নি — শুধু আগে থেকে-দেওয়া ফাংশন-অনুমতি
-- আসলে কার্যকর হওয়ার জন্য যে স্কিমা-অনুমতিটা লাগে, সেটা যোগ করা হলো।
-- ============================================================================

grant usage on schema reports to authenticated;
notify pgrst, 'reload schema';
