# V705 — স্টাফের ফোনে নোটিশ বোর্ডের উপরে বিশাল সাদা ফাঁকা
**তারিখ:** ২৬.০৮.২০২৬ · **ভার্সন:** V705 / 7.05

## TK কী দেখেছেন
> "এটা কিষানগঞ্জ এর মোবাইল, এখানে এরকম কেন দেখাচ্ছে?" — নোটিশ বোর্ড খুললে
> উপরে পর্দার প্রায় দুই-তৃতীয়াংশ জুড়ে **সাদা ফাঁকা**, তার নিচে তবে নোটিশগুলো।
> "এর থেকে আর স্ক্রোল হচ্ছে না।"

## ❌ আমার প্রথম আন্দাজ ভুল ছিল
প্রথমে ভেবেছিলাম বাংলা-বন্ধ স্টাফের ফোনে অনুবাদে না-থাকা বাংলা মুছে গিয়ে
বার্তা ফাঁকা হচ্ছে। **চালিয়ে যাচাই করে দেখলাম ওটা কারণ নয়** — পুরো বাংলা
মুছে গেলে `NoBengali.fix()`-এর শেষে `trim()` থাকায় ফল হয় **একদম খালি**,
লম্বা ফাঁকা নয়। তাই TK-এর কাছে দ্বিতীয় ছবি চেয়ে নিলাম — এবং ছবিতে দেখা
গেল কার্ডের বাঁ পাশের লাল দাগটা **ছোট**, অর্থাৎ কার্ড ছোটই; ফাঁকাটা
কার্ডের **উপরে**, তালিকার বাইরে।

## ✅ আসল কারণ (কোড ধরে যাচাই)
`BriefingActivity.clampPanelsHeight()` — লাইন ৮৭৭ (আগে):

```kotlin
val target = if (want in 1 until cap) want else cap
```

* `want` = উপরের আটটা খোপের দরকারি উচ্চতা · `cap` = পর্দার ৬২%
* **স্টাফের ফোনে আটটা খোপই GONE** (সবগুলোই Master-only বা pending-only),
  তাই `want = 0`
* `0 in 1 until cap` → **false** ⇒ `target = cap` ⇒ **ফাঁকা খোপগুলোকে জোর
  করে পর্দার ৬২% উঁচু** করে বসানো হত
* Master-এর ফোনে খোপ থাকে (`want > 0`), তাই সেখানে কখনো চোখে পড়েনি —
  এই কারণেই দোষটা এতদিন লুকিয়ে ছিল

## সমাধান
```kotlin
val target = when {
    want <= 0  -> android.view.ViewGroup.LayoutParams.WRAP_CONTENT
    want < cap -> want
    else       -> cap
}
```
⛔ **স্থির ০ বসানো হয়নি, `wrap_content` বসানো হয়েছে** — ০ বসালে ভিতরের
ছেলেটার layout আর হত না, ফলে `installPanelsScrollClamp()`-এর শোনার কাজও
চালু হত না এবং পরে খোপ এলে সেটা চিরতরে লুকিয়ে থাকত।

## যাচাই — পুরোনো বনাম নতুন (পর্দা ২৪০০px, cap = ১৪৮৮)
| খোপের দরকার | পুরোনো | নতুন |
|---|---|---|
| **0 (স্টাফ)** | **1488** ← দোষ | **wrap_content (০)** |
| 300 | 300 | 300 |
| 1000 | 1000 | 1000 |
| 1487 | 1487 | 1487 |
| 1488 | 1488 | 1488 |
| 2000 | 1488 | 1488 |

⇒ **শুধু ০-এর ঘরটাই বদলেছে**, বাকি প্রতিটা মান হুবহু আগের মতোই।

| যাচাই | ফল |
|---|---|
| `verify_kotlin_compile.py` | ✅ নতুন ভুল ০ |
| `tk_guard.py` (২২টা মেশিন-যাচাই) | ✅ সব পাশ |
| একই দোষ প্রজেক্টের অন্য কোথাও? | ✅ খুঁজে দেখা হয়েছে — নেই (`clampPanels` শুধু এখানেই) |

⛔ কম্পিউটারের অ্যাপে এই দোষ নেই — ওখানে খোপগুলো সাধারণ HTML, ফাঁকা হলে
নিজে থেকেই জায়গা নেয় না। তাই app.js-এ কিছু বদলানোর দরকার হয়নি।

⚠️ Android চালানোর উপায় নেই — TK দেখে বলবেন।
