# V586 — চেক-আপের লেখা দু'জায়গায় পড়া · Realtime (ছবি বাদ)
**তারিখ:** ২৩.০৮.২০২৬ · **ভার্সন:** V586 · **অবস্থা:** কোড শেষ, লাইভ টেস্ট বাকি

TK-এর দুটো সিদ্ধান্ত অনুযায়ী (একটা একটা করে আলোচনার পরে)।

---

## ১) ওয়েবে সেভ করা চেক-আপ ফোনে পুরোটা পড়া

**দোষটা কী ছিল:** চেক-আপের সব ঘর একটা লম্বা লেখায় জমা হয়। ফোন জোড়ে `"; "` দিয়ে, ওয়েব জোড়ে `" | "` দিয়ে। পড়ার কোড (`CheckupA4Report.parseDetails`) শুধু `"; "` চিনত।

**চালিয়ে প্রমাণ (পুরনো কোডে):**
```
পুরনো কোডে — ফোনের লেখা : Grade=''
পুরনো কোডে — ওয়েবের লেখা: Duration=''
```

তিনটে আলাদা দোষ ধরা পড়ল — **তিনটেই সারানো হলো:**

| # | দোষ | কেন হত | এখন |
|---|---|---|---|
| ক | ওয়েবের লেখা ফোনে পড়া যেত না | শুধু `"; "` খোঁজা হত | দুটো বিভাজকই চেনা হয় |
| খ | **গ্রেড কোনোদিনই দেখাত না** | ফোন লেখে `Internal Piles Grade`, খোঁজা হত শুধু `Grade` — আর তার আগে বিভাজক না থাকায় কখনো মিলত না | দুই নামেই খোঁজা হয় |
| গ | **ভাগ ২ · ৩ · ৪-এর লেখা কেটে যেত** | ওই লেখাগুলোর **ভিতরেও** `"; "` / `" | "` থাকে (মডেলের `readable()` ওভাবেই জোড়ে), তাই পড়া থেমে যেত — **ফোনের নিজের রেকর্ডেও** | ঘরের শেষ চেনা হয় **বিভাজকের পরে চেনা লেবেল আছে কি না** দেখে |

সঙ্গে লেবেলের বিকল্প নাম: `Investigation`/`Investigations`, `Est Cost`/`Financial`।

**ওয়েবের সেভে যোগ:** `Patient Reported` · `History Detail` · `Habits` — ফোনের **হুবহু একই লেবেলে**। ⛔ শুধু নতুন সেভে; পুরনো রেকর্ড ছোঁয়া হয়নি।

⛔ **সেভের নিয়মে এক অক্ষরও হাত পড়েনি** — তাই পুরনো কোনো রেকর্ড নষ্ট হওয়ার পথ নেই; সবই এখন **আরও ভালো** পড়ে।

### চালিয়ে যাচাই (অনুমানে নয়)

ওয়েবের আসল কোড ব্রাউজারে চালিয়ে যে লেখাটা বেরোল, সেটাই আসল Kotlin-এ ঢুকিয়ে:

```
ওয়েবের আসল লেখা → ফোনের পড়া:
  Complaint     = 'ITCHING'
  Duration      = '6 Years'
  Patient Rep.  = 'পায়ুপথে রক্তপাত — 2 Years; মলদ্বারে ব্যথা — 6 Months'
  History Det.  = '🩸 রক্তপাতের ইতিহাস — রঙ: টকটকে লাল; সময়: মলের পরে'
  Habits        = 'দীর্ঘমেয়াদী কোনো রোগ আছে কি না? ডায়াবেটিস; খাবারে ফাইবারের পরিমাণ কম; দৈনিক জল পানের পরিমাণ 2.5 লিটার'
  Grade         = 'Grade III'
  Investigation = 'MRI'
  Est Cost      = '24000'
```

ফোনের নিজের লেখাও (ভিতরে `;` থাকা ভাগ ২/৪ সহ) পুরোপুরি পড়ে — আলাদা করে চালিয়ে দেখা হয়েছে।

---

## ২) Realtime — ছবি বাদ দিয়ে চালু

**ফাইল:** `04_SUPABASE_DATABASE_SETUP/V586_REALTIME_CHOBI_CHARA_CHALU_2026-08-23.sql`

**ওয়েবের কোডে কিছু বদলাতে হয়নি** — `wireRealtime()` (B623) আগে থেকেই তৈরি: একটা সারি এলে **শুধু সেটাই** ক্যাশে বসায়, নতুন কোনো ডাউনলোড করে না। তাই "ছবি বাদ" পুরোটাই প্রকাশনার (publication) কলাম-তালিকায়।

| যা করা হলো | কেন |
|---|---|
| ৯টা টেবিল `supabase_realtime`-এ যোগ | কেউ সেভ করলে শুধু ওই সারিটাই সবার কাছে যাবে |
| `patients.photo` · `followups.photo` · `medical.photos` **বাদ** | ছবি base64-এ ৫৫–১২০ KB; সবার ব্রাউজারে গেলে খরচ উল্টো বাড়ত |
| `trash` ইচ্ছে করে বাদ | ওটা শুধু দরকারে নামে (V509), স্ট্রিম হয় না |
| **REPLICA IDENTITY ছোঁয়া হয়নি** | FULL করলে প্রতিটা আপডেট/ডিলিটে পুরনো গোটা সারিটাও (ছবিসহ) যেত। ডিফল্টে শুধু `id` যায়, আর ওয়েবের কোড ডিলিটে শুধু `payload.old.id`-ই পড়ে |
| ঘরের নাম `information_schema` থেকে পড়া | আন্দাজে কলামের নাম লেখা হয়নি |

**জমা থাকা ছবি মুছবে না** — যাচাই করা হয়েছে: `mergeById()` ঘর-ধরে জোড়ে (`{...a,...r}`, app.js:591), তাই ছবির ঘরটা না এলে আগের ছবিটাই থেকে যায়।

### চালিয়ে যাচাই — সত্যিকারের PostgreSQL 16-এ

```
     টেবিল     | কতগুলো ঘর যাবে | ছবি যাচ্ছে?
---------------+---------------+------------
 address_tags  |             2 | না ✅
 briefings     |             2 | না ✅
 doctor_visits |             3 | না ✅
 enquiries     |             3 | না ✅
 followups     |             3 | না ✅
 medical       |             4 | না ✅
 patients      |             4 | না ✅
 payments      |             3 | না ✅
 products      |             2 | না ✅
```
`trash নেই ✅` · দুবার চালিয়েও কোনো ভুল নেই (যতবার খুশি চালানো যায়)।

⚠️ পরে কোনো টেবিলে **নতুন কলাম** যোগ হলে সেটা নিজে থেকে ঢুকবে না — তখন এই SQL আবার একবার চালালেই হবে।

---

## নতুন ও বদলানো ফাইল

**নতুন:** `V586_REALTIME_CHOBI_CHARA_CHALU_2026-08-23.sql` · `V586_RMP_PURONO_ROGIR_COMMISSION_DEKHAR_SQL_2026-08-23.sql` · `00_GUARD/verify_v586_note_read.py`
**বদলানো:** `clinical/CheckupA4Report.kt` · `03_NETLIFY_READY/app.js` · `version.json` · `build.gradle.kts`

## যাচাই

| পাহারা | ফল |
|--------|-----|
| `verify_v586_note_read.py` (নতুন) | ১৫/১৫ ✅ |
| `verify_kotlin_compile.py` | নতুন ভুল **০** ✅ |
| **00_GUARD-এর ১৮টা পাহারাই** | সবগুলো সবুজ ✅ |
| `node --check app.js` | ঠিক ✅ |
| Realtime SQL | সত্যিকারের PostgreSQL 16-এ চালিয়ে যাচাই ✅ |
| ভার্সন | দুটোই **V586 / 5.86** ✅ |

## TK-কে যা করতে হবে

1. **Supabase → SQL Editor**-এ `V586_REALTIME_CHOBI_CHARA_CHALU_2026-08-23.sql` চালানো (উপরের তালিকাটা বেরোবে)।
2. তারপর কয়েকদিন Usage দেখা — Realtime বার্তা বাড়ছে ও Egress কমছে কি না।
3. **সব ফোনে V586 বসানো + ওয়েব Netlify-তে তোলা** (তালিকার ক১ · ক২)।
