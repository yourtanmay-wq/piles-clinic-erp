# ⌨️🔴 V772 — নম্বরের সাজেশন: **এবার আসল কারণটা পাওয়া গেছে** (২৮.০৮.২০২৬)

**TK:** *"যেকোনো ঘরে চাপ দিয়ে দেখলাম — নম্বরের সাজেশন আসে, সম্পূর্ণ প্রজেক্টের
সমস্ত জায়গায় আসতেছে। এটা সঠিকভাবে বন্ধ করতে হবে, খুব সাবধানে, সততার সাথে।
আর কতবার আপনাকে বলতে হবে?"*

---

## ১. আগের চারবার কেন ব্যর্থ হলো — সৎ জবাব

| ভার্সন | আমি কী ধরে নিয়েছিলাম | ফল |
|---|---|---|
| V418 | Android-এর Autofill | কমেনি |
| V752 | কীবোর্ডের personalized learning | কমেনি |
| V758 | `disableAutofillServices()` = "আসল সমাধান" | **কমেনি** |
| V761 | ফোকাস পড়লেই `cancel()` | কমেনি |

**চারবারই আমি একই দিকেই খুঁজেছি — Autofill।** সেটাই ভুল ছিল।

### 🔴 V758-এর দাবিটা ভুল ছিল — লিখে রাখছি
`AutofillManager.disableAutofillServices()` Android-এর ভিতরে আসলে ডাকে
`disableOwnedAutofillServices()` — অর্থাৎ **যে অ্যাপ নিজেই একটা Autofill
সেবা**, শুধু তারই সেবা বন্ধ হয়। আমাদের অ্যাপ Autofill সেবা নয়
⇒ **ডাকটা কার্যত কিছুই করে না।** TK-কে "সমাধান হয়ে গেছে" বলা ঠিক হয়নি।

---

## ২. আসল কারণ — কোড গুনে বের করা (আন্দাজ নয়)

এবার উল্টো দিক থেকে খোঁজা হলো: **অ্যাপ নিজে কী কী ক্লিপবোর্ডে রাখে?**

```
$ grep -rn "setPrimaryClip" --include=*.kt .
→ ১৭টা জায়গা
```

| ফাইল | কোথায় |
|---|---|
| `DialerActivity` | নম্বর কপি |
| `ChamberAttendanceAdapter` | ৭ জায়গা — নাম · মোবাইল · Patient ID · পুরো তথ্য |
| `FollowUpAdapter` · `DoctorVisitAdapter` | কার্ডের মোবাইল |
| `PatientTimelineActivity` · `ExpectedTomorrowActivity` · `DoctorVisitActivity` | নাম ও মোবাইল |
| `ClipboardUtil.copyOnLongPress` | পুরো অ্যাপে লম্বা-চাপে কপি |

**Gboard (আর প্রায় সব কীবোর্ড) সদ্য কপি করা লেখাটা ~১ ঘণ্টা ধরে
প্রত্যেকটা ঘরের সাজেশন-পট্টিতে চিপ হিসেবে দেখায়** — কোন অ্যাপ, কোন ঘর,
তাতে কিছুই যায় আসে না।

⇒ **এই কারণেই "সম্পূর্ণ প্রজেক্টের সমস্ত জায়গায়" আসছিল।**
⇒ Autofill-এর কোনো পতাকা দিয়ে এটা কখনোই বন্ধ হতো না — ওটা আলাদা জিনিস।

---

## ৩. কী করা হলো

### ক) 📋 ক্লিপবোর্ডে "গোপন" চিহ্ন — **মূল সমাধান**
`ClipboardUtil.kt`-এ নতুন `object Clip`। কপি করার সময় লেখাটাকে
`IS_SENSITIVE` চিহ্ন দেওয়া হয় ⇒ কীবোর্ড ওটা নিজের ইতিহাসে রাখে না,
সাজেশনেও দেখায় না।
**১৭টা জায়গার সবক'টা** এখন এই একটাই দরজা দিয়ে যায়।

- ⛔ পেস্ট করা একটুও বদলায়নি — WhatsApp/ডায়ালারে আগের মতোই পেস্ট হবে।
- ⛔ "copied" টোস্ট আগের মতোই।
- ⛔ কোনো তথ্য হারায় না।

### খ) 🕸️ প্রতিটা উইন্ডোর জন্য জাল (`NoAutofill.netForEveryWindow`)
গুনে দেখা গেল **৬৫টা পপ-আপ** `PremiumAlert` দিয়ে যায় না, তাই সেখানে
Autofill-এর পাহারা পৌঁছাত না। ৬৫টা ফাইল হাতে বদলানোর বদলে **একটাই জাল** —
`AutofillCallback`; কোনো ঘরে Autofill-সাজেশন ভেসে উঠলেই Android খবর দেয়,
তখন ওই ঘরকে চিরতরে চিহ্ন দিয়ে সাজেশনটা বন্ধ করা হয়।
পর্দা বন্ধ হলে জাল খুলে নেওয়া হয় (স্মৃতি জমে না)।

### গ) 🛡️ পাহারা ৯.২৯ — যেন আর কখনো ফাঁক না হয়
`ClipboardUtil.kt` ছাড়া প্রজেক্টের আর কোথাও সরাসরি `setPrimaryClip(`
লেখা থাকলে guard **FAIL** করবে।
✅ ফাঁদ-পরীক্ষা করা হয়েছে: একটা জায়গায় ইচ্ছে করে পুরনো কোড ফিরিয়ে দিলে
guard ধরে ফেলে, ফাইলের নামসহ।

---

## ৪. ⚠️ সৎ সীমা (লুকানো হয়নি)

1. `IS_SENSITIVE` পতাকাটা **Android 13 (API 33) থেকে সরকারি** — সেখানে
   নিশ্চিত কাজ করে। তার আগের Android-এ Gboard এই একই নামের পতাকা দেখে বলে
   জানা আছে, কিন্তু সেটা Google-এর নিজের সিদ্ধান্ত — **১০০% নিশ্চিত নয়**।
2. **যদি কোনো ফোনে তবু থেকে যায়** — ওই ফোনে একবার এই পথটা:
   **Gboard → ⚙️ Settings → Clipboard → "Show recently copied text in the
   suggestion strip" → বন্ধ।** এটা নিশ্চিত কাজ করে, একবারই করতে হয়।
3. **কম্পিউটারে (ওয়েব)** ব্রাউজারে "গোপন কপি" বলে কিছু নেই — ওখানে
   ব্রাউজারের নিজের সাজেশন V757-এ আগেই বন্ধ করা আছে (`wlv1NoSuggest`)।

---

## ৫. যাচাই

| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py` (৩৩টা যাচাই, নতুন ৯.২৯ সহ) | ✅ পাশ |
| ৯.২৯-এর ফাঁদ-পরীক্ষা (ইচ্ছে করে ভুল বসিয়ে) | ✅ ধরা পড়ল |
| `verify_android_resources.py` | ✅ পাশ |
| `verify_kotlin_compile.py` | ✅ নতুন ভুল **০** (বেসলাইন বাড়েনি) |
| ১৭টা কপি-জায়গার একটাও বাদ পড়েনি | ✅ grep-এ প্রমাণ |
| টোস্ট ও লেবেলের লেখা এক আছে | ✅ হুবহু আগের মতো |
