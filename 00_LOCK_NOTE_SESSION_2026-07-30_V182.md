# 🔒 LOCK NOTE — V182 (৩০.০৭.২০২৬ দুপুর ১২.৪০) · খাতার সারি **B166**

> **এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না।**

**ভার্সন:** `versionCode 182` · `versionName 1.82` · পর্দায় **V182**
**⛔ Supabase-এ কোনো SQL লাগবে না।**

---

## TK কী বলেছেন (৩ নম্বর সন্দেহ)

> *"ব্যর্থ Delete-এর স্থায়ী Retry নেই। Save এবং Update ব্যর্থ হলে Retry ব্যবস্থা আছে। কিন্তু মূল Delete ব্যর্থ হলে সেটি কেন্দ্রীয়ভাবে আবার পাঠানোর তালিকায় ঢোকে না। ফল: দুর্বল নেটে Delete চাপার পরে কাজটি অসম্পূর্ণ থেকে যেতে পারে।"*

**TK-এর অনুমতি:** "করুন"।
**TK-এর কথা হুবহু ঠিক ছিল।** কোডে মিলিয়ে দেখা গেল — `upsert` ও `updateById` দুটোই ব্যর্থ হলে কাজটা কেন্দ্রীয় তালিকায় তুলে রাখত, **শুধু `deleteById`-ই রাখত না** (তিনটে ফাংশন একই ফাইলে, পাশাপাশি)।

---

## যা করা হলো

ডিলিট ব্যর্থ হলে কাজটা এখন `CloudWriteQueue`-এ ওঠে। তাই **নেট ফিরলে বা অ্যাপ খুললে ডিলিটটা নিজে থেকেই শেষ হয়ে যায়** — খাতার সারি B165-এর সেই আধা-কাজ (Trash তৈরি · সারি লুকানো · কিন্তু মূল রেকর্ড রয়ে গেছে) আর পড়ে থাকবে না।

⛔ **আইডি ধরে মোছা হয়**, তাই একই কাজ দুবার পাঠালেও ভুল সারি কখনো মুছবে না।

## 🔒🔒 সঙ্গে তিনটে পাহারা বসাতেই হয়েছে — নইলে এই কাজটাই বিপদ ডেকে আনত

TK-কে কাজ শুরুর আগেই এই ঝুঁকির কথা জানানো হয়েছিল।

**(১) Restore-এর পাহারা — সবচেয়ে জরুরি**
ডিলিট ব্যর্থ হলো → তালিকায় "মুছে ফেলো" জমা রইল → এর মধ্যে মাস্টার **Trash থেকে রেকর্ডটা Restore করলেন** → পরে ওই পুরনো "মুছে ফেলো" চলে যেত এবং **ফেরানো রেকর্ডটা আবার মুছে যেত।**
**এখন:** সারিটা ফিরে আসামাত্র তার অপেক্ষমাণ "মুছে ফেলো" তালিকা থেকে উঠে যায় (নতুন `CloudWriteQueue.forget()`, ডাকা হয় `DeletedGuard.unmark` থেকে) — **সবচেয়ে নতুন ইচ্ছেটাই জেতে**।
⛔ এটা `unmark`-এর **একদম প্রথমে** বসানো হয়েছে, কারণ যে ডিলিট ব্যর্থ হয়েছিল তার চিহ্ন ওই তালিকায় কখনো বসেইনি (চিহ্ন বসে শুধু **সফল** ডিলিটে) — তাই পরে বসালে ফাংশনটা আগেই ফিরে যেত ও পাহারাটা চলতই না।

**(২) ডিলিটের ক্লাউড-চিহ্নের পাহারা**
নতুন করে ডিলিট করলে ক্লাউডের চিহ্নটা (`deleted_records`) আবার লেখা হয়। ওই আইডির পুরনো "মুছে ফেলো" তালিকায় পড়ে থাকলে সেটা পরে গিয়ে **নতুন চিহ্নটাই মুছে দিত**, আর অন্য ফোনগুলো ডিলিটের খবর পেত না। সেটাও বন্ধ।

**(৩) ৫০ বারের সীমার পাহারা**
ব্যর্থ হওয়ার মুহূর্তেই কাজটা আবার তালিকায় ওঠে (তখন গোনা ০ থেকে শুরু)। তাই সংখ্যাটা প্রতিবার শূন্য হয়ে গেলে **৫০ বারের সীমাটা কোনোদিন ছোঁয়াই যেত না** — যে কাজ কোনোদিন যাবে না সেটা চিরকাল চেষ্টা করে যেত। এখন দুটোর মধ্যে **বড় সংখ্যাটাই** রাখা হয়।

## ⚡ রোজকার কাজে বাড়তি ভার নেই

নতুন পাহারাটা (`forget`) প্রতিটা সফল সেভের সঙ্গে ডাকা হয়। প্রতিবার ফাইল পড়লে সেটা হত রোজকার কাজের গায়ে বাড়তি ভার — ঠিক **খাতার সারি B27 · B31**-এ TK-এর স্টাফদের যা হয়েছিল (পর্দা ধীর হয়ে যাওয়া)।

তাই ভিতরে একটা **স্মৃতির চিহ্ন** রাখা হয়েছে: অপেক্ষমাণ কোনো Delete না থাকলে **একটাও ফাইল ছোঁয়া হয় না**।

## ⛔ যা ছোঁয়া হয়নি

- নতুন কোনো **ক্লাউড-কল নেই** · কোনো **ডিজাইন** বদলায়নি · **টাকার হিসাব** ছোঁয়া হয়নি · **SQL লাগেনি**।
- **Deadlock-এর ঝুঁকি নেই** — তালার ভিতরে একটাও নেট-কল বা অন্য তালা নেই।
- Trash Bin · Restore · Delete Forever · মাস্টারের অনুমতির নিয়ম — সব আগের মতোই।

## যাচাই (কাজের পরে আবার, TK-এর নিয়ম)

- নিজের হাতে ব্র্যাকেট গোনা — **৩টে ফাইলেই পাশ**
- প্রতিটা নতুন লাইনের API/টাইপ হাতে মিলিয়ে দেখা (`JSONObject.put(String, int)` · `optInt` · `@Volatile Boolean?` · `synchronized`) — **পাশ**
- পাহারাদার `tk_guard.py` — **১৭টা যাচাই, সব পাশ**
- আগের অনুমোদিত কাজের যাচাই — **৫৮/৫৮ পাশ**
- ভার্সন চার জায়গায় এক — ZIP · `versionCode 182` · `versionName 1.82` · পর্দায় **V182**

**ফাইল:** `SupabaseClient.kt` · `CloudWriteQueue.kt` · `DeletedGuard.kt` · `build.gradle.kts` · `DashboardActivity.kt` · `app.js` (শুধু পর্দায় লেখা ভার্সন)

---

## 🔴 যা এখনো বাকি

TK-এর ৯টা সন্দেহের **বাকি ৬টা** — ⛔ প্রতিটার আগে আলোচনা ও TK-এর স্পষ্ট অনুমতি বাধ্যতামূলক:

৪. অন্য ফোনে Restore-এর খবর · ৫. ওয়েব অ্যাপের Delete · ৬. একাধিক Retry এক তালার নিচে · ৭. Chamber Close সতর্কবাতিতে গোনা · ৮. Automatic Refresh (খরচের সিদ্ধান্ত TK-এর) · ৯. TK-এর লেখাটা কেটে গেছে — পুরো কথা জানতে হবে।

আগে থেকেই বাকি: **B148 — RLS** (⛔ TK-এর অনুমতি ছাড়া নিষেধ) · TK-এর লাইভ টেস্ট · সপ্তাহখানেক পরে Supabase-এর খরচ দেখা।
