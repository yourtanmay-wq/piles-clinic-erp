# 📒 খাতা — সেশন ২১.০৮.২০২৬ (V495)

**ভিত্তি:** V494 · **TK-এর নির্দেশ:** ওয়েবেও ডুপ্লিকেট ঠেকানোর ব্যবস্থা বসানো

## কোথায় বসানো হলো — এবং কেন ঠিক সেখানেই
অ্যাপে (V493) পাহারাটা বসেছিল `SupabaseClient.fetchListOrNull`-এ — অর্থাৎ
**যে একটাই জায়গা দিয়ে সব পড়া যায়**। ওয়েবে সেই একই জায়গা হলো `fetch`:
supabase-js (CDN v2) প্রতিটা পড়া/লেখা এর মধ্য দিয়েই পাঠায়।

তাই `app.js`-এর একদম শুরুতে একটাই মোড়ক বসানো হলো ⇒ **app.js · finance.js ·
partners.js · profile.js · rmp_commission.js · notebook.js — সব ফাইলের সব
পড়াই** ঢাকা পড়ল, অথচ **একটাও পর্দার কোড বদলাতে হয়নি**।

## নিয়ম
- **একসঙ্গে:** হুবহু একই অনুরোধ চলমান থাকলে দ্বিতীয়জন সেটারই উত্তর নেয়।
- **সদ্য শেষ:** ২০ সেকেন্ডের TTL (অ্যাপে ৬০; ওয়েবে মাস্টার বেশি কাজ করেন
  বলে ইচ্ছে করে ছোট — প্রজেক্টের পুরনো `CloudReadCache`-ও ২০ সেকেন্ডই ছিল)।

## 🔒 সুরক্ষা
- শুধু **GET**; সব লেখা হুবহু আগের মতো সরাসরি যায়
- শুধু **Supabase-এর `/rest/v1/`**; auth · storage · realtime · অন্য সাইট অস্পৃশ্য
- **লেখামাত্র** সব জমানো মুছে যায়
- **ব্যবহারকারী বদলালেই** সব মুছে যায় (চাবিতে পরিচয়ও থাকে)
- **ব্যর্থ উত্তর কখনো জমে না** (B446-এর একই নীতি)
- প্রত্যেকে **নিজের আলাদা `Response`** পায় ⇒ একজনের পড়া উত্তর অন্যজনের
  কাছে খালি হয়ে যেতে পারে না
- চাবিতে ঠিকানা + Authorization + **Accept-Profile (hr/fin)** + **Range
  (পাতা)** + Prefer ⇒ আলাদা অনুরোধ কখনো এক হয় না
- **"Sync Now"** চাপলে জমানো মুছে সব টাটকা আনা হয়
- যেকোনো গোলমালে চুপচাপ সরে দাঁড়ায় — তখন আসল `fetch` আগের মতোই

## প্রমাণ — ১৫/১৫ পাশ (`00_GUARD/web_dedupe_test/`)
পরীক্ষা কোডটা **`app.js` থেকেই বের করে** চালানো হয়, তাই পরীক্ষা যা চালায়
আর ওয়েবে যা চলে — দুটো কখনো আলাদা হতে পারে না।

- ৮টা একসঙ্গে একই অনুরোধ → **নেটে ১ বার**; আটজনই নিজের উত্তর পড়তে পারল
- `content-range` হেডার অটুট (পাতা গোনা ভাঙে না)
- লেখা সরাসরি যায়; লেখার পরে টাটকা আনে
- Supabase ছাড়া অন্য ঠিকানা **একেবারেই ছোঁয়া হয় না**
- ব্যর্থ (500) উত্তর জমে না; পরের বার নেটে যায়
- `hr` ও `fin` schema আলাদা; আলাদা পাতা আলাদা
- ব্যবহারকারী বদলালে দ্বিতীয়জন **আগের জনের তথ্য পায়নি**
- চলমান-অনুরোধ তালিকা খালি (মেমরি লিক নেই)

## ছোঁয়া হয়নি
schema · RLS · role · branch permission · ডিজাইন · টাকার হিসাব · payment/refund ·
notification · কোনো তথ্য মোছা হয়নি · **একটাও SQL চালানো হয়নি** ·
**Android-এর কোনো ফাইল বদলানো হয়নি** (শুধু ভার্সন নম্বর)।

## ⚠️ সীমাবদ্ধতা
এখানে Android build করা যায় না (SDK ও Maven ব্লকড)। Egress সত্যিই কমল কিনা —
**২৪–৪৮ ঘণ্টার Supabase chart-ই চূড়ান্ত প্রমাণ**।

*লেখা হলো ২১.০৮.২০২৬*
