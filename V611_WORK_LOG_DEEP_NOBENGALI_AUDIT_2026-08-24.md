# V611 — গভীর NoBengali যাচাই + একটা বড় সৎ আবিষ্কার
**তারিখ:** ২৪.০৮.২০২৬ · **ভার্সন:** V611 / 6.11

## TK-এর নির্দেশ
"আরো একবার আরো গভীরে যাচাই করে দেখুন এবং নিরাপদে ঠিক করুন।"

## 🔍 বড় আবিষ্কার (সততার সাথে জানানো হচ্ছে)
গভীরে গিয়ে `NoBengali.kt` পুরোটা পড়ে ধরা পড়ল — **প্রজেক্টে ইতিমধ্যেই
একটা app-wide, স্বয়ংক্রিয় সুরক্ষা-ব্যবস্থা আছে** (`hookApp()`,
`PilesClinicApplication.kt`-এ বসানো), যেটা:
- প্রতিটা Activity-তে (কোনো ব্যতিক্রম ছাড়া) স্বয়ংক্রিয়ভাবে বসে যায়
- **প্রতিটা লেআউট-বদলে** (নতুন কিছু যোগ/সরানো হলেই) আবার পুরো পর্দা
  ঝেড়ে দেখে — তাই পরে (async) যোগ হওয়া কার্ড/লেখাও ধরা পড়ার কথা
- ব্যতিক্রম মাত্র একটা: **EditText-এর ভিতরের লেখা কখনো ছোঁয়া হয় না**
  (ইচ্ছাকৃত — রোগীর আসল বাংলা ডেটা নষ্ট হওয়া এড়াতে)

**এর মানে:** V610-এ যা "বাগ" বলে ধরেছিলাম, তার একাংশ আসলে এই
স্বয়ংক্রিয় ব্যবস্থাতেই ইতিমধ্যে ঢাকা পড়ছিল হয়তো — আমি নিশ্চিত করে বলতে
পারি না (ফোনে নিজে চালিয়ে দেখার সুযোগ আমার নেই)। তাই V610/V611-এর সব
ফিক্সই **অতিরিক্ত সুরক্ষার স্তর** (defense-in-depth) — ক্ষতি করে না,
নিশ্চয়তা বাড়ায়।

## এবার যা সত্যিই নতুন করে ধরা পড়ল ও ঠিক হলো
| ফাইল | কী | ধরন |
|---|---|---|
| IncomeExpenseActivity.kt | "টাকার খাতা"/"মোট খরচ"/"আজকের হিসাব" ইত্যাদি (৭ জায়গা) | TextView |
| PartnerSharesActivity.kt | "হালনাগাদ হচ্ছে…" | TextView |
| PatientClinicalHistoryActivity.kt | "লোড করা গেল না…" | TextView |
| **DoctorCheckupActivity.kt — EditText** | "নিজের তোলা ছবি" (২ জায়গা) | ⚠️ **সত্যিকারের ফাঁক** — EditText sweep-এর বাইরে, তাই এটা নিশ্চিতভাবেই ঠিক করা দরকার ছিল |
| DoctorCheckupActivity.kt — ৩টা Dialog | "ছবির নাম"/"ছবির সব দাগ মুছে যাবে"/"ঘড়ির কাঁটা অনুযায়ী জায়গা" | `installDialog()` যোগ (V512-এর প্রমাণিত পথ) |
| DoctorCheckupActivity.kt — ২টা TextView | "যে ফোলা বা নালীতে..." / "রিপোর্ট কোন ভাষায়..." | TextView |

## যা ইচ্ছাকৃতভাবে ছোঁয়া হয়নি
- **PrescriptionActivity.kt** — যাচাই করে দেখা গেল এটা **আগে থেকেই**
  নিরাপদ (V512-এ `installDialog()` দিয়ে ঢাকা) — ভুল করে import যোগ
  করেছিলাম, নিজে ধরে ফিরিয়ে নিয়েছি।
- **PublicSiteActivity.kt** — রোগীর পর্দা, ইচ্ছাকৃতভাবে বাদ (আগের সেশন
  থেকেই এই নিয়ম, B17)।

## নিরাপত্তা — ভুল import ধরে ঠিক করা (নিজের ভুল, সৎভাবে জানানো)
`IncomeExpenseActivity.kt` ও `PartnerSharesActivity.kt`-এ `NoBengali`
ক্লাসের import ছিলই না (অন্য package-এ) — যোগ না করলে কম্পাইল হতো না,
নিজে ধরে যোগ করা হয়েছে। `PatientClinicalHistoryActivity.kt`-তেও একই।

## যাচাই
| পরীক্ষা | ফল |
|---|---|
| `tk_guard.py --release` | ✅ পাশ (একাধিকবার, প্রতিটা ফাইল-পরিবর্তনের পরে) |
| `verify_version_json.py` | ✅ V611/6.11 |
| Import সব ঠিক আছে | ✅ ([৯.১৮] গার্ড-চেকে ধরা পড়ত, তার আগেই নিজে ধরেছি) |

## সততার সাথে সীমাবদ্ধতা
এই ধরনের অডিট *কোডে যা লেখা আছে* তার উপর ভিত্তি করে — বাস্তব ফোনে
`hookApp()` কতটা নির্ভরযোগ্যভাবে সব ক্ষেত্রে কাজ করে তা আমি সরাসরি
পরীক্ষা করতে পারিনি (এই পরিবেশে Android চালানোর সুযোগ নেই)। V610/V611
ফিক্স করা জায়গাগুলো TK চাইলে Kishanganj স্টাফের ফোনে সরাসরি টেস্ট করে
নিশ্চিত হতে পারেন।
