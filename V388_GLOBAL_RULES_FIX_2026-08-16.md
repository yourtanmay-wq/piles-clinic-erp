# V388 — দুটো গ্লোবাল নিয়ম, পুরো প্রজেক্ট ধরে যাচাই ও সংশোধন

তারিখ: ১৬.০৮.২০২৬
আদেশ: **(১) "১৭ জুলাই" ক্যালেন্ডার-ইমোজি কোথাও থাকবে না — এটা আগেও গ্লোবাল রুল
হিসেবে বলা হয়েছিল। (২) একই পর্দায় দু'বার বাঁ-দিকের তীর থাকবে না।
সত্যতা যাচাই করে সম্পূর্ণ প্রজেক্টে দেখুন।**

**প্রথমেই স্বীকার করছি:** নিয়ম ১ আপনি আগে বলেছিলেন, আমি সেটা মানিনি — V387-এ
Appointment পাতায় ঐ ইমোজিটাই বসিয়েছিলাম। ভুলটা আমার। নিয়ম ২-এর ব্যাপারটাও আমি
আগে ধরিনি। দুটোই এখন পুরো প্রজেক্ট ধরে খুঁজে ঠিক করা হলো।

---

## ক) 📅 "১৭ জুলাই" ক্যালেন্ডার-ইমোজি

স্ক্রিপ্ট দিয়ে **সব ওয়েব-ফাইল** (`.js` · `.html` · `.css`) খোঁজা হয়েছে — সরাসরি
অক্ষর **এবং** `\u{1F4C5}` ধরনের লুকানো রূপ, দুটোই। সাথে 📆 আর 🗓-ও ধরা হয়েছে
(ওগুলোও একই ধরনের ক্যালেন্ডার-চিহ্ন)।

**পাওয়া গিয়েছিল ৪ জায়গায় — চারটেই সরানো হলো:**

| ফাইল | কোথায় | কী ছিল | কী হলো | কেন ঠিক |
|---|---|---|---|---|
| `app.js` | মেনু/নেভিগেশনে "Expected Tomorrow" | 📅 | **📞** | অ্যান্ড্রয়েডে ঠিক এই ফিচারটাই 📞 ব্যবহার করে — `item_chamber_header.xml:141` → `"📞 কাল আসার কথা"` |
| `app.js` | ☰ মেনুর তালিকায় একই সারি | 📅 | **📞** | একই কারণ |
| `app.js` | "Custom Date" পপ-আপের শিরোনাম | 📅 | **কিছু নয়** | শিরোনাম থেকে ইমোজি বাদ দিলে `modal()` নিজেই অ্যান্ড্রয়েডের নিয়মে (`PremiumAlert.iconFor`) **ℹ️** বসাবে — ফোনে যা হয়, হুবহু তাই |
| `app.js` | Appointment কার্ডের তারিখ-লাইন | 📅 | **⏰** | অ্যান্ড্রয়েডে ওখানে ⏰ — `AppointmentActivity.kt:295` |

`styles.css`-এ আমার লেখা একটা মন্তব্যেও চিহ্নটা ছিল — সেটাও সরানো হয়েছে।

**যাচাই (স্ক্রিপ্টে):** ওয়েবের কোনো ফাইলে 📅 / 📆 / 🗓 **একটিও নেই ✅**

> **⚠️ সৎভাবে জানাচ্ছি — অ্যান্ড্রয়েডে একটা আছে।**
> `DoctorVisitActivity.kt:4311` — `fieldLabel("📅", "Payment Date")`।
> এটা আমার বসানো নয়, আগে থেকেই ছিল। আপনি "অ্যান্ড্রয়েডে হাত দেবেন না" বলেছেন,
> তাই **আমি ছুঁইনি** — শুধু জানিয়ে রাখলাম। **বললে ওটাও বদলে দেব** (তবে তখন
> APK নতুন করে বিল্ড করতে হবে)।

---

## খ) একই পর্দায় দ্বিতীয় বাঁ-দিকের তীর

`page()` যে হেডার আঁকে তাতে **সবসময়** একটা `←` থাকে (`app.js:1265`)। কিন্তু
অনেক পাতার **ভিতরেও** আরেকটা `← Back` বসানো ছিল — আপনি ঠিকই ধরেছেন, ওটা
বেফালতু, আর অ্যান্ড্রয়েডেও নেই (ফোনে প্রতিটা পর্দায় তীর **একটাই**)।

স্ক্রিপ্ট দিয়ে সব `.js` ফাইলে বাঁ-তীরওয়ালা প্রতিটি বোতাম বের করে, **একটা একটা
করে** দেখা হয়েছে ঐ পর্দায় হেডার আছে কিনা।

**সরানো হলো ১১টি (আপনার দেওয়া Draft-এর উদাহরণসহ):**

| # | পাতা | কী ছিল | এখন |
|---|---|---|---|
| ১ | **Draft তালিকা** ← *আপনার উদাহরণ* | `← Draft` | বাদ |
| ২ | Notifications | `← Back` | বাদ (📜 Notices বোতামটা আছে) |
| ৩ | ☰ Menu | `← Back` | বাদ |
| ৪ | Patient Photo | `← Back` | বাদ |
| ৫ | Follow-up (ছাঁকনি দেওয়া তালিকা) | `← Back` | বাদ (⛃ ছাঁকনি বোতাম আছে) |
| ৬ | View All Timeline | `← Back` | বাদ (Add Remark আছে) |
| ৭ | Follow-up Calendar | `← Follow-up` | বাদ |
| ৮ | আয়-ব্যয় → Add Collection | `← Back` | বাদ (উপরের পটিতে `←` আছে) |
| ৯ | আয়-ব্যয় → Add Expense | `← Back` | বাদ (উপরের পটিতে `←` আছে) |
| ১০ | Check-up ধাপ | `◀ Back` | **`Back`** — কাজ একই, শুধু তীরটা বাদ |
| ১১ | Investigation ক্যাটাগরি | `← All Categories` | **`All Categories`** — কাজ একই, শুধু তীরটা বাদ |

⛔ **কোনো বোতামের কাজ নষ্ট করা হয়নি** — যেগুলো সরানো হয়েছে, সেগুলোর কাজ
হেডারের `←` (`goBackOnePage()`) হুবহু একইভাবে করে। ১০ ও ১১ নম্বরে কাজটা
আলাদা (ভিতরের এক ধাপ পিছনো), তাই বোতাম রেখে শুধু চিহ্নটা বাদ দেওয়া হয়েছে।

**যেগুলো রাখা হলো (যাচাই করে দেখা গেছে ঐ পর্দায় ওটাই একমাত্র তীর):**

| কোথায় | কেন রাখা |
|---|---|
| হেডারের `←` (`page()`) | এটাই আসল Back — এটাই থাকবে |
| পাবলিক ওয়েবসাইটের `←` | ঐ পাতার নিজস্ব হেডার, অ্যাপের topbar নেই |
| Patient History-র `← Back` | পুরো পাতা বদলে যায়, topbar থাকে না |
| প্রিন্ট পাতার `← Print Menu` / `← Patient` / `← Chamber` | প্রিন্ট-ভিউ পুরো পাতা দখল করে, topbar নেই |
| আয়-ব্যয়ের `←  Add Collection` / `←  Add Expense` পটি | ঐ মডিউলের topbar-এ তীর নেই, এটাই একমাত্র |
| অংশীদারের `← Back` | `partnerHead()`-এর topbar-এ তীর নেই (↻ ও Logout), এটাই একমাত্র |
| ক্যালেন্ডারের `◀` `▶` | মাস বদলানোর বোতাম — অ্যান্ড্রয়েডেও হুবহু এই দুটো (`activity_follow_calendar.xml:64-91`) |

---

## গ) ক্ষতি হয়নি — যন্ত্রে প্রমাণ (আবার চালানো হয়েছে)

* **মোবাইল ৩৯০px, ৩৭টি পর্দা, পুরনো বনাম নতুন:** ৩০টি **হুবহু এক**; বদলেছে ঠিক
  সেই ৭টিই যেখানে V387-এ কাজ হয়েছে। **অন্য কোথাও একটি পিক্সেলও নয় ✅**
* **ডেস্কটপ:** নতুন CSS-এর **প্রতিটি নিয়ম** `@media (max-width:899px)`-এর ভেতরে
  (পার্স করে গোনা) — ৯০০px+ পর্দায় পৌঁছায়ই না।
  ৩টি পর্দা ছবি-মিলানে আলাদা এসেছিল; **একই CSS দিয়ে ৪ বার এঁকে** দেখা গেল
  ওগুলো নিজেরাই প্রতিবার আলাদা আঁকে (তারিখ-বাক্স ও চলমান SVG থাকে) —
  **অর্থাৎ ব্রাউজারের অস্থিরতা, আমার CSS নয় ✅**
* **অ্যান্ড্রয়েড সোর্সে বদল: ০ ফাইল। SQL-এ বদল: ০ ফাইল।** নতুন SQL লাগবে না।
* **যান্ত্রিক অডিট (৩৬০px ও ৩৯০px):** নতুন ৮টি পাতায় পর্দা-ছাড়ানো, ছোট লেখা বা
  ছোট বোতাম — একটিও নেই।
* **সিনট্যাক্স:** `app.js` ও `finance.js` — `node --check` পাশ।

---

## ঘ) বদলানো ফাইল (মোট ৪টি)

| ফাইল | কী বদলাল |
|---|---|
| `app.js` | ৪টি 📅 সরানো · ৯টি দ্বিতীয় বাঁ-তীর সরানো |
| `finance.js` | ২টি দ্বিতীয় `← Back` সরানো |
| `styles.css` | শুধু একটা মন্তব্যে 📅 বাদ (নিয়ম অপরিবর্তিত) |
| `index.html` | ক্যাশ-নম্বর `?v=v415` (app.js · styles.css · finance.js) |

---

## ঙ) আপনার নিয়ম দুটো এখন থেকে লক

আমি এই দুটো নিয়ম **স্থায়ীভাবে** ধরে রাখলাম, পরের প্রতিটি পাতায় প্রথমেই মিলিয়ে
নেব:

1. 📅 / 📆 / 🗓 — **কোথাও নয়।** তারিখ বোঝাতে হলে অ্যান্ড্রয়েড যা ব্যবহার করে
   (⏰ / 📞 / ℹ️) ঠিক সেটাই।
2. **এক পর্দা = এক বাঁ-তীর।** হেডারেরটাই থাকবে; পাতার ভিতরে দ্বিতীয় তীর নয়।

**পরের ধাপ:** লাইভ টেস্ট করে দেখুন। ঠিক থাকলে চিকিৎসা-সংক্রান্ত ৮টি পাতা ধরব —
এবং সেখানে শুরু থেকেই এই দুটো নিয়ম মেনে চলব।
