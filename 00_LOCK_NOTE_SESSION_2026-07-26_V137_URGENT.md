# 🚨 LOCK NOTE — V137 (2026-07-26): Visit ট্যাব "Loading..." — আসল কারণ

**Base:** V136 · **ZIP:** `PILES_CLINIC_APP_V137_FINAL.zip` · **versionCode/Name:** 137 / V137

## V136-এর পর কী দাঁড়াল (TK-এর ১১:৫৬-র ছবি)
- উপরের সংখ্যা ঠিক হয়ে গেছে: **46 Enquiry / 35 Visit / 31 Patient** ✅ (আগে 0/0 ছিল)
- **Patient ট্যাব খুলছে** ✅
- **Visit ট্যাব এখনো "Loading..."** ❌

## V137-এ যা ঠিক করা হলো (একটাই ফাইল: `native/FollowUpRepository.kt`)
**আসল কারণ:** Visit ট্যাব লোড হওয়ার সময়, যেসব রোগীর `followups` সারি ক্লাউডে নেই তাদের জন্য কোড **তালিকা দেখানোর আগেই** এক এক করে ক্লাউডে সারি লিখছিল (self-heal)। Visit ট্যাবে এমন রোগী অনেক (৩৫ পর্যন্ত), তাই এক-একটা করে কয়েক ডজন নেটওয়ার্ক-লেখা শেষ না হওয়া পর্যন্ত পর্দা "Loading..." দেখাত — ভালো নেটেও কয়েক মিনিট, দুর্বল নেটে কার্যত চিরকাল।
Patient ট্যাবে এমন সারি কম, তাই ওটা খুলছিল — TK-এর ছবির সাথে হুবহু মেলে।

**সমাধান:** ওই মেরামতের কাজ এখন **ব্যাকগ্রাউন্ডে একটি আলাদা থ্রেডে** হয়। তালিকা সঙ্গে সঙ্গে দেখা যায়; মেরামত নিজে নিজে চলতে থাকে, ব্যর্থ হলে আগের মতোই retry queue-তে যায়।
**কী লেখা হচ্ছে তার কোনো পরিবর্তন নেই** — একই সারি, একই টেবিল, একই retry। শুধু অপেক্ষাটা সরানো হয়েছে।

## অন্য কিছু বদলানো হয়নি
V136/V135-এর ফিক্স, V134-এর লক করা ডিজাইন, ওয়েব অ্যাপ, ডেটাবেস, SQL, RLS — কিছুই ছোঁয়া হয়নি।

## ⚠️ NOT TESTED
Android Studio Build ও লাইভ টেস্ট এখানে করা যায়নি। TK build করে Visit ট্যাব খুলে দেখবেন।

---

**🔒 LOCK NOTE:** এই ফাইলে যা আছে তা ধ্বংস করা যাবে না, কোনো ডিজাইন TK-কে না জানিয়ে বদলানো যাবে না, কোনো working flow খারাপ করা যাবে না, অ্যাপ স্লো করা যাবে না।
