# V536 — বাকি পাঁচটা: একজনের কাজ আর অন্যজনের রেকর্ডে পড়বে না
তারিখ: ২২.০৮.২০২৬ · V535 → **V536 (5.36)**

TK: *"বাকি কাজগুলো খুব সাবধানে সততার সাথে সঠিকভাবে করুন / কোন কাজ আন্দাজে করে
সময় নষ্ট করবেন না।"*

---

## ১. পাঁচটাই হয়েছে

| # | কোথায় | আগে যা হত |
|---|---|---|
| ১ | **Chamber-এ চিকিৎসার নোট** | ওইদিনের **সব** পেমেন্ট সারিতে বসত ⇒ অন্য রোগীর Report Card-এ এঁর নোট |
| ২ | **রেজিস্ট্রেশন** | ওই নম্বরের **সব** খোলা Inquiry বন্ধ ⇒ অন্যজনেরটাও |
| ৩ | **Treatment-এ রূপান্তর** | একই — অন্যজনের Inquiry বন্ধ + "Converted" লেখা |
| ৪ | **Follow-up-এ status বদল** | একই ধাপের **সব** সারিতে বসত |
| ৫ | **Draft/Reject থেকে Restore** | **সব** follow-up সারি Active/Treatment |

---

## ২. 🔴 একটা ফাঁদ এড়ানো হয়েছে (আন্দাজে করলে ভুল হত)

V534-এর `rowBelongsTo()` **Chamber-এর নোটে খাটে না** —
ওটা সারির নিজের `id` ধরে মেলায়, কিন্তু **payment সারির `id` হলো পেমেন্টের
আইডি, রোগীর নয়**; সেখানে মালিক থাকে `patientId` ঘরে
(`ChamberAttendanceRepository.kt:897`-এ কোডে লেখা, V520-এ প্রমাণিত)।
⇒ তাই ওখানে সঠিক মিলটাই **আলাদা করে** লেখা হয়েছে, পুরোনো ফাংশন জোর করে
চাপানো হয়নি।

---

## ৩. নিয়ম — এক জায়গায়, প্রমাণ-ভিত্তিক

`PatientIdentity.provablyOtherPatient(row, mobile, myRowId, myCode, myName)`

```
১) সারিটা ঘোষিত-আলাদা কোনো রোগীর, আর সেই রোগী আমি নই   ⇒ অন্যের, ছোঁব না
২) আমি নিজেই ঘোষিত-আলাদা ⇒ refId / কোড / নাম — কোনো একটায় প্রমাণ চাই;
   প্রমাণ না মিললে ⇒ অন্যের, ছোঁব না
৩) সাধারণ রোগী + কোনো প্রমাণ নেই ⇒ **false** = হুবহু আগের আচরণ
```

⛔ **শুধু প্রমাণ থাকলেই বাদ দেয়** — আন্দাজে একটাও সারি বাদ পড়ে না।
⛔ **রোজকার ৯৯% নম্বরে একটাও সারি বাদ পড়ে না।**
⛔ Schema · RLS · data — কিছুই ছোঁয়া হয়নি। **SQL লাগে না।**
⛔ নতুন কোনো ক্লাউড-অনুরোধ নেই। বাছাইয়ের জন্য শুধু ৩টে **ছোট ঘর**
   (`refId,patientId,name`) দুটো query-তে যোগ হয়েছে — সারি এমনিতেই হাতে গোনা।

---

## ৪. যাচাই

**নতুন নিয়মটা compile করে চালানো হয়েছে** (আসল `PatientIdentity.kt`) —
**৩৭/৩৭ PASS**, তার মধ্যে V536-এর ১০টা:
সাধারণ রোগী + প্রমাণ নেই → বাদ পড়ে না · ঘোষিত-আলাদা সারি → অন্যের ·
নিজের ঘোষিত সারি → আমারই · কোড/নাম মিললে আমার, না মিললে অন্যের ·
**প্রমাণ না থাকলে ছোঁব না** · **অন্য নম্বরের `pat_` আইডি ঘোষিত ধরা হয় না** ·
null সারি নিরাপদ।

**আসল `loadBoard()` compile করে চালানো — cham ৩১/৩১ PASS।**

বাকি সব: rep **25·0** · tt **19·0** · rc **19·0** · family **29·0** ·
rot **21·0** · futab **19·0** · gtest **13·0** · ওয়েব ৮/৮ ✅
বন্ধনীর হিসাব ছয় ফাইলেই `git HEAD`-এর সাথে **হুবহু এক**।

---

## ৫. ⚠️ সৎ সীমা (লুকাচ্ছি না)

**Inquiry সারিতে (রেজিস্ট্রেশনের আগে) প্রায়ই `refId` ফাঁকা থাকে** — তখন ওই
Inquiry কার, তা **জানার কোনো উপায় নেই**। এমন সারিতে আচরণ **আগের মতোই**
থাকে (বন্ধ হয়)। জোর করে অনুমান করা হয়নি — সেটাই বেশি বিপজ্জনক হত।
⇒ ঢাকা পড়ে তখনই, যখন সারিটা **প্রমাণ বহন করে**।

⚠️ Android SDK এখানে নামে না ⇒ `ChamberAttendanceActivity` compile করা যায়নি
(বাকি সব Repository-তে, সেগুলো সত্যিই চালানো হয়েছে)।

---

## ৬. TK-এর করণীয়
1. **V536 বিল্ড** (নতুন কোনো SQL নেই)
2. এক নম্বরে দু'জন রেখে — একজনকে **রেজিস্টার/Treatment** করুন → অন্যজনের
   Inquiry খোলা আছে কিনা · Chamber-এ নোট লিখুন → অন্যজনের Report Card-এ
   ওঠে কিনা
3. 🔴 **সাধারণ (একজন রোগীর) নম্বরে সব আগের মতোই আছে কিনা** — সবচেয়ে জরুরি
