# V396 — সৎ অবস্থা-জানানো (১৬.০৮.২০২৬)

## ✅ এই ফাইলে যা **সত্যিই বসানো ও যাচাই করা** আছে

| # | কাজ | অবস্থা |
|---|---|---|
| ১ | Diet Chart — নতুন A4 ডিজাইন, **Android + Web দুটোতেই** | ✅ বসানো |
| ২ | গ্লোবাল রুল ১ — `TK BISWAS / Founder & Consultant` (১১ জায়গা) | ✅ বসানো |
| ৩ | গ্লোবাল রুল ২ — `Dr. K.H MANDAL / (B.A.M.S) Regd 12386` (১০ জায়গা) | ✅ বসানো |
| ৪ | প্রেসক্রিপশনের **কাটা-পড়া বাড়তি Rx** সরানো | ✅ বসানো |
| ৫ | প্রেসক্রিপশনে **একটাই বড় হালকা জলছাপ** (আগে দুটো ছিল, একটা ছোট ও একদিকে সরানো) | ✅ বসানো |
| ৬ | রোজকার ৮ পাতা + চিকিৎসার ৬ পাতা — মোবাইল ভিউ অ্যান্ড্রয়েডের মতো | ✅ বসানো |

**যাচাই:** সব `.js` ফাইলে `node --check` পাশ · `styles.css`-এর ব্রেস ভারসাম্য ০ ·
SQL-এ ০ বদল · অ্যান্ড্রয়েডে আজ ৪টি ফাইল (Diet Chart ২টি + দুই গ্লোবাল রুলের ২টি)।

---

## ⛔ যা **এখনো বসানো হয়নি** — সৎভাবে জানাচ্ছি

আপনার অনুমোদিত **নতুন প্রেসক্রিপশন লেআউট** এখনো প্রজেক্টে **বসেনি**। ওটা এতক্ষণ
শুধু **প্রুফ-রেন্ডারে** ছিল (আসল CSS + আসল কোড দিয়ে আঁকা, কিন্তু প্রজেক্টের
ফাইলে লেখা হয়নি)। যেগুলো বাকি:

1. রোগীর ঘর — অ্যান্ড্রয়েডের দু'কলাম ফরম্যাট (Name/Age/ID/Mobile ‖ Date/Sex/Diseases/Address)
2. Disease & Complaint ঘরের বাঁ ও উপরের দাগ সরানো, ডানে পাতলা খাড়া দাগ
3. ওষুধের তালিকা ℞-এর ঠিক নিচে তোলা
4. ADVICE · DIET · NEXT FOLLOW-UP DATE — বাঁ ঘরের তলায়
5. সই-সারি পাতার তলা থেকে ৭ মিমি-তে ফিক্স, তিনটে এক সমান্তরালে
6. বারকোড ছোট, নিচে ছোট করে "Document Digitally Verified", পেশেন্ট ID বাদ
7. নিচের পটিতে — *In an emergency, visit your nearest hospital immediately*
8. **Medicine Slip-এ হুবহু একই ডিজাইন** (এখন ওটা সাদামাটা টেবিল)
9. **Android-এ একই ডিজাইন** (Diet Chart-এর মতো HTML-print পথে)

### কেন এখনই বসালাম না

এই কাজটা ছোট নয় — ওয়েবের `printSimple` / `rxPrintPatientHtml` / `printFoot` /
`printHead` চারটেই বদলাতে হবে, দুটো পর্দার (Prescription ও Medicine Slip) গঠন
এক করতে হবে, তারপর অ্যান্ড্রয়েডে নতুন একটা HTML-print ফাইল লিখতে হবে — এবং
**প্রতিটা ধাপে মেপে যাচাই** করতে হবে যাতে অন্য প্রিন্ট (Registration, Blood Test,
Payment Receipt, Chamber Register) না ভাঙে।

আপনি বলেছেন **"যাচাই করে তবেই ফাইল পাঠাবেন"**। এই সেশনে অতটা জায়গা আর নেই।
তাড়াহুড়ো করে অর্ধেক বসিয়ে দিলে প্রিন্ট ভেঙে যেতে পারত — সেটাই আপনি বারবার
ঠিকই আপত্তি করেছেন। তাই **অর্ধেক কাজ করে পাঠাইনি**।

### পরের সেশনে ঠিক কী করতে হবে

আমার তৈরি করা প্রুফের সব CSS ও গঠন `/tmp/probe/rx_proof.js`-এ ধাপে ধাপে লেখা
আছে — ওখান থেকে হুবহু তুলে বসালেই হবে। ক্রম:
`printHead`-এ দুই পর্দার জন্য একটাই ক্লাস → CSS একবার → `rxPrintPatientHtml`
→ `printSimple`-এ Medicine Slip-ও একই গঠনে → `printFoot` → Android HTML-print।

**এই ফাইলটা নিরাপদ ও চালু** — উপরের ✅ তালিকার সব কাজ এতে আছে ও যাচাই করা।
